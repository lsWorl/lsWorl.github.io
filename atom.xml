<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>LsWorld</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://lsworl.github.io/</id>
  <link href="https://lsworl.github.io/" rel="alternate"/>
  <link href="https://lsworl.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, LsWorld</rights>
  <subtitle>机器学习与代码笔记</subtitle>
  <title>LsWorld</title>
  <updated>2026-09-03T16:46:30.000Z</updated>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Hugging Face" scheme="https://lsworl.github.io/tags/Hugging-Face/"/>
    <category term="LLM" scheme="https://lsworl.github.io/tags/LLM/"/>
    <category term="SFT" scheme="https://lsworl.github.io/tags/SFT/"/>
    <category term="TRL" scheme="https://lsworl.github.io/tags/TRL/"/>
    <category term="PEFT" scheme="https://lsworl.github.io/tags/PEFT/"/>
    <category term="LoRA" scheme="https://lsworl.github.io/tags/LoRA/"/>
    <category term="QLoRA" scheme="https://lsworl.github.io/tags/QLoRA/"/>
    <category term="bitsandbytes" scheme="https://lsworl.github.io/tags/bitsandbytes/"/>
    <category term="模型量化" scheme="https://lsworl.github.io/tags/%E6%A8%A1%E5%9E%8B%E9%87%8F%E5%8C%96/"/>
    <content>
      <![CDATA[<p>在上一篇 <a href="https://lsworl.github.io/2026/08/24/nlp-sft-trl-lora/">SFT（三）：使用 TRL SFTTrainer 与 LoRA 微调对话模型</a> 中，我已经知道：</p><ul><li>Base Model 的参数保持冻结。</li><li>LoRA 在线性层旁边增加两个低秩矩阵 $A$ 和 $B$。</li><li>反向传播只更新 LoRA Adapter。</li><li>训练后保存的是 Adapter，而不是另一份完整 Base Model。</li></ul><p>LoRA 已经大幅减少了<strong>可训练参数、梯度和优化器状态</strong>，但还有一个问题：</p><blockquote><p>冻结不等于不占显存。Base Model 虽然不更新，前向传播时仍然要放在设备上。</p></blockquote><p>如果一个 7B 模型以 BF16 保存，仅权重理论上就约占：</p><p>$$<br>7 \times 10^9 \times 2\text{ Byte}<br>\approx 14\text{ GB}<br>$$</p><p>再加上激活值、LoRA 参数、梯度、优化器状态和 CUDA 临时工作区，普通 LoRA 仍可能超出消费级显卡的容量。</p><p>QLoRA 解决的正是这部分问题：<strong>把冻结的 Base Model 以 4-bit 保存，同时继续用较高精度训练 LoRA Adapter。</strong></p><span id="more"></span><p><img src="/images/nlp-blog/qlora-overview.svg" alt="QLoRA整体训练流程"></p><h2 id="1-QLoRA-到底是什么"><a href="#1-QLoRA-到底是什么" class="headerlink" title="1. QLoRA 到底是什么"></a>1. QLoRA 到底是什么</h2><p>QLoRA 的全称是 <strong>Quantized Low-Rank Adaptation</strong>。</p><p>它不是一个替代 SFT 的新损失函数，也不是新的 Transformer 架构，而是一种更节省显存的参数高效微调方式：</p><pre class="language-text" data-language="text"><code class="language-text">SFT 数据与 Loss        +4-bit 冻结 Base Model        +高精度可训练 LoRA Adapter        =QLoRA SFT</code></pre><p>论文中的核心描述可以压缩成一句话：</p><blockquote><p>梯度穿过冻结的 4-bit 量化模型，但只流入 LoRA Adapter 并更新其参数。</p></blockquote><p>这里有两个关键词：</p><ol><li><strong>穿过</strong>量化 Base Model。</li><li>不<strong>更新</strong>量化 Base Model。</li></ol><p>Base Model 仍然参与前向计算和梯度链式法则，只是其参数 <code>requires_grad=False</code>。</p><h2 id="2-QLoRA-与-SFT、LoRA、量化是什么关系"><a href="#2-QLoRA-与-SFT、LoRA、量化是什么关系" class="headerlink" title="2. QLoRA 与 SFT、LoRA、量化是什么关系"></a>2. QLoRA 与 SFT、LoRA、量化是什么关系</h2><p>这几个术语处在不同层次。</p><table><thead><tr><th>名称</th><th>回答的问题</th></tr></thead><tbody><tr><td>SFT</td><td>使用什么数据和监督信号训练</td></tr><tr><td>LoRA</td><td>更新哪些参数，如何低秩参数化</td></tr><tr><td>4-bit Quantization</td><td>Base Model 权重如何压缩存储</td></tr><tr><td>QLoRA</td><td>如何组合 4-bit Base Model 与 LoRA 进行训练</td></tr></tbody></table><p>因此下面的说法更准确：</p><pre class="language-text" data-language="text"><code class="language-text">我使用 Assistant-only SFT 作为训练目标，并使用 QLoRA 作为参数与显存高效的训练方式。</code></pre><p>只说“我用了 QLoRA”，并没有说明：</p><ul><li>数据是什么。</li><li>Loss 计算哪些 token。</li><li>Chat Template 是什么。</li><li>训练了多少步。</li><li>最终模型质量如何。</li></ul><h2 id="3-Full-Fine-tuning、LoRA-与-QLoRA-对比"><a href="#3-Full-Fine-tuning、LoRA-与-QLoRA-对比" class="headerlink" title="3. Full Fine-tuning、LoRA 与 QLoRA 对比"></a>3. Full Fine-tuning、LoRA 与 QLoRA 对比</h2><p><img src="/images/nlp-blog/qlora-vs-lora-memory.svg" alt="Full Fine-tuning、LoRA与QLoRA对比"></p><h3 id="3-1-Full-Fine-tuning"><a href="#3-1-Full-Fine-tuning" class="headerlink" title="3.1 Full Fine-tuning"></a>3.1 Full Fine-tuning</h3><pre class="language-text" data-language="text"><code class="language-text">Base Model：BF16 / FP16，可训练梯度：所有参数优化器状态：所有参数</code></pre><p>它更新能力最完整，但训练显存通常最高。</p><h3 id="3-2-LoRA"><a href="#3-2-LoRA" class="headerlink" title="3.2 LoRA"></a>3.2 LoRA</h3><pre class="language-text" data-language="text"><code class="language-text">Base Model：通常 BF16 / FP16，冻结LoRA：BF16 / FP32，可训练梯度与优化器状态：仅 LoRA</code></pre><p>它省掉了绝大部分梯度和优化器状态，却仍然要保存一份 16-bit Base Model。</p><h3 id="3-3-QLoRA"><a href="#3-3-QLoRA" class="headerlink" title="3.3 QLoRA"></a>3.3 QLoRA</h3><pre class="language-text" data-language="text"><code class="language-text">Base Model：4-bit，冻结LoRA：较高精度，可训练梯度与优化器状态：仅 LoRA</code></pre><p>它在 LoRA 的基础上，继续压缩占显存大头的冻结权重。</p><h2 id="4-“4-bit-训练”是一个容易误导的简称"><a href="#4-“4-bit-训练”是一个容易误导的简称" class="headerlink" title="4. “4-bit 训练”是一个容易误导的简称"></a>4. “4-bit 训练”是一个容易误导的简称</h2><p>QLoRA 常被简称为“用 4-bit 训练模型”，但实际并不是所有张量都只有 4 bit。</p><p>至少要区分四种精度：</p><table><thead><tr><th>对象</th><th>常见精度</th><th>是否训练</th></tr></thead><tbody><tr><td>Base Model 权重存储</td><td>NF4 4-bit</td><td>否</td></tr><tr><td>矩阵乘法计算</td><td>BF16 或 FP16</td><td>参与计算</td></tr><tr><td>LoRA 参数</td><td>BF16 &#x2F; FP32</td><td>是</td></tr><tr><td>LoRA 优化器状态</td><td>8-bit 或 32-bit</td><td>随优化器而定</td></tr></tbody></table><p>因此更准确的描述是：</p><blockquote><p>Base Model 权重以 4-bit 形式存储，使用时分块反量化到计算 dtype；LoRA 分支保持可训练的浮点参数。</p></blockquote><p>QLoRA 不是在一个只能表示 16 个值的空间里直接更新全部模型权重。</p><h2 id="5-为什么冻结的-Base-Model-仍然消耗大量显存"><a href="#5-为什么冻结的-Base-Model-仍然消耗大量显存" class="headerlink" title="5. 为什么冻结的 Base Model 仍然消耗大量显存"></a>5. 为什么冻结的 Base Model 仍然消耗大量显存</h2><p>以 $N$ 个参数为例，只看权重存储的理论下限：</p><p>$$<br>M_{\text{BF16}} &#x3D; 2N\text{ Byte}<br>$$</p><p>$$<br>M_{\text{INT8}} &#x3D; 1N\text{ Byte}<br>$$</p><p>$$<br>M_{\text{4bit}} &#x3D; 0.5N\text{ Byte}<br>$$</p><p>对 7B 模型：</p><table><thead><tr><th>存储形式</th><th align="right">纯权重理论值</th></tr></thead><tbody><tr><td>BF16 &#x2F; FP16</td><td align="right">约 14 GB</td></tr><tr><td>INT8</td><td align="right">约 7 GB</td></tr><tr><td>4-bit</td><td align="right">约 3.5 GB</td></tr></tbody></table><p>实际显存会更高，因为还有：</p><ul><li>每个量化块的 scale 和量化元数据。</li><li>激活值。</li><li>LoRA 参数、梯度和优化器状态。</li><li>Attention 与 Loss 的中间张量。</li><li>内存碎片和 CUDA 工作区。</li></ul><p>所以“7B 的 4-bit 权重约 3.5 GB”只是理解数量级，不能直接当成训练所需显存。</p><h2 id="6-先理解最普通的分块量化"><a href="#6-先理解最普通的分块量化" class="headerlink" title="6. 先理解最普通的分块量化"></a>6. 先理解最普通的分块量化</h2><p>NF4 不是普通的均匀 INT4，但先理解分块量化会更容易进入 QLoRA。</p><p>假设一个权重块为：</p><p>$$<br>W&#x3D;[-1.20,-0.40,0.10,0.85]<br>$$</p><p>为了演示，先使用有符号 4-bit 均匀量化，整数范围取 $[-7,7]$。</p><h3 id="6-1-计算缩放因子"><a href="#6-1-计算缩放因子" class="headerlink" title="6.1 计算缩放因子"></a>6.1 计算缩放因子</h3><p>$$<br>s&#x3D;\frac{\max(|W|)}{7}&#x3D;\frac{1.20}{7}\approx0.1714<br>$$</p><h3 id="6-2-把浮点权重映射为整数编号"><a href="#6-2-把浮点权重映射为整数编号" class="headerlink" title="6.2 把浮点权重映射为整数编号"></a>6.2 把浮点权重映射为整数编号</h3><p>$$<br>q_i&#x3D;\operatorname{clip}\left(\operatorname{round}\left(\frac{w_i}{s}\right),-7,7\right)<br>$$</p><p>得到：</p><p>$$<br>q&#x3D;[-7,-2,1,5]<br>$$</p><h3 id="6-3-反量化得到近似权重"><a href="#6-3-反量化得到近似权重" class="headerlink" title="6.3 反量化得到近似权重"></a>6.3 反量化得到近似权重</h3><p>$$<br>\hat w_i&#x3D;sq_i<br>$$</p><p>$$<br>\hat W\approx[-1.200,-0.343,0.171,0.857]<br>$$</p><p>误差为：</p><p>$$<br>E&#x3D;W-\hat W\approx[0,-0.057,-0.071,-0.007]<br>$$</p><p><img src="/images/nlp-blog/qlora-block-quantization.svg" alt="分块量化与反量化计算"></p><p>这个例子揭示了三个事实：</p><ol><li>4 bit 保存的是每个权重的<strong>编号</strong>。</li><li>想恢复近似浮点值，还需要保存 scale。</li><li>量化是有损的，$W$ 与 $\hat W$ 不完全相等。</li></ol><p>注意：这个例子用于讲清基本过程，<strong>并不是 NF4 的实际编码表</strong>。</p><h2 id="7-为什么要分块，而不是整层只用一个-scale"><a href="#7-为什么要分块，而不是整层只用一个-scale" class="headerlink" title="7. 为什么要分块，而不是整层只用一个 scale"></a>7. 为什么要分块，而不是整层只用一个 scale</h2><p>如果一个线性层中既有很小的权重，又有少量极端大值，整层共用一个 scale 会导致小权重挤在很少的量化档位中。</p><p>分块后，每一小组权重拥有自己的缩放统计量：</p><pre class="language-text" data-language="text"><code class="language-text">block 0: 64 个权重 -> scale_0block 1: 64 个权重 -> scale_1block 2: 64 个权重 -> scale_2...</code></pre><p>这样通常能降低量化误差，但也需要额外保存更多 scale。</p><p>这正好引出 Double Quantization：scale 本身也会占空间。</p><h2 id="8-NF4-是什么"><a href="#8-NF4-是什么" class="headerlink" title="8. NF4 是什么"></a>8. NF4 是什么</h2><p>NF4 是 QLoRA 论文提出的 <strong>4-bit NormalFloat</strong> 数据类型。</p><p>预训练神经网络的权重通常近似集中在 0 附近，并向两侧逐渐变少。若 16 个表示值等距离分布，会把相同数量的档位分给稀疏尾部和密集中心区域。</p><p>NF4 的思路是：</p><blockquote><p>根据近似正态分布设计非均匀量化档位，使每个区间承载更接近的概率质量。</p></blockquote><p><img src="/images/nlp-blog/qlora-nf4-distribution.svg" alt="NF4非均匀量化直觉"></p><p>因此 NF4 的档位在 0 附近更密，在分布尾部更疏。</p><p>在 Hugging Face 中对应：</p><pre class="language-python" data-language="python"><code class="language-python">bnb_4bit_quant_type<span class="token operator">=</span><span class="token string">"nf4"</span></code></pre><p>官方文档建议在训练 4-bit Base Model 时使用 NF4。</p><h2 id="9-NF4-与-FP4-不要混为一谈"><a href="#9-NF4-与-FP4-不要混为一谈" class="headerlink" title="9. NF4 与 FP4 不要混为一谈"></a>9. NF4 与 FP4 不要混为一谈</h2><p><code>bitsandbytes</code> 常见选项包括：</p><pre class="language-python" data-language="python"><code class="language-python">bnb_4bit_quant_type<span class="token operator">=</span><span class="token string">"nf4"</span></code></pre><p>或：</p><pre class="language-python" data-language="python"><code class="language-python">bnb_4bit_quant_type<span class="token operator">=</span><span class="token string">"fp4"</span></code></pre><p>两者都使用 4-bit 编码，但量化表示方式不同。</p><p>对于 QLoRA 训练，通常从 NF4 开始，因为它针对近似正态分布的权重设计。不要因为名字中都有“4”就认为两者完全相同。</p><h2 id="10-Double-Quantization-是什么"><a href="#10-Double-Quantization-是什么" class="headerlink" title="10. Double Quantization 是什么"></a>10. Double Quantization 是什么</h2><p>分块量化需要给每个 block 保存一个 scale。如果模型很大，scale 的数量也很多。</p><p>Double Quantization 又称 Nested Quantization，其思想是：</p><pre class="language-text" data-language="text"><code class="language-text">第一次量化：量化 Base Model 权重第二次量化：继续量化第一次量化所需的 scale / constant</code></pre><p><img src="/images/nlp-blog/qlora-double-quantization.svg" alt="Double Quantization流程"></p><p>在 Hugging Face 中开启：</p><pre class="language-python" data-language="python"><code class="language-python">bnb_4bit_use_double_quant<span class="token operator">=</span><span class="token boolean">True</span></code></pre><p>当前 Transformers 文档给出的说明是，它还能额外节省约 <code>0.4 bit / parameter</code>。</p><p>需要注意：</p><ul><li>它不是“把 4 bit 再除以 2，变成 2 bit 权重”。</li><li>第二次量化针对的是量化常数，而不是重新量化每个原始权重。</li><li>这会引入额外元数据与反量化步骤，但换来更低的平均存储开销。</li></ul><h2 id="11-QLoRA-前向传播究竟怎样计算"><a href="#11-QLoRA-前向传播究竟怎样计算" class="headerlink" title="11. QLoRA 前向传播究竟怎样计算"></a>11. QLoRA 前向传播究竟怎样计算</h2><p>对一个加入 LoRA 的线性层，普通 LoRA 可以写成：</p><p>$$<br>y&#x3D;xW^T+\frac{\alpha}{r}xA^TB^T<br>$$</p><p>QLoRA 中保存的 Base Model 权重是量化后的 $W_q$ 和量化状态 $S$。计算时先近似反量化：</p><p>$$<br>\hat W&#x3D;\operatorname{dequant}(W_q,S)<br>$$</p><p>再执行：</p><p>$$<br>y&#x3D;x\hat W^T+\frac{\alpha}{r}xA^TB^T<br>$$</p><p>这里：</p><ul><li>$W_q$：4-bit 存储的 Base Model 权重。</li><li>$S$：scale 等量化状态。</li><li>$\hat W$：用于本次计算的近似浮点权重。</li><li>$A,B$：可训练 LoRA 参数。</li></ul><p><img src="/images/nlp-blog/qlora-forward-backward.svg" alt="QLoRA前向与反向传播"></p><h2 id="12-反向传播会不会更新-4-bit-权重"><a href="#12-反向传播会不会更新-4-bit-权重" class="headerlink" title="12. 反向传播会不会更新 4-bit 权重"></a>12. 反向传播会不会更新 4-bit 权重</h2><p>不会。</p><p>设损失为 $\mathcal L$，QLoRA 的训练目标是计算：</p><p>$$<br>\frac{\partial \mathcal L}{\partial A},\qquad<br>\frac{\partial \mathcal L}{\partial B}<br>$$</p><p>但不对 Base Model 执行参数更新：</p><p>$$<br>\frac{\partial \mathcal L}{\partial W_q}<br>\quad\text{不用于更新 }W_q<br>$$</p><p>梯度仍要经过 Base Model 的运算回传到较早位置的 LoRA 分支，所以“冻结”不能理解为“反向传播绕过整个 Base Model”。</p><p>这也是为什么 QLoRA 仍然需要激活值，且序列长度会显著影响显存。</p><h2 id="13-LoRA-参数为什么不能也简单存成-4-bit-训练"><a href="#13-LoRA-参数为什么不能也简单存成-4-bit-训练" class="headerlink" title="13. LoRA 参数为什么不能也简单存成 4 bit 训练"></a>13. LoRA 参数为什么不能也简单存成 4 bit 训练</h2><p>训练参数需要表示细小的连续更新。</p><p>如果直接用极低精度保存和更新 LoRA 参数：</p><ul><li>小梯度可能舍入为 0。</li><li>更新噪声可能变大。</li><li>优化过程更容易不稳定。</li></ul><p>QLoRA 的关键平衡是：</p><pre class="language-text" data-language="text"><code class="language-text">占绝大多数、又被冻结的 Base 权重 -> 4-bit数量较少、需要学习的 LoRA 参数       -> 较高精度</code></pre><p>因此它同时获得较低存储开销与可训练性。</p><h2 id="14-Paged-Optimizer-解决什么问题"><a href="#14-Paged-Optimizer-解决什么问题" class="headerlink" title="14. Paged Optimizer 解决什么问题"></a>14. Paged Optimizer 解决什么问题</h2><p>训练显存并不是恒定的。</p><p>某些 step 可能因为长序列、临时张量或梯度检查点重计算产生显存峰值。QLoRA 论文使用 Paged Optimizer，借助统一内存分页思想在显存压力突然升高时转移部分优化器数据，降低 OOM 峰值风险。</p><p>在 Transformers 中可以配置：</p><pre class="language-python" data-language="python"><code class="language-python">optim<span class="token operator">=</span><span class="token string">"paged_adamw_8bit"</span></code></pre><p>它同时表达两件事：</p><ul><li><code>paged</code>：支持分页管理。</li><li><code>8bit</code>：以 8-bit 形式保存大部分 AdamW 优化器状态。</li></ul><p>不过 QLoRA 中优化器只管理 LoRA 等可训练参数，因此它节省的不是 4-bit Base Model 的优化器状态，因为 Base Model 原本就没有优化器状态。</p><h2 id="15-QLoRA-到底省了哪些显存"><a href="#15-QLoRA-到底省了哪些显存" class="headerlink" title="15. QLoRA 到底省了哪些显存"></a>15. QLoRA 到底省了哪些显存</h2><p><img src="/images/nlp-blog/qlora-memory-anatomy.svg" alt="QLoRA训练显存组成"></p><p>QLoRA 主要降低：</p><ol><li><strong>Base Model 权重存储</strong>：16-bit 降到约 4-bit 加元数据。</li><li><strong>LoRA 优化器状态</strong>：可选择 8-bit Optimizer。</li><li><strong>显存峰值风险</strong>：Paged Optimizer 可缓解部分瞬时峰值。</li></ol><p>QLoRA 没有自动消除：</p><ul><li>激活值。</li><li>Attention 中间张量。</li><li>Logits 和 Loss 中间张量。</li><li>LoRA 梯度。</li><li>数据 batch。</li><li>CUDA 上下文与工作区。</li></ul><p>所以长度从 512 增加到 4096 时，即使 Base Model 仍是 4-bit，训练仍可能 OOM。</p><h2 id="16-QLoRA-不是把磁盘文件简单压缩成四分之一"><a href="#16-QLoRA-不是把磁盘文件简单压缩成四分之一" class="headerlink" title="16. QLoRA 不是把磁盘文件简单压缩成四分之一"></a>16. QLoRA 不是把磁盘文件简单压缩成四分之一</h2><p>量化模型的实际大小不严格等于 BF16 的四分之一，原因包括：</p><ul><li>分块 scale。</li><li>Double Quantization 状态。</li><li>未量化模块。</li><li>Token Embedding 或输出层的处理差异。</li><li>配置和序列化元数据。</li></ul><p>同样，“显存降为四分之一”也不是稳定承诺，因为激活值等部分不会按权重位宽同比下降。</p><h2 id="17-在-Hugging-Face-中需要哪些组件"><a href="#17-在-Hugging-Face-中需要哪些组件" class="headerlink" title="17. 在 Hugging Face 中需要哪些组件"></a>17. 在 Hugging Face 中需要哪些组件</h2><p><img src="/images/nlp-blog/qlora-hugging-face-pipeline.svg" alt="Hugging Face实现QLoRA的组件关系"></p><p>本篇使用：</p><table><thead><tr><th>库</th><th>职责</th></tr></thead><tbody><tr><td>Transformers</td><td>Tokenizer、Base Model、<code>BitsAndBytesConfig</code>、训练参数</td></tr><tr><td>bitsandbytes</td><td>NF4 线性层、4-bit 量化、分页&#x2F;8-bit Optimizer</td></tr><tr><td>PEFT</td><td><code>LoraConfig</code>、Adapter 保存加载</td></tr><tr><td>TRL</td><td><code>SFTTrainer</code>、对话 SFT 与 Assistant-only Loss</td></tr><tr><td>Datasets</td><td>构建训练集和验证集</td></tr></tbody></table><p>数据与 Loss 仍沿用 SFT 系列已经学过的逻辑。</p><p>QLoRA 只改变参数加载与更新方式，不会替我修复错误的 Chat Template 或标签范围。</p><h2 id="18-安装依赖与记录环境"><a href="#18-安装依赖与记录环境" class="headerlink" title="18. 安装依赖与记录环境"></a>18. 安装依赖与记录环境</h2><pre class="language-bash" data-language="bash"><code class="language-bash">pip <span class="token function">install</span> <span class="token parameter variable">-U</span> torch transformers datasets accelerate trl peft bitsandbytes</code></pre><p>记录版本：</p><pre class="language-bash" data-language="bash"><code class="language-bash">python <span class="token parameter variable">-c</span> <span class="token string">"import torch, transformers, datasets, trl, peft, bitsandbytes; print(torch.__version__); print(transformers.__version__); print(datasets.__version__); print(trl.__version__); print(peft.__version__); print(bitsandbytes.__version__)"</span></code></pre><p><code>transformers</code>、<code>trl</code>、<code>peft</code> 和 <code>bitsandbytes</code> 都会持续更新。文章中的代码按当前官方接口编写，真实项目应锁定经过验证的一组版本。</p><h2 id="19-先检查硬件与-BF16-支持"><a href="#19-先检查硬件与-BF16-支持" class="headerlink" title="19. 先检查硬件与 BF16 支持"></a>19. 先检查硬件与 BF16 支持</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"CUDA:"</span><span class="token punctuation">,</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_available<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">if</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_available<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"GPU:"</span><span class="token punctuation">,</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>get_device_name<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"BF16:"</span><span class="token punctuation">,</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_bf16_supported<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>本篇完整示例为了让行为可预测，要求 CUDA GPU。</p><p>当前 bitsandbytes 已在扩展 NVIDIA CUDA 之外的后端支持，但不同平台、版本和算子的成熟度会变化。运行前应检查最新硬件兼容表，而不是照搬多年前“只支持某个平台”的结论。</p><h2 id="20-选择计算-dtype"><a href="#20-选择计算-dtype" class="headerlink" title="20. 选择计算 dtype"></a>20. 选择计算 dtype</h2><pre class="language-python" data-language="python"><code class="language-python">compute_dtype <span class="token operator">=</span> <span class="token punctuation">(</span>    torch<span class="token punctuation">.</span>bfloat16    <span class="token keyword">if</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_bf16_supported<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">else</span> torch<span class="token punctuation">.</span>float16<span class="token punctuation">)</span></code></pre><p>这里选择的是<strong>计算 dtype</strong>，不是 Base Model 的权重存储 dtype。</p><p>BF16 与 FP16 都占 16 bit，但数值范围不同。支持 BF16 的 GPU 上通常优先使用 BF16；旧设备则回退到 FP16。</p><p>不要在不支持 BF16 的硬件上只改：</p><pre class="language-python" data-language="python"><code class="language-python">bf16<span class="token operator">=</span><span class="token boolean">True</span></code></pre><p>配置必须与设备能力一致。</p><h2 id="21-配置-BitsAndBytesConfig"><a href="#21-配置-BitsAndBytesConfig" class="headerlink" title="21. 配置 BitsAndBytesConfig"></a>21. 配置 BitsAndBytesConfig</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> BitsAndBytesConfigquantization_config <span class="token operator">=</span> BitsAndBytesConfig<span class="token punctuation">(</span>    load_in_4bit<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    bnb_4bit_quant_type<span class="token operator">=</span><span class="token string">"nf4"</span><span class="token punctuation">,</span>    bnb_4bit_use_double_quant<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    bnb_4bit_compute_dtype<span class="token operator">=</span>compute_dtype<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>逐项理解：</p><h3 id="21-1-load-in-4bit-True"><a href="#21-1-load-in-4bit-True" class="headerlink" title="21.1 load_in_4bit=True"></a>21.1 <code>load_in_4bit=True</code></h3><p>加载模型时将支持的线性层替换为 4-bit 量化实现。</p><p>它不是在训练结束后才压缩模型。</p><h3 id="21-2-bnb-4bit-quant-type-quot-nf4-quot"><a href="#21-2-bnb-4bit-quant-type-quot-nf4-quot" class="headerlink" title="21.2 bnb_4bit_quant_type=&quot;nf4&quot;"></a>21.2 <code>bnb_4bit_quant_type=&quot;nf4&quot;</code></h3><p>指定使用 NF4 编码 Base Model 权重。</p><h3 id="21-3-bnb-4bit-use-double-quant-True"><a href="#21-3-bnb-4bit-use-double-quant-True" class="headerlink" title="21.3 bnb_4bit_use_double_quant=True"></a>21.3 <code>bnb_4bit_use_double_quant=True</code></h3><p>继续压缩第一次量化产生的量化常数。</p><h3 id="21-4-bnb-4bit-compute-dtype-compute-dtype"><a href="#21-4-bnb-4bit-compute-dtype-compute-dtype" class="headerlink" title="21.4 bnb_4bit_compute_dtype=compute_dtype"></a>21.4 <code>bnb_4bit_compute_dtype=compute_dtype</code></h3><p>指定矩阵乘法等运算使用 BF16 或 FP16。</p><p>再次强调：</p><pre class="language-text" data-language="text"><code class="language-text">storage dtype != compute dtype</code></pre><h2 id="22-配置-LoRA"><a href="#22-配置-LoRA" class="headerlink" title="22. 配置 LoRA"></a>22. 配置 LoRA</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> peft <span class="token keyword">import</span> LoraConfigpeft_config <span class="token operator">=</span> LoraConfig<span class="token punctuation">(</span>    task_type<span class="token operator">=</span><span class="token string">"CAUSAL_LM"</span><span class="token punctuation">,</span>    r<span class="token operator">=</span><span class="token number">16</span><span class="token punctuation">,</span>    lora_alpha<span class="token operator">=</span><span class="token number">32</span><span class="token punctuation">,</span>    lora_dropout<span class="token operator">=</span><span class="token number">0.05</span><span class="token punctuation">,</span>    bias<span class="token operator">=</span><span class="token string">"none"</span><span class="token punctuation">,</span>    target_modules<span class="token operator">=</span><span class="token string">"all-linear"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>上一篇已经推导过 LoRA：</p><p>$$<br>W’&#x3D;W+\Delta W,<br>\qquad<br>\Delta W&#x3D;\frac{\alpha}{r}BA<br>$$</p><p>QLoRA 没有改变这条公式，只是 $W$ 来自量化 Base Model 的近似反量化结果。</p><h3 id="22-1-为什么使用-all-linear"><a href="#22-1-为什么使用-all-linear" class="headerlink" title="22.1 为什么使用 all-linear"></a>22.1 为什么使用 <code>all-linear</code></h3><p><code>target_modules=&quot;all-linear&quot;</code> 让 PEFT 尽量把 LoRA 加入模型的线性层，避免为每一种架构手写 <code>q_proj</code>、<code>v_proj</code> 等名称。</p><p>它不是永远最优的超参数，但适合构建结构无关、容易检查的 QLoRA 基线。训练后仍应打印实际命中的模块。</p><h2 id="23-当前-TRL-可以直接组合量化与-PEFT"><a href="#23-当前-TRL-可以直接组合量化与-PEFT" class="headerlink" title="23. 当前 TRL 可以直接组合量化与 PEFT"></a>23. 当前 TRL 可以直接组合量化与 PEFT</h2><p>当前 <code>SFTTrainer</code> 支持同时接收：</p><pre class="language-python" data-language="python"><code class="language-python">quantization_config<span class="token operator">=</span>quantization_configpeft_config<span class="token operator">=</span>peft_config</code></pre><p>当 <code>model</code> 传入模型仓库 ID 时，Trainer 可以完成：</p><pre class="language-text" data-language="text"><code class="language-text">根据 model id 加载 Causal LM-> 以 BitsAndBytesConfig 量化-> 为 k-bit 训练做准备-> 根据 LoraConfig 包装 PEFT Model-> 执行 SFT</code></pre><p>代码变短了，但每一步仍应被检查。</p><h2 id="24-手动加载时为什么有-prepare-model-for-kbit-training"><a href="#24-手动加载时为什么有-prepare-model-for-kbit-training" class="headerlink" title="24. 手动加载时为什么有 prepare_model_for_kbit_training"></a>24. 手动加载时为什么有 prepare_model_for_kbit_training</h2><p>如果不让 <code>SFTTrainer</code> 负责模型初始化，而是自己加载模型，PEFT 官方流程会调用：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> peft <span class="token keyword">import</span> prepare_model_for_kbit_trainingmodel <span class="token operator">=</span> AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    MODEL_ID<span class="token punctuation">,</span>    quantization_config<span class="token operator">=</span>quantization_config<span class="token punctuation">,</span><span class="token punctuation">)</span>model <span class="token operator">=</span> prepare_model_for_kbit_training<span class="token punctuation">(</span>model<span class="token punctuation">)</span></code></pre><p>这个函数用于把量化模型整理为适合 k-bit Adapter 训练的状态，例如冻结 Base Model 参数，并对部分数值敏感模块进行必要处理。</p><p>不要把它理解为“开始训练”；它只是训练前准备。</p><h2 id="25-构造对话数据"><a href="#25-构造对话数据" class="headerlink" title="25. 构造对话数据"></a>25. 构造对话数据</h2><p>沿用前文的结构化格式：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> datasets <span class="token keyword">import</span> Datasettrain_dataset <span class="token operator">=</span> Dataset<span class="token punctuation">.</span>from_list<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"messages"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>            <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"system"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"你是一名耐心的机器学习助教。"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>            <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"什么是过拟合？"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>            <span class="token punctuation">&#123;</span>                <span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span>                <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"过拟合是模型在训练数据上表现很好，却难以推广到新样本。"</span><span class="token punctuation">,</span>            <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>        <span class="token punctuation">]</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">]</span><span class="token punctuation">)</span></code></pre><p>量化不会改变数据协议。</p><p>如果 <code>messages</code>、Chat Template 或 Assistant Mask 错了，QLoRA 只会更省显存地训练错误目标。</p><h2 id="26-配置-SFTConfig"><a href="#26-配置-SFTConfig" class="headerlink" title="26. 配置 SFTConfig"></a>26. 配置 SFTConfig</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> trl <span class="token keyword">import</span> SFTConfigtraining_args <span class="token operator">=</span> SFTConfig<span class="token punctuation">(</span>    output_dir<span class="token operator">=</span><span class="token string">"outputs/qwen3-0.6b-qlora"</span><span class="token punctuation">,</span>    max_length<span class="token operator">=</span><span class="token number">512</span><span class="token punctuation">,</span>    assistant_only_loss<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    eos_token<span class="token operator">=</span><span class="token string">"&lt;|im_end|>"</span><span class="token punctuation">,</span>    per_device_train_batch_size<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">,</span>    gradient_accumulation_steps<span class="token operator">=</span><span class="token number">8</span><span class="token punctuation">,</span>    gradient_checkpointing<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    gradient_checkpointing_kwargs<span class="token operator">=</span><span class="token punctuation">&#123;</span><span class="token string">"use_reentrant"</span><span class="token punctuation">:</span> <span class="token boolean">False</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    learning_rate<span class="token operator">=</span><span class="token number">2e-4</span><span class="token punctuation">,</span>    num_train_epochs<span class="token operator">=</span><span class="token number">3</span><span class="token punctuation">,</span>    warmup_ratio<span class="token operator">=</span><span class="token number">0.05</span><span class="token punctuation">,</span>    lr_scheduler_type<span class="token operator">=</span><span class="token string">"cosine"</span><span class="token punctuation">,</span>    optim<span class="token operator">=</span><span class="token string">"paged_adamw_8bit"</span><span class="token punctuation">,</span>    max_grad_norm<span class="token operator">=</span><span class="token number">0.3</span><span class="token punctuation">,</span>    bf16<span class="token operator">=</span><span class="token punctuation">(</span>compute_dtype <span class="token operator">==</span> torch<span class="token punctuation">.</span>bfloat16<span class="token punctuation">)</span><span class="token punctuation">,</span>    fp16<span class="token operator">=</span><span class="token punctuation">(</span>compute_dtype <span class="token operator">==</span> torch<span class="token punctuation">.</span>float16<span class="token punctuation">)</span><span class="token punctuation">,</span>    eval_strategy<span class="token operator">=</span><span class="token string">"epoch"</span><span class="token punctuation">,</span>    save_strategy<span class="token operator">=</span><span class="token string">"epoch"</span><span class="token punctuation">,</span>    report_to<span class="token operator">=</span><span class="token string">"none"</span><span class="token punctuation">,</span>    use_cache<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="26-1-为什么微批次设为-1"><a href="#26-1-为什么微批次设为-1" class="headerlink" title="26.1 为什么微批次设为 1"></a>26.1 为什么微批次设为 1</h3><p>显存不足时先减少每次真正送入 GPU 的样本数：</p><pre class="language-python" data-language="python"><code class="language-python">per_device_train_batch_size<span class="token operator">=</span><span class="token number">1</span></code></pre><p>再用梯度累积增大有效 Batch Size：</p><p>$$<br>B_{\text{effective}}<br>&#x3D;B_{\text{device}}<br>\times G_{\text{accumulation}}<br>\times N_{\text{devices}}<br>$$</p><p>单卡示例中：</p><p>$$<br>1\times8\times1&#x3D;8<br>$$</p><h3 id="26-2-为什么开启-Gradient-Checkpointing"><a href="#26-2-为什么开启-Gradient-Checkpointing" class="headerlink" title="26.2 为什么开启 Gradient Checkpointing"></a>26.2 为什么开启 Gradient Checkpointing</h3><p>它不保存所有中间激活，而是在反向传播时重新计算一部分前向过程：</p><pre class="language-text" data-language="text"><code class="language-text">更低激活显存 &lt;-> 更多计算时间</code></pre><p>QLoRA 主要压缩权重，Gradient Checkpointing 主要压缩激活保存，两者解决不同问题。</p><h3 id="26-3-为什么-use-cache-False"><a href="#26-3-为什么-use-cache-False" class="headerlink" title="26.3 为什么 use_cache=False"></a>26.3 为什么 <code>use_cache=False</code></h3><p>KV Cache 主要服务于自回归生成。</p><p>训练时还要反向传播，KV Cache 通常没有生成阶段的意义，并可能与 Gradient Checkpointing 冲突。因此训练配置关闭，推理时再使用。</p><h2 id="27-创建-SFTTrainer"><a href="#27-创建-SFTTrainer" class="headerlink" title="27. 创建 SFTTrainer"></a>27. 创建 SFTTrainer</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> trl <span class="token keyword">import</span> SFTTrainertrainer <span class="token operator">=</span> SFTTrainer<span class="token punctuation">(</span>    model<span class="token operator">=</span><span class="token string">"Qwen/Qwen3-0.6B-Base"</span><span class="token punctuation">,</span>    args<span class="token operator">=</span>training_args<span class="token punctuation">,</span>    train_dataset<span class="token operator">=</span>train_dataset<span class="token punctuation">,</span>    eval_dataset<span class="token operator">=</span>eval_dataset<span class="token punctuation">,</span>    processing_class<span class="token operator">=</span>tokenizer<span class="token punctuation">,</span>    quantization_config<span class="token operator">=</span>quantization_config<span class="token punctuation">,</span>    peft_config<span class="token operator">=</span>peft_config<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这几行连接了三条独立主线：</p><pre class="language-text" data-language="text"><code class="language-text">SFT 主线：messages -> Chat Template -> labels -> loss量化主线：Base Model -> NF4 -> Double QuantizationPEFT 主线：Linear -> LoRA A/B -> 只更新 Adapter</code></pre><h2 id="28-如何确认加载的真是-4-bit-模型"><a href="#28-如何确认加载的真是-4-bit-模型" class="headerlink" title="28. 如何确认加载的真是 4-bit 模型"></a>28. 如何确认加载的真是 4-bit 模型</h2><p>不能只因为配置中写了 <code>load_in_4bit=True</code> 就停止检查。</p><pre class="language-python" data-language="python"><code class="language-python">linear4bit <span class="token operator">=</span> <span class="token punctuation">[</span>    name    <span class="token keyword">for</span> name<span class="token punctuation">,</span> module <span class="token keyword">in</span> trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>named_modules<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> module<span class="token punctuation">.</span>__class__<span class="token punctuation">.</span>__name__ <span class="token operator">==</span> <span class="token string">"Linear4bit"</span><span class="token punctuation">]</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"4-bit Linear layers:"</span><span class="token punctuation">,</span> <span class="token builtin">len</span><span class="token punctuation">(</span>linear4bit<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>linear4bit<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token number">8</span><span class="token punctuation">]</span><span class="token punctuation">)</span></code></pre><p>还可以检查：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span>trainer<span class="token punctuation">.</span>model<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>get_memory_footprint<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>如果量化层数量为 0，应先排查版本、硬件后端、模型支持与加载路径，而不是继续训练。</p><h2 id="29-如何确认只有-LoRA-在训练"><a href="#29-如何确认只有-LoRA-在训练" class="headerlink" title="29. 如何确认只有 LoRA 在训练"></a>29. 如何确认只有 LoRA 在训练</h2><pre class="language-python" data-language="python"><code class="language-python">trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>print_trainable_parameters<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>或手动统计：</p><pre class="language-python" data-language="python"><code class="language-python">trainable <span class="token operator">=</span> <span class="token builtin">sum</span><span class="token punctuation">(</span>    p<span class="token punctuation">.</span>numel<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">for</span> p <span class="token keyword">in</span> trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> p<span class="token punctuation">.</span>requires_grad<span class="token punctuation">)</span>total <span class="token operator">=</span> <span class="token builtin">sum</span><span class="token punctuation">(</span>p<span class="token punctuation">.</span>numel<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">for</span> p <span class="token keyword">in</span> trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"trainable:"</span><span class="token punctuation">,</span> trainable<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"ratio:"</span><span class="token punctuation">,</span> <span class="token number">100</span> <span class="token operator">*</span> trainable <span class="token operator">/</span> total<span class="token punctuation">,</span> <span class="token string">"%"</span><span class="token punctuation">)</span></code></pre><p>再查看具体名称：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">for</span> name<span class="token punctuation">,</span> parameter <span class="token keyword">in</span> trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>named_parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> parameter<span class="token punctuation">.</span>requires_grad<span class="token punctuation">:</span>        <span class="token keyword">print</span><span class="token punctuation">(</span>name<span class="token punctuation">,</span> parameter<span class="token punctuation">.</span>shape<span class="token punctuation">,</span> parameter<span class="token punctuation">.</span>dtype<span class="token punctuation">)</span></code></pre><p>正常情况下，大部分可训练名称会带有 <code>lora_A</code> 或 <code>lora_B</code>。</p><h2 id="30-启动训练并记录峰值显存"><a href="#30-启动训练并记录峰值显存" class="headerlink" title="30. 启动训练并记录峰值显存"></a>30. 启动训练并记录峰值显存</h2><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>reset_peak_memory_stats<span class="token punctuation">(</span><span class="token punctuation">)</span>trainer<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span>peak_gib <span class="token operator">=</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>max_memory_allocated<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">/</span> <span class="token number">1024</span><span class="token operator">**</span><span class="token number">3</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"peak allocated VRAM: </span><span class="token interpolation"><span class="token punctuation">&#123;</span>peak_gib<span class="token punctuation">:</span><span class="token format-spec">.2f</span><span class="token punctuation">&#125;</span></span><span class="token string"> GiB"</span></span><span class="token punctuation">)</span></code></pre><p>比较 LoRA 与 QLoRA 时必须保持尽量相同的：</p><ul><li>Base Model。</li><li>序列长度。</li><li>微批次。</li><li>梯度累积。</li><li>Gradient Checkpointing。</li><li>LoRA rank 与目标层。</li><li>数据和训练步数。</li></ul><p>否则显存差异不能只归因于量化。</p><h2 id="31-保存的仍然主要是-LoRA-Adapter"><a href="#31-保存的仍然主要是-LoRA-Adapter" class="headerlink" title="31. 保存的仍然主要是 LoRA Adapter"></a>31. 保存的仍然主要是 LoRA Adapter</h2><pre class="language-python" data-language="python"><code class="language-python">ADAPTER_DIR <span class="token operator">=</span> <span class="token string">"outputs/qwen3-0.6b-qlora/final-adapter"</span>trainer<span class="token punctuation">.</span>save_model<span class="token punctuation">(</span>ADAPTER_DIR<span class="token punctuation">)</span>tokenizer<span class="token punctuation">.</span>save_pretrained<span class="token punctuation">(</span>ADAPTER_DIR<span class="token punctuation">)</span></code></pre><p>目录中通常会看到：</p><pre class="language-text" data-language="text"><code class="language-text">adapter_model.safetensorsadapter_config.jsontokenizer fileschat template related files</code></pre><p>QLoRA 不会自动把完整 Base Model 权重复制进 Adapter。</p><p>推理仍需要：</p><pre class="language-text" data-language="text"><code class="language-text">原始 Base Model+ 相同量化配置+ QLoRA Adapter+ 匹配的 Tokenizer / Chat Template</code></pre><h2 id="32-重新加载-QLoRA-Adapter"><a href="#32-重新加载-QLoRA-Adapter" class="headerlink" title="32. 重新加载 QLoRA Adapter"></a>32. 重新加载 QLoRA Adapter</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> peft <span class="token keyword">import</span> PeftModel<span class="token keyword">from</span> transformers <span class="token keyword">import</span> AutoModelForCausalLMbase_model <span class="token operator">=</span> AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    MODEL_ID<span class="token punctuation">,</span>    quantization_config<span class="token operator">=</span>quantization_config<span class="token punctuation">,</span>    device_map<span class="token operator">=</span><span class="token punctuation">&#123;</span><span class="token string">""</span><span class="token punctuation">:</span> <span class="token number">0</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">)</span>model <span class="token operator">=</span> PeftModel<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    base_model<span class="token punctuation">,</span>    ADAPTER_DIR<span class="token punctuation">,</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>这里的顺序很重要：</p><pre class="language-text" data-language="text"><code class="language-text">先加载与训练一致的 Base Model-> 以 4-bit 放入设备-> 再挂载 Adapter</code></pre><p>如果 Base Model ID 或 revision 不一致，Adapter 的效果和兼容性都可能出问题。</p><h2 id="33-使用-Chat-Template-推理"><a href="#33-使用-Chat-Template-推理" class="headerlink" title="33. 使用 Chat Template 推理"></a>33. 使用 Chat Template 推理</h2><pre class="language-python" data-language="python"><code class="language-python">messages <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"system"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"你是一名耐心的机器学习助教。"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"请用生活中的例子解释过拟合。"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span>input_ids <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>apply_chat_template<span class="token punctuation">(</span>    messages<span class="token punctuation">,</span>    tokenize<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    add_generation_prompt<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    return_tensors<span class="token operator">=</span><span class="token string">"pt"</span><span class="token punctuation">,</span>    enable_thinking<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span><span class="token punctuation">)</span>device <span class="token operator">=</span> model<span class="token punctuation">.</span>get_input_embeddings<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>weight<span class="token punctuation">.</span>deviceinput_ids <span class="token operator">=</span> input_ids<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span></code></pre><p>训练时完整样本已经包含 Assistant 回答，所以不添加空的 Assistant 开头；推理时要让模型开始生成 Assistant 内容，因此：</p><pre class="language-python" data-language="python"><code class="language-python">add_generation_prompt<span class="token operator">=</span><span class="token boolean">True</span></code></pre><p>QLoRA 没有改变这一规则。</p><h2 id="34-配置-GenerationConfig"><a href="#34-配置-GenerationConfig" class="headerlink" title="34. 配置 GenerationConfig"></a>34. 配置 GenerationConfig</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> GenerationConfigim_end_id <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>convert_tokens_to_ids<span class="token punctuation">(</span><span class="token string">"&lt;|im_end|>"</span><span class="token punctuation">)</span>generation_config <span class="token operator">=</span> GenerationConfig<span class="token punctuation">(</span>    max_new_tokens<span class="token operator">=</span><span class="token number">128</span><span class="token punctuation">,</span>    do_sample<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    temperature<span class="token operator">=</span><span class="token number">0.7</span><span class="token punctuation">,</span>    top_p<span class="token operator">=</span><span class="token number">0.9</span><span class="token punctuation">,</span>    repetition_penalty<span class="token operator">=</span><span class="token number">1.05</span><span class="token punctuation">,</span>    pad_token_id<span class="token operator">=</span>tokenizer<span class="token punctuation">.</span>pad_token_id<span class="token punctuation">,</span>    eos_token_id<span class="token operator">=</span>im_end_id<span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>inference_mode<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    output_ids <span class="token operator">=</span> model<span class="token punctuation">.</span>generate<span class="token punctuation">(</span>        input_ids<span class="token operator">=</span>input_ids<span class="token punctuation">,</span>        generation_config<span class="token operator">=</span>generation_config<span class="token punctuation">,</span>    <span class="token punctuation">)</span></code></pre><p>只解码新生成部分：</p><pre class="language-python" data-language="python"><code class="language-python">new_tokens <span class="token operator">=</span> output_ids<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">,</span> input_ids<span class="token punctuation">.</span>shape<span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">:</span><span class="token punctuation">]</span>answer <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>decode<span class="token punctuation">(</span>new_tokens<span class="token punctuation">,</span> skip_special_tokens<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>answer<span class="token punctuation">)</span></code></pre><p>GenerationConfig 控制生成策略，不参与 QLoRA 的训练 Loss。</p><h2 id="35-QLoRA-Adapter-可以合并吗"><a href="#35-QLoRA-Adapter-可以合并吗" class="headerlink" title="35. QLoRA Adapter 可以合并吗"></a>35. QLoRA Adapter 可以合并吗</h2><p>可以讨论合并，但要理解合并对象。</p><p>训练时实际有效权重近似为：</p><p>$$<br>W_{\text{effective}}&#x3D;\hat W+\frac{\alpha}{r}BA<br>$$</p><p>若希望导出一个普通浮点合并模型，更稳妥的流程是：</p><pre class="language-text" data-language="text"><code class="language-text">重新加载未量化的 Base Model-> 加载 Adapter-> merge_and_unload()-> 保存浮点合并模型</code></pre><p>示例：</p><pre class="language-python" data-language="python"><code class="language-python">merge_base <span class="token operator">=</span> AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    MODEL_ID<span class="token punctuation">,</span>    dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span>bfloat16<span class="token punctuation">,</span>    device_map<span class="token operator">=</span><span class="token string">"cpu"</span><span class="token punctuation">,</span><span class="token punctuation">)</span>merge_model <span class="token operator">=</span> PeftModel<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    merge_base<span class="token punctuation">,</span>    ADAPTER_DIR<span class="token punctuation">,</span><span class="token punctuation">)</span>merged <span class="token operator">=</span> merge_model<span class="token punctuation">.</span>merge_and_unload<span class="token punctuation">(</span><span class="token punctuation">)</span>merged<span class="token punctuation">.</span>save_pretrained<span class="token punctuation">(</span>    <span class="token string">"outputs/qwen3-0.6b-qlora/merged-model"</span><span class="token punctuation">,</span>    safe_serialization<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span>tokenizer<span class="token punctuation">.</span>save_pretrained<span class="token punctuation">(</span>    <span class="token string">"outputs/qwen3-0.6b-qlora/merged-model"</span><span class="token punctuation">)</span></code></pre><p>这一步需要足够的 CPU 内存或 GPU 显存容纳未量化模型。合并后文件也会明显变大。</p><p>如果部署系统支持 PEFT Adapter，保留 <code>Base + Adapter</code> 往往更灵活。</p><h2 id="36-QLoRA-与“量化后推理”有什么区别"><a href="#36-QLoRA-与“量化后推理”有什么区别" class="headerlink" title="36. QLoRA 与“量化后推理”有什么区别"></a>36. QLoRA 与“量化后推理”有什么区别</h2><p>两者都可能加载 4-bit Base Model，但目的不同。</p><h3 id="36-1-量化推理"><a href="#36-1-量化推理" class="headerlink" title="36.1 量化推理"></a>36.1 量化推理</h3><pre class="language-text" data-language="text"><code class="language-text">4-bit Base Model-> generate()-> 不进行 backward</code></pre><h3 id="36-2-QLoRA-训练"><a href="#36-2-QLoRA-训练" class="headerlink" title="36.2 QLoRA 训练"></a>36.2 QLoRA 训练</h3><pre class="language-text" data-language="text"><code class="language-text">4-bit frozen Base Model+ trainable LoRA-> forward-> SFT loss-> backward through Base computations-> update LoRA</code></pre><p>只执行：</p><pre class="language-python" data-language="python"><code class="language-python">load_in_4bit<span class="token operator">=</span><span class="token boolean">True</span></code></pre><p>并不会自动变成 QLoRA。还必须有可训练 Adapter 和训练过程。</p><h2 id="37-QLoRA-与-QAT-不要混淆"><a href="#37-QLoRA-与-QAT-不要混淆" class="headerlink" title="37. QLoRA 与 QAT 不要混淆"></a>37. QLoRA 与 QAT 不要混淆</h2><p>QAT 是 Quantization-Aware Training，通常会在训练中模拟量化误差，让待部署模型适应低精度表示。</p><p>QLoRA 的核心路径是：</p><ul><li>Base Model 先被量化并冻结。</li><li>不直接更新 Base Model 的量化权重。</li><li>训练额外的 LoRA Adapter。</li></ul><p>因此 QLoRA 不是标准意义上的“对全部模型权重执行 QAT”。</p><h2 id="38-常见错误"><a href="#38-常见错误" class="headerlink" title="38. 常见错误"></a>38. 常见错误</h2><h3 id="38-1-把-torch-dtype-与-bnb-4bit-compute-dtype-当成一回事"><a href="#38-1-把-torch-dtype-与-bnb-4bit-compute-dtype-当成一回事" class="headerlink" title="38.1 把 torch_dtype 与 bnb_4bit_compute_dtype 当成一回事"></a>38.1 把 <code>torch_dtype</code> 与 <code>bnb_4bit_compute_dtype</code> 当成一回事</h3><p>一个控制普通浮点模块或加载 dtype，一个明确控制 4-bit 算子的计算 dtype。要结合模型和库版本理解，不能只看某一个参数。</p><h3 id="38-2-认为量化参数可以像普通-FP32-Parameter-一样直接更新"><a href="#38-2-认为量化参数可以像普通-FP32-Parameter-一样直接更新" class="headerlink" title="38.2 认为量化参数可以像普通 FP32 Parameter 一样直接更新"></a>38.2 认为量化参数可以像普通 FP32 Parameter 一样直接更新</h3><p>QLoRA 冻结 4-bit Base Model，只训练 Adapter。</p><h3 id="38-3-忘记安装-bitsandbytes"><a href="#38-3-忘记安装-bitsandbytes" class="headerlink" title="38.3 忘记安装 bitsandbytes"></a>38.3 忘记安装 bitsandbytes</h3><p>只有 Transformers 和 PEFT 不足以使用这里的 NF4 路径。</p><h3 id="38-4-硬件不支持却强行设置-BF16"><a href="#38-4-硬件不支持却强行设置-BF16" class="headerlink" title="38.4 硬件不支持却强行设置 BF16"></a>38.4 硬件不支持却强行设置 BF16</h3><p>先检查 <code>torch.cuda.is_bf16_supported()</code>。</p><h3 id="38-5-使用-device-map-quot-auto-quot-训练后又直接扩展到分布式"><a href="#38-5-使用-device-map-quot-auto-quot-训练后又直接扩展到分布式" class="headerlink" title="38.5 使用 device_map=&quot;auto&quot; 训练后又直接扩展到分布式"></a>38.5 使用 <code>device_map=&quot;auto&quot;</code> 训练后又直接扩展到分布式</h3><p>推理的自动切分与训练的设备放置不是同一问题。单卡示例跑通后，再根据 Accelerate、FSDP 或 TRL 当前文档设计多卡方案。</p><h3 id="38-6-只看-nvidia-smi-的某一个瞬间"><a href="#38-6-只看-nvidia-smi-的某一个瞬间" class="headerlink" title="38.6 只看 nvidia-smi 的某一个瞬间"></a>38.6 只看 <code>nvidia-smi</code> 的某一个瞬间</h3><p>应该记录峰值显存、训练吞吐和完整配置。</p><h3 id="38-7-只量化了模型，却没有挂载-LoRA"><a href="#38-7-只量化了模型，却没有挂载-LoRA" class="headerlink" title="38.7 只量化了模型，却没有挂载 LoRA"></a>38.7 只量化了模型，却没有挂载 LoRA</h3><p>那只是量化加载，不是 QLoRA 训练。</p><h3 id="38-8-Adapter-保存后删除了-Base-Model-信息"><a href="#38-8-Adapter-保存后删除了-Base-Model-信息" class="headerlink" title="38.8 Adapter 保存后删除了 Base Model 信息"></a>38.8 Adapter 保存后删除了 Base Model 信息</h3><p>至少记录准确的模型 ID、revision、Tokenizer 和量化配置。</p><h3 id="38-9-把-4-bit-权重直接-merge-后当作无损浮点模型"><a href="#38-9-把-4-bit-权重直接-merge-后当作无损浮点模型" class="headerlink" title="38.9 把 4-bit 权重直接 merge 后当作无损浮点模型"></a>38.9 把 4-bit 权重直接 merge 后当作无损浮点模型</h3><p>量化本身已经产生近似误差。需要明确合并来源、输出 dtype，并重新评估模型。</p><h3 id="38-10-因为更省显存就盲目增加序列长度"><a href="#38-10-因为更省显存就盲目增加序列长度" class="headerlink" title="38.10 因为更省显存就盲目增加序列长度"></a>38.10 因为更省显存就盲目增加序列长度</h3><p>激活值和 Attention 开销仍然会增长。先测量，再扩大 <code>max_length</code>。</p><h3 id="38-11-用少量演示数据宣称模型已经学会领域知识"><a href="#38-11-用少量演示数据宣称模型已经学会领域知识" class="headerlink" title="38.11 用少量演示数据宣称模型已经学会领域知识"></a>38.11 用少量演示数据宣称模型已经学会领域知识</h3><p>本篇数据只验证流程，不构成可靠模型训练实验。</p><h3 id="38-12-不验证-Assistant-only-Mask"><a href="#38-12-不验证-Assistant-only-Mask" class="headerlink" title="38.12 不验证 Assistant-only Mask"></a>38.12 不验证 Assistant-only Mask</h3><p>QLoRA 与 Loss Mask 是正交问题。量化正确不代表监督目标正确。</p><h2 id="39-OOM-时应该按什么顺序排查"><a href="#39-OOM-时应该按什么顺序排查" class="headerlink" title="39. OOM 时应该按什么顺序排查"></a>39. OOM 时应该按什么顺序排查</h2><p>可以按下面顺序处理：</p><ol><li>减小 <code>per_device_train_batch_size</code>。</li><li>减小 <code>max_length</code>。</li><li>开启 Gradient Checkpointing。</li><li>增加 Gradient Accumulation 保持有效 Batch Size。</li><li>确认 Base Model 确实以 4-bit 加载。</li><li>检查是否意外让 Base Model 参数可训练。</li><li>减小 LoRA rank 或缩小目标层范围。</li><li>检查评估和保存阶段是否出现峰值。</li><li>再考虑 CPU Offload、多卡或更复杂的并行策略。</li></ol><p>不要一次修改所有设置，否则很难知道真正解决问题的是哪一项。</p><h2 id="40-如何做一个可信的-LoRA-与-QLoRA-对比实验"><a href="#40-如何做一个可信的-LoRA-与-QLoRA-对比实验" class="headerlink" title="40. 如何做一个可信的 LoRA 与 QLoRA 对比实验"></a>40. 如何做一个可信的 LoRA 与 QLoRA 对比实验</h2><p>至少记录：</p><h3 id="模型与量化"><a href="#模型与量化" class="headerlink" title="模型与量化"></a>模型与量化</h3><pre class="language-text" data-language="text"><code class="language-text">model id / revisionLoRA or QLoRAquant typedouble quantcompute dtype</code></pre><h3 id="训练"><a href="#训练" class="headerlink" title="训练"></a>训练</h3><pre class="language-text" data-language="text"><code class="language-text">max_lengthmicro batch sizegradient accumulationeffective batch sizeLoRA r / alpha / target modulesoptimizerlearning rategradient checkpointing</code></pre><h3 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h3><pre class="language-text" data-language="text"><code class="language-text">peak allocated VRAMpeak reserved VRAMtokens per secondwall-clock timeeval losstask metricsgeneration samples</code></pre><p>QLoRA 的价值不应只用“成功跑起来”判断，还要比较速度、显存与质量之间的交换。</p><h2 id="41-一个具体的显存思考例子"><a href="#41-一个具体的显存思考例子" class="headerlink" title="41. 一个具体的显存思考例子"></a>41. 一个具体的显存思考例子</h2><p>假设有一个 7B Base Model，先只估计权重：</p><h3 id="BF16-LoRA"><a href="#BF16-LoRA" class="headerlink" title="BF16 LoRA"></a>BF16 LoRA</h3><p>$$<br>7\text{B}\times16\text{ bit}&#x3D;112\text{ Gbit}\approx14\text{ GB}<br>$$</p><h3 id="理想-4-bit-QLoRA"><a href="#理想-4-bit-QLoRA" class="headerlink" title="理想 4-bit QLoRA"></a>理想 4-bit QLoRA</h3><p>$$<br>7\text{B}\times4\text{ bit}&#x3D;28\text{ Gbit}\approx3.5\text{ GB}<br>$$</p><p>理论差值约为：</p><p>$$<br>14-3.5&#x3D;10.5\text{ GB}<br>$$</p><p>但假设训练时激活、Adapter、优化器、缓存和工作区还需要 6 GB，那么粗略总量可能是：</p><pre class="language-text" data-language="text"><code class="language-text">LoRA  : 14 + 6 = 20 GBQLoRA : 3.5 + 量化元数据 + 6 GB</code></pre><p>这解释了两个现象：</p><ol><li>QLoRA 能显著节省显存。</li><li>实际总显存不会严格变成 LoRA 的四分之一。</li></ol><h2 id="42-完整代码"><a href="#42-完整代码" class="headerlink" title="42. 完整代码"></a>42. 完整代码</h2><p>本文完整示例位于：</p><p><a href="https://github.com/lsworl/lsworl.github.io/blob/main/source/code/nlp-blog/sft_qlora.py">sft_qlora.py</a></p><p>本地运行：</p><pre class="language-bash" data-language="bash"><code class="language-bash">python source/code/nlp-blog/sft_qlora.py</code></pre><p>运行前请先把它当作一次环境验证：</p><pre class="language-text" data-language="text"><code class="language-text">能否识别 CUDA-> 能否加载 NF4 Linear4bit-> 能否构造 Assistant Mask-> 可训练参数是否只有 LoRA-> 能否完成一个训练 step-> 能否保存并重新加载 Adapter-> 能否生成回答</code></pre><p>全部通过后，再替换为真实数据和更大的模型。</p><h2 id="43-从已经学过的-Hugging-Face-API-重新理解-QLoRA"><a href="#43-从已经学过的-Hugging-Face-API-重新理解-QLoRA" class="headerlink" title="43. 从已经学过的 Hugging Face API 重新理解 QLoRA"></a>43. 从已经学过的 Hugging Face API 重新理解 QLoRA</h2><p>现在可以把 QLoRA 映射回前面学过的内容：</p><h3 id="Tokenizer"><a href="#Tokenizer" class="headerlink" title="Tokenizer"></a>Tokenizer</h3><p>仍然负责把 Chat Template 的文本变成 <code>input_ids</code>，量化不会改变 token id。</p><h3 id="Model-x2F-Forward"><a href="#Model-x2F-Forward" class="headerlink" title="Model &#x2F; Forward"></a>Model &#x2F; Forward</h3><p>模型内部部分 <code>Linear</code> 被替换为 <code>Linear4bit</code>。Forward 时 Base 权重反量化计算，并与 LoRA 分支结果相加。</p><h3 id="Config-x2F-Architecture"><a href="#Config-x2F-Architecture" class="headerlink" title="Config &#x2F; Architecture"></a>Config &#x2F; Architecture</h3><p>Transformer 主体架构不因 QLoRA 改变。量化配置决定权重如何加载，PEFT 配置决定在哪些模块增加 Adapter。</p><h3 id="Generation-x2F-Sampling"><a href="#Generation-x2F-Sampling" class="headerlink" title="Generation &#x2F; Sampling"></a>Generation &#x2F; Sampling</h3><p>训练完成后仍通过 <code>generate()</code> 自回归预测下一个 token，Temperature、Top-p 等规则不参与训练。</p><h3 id="GenerationConfig"><a href="#GenerationConfig" class="headerlink" title="GenerationConfig"></a>GenerationConfig</h3><p>继续管理推理策略，不管理 NF4、LoRA rank 或 SFT Loss。</p><h3 id="SFT"><a href="#SFT" class="headerlink" title="SFT"></a>SFT</h3><p>Loss 仍然是带 Label Mask 的 next-token cross entropy。QLoRA 只改变模型参数如何存储和更新。</p><h2 id="44-总结"><a href="#44-总结" class="headerlink" title="44. 总结"></a>44. 总结</h2><p>QLoRA 可以用下面这条主线记忆：</p><pre class="language-text" data-language="text"><code class="language-text">Base Model 权重-> 分块 NF4 4-bit 存储-> Double Quantization 压缩量化常数-> 前向时反量化到 BF16 / FP16 计算-> 与高精度 LoRA 分支相加-> SFT Loss 反向传播-> 冻结 Base Model-> 只更新 LoRA Adapter-> Paged Optimizer 缓解优化器状态与峰值压力</code></pre><p>真正需要记住的不是四个参数名，而是下面四句话：</p><ol><li>QLoRA 是量化 Base Model 与 LoRA 的组合，不是新的 SFT Loss。</li><li>4-bit 主要描述 Base Model 权重的存储，不代表所有计算都在 4 bit 中进行。</li><li>梯度经过量化模型的计算路径，但只更新高精度 Adapter。</li><li>QLoRA 大幅压缩权重显存，却不会消除激活值、序列长度和数据质量问题。</li></ol><p>理解这些之后，<code>BitsAndBytesConfig</code> 就不再是一组需要背诵的开关，而是每一步数学与显存取舍在 Hugging Face 中的具体入口。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li><a href="https://arxiv.org/abs/2305.14314">QLoRA: Efficient Finetuning of Quantized LLMs</a></li><li><a href="https://huggingface.co/docs/transformers/quantization/bitsandbytes">Hugging Face Transformers：Bitsandbytes</a></li><li><a href="https://huggingface.co/docs/peft/developer_guides/quantization">Hugging Face PEFT：Quantization</a></li><li><a href="https://huggingface.co/docs/trl/peft_integration">Hugging Face TRL：PEFT Integration</a></li><li><a href="https://huggingface.co/docs/trl/sft_trainer">Hugging Face TRL：SFTTrainer</a></li><li><a href="https://huggingface.co/docs/bitsandbytes/installation">Hugging Face bitsandbytes：Installation Guide</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/09/04/nlp-qlora-4bit-finetuning/</id>
    <link href="https://lsworl.github.io/2026/09/04/nlp-qlora-4bit-finetuning/"/>
    <published>2026-09-03T16:00:00.000Z</published>
    <summary>承接LoRA监督微调，从显存组成、分块量化、NF4、Double Quantization和Paged Optimizer出发，解释QLoRA的前向与反向传播，并使用Hugging Face Transformers、TRL、PEFT和bitsandbytes完成可检查的4-bit SFT项目。</summary>
    <title>SFT（四）：QLoRA的4-bit量化与LoRA微调原理</title>
    <updated>2026-09-03T16:46:30.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Hugging Face" scheme="https://lsworl.github.io/tags/Hugging-Face/"/>
    <category term="LLM" scheme="https://lsworl.github.io/tags/LLM/"/>
    <category term="SFT" scheme="https://lsworl.github.io/tags/SFT/"/>
    <category term="TRL" scheme="https://lsworl.github.io/tags/TRL/"/>
    <category term="PEFT" scheme="https://lsworl.github.io/tags/PEFT/"/>
    <category term="LoRA" scheme="https://lsworl.github.io/tags/LoRA/"/>
    <content>
      <![CDATA[<p>前两篇已经完成了两层理解：</p><ol><li><a href="https://lsworl.github.io/2026/08/24/nlp-sft-principles/">SFT 原理篇</a>：SFT 仍然是 next-token prediction，关键在于数据、Chat Template 和 Loss Mask。</li><li><a href="https://lsworl.github.io/2026/08/24/nlp-sft-transformers-from-scratch/">Transformers 手写篇</a>：亲手构造 <code>input_ids / attention_mask / labels</code>，再交给原生 <code>Trainer</code>。</li></ol><p>现在可以进入更接近真实项目的组合：</p><pre class="language-text" data-language="text"><code class="language-text">Hugging Face Datasets  + Transformers  + TRL SFTTrainer  + PEFT LoRA</code></pre><p>本篇不会把 <code>SFTTrainer</code> 描述成“一行代码训练大模型”。它确实减少了很多样板代码，但仍然需要对下面这些问题负责：</p><ul><li>数据格式是否正确。</li><li>Chat Template 是否与模型匹配。</li><li>Assistant-only loss 是否真的生效。</li><li>截断是否删除了回答。</li><li>LoRA 是否插入了预期模块。</li><li>训练与验证是否存在数据泄漏。</li><li>保存的是完整模型还是 Adapter。</li><li>推理时是否加载了正确的 Base Model。</li></ul><span id="more"></span><p><img src="/images/nlp-blog/sft-trl-lora-overview.svg" alt="TRL SFTTrainer与PEFT LoRA整体流程"></p><h2 id="1-TRL-是什么"><a href="#1-TRL-是什么" class="headerlink" title="1. TRL 是什么"></a>1. TRL 是什么</h2><p>TRL 是 Hugging Face 生态中面向语言模型后训练的库。</p><p>它提供多种 Trainer，例如：</p><ul><li><code>SFTTrainer</code>：监督微调。</li><li><code>DPOTrainer</code>：偏好优化。</li><li><code>RewardTrainer</code>：奖励模型训练。</li><li><code>PPOTrainer</code>：基于 PPO 的强化学习训练。</li><li>其他后训练算法 Trainer。</li></ul><p>本篇只讨论 <code>SFTTrainer</code>。</p><p>它建立在 Transformers <code>Trainer</code> 之上，额外处理：</p><pre class="language-text" data-language="text"><code class="language-text">SFT 常见数据格式Chat TemplateTokenizationCompletion / Assistant Loss MaskPackingPEFT 集成</code></pre><h2 id="2-SFTTrainer-自动做了什么"><a href="#2-SFTTrainer-自动做了什么" class="headerlink" title="2. SFTTrainer 自动做了什么"></a>2. SFTTrainer 自动做了什么</h2><p>上一份代码中，我们手动完成：</p><pre class="language-text" data-language="text"><code class="language-text">format_prompt  -> tokenize Prompt  -> tokenize Answer  -> 拼接 input_ids  -> 构造 labels=-100  -> 动态 Padding</code></pre><p>现在可以直接提供：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token punctuation">&#123;</span>    <span class="token string">"messages"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>        <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"什么是过拟合？"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>        <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"过拟合是……"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">]</span><span class="token punctuation">&#125;</span></code></pre><p><code>SFTTrainer</code> 会根据 Tokenizer 与配置完成中间处理。</p><p>但“自动”不等于“无需理解”。如果模板、角色或 EOS 配置错了，Trainer 只会稳定地训练错误数据。</p><h2 id="3-安装依赖"><a href="#3-安装依赖" class="headerlink" title="3. 安装依赖"></a>3. 安装依赖</h2><pre class="language-bash" data-language="bash"><code class="language-bash">pip <span class="token function">install</span> <span class="token parameter variable">-U</span> torch transformers datasets accelerate trl peft</code></pre><p>记录版本：</p><pre class="language-bash" data-language="bash"><code class="language-bash">python <span class="token parameter variable">-c</span> <span class="token string">"import torch, transformers, datasets, trl, peft; print(torch.__version__); print(transformers.__version__); print(datasets.__version__); print(trl.__version__); print(peft.__version__)"</span></code></pre><p>TRL 迭代较快，尤其是：</p><ul><li>数据格式支持。</li><li><code>SFTConfig</code> 参数。</li><li>Chat Template 处理。</li><li>Completion-only 与 Assistant-only loss。</li><li>Packing 和 Loss 实现。</li></ul><p>运行项目时应固定一组经过验证的依赖版本，并保留 <code>pip freeze</code>。</p><h2 id="4-本篇模型与训练目标"><a href="#4-本篇模型与训练目标" class="headerlink" title="4. 本篇模型与训练目标"></a>4. 本篇模型与训练目标</h2><p>教学代码使用：</p><pre class="language-python" data-language="python"><code class="language-python">MODEL_ID <span class="token operator">=</span> <span class="token string">"Qwen/Qwen3-0.6B-Base"</span></code></pre><p>目标是：</p><pre class="language-text" data-language="text"><code class="language-text">Qwen3 Base Causal LM  + 中文机器学习对话示范  + Chat Template  + Assistant-only loss  + LoRA  -> 一个教学用指令微调 Adapter</code></pre><p>选择小模型只是为了降低学习门槛。少量示例不能构建可靠的通用助手。</p><p>使用任何模型前都要检查 Model Card、许可证和最低 Transformers 版本。</p><h2 id="5-为什么使用-Base-Model"><a href="#5-为什么使用-Base-Model" class="headerlink" title="5. 为什么使用 Base Model"></a>5. 为什么使用 Base Model</h2><p>Base Model 的主要训练阶段是预训练。</p><p>它可能已经具备语言和知识能力，但并不保证稳定遵循对话指令。</p><p>从 Base Model 开始能更清楚地观察：</p><pre class="language-text" data-language="text"><code class="language-text">Chat Template + SFT Data如何让“文本续写”呈现为“用户与助手对话”</code></pre><p>如果目标是快速适配业务，通常也可以从 Instruct Model 开始；此时应沿用原模型自带的 Chat Template，避免改变角色协议。</p><h2 id="6-TRL-支持哪些-SFT-数据格式"><a href="#6-TRL-支持哪些-SFT-数据格式" class="headerlink" title="6. TRL 支持哪些 SFT 数据格式"></a>6. TRL 支持哪些 SFT 数据格式</h2><p>当前 <code>SFTTrainer</code> 主要支持两种数据类型，每种又有标准与对话形式。</p><h3 id="6-1-Standard-language-modeling"><a href="#6-1-Standard-language-modeling" class="headerlink" title="6.1 Standard language modeling"></a>6.1 Standard language modeling</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token punctuation">&#123;</span><span class="token string">"text"</span><span class="token punctuation">:</span> <span class="token string">"The sky is blue."</span><span class="token punctuation">&#125;</span></code></pre><h3 id="6-2-Conversational-language-modeling"><a href="#6-2-Conversational-language-modeling" class="headerlink" title="6.2 Conversational language modeling"></a>6.2 Conversational language modeling</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token punctuation">&#123;</span>    <span class="token string">"messages"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>        <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"What color is the sky?"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>        <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"It is blue."</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">]</span><span class="token punctuation">&#125;</span></code></pre><h3 id="6-3-Standard-prompt-completion"><a href="#6-3-Standard-prompt-completion" class="headerlink" title="6.3 Standard prompt-completion"></a>6.3 Standard prompt-completion</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token punctuation">&#123;</span>    <span class="token string">"prompt"</span><span class="token punctuation">:</span> <span class="token string">"The sky is"</span><span class="token punctuation">,</span>    <span class="token string">"completion"</span><span class="token punctuation">:</span> <span class="token string">" blue."</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span></code></pre><h3 id="6-4-Conversational-prompt-completion"><a href="#6-4-Conversational-prompt-completion" class="headerlink" title="6.4 Conversational prompt-completion"></a>6.4 Conversational prompt-completion</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token punctuation">&#123;</span>    <span class="token string">"prompt"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>        <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"What color is the sky?"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"completion"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>        <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"It is blue."</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span></code></pre><p>本篇使用 <code>messages</code>，因为它最接近聊天模型的真实输入结构。</p><h2 id="7-构造-Conversational-Dataset"><a href="#7-构造-Conversational-Dataset" class="headerlink" title="7. 构造 Conversational Dataset"></a>7. 构造 Conversational Dataset</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> datasets <span class="token keyword">import</span> Dataset<span class="token punctuation">,</span> DatasetDictTRAIN_CONVERSATIONS <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"messages"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>            <span class="token punctuation">&#123;</span>                <span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"system"</span><span class="token punctuation">,</span>                <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"你是一名耐心、准确的机器学习助教。"</span><span class="token punctuation">,</span>            <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>            <span class="token punctuation">&#123;</span>                <span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span>                <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"请用一句话解释什么是过拟合。"</span><span class="token punctuation">,</span>            <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>            <span class="token punctuation">&#123;</span>                <span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span>                <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"过拟合是模型过度记忆训练数据，导致在未见数据上泛化较差的现象。"</span><span class="token punctuation">,</span>            <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>        <span class="token punctuation">]</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span>dataset <span class="token operator">=</span> DatasetDict<span class="token punctuation">(</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"train"</span><span class="token punctuation">:</span> Dataset<span class="token punctuation">.</span>from_list<span class="token punctuation">(</span>TRAIN_CONVERSATIONS<span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"validation"</span><span class="token punctuation">:</span> Dataset<span class="token punctuation">.</span>from_list<span class="token punctuation">(</span>VALIDATION_CONVERSATIONS<span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">)</span></code></pre><h3 id="7-1-为什么保留结构化-messages"><a href="#7-1-为什么保留结构化-messages" class="headerlink" title="7.1 为什么保留结构化 messages"></a>7.1 为什么保留结构化 messages</h3><p>结构化数据比提前拼接字符串更容易：</p><ul><li>检查角色顺序。</li><li>替换 system prompt。</li><li>支持多轮对话。</li><li>应用不同模型的 Chat Template。</li><li>区分 assistant token。</li><li>扩展工具调用字段。</li></ul><h2 id="8-数据校验不能省"><a href="#8-数据校验不能省" class="headerlink" title="8. 数据校验不能省"></a>8. 数据校验不能省</h2><pre class="language-python" data-language="python"><code class="language-python">ALLOWED_ROLES <span class="token operator">=</span> <span class="token punctuation">&#123;</span><span class="token string">"system"</span><span class="token punctuation">,</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token string">"assistant"</span><span class="token punctuation">&#125;</span><span class="token keyword">def</span> <span class="token function">validate_conversation</span><span class="token punctuation">(</span>example<span class="token punctuation">)</span><span class="token punctuation">:</span>    messages <span class="token operator">=</span> example<span class="token punctuation">[</span><span class="token string">"messages"</span><span class="token punctuation">]</span>    <span class="token keyword">assert</span> messages<span class="token punctuation">,</span> <span class="token string">"messages 不能为空"</span>    assistant_count <span class="token operator">=</span> <span class="token number">0</span>    <span class="token keyword">for</span> message <span class="token keyword">in</span> messages<span class="token punctuation">:</span>        <span class="token keyword">assert</span> message<span class="token punctuation">[</span><span class="token string">"role"</span><span class="token punctuation">]</span> <span class="token keyword">in</span> ALLOWED_ROLES        <span class="token keyword">assert</span> <span class="token builtin">isinstance</span><span class="token punctuation">(</span>message<span class="token punctuation">[</span><span class="token string">"content"</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token builtin">str</span><span class="token punctuation">)</span>        <span class="token keyword">assert</span> message<span class="token punctuation">[</span><span class="token string">"content"</span><span class="token punctuation">]</span><span class="token punctuation">.</span>strip<span class="token punctuation">(</span><span class="token punctuation">)</span>        assistant_count <span class="token operator">+=</span> message<span class="token punctuation">[</span><span class="token string">"role"</span><span class="token punctuation">]</span> <span class="token operator">==</span> <span class="token string">"assistant"</span>    <span class="token keyword">assert</span> assistant_count <span class="token operator">></span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token string">"每条训练样本至少需要一个 assistant 回答"</span></code></pre><p>还应检查：</p><ul><li>是否连续出现多个相同角色。</li><li>system 是否只出现在允许位置。</li><li>assistant 是否复制了 Prompt。</li><li>是否存在空回答。</li><li>是否存在超长样本。</li><li>训练集与验证集是否重复。</li></ul><h2 id="9-Chat-Template-是训练协议"><a href="#9-Chat-Template-是训练协议" class="headerlink" title="9. Chat Template 是训练协议"></a>9. Chat Template 是训练协议</h2><p><code>messages</code> 只是数据结构，模型真正看到的仍是一串 token。</p><p>Chat Template 负责把：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"什么是SFT？"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"SFT是监督微调。"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span></code></pre><p>变成类似：</p><pre class="language-text" data-language="text"><code class="language-text">&lt;|user|>什么是SFT？&lt;|end|>&lt;|assistant|>SFT是监督微调。&lt;|end|></code></pre><p>模板定义：</p><ul><li>角色起始 token。</li><li>消息结束 token。</li><li>system prompt 放置方式。</li><li>assistant 回答边界。</li><li>EOS 对齐。</li><li>可选工具调用格式。</li></ul><p><img src="/images/nlp-blog/sft-chat-template-assistant-mask.svg" alt="Chat Template如何生成Assistant Mask"></p><h2 id="10-内置模板与-chat-template-path"><a href="#10-内置模板与-chat-template-path" class="headerlink" title="10. 内置模板与 chat_template_path"></a>10. 内置模板与 chat_template_path</h2><p>本文完整脚本使用 Qwen3 Tokenizer 已有的 Chat Template 和控制 token。当前 TRL 能为已知的 Qwen3 模板补充 Assistant-only loss 所需的生成区域标记，因此核心配置不必克隆另一套角色协议。</p><p>如果模型没有模板，<code>SFTConfig</code> 也可以指定一个模板来源：</p><pre class="language-python" data-language="python"><code class="language-python">SFTConfig<span class="token punctuation">(</span>    chat_template_path<span class="token operator">=</span><span class="token string">"HuggingFaceTB/SmolLM3-3B"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>TRL 会使用对应模板处理 conversational dataset，并处理所需特殊 token。</p><p>有些 Base Model Tokenizer 已经包含合适 Chat Template，这时不一定需要克隆其他模板。</p><p>判断原则：</p><ol><li>优先检查模型自己的 Tokenizer 配置。</li><li>确认模板是否适合训练目标。</li><li>确认 EOS 与消息结束 token 对齐。</li><li>确认 Assistant-only loss 所需标记是否受支持。</li><li>保存最终 Tokenizer，不能只保存 Adapter。</li></ol><h2 id="11-assistant-only-loss-x3D-True-到底做什么"><a href="#11-assistant-only-loss-x3D-True-到底做什么" class="headerlink" title="11. assistant_only_loss&#x3D;True 到底做什么"></a>11. assistant_only_loss&#x3D;True 到底做什么</h2><pre class="language-python" data-language="python"><code class="language-python">SFTConfig<span class="token punctuation">(</span>    assistant_only_loss<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>它让 system 和 user token 对应 labels 被忽略：</p><pre class="language-text" data-language="text"><code class="language-text">system token    -> labels = -100user token      -> labels = -100assistant token -> labels = token idPadding token   -> labels = -100</code></pre><p>这与上一篇手写的：</p><pre class="language-python" data-language="python"><code class="language-python">labels <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">100</span><span class="token punctuation">]</span> <span class="token operator">*</span> <span class="token builtin">len</span><span class="token punctuation">(</span>prompt_ids<span class="token punctuation">)</span> <span class="token operator">+</span> answer_ids</code></pre><p>属于同一原理，只是 TRL 根据 Chat Template 生成 assistant mask。</p><h3 id="11-1-不是所有模板都能自动识别-Assistant"><a href="#11-1-不是所有模板都能自动识别-Assistant" class="headerlink" title="11.1 不是所有模板都能自动识别 Assistant"></a>11.1 不是所有模板都能自动识别 Assistant</h3><p>Assistant-only loss 需要模板能够标记 assistant 生成区域。</p><p>当前 TRL 文档使用模板中的 generation 区域标记来获得 assistant mask，并对部分已知模型族提供适配。</p><p>因此开启参数后仍应检查一条处理后的样本，而不是只相信配置名。</p><h2 id="12-completion-only-loss-与-assistant-only-loss-的区别"><a href="#12-completion-only-loss-与-assistant-only-loss-的区别" class="headerlink" title="12. completion_only_loss 与 assistant_only_loss 的区别"></a>12. completion_only_loss 与 assistant_only_loss 的区别</h2><h3 id="12-1-completion-only-loss"><a href="#12-1-completion-only-loss" class="headerlink" title="12.1 completion_only_loss"></a>12.1 completion_only_loss</h3><p>适合 <code>prompt / completion</code> 数据：</p><pre class="language-text" data-language="text"><code class="language-text">prompt     -> -100completion -> 参与 loss</code></pre><h3 id="12-2-assistant-only-loss"><a href="#12-2-assistant-only-loss" class="headerlink" title="12.2 assistant_only_loss"></a>12.2 assistant_only_loss</h3><p>适合 conversational 数据：</p><pre class="language-text" data-language="text"><code class="language-text">system / user -> -100assistant     -> 参与 loss</code></pre><p>多轮对话中可以有多个 assistant 区域。</p><h3 id="12-3-可以同时使用吗"><a href="#12-3-可以同时使用吗" class="headerlink" title="12.3 可以同时使用吗"></a>12.3 可以同时使用吗</h3><p>对于 conversational prompt-completion 数据，两种范围可以组合：先只考虑 completion 部分，再只训练其中 assistant 消息。</p><p>具体行为与 TRL 版本和数据格式有关，应根据当前官方文档与处理结果确认。</p><h2 id="13-SFTConfig-的核心参数"><a href="#13-SFTConfig-的核心参数" class="headerlink" title="13. SFTConfig 的核心参数"></a>13. SFTConfig 的核心参数</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> trl <span class="token keyword">import</span> SFTConfigtraining_args <span class="token operator">=</span> SFTConfig<span class="token punctuation">(</span>    output_dir<span class="token operator">=</span><span class="token string">"outputs/sft-trl-lora/checkpoints"</span><span class="token punctuation">,</span>    max_length<span class="token operator">=</span><span class="token number">512</span><span class="token punctuation">,</span>    eos_token<span class="token operator">=</span><span class="token string">"&lt;|im_end|>"</span><span class="token punctuation">,</span>    assistant_only_loss<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    packing<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    loss_type<span class="token operator">=</span><span class="token string">"nll"</span><span class="token punctuation">,</span>    num_train_epochs<span class="token operator">=</span><span class="token number">3</span><span class="token punctuation">,</span>    per_device_train_batch_size<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">,</span>    per_device_eval_batch_size<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">,</span>    gradient_accumulation_steps<span class="token operator">=</span><span class="token number">8</span><span class="token punctuation">,</span>    learning_rate<span class="token operator">=</span><span class="token number">1e-4</span><span class="token punctuation">,</span>    warmup_ratio<span class="token operator">=</span><span class="token number">0.1</span><span class="token punctuation">,</span>    lr_scheduler_type<span class="token operator">=</span><span class="token string">"cosine"</span><span class="token punctuation">,</span>    logging_steps<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">,</span>    eval_strategy<span class="token operator">=</span><span class="token string">"epoch"</span><span class="token punctuation">,</span>    save_strategy<span class="token operator">=</span><span class="token string">"epoch"</span><span class="token punctuation">,</span>    load_best_model_at_end<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    metric_for_best_model<span class="token operator">=</span><span class="token string">"eval_loss"</span><span class="token punctuation">,</span>    greater_is_better<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    gradient_checkpointing<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    report_to<span class="token operator">=</span><span class="token string">"none"</span><span class="token punctuation">,</span>    seed<span class="token operator">=</span><span class="token number">42</span><span class="token punctuation">,</span>    model_init_kwargs<span class="token operator">=</span><span class="token punctuation">&#123;</span><span class="token string">"dtype"</span><span class="token punctuation">:</span> dtype<span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="13-1-max-length"><a href="#13-1-max-length" class="headerlink" title="13.1 max_length"></a>13.1 max_length</h3><p>它限制处理后的序列长度。</p><p>不能只根据模型最大上下文设置。还要结合：</p><ul><li>数据长度分布。</li><li>GPU 显存。</li><li>Batch Size。</li><li>Attention 的二次复杂度。</li><li>截断后回答是否保留。</li></ul><h3 id="13-2-packing"><a href="#13-2-packing" class="headerlink" title="13.2 packing"></a>13.2 packing</h3><p><code>packing=True</code> 会把多个短样本打包，提高 token 利用率。</p><p>学习阶段先设为 <code>False</code>，更容易逐条检查。</p><h3 id="13-3-eos-token"><a href="#13-3-eos-token" class="headerlink" title="13.3 eos_token"></a>13.3 eos_token</h3><p>Qwen3 Chat Template 使用 <code>&lt;|im_end|&gt;</code> 结束一条消息，因此显式把训练结束 token 与模板对齐：</p><pre class="language-python" data-language="python"><code class="language-python">eos_token<span class="token operator">=</span><span class="token string">"&lt;|im_end|>"</span></code></pre><h3 id="13-4-learning-rate"><a href="#13-4-learning-rate" class="headerlink" title="13.4 learning_rate"></a>13.4 learning_rate</h3><p>LoRA 只训练新增参数，常使用比 Full Fine-tuning 更高的学习率。</p><p><code>1e-4</code> 是一个实验起点，不是通用最优值。</p><h3 id="13-5-model-init-kwargs"><a href="#13-5-model-init-kwargs" class="headerlink" title="13.5 model_init_kwargs"></a>13.5 model_init_kwargs</h3><p>当 <code>model</code> 以 Hub ID 字符串传给 <code>SFTTrainer</code> 时，可以通过它传递 <code>from_pretrained()</code> 参数：</p><pre class="language-python" data-language="python"><code class="language-python">model_init_kwargs<span class="token operator">=</span><span class="token punctuation">&#123;</span><span class="token string">"dtype"</span><span class="token punctuation">:</span> torch<span class="token punctuation">.</span>bfloat16<span class="token punctuation">&#125;</span></code></pre><h2 id="14-为什么本篇显式设置-loss-type-x3D-”nll”"><a href="#14-为什么本篇显式设置-loss-type-x3D-”nll”" class="headerlink" title="14. 为什么本篇显式设置 loss_type&#x3D;”nll”"></a>14. 为什么本篇显式设置 loss_type&#x3D;”nll”</h2><p>当前 TRL 还提供降低大词表 logits 峰值显存的 chunked loss 实现。</p><p>但具体 loss 实现与 PEFT、Liger 等功能之间可能存在版本相关的兼容限制。</p><p>本篇目标是讲清 LoRA SFT，因此显式使用标准负对数似然：</p><pre class="language-python" data-language="python"><code class="language-python">loss_type<span class="token operator">=</span><span class="token string">"nll"</span></code></pre><p>数学目标仍然是：</p><p>$$<br>\mathcal{L}<br>&#x3D;-<br>\frac{1}{N}<br>\sum_{t: label_t\neq -100}<br>\log p_{\theta}(x_t\mid x_{&lt;t})<br>$$</p><p>引入新的显存优化前，应先阅读正在使用的 TRL 版本文档并单独验证兼容性。</p><h2 id="15-LoRA-的数学原理"><a href="#15-LoRA-的数学原理" class="headerlink" title="15. LoRA 的数学原理"></a>15. LoRA 的数学原理</h2><p>原线性层：</p><p>$$<br>y&#x3D;Wx<br>$$</p><p>LoRA 冻结 $W$，学习低秩增量：</p><p>$$<br>y&#x3D;(W+\Delta W)x<br>$$</p><p>$$<br>\Delta W&#x3D;\frac{\alpha}{r}BA<br>$$</p><p>其中：</p><p>$$<br>A\in\mathbb{R}^{r\times d_{in}}<br>$$</p><p>$$<br>B\in\mathbb{R}^{d_{out}\times r}<br>$$</p><p>如果原矩阵参数量为：</p><p>$$<br>d_{out}d_{in}<br>$$</p><p>LoRA 新增参数量为：</p><p>$$<br>r(d_{in}+d_{out})<br>$$</p><p>当 $r$ 很小时，新增参数远少于完整矩阵。</p><h2 id="16-配置-LoraConfig"><a href="#16-配置-LoraConfig" class="headerlink" title="16. 配置 LoraConfig"></a>16. 配置 LoraConfig</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> peft <span class="token keyword">import</span> LoraConfigpeft_config <span class="token operator">=</span> LoraConfig<span class="token punctuation">(</span>    r<span class="token operator">=</span><span class="token number">16</span><span class="token punctuation">,</span>    lora_alpha<span class="token operator">=</span><span class="token number">32</span><span class="token punctuation">,</span>    lora_dropout<span class="token operator">=</span><span class="token number">0.05</span><span class="token punctuation">,</span>    bias<span class="token operator">=</span><span class="token string">"none"</span><span class="token punctuation">,</span>    task_type<span class="token operator">=</span><span class="token string">"CAUSAL_LM"</span><span class="token punctuation">,</span>    target_modules<span class="token operator">=</span><span class="token string">"all-linear"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="16-1-r"><a href="#16-1-r" class="headerlink" title="16.1 r"></a>16.1 r</h3><p><code>r</code> 是低秩维度。</p><p>更大的 <code>r</code>：</p><ul><li>表达能力更高。</li><li>可训练参数更多。</li><li>显存和计算增加。</li><li>不一定带来更好泛化。</li></ul><h3 id="16-2-lora-alpha"><a href="#16-2-lora-alpha" class="headerlink" title="16.2 lora_alpha"></a>16.2 lora_alpha</h3><p>它控制 LoRA 更新的缩放。</p><p>原始形式常写为：</p><p>$$<br>\frac{\alpha}{r}<br>$$</p><h3 id="16-3-lora-dropout"><a href="#16-3-lora-dropout" class="headerlink" title="16.3 lora_dropout"></a>16.3 lora_dropout</h3><p>只在 LoRA 分支上应用 Dropout，可缓解小数据过拟合。</p><h3 id="16-4-target-modules-x3D-”all-linear”"><a href="#16-4-target-modules-x3D-”all-linear”" class="headerlink" title="16.4 target_modules&#x3D;”all-linear”"></a>16.4 target_modules&#x3D;”all-linear”</h3><p>PEFT 会选择模型中的线性层，同时对 <code>PreTrainedModel</code> 排除最终输出层。</p><p>优点是避免硬编码不同架构的模块名。</p><p>如果只想训练特定投影，也可以显式配置：</p><pre class="language-python" data-language="python"><code class="language-python">target_modules<span class="token operator">=</span><span class="token punctuation">[</span><span class="token string">"q_proj"</span><span class="token punctuation">,</span> <span class="token string">"k_proj"</span><span class="token punctuation">,</span> <span class="token string">"v_proj"</span><span class="token punctuation">,</span> <span class="token string">"o_proj"</span><span class="token punctuation">]</span></code></pre><p>前提是目标模型确实使用这些名称。</p><p><img src="/images/nlp-blog/sft-lora-linear-layer.svg" alt="LoRA如何插入Causal LM线性层"></p><h2 id="17-创建-SFTTrainer"><a href="#17-创建-SFTTrainer" class="headerlink" title="17. 创建 SFTTrainer"></a>17. 创建 SFTTrainer</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> trl <span class="token keyword">import</span> SFTTrainertrainer <span class="token operator">=</span> SFTTrainer<span class="token punctuation">(</span>    model<span class="token operator">=</span>MODEL_ID<span class="token punctuation">,</span>    args<span class="token operator">=</span>training_args<span class="token punctuation">,</span>    train_dataset<span class="token operator">=</span>dataset<span class="token punctuation">[</span><span class="token string">"train"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    eval_dataset<span class="token operator">=</span>dataset<span class="token punctuation">[</span><span class="token string">"validation"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    peft_config<span class="token operator">=</span>peft_config<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这几行背后发生：</p><pre class="language-text" data-language="text"><code class="language-text">根据 MODEL_ID 加载 Causal LM  -> 加载 Tokenizer / Processing Class  -> 准备 Chat Template  -> 处理 messages  -> Tokenize  -> 构造 Assistant Mask 与 labels  -> 用 PEFT 包装模型  -> 创建 Trainer 训练循环</code></pre><h2 id="18-确认可训练参数"><a href="#18-确认可训练参数" class="headerlink" title="18. 确认可训练参数"></a>18. 确认可训练参数</h2><pre class="language-python" data-language="python"><code class="language-python">trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>print_trainable_parameters<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>预期看到：</p><pre class="language-text" data-language="text"><code class="language-text">trainable params: 较小数值all params: Base Model + Adaptertrainable%: 很小比例</code></pre><p>还可以手动统计：</p><pre class="language-python" data-language="python"><code class="language-python">trainable <span class="token operator">=</span> <span class="token builtin">sum</span><span class="token punctuation">(</span>    parameter<span class="token punctuation">.</span>numel<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">for</span> parameter <span class="token keyword">in</span> trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> parameter<span class="token punctuation">.</span>requires_grad<span class="token punctuation">)</span>total <span class="token operator">=</span> <span class="token builtin">sum</span><span class="token punctuation">(</span>    parameter<span class="token punctuation">.</span>numel<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">for</span> parameter <span class="token keyword">in</span> trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>trainable<span class="token punctuation">,</span> total<span class="token punctuation">,</span> trainable <span class="token operator">/</span> total<span class="token punctuation">)</span></code></pre><p>如果几乎所有参数都可训练，说明 PEFT 配置没有按预期生效。</p><h2 id="19-LoRA-为什么能减少训练显存"><a href="#19-LoRA-为什么能减少训练显存" class="headerlink" title="19. LoRA 为什么能减少训练显存"></a>19. LoRA 为什么能减少训练显存</h2><p>Full Fine-tuning 需要为大量参数保存：</p><ul><li>梯度。</li><li>AdamW 一阶矩。</li><li>AdamW 二阶矩。</li></ul><p>LoRA 冻结主干后，这些状态只为 Adapter 参数创建。</p><p>但 LoRA 不会消除所有显存：</p><pre class="language-text" data-language="text"><code class="language-text">Base Model 权重仍要加载前向激活仍然存在Attention 中间结果仍然存在部分 logits 仍然存在</code></pre><p>因此序列过长或 Batch 太大时，LoRA 仍可能 OOM。</p><p><img src="/images/nlp-blog/sft-full-vs-lora-memory.svg" alt="Full Fine-tuning与LoRA显存组成对比"></p><h2 id="20-Gradient-Checkpointing"><a href="#20-Gradient-Checkpointing" class="headerlink" title="20. Gradient Checkpointing"></a>20. Gradient Checkpointing</h2><pre class="language-python" data-language="python"><code class="language-python">gradient_checkpointing<span class="token operator">=</span><span class="token boolean">True</span></code></pre><p>它不保存全部前向激活，而是在反向传播时重新计算一部分中间结果。</p><p>交换关系是：</p><pre class="language-text" data-language="text"><code class="language-text">更少显存  &lt;- 代价 ->更多计算时间</code></pre><p>它和 LoRA 解决不同问题：</p><ul><li>LoRA 减少可训练参数与优化器状态。</li><li>Gradient Checkpointing 减少前向激活保存。</li></ul><h2 id="21-BF16-与-FP16"><a href="#21-BF16-与-FP16" class="headerlink" title="21. BF16 与 FP16"></a>21. BF16 与 FP16</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">choose_dtype</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> <span class="token keyword">not</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_available<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> torch<span class="token punctuation">.</span>float32    <span class="token keyword">if</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_bf16_supported<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> torch<span class="token punctuation">.</span>bfloat16    <span class="token keyword">return</span> torch<span class="token punctuation">.</span>float16</code></pre><p>一般而言：</p><ul><li>支持 BF16 的设备优先尝试 BF16。</li><li>较旧 GPU 可能只能高效使用 FP16。</li><li>CPU 教学检查使用 FP32 更稳妥，但训练很慢。</li></ul><p>混合精度不是简单把所有张量永久变成低精度；训练框架会对不同计算使用合适策略。</p><h2 id="22-Packing-何时开启"><a href="#22-Packing-何时开启" class="headerlink" title="22. Packing 何时开启"></a>22. Packing 何时开启</h2><p>先统计长度：</p><pre class="language-text" data-language="text"><code class="language-text">P50 = 120 tokenP90 = 220 tokenmax_length = 1024</code></pre><p>如果大多数样本很短，Packing 可以显著降低 Padding 比例。</p><pre class="language-python" data-language="python"><code class="language-python">SFTConfig<span class="token punctuation">(</span>    packing<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>但第一次训练建议关闭：</p><pre class="language-python" data-language="python"><code class="language-python">packing<span class="token operator">=</span><span class="token boolean">False</span></code></pre><p>先确认：</p><ul><li>不 Packing 时 loss 正常下降。</li><li>Assistant Mask 正确。</li><li>单条样本能被正确 decode。</li><li>验证集行为符合预期。</li></ul><p>再开启 Packing 做速度对比。</p><h2 id="23-启动训练"><a href="#23-启动训练" class="headerlink" title="23. 启动训练"></a>23. 启动训练</h2><pre class="language-python" data-language="python"><code class="language-python">trainer<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>建议重点观察：</p><ul><li><code>loss</code>。</li><li><code>eval_loss</code>。</li><li><code>learning_rate</code>。</li><li><code>grad_norm</code>。</li><li>有效 token 数。</li><li>每秒处理 token 数。</li><li>GPU 峰值显存。</li></ul><p>训练 loss 突然为 0 或 NaN 时，先检查数据与 labels，而不是立即调整优化器。</p><h2 id="24-保存-LoRA-Adapter"><a href="#24-保存-LoRA-Adapter" class="headerlink" title="24. 保存 LoRA Adapter"></a>24. 保存 LoRA Adapter</h2><pre class="language-python" data-language="python"><code class="language-python">ADAPTER_DIR <span class="token operator">=</span> <span class="token string">"outputs/sft-trl-lora/final-adapter"</span>trainer<span class="token punctuation">.</span>save_model<span class="token punctuation">(</span>ADAPTER_DIR<span class="token punctuation">)</span>trainer<span class="token punctuation">.</span>processing_class<span class="token punctuation">.</span>save_pretrained<span class="token punctuation">(</span>ADAPTER_DIR<span class="token punctuation">)</span></code></pre><p>Adapter 目录通常包含：</p><pre class="language-text" data-language="text"><code class="language-text">final-adapter/  adapter_config.json  adapter_model.safetensors  tokenizer / chat template assets  training_args.bin</code></pre><p>它通常不包含完整 Base Model 权重。</p><p>部署时需要：</p><pre class="language-text" data-language="text"><code class="language-text">原 Base Model  + LoRA Adapter  + 训练时对应 Tokenizer / Chat Template</code></pre><p><img src="/images/nlp-blog/sft-lora-save-load-merge.svg" alt="LoRA Adapter保存、加载与合并"></p><h2 id="25-重新加载-Adapter"><a href="#25-重新加载-Adapter" class="headerlink" title="25. 重新加载 Adapter"></a>25. 重新加载 Adapter</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> peft <span class="token keyword">import</span> PeftModel<span class="token keyword">from</span> transformers <span class="token keyword">import</span> AutoModelForCausalLM<span class="token punctuation">,</span> AutoTokenizertokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>ADAPTER_DIR<span class="token punctuation">)</span>base_model <span class="token operator">=</span> AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    MODEL_ID<span class="token punctuation">,</span>    dtype<span class="token operator">=</span>dtype<span class="token punctuation">,</span><span class="token punctuation">)</span>model <span class="token operator">=</span> PeftModel<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    base_model<span class="token punctuation">,</span>    ADAPTER_DIR<span class="token punctuation">,</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>必须保证：</p><ul><li><code>MODEL_ID</code> 与训练 Adapter 时的 Base Model 一致。</li><li>Tokenizer 使用训练后保存的版本。</li><li>特殊 token 数量与 embedding 对齐。</li><li>Chat Template 与训练时一致。</li></ul><h2 id="26-使用-Chat-Template-推理"><a href="#26-使用-Chat-Template-推理" class="headerlink" title="26. 使用 Chat Template 推理"></a>26. 使用 Chat Template 推理</h2><p>推理数据不包含标准答案：</p><pre class="language-python" data-language="python"><code class="language-python">messages <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"system"</span><span class="token punctuation">,</span>        <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"你是一名耐心、准确的机器学习助教。"</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span>        <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"请解释训练集和验证集的区别。"</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span></code></pre><p>构造模型输入：</p><pre class="language-python" data-language="python"><code class="language-python">inputs <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>apply_chat_template<span class="token punctuation">(</span>    messages<span class="token punctuation">,</span>    tokenize<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    add_generation_prompt<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    return_dict<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    return_tensors<span class="token operator">=</span><span class="token string">"pt"</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">.</span>to<span class="token punctuation">(</span>model<span class="token punctuation">.</span>device<span class="token punctuation">)</span></code></pre><p>训练时使用 <code>add_generation_prompt=False</code>，因为标准 assistant 答案已经存在；推理时使用 <code>True</code>，提示模型开始 assistant turn。</p><h2 id="27-GenerationConfig-与生成"><a href="#27-GenerationConfig-与生成" class="headerlink" title="27. GenerationConfig 与生成"></a>27. GenerationConfig 与生成</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> GenerationConfiggeneration_config <span class="token operator">=</span> GenerationConfig<span class="token punctuation">(</span>    max_new_tokens<span class="token operator">=</span><span class="token number">128</span><span class="token punctuation">,</span>    do_sample<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    temperature<span class="token operator">=</span><span class="token number">0.8</span><span class="token punctuation">,</span>    top_p<span class="token operator">=</span><span class="token number">0.9</span><span class="token punctuation">,</span>    pad_token_id<span class="token operator">=</span>tokenizer<span class="token punctuation">.</span>pad_token_id<span class="token punctuation">,</span>    eos_token_id<span class="token operator">=</span>tokenizer<span class="token punctuation">.</span>eos_token_id<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>生成：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    output_ids <span class="token operator">=</span> model<span class="token punctuation">.</span>generate<span class="token punctuation">(</span>        <span class="token operator">**</span>inputs<span class="token punctuation">,</span>        generation_config<span class="token operator">=</span>generation_config<span class="token punctuation">,</span>    <span class="token punctuation">)</span>prompt_length <span class="token operator">=</span> inputs<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span><span class="token punctuation">.</span>shape<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span>new_tokens <span class="token operator">=</span> output_ids<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">,</span> prompt_length<span class="token punctuation">:</span><span class="token punctuation">]</span>answer <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>decode<span class="token punctuation">(</span>new_tokens<span class="token punctuation">,</span> skip_special_tokens<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>answer<span class="token punctuation">)</span></code></pre><p>SFT 更新了模型参数；<code>GenerationConfig</code> 决定如何从新 logits 分布中选择 token。两者仍然属于不同层次。</p><h2 id="28-可选：合并-Adapter"><a href="#28-可选：合并-Adapter" class="headerlink" title="28. 可选：合并 Adapter"></a>28. 可选：合并 Adapter</h2><p>如果推理框架不方便分别加载 Base Model 和 Adapter，可以合并：</p><pre class="language-python" data-language="python"><code class="language-python">merged_model <span class="token operator">=</span> model<span class="token punctuation">.</span>merge_and_unload<span class="token punctuation">(</span><span class="token punctuation">)</span>merged_model<span class="token punctuation">.</span>save_pretrained<span class="token punctuation">(</span>    <span class="token string">"outputs/sft-trl-lora/merged-model"</span><span class="token punctuation">,</span>    safe_serialization<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span>tokenizer<span class="token punctuation">.</span>save_pretrained<span class="token punctuation">(</span>    <span class="token string">"outputs/sft-trl-lora/merged-model"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>合并后：</p><pre class="language-text" data-language="text"><code class="language-text">W_merged = W_base + Delta_W_lora</code></pre><p>优点：</p><ul><li>推理时只加载一个模型目录。</li><li>不需要额外 Adapter 路由。</li></ul><p>代价：</p><ul><li>失去 Adapter 小体积优势。</li><li>每个任务可能都要保存一份完整模型。</li><li>不再方便动态切换多个 Adapter。</li></ul><p>因此完整脚本默认不自动合并，必须显式开启开关。</p><h2 id="29-QLoRA-与本篇-LoRA-有什么区别"><a href="#29-QLoRA-与本篇-LoRA-有什么区别" class="headerlink" title="29. QLoRA 与本篇 LoRA 有什么区别"></a>29. QLoRA 与本篇 LoRA 有什么区别</h2><p>本篇：</p><pre class="language-text" data-language="text"><code class="language-text">低精度或普通精度 Base Model  + LoRA Adapter</code></pre><p>QLoRA 通常进一步：</p><pre class="language-text" data-language="text"><code class="language-text">4-bit 量化 Base Model  + 可训练 LoRA Adapter</code></pre><p>它进一步降低 Base Model 权重显存，但会增加：</p><ul><li>量化配置。</li><li>bitsandbytes 等依赖。</li><li>设备兼容问题。</li><li>量化 dtype 与计算 dtype 的选择。</li><li>保存、加载和合并复杂度。</li></ul><p>在 LoRA 流程完全正确前，不建议一开始就叠加 QLoRA。</p><p>下一篇会把这里展开到量化块的具体计算、NF4、Double Quantization、Paged Optimizer 与完整 Hugging Face 实现：</p><p><a href="https://lsworl.github.io/2026/09/04/nlp-qlora-4bit-finetuning/">SFT（四）：QLoRA 的 4-bit 量化与 LoRA 微调原理</a></p><h2 id="30-完整代码"><a href="#30-完整代码" class="headerlink" title="30. 完整代码"></a>30. 完整代码</h2><p>完整代码位于：</p><pre class="language-text" data-language="text"><code class="language-text">source/code/nlp-blog/sft_trl_lora.py</code></pre><p>运行：</p><pre class="language-bash" data-language="bash"><code class="language-bash">python source/code/nlp-blog/sft_trl_lora.py</code></pre><p>脚本会执行：</p><pre class="language-text" data-language="text"><code class="language-text">创建 messages Dataset  -> 校验角色与内容  -> 创建 SFTConfig  -> 创建 LoraConfig  -> SFTTrainer 自动应用 Chat Template  -> Assistant-only loss  -> 训练与验证  -> 保存 Adapter 与 Tokenizer  -> 重新加载 Base + Adapter  -> apply_chat_template  -> GenerationConfig  -> generate  -> 可选 merge_and_unload</code></pre><h2 id="31-怎样检查-Assistant-Mask-是否真的正确"><a href="#31-怎样检查-Assistant-Mask-是否真的正确" class="headerlink" title="31. 怎样检查 Assistant Mask 是否真的正确"></a>31. 怎样检查 Assistant Mask 是否真的正确</h2><p>不同版本的内部处理接口可能变化，因此不要依赖私有属性。</p><p>可以采用三层检查。</p><h3 id="31-1-数据层"><a href="#31-1-数据层" class="headerlink" title="31.1 数据层"></a>31.1 数据层</h3><p>打印原始 <code>messages</code>，确认角色和内容。</p><h3 id="31-2-模板层"><a href="#31-2-模板层" class="headerlink" title="31.2 模板层"></a>31.2 模板层</h3><pre class="language-python" data-language="python"><code class="language-python">formatted <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>apply_chat_template<span class="token punctuation">(</span>    messages<span class="token punctuation">,</span>    tokenize<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    add_generation_prompt<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>formatted<span class="token punctuation">)</span></code></pre><p>确认 user、assistant 和 turn-end token 都在正确位置。</p><h3 id="31-3-训练行为层"><a href="#31-3-训练行为层" class="headerlink" title="31.3 训练行为层"></a>31.3 训练行为层</h3><p>构造最小数据集并比较：</p><ul><li><code>assistant_only_loss=False</code>。</li><li><code>assistant_only_loss=True</code>。</li></ul><p>观察有效 token 数、loss 和显存是否符合预期。</p><p>如果需要逐 token 审计，可以预先使用 Chat Template 生成 <code>input_ids</code> 与 assistant mask，构造预 Tokenized Dataset，再把 <code>labels=-100</code> 的位置 decode 出来检查。</p><h2 id="32-常见错误"><a href="#32-常见错误" class="headerlink" title="32. 常见错误"></a>32. 常见错误</h2><h3 id="32-1-把-prompt-only-数据传给-SFTTrainer"><a href="#32-1-把-prompt-only-数据传给-SFTTrainer" class="headerlink" title="32.1 把 prompt-only 数据传给 SFTTrainer"></a>32.1 把 prompt-only 数据传给 SFTTrainer</h3><p>SFT 需要目标文本。只有 Prompt 没有 Completion，无法构造标准监督答案。</p><h3 id="32-2-使用了错误-Chat-Template"><a href="#32-2-使用了错误-Chat-Template" class="headerlink" title="32.2 使用了错误 Chat Template"></a>32.2 使用了错误 Chat Template</h3><p>模型可能继续用户消息、输出错误控制 token 或无法停止。</p><h3 id="32-3-训练时-add-generation-prompt-x3D-True"><a href="#32-3-训练时-add-generation-prompt-x3D-True" class="headerlink" title="32.3 训练时 add_generation_prompt&#x3D;True"></a>32.3 训练时 add_generation_prompt&#x3D;True</h3><p>标准 assistant 答案已经在数据中，额外的生成起始标记可能制造错误尾部。</p><h3 id="32-4-assistant-only-loss-x3D-True-但模板不支持-Assistant-Mask"><a href="#32-4-assistant-only-loss-x3D-True-但模板不支持-Assistant-Mask" class="headerlink" title="32.4 assistant_only_loss&#x3D;True 但模板不支持 Assistant Mask"></a>32.4 assistant_only_loss&#x3D;True 但模板不支持 Assistant Mask</h3><p>参数名存在不代表模板能够正确标记区域。检查当前官方支持条件。</p><h3 id="32-5-EOS-与模板结束-token-不一致"><a href="#32-5-EOS-与模板结束-token-不一致" class="headerlink" title="32.5 EOS 与模板结束 token 不一致"></a>32.5 EOS 与模板结束 token 不一致</h3><p>模型可能无法在预期位置停止。</p><h3 id="32-6-LoRA-target-modules-不匹配"><a href="#32-6-LoRA-target-modules-不匹配" class="headerlink" title="32.6 LoRA target_modules 不匹配"></a>32.6 LoRA target_modules 不匹配</h3><p>可能没有插入 Adapter，或插入范围与预期不同。打印模型和可训练参数。</p><h3 id="32-7-误以为-LoRA-不需要-Base-Model"><a href="#32-7-误以为-LoRA-不需要-Base-Model" class="headerlink" title="32.7 误以为 LoRA 不需要 Base Model"></a>32.7 误以为 LoRA 不需要 Base Model</h3><p>未合并的 Adapter 只保存增量参数。</p><h3 id="32-8-保存-Adapter-却遗漏-Tokenizer"><a href="#32-8-保存-Adapter-却遗漏-Tokenizer" class="headerlink" title="32.8 保存 Adapter 却遗漏 Tokenizer"></a>32.8 保存 Adapter 却遗漏 Tokenizer</h3><p>Chat Template 和特殊 token 可能随 Tokenizer 保存，必须一起保留。</p><h3 id="32-9-eval-loss-下降就认为回答变好"><a href="#32-9-eval-loss-下降就认为回答变好" class="headerlink" title="32.9 eval_loss 下降就认为回答变好"></a>32.9 eval_loss 下降就认为回答变好</h3><p>仍需生成评估、任务指标和人工检查。</p><h3 id="32-10-小数据训练太多-Epoch"><a href="#32-10-小数据训练太多-Epoch" class="headerlink" title="32.10 小数据训练太多 Epoch"></a>32.10 小数据训练太多 Epoch</h3><p>模型可能记住答案，同时破坏原有通用能力。</p><h3 id="32-11-随意开启所有显存优化"><a href="#32-11-随意开启所有显存优化" class="headerlink" title="32.11 随意开启所有显存优化"></a>32.11 随意开启所有显存优化</h3><p>Packing、Gradient Checkpointing、量化、Liger、Flash Attention 和特殊 Loss 实现可能存在组合限制。一次只增加一个变量并验证。</p><h3 id="32-12-用测试集调整超参数"><a href="#32-12-用测试集调整超参数" class="headerlink" title="32.12 用测试集调整超参数"></a>32.12 用测试集调整超参数</h3><p>验证集用于选择配置，测试集只用于最终评估。</p><h2 id="33-一个可靠的-SFT-实验记录应包含什么"><a href="#33-一个可靠的-SFT-实验记录应包含什么" class="headerlink" title="33. 一个可靠的 SFT 实验记录应包含什么"></a>33. 一个可靠的 SFT 实验记录应包含什么</h2><h3 id="模型"><a href="#模型" class="headerlink" title="模型"></a>模型</h3><pre class="language-text" data-language="text"><code class="language-text">model_idrevision / commit hasharchitectureparameter counttokenizer revisionchat template</code></pre><h3 id="数据"><a href="#数据" class="headerlink" title="数据"></a>数据</h3><pre class="language-text" data-language="text"><code class="language-text">数据来源与许可证清洗规则去重规则训练/验证/测试数量token 长度分布截断比例有效 assistant token 比例</code></pre><h3 id="训练"><a href="#训练" class="headerlink" title="训练"></a>训练</h3><pre class="language-text" data-language="text"><code class="language-text">Full / LoRA / QLoRALoRA r / alpha / target moduleslearning ratebatch sizegradient accumulationmax_lengthpackingdtype训练步数随机种子</code></pre><h3 id="评估"><a href="#评估" class="headerlink" title="评估"></a>评估</h3><pre class="language-text" data-language="text"><code class="language-text">eval_loss任务指标固定生成 PromptGenerationConfig人工错误分类Base Model 对照</code></pre><h2 id="34-从-SFT-继续学习什么"><a href="#34-从-SFT-继续学习什么" class="headerlink" title="34. 从 SFT 继续学习什么"></a>34. 从 SFT 继续学习什么</h2><p>完成本系列后，可以继续：</p><pre class="language-text" data-language="text"><code class="language-text">数据去重与质量评分  -> Chat Template 自定义  -> LoRA target modules 对比  -> QLoRA  -> Packing 与长度分桶  -> 生成式评估  -> 多轮对话评估  -> DPO 偏好优化  -> Reward Model  -> PPO / GRPO 等在线或强化学习方法  -> 分布式训练与推理部署</code></pre><p>其中 DPO、PPO 或 GRPO 不会替代 SFT 的基础作用。多数后训练流程仍需要一个质量足够的 SFT 模型作为起点。</p><h2 id="35-总结"><a href="#35-总结" class="headerlink" title="35. 总结"></a>35. 总结</h2><p>这一篇把手写 SFT 数据管线替换成了 TRL 与 PEFT：</p><pre class="language-text" data-language="text"><code class="language-text">messages  -> Chat Template  -> Tokenizer  -> Assistant Mask  -> labels=-100 / token ids  -> SFTTrainer  -> Causal LM Loss  -> LoRA 参数更新  -> Adapter + Tokenizer 保存  -> Base + Adapter 重新加载  -> GenerationConfig + generate</code></pre><p>我现在可以把 <code>SFTTrainer</code> 理解成：</p><blockquote><p>一个知道 SFT 常见数据格式和处理规则、并与 Transformers Trainer 和 PEFT 集成的训练器，而不是一种新的模型或新的语言模型数学目标。</p></blockquote><p>系列三篇对应三层能力：</p><ol><li><strong>原理层</strong>：知道为什么是 next-token loss，知道三个 Mask 的区别。</li><li><strong>张量层</strong>：能自己构造 <code>input_ids / attention_mask / labels</code> 并手动 forward。</li><li><strong>工程层</strong>：能使用 <code>SFTTrainer + LoRA</code>，也知道自动化边界和排错方法。</li></ol><p>真正开始项目时，最先投入精力的往往不是继续增加 Trainer 参数，而是数据质量、模板一致性和可靠评估。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://huggingface.co/docs/trl/sft_trainer">Hugging Face TRL：SFT Trainer</a></li><li><a href="https://huggingface.co/docs/trl/dataset_formats">Hugging Face TRL：Dataset formats</a></li><li><a href="https://huggingface.co/docs/trl/reducing_memory_usage">Hugging Face TRL：Reducing memory usage</a></li><li><a href="https://huggingface.co/docs/transformers/chat_templating">Hugging Face Transformers：Chat templates</a></li><li><a href="https://huggingface.co/docs/peft/main/conceptual_guides/lora">Hugging Face PEFT：LoRA conceptual guide</a></li><li><a href="https://huggingface.co/docs/peft/package_reference/lora">Hugging Face PEFT：LoraConfig</a></li><li><a href="https://huggingface.co/Qwen/Qwen3-0.6B-Base">Qwen3-0.6B-Base Model Card</a></li><li><a href="https://arxiv.org/abs/2106.09685">LoRA: Low-Rank Adaptation of Large Language Models</a></li><li><a href="https://arxiv.org/abs/2305.14314">QLoRA: Efficient Finetuning of Quantized LLMs</a></li><li><a href="https://lsworl.github.io/2026/08/24/nlp-sft-transformers-from-scratch/">上一篇：SFT（二）用Hugging Face Transformers手写监督微调</a></li></ul>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/24/nlp-sft-trl-lora/</id>
    <link href="https://lsworl.github.io/2026/08/24/nlp-sft-trl-lora/"/>
    <published>2026-08-24T15:59:00.000Z</published>
    <summary>在理解input_ids、labels与Completion-only loss后，使用Hugging Face TRL SFTTrainer、Conversational Dataset、Chat Template和PEFT LoRA完成对话SFT，并讲清assistant-only loss、Packing、显存、Adapter保存加载与推理。</summary>
    <title>SFT（三）：使用TRL SFTTrainer与LoRA微调对话模型</title>
    <updated>2026-09-03T16:49:40.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Hugging Face" scheme="https://lsworl.github.io/tags/Hugging-Face/"/>
    <category term="Transformers" scheme="https://lsworl.github.io/tags/Transformers/"/>
    <category term="Trainer" scheme="https://lsworl.github.io/tags/Trainer/"/>
    <category term="LLM" scheme="https://lsworl.github.io/tags/LLM/"/>
    <category term="SFT" scheme="https://lsworl.github.io/tags/SFT/"/>
    <content>
      <![CDATA[<p>上一篇 <a href="https://lsworl.github.io/2026/08/24/nlp-sft-principles/">SFT 原理篇</a>已经把监督微调还原成 next-token prediction：</p><pre class="language-text" data-language="text"><code class="language-text">Prompt 与标准答案  -> 一串 Causal LM token  -> Prompt 对应 labels 设为 -100  -> 模型内部进行 one-token shift  -> 只在 Answer token 上计算交叉熵</code></pre><p>这一篇不使用 TRL 的 <code>SFTTrainer</code>，而只部分的 Hugging Face 组件：</p><ul><li><code>Dataset</code>。</li><li><code>AutoTokenizer</code>。</li><li><code>AutoModelForCausalLM</code>。</li><li><code>forward()</code>。</li><li><code>Trainer</code> 与 <code>TrainingArguments</code>。</li><li><code>GenerationConfig</code> 与 <code>generate()</code>。</li></ul><p>目标不是重复造一个完善的训练框架，而是让我能亲眼看到 SFT 中最重要的三组张量：</p><pre class="language-text" data-language="text"><code class="language-text">input_idsattention_masklabels</code></pre><p>等这些张量完全对上后，下一篇再使用 <code>TRL SFTTrainer + LoRA</code>，就不会把自动化误认为魔法。</p><span id="more"></span><p><img src="/images/nlp-blog/sft-transformers-pipeline.svg" alt="原生Transformers实现SFT的完整流程"></p><h2 id="1-本篇要训练什么"><a href="#1-本篇要训练什么" class="headerlink" title="1. 本篇要训练什么"></a>1. 本篇要训练什么</h2><p>我们使用一个小型 Causal Language Model，让它学习一组中文机器学习问答。</p><p>示例：</p><pre class="language-text" data-language="text"><code class="language-text">Instruction:用一句话解释什么是过拟合。Response:过拟合是模型过度记忆训练数据，导致在未见数据上泛化较差的现象。</code></pre><p>为了让流程容易检查，训练数据直接写在 Python 文件中。</p><p>这份数据只能验证代码链路，不能训练出真正可靠的领域助手。</p><p>项目目标是：</p><ol><li>加载 Base Causal LM。</li><li>构造 Prompt 与 Answer。</li><li>只对 Answer 计算 loss。</li><li>使用 <code>Trainer</code> 完成优化。</li><li>保存模型、Tokenizer 和配置。</li><li>使用 <code>GenerationConfig</code> 重新加载并生成。</li></ol><h2 id="2-为什么先不用-Chat-Template"><a href="#2-为什么先不用-Chat-Template" class="headerlink" title="2. 为什么先不用 Chat Template"></a>2. 为什么先不用 Chat Template</h2><p>真实对话模型通常应使用 Tokenizer 自带的 Chat Template。</p><p>但本篇暂时使用一个简单、显式的文本模板：</p><pre class="language-text" data-language="text"><code class="language-text">### Instruction:&#123;instruction&#125;### Response:&#123;answer&#125;&lt;eos></code></pre><p>原因是我们想直接观察：</p><pre class="language-text" data-language="text"><code class="language-text">Prompt token 到哪里结束Answer token 从哪里开始哪些 labels 应该设为 -100</code></pre><p>第三篇切换到 TRL 时，会换回标准 <code>messages + apply_chat_template()</code> 流程。</p><h2 id="3-环境准备"><a href="#3-环境准备" class="headerlink" title="3. 环境准备"></a>3. 环境准备</h2><p>安装基础依赖：</p><pre class="language-bash" data-language="bash"><code class="language-bash">pip <span class="token function">install</span> <span class="token parameter variable">-U</span> torch transformers datasets accelerate</code></pre><p>建议记录实际版本：</p><pre class="language-bash" data-language="bash"><code class="language-bash">python <span class="token parameter variable">-c</span> <span class="token string">"import torch, transformers, datasets, accelerate; print(torch.__version__); print(transformers.__version__); print(datasets.__version__); print(accelerate.__version__)"</span></code></pre><p>Hugging Face API 更新较快。本文代码使用当前文档中的参数名，例如：</p><ul><li><code>eval_strategy</code>。</li><li><code>processing_class=tokenizer</code>。</li><li><code>dtype=...</code>。</li></ul><p>如果使用旧版本，应查看对应版本文档，不要混用不同年代的代码片段。</p><h2 id="4-选择-Base-Model"><a href="#4-选择-Base-Model" class="headerlink" title="4. 选择 Base Model"></a>4. 选择 Base Model</h2><p>教学代码使用：</p><pre class="language-python" data-language="python"><code class="language-python">MODEL_ID <span class="token operator">=</span> <span class="token string">"Qwen/Qwen2.5-0.5B"</span></code></pre><p>它是 Causal Language Model，可以通过：</p><pre class="language-python" data-language="python"><code class="language-python">AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>MODEL_ID<span class="token punctuation">)</span></code></pre><p>加载。</p><p>这里选择 Base Model 是为了观察 SFT 前后行为变化，而不是声称 0.5B 参数足以完成复杂任务。</p><p>实际选择模型时还应检查：</p><ul><li>模型许可证。</li><li>支持语言。</li><li>参数量和显存需求。</li><li>上下文长度。</li><li>Tokenizer 与 Chat Template。</li><li><code>transformers</code> 最低版本。</li><li>Base 或 Instruct 训练阶段。</li></ul><h2 id="5-构造本地监督数据"><a href="#5-构造本地监督数据" class="headerlink" title="5. 构造本地监督数据"></a>5. 构造本地监督数据</h2><p>先定义若干 <code>instruction / response</code> 对：</p><pre class="language-python" data-language="python"><code class="language-python">TRAIN_EXAMPLES <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"instruction"</span><span class="token punctuation">:</span> <span class="token string">"用一句话解释什么是过拟合。"</span><span class="token punctuation">,</span>        <span class="token string">"response"</span><span class="token punctuation">:</span> <span class="token string">"过拟合是模型过度记忆训练数据，导致在未见数据上泛化较差的现象。"</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"instruction"</span><span class="token punctuation">:</span> <span class="token string">"梯度下降的作用是什么？"</span><span class="token punctuation">,</span>        <span class="token string">"response"</span><span class="token punctuation">:</span> <span class="token string">"梯度下降通过沿损失函数负梯度方向更新参数，使模型损失逐步减小。"</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span></code></pre><p>再转换成 <code>Dataset</code>：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> datasets <span class="token keyword">import</span> Dataset<span class="token punctuation">,</span> DatasetDictdataset <span class="token operator">=</span> DatasetDict<span class="token punctuation">(</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"train"</span><span class="token punctuation">:</span> Dataset<span class="token punctuation">.</span>from_list<span class="token punctuation">(</span>TRAIN_EXAMPLES<span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"validation"</span><span class="token punctuation">:</span> Dataset<span class="token punctuation">.</span>from_list<span class="token punctuation">(</span>VALIDATION_EXAMPLES<span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">)</span></code></pre><p>训练、验证和测试集必须按真实任务划分。</p><p>不能把训练问题只改一个标点后放入验证集，否则评估接近记忆测试。</p><h2 id="6-定义-Prompt-模板"><a href="#6-定义-Prompt-模板" class="headerlink" title="6. 定义 Prompt 模板"></a>6. 定义 Prompt 模板</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">format_prompt</span><span class="token punctuation">(</span>instruction<span class="token punctuation">:</span> <span class="token builtin">str</span><span class="token punctuation">)</span> <span class="token operator">-</span><span class="token operator">></span> <span class="token builtin">str</span><span class="token punctuation">:</span>    <span class="token keyword">return</span> <span class="token punctuation">(</span>        <span class="token string">"### Instruction:\n"</span>        <span class="token string-interpolation"><span class="token string">f"</span><span class="token interpolation"><span class="token punctuation">&#123;</span>instruction<span class="token punctuation">.</span>strip<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">&#125;</span></span><span class="token string">\n\n"</span></span>        <span class="token string">"### Response:\n"</span>    <span class="token punctuation">)</span></code></pre><p>注意返回文本停在 <code>Response:</code> 后面，不包含标准答案。</p><p>例如：</p><pre class="language-python" data-language="python"><code class="language-python">prompt <span class="token operator">=</span> format_prompt<span class="token punctuation">(</span><span class="token string">"梯度下降的作用是什么？"</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>prompt<span class="token punctuation">)</span></code></pre><p>结果：</p><pre class="language-text" data-language="text"><code class="language-text">### Instruction:梯度下降的作用是什么？### Response:</code></pre><p>标准答案单独构造：</p><pre class="language-python" data-language="python"><code class="language-python">answer <span class="token operator">=</span> response<span class="token punctuation">.</span>strip<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> tokenizer<span class="token punctuation">.</span>eos_token</code></pre><p>这样 Prompt 与 Answer 的边界在代码中是明确的。</p><h2 id="7-加载-Tokenizer"><a href="#7-加载-Tokenizer" class="headerlink" title="7. 加载 Tokenizer"></a>7. 加载 Tokenizer</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> AutoTokenizertokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>MODEL_ID<span class="token punctuation">)</span>tokenizer<span class="token punctuation">.</span>padding_side <span class="token operator">=</span> <span class="token string">"right"</span><span class="token keyword">if</span> tokenizer<span class="token punctuation">.</span>pad_token_id <span class="token keyword">is</span> <span class="token boolean">None</span><span class="token punctuation">:</span>    tokenizer<span class="token punctuation">.</span>pad_token <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>eos_token</code></pre><h3 id="7-1-为什么训练时使用右-Padding"><a href="#7-1-为什么训练时使用右-Padding" class="headerlink" title="7.1 为什么训练时使用右 Padding"></a>7.1 为什么训练时使用右 Padding</h3><p>右 Padding 后：</p><pre class="language-text" data-language="text"><code class="language-text">真实 token ... EOS PAD PAD PAD</code></pre><p>这与多数训练 collator 的处理方式一致，也便于观察标签。</p><p>生成时批量处理不同长度 Prompt，有些 Decoder-only 模型更适合左 Padding。训练和生成可根据模型要求分别设置，不要只凭一个固定习惯。</p><h3 id="7-2-为什么要确认-EOS"><a href="#7-2-为什么要确认-EOS" class="headerlink" title="7.2 为什么要确认 EOS"></a>7.2 为什么要确认 EOS</h3><p>如果答案末尾没有结束 token，模型可能只学习“怎样开始回答”，却没有明确学习“何时结束”。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">assert</span> tokenizer<span class="token punctuation">.</span>eos_token_id <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token boolean">None</span></code></pre><h2 id="8-Prompt-与-Answer-为什么分开-Tokenize"><a href="#8-Prompt-与-Answer-为什么分开-Tokenize" class="headerlink" title="8. Prompt 与 Answer 为什么分开 Tokenize"></a>8. Prompt 与 Answer 为什么分开 Tokenize</h2><p>我们需要知道回答 token 从哪里开始。</p><pre class="language-python" data-language="python"><code class="language-python">prompt_ids <span class="token operator">=</span> tokenizer<span class="token punctuation">(</span>    prompt<span class="token punctuation">,</span>    add_special_tokens<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    truncation<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span>answer_ids <span class="token operator">=</span> tokenizer<span class="token punctuation">(</span>    answer<span class="token punctuation">,</span>    add_special_tokens<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    truncation<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span></code></pre><p>只在 Prompt 侧增加模型默认起始特殊 token，Answer 侧不重复增加。</p><p>然后拼接：</p><pre class="language-python" data-language="python"><code class="language-python">input_ids <span class="token operator">=</span> prompt_ids <span class="token operator">+</span> answer_ids</code></pre><p>如果把 Prompt 与 Answer 各自都设置 <code>add_special_tokens=True</code>，中间可能重复插入 BOS 或其他边界 token。</p><h3 id="8-1-为什么不直接用-len-tokenizer-prompt-切完整序列"><a href="#8-1-为什么不直接用-len-tokenizer-prompt-切完整序列" class="headerlink" title="8.1 为什么不直接用 len(tokenizer(prompt)) 切完整序列"></a>8.1 为什么不直接用 len(tokenizer(prompt)) 切完整序列</h3><p>某些子词 Tokenizer 的结果受字符串边界影响。</p><p>下面两种方法未必严格等价：</p><pre class="language-python" data-language="python"><code class="language-python">tokenizer<span class="token punctuation">(</span>prompt <span class="token operator">+</span> answer<span class="token punctuation">)</span></code></pre><pre class="language-python" data-language="python"><code class="language-python">tokenizer<span class="token punctuation">(</span>prompt<span class="token punctuation">)</span> <span class="token operator">+</span> tokenizer<span class="token punctuation">(</span>answer<span class="token punctuation">)</span></code></pre><p>本篇模板在 Prompt 末尾保留清晰换行，并显式分段编码，目的是得到稳定边界。</p><p>生产系统如果必须保持“整串文本一次分词”的精确结果，可以使用 offset mapping、模型 Chat Template 返回的 assistant mask，或 TRL 已实现的数据处理路径。</p><h2 id="9-构造-Completion-only-Labels"><a href="#9-构造-Completion-only-Labels" class="headerlink" title="9. 构造 Completion-only Labels"></a>9. 构造 Completion-only Labels</h2><p>Prompt 不进入 loss：</p><pre class="language-python" data-language="python"><code class="language-python">labels <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">100</span><span class="token punctuation">]</span> <span class="token operator">*</span> <span class="token builtin">len</span><span class="token punctuation">(</span>prompt_ids<span class="token punctuation">)</span> <span class="token operator">+</span> answer_ids<span class="token punctuation">.</span>copy<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>因此：</p><pre class="language-text" data-language="text"><code class="language-text">input_ids:[Prompt tokens........................][Answer tokens......]labels:[-100, -100, -100, ...................][Answer token ids...]</code></pre><p><code>input_ids</code> 和 <code>labels</code> 必须等长：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">assert</span> <span class="token builtin">len</span><span class="token punctuation">(</span>input_ids<span class="token punctuation">)</span> <span class="token operator">==</span> <span class="token builtin">len</span><span class="token punctuation">(</span>labels<span class="token punctuation">)</span></code></pre><p>模型仍能看到 Prompt，只是不会因“没有预测好 Prompt 自身”受到惩罚。</p><p><img src="/images/nlp-blog/sft-feature-construction.svg" alt="Prompt与Answer如何变成SFT张量"></p><h2 id="10-截断时优先保留回答"><a href="#10-截断时优先保留回答" class="headerlink" title="10. 截断时优先保留回答"></a>10. 截断时优先保留回答</h2><p>最简单的：</p><pre class="language-python" data-language="python"><code class="language-python">input_ids <span class="token operator">=</span> input_ids<span class="token punctuation">[</span><span class="token punctuation">:</span>max_length<span class="token punctuation">]</span>labels <span class="token operator">=</span> labels<span class="token punctuation">[</span><span class="token punctuation">:</span>max_length<span class="token punctuation">]</span></code></pre><p>可能把回答完全截掉。</p><p>本篇采用一个明确策略：</p><ol><li>为答案预留空间。</li><li>Prompt 太长时从左侧截断 Prompt。</li><li>答案仍太长时截断答案。</li><li>尽量让最后一个答案 token 保持为 EOS。</li></ol><p>示例实现：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">truncate_prompt_and_answer</span><span class="token punctuation">(</span>prompt_ids<span class="token punctuation">,</span> answer_ids<span class="token punctuation">,</span> max_length<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> <span class="token builtin">len</span><span class="token punctuation">(</span>answer_ids<span class="token punctuation">)</span> <span class="token operator">>=</span> max_length<span class="token punctuation">:</span>        answer_ids <span class="token operator">=</span> answer_ids<span class="token punctuation">[</span><span class="token punctuation">:</span>max_length<span class="token punctuation">]</span>        answer_ids<span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span> <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>eos_token_id        <span class="token keyword">return</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span> answer_ids    prompt_budget <span class="token operator">=</span> max_length <span class="token operator">-</span> <span class="token builtin">len</span><span class="token punctuation">(</span>answer_ids<span class="token punctuation">)</span>    prompt_ids <span class="token operator">=</span> prompt_ids<span class="token punctuation">[</span><span class="token operator">-</span>prompt_budget<span class="token punctuation">:</span><span class="token punctuation">]</span>    <span class="token keyword">return</span> prompt_ids<span class="token punctuation">,</span> answer_ids</code></pre><p>这适合“回答是主要监督目标”的教学问答。</p><p>但它并不是所有任务的通用答案。如果 Prompt 中最前面的 system rule 不能删除，就要采用更精细的分段截断。</p><h2 id="11-完整预处理函数"><a href="#11-完整预处理函数" class="headerlink" title="11. 完整预处理函数"></a>11. 完整预处理函数</h2><pre class="language-python" data-language="python"><code class="language-python">MAX_LENGTH <span class="token operator">=</span> <span class="token number">256</span><span class="token keyword">def</span> <span class="token function">tokenize_example</span><span class="token punctuation">(</span>example<span class="token punctuation">)</span><span class="token punctuation">:</span>    prompt <span class="token operator">=</span> format_prompt<span class="token punctuation">(</span>example<span class="token punctuation">[</span><span class="token string">"instruction"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>    answer <span class="token operator">=</span> example<span class="token punctuation">[</span><span class="token string">"response"</span><span class="token punctuation">]</span><span class="token punctuation">.</span>strip<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> tokenizer<span class="token punctuation">.</span>eos_token    prompt_ids <span class="token operator">=</span> tokenizer<span class="token punctuation">(</span>        prompt<span class="token punctuation">,</span>        add_special_tokens<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>        truncation<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span><span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span>    answer_ids <span class="token operator">=</span> tokenizer<span class="token punctuation">(</span>        answer<span class="token punctuation">,</span>        add_special_tokens<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>        truncation<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span><span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span>    prompt_ids<span class="token punctuation">,</span> answer_ids <span class="token operator">=</span> truncate_prompt_and_answer<span class="token punctuation">(</span>        prompt_ids<span class="token punctuation">,</span>        answer_ids<span class="token punctuation">,</span>        MAX_LENGTH<span class="token punctuation">,</span>    <span class="token punctuation">)</span>    input_ids <span class="token operator">=</span> prompt_ids <span class="token operator">+</span> answer_ids    labels <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">100</span><span class="token punctuation">]</span> <span class="token operator">*</span> <span class="token builtin">len</span><span class="token punctuation">(</span>prompt_ids<span class="token punctuation">)</span> <span class="token operator">+</span> answer_ids<span class="token punctuation">.</span>copy<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">return</span> <span class="token punctuation">&#123;</span>        <span class="token string">"input_ids"</span><span class="token punctuation">:</span> input_ids<span class="token punctuation">,</span>        <span class="token string">"attention_mask"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span> <span class="token operator">*</span> <span class="token builtin">len</span><span class="token punctuation">(</span>input_ids<span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"labels"</span><span class="token punctuation">:</span> labels<span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span></code></pre><p>调用 <code>map()</code>：</p><pre class="language-python" data-language="python"><code class="language-python">tokenized_dataset <span class="token operator">=</span> dataset<span class="token punctuation">.</span><span class="token builtin">map</span><span class="token punctuation">(</span>    tokenize_example<span class="token punctuation">,</span>    remove_columns<span class="token operator">=</span>dataset<span class="token punctuation">[</span><span class="token string">"train"</span><span class="token punctuation">]</span><span class="token punctuation">.</span>column_names<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>由于每条样本长度不同，此时先不 Padding。</p><h2 id="12-必须检查一条-Tokenized-样本"><a href="#12-必须检查一条-Tokenized-样本" class="headerlink" title="12. 必须检查一条 Tokenized 样本"></a>12. 必须检查一条 Tokenized 样本</h2><p>在启动训练前，至少打印一条样本：</p><pre class="language-python" data-language="python"><code class="language-python">sample <span class="token operator">=</span> tokenized_dataset<span class="token punctuation">[</span><span class="token string">"train"</span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"length:"</span><span class="token punctuation">,</span> <span class="token builtin">len</span><span class="token punctuation">(</span>sample<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"input:"</span><span class="token punctuation">,</span> tokenizer<span class="token punctuation">.</span>decode<span class="token punctuation">(</span>sample<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">)</span>target_ids <span class="token operator">=</span> <span class="token punctuation">[</span>    token_id    <span class="token keyword">for</span> token_id<span class="token punctuation">,</span> label <span class="token keyword">in</span> <span class="token builtin">zip</span><span class="token punctuation">(</span>sample<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span><span class="token punctuation">,</span> sample<span class="token punctuation">[</span><span class="token string">"labels"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> label <span class="token operator">!=</span> <span class="token operator">-</span><span class="token number">100</span><span class="token punctuation">]</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"target:"</span><span class="token punctuation">,</span> tokenizer<span class="token punctuation">.</span>decode<span class="token punctuation">(</span>target_ids<span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>预期：</p><pre class="language-text" data-language="text"><code class="language-text">input:### Instruction:用一句话解释什么是过拟合。### Response:过拟合是……&lt;eos>target:过拟合是……&lt;eos></code></pre><p>还要检查：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">assert</span> <span class="token builtin">len</span><span class="token punctuation">(</span>sample<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token operator">==</span> <span class="token builtin">len</span><span class="token punctuation">(</span>sample<span class="token punctuation">[</span><span class="token string">"labels"</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">assert</span> <span class="token builtin">any</span><span class="token punctuation">(</span>label <span class="token operator">!=</span> <span class="token operator">-</span><span class="token number">100</span> <span class="token keyword">for</span> label <span class="token keyword">in</span> sample<span class="token punctuation">[</span><span class="token string">"labels"</span><span class="token punctuation">]</span><span class="token punctuation">)</span></code></pre><p>如果整条 <code>labels</code> 都是 <code>-100</code>，交叉熵没有有效目标，训练会得到无意义 loss 或 NaN。</p><h2 id="13-为什么不在-map-时-Padding-到-max-length"><a href="#13-为什么不在-map-时-Padding-到-max-length" class="headerlink" title="13. 为什么不在 map() 时 Padding 到 max_length"></a>13. 为什么不在 map() 时 Padding 到 max_length</h2><p>假设三条样本长度是：</p><pre class="language-text" data-language="text"><code class="language-text">61, 78, 93</code></pre><p>直接全部 Padding 到 256 会浪费大量计算。</p><p>更合理的是在每个 batch 中动态补到当前最长长度：</p><pre class="language-text" data-language="text"><code class="language-text">Batch 1: [61, 78] -> Padding 到 78Batch 2: [93, 70] -> Padding 到 93</code></pre><p>因此 <code>map()</code> 只负责：</p><ul><li>Tokenize。</li><li>截断。</li><li>构造 labels。</li></ul><p>Data Collator 再负责：</p><ul><li>当前 batch 的 Padding。</li><li>列表转 PyTorch 张量。</li></ul><h2 id="14-手写-Completion-only-Data-Collator"><a href="#14-手写-Completion-only-Data-Collator" class="headerlink" title="14. 手写 Completion-only Data Collator"></a>14. 手写 Completion-only Data Collator</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> dataclasses <span class="token keyword">import</span> dataclass<span class="token keyword">import</span> torch<span class="token decorator annotation punctuation">@dataclass</span><span class="token keyword">class</span> <span class="token class-name">CompletionOnlyCollator</span><span class="token punctuation">:</span>    pad_token_id<span class="token punctuation">:</span> <span class="token builtin">int</span>    pad_to_multiple_of<span class="token punctuation">:</span> <span class="token builtin">int</span> <span class="token operator">|</span> <span class="token boolean">None</span> <span class="token operator">=</span> <span class="token number">8</span>    <span class="token keyword">def</span> <span class="token function">__call__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> features<span class="token punctuation">)</span><span class="token punctuation">:</span>        max_length <span class="token operator">=</span> <span class="token builtin">max</span><span class="token punctuation">(</span><span class="token builtin">len</span><span class="token punctuation">(</span>item<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token keyword">for</span> item <span class="token keyword">in</span> features<span class="token punctuation">)</span>        <span class="token keyword">if</span> self<span class="token punctuation">.</span>pad_to_multiple_of<span class="token punctuation">:</span>            multiple <span class="token operator">=</span> self<span class="token punctuation">.</span>pad_to_multiple_of            max_length <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token punctuation">(</span>max_length <span class="token operator">+</span> multiple <span class="token operator">-</span> <span class="token number">1</span><span class="token punctuation">)</span> <span class="token operator">//</span> multiple<span class="token punctuation">)</span> <span class="token operator">*</span> multiple        batch_input_ids <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>        batch_attention_mask <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>        batch_labels <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>        <span class="token keyword">for</span> item <span class="token keyword">in</span> features<span class="token punctuation">:</span>            pad_length <span class="token operator">=</span> max_length <span class="token operator">-</span> <span class="token builtin">len</span><span class="token punctuation">(</span>item<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>            batch_input_ids<span class="token punctuation">.</span>append<span class="token punctuation">(</span>                item<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span> <span class="token operator">+</span> <span class="token punctuation">[</span>self<span class="token punctuation">.</span>pad_token_id<span class="token punctuation">]</span> <span class="token operator">*</span> pad_length            <span class="token punctuation">)</span>            batch_attention_mask<span class="token punctuation">.</span>append<span class="token punctuation">(</span>                item<span class="token punctuation">[</span><span class="token string">"attention_mask"</span><span class="token punctuation">]</span> <span class="token operator">+</span> <span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span> <span class="token operator">*</span> pad_length            <span class="token punctuation">)</span>            batch_labels<span class="token punctuation">.</span>append<span class="token punctuation">(</span>                item<span class="token punctuation">[</span><span class="token string">"labels"</span><span class="token punctuation">]</span> <span class="token operator">+</span> <span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">100</span><span class="token punctuation">]</span> <span class="token operator">*</span> pad_length            <span class="token punctuation">)</span>        <span class="token keyword">return</span> <span class="token punctuation">&#123;</span>            <span class="token string">"input_ids"</span><span class="token punctuation">:</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span>batch_input_ids<span class="token punctuation">,</span> dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">long</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            <span class="token string">"attention_mask"</span><span class="token punctuation">:</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span>batch_attention_mask<span class="token punctuation">,</span> dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">long</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            <span class="token string">"labels"</span><span class="token punctuation">:</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span>batch_labels<span class="token punctuation">,</span> dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">long</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">&#125;</span></code></pre><p>三种补充值不同：</p><table><thead><tr><th>张量</th><th align="right">Padding 值</th><th>含义</th></tr></thead><tbody><tr><td><code>input_ids</code></td><td align="right"><code>pad_token_id</code></td><td>合法词表 id</td></tr><tr><td><code>attention_mask</code></td><td align="right"><code>0</code></td><td>不作为有效上下文</td></tr><tr><td><code>labels</code></td><td align="right"><code>-100</code></td><td>不进入交叉熵</td></tr></tbody></table><p><img src="/images/nlp-blog/sft-dynamic-padding-batch.svg" alt="SFT动态Padding后的Batch张量"></p><h2 id="15-检查-Collator-输出"><a href="#15-检查-Collator-输出" class="headerlink" title="15. 检查 Collator 输出"></a>15. 检查 Collator 输出</h2><pre class="language-python" data-language="python"><code class="language-python">collator <span class="token operator">=</span> CompletionOnlyCollator<span class="token punctuation">(</span>    pad_token_id<span class="token operator">=</span>tokenizer<span class="token punctuation">.</span>pad_token_id<span class="token punctuation">,</span><span class="token punctuation">)</span>batch <span class="token operator">=</span> collator<span class="token punctuation">(</span>    <span class="token punctuation">[</span>        tokenized_dataset<span class="token punctuation">[</span><span class="token string">"train"</span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>        tokenized_dataset<span class="token punctuation">[</span><span class="token string">"train"</span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">for</span> name<span class="token punctuation">,</span> tensor <span class="token keyword">in</span> batch<span class="token punctuation">.</span>items<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>name<span class="token punctuation">,</span> tensor<span class="token punctuation">.</span>shape<span class="token punctuation">)</span></code></pre><p>可能得到：</p><pre class="language-text" data-language="text"><code class="language-text">input_ids      torch.Size([2, 80])attention_mask torch.Size([2, 80])labels         torch.Size([2, 80])</code></pre><p>三个张量形状必须一致。</p><p>还可以统计有效目标 token：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token punctuation">(</span>batch<span class="token punctuation">[</span><span class="token string">"labels"</span><span class="token punctuation">]</span> <span class="token operator">!=</span> <span class="token operator">-</span><span class="token number">100</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token builtin">sum</span><span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>输出例如：</p><pre class="language-text" data-language="text"><code class="language-text">tensor([31, 28])</code></pre><p>表示两条样本分别有 31 和 28 个 token 参与 loss。</p><h2 id="16-加载-AutoModelForCausalLM"><a href="#16-加载-AutoModelForCausalLM" class="headerlink" title="16. 加载 AutoModelForCausalLM"></a>16. 加载 AutoModelForCausalLM</h2><p>根据设备选择 dtype：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">choose_dtype</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> <span class="token keyword">not</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_available<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> torch<span class="token punctuation">.</span>float32    <span class="token keyword">if</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_bf16_supported<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> torch<span class="token punctuation">.</span>bfloat16    <span class="token keyword">return</span> torch<span class="token punctuation">.</span>float16</code></pre><p>加载模型：</p><pre class="language-python" data-language="python"><code class="language-python">dtype <span class="token operator">=</span> choose_dtype<span class="token punctuation">(</span><span class="token punctuation">)</span>model <span class="token operator">=</span> AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    MODEL_ID<span class="token punctuation">,</span>    dtype<span class="token operator">=</span>dtype<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>本篇执行 Full Fine-tuning，所以默认所有参数都可训练：</p><pre class="language-python" data-language="python"><code class="language-python">trainable <span class="token operator">=</span> <span class="token builtin">sum</span><span class="token punctuation">(</span>p<span class="token punctuation">.</span>numel<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">for</span> p <span class="token keyword">in</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">if</span> p<span class="token punctuation">.</span>requires_grad<span class="token punctuation">)</span>total <span class="token operator">=</span> <span class="token builtin">sum</span><span class="token punctuation">(</span>p<span class="token punctuation">.</span>numel<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">for</span> p <span class="token keyword">in</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"trainable: </span><span class="token interpolation"><span class="token punctuation">&#123;</span>trainable<span class="token punctuation">:</span><span class="token format-spec">,</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"total: </span><span class="token interpolation"><span class="token punctuation">&#123;</span>total<span class="token punctuation">:</span><span class="token format-spec">,</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span><span class="token punctuation">)</span></code></pre><p>对 0.5B 模型进行全参数训练仍需要明显多于推理的显存。教学代码小，不代表可以在任意硬件上完整训练。</p><h2 id="17-先手动执行一次-forward"><a href="#17-先手动执行一次-forward" class="headerlink" title="17. 先手动执行一次 forward()"></a>17. 先手动执行一次 forward()</h2><p>在交给 <code>Trainer</code> 前，先验证最底层路径：</p><pre class="language-python" data-language="python"><code class="language-python">device <span class="token operator">=</span> <span class="token builtin">next</span><span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">.</span>devicebatch <span class="token operator">=</span> <span class="token punctuation">&#123;</span>name<span class="token punctuation">:</span> tensor<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span> <span class="token keyword">for</span> name<span class="token punctuation">,</span> tensor <span class="token keyword">in</span> batch<span class="token punctuation">.</span>items<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">&#125;</span>outputs <span class="token operator">=</span> model<span class="token punctuation">(</span><span class="token operator">**</span>batch<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>outputs<span class="token punctuation">.</span>logits<span class="token punctuation">.</span>shape<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>outputs<span class="token punctuation">.</span>loss<span class="token punctuation">)</span></code></pre><p>如果 batch 形状为 <code>[2, 80]</code>，词表大小为 $V$：</p><pre class="language-text" data-language="text"><code class="language-text">outputs.logits.shape = [2, 80, V]outputs.loss.shape   = []</code></pre><p><code>loss</code> 是标量。</p><h3 id="17-1-模型内部做了什么"><a href="#17-1-模型内部做了什么" class="headerlink" title="17.1 模型内部做了什么"></a>17.1 模型内部做了什么</h3><p>概念上：</p><pre class="language-python" data-language="python"><code class="language-python">shift_logits <span class="token operator">=</span> logits<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token punctuation">]</span>shift_labels <span class="token operator">=</span> labels<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">:</span><span class="token punctuation">]</span>loss <span class="token operator">=</span> cross_entropy<span class="token punctuation">(</span>    shift_logits<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> vocab_size<span class="token punctuation">)</span><span class="token punctuation">,</span>    shift_labels<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    ignore_index<span class="token operator">=</span><span class="token operator">-</span><span class="token number">100</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>因此外部传入的 <code>labels</code> 与 <code>input_ids</code> 等长即可。</p><h2 id="18-为什么训练时关闭-use-cache"><a href="#18-为什么训练时关闭-use-cache" class="headerlink" title="18. 为什么训练时关闭 use_cache"></a>18. 为什么训练时关闭 use_cache</h2><p>KV Cache 用于加速自回归生成。</p><p>训练时我们需要一次并行处理完整序列并进行反向传播，通常不需要保留生成缓存：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span>config<span class="token punctuation">.</span>use_cache <span class="token operator">=</span> <span class="token boolean">False</span></code></pre><p>使用 Gradient Checkpointing 时，<code>use_cache=True</code> 还可能产生不兼容警告。</p><p>训练完成、准备推理前再恢复：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span>config<span class="token punctuation">.</span>use_cache <span class="token operator">=</span> <span class="token boolean">True</span></code></pre><h2 id="19-TrainingArguments"><a href="#19-TrainingArguments" class="headerlink" title="19. TrainingArguments"></a>19. TrainingArguments</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> TrainingArgumentstraining_args <span class="token operator">=</span> TrainingArguments<span class="token punctuation">(</span>    output_dir<span class="token operator">=</span><span class="token string">"outputs/sft-transformers/checkpoints"</span><span class="token punctuation">,</span>    overwrite_output_dir<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    num_train_epochs<span class="token operator">=</span><span class="token number">5</span><span class="token punctuation">,</span>    per_device_train_batch_size<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">,</span>    per_device_eval_batch_size<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">,</span>    gradient_accumulation_steps<span class="token operator">=</span><span class="token number">4</span><span class="token punctuation">,</span>    learning_rate<span class="token operator">=</span><span class="token number">2e-5</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">0.01</span><span class="token punctuation">,</span>    warmup_ratio<span class="token operator">=</span><span class="token number">0.1</span><span class="token punctuation">,</span>    lr_scheduler_type<span class="token operator">=</span><span class="token string">"cosine"</span><span class="token punctuation">,</span>    logging_steps<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">,</span>    eval_strategy<span class="token operator">=</span><span class="token string">"epoch"</span><span class="token punctuation">,</span>    save_strategy<span class="token operator">=</span><span class="token string">"epoch"</span><span class="token punctuation">,</span>    load_best_model_at_end<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    metric_for_best_model<span class="token operator">=</span><span class="token string">"eval_loss"</span><span class="token punctuation">,</span>    greater_is_better<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    gradient_checkpointing<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    bf16<span class="token operator">=</span>dtype <span class="token operator">==</span> torch<span class="token punctuation">.</span>bfloat16<span class="token punctuation">,</span>    fp16<span class="token operator">=</span>dtype <span class="token operator">==</span> torch<span class="token punctuation">.</span>float16<span class="token punctuation">,</span>    report_to<span class="token operator">=</span><span class="token string">"none"</span><span class="token punctuation">,</span>    seed<span class="token operator">=</span><span class="token number">42</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="19-1-有效-Batch-Size"><a href="#19-1-有效-Batch-Size" class="headerlink" title="19.1 有效 Batch Size"></a>19.1 有效 Batch Size</h3><p>单卡时：</p><p>$$<br>B_{effective}<br>&#x3D;B_{device}\times N_{accumulation}<br>$$</p><p>这里：</p><p>$$<br>B_{effective}&#x3D;2\times4&#x3D;8<br>$$</p><p>多卡数据并行时还要乘设备数。</p><h3 id="19-2-为什么-SFT-学习率通常较小"><a href="#19-2-为什么-SFT-学习率通常较小" class="headerlink" title="19.2 为什么 SFT 学习率通常较小"></a>19.2 为什么 SFT 学习率通常较小</h3><p>模型已经有预训练参数。全参数微调如果学习率过大，可能快速破坏已有表示。</p><p><code>2e-5</code> 只是常见起点，不是固定答案。需要结合：</p><ul><li>模型大小。</li><li>数据量。</li><li>Full Fine-tuning 或 LoRA。</li><li>Batch Size。</li><li>训练步数。</li><li>验证集表现。</li></ul><h2 id="20-创建-Trainer"><a href="#20-创建-Trainer" class="headerlink" title="20. 创建 Trainer"></a>20. 创建 Trainer</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> Trainertrainer <span class="token operator">=</span> Trainer<span class="token punctuation">(</span>    model<span class="token operator">=</span>model<span class="token punctuation">,</span>    args<span class="token operator">=</span>training_args<span class="token punctuation">,</span>    train_dataset<span class="token operator">=</span>tokenized_dataset<span class="token punctuation">[</span><span class="token string">"train"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    eval_dataset<span class="token operator">=</span>tokenized_dataset<span class="token punctuation">[</span><span class="token string">"validation"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    data_collator<span class="token operator">=</span>collator<span class="token punctuation">,</span>    processing_class<span class="token operator">=</span>tokenizer<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这里的 <code>Trainer</code> 并不知道“SFT”这个名字。</p><p>它只看到：</p><pre class="language-text" data-language="text"><code class="language-text">Dataset 提供 input_ids / attention_mask / labelsCollator 组成 batchModel forward 返回 lossTrainer 对 loss 反向传播</code></pre><p>SFT 语义主要由我们的数据构造方式决定。</p><p><img src="/images/nlp-blog/sft-trainer-step.svg" alt="Trainer如何执行一次SFT更新"></p><h2 id="21-启动训练"><a href="#21-启动训练" class="headerlink" title="21. 启动训练"></a>21. 启动训练</h2><pre class="language-python" data-language="python"><code class="language-python">train_result <span class="token operator">=</span> trainer<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>train_result<span class="token punctuation">.</span>metrics<span class="token punctuation">)</span>eval_metrics <span class="token operator">=</span> trainer<span class="token punctuation">.</span>evaluate<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>eval_metrics<span class="token punctuation">)</span></code></pre><p>一次优化步骤可以展开为：</p><pre class="language-text" data-language="text"><code class="language-text">DataLoader 取样本  -> Collator 动态 Padding  -> model(**batch)  -> Causal LM logits  -> shift + ignore_index=-100  -> loss  -> backward  -> 梯度累积  -> 梯度裁剪  -> AdamW step  -> scheduler step</code></pre><h2 id="22-为什么评估时先看-eval-loss"><a href="#22-为什么评估时先看-eval-loss" class="headerlink" title="22. 为什么评估时先看 eval_loss"></a>22. 为什么评估时先看 eval_loss</h2><p>本篇数据很小，不适合构造复杂自动指标。</p><p><code>eval_loss</code> 至少能回答：</p><blockquote><p>模型对未参与更新的标准 Answer token，平均负对数似然是否下降？</p></blockquote><p>但它不能完整回答：</p><ul><li>回答是否事实正确。</li><li>是否严格遵循指令。</li><li>是否产生多余内容。</li><li>是否满足 JSON 等结构约束。</li></ul><p>真正项目还要增加生成评估和人工检查。</p><h2 id="23-保存最终模型"><a href="#23-保存最终模型" class="headerlink" title="23. 保存最终模型"></a>23. 保存最终模型</h2><pre class="language-python" data-language="python"><code class="language-python">FINAL_DIR <span class="token operator">=</span> <span class="token string">"outputs/sft-transformers/final"</span>trainer<span class="token punctuation">.</span>model<span class="token punctuation">.</span>config<span class="token punctuation">.</span>use_cache <span class="token operator">=</span> <span class="token boolean">True</span>trainer<span class="token punctuation">.</span>save_model<span class="token punctuation">(</span>FINAL_DIR<span class="token punctuation">)</span>tokenizer<span class="token punctuation">.</span>save_pretrained<span class="token punctuation">(</span>FINAL_DIR<span class="token punctuation">)</span></code></pre><p>目录中通常包括：</p><pre class="language-text" data-language="text"><code class="language-text">final/  config.json  generation_config.json  model.safetensors  tokenizer.json / vocab assets  tokenizer_config.json  special_tokens_map.json  training_args.bin</code></pre><p>实际文件名取决于模型和 Tokenizer 类型。</p><p>Full Fine-tuning 保存的是完整模型，因此目录体积接近 Base Model 权重大小。</p><h2 id="24-用-GenerationConfig-推理"><a href="#24-用-GenerationConfig-推理" class="headerlink" title="24. 用 GenerationConfig 推理"></a>24. 用 GenerationConfig 推理</h2><p>重新加载：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> GenerationConfigtokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>FINAL_DIR<span class="token punctuation">)</span>model <span class="token operator">=</span> AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    FINAL_DIR<span class="token punctuation">,</span>    dtype<span class="token operator">=</span>dtype<span class="token punctuation">,</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>构造生成配置：</p><pre class="language-python" data-language="python"><code class="language-python">generation_config <span class="token operator">=</span> GenerationConfig<span class="token punctuation">(</span>    max_new_tokens<span class="token operator">=</span><span class="token number">96</span><span class="token punctuation">,</span>    do_sample<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    pad_token_id<span class="token operator">=</span>tokenizer<span class="token punctuation">.</span>pad_token_id<span class="token punctuation">,</span>    eos_token_id<span class="token operator">=</span>tokenizer<span class="token punctuation">.</span>eos_token_id<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>生成：</p><pre class="language-python" data-language="python"><code class="language-python">prompt <span class="token operator">=</span> format_prompt<span class="token punctuation">(</span><span class="token string">"什么是学习率？"</span><span class="token punctuation">)</span>inputs <span class="token operator">=</span> tokenizer<span class="token punctuation">(</span>prompt<span class="token punctuation">,</span> return_tensors<span class="token operator">=</span><span class="token string">"pt"</span><span class="token punctuation">)</span><span class="token punctuation">.</span>to<span class="token punctuation">(</span>model<span class="token punctuation">.</span>device<span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    output_ids <span class="token operator">=</span> model<span class="token punctuation">.</span>generate<span class="token punctuation">(</span>        <span class="token operator">**</span>inputs<span class="token punctuation">,</span>        generation_config<span class="token operator">=</span>generation_config<span class="token punctuation">,</span>    <span class="token punctuation">)</span>new_tokens <span class="token operator">=</span> output_ids<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">,</span> inputs<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span><span class="token punctuation">.</span>shape<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">:</span><span class="token punctuation">]</span>answer <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>decode<span class="token punctuation">(</span>new_tokens<span class="token punctuation">,</span> skip_special_tokens<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>answer<span class="token punctuation">)</span></code></pre><p>切片非常重要：</p><pre class="language-python" data-language="python"><code class="language-python">output_ids<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">,</span> prompt_length<span class="token punctuation">:</span><span class="token punctuation">]</span></code></pre><p>否则 <code>decode()</code> 会同时返回 Prompt 和模型新生成的文本。</p><h2 id="25-Greedy-与-Sampling-怎样选择"><a href="#25-Greedy-与-Sampling-怎样选择" class="headerlink" title="25. Greedy 与 Sampling 怎样选择"></a>25. Greedy 与 Sampling 怎样选择</h2><p>比较微调前后时，优先使用确定性的 Greedy：</p><pre class="language-python" data-language="python"><code class="language-python">GenerationConfig<span class="token punctuation">(</span>    do_sample<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    max_new_tokens<span class="token operator">=</span><span class="token number">96</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这样相同 Prompt 的结果更容易比较。</p><p>需要开放式表达时再使用：</p><pre class="language-python" data-language="python"><code class="language-python">GenerationConfig<span class="token punctuation">(</span>    do_sample<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    temperature<span class="token operator">=</span><span class="token number">0.8</span><span class="token punctuation">,</span>    top_p<span class="token operator">=</span><span class="token number">0.9</span><span class="token punctuation">,</span>    max_new_tokens<span class="token operator">=</span><span class="token number">96</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>不要把 Sampling 的随机差异当作 SFT 效果。</p><h2 id="26-微调前后应该怎样对比"><a href="#26-微调前后应该怎样对比" class="headerlink" title="26. 微调前后应该怎样对比"></a>26. 微调前后应该怎样对比</h2><p>保存一组固定测试 Prompt：</p><pre class="language-python" data-language="python"><code class="language-python">EVAL_PROMPTS <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token string">"用一句话解释什么是验证集。"</span><span class="token punctuation">,</span>    <span class="token string">"为什么学习率太大会导致训练不稳定？"</span><span class="token punctuation">,</span>    <span class="token string">"给出两种缓解过拟合的方法。"</span><span class="token punctuation">,</span><span class="token punctuation">]</span></code></pre><p>固定：</p><ul><li>相同 Prompt 模板。</li><li>相同 Tokenizer。</li><li>相同 GenerationConfig。</li><li>相同随机种子，或直接使用 Greedy。</li><li>相同最大生成长度。</li></ul><p>记录：</p><pre class="language-text" data-language="text"><code class="language-text">Base Model 输出SFT Model 输出目标行为错误类型</code></pre><h2 id="27-完整代码"><a href="#27-完整代码" class="headerlink" title="27. 完整代码"></a>27. 完整代码</h2><p>完整项目代码位于：</p><pre class="language-text" data-language="text"><code class="language-text">source/code/nlp-blog/sft_transformers.py</code></pre><p>运行：</p><pre class="language-bash" data-language="bash"><code class="language-bash">python source/code/nlp-blog/sft_transformers.py</code></pre><p>脚本会依次执行：</p><pre class="language-text" data-language="text"><code class="language-text">创建本地 Dataset  -> 加载 Tokenizer  -> 构造 Completion-only labels  -> 检查一条样本  -> 检查一个 batch  -> 加载 Base Model  -> 训练与验证  -> 保存完整模型  -> 重新加载并生成</code></pre><p>默认示例用于学习流程。实际下载约 0.5B 模型并进行 Full Fine-tuning 仍需要合适的网络、内存和 GPU 环境。</p><h2 id="28-常见错误"><a href="#28-常见错误" class="headerlink" title="28. 常见错误"></a>28. 常见错误</h2><h3 id="28-1-Prompt-与-Answer-都参与-loss"><a href="#28-1-Prompt-与-Answer-都参与-loss" class="headerlink" title="28.1 Prompt 与 Answer 都参与 loss"></a>28.1 Prompt 与 Answer 都参与 loss</h3><p>如果目标是 Completion-only，应检查 Prompt labels 是否为 <code>-100</code>。</p><h3 id="28-2-把-input-ids-中的-Prompt-也改成-100"><a href="#28-2-把-input-ids-中的-Prompt-也改成-100" class="headerlink" title="28.2 把 input_ids 中的 Prompt 也改成 -100"></a>28.2 把 input_ids 中的 Prompt 也改成 -100</h3><p><code>input_ids</code> 必须是合法词表 id。<code>-100</code> 只应出现在 labels 中。</p><h3 id="28-3-Padding-labels-使用-pad-token-id"><a href="#28-3-Padding-labels-使用-pad-token-id" class="headerlink" title="28.3 Padding labels 使用 pad_token_id"></a>28.3 Padding labels 使用 pad_token_id</h3><p>这会训练模型预测大量 Padding。Padding labels 应设为 <code>-100</code>。</p><h3 id="28-4-手动提前-shift-labels"><a href="#28-4-手动提前-shift-labels" class="headerlink" title="28.4 手动提前 shift labels"></a>28.4 手动提前 shift labels</h3><p><code>AutoModelForCausalLM</code> 通常已经内部 shift。外部再次 shift 会错位。</p><h3 id="28-5-截断后没有-Answer-token"><a href="#28-5-截断后没有-Answer-token" class="headerlink" title="28.5 截断后没有 Answer token"></a>28.5 截断后没有 Answer token</h3><p>检查每条样本：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token builtin">sum</span><span class="token punctuation">(</span>label <span class="token operator">!=</span> <span class="token operator">-</span><span class="token number">100</span> <span class="token keyword">for</span> label <span class="token keyword">in</span> labels<span class="token punctuation">)</span> <span class="token operator">></span> <span class="token number">0</span></code></pre><h3 id="28-6-重复添加特殊-token"><a href="#28-6-重复添加特殊-token" class="headerlink" title="28.6 重复添加特殊 token"></a>28.6 重复添加特殊 token</h3><p>Prompt 与 Answer 分开编码时，应明确哪一段添加 BOS；使用 Chat Template 后更要避免再次增加重复 special token。</p><h3 id="28-7-忘记-EOS"><a href="#28-7-忘记-EOS" class="headerlink" title="28.7 忘记 EOS"></a>28.7 忘记 EOS</h3><p>模型可能学不会在回答末尾停止。</p><h3 id="28-8-开启-Gradient-Checkpointing-却保留-use-cache"><a href="#28-8-开启-Gradient-Checkpointing-却保留-use-cache" class="headerlink" title="28.8 开启 Gradient Checkpointing 却保留 use_cache"></a>28.8 开启 Gradient Checkpointing 却保留 use_cache</h3><p>训练前设置：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span>config<span class="token punctuation">.</span>use_cache <span class="token operator">=</span> <span class="token boolean">False</span></code></pre><p>推理前再恢复。</p><h3 id="28-9-CPU-运行看似卡住"><a href="#28-9-CPU-运行看似卡住" class="headerlink" title="28.9 CPU 运行看似卡住"></a>28.9 CPU 运行看似卡住</h3><p>0.5B 模型的全参数前向、反向和优化器状态对 CPU 很重。教学代码不是轻量性能基准。</p><h3 id="28-10-用十几条数据判断模型能力"><a href="#28-10-用十几条数据判断模型能力" class="headerlink" title="28.10 用十几条数据判断模型能力"></a>28.10 用十几条数据判断模型能力</h3><p>小数据只验证代码和过拟合能力，不能证明真实泛化。</p><h2 id="29-Full-Fine-tuning-的显存都花在哪里"><a href="#29-Full-Fine-tuning-的显存都花在哪里" class="headerlink" title="29. Full Fine-tuning 的显存都花在哪里"></a>29. Full Fine-tuning 的显存都花在哪里</h2><p>训练显存不只有模型权重：</p><pre class="language-text" data-language="text"><code class="language-text">模型参数梯度优化器状态前向激活临时 logitsCUDA kernel workspace</code></pre><p>特别是 Causal LM logits：</p><p>$$<br>[B,T,V]<br>$$</p><p>当词表 $V$ 很大时，也会占用明显显存。</p><p>常见优化包括：</p><ul><li>BF16 &#x2F; FP16。</li><li>Gradient Accumulation。</li><li>Gradient Checkpointing。</li><li>缩短 <code>max_length</code>。</li><li>动态 Padding。</li><li>Packing。</li><li>Flash Attention &#x2F; SDPA。</li><li>FSDP &#x2F; DeepSpeed。</li><li>LoRA &#x2F; QLoRA。</li></ul><p>下一篇重点使用 LoRA，减少可训练参数和优化器状态。</p><h2 id="30-怎样把本篇改成-Chat-Template-训练"><a href="#30-怎样把本篇改成-Chat-Template-训练" class="headerlink" title="30. 怎样把本篇改成 Chat Template 训练"></a>30. 怎样把本篇改成 Chat Template 训练</h2><p>本篇手写文本模板是为了学习边界。</p><p>真实模型可改为：</p><pre class="language-python" data-language="python"><code class="language-python">messages <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> instruction<span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> response<span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span></code></pre><p>然后：</p><pre class="language-python" data-language="python"><code class="language-python">tokenizer<span class="token punctuation">.</span>apply_chat_template<span class="token punctuation">(</span>    messages<span class="token punctuation">,</span>    tokenize<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    add_generation_prompt<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>难点转变为：</p><blockquote><p>怎样准确得到 assistant token 对应的 mask？</p></blockquote><p>可以选择：</p><ol><li>使用模板提供的 assistant mask。</li><li>使用 Prompt 与 Completion 分离格式。</li><li>使用 TRL <code>SFTTrainer</code> 的 <code>completion_only_loss</code> 或 <code>assistant_only_loss</code>。</li></ol><p>第三篇采用第 3 种方式。</p><h2 id="31-Trainer-与-SFTTrainer-的关系"><a href="#31-Trainer-与-SFTTrainer-的关系" class="headerlink" title="31. Trainer 与 SFTTrainer 的关系"></a>31. Trainer 与 SFTTrainer 的关系</h2><p>原生 <code>Trainer</code>：</p><pre class="language-text" data-language="text"><code class="language-text">负责训练循环但要求我提前准备好 input_ids / labels</code></pre><p>TRL <code>SFTTrainer</code>：</p><pre class="language-text" data-language="text"><code class="language-text">继承并扩展 Trainer额外理解 text / prompt-completion / messages 数据自动应用 Chat Template自动构造 completion 或 assistant mask支持 Packing 和 PEFT</code></pre><p>所以学习顺序应是：</p><pre class="language-text" data-language="text"><code class="language-text">先理解 Trainer 接收到什么  -> 再理解 SFTTrainer 替我生成了什么</code></pre><h2 id="32-总结"><a href="#32-总结" class="headerlink" title="32. 总结"></a>32. 总结</h2><p>本篇使用原生 Hugging Face Transformers 完成了 SFT 的完整闭环：</p><pre class="language-text" data-language="text"><code class="language-text">instruction / response  -> format_prompt  -> Prompt 与 Answer 分开 Tokenize  -> input_ids  -> Prompt labels = -100  -> Answer labels = token ids  -> Collator 动态 Padding  -> AutoModelForCausalLM.forward  -> 内部 label shift + CrossEntropyLoss  -> Trainer 反向传播  -> save_pretrained  -> GenerationConfig + generate</code></pre><p>最值得记住的并不是 <code>Trainer(...)</code> 的参数，而是这组不变量：</p><pre class="language-text" data-language="text"><code class="language-text">input_ids、attention_mask、labels 形状一致Prompt 保留在 input_ids 中Prompt 对应 labels 使用 -100Answer 至少包含一个有效目标 tokenPadding 对应 attention_mask=0、labels=-100训练时不需要 Sampling推理时不再传 labels</code></pre><p>下一篇将用 <code>TRL SFTTrainer</code> 和 PEFT LoRA 重写同一条流程，并解释它自动处理了哪些步骤、哪些责任仍然必须由我承担。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://huggingface.co/docs/transformers/tasks/language_modeling">Hugging Face Transformers：Causal language modeling</a></li><li><a href="https://huggingface.co/docs/transformers/main_classes/trainer">Hugging Face Transformers：Trainer</a></li><li><a href="https://huggingface.co/docs/transformers/chat_templating">Hugging Face Transformers：Chat templates</a></li><li><a href="https://huggingface.co/docs/datasets/process">Hugging Face Datasets：Process</a></li><li><a href="https://huggingface.co/Qwen/Qwen2.5-0.5B">Qwen2.5-0.5B Model Card</a></li><li><a href="https://lsworl.github.io/2026/08/24/nlp-sft-principles/">上一篇：SFT（一）从Next Token Prediction到指令微调</a></li></ul>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/24/nlp-sft-transformers-from-scratch/</id>
    <link href="https://lsworl.github.io/2026/08/24/nlp-sft-transformers-from-scratch/"/>
    <published>2026-08-24T15:10:58.000Z</published>
    <summary>不依赖TRL，从prompt-completion数据出发，使用Hugging Face Transformers亲手构造input_ids、attention_mask与labels，实现Completion-only loss、动态Padding、Trainer训练、保存和GenerationConfig推理。</summary>
    <title>SFT（二）：用Hugging Face Transformers手写监督微调</title>
    <updated>2026-08-24T15:58:26.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Hugging Face" scheme="https://lsworl.github.io/tags/Hugging-Face/"/>
    <category term="Transformers" scheme="https://lsworl.github.io/tags/Transformers/"/>
    <category term="LLM" scheme="https://lsworl.github.io/tags/LLM/"/>
    <category term="SFT" scheme="https://lsworl.github.io/tags/SFT/"/>
    <category term="指令微调" scheme="https://lsworl.github.io/tags/%E6%8C%87%E4%BB%A4%E5%BE%AE%E8%B0%83/"/>
    <category term="后训练" scheme="https://lsworl.github.io/tags/%E5%90%8E%E8%AE%AD%E7%BB%83/"/>
    <content>
      <![CDATA[<p>在<a href="https://lsworl.github.io/2026/08/12/nlp-mini-gpt-from-scratch-pytorch/">手写 Mini-GPT</a>中，也已经从底层走通过：</p><pre class="language-text" data-language="text"><code class="language-text">Prompt  -> Tokenizer  -> Embedding  -> Causal Transformer  -> LM Head  -> logits  -> sampling  -> next token</code></pre><p>但是理解推理流程后，新的问题随之出现：</p><blockquote><p>一个只会续写文本的 Base Model，为什么经过训练后会开始理解“用户提问”和“助手回答”，并按照指令组织答案？</p></blockquote><p>这就是本文要讨论的 <strong>Supervised Fine-Tuning，监督微调，简称 SFT</strong>。</p><p>本文不会从“调用一个 Trainer”开始，而会先把 SFT 还原成我已经熟悉的 next-token prediction。等把数据、标签和损失看清楚后，下一篇再用 Hugging Face <code>Transformers</code> 亲手实现，第三篇再进入 <code>TRL SFTTrainer + LoRA</code>。</p><span id="more"></span><p><img src="/images/nlp-blog/sft-post-training-overview.svg" alt="SFT在LLM训练流程中的位置"></p><h2 id="1-SFT-到底是什么"><a href="#1-SFT-到底是什么" class="headerlink" title="1. SFT 到底是什么"></a>1. SFT 到底是什么</h2><p>SFT 的完整名称是 <strong>Supervised Fine-Tuning</strong>。</p><p>它表示：</p><blockquote><p>从一个已经预训练的模型出发，使用带有目标答案的监督数据继续训练，让模型学习特定任务、输出格式或交互方式。</p></blockquote><p>对于聊天模型，一条最简单的数据可以写成：</p><pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>  <span class="token property">"prompt"</span><span class="token operator">:</span> <span class="token string">"请解释什么是过拟合"</span><span class="token punctuation">,</span>  <span class="token property">"completion"</span><span class="token operator">:</span> <span class="token string">"过拟合是模型在训练数据上表现很好，但在未见数据上泛化较差的现象。"</span><span class="token punctuation">&#125;</span></code></pre><p>也可以写成对话形式：</p><pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>  <span class="token property">"messages"</span><span class="token operator">:</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token property">"role"</span><span class="token operator">:</span> <span class="token string">"system"</span><span class="token punctuation">,</span> <span class="token property">"content"</span><span class="token operator">:</span> <span class="token string">"你是一名耐心的机器学习助教。"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token property">"role"</span><span class="token operator">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token property">"content"</span><span class="token operator">:</span> <span class="token string">"请解释什么是过拟合。"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token property">"role"</span><span class="token operator">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span> <span class="token property">"content"</span><span class="token operator">:</span> <span class="token string">"过拟合是模型在训练数据上表现很好，但在未见数据上泛化较差的现象。"</span><span class="token punctuation">&#125;</span>  <span class="token punctuation">]</span><span class="token punctuation">&#125;</span></code></pre><p>训练时，<code>assistant</code> 的回答不是模型现场生成出来再打分，而是数据集已经提供的标准答案。</p><p>因此 SFT 中的“监督”来自：</p><ul><li>输入指令是已知的。</li><li>目标回答也是已知的。</li><li>每一个需要学习的目标 token 都可以直接计算交叉熵。</li></ul><h2 id="2-SFT-在-LLM-训练流程中的位置"><a href="#2-SFT-在-LLM-训练流程中的位置" class="headerlink" title="2. SFT 在 LLM 训练流程中的位置"></a>2. SFT 在 LLM 训练流程中的位置</h2><p>一个简化的大语言模型训练流程可以写成：</p><pre class="language-text" data-language="text"><code class="language-text">随机初始化参数  -> 大规模预训练 Pretraining  -> 得到 Base Model  -> 监督微调 SFT  -> 得到能够遵循指令的 SFT Model  -> 可选：偏好对齐 DPO / PPO / GRPO 等  -> 安全评估、部署与持续迭代</code></pre><h3 id="2-1-预训练解决什么问题"><a href="#2-1-预训练解决什么问题" class="headerlink" title="2.1 预训练解决什么问题"></a>2.1 预训练解决什么问题</h3><p>预训练数据通常来自大规模自然文本、代码、书籍和网页。</p><p>模型学习的目标仍然是预测下一个 token：</p><p>$$<br>\mathcal{L}<em>{\text{PT}}(\theta)<br>&#x3D;-<br>\sum</em>{t&#x3D;1}^{T}<br>\log p_{\theta}(x_t\mid x_{&lt;t})<br>$$</p><p>预训练让模型获得：</p><ul><li>语言规律。</li><li>词语和概念之间的关系。</li><li>一部分世界知识。</li><li>代码和文本模式。</li><li>根据上下文续写的能力。</li></ul><p>但“会续写”不一定等于“会按用户要求回答”。</p><h3 id="2-2-SFT-解决什么问题"><a href="#2-2-SFT-解决什么问题" class="headerlink" title="2.2 SFT 解决什么问题"></a>2.2 SFT 解决什么问题</h3><p>SFT 数据通常更小，但结构更明确，例如：</p><ul><li>指令与回答。</li><li>多轮对话。</li><li>摘要任务。</li><li>信息抽取。</li><li>代码生成。</li><li>特定 JSON 格式输出。</li><li>某个领域中的标准问答。</li></ul><p>SFT 主要让模型学习：</p><ul><li>什么内容是用户输入。</li><li>什么位置应该由助手开始回答。</li><li>指令与答案之间的映射。</li><li>回答的风格、长度和结构。</li><li>在何处结束回答。</li></ul><h3 id="2-3-偏好对齐解决什么问题"><a href="#2-3-偏好对齐解决什么问题" class="headerlink" title="2.3 偏好对齐解决什么问题"></a>2.3 偏好对齐解决什么问题</h3><p>一条指令可能存在多个语法正确的答案，但人类通常更喜欢其中某些答案。</p><p>例如两个回答可能都没有事实错误，但其中一个：</p><ul><li>更清晰。</li><li>更有帮助。</li><li>更符合安全规范。</li><li>更少废话。</li><li>更遵守格式要求。</li></ul><p>DPO、PPO、GRPO 等方法处理的是“不同回答之间哪个更好”或“什么行为能得到更高奖励”。这与 SFT 直接模仿标准答案并不相同。</p><h2 id="3-SFT-不会自动改变模型架构"><a href="#3-SFT-不会自动改变模型架构" class="headerlink" title="3. SFT 不会自动改变模型架构"></a>3. SFT 不会自动改变模型架构</h2><p>这是我学习 SFT 时最先需要稳定下来的概念。</p><p>假设微调前加载：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span></code></pre><p>SFT 后通常仍然是同一个 <code>AutoModelForCausalLM</code> 架构：</p><pre class="language-text" data-language="text"><code class="language-text">Tokenizer  -> Token Embedding  -> 多个 Decoder-only Transformer Block  -> Final Norm  -> LM Head  -> vocab logits</code></pre><p>SFT 一般改变的是参数值，而不是把 Decoder-only 模型改成另一种网络。</p><p>如果使用 LoRA，则主干参数可以保持冻结，只新增一组小型可训练矩阵；但前向计算的基础架构仍然是同一个 Causal LM。</p><p>因此要区分：</p><table><thead><tr><th>概念</th><th>回答的问题</th></tr></thead><tbody><tr><td>Architecture</td><td>网络由哪些层组成</td></tr><tr><td>Config</td><td>层数、隐藏维度、头数、词表大小等配置是什么</td></tr><tr><td>Checkpoint</td><td>当前参数具体取值是什么</td></tr><tr><td>SFT</td><td>用什么监督数据和目标继续优化参数</td></tr><tr><td>LoRA</td><td>训练时允许哪些附加参数更新</td></tr></tbody></table><p>SFT 是训练阶段和目标；LoRA 是参数高效更新方法。两者不是同义词。</p><h2 id="4-SFT-和普通-Fine-tuning-有什么关系"><a href="#4-SFT-和普通-Fine-tuning-有什么关系" class="headerlink" title="4. SFT 和普通 Fine-tuning 有什么关系"></a>4. SFT 和普通 Fine-tuning 有什么关系</h2><p>“Fine-tuning”是一个更宽泛的概念。</p><p>例如前一篇 <a href="https://lsworl.github.io/2026/08/22/nlp-hugging-face-transformers-tutorial/">Hugging Face 入门文章</a>使用 BERT 完成中文文本分类：</p><pre class="language-text" data-language="text"><code class="language-text">文本  -> Encoder  -> 分类头  -> 两个类别 logits  -> 分类标签</code></pre><p>这同样是监督微调。</p><p>但 LLM 语境中的 SFT 通常特指：</p><pre class="language-text" data-language="text"><code class="language-text">指令 / 对话  -> Causal Language Model  -> 每个位置的词表 logits  -> 目标回答 token</code></pre><p>两者都使用监督数据和梯度下降，区别主要在于：</p><ul><li>模型头不同。</li><li>标签形状不同。</li><li>损失计算位置不同。</li><li>推理方式不同。</li></ul><p>文本分类通常每条样本只有一个类别标签；Causal LM SFT 通常每条样本包含一串 token 标签。</p><h2 id="5-Instruction-Tuning-是不是-SFT"><a href="#5-Instruction-Tuning-是不是-SFT" class="headerlink" title="5. Instruction Tuning 是不是 SFT"></a>5. Instruction Tuning 是不是 SFT</h2><p>Instruction Tuning 可以看作 SFT 的常见形式。</p><p>如果数据主要是：</p><pre class="language-text" data-language="text"><code class="language-text">Instruction -> Response</code></pre><p>那么这就是指令微调。</p><p>但 SFT 的范围可以更广：</p><ul><li>让模型适应某个领域文本。</li><li>学习固定输出格式。</li><li>学习多轮客服对话。</li><li>学习工具调用格式。</li><li>学习把自然语言转换为 SQL。</li></ul><p>因此更准确的关系是：</p><pre class="language-text" data-language="text"><code class="language-text">Fine-tuning  └─ Supervised Fine-Tuning       ├─ Instruction Tuning       ├─ Dialogue Tuning       ├─ Domain Task Tuning       └─ Structured Output Tuning</code></pre><p>具体术语在不同项目中可能被宽松使用，判断时应看数据和损失，而不是只看名称。</p><h2 id="6-SFT-与继续预训练不要混淆"><a href="#6-SFT-与继续预训练不要混淆" class="headerlink" title="6. SFT 与继续预训练不要混淆"></a>6. SFT 与继续预训练不要混淆</h2><p>继续预训练常写作 Continued Pretraining 或 Domain-Adaptive Pretraining。</p><p>假设我有大量金融研报：</p><pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span><span class="token property">"text"</span><span class="token operator">:</span> <span class="token string">"某公司本季度营业收入同比增长……"</span><span class="token punctuation">&#125;</span></code></pre><p>直接让模型在这些连续文本上继续做全序列 next-token prediction，更接近继续预训练。</p><p>如果我把数据整理成：</p><pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>  <span class="token property">"prompt"</span><span class="token operator">:</span> <span class="token string">"根据下面的财报解释营业收入增长原因：……"</span><span class="token punctuation">,</span>  <span class="token property">"completion"</span><span class="token operator">:</span> <span class="token string">"营业收入增长主要来自……"</span><span class="token punctuation">&#125;</span></code></pre><p>这更接近 SFT。</p><p>二者底层都可能使用 Causal LM 交叉熵，但数据分布和训练目标位置不同。</p><table><thead><tr><th>对比项</th><th>继续预训练</th><th>SFT</th></tr></thead><tbody><tr><td>常见数据</td><td>连续领域文本</td><td>指令、问题、回答、对话</td></tr><tr><td>常见目标</td><td>学习整个文本分布</td><td>学习输入到目标输出的映射</td></tr><tr><td>Loss 范围</td><td>通常覆盖大部分非 Padding token</td><td>可只覆盖 completion &#x2F; assistant</td></tr><tr><td>主要目的</td><td>增强领域语言和知识建模</td><td>增强任务与指令遵循</td></tr></tbody></table><h2 id="7-从-messages-到模型真正看到的-token"><a href="#7-从-messages-到模型真正看到的-token" class="headerlink" title="7. 从 messages 到模型真正看到的 token"></a>7. 从 messages 到模型真正看到的 token</h2><p>模型不会直接理解 Python 字典中的 <code>role</code> 字段。</p><p>下面的结构：</p><pre class="language-python" data-language="python"><code class="language-python">messages <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"system"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"你是一名机器学习助教。"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"什么是梯度下降？"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token string">"role"</span><span class="token punctuation">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">:</span> <span class="token string">"梯度下降是一种沿损失函数负梯度方向更新参数的方法。"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span></code></pre><p>必须先经过 Chat Template，变成一段线性 token 序列。</p><p>不同模型使用的格式可能不同。为了说明原理，假设模板产生：</p><pre class="language-text" data-language="text"><code class="language-text">&lt;|system|>你是一名机器学习助教。&lt;|end|>&lt;|user|>什么是梯度下降？&lt;|end|>&lt;|assistant|>梯度下降是一种沿损失函数负梯度方向更新参数的方法。&lt;|end|></code></pre><p>再经过 Tokenizer：</p><pre class="language-text" data-language="text"><code class="language-text">文本模板  -> input_ids  -> attention_mask  -> labels</code></pre><p><code>role</code> 的意义最终由 <code>&lt;|user|&gt;</code>、<code>&lt;|assistant|&gt;</code> 等控制 token 表达。</p><p>这也解释了为什么不能随意把 A 模型的数据模板拿给 B 模型：即使文本内容相同，角色边界 token 也可能不同。</p><p><img src="/images/nlp-blog/sft-chat-template-tokenization.svg" alt="对话数据经过Chat Template与Tokenizer"></p><h2 id="8-训练模板与生成模板有什么区别"><a href="#8-训练模板与生成模板有什么区别" class="headerlink" title="8. 训练模板与生成模板有什么区别"></a>8. 训练模板与生成模板有什么区别</h2><p>训练样本中已经包含 assistant 的完整答案：</p><pre class="language-text" data-language="text"><code class="language-text">user 消息assistant 消息及完整答案</code></pre><p>因此训练格式通常使用：</p><pre class="language-python" data-language="python"><code class="language-python">tokenizer<span class="token punctuation">.</span>apply_chat_template<span class="token punctuation">(</span>    messages<span class="token punctuation">,</span>    tokenize<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    add_generation_prompt<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>推理时只有用户输入，需要告诉模型“接下来应该由 assistant 说话”：</p><pre class="language-python" data-language="python"><code class="language-python">tokenizer<span class="token punctuation">.</span>apply_chat_template<span class="token punctuation">(</span>    messages_without_answer<span class="token punctuation">,</span>    tokenize<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    add_generation_prompt<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>两种情况的区别是：</p><pre class="language-text" data-language="text"><code class="language-text">训练：序列中已经有 assistant 开头和标准答案推理：只补 assistant 开头，答案由 generate() 产生</code></pre><p>如果训练时额外增加一个新的 assistant 开头，却又没有对应答案，就可能制造一个无意义的尾部片段。</p><h2 id="9-SFT-最关键的三个-Mask"><a href="#9-SFT-最关键的三个-Mask" class="headerlink" title="9. SFT 最关键的三个 Mask"></a>9. SFT 最关键的三个 Mask</h2><p>我以前学习 Attention 时已经见过 Padding Mask 和 Causal Mask。SFT 又引入了标签屏蔽，因此很容易混淆。</p><h3 id="9-1-Causal-Mask"><a href="#9-1-Causal-Mask" class="headerlink" title="9.1 Causal Mask"></a>9.1 Causal Mask</h3><p>Causal Mask 决定一个位置能看见哪些历史位置。</p><p>对于长度为 5 的序列：</p><p>$$<br>M_{causal}&#x3D;<br>\begin{bmatrix}<br>1&amp;0&amp;0&amp;0&amp;0\<br>1&amp;1&amp;0&amp;0&amp;0\<br>1&amp;1&amp;1&amp;0&amp;0\<br>1&amp;1&amp;1&amp;1&amp;0\<br>1&amp;1&amp;1&amp;1&amp;1<br>\end{bmatrix}<br>$$</p><p>它防止模型在预测当前 token 时偷看未来答案。</p><h3 id="9-2-Attention-Mask"><a href="#9-2-Attention-Mask" class="headerlink" title="9.2 Attention Mask"></a>9.2 Attention Mask</h3><p><code>attention_mask</code> 通常区分真实 token 和 Padding：</p><pre class="language-text" data-language="text"><code class="language-text">input_ids:      [11, 25, 39,  2,  0,  0]attention_mask: [ 1,  1,  1,  1,  0,  0]</code></pre><p>它回答：</p><blockquote><p>哪些输入位置是真实序列，哪些只是为了组成 batch 而补齐？</p></blockquote><h3 id="9-3-Label-Mask"><a href="#9-3-Label-Mask" class="headerlink" title="9.3 Label Mask"></a>9.3 Label Mask</h3><p>Label Mask 决定哪些位置进入损失。</p><p>Hugging Face Causal LM 通常使用 <code>-100</code> 表示忽略某个标签：</p><pre class="language-text" data-language="text"><code class="language-text">labels: [-100, -100, -100, 52, 81, 2]</code></pre><p>它回答：</p><blockquote><p>哪些目标 token 应该产生训练梯度？</p></blockquote><p>重要的是，被设为 <code>-100</code> 的 Prompt token 仍然保留在 <code>input_ids</code> 中，也仍然可以成为回答 token 的上下文；只是它们对应的预测误差不计入 loss。</p><p><img src="/images/nlp-blog/sft-three-masks.svg" alt="Causal Mask、Attention Mask与Label Mask"></p><h2 id="10-三种常见-Loss-范围"><a href="#10-三种常见-Loss-范围" class="headerlink" title="10. 三种常见 Loss 范围"></a>10. 三种常见 Loss 范围</h2><p>假设一条序列是：</p><pre class="language-text" data-language="text"><code class="language-text">&lt;system> 规则 &lt;user> 问题 &lt;assistant> 回答 &lt;eos></code></pre><h3 id="10-1-Full-sequence-loss"><a href="#10-1-Full-sequence-loss" class="headerlink" title="10.1 Full-sequence loss"></a>10.1 Full-sequence loss</h3><p>除了 Padding 外，几乎所有 token 都参与 loss：</p><pre class="language-text" data-language="text"><code class="language-text">labels:规则 &lt;user> 问题 &lt;assistant> 回答 &lt;eos></code></pre><p>这会训练模型复现整段对话，包括用户消息和系统消息。</p><h3 id="10-2-Completion-only-loss"><a href="#10-2-Completion-only-loss" class="headerlink" title="10.2 Completion-only loss"></a>10.2 Completion-only loss</h3><p>Prompt 部分被忽略，只训练 Completion：</p><pre class="language-text" data-language="text"><code class="language-text">labels:-100 -100 -100 -100  回答 &lt;eos></code></pre><p>这常用于明确的 <code>prompt / completion</code> 数据。</p><h3 id="10-3-Assistant-only-loss"><a href="#10-3-Assistant-only-loss" class="headerlink" title="10.3 Assistant-only loss"></a>10.3 Assistant-only loss</h3><p>只训练所有 assistant 消息，忽略 system 与 user 消息：</p><pre class="language-text" data-language="text"><code class="language-text">system:    -100user:      -100assistant: 参与 lossuser:      -100assistant: 参与 loss</code></pre><p>它适合多轮 conversational 数据。</p><p>三种策略没有脱离 next-token prediction，只是决定了求和集合 $\mathcal{T}_{loss}$：</p><p>$$<br>\mathcal{L}<em>{SFT}(\theta)<br>&#x3D;-<br>\frac{1}{|\mathcal{T}</em>{loss}|}<br>\sum_{t\in\mathcal{T}<em>{loss}}<br>\log p</em>{\theta}(x_t\mid x_{&lt;t})<br>$$</p><h2 id="11-Label-Shift-到底在哪里发生"><a href="#11-Label-Shift-到底在哪里发生" class="headerlink" title="11. Label Shift 到底在哪里发生"></a>11. Label Shift 到底在哪里发生</h2><p>假设 Tokenizer 得到：</p><pre class="language-text" data-language="text"><code class="language-text">input_ids = [BOS, A, B, C, EOS]labels    = [BOS, A, B, C, EOS]</code></pre><p>模型输出的 logits 形状是：</p><pre class="language-text" data-language="text"><code class="language-text">[batch_size, sequence_length, vocab_size]</code></pre><p>损失比较时会错开一个位置：</p><pre class="language-text" data-language="text"><code class="language-text">logits at position: 0    1    2    3target token:       A    B    C    EOS</code></pre><p>可以理解为内部执行：</p><pre class="language-python" data-language="python"><code class="language-python">shift_logits <span class="token operator">=</span> logits<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token punctuation">]</span>shift_labels <span class="token operator">=</span> labels<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">:</span><span class="token punctuation">]</span></code></pre><p>因此在调用 Hugging Face <code>AutoModelForCausalLM</code> 时，通常把与 <code>input_ids</code> 等长的 <code>labels</code> 直接传入即可：</p><pre class="language-python" data-language="python"><code class="language-python">outputs <span class="token operator">=</span> model<span class="token punctuation">(</span>    input_ids<span class="token operator">=</span>input_ids<span class="token punctuation">,</span>    attention_mask<span class="token operator">=</span>attention_mask<span class="token punctuation">,</span>    labels<span class="token operator">=</span>labels<span class="token punctuation">,</span><span class="token punctuation">)</span>loss <span class="token operator">=</span> outputs<span class="token punctuation">.</span>loss</code></pre><p>不应该在外部又手动右移一次，否则会发生双重错位。</p><p><img src="/images/nlp-blog/sft-label-shift-loss.svg" alt="SFT中的Label Shift与Loss位置"></p><h2 id="12-手算一次-Assistant-only-SFT-Loss"><a href="#12-手算一次-Assistant-only-SFT-Loss" class="headerlink" title="12. 手算一次 Assistant-only SFT Loss"></a>12. 手算一次 Assistant-only SFT Loss</h2><p>为了让计算可以手工完成，假设一条对话经过模板和 Tokenizer 后只有 8 个 token：</p><table><thead><tr><th align="right">位置</th><th>token</th><th>区域</th></tr></thead><tbody><tr><td align="right">0</td><td><code>&lt;user&gt;</code></td><td>Prompt</td></tr><tr><td align="right">1</td><td><code>2</code></td><td>Prompt</td></tr><tr><td align="right">2</td><td><code>+</code></td><td>Prompt</td></tr><tr><td align="right">3</td><td><code>3</code></td><td>Prompt</td></tr><tr><td align="right">4</td><td><code>=?</code></td><td>Prompt</td></tr><tr><td align="right">5</td><td><code>&lt;assistant&gt;</code></td><td>Prompt 边界</td></tr><tr><td align="right">6</td><td><code>5</code></td><td>Assistant answer</td></tr><tr><td align="right">7</td><td><code>&lt;eos&gt;</code></td><td>Assistant answer</td></tr></tbody></table><h3 id="12-1-构造-input-ids"><a href="#12-1-构造-input-ids" class="headerlink" title="12.1 构造 input_ids"></a>12.1 构造 input_ids</h3><p>假设它们的 id 是：</p><pre class="language-text" data-language="text"><code class="language-text">input_ids = [10, 21, 22, 23, 24, 11, 25, 2]</code></pre><p>所有 token 都是真实输入：</p><pre class="language-text" data-language="text"><code class="language-text">attention_mask = [1, 1, 1, 1, 1, 1, 1, 1]</code></pre><h3 id="12-2-构造-labels"><a href="#12-2-构造-labels" class="headerlink" title="12.2 构造 labels"></a>12.2 构造 labels</h3><p>只训练 assistant 的答案 <code>5 &lt;eos&gt;</code>：</p><pre class="language-text" data-language="text"><code class="language-text">labels = [-100, -100, -100, -100, -100, -100, 25, 2]</code></pre><p>由于内部会进行 label shift：</p><ul><li>位置 5 的 logits 用来预测位置 6 的 token <code>5</code>。</li><li>位置 6 的 logits 用来预测位置 7 的 token <code>&lt;eos&gt;</code>。</li></ul><p>虽然 <code>&lt;assistant&gt;</code> 自己的 label 被屏蔽，但它的隐藏状态仍然负责预测答案第一个 token。</p><h3 id="12-3-假设模型给出的概率"><a href="#12-3-假设模型给出的概率" class="headerlink" title="12.3 假设模型给出的概率"></a>12.3 假设模型给出的概率</h3><p>假设：</p><p>$$<br>p_{\theta}(\text{&#96;5’}\mid\text{完整 Prompt})&#x3D;0.60<br>$$</p><p>$$<br>p_{\theta}(\text{<code>&lt;eos&gt;&#39;&#125;\mid\text&#123;Prompt + </code>5’})&#x3D;0.80<br>$$</p><p>只对两个目标 token 求平均：</p><p>$$<br>\mathcal{L}_{SFT}<br>&#x3D;-<br>\frac{1}{2}<br>(\log 0.60+\log 0.80)<br>$$</p><p>取自然对数：</p><p>$$<br>\log 0.60\approx -0.5108<br>$$</p><p>$$<br>\log 0.80\approx -0.2231<br>$$</p><p>所以：</p><p>$$<br>\mathcal{L}_{SFT}<br>\approx<br>-\frac{-0.5108-0.2231}{2}<br>&#x3D;0.3670<br>$$</p><h3 id="12-4-一次梯度更新后会发生什么"><a href="#12-4-一次梯度更新后会发生什么" class="headerlink" title="12.4 一次梯度更新后会发生什么"></a>12.4 一次梯度更新后会发生什么</h3><p>理想情况下，更新后模型对正确 token 的概率上升，例如：</p><pre class="language-text" data-language="text"><code class="language-text">P("5")      : 0.60 -> 0.68P("&lt;eos>") : 0.80 -> 0.84</code></pre><p>新损失为：</p><p>$$<br>-\frac{1}{2}(\log 0.68+\log 0.84)\approx 0.280<br>$$</p><p>Loss 下降并不表示模型“记住了一个完整字符串”这么简单。梯度会经过：</p><pre class="language-text" data-language="text"><code class="language-text">LM Head  -> Final Norm  -> MLP  -> Attention  -> Embedding / LoRA 参数</code></pre><p>相似指令和上下文可能共享一部分表示，因此模型有机会把行为泛化到未见样本。</p><h2 id="13-Prompt-被-Mask-后，模型还看得到-Prompt-吗"><a href="#13-Prompt-被-Mask-后，模型还看得到-Prompt-吗" class="headerlink" title="13. Prompt 被 Mask 后，模型还看得到 Prompt 吗"></a>13. Prompt 被 Mask 后，模型还看得到 Prompt 吗</h2><p>看得到。</p><p>这是 <code>attention_mask</code> 与 <code>labels</code> 最容易混淆的地方。</p><pre class="language-text" data-language="text"><code class="language-text">input_ids:&lt;user> 2 + 3 = ? &lt;assistant> 5 &lt;eos>attention_mask:   1   1 1 1 1 1      1       1   1labels: -100 -100 ...       -100      5  &lt;eos></code></pre><p>预测 <code>5</code> 时，模型仍然能够通过 Causal Attention 查看：</p><pre class="language-text" data-language="text"><code class="language-text">&lt;user> 2 + 3 = ? &lt;assistant></code></pre><p><code>-100</code> 只告诉交叉熵不要评价那些位置，并没有从输入中删除 Prompt。</p><h2 id="14-为什么训练时叫-Teacher-Forcing"><a href="#14-为什么训练时叫-Teacher-Forcing" class="headerlink" title="14. 为什么训练时叫 Teacher Forcing"></a>14. 为什么训练时叫 Teacher Forcing</h2><p>在 SFT 训练中，预测第 $t$ 个回答 token 时，模型看到的是数据集中真实的历史 token：</p><p>$$<br>p_{\theta}(y_t\mid x,y_{&lt;t}^{*})<br>$$</p><p>其中 $y_{&lt;t}^{*}$ 表示标准答案的前缀。</p><p>例如标准回答是：</p><pre class="language-text" data-language="text"><code class="language-text">梯度 下降 是 ...</code></pre><p>训练到“是”时，输入历史中使用的仍然是真实的“梯度 下降”，即使模型前一个位置最可能预测成了别的 token。</p><p>这使得所有位置可以并行计算 loss：</p><pre class="language-text" data-language="text"><code class="language-text">一次 forward  -> 得到所有位置 logits  -> 同时计算多个目标 token 的交叉熵</code></pre><h2 id="15-SFT-训练与-generate-是两个阶段"><a href="#15-SFT-训练与-generate-是两个阶段" class="headerlink" title="15. SFT 训练与 generate() 是两个阶段"></a>15. SFT 训练与 generate() 是两个阶段</h2><p>训练时：</p><pre class="language-text" data-language="text"><code class="language-text">完整标准序列  -> 一次并行 forward  -> token-level loss  -> backward  -> optimizer.step()</code></pre><p>推理时：</p><pre class="language-text" data-language="text"><code class="language-text">只有 Prompt  -> forward  -> 选择 next token  -> 拼回输入  -> 再次 forward / 使用 KV Cache  -> 循环直到 EOS 或长度上限</code></pre><p><img src="/images/nlp-blog/sft-training-vs-generation.svg" alt="SFT训练与自回归生成的区别"></p><p>训练阶段并不需要在每个 token 上调用一次 <code>generate()</code>。</p><p><code>Trainer</code> 的核心路径是：</p><pre class="language-python" data-language="python"><code class="language-python">outputs <span class="token operator">=</span> model<span class="token punctuation">(</span><span class="token operator">**</span>batch<span class="token punctuation">)</span>loss <span class="token operator">=</span> outputs<span class="token punctuation">.</span>lossloss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>而不是：</p><pre class="language-python" data-language="python"><code class="language-python">generated <span class="token operator">=</span> model<span class="token punctuation">.</span>generate<span class="token punctuation">(</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span></code></pre><h2 id="16-GenerationConfig-会参与-SFT-Loss-吗"><a href="#16-GenerationConfig-会参与-SFT-Loss-吗" class="headerlink" title="16. GenerationConfig 会参与 SFT Loss 吗"></a>16. GenerationConfig 会参与 SFT Loss 吗</h2><p>通常不会。</p><p><code>GenerationConfig</code> 中常见参数包括：</p><pre class="language-python" data-language="python"><code class="language-python">GenerationConfig<span class="token punctuation">(</span>    max_new_tokens<span class="token operator">=</span><span class="token number">128</span><span class="token punctuation">,</span>    do_sample<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    temperature<span class="token operator">=</span><span class="token number">0.8</span><span class="token punctuation">,</span>    top_p<span class="token operator">=</span><span class="token number">0.9</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>它们控制的是：</p><pre class="language-text" data-language="text"><code class="language-text">logits -> 怎样选择下一个 token</code></pre><p>SFT 训练控制的是：</p><pre class="language-text" data-language="text"><code class="language-text">logits + labels -> 怎样计算交叉熵并更新参数</code></pre><p>因此：</p><table><thead><tr><th>参数</th><th>典型作用阶段</th></tr></thead><tbody><tr><td><code>learning_rate</code></td><td>训练</td></tr><tr><td><code>gradient_accumulation_steps</code></td><td>训练</td></tr><tr><td><code>labels == -100</code></td><td>训练 loss</td></tr><tr><td><code>temperature</code></td><td>生成</td></tr><tr><td><code>top_p</code></td><td>生成</td></tr><tr><td><code>do_sample</code></td><td>生成</td></tr><tr><td><code>max_new_tokens</code></td><td>生成</td></tr></tbody></table><p>在训练后的评估阶段，如果要生成回答并计算 ROUGE、BLEU 或任务成功率，才会再次用到生成配置。</p><h2 id="17-SFT-后-Config、Tokenizer-和-GenerationConfig-哪些会变"><a href="#17-SFT-后-Config、Tokenizer-和-GenerationConfig-哪些会变" class="headerlink" title="17. SFT 后 Config、Tokenizer 和 GenerationConfig 哪些会变"></a>17. SFT 后 Config、Tokenizer 和 GenerationConfig 哪些会变</h2><h3 id="17-1-Model-Config"><a href="#17-1-Model-Config" class="headerlink" title="17.1 Model Config"></a>17.1 Model Config</h3><p>模型层数、隐藏维度和注意力头数通常不变。</p><p>但某些配置可能需要同步，例如：</p><ul><li>增加特殊 token 后的 <code>vocab_size</code>。</li><li><code>pad_token_id</code>。</li><li><code>bos_token_id</code>。</li><li><code>eos_token_id</code>。</li><li>推理时的 <code>use_cache</code>。</li></ul><h3 id="17-2-Tokenizer"><a href="#17-2-Tokenizer" class="headerlink" title="17.2 Tokenizer"></a>17.2 Tokenizer</h3><p>如果沿用模型已有 Chat Template 和控制 token，词表可以完全不变。</p><p>如果新增特殊 token：</p><pre class="language-python" data-language="python"><code class="language-python">num_added <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>add_special_tokens<span class="token punctuation">(</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span>resize_token_embeddings<span class="token punctuation">(</span><span class="token builtin">len</span><span class="token punctuation">(</span>tokenizer<span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>新增 token 对应的 embedding 一开始没有预训练语义，需要在 SFT 中学出来。</p><h3 id="17-3-GenerationConfig"><a href="#17-3-GenerationConfig" class="headerlink" title="17.3 GenerationConfig"></a>17.3 GenerationConfig</h3><p>SFT 不会自动推导出“最佳 Temperature”。</p><p>训练后可以单独保存一个适合任务的生成配置，但它是推理策略，不是 SFT 权重的一部分。</p><h2 id="18-Base-Model-还是-Instruct-Model"><a href="#18-Base-Model-还是-Instruct-Model" class="headerlink" title="18. Base Model 还是 Instruct Model"></a>18. Base Model 还是 Instruct Model</h2><h3 id="18-1-从-Base-Model-开始"><a href="#18-1-从-Base-Model-开始" class="headerlink" title="18.1 从 Base Model 开始"></a>18.1 从 Base Model 开始</h3><p>优点：</p><ul><li>能清楚观察从续写模型到指令模型的变化。</li><li>行为更少受到已有对话风格干扰。</li><li>适合研究自己的 Chat Template 和后训练配方。</li></ul><p>缺点：</p><ul><li>需要更多高质量数据。</li><li>小数据很难建立稳定的通用指令能力。</li><li>对安全和拒答行为需要额外设计。</li></ul><h3 id="18-2-从-Instruct-Model-开始"><a href="#18-2-从-Instruct-Model-开始" class="headerlink" title="18.2 从 Instruct Model 开始"></a>18.2 从 Instruct Model 开始</h3><p>优点：</p><ul><li>已经会基本对话和遵循指令。</li><li>少量领域数据就可能产生明显适配效果。</li><li>更适合资源有限的业务实验。</li></ul><p>缺点：</p><ul><li>容易覆盖或破坏原有能力。</li><li>必须保持原模型 Chat Template 一致。</li><li>很难仅凭结果判断能力来自原模型还是自己的数据。</li></ul><p>教学系列中，我会分别使用：</p><ul><li>原生 <code>Transformers</code> 篇：小型 Base Causal LM，加深对 labels 的理解。</li><li><code>TRL + LoRA</code> 篇：按照当前 TRL 的标准对话数据与 Chat Template 流程实现。</li></ul><h2 id="19-数据格式比-API-名字更重要"><a href="#19-数据格式比-API-名字更重要" class="headerlink" title="19. 数据格式比 API 名字更重要"></a>19. 数据格式比 API 名字更重要</h2><p>SFT 数据常见四种表示。</p><h3 id="19-1-标准-Language-Modeling"><a href="#19-1-标准-Language-Modeling" class="headerlink" title="19.1 标准 Language Modeling"></a>19.1 标准 Language Modeling</h3><pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span><span class="token property">"text"</span><span class="token operator">:</span> <span class="token string">"天空是蓝色的。"</span><span class="token punctuation">&#125;</span></code></pre><h3 id="19-2-对话-Language-Modeling"><a href="#19-2-对话-Language-Modeling" class="headerlink" title="19.2 对话 Language Modeling"></a>19.2 对话 Language Modeling</h3><pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>  <span class="token property">"messages"</span><span class="token operator">:</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token property">"role"</span><span class="token operator">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token property">"content"</span><span class="token operator">:</span> <span class="token string">"天空是什么颜色？"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token property">"role"</span><span class="token operator">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span> <span class="token property">"content"</span><span class="token operator">:</span> <span class="token string">"晴朗天气下通常呈蓝色。"</span><span class="token punctuation">&#125;</span>  <span class="token punctuation">]</span><span class="token punctuation">&#125;</span></code></pre><h3 id="19-3-标准-Prompt-completion"><a href="#19-3-标准-Prompt-completion" class="headerlink" title="19.3 标准 Prompt-completion"></a>19.3 标准 Prompt-completion</h3><pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>  <span class="token property">"prompt"</span><span class="token operator">:</span> <span class="token string">"天空是"</span><span class="token punctuation">,</span>  <span class="token property">"completion"</span><span class="token operator">:</span> <span class="token string">"蓝色的。"</span><span class="token punctuation">&#125;</span></code></pre><h3 id="19-4-对话-Prompt-completion"><a href="#19-4-对话-Prompt-completion" class="headerlink" title="19.4 对话 Prompt-completion"></a>19.4 对话 Prompt-completion</h3><pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>  <span class="token property">"prompt"</span><span class="token operator">:</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token property">"role"</span><span class="token operator">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span> <span class="token property">"content"</span><span class="token operator">:</span> <span class="token string">"天空是什么颜色？"</span><span class="token punctuation">&#125;</span>  <span class="token punctuation">]</span><span class="token punctuation">,</span>  <span class="token property">"completion"</span><span class="token operator">:</span> <span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token property">"role"</span><span class="token operator">:</span> <span class="token string">"assistant"</span><span class="token punctuation">,</span> <span class="token property">"content"</span><span class="token operator">:</span> <span class="token string">"晴朗天气下通常呈蓝色。"</span><span class="token punctuation">&#125;</span>  <span class="token punctuation">]</span><span class="token punctuation">&#125;</span></code></pre><p>选择格式时应先回答：</p><ol><li>数据是一整段语言模型文本，还是明确的输入输出对？</li><li>是否包含多轮角色结构？</li><li>Loss 应覆盖全序列、Completion，还是只有 assistant？</li></ol><h2 id="20-多轮对话的-Loss-怎么计算"><a href="#20-多轮对话的-Loss-怎么计算" class="headerlink" title="20. 多轮对话的 Loss 怎么计算"></a>20. 多轮对话的 Loss 怎么计算</h2><p>一条多轮数据可能是：</p><pre class="language-text" data-language="text"><code class="language-text">system: 你是一名助教user: 什么是过拟合assistant: ...user: 如何缓解assistant: ...</code></pre><p>使用 assistant-only loss 时：</p><pre class="language-text" data-language="text"><code class="language-text">system       -> -100user 1       -> -100assistant 1  -> 参与 lossuser 2       -> -100assistant 2  -> 参与 loss</code></pre><p>第二个 assistant 回答仍然能够看到第一轮对话，因为第一轮 token 没有从 <code>input_ids</code> 中删除。</p><p>但需要警惕：</p><ul><li>一条样本过长导致后半段被截断。</li><li>system prompt 在不同样本中互相矛盾。</li><li>对话角色没有严格交替。</li><li>assistant 答案中混入用户文本。</li><li>末尾没有正确的 turn-end 或 EOS token。</li></ul><h2 id="21-截断不是简单取前-max-length-个-token"><a href="#21-截断不是简单取前-max-length-个-token" class="headerlink" title="21. 截断不是简单取前 max_length 个 token"></a>21. 截断不是简单取前 max_length 个 token</h2><p>假设：</p><pre class="language-text" data-language="text"><code class="language-text">Prompt 长度 = 500Answer 长度 = 80max_length = 512</code></pre><p>如果直接保留前 512 个 token，最终只有 12 个回答 token 能产生 loss。</p><p>更糟糕的是，可能完整答案和 EOS 都被截掉。</p><p>因此 SFT 应统计：</p><ul><li>Prompt token 长度分布。</li><li>Completion token 长度分布。</li><li>完整序列长度分布。</li><li>被截断样本比例。</li><li>截断后可训练回答 token 数量。</li></ul><p>对于明确的 Prompt-completion 数据，可以制定策略：</p><pre class="language-text" data-language="text"><code class="language-text">优先保留回答  -> Prompt 太长时从左侧或中间压缩上下文  -> 回答太长时再截断回答  -> 始终尽量保留 EOS</code></pre><p>具体策略取决于任务。文档问答可能必须保留问题和关键证据，代码任务则可能不能随意从中间删除。</p><h2 id="22-Padding-token-与-EOS-token-可以相同吗"><a href="#22-Padding-token-与-EOS-token-可以相同吗" class="headerlink" title="22. Padding token 与 EOS token 可以相同吗"></a>22. Padding token 与 EOS token 可以相同吗</h2><p>部分 Causal LM 没有单独的 Padding token，常见处理是：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">if</span> tokenizer<span class="token punctuation">.</span>pad_token_id <span class="token keyword">is</span> <span class="token boolean">None</span><span class="token punctuation">:</span>    tokenizer<span class="token punctuation">.</span>pad_token <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>eos_token</code></pre><p>这不表示所有 EOS 都会被当作 Padding。</p><p>三组张量会共同区分：</p><pre class="language-text" data-language="text"><code class="language-text">真实 EOS:input_id = eos_idattention_mask = 1label = eos_idPadding:input_id = eos_idattention_mask = 0label = -100</code></pre><p>真正需要确认的是 collator 是否正确同步构造 <code>attention_mask</code> 和 <code>labels</code>。</p><h2 id="23-Packing-是什么"><a href="#23-Packing-是什么" class="headerlink" title="23. Packing 是什么"></a>23. Packing 是什么</h2><p>如果大量训练样本很短，而每个样本都 Padding 到 512：</p><pre class="language-text" data-language="text"><code class="language-text">样本 A: 80 token + 432 Padding样本 B: 60 token + 452 Padding样本 C: 90 token + 422 Padding</code></pre><p>计算会大量浪费在 Padding 上。</p><p>Packing 会把多个短样本装入较长序列：</p><pre class="language-text" data-language="text"><code class="language-text">[样本 A][EOS][样本 B][EOS][样本 C][EOS][少量 Padding]</code></pre><p>它提高 token 利用率，但要确认：</p><ul><li>样本边界如何隔开。</li><li>不同样本之间是否允许互相 Attention。</li><li>position id 如何处理。</li><li>label mask 是否与拼接后位置对齐。</li><li>评估集是否也适合 Packing。</li></ul><p>初学阶段应先关闭 Packing，看懂单条样本；确认结果正确后再优化吞吐。</p><h2 id="24-SFT-的梯度到底更新哪些参数"><a href="#24-SFT-的梯度到底更新哪些参数" class="headerlink" title="24. SFT 的梯度到底更新哪些参数"></a>24. SFT 的梯度到底更新哪些参数</h2><h3 id="24-1-Full-Fine-tuning"><a href="#24-1-Full-Fine-tuning" class="headerlink" title="24.1 Full Fine-tuning"></a>24.1 Full Fine-tuning</h3><p>所有允许求梯度的模型参数都更新：</p><pre class="language-text" data-language="text"><code class="language-text">EmbeddingAttention projectionsMLPNormalizationLM Head</code></pre><p>优化器还需要保存参数对应的一阶和二阶状态，因此显存消耗远大于只加载权重进行推理。</p><h3 id="24-2-LoRA-Fine-tuning"><a href="#24-2-LoRA-Fine-tuning" class="headerlink" title="24.2 LoRA Fine-tuning"></a>24.2 LoRA Fine-tuning</h3><p>主干权重冻结，只训练低秩增量：</p><p>$$<br>W’&#x3D;W+\Delta W<br>$$</p><p>$$<br>\Delta W&#x3D;BA<br>$$</p><p>其中：</p><p>$$<br>A\in\mathbb{R}^{r\times d_{in}},\qquad<br>B\in\mathbb{R}^{d_{out}\times r}<br>$$</p><p>当 $r$ 远小于输入输出维度时，可训练参数显著减少。</p><p>但无论 Full Fine-tuning 还是 LoRA，只要数据和目标是监督答案 token，它们做的都可以是 SFT。</p><h2 id="25-怎样判断-SFT-是否有效"><a href="#25-怎样判断-SFT-是否有效" class="headerlink" title="25. 怎样判断 SFT 是否有效"></a>25. 怎样判断 SFT 是否有效</h2><p>只看训练 loss 下降远远不够。</p><h3 id="25-1-验证-Loss"><a href="#25-1-验证-Loss" class="headerlink" title="25.1 验证 Loss"></a>25.1 验证 Loss</h3><p>验证 loss 能观察模型对未见标准答案 token 的拟合程度。</p><p>如果：</p><pre class="language-text" data-language="text"><code class="language-text">train loss 持续下降eval loss 先下降后上升</code></pre><p>通常意味着开始过拟合。</p><h3 id="25-2-生成评估"><a href="#25-2-生成评估" class="headerlink" title="25.2 生成评估"></a>25.2 生成评估</h3><p>实际任务最终要看生成结果：</p><ul><li>是否遵循指令。</li><li>是否正确结束。</li><li>是否符合目标格式。</li><li>是否出现事实错误。</li><li>是否泄漏训练答案。</li><li>对未见表达是否仍然有效。</li></ul><h3 id="25-3-任务指标"><a href="#25-3-任务指标" class="headerlink" title="25.3 任务指标"></a>25.3 任务指标</h3><p>根据任务选择：</p><ul><li>Exact Match。</li><li>F1。</li><li>ROUGE。</li><li>BLEU。</li><li>JSON 解析成功率。</li><li>SQL 执行正确率。</li><li>单元测试通过率。</li><li>人工偏好评估。</li></ul><h3 id="25-4-基线对比"><a href="#25-4-基线对比" class="headerlink" title="25.4 基线对比"></a>25.4 基线对比</h3><p>至少比较：</p><pre class="language-text" data-language="text"><code class="language-text">Base / 原 Instruct ModelvsSFT 后模型</code></pre><p>并固定同一份 Prompt、Chat Template 和 GenerationConfig，否则 Sampling 波动可能被误认为训练收益。</p><h2 id="26-数据质量为什么比堆-Epoch-更重要"><a href="#26-数据质量为什么比堆-Epoch-更重要" class="headerlink" title="26. 数据质量为什么比堆 Epoch 更重要"></a>26. 数据质量为什么比堆 Epoch 更重要</h2><p>SFT 是模仿学习。数据中的模式会直接进入模型行为。</p><p>低质量数据常见问题：</p><ul><li>答案事实错误。</li><li>指令与回答不匹配。</li><li>同一问题存在互相冲突的答案。</li><li>大量机械重复模板。</li><li>回答无意义地过长。</li><li>混入隐私或无授权内容。</li><li>训练集和测试集重复。</li><li>system prompt 规则相互冲突。</li><li>工具调用 JSON 无法解析。</li></ul><p>一个可靠的数据流程至少包括：</p><pre class="language-text" data-language="text"><code class="language-text">来源与许可证检查  -> 字段标准化  -> 空值与乱码清理  -> 去重  -> 长度统计  -> 规则校验  -> 训练/验证/测试划分  -> 人工抽样复核  -> 训练后错误分析</code></pre><h2 id="27-SFT-不能保证模型获得新知识"><a href="#27-SFT-不能保证模型获得新知识" class="headerlink" title="27. SFT 不能保证模型获得新知识"></a>27. SFT 不能保证模型获得新知识</h2><p>SFT 可以教模型如何表达、调用和组合已有能力，也可以让它记住一部分新样本。</p><p>但少量 SFT 数据不一定能稳定注入大量知识。</p><p>例如只用十条医学问答训练后：</p><ul><li>训练问题可能回答得很好。</li><li>相似改写可能有效。</li><li>更广泛医学知识未必可靠。</li><li>原有能力还可能因过拟合而下降。</li></ul><p>知识密集型需求可能还要考虑：</p><ul><li>更高质量和更充分的数据。</li><li>继续预训练。</li><li>检索增强生成 RAG。</li><li>外部工具。</li><li>更严格的事实评估。</li></ul><h2 id="28-常见误解"><a href="#28-常见误解" class="headerlink" title="28. 常见误解"></a>28. 常见误解</h2><h3 id="28-1-SFT-就是给模型加一个分类头"><a href="#28-1-SFT-就是给模型加一个分类头" class="headerlink" title="28.1 SFT 就是给模型加一个分类头"></a>28.1 SFT 就是给模型加一个分类头</h3><p>LLM SFT 通常仍使用 LM Head，输出每个位置上的完整词表 logits。</p><h3 id="28-2-SFT-必须使用-LoRA"><a href="#28-2-SFT-必须使用-LoRA" class="headerlink" title="28.2 SFT 必须使用 LoRA"></a>28.2 SFT 必须使用 LoRA</h3><p>不是。LoRA、QLoRA 和 Full Fine-tuning 都可以实现 SFT。</p><h3 id="28-3-Prompt-位置设成-100-后模型看不到-Prompt"><a href="#28-3-Prompt-位置设成-100-后模型看不到-Prompt" class="headerlink" title="28.3 Prompt 位置设成 -100 后模型看不到 Prompt"></a>28.3 Prompt 位置设成 -100 后模型看不到 Prompt</h3><p>错误。它只是不对 Prompt 目标计算 loss。</p><h3 id="28-4-训练时要设置-Temperature"><a href="#28-4-训练时要设置-Temperature" class="headerlink" title="28.4 训练时要设置 Temperature"></a>28.4 训练时要设置 Temperature</h3><p>普通交叉熵 SFT 不通过 Sampling 选择目标 token；Temperature 主要属于生成阶段。</p><h3 id="28-5-labels-必须在外部手动右移"><a href="#28-5-labels-必须在外部手动右移" class="headerlink" title="28.5 labels 必须在外部手动右移"></a>28.5 labels 必须在外部手动右移</h3><p>Hugging Face Causal LM 通常在 loss 内部完成错位。再次手动右移会出错。</p><h3 id="28-6-Chat-Template-只是为了显示好看"><a href="#28-6-Chat-Template-只是为了显示好看" class="headerlink" title="28.6 Chat Template 只是为了显示好看"></a>28.6 Chat Template 只是为了显示好看</h3><p>错误。它决定角色边界和特殊 token，是模型训练分布的一部分。</p><h3 id="28-7-Loss-越低，模型就一定越有帮助"><a href="#28-7-Loss-越低，模型就一定越有帮助" class="headerlink" title="28.7 Loss 越低，模型就一定越有帮助"></a>28.7 Loss 越低，模型就一定越有帮助</h3><p>Loss 只衡量对目标 token 的拟合。数据偏差、事实性、安全性和真实任务效果还要单独评估。</p><h3 id="28-8-只保存-LoRA-Adapter-就可以独立推理"><a href="#28-8-只保存-LoRA-Adapter-就可以独立推理" class="headerlink" title="28.8 只保存 LoRA Adapter 就可以独立推理"></a>28.8 只保存 LoRA Adapter 就可以独立推理</h3><p>Adapter 通常还需要对应 Base Model；除非将权重合并并保存为完整模型。</p><h2 id="29-把-SFT-映射回我已经学过的-Hugging-Face-API"><a href="#29-把-SFT-映射回我已经学过的-Hugging-Face-API" class="headerlink" title="29. 把 SFT 映射回我已经学过的 Hugging Face API"></a>29. 把 SFT 映射回我已经学过的 Hugging Face API</h2><p>现在可以把已有知识重新排列：</p><pre class="language-text" data-language="text"><code class="language-text">Dataset  -> 提供 prompt / completion / messagesTokenizer + Chat Template  -> 构造 input_idsData Collator  -> 动态 Padding  -> 构造 attention_mask  -> 用 -100 补 labelsAutoModelForCausalLM.forward  -> 输出 [B, T, V] logits  -> 内部 shift labels  -> 计算 token-level lossTrainer / SFTTrainer  -> backward  -> AdamW  -> scheduler  -> checkpointGenerationConfig + generate  -> 训练完成后生成回答</code></pre><p><img src="/images/nlp-blog/sft-hugging-face-map.svg" alt="SFT概念与Hugging Face API的对应关系"></p><h2 id="30-接下来两篇会实现什么"><a href="#30-接下来两篇会实现什么" class="headerlink" title="30. 接下来两篇会实现什么"></a>30. 接下来两篇会实现什么</h2><h3 id="第二篇：原生-Transformers-实现"><a href="#第二篇：原生-Transformers-实现" class="headerlink" title="第二篇：原生 Transformers 实现"></a>第二篇：原生 Transformers 实现</h3><p>下一篇会刻意不使用 <code>SFTTrainer</code>，亲手完成：</p><pre class="language-text" data-language="text"><code class="language-text">本地 prompt-completion 数据  -> 格式化 Prompt  -> 分开 Tokenize Prompt 与 Answer  -> 拼接 input_ids  -> Prompt labels 设为 -100  -> 自定义动态 Padding Collator  -> AutoModelForCausalLM  -> Trainer  -> 保存并 generate</code></pre><p>这样可以直接检查每个张量。</p><h3 id="第三篇：TRL-SFTTrainer-LoRA"><a href="#第三篇：TRL-SFTTrainer-LoRA" class="headerlink" title="第三篇：TRL SFTTrainer + LoRA"></a>第三篇：TRL SFTTrainer + LoRA</h3><p>理解底层后，再把重复工作交给 TRL：</p><pre class="language-text" data-language="text"><code class="language-text">messages 数据  -> Chat Template  -> assistant-only loss  -> SFTTrainer  -> PEFT LoRA  -> Adapter 保存、加载与可选合并</code></pre><h2 id="31-总结"><a href="#31-总结" class="headerlink" title="31. 总结"></a>31. 总结</h2><p>SFT 并没有创造一种新的语言模型目标。</p><p>它仍然可以还原成：</p><p>$$<br>\text{根据历史 token，提升正确下一个 token 的概率}<br>$$</p><p>真正需要掌握的是：</p><ol><li><strong>数据结构</strong>：哪些内容属于 Prompt，哪些属于目标回答。</li><li><strong>Chat Template</strong>：角色如何变成模型真实看到的 token。</li><li><strong>三种 Mask</strong>：Causal Mask、Attention Mask 和 Label Mask 各管什么。</li><li><strong>Loss 范围</strong>：全序列、Completion-only 或 Assistant-only。</li><li><strong>训练与生成的边界</strong>：SFT 优化交叉熵，Sampling 决定推理时怎样选 token。</li><li><strong>参数更新方式</strong>：Full Fine-tuning 和 LoRA 都能承载同一个 SFT 目标。</li></ol><p>如果只记住一句话，可以记成：</p><blockquote><p>SFT 是把高质量的输入输出示范变成一串 Causal LM token，并只在希望模型学习的位置计算 next-token loss。</p></blockquote><p>下一篇将把这句话变成真正的 <code>input_ids</code>、<code>attention_mask</code>、<code>labels</code> 和 Hugging Face <code>Trainer</code> 代码。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://huggingface.co/docs/trl/sft_trainer">Hugging Face TRL：SFT Trainer</a></li><li><a href="https://huggingface.co/docs/trl/dataset_formats">Hugging Face TRL：Dataset formats</a></li><li><a href="https://huggingface.co/docs/transformers/chat_templating">Hugging Face Transformers：Chat templates</a></li><li><a href="https://huggingface.co/docs/transformers/tasks/language_modeling">Hugging Face Transformers：Causal language modeling</a></li><li><a href="https://huggingface.co/docs/peft/main/conceptual_guides/lora">Hugging Face PEFT：LoRA</a></li><li><a href="https://arxiv.org/abs/2203.02155">Training language models to follow instructions with human feedback</a></li><li><a href="https://arxiv.org/abs/2212.10560">Self-Instruct: Aligning Language Models with Self-Generated Instructions</a></li></ul>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/24/nlp-sft-principles/</id>
    <link href="https://lsworl.github.io/2026/08/24/nlp-sft-principles/"/>
    <published>2026-08-23T16:00:00.000Z</published>
    <summary>承接Mini-GPT与Hugging Face学习记录，从next-token prediction出发理解SFT在LLM后训练中的位置，拆解Chat Template、label shift、attention mask、loss mask与assistant-only loss，并手算一次完整SFT损失。</summary>
    <title>SFT（一）：从Next Token Prediction到指令微调</title>
    <updated>2026-08-24T15:57:08.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Hugging Face" scheme="https://lsworl.github.io/tags/Hugging-Face/"/>
    <category term="Transformers" scheme="https://lsworl.github.io/tags/Transformers/"/>
    <category term="Datasets" scheme="https://lsworl.github.io/tags/Datasets/"/>
    <category term="Trainer" scheme="https://lsworl.github.io/tags/Trainer/"/>
    <category term="迁移学习" scheme="https://lsworl.github.io/tags/%E8%BF%81%E7%A7%BB%E5%AD%A6%E4%B9%A0/"/>
    <content>
      <![CDATA[<p>前面的文章已经从零实现了 Transformer、Mini-GPT 和 RoPE。理解这些底层原理之后，一个很自然的问题是：</p><blockquote><p>实际项目中，难道每次都要从头实现网络、训练 Tokenizer，再用海量数据预训练模型吗？</p></blockquote><p>通常不需要。</p><p>很多任务可以从一个已经预训练好的模型出发，只添加或替换任务头，再使用自己的数据进行微调。Hugging Face 正是把“寻找模型、下载权重、统一调用、处理数据、训练评估和分享结果”串起来的一套生态。</p><p>不过，初学者很容易把下面这些概念混在一起：</p><ul><li>Hugging Face 网站。</li><li>Hugging Face Hub。</li><li><code>transformers</code> Python 库。</li><li><code>datasets</code> Python 库。</li><li><code>pipeline()</code>。</li><li><code>AutoTokenizer</code> 与 <code>AutoModel</code>。</li><li><code>Trainer</code>。</li><li>Spaces 与在线推理服务。</li></ul><p>本文会先把它们拆开，再通过一个中文情感分类项目完整走通：</p><pre class="language-text" data-language="text"><code class="language-text">选择预训练模型  -> 加载 Tokenizer  -> 文本转成模型输入  -> 加载任务模型  -> 手动完成一次前向推理  -> 构造 Dataset  -> 批量 Tokenize  -> 动态 Padding  -> Trainer 微调与评估  -> 保存到本地  -> 重新加载并推理  -> 可选上传到 Hub</code></pre><span id="more"></span><p><img src="https://lsworl.github.io/images/nlp-blog/hugging-face-ecosystem.svg" alt="Hugging Face生态总览"></p><h2 id="1-Hugging-Face-到底是什么"><a href="#1-Hugging-Face-到底是什么" class="headerlink" title="1. Hugging Face 到底是什么"></a>1. Hugging Face 到底是什么</h2><p>Hugging Face 不是单独一个库，也不只是一个模型下载网站。可以从三个层面理解它。</p><h3 id="1-1-社区与平台"><a href="#1-1-社区与平台" class="headerlink" title="1.1 社区与平台"></a>1.1 社区与平台</h3><p>Hugging Face Hub 是一个围绕机器学习资产构建的协作平台。常见资产包括：</p><ul><li><strong>Models</strong>：模型配置、Tokenizer、权重和 Model Card。</li><li><strong>Datasets</strong>：训练、验证与测试数据，以及 Dataset Card。</li><li><strong>Spaces</strong>：可以在线运行的机器学习演示或应用。</li></ul><p>可以把 Hub 粗略理解为“面向机器学习项目的代码与资产仓库平台”，但模型仓库不只有代码，还包含体积很大的参数文件、任务元数据、许可证和在线推理信息。</p><h3 id="1-2-Python-库生态"><a href="#1-2-Python-库生态" class="headerlink" title="1.2 Python 库生态"></a>1.2 Python 库生态</h3><table><thead><tr><th>库</th><th>主要作用</th></tr></thead><tbody><tr><td><code>transformers</code></td><td>加载和使用 Transformer 模型、Tokenizer、Trainer</td></tr><tr><td><code>datasets</code></td><td>加载、处理、缓存和流式读取数据集</td></tr><tr><td><code>huggingface_hub</code></td><td>搜索、下载、上传和管理 Hub 仓库</td></tr><tr><td><code>tokenizers</code></td><td>高性能 Tokenizer 实现</td></tr><tr><td><code>accelerate</code></td><td>设备管理、混合精度和分布式训练</td></tr><tr><td><code>evaluate</code></td><td>加载和计算评估指标</td></tr><tr><td><code>safetensors</code></td><td>安全、快速地保存和加载张量</td></tr><tr><td><code>peft</code></td><td>LoRA 等参数高效微调方法</td></tr></tbody></table><p>本文的主线使用前五项中的核心能力，并把 <code>evaluate</code> 作为可选工具介绍。</p><h3 id="1-3-一组统一约定"><a href="#1-3-一组统一约定" class="headerlink" title="1.3 一组统一约定"></a>1.3 一组统一约定</h3><p>Hugging Face 很重要的一点不是“收集了很多模型”，而是让大量不同架构能够使用相似接口：</p><pre class="language-python" data-language="python"><code class="language-python">tokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span>model <span class="token operator">=</span> AutoModelForSequenceClassification<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span></code></pre><p>模型可以从 BERT 换成 RoBERTa，代码骨架仍然相似。差异主要由仓库中的 <code>config.json</code>、Tokenizer 配置与权重文件描述。</p><h2 id="2-Hub、Transformers-和-PyTorch-的关系"><a href="#2-Hub、Transformers-和-PyTorch-的关系" class="headerlink" title="2. Hub、Transformers 和 PyTorch 的关系"></a>2. Hub、Transformers 和 PyTorch 的关系</h2><p>这三个概念处在不同层次：</p><pre class="language-text" data-language="text"><code class="language-text">Hugging Face Hub  保存与分发配置、Tokenizer、权重、说明文档Transformers  根据配置构造模型类并下载权重，提供统一高级 APIPyTorch  执行张量运算、自动微分和参数更新</code></pre><p>当我们执行：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> AutoModelForSequenceClassification<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span></code></pre><p>背后大致发生：</p><ol><li>根据 <code>model_id</code> 定位 Hub 仓库或本地目录。</li><li>读取 <code>config.json</code>。</li><li>从配置识别模型架构。</li><li>实例化对应的 PyTorch <code>nn.Module</code>。</li><li>下载并加载模型权重。</li><li>返回可以正常前向传播的模型对象。</li></ol><p>因此，Transformers 没有替代 PyTorch。它是在 PyTorch 之上提供模型实现、配置系统、预训练权重和训练工具。</p><h2 id="3-模型仓库里有什么"><a href="#3-模型仓库里有什么" class="headerlink" title="3. 模型仓库里有什么"></a>3. 模型仓库里有什么</h2><p>一个典型的 Transformers 模型仓库可能包含：</p><pre class="language-text" data-language="text"><code class="language-text">README.mdconfig.jsonmodel.safetensorstokenizer.jsontokenizer_config.jsonspecial_tokens_map.jsonvocab.txt / vocab.json / merges.txtgeneration_config.json</code></pre><p>不同模型的文件并不完全相同。</p><p><img src="https://lsworl.github.io/images/nlp-blog/hugging-face-model-repository.svg" alt="Hugging Face模型仓库结构"></p><h3 id="3-1-README-md"><a href="#3-1-README-md" class="headerlink" title="3.1 README.md"></a>3.1 <code>README.md</code></h3><p>在模型仓库中，它通常会渲染成 Model Card，应说明：</p><ul><li>模型适合做什么。</li><li>不适合做什么。</li><li>训练数据与训练方法。</li><li>评估结果。</li><li>已知偏差与限制。</li><li>许可证。</li><li>使用示例。</li></ul><p>下载数高不代表模型适合你的任务。选择模型时，Model Card 应该是第一阅读入口。</p><h3 id="3-2-config-json"><a href="#3-2-config-json" class="headerlink" title="3.2 config.json"></a>3.2 <code>config.json</code></h3><p>它描述模型结构和任务相关配置，例如：</p><pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>  <span class="token property">"model_type"</span><span class="token operator">:</span> <span class="token string">"bert"</span><span class="token punctuation">,</span>  <span class="token property">"hidden_size"</span><span class="token operator">:</span> <span class="token number">768</span><span class="token punctuation">,</span>  <span class="token property">"num_hidden_layers"</span><span class="token operator">:</span> <span class="token number">12</span><span class="token punctuation">,</span>  <span class="token property">"num_attention_heads"</span><span class="token operator">:</span> <span class="token number">12</span><span class="token punctuation">,</span>  <span class="token property">"vocab_size"</span><span class="token operator">:</span> <span class="token number">21128</span><span class="token punctuation">,</span>  <span class="token property">"num_labels"</span><span class="token operator">:</span> <span class="token number">2</span><span class="token punctuation">&#125;</span></code></pre><p>实际字段由模型架构决定。</p><h3 id="3-3-权重文件"><a href="#3-3-权重文件" class="headerlink" title="3.3 权重文件"></a>3.3 权重文件</h3><p>常见格式包括：</p><pre class="language-text" data-language="text"><code class="language-text">model.safetensorspytorch_model.bin</code></pre><p>大型模型还可能被切成多个 shard，并使用索引文件记录每个参数所在位置。</p><h3 id="3-4-Tokenizer-文件"><a href="#3-4-Tokenizer-文件" class="headerlink" title="3.4 Tokenizer 文件"></a>3.4 Tokenizer 文件</h3><p>Tokenizer 与模型必须匹配。它们共同决定：</p><ul><li>词表。</li><li>子词切分规则。</li><li>特殊 token。</li><li>padding 方向。</li><li>最大长度等默认行为。</li></ul><p>模型权重和 Tokenizer 随意混用，轻则效果异常，重则 token id 超出词表范围。</p><h2 id="4-三个容易混淆的词：架构、Checkpoint-和任务"><a href="#4-三个容易混淆的词：架构、Checkpoint-和任务" class="headerlink" title="4. 三个容易混淆的词：架构、Checkpoint 和任务"></a>4. 三个容易混淆的词：架构、Checkpoint 和任务</h2><h3 id="4-1-架构-Architecture"><a href="#4-1-架构-Architecture" class="headerlink" title="4.1 架构 Architecture"></a>4.1 架构 Architecture</h3><p>架构描述模型骨架，例如：</p><pre class="language-text" data-language="text"><code class="language-text">BERTRoBERTaGPT-2T5ViT</code></pre><h3 id="4-2-Checkpoint"><a href="#4-2-Checkpoint" class="headerlink" title="4.2 Checkpoint"></a>4.2 Checkpoint</h3><p>Checkpoint 是某个架构在特定训练阶段保存的参数和配置。例如：</p><pre class="language-text" data-language="text"><code class="language-text">google-bert/bert-base-chinese</code></pre><p>它是一个具体 Hub 仓库 ID，不等于“所有 BERT”。</p><h3 id="4-3-任务-Task"><a href="#4-3-任务-Task" class="headerlink" title="4.3 任务 Task"></a>4.3 任务 Task</h3><p>同一 Backbone 可以连接不同任务头：</p><pre class="language-text" data-language="text"><code class="language-text">BERT Backbone  -> Sequence Classification Head  文本分类  -> Token Classification Head     命名实体识别  -> Question Answering Head        抽取式问答  -> Masked LM Head                 掩码语言模型</code></pre><p>这就是为什么 Transformers 提供多种 <code>AutoModelFor...</code> 类。</p><h2 id="5-安装与环境准备"><a href="#5-安装与环境准备" class="headerlink" title="5. 安装与环境准备"></a>5. 安装与环境准备</h2><p>建议为项目创建独立虚拟环境，再安装稳定发布版本：</p><pre class="language-bash" data-language="bash"><code class="language-bash">python <span class="token parameter variable">-m</span> venv .venv</code></pre><p>Windows PowerShell：</p><pre class="language-powershell" data-language="powershell"><code class="language-powershell"><span class="token punctuation">.</span>\<span class="token punctuation">.</span>venv\Scripts\Activate<span class="token punctuation">.</span>ps1</code></pre><p>安装 PyTorch 时应根据 CPU 或 CUDA 环境使用 PyTorch 官方给出的命令。安装 Hugging Face 相关库：</p><pre class="language-bash" data-language="bash"><code class="language-bash">pip <span class="token function">install</span> transformers datasets accelerate safetensors</code></pre><p>如果还要使用官方评估指标：</p><pre class="language-bash" data-language="bash"><code class="language-bash">pip <span class="token function">install</span> evaluate</code></pre><p>检查版本：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">import</span> transformers<span class="token keyword">import</span> datasets<span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"torch:"</span><span class="token punctuation">,</span> torch<span class="token punctuation">.</span>__version__<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"transformers:"</span><span class="token punctuation">,</span> transformers<span class="token punctuation">.</span>__version__<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"datasets:"</span><span class="token punctuation">,</span> datasets<span class="token punctuation">.</span>__version__<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"cuda available:"</span><span class="token punctuation">,</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_available<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><h3 id="5-1-为什么要记录版本"><a href="#5-1-为什么要记录版本" class="headerlink" title="5.1 为什么要记录版本"></a>5.1 为什么要记录版本</h3><p>Hugging Face 生态更新较快，不同大版本可能调整参数名、默认值或返回类型。真实项目应记录依赖：</p><pre class="language-bash" data-language="bash"><code class="language-bash">pip freeze <span class="token operator">></span> requirements-lock.txt</code></pre><p>教程代码无法替代项目自己的版本锁定。遇到 API 报错时，首先核对：</p><pre class="language-text" data-language="text"><code class="language-text">本地安装版本官方文档所选版本示例代码对应版本</code></pre><h2 id="6-第一层-API：用-pipeline-快速推理"><a href="#6-第一层-API：用-pipeline-快速推理" class="headerlink" title="6. 第一层 API：用 pipeline() 快速推理"></a>6. 第一层 API：用 <code>pipeline()</code> 快速推理</h2><p><code>pipeline()</code> 是最方便的推理入口。以中文情感分析为例：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> pipelinemodel_id <span class="token operator">=</span> <span class="token string">"uer/roberta-base-finetuned-jd-binary-chinese"</span>classifier <span class="token operator">=</span> pipeline<span class="token punctuation">(</span>    task<span class="token operator">=</span><span class="token string">"text-classification"</span><span class="token punctuation">,</span>    model<span class="token operator">=</span>model_id<span class="token punctuation">,</span><span class="token punctuation">)</span>results <span class="token operator">=</span> classifier<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token string">"这个耳机音质清晰，佩戴也很舒服。"</span><span class="token punctuation">,</span>    <span class="token string">"包装破损，使用两天就无法开机。"</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>results<span class="token punctuation">)</span></code></pre><p>首次运行通常会下载模型，之后优先从本地缓存读取。</p><h3 id="6-1-不要猜测-LABEL-0-的含义"><a href="#6-1-不要猜测-LABEL-0-的含义" class="headerlink" title="6.1 不要猜测 LABEL_0 的含义"></a>6.1 不要猜测 <code>LABEL_0</code> 的含义</h3><p>有些模型会输出：</p><pre class="language-text" data-language="text"><code class="language-text">LABEL_0LABEL_1</code></pre><p>不能想当然地认为 <code>LABEL_1</code> 一定表示正面。正确做法是查看 Model Card 和模型配置：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span>classifier<span class="token punctuation">.</span>model<span class="token punctuation">.</span>config<span class="token punctuation">.</span>id2label<span class="token punctuation">)</span></code></pre><p>模型作者若没有正确填写标签映射，使用者还需要结合训练数据说明确认语义。</p><h3 id="6-2-pipeline-不是模型本身"><a href="#6-2-pipeline-不是模型本身" class="headerlink" title="6.2 pipeline 不是模型本身"></a>6.2 <code>pipeline</code> 不是模型本身</h3><p><code>pipeline</code> 是一个推理工作流封装，大致包含三个阶段：</p><pre class="language-text" data-language="text"><code class="language-text">preprocess  文本 -> Tokenizer -> 模型输入张量forward  张量 -> 模型 -> logitspostprocess  logits -> Softmax -> 标签与分数</code></pre><p><img src="https://lsworl.github.io/images/nlp-blog/hugging-face-pipeline.svg" alt="Pipeline内部三阶段"></p><p>它适合：</p><ul><li>快速验证模型。</li><li>编写原型。</li><li>常规批量推理。</li></ul><p>需要精确控制张量、损失函数或训练过程时，应继续下潜到 <code>AutoTokenizer</code> 和 <code>AutoModel</code>。</p><h2 id="7-pipeline-中的任务字符串"><a href="#7-pipeline-中的任务字符串" class="headerlink" title="7. pipeline 中的任务字符串"></a>7. <code>pipeline</code> 中的任务字符串</h2><p>常见任务包括：</p><table><thead><tr><th>任务</th><th>常见 <code>task</code></th></tr></thead><tbody><tr><td>文本分类</td><td><code>text-classification</code></td></tr><tr><td>文本生成</td><td><code>text-generation</code></td></tr><tr><td>命名实体识别</td><td><code>token-classification</code></td></tr><tr><td>抽取式问答</td><td><code>question-answering</code></td></tr><tr><td>掩码填空</td><td><code>fill-mask</code></td></tr><tr><td>摘要</td><td><code>summarization</code></td></tr><tr><td>翻译</td><td><code>translation</code> 或模型对应任务</td></tr><tr><td>图像分类</td><td><code>image-classification</code></td></tr><tr><td>自动语音识别</td><td><code>automatic-speech-recognition</code></td></tr></tbody></table><p>模型必须与任务兼容。不能把仅有 BERT Masked LM 头的 Checkpoint 当作自回归文本生成模型，也不能认为任何文本模型都能直接做分类。</p><h3 id="7-1-显式指定模型"><a href="#7-1-显式指定模型" class="headerlink" title="7.1 显式指定模型"></a>7.1 显式指定模型</h3><p>下面的写法虽然短：</p><pre class="language-python" data-language="python"><code class="language-python">classifier <span class="token operator">=</span> pipeline<span class="token punctuation">(</span><span class="token string">"text-classification"</span><span class="token punctuation">)</span></code></pre><p>但它会选择一个默认模型。教学实验可以使用，正式项目最好显式指定：</p><pre class="language-python" data-language="python"><code class="language-python">classifier <span class="token operator">=</span> pipeline<span class="token punctuation">(</span>    <span class="token string">"text-classification"</span><span class="token punctuation">,</span>    model<span class="token operator">=</span><span class="token string">"组织名/模型名"</span><span class="token punctuation">,</span>    revision<span class="token operator">=</span><span class="token string">"明确的分支、标签或提交哈希"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这样更容易复现，也能避免默认模型改变带来的行为漂移。</p><h2 id="8-第二层-API：AutoTokenizer"><a href="#8-第二层-API：AutoTokenizer" class="headerlink" title="8. 第二层 API：AutoTokenizer"></a>8. 第二层 API：<code>AutoTokenizer</code></h2><p>Tokenizer 把字符串转换成模型能够接收的整数张量：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> AutoTokenizermodel_id <span class="token operator">=</span> <span class="token string">"google-bert/bert-base-chinese"</span>tokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span>encoded <span class="token operator">=</span> tokenizer<span class="token punctuation">(</span>    <span class="token string">"Hugging Face 让模型复用更方便"</span><span class="token punctuation">,</span>    return_tensors<span class="token operator">=</span><span class="token string">"pt"</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">for</span> name<span class="token punctuation">,</span> tensor <span class="token keyword">in</span> encoded<span class="token punctuation">.</span>items<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>name<span class="token punctuation">,</span> tensor<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>tensor<span class="token punctuation">)</span></code></pre><p>对 BERT 类模型，常见输出包括：</p><pre class="language-text" data-language="text"><code class="language-text">input_idsattention_masktoken_type_ids  某些模型有，某些没有</code></pre><h3 id="8-1-input-ids"><a href="#8-1-input-ids" class="headerlink" title="8.1 input_ids"></a>8.1 <code>input_ids</code></h3><p>Tokenization 的概念流程为：</p><pre class="language-text" data-language="text"><code class="language-text">原始文本  -> 规范化  -> 子词切分  -> 加特殊 token  -> 查词表得到 token id</code></pre><p>可以直接查看：</p><pre class="language-python" data-language="python"><code class="language-python">tokens <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>tokenize<span class="token punctuation">(</span><span class="token string">"我正在学习Hugging Face"</span><span class="token punctuation">)</span>ids <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>convert_tokens_to_ids<span class="token punctuation">(</span>tokens<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>tokens<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>ids<span class="token punctuation">)</span></code></pre><p>也可以把完整编码转换回 token：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span>tokenizer<span class="token punctuation">.</span>convert_ids_to_tokens<span class="token punctuation">(</span>encoded<span class="token punctuation">[</span><span class="token string">"input_ids"</span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><h3 id="8-2-特殊-token"><a href="#8-2-特殊-token" class="headerlink" title="8.2 特殊 token"></a>8.2 特殊 token</h3><p>BERT 类 Tokenizer 常在序列两侧加入类似：</p><pre class="language-text" data-language="text"><code class="language-text">[CLS] 文本 token ... [SEP]</code></pre><p>不同架构可能使用不同特殊 token。不要在业务代码里硬编码它们，应使用 Tokenizer 的配置：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span>tokenizer<span class="token punctuation">.</span>cls_token<span class="token punctuation">,</span> tokenizer<span class="token punctuation">.</span>cls_token_id<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>tokenizer<span class="token punctuation">.</span>sep_token<span class="token punctuation">,</span> tokenizer<span class="token punctuation">.</span>sep_token_id<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>tokenizer<span class="token punctuation">.</span>pad_token<span class="token punctuation">,</span> tokenizer<span class="token punctuation">.</span>pad_token_id<span class="token punctuation">)</span></code></pre><h3 id="8-3-attention-mask"><a href="#8-3-attention-mask" class="headerlink" title="8.3 attention_mask"></a>8.3 <code>attention_mask</code></h3><p>在常见文本模型中：</p><pre class="language-text" data-language="text"><code class="language-text">1 表示真实 token0 表示 Padding token</code></pre><p>模型利用它避免把补齐部分当成有效内容。</p><p><img src="https://lsworl.github.io/images/nlp-blog/hugging-face-tokenizer-tensors.svg" alt="Tokenizer输出张量"></p><h2 id="9-Padding-与-Truncation"><a href="#9-Padding-与-Truncation" class="headerlink" title="9. Padding 与 Truncation"></a>9. Padding 与 Truncation</h2><p>一个 batch 中的句子长度往往不同，但张量需要组成规则矩形。</p><pre class="language-python" data-language="python"><code class="language-python">batch <span class="token operator">=</span> tokenizer<span class="token punctuation">(</span>    <span class="token punctuation">[</span>        <span class="token string">"这个产品很好用。"</span><span class="token punctuation">,</span>        <span class="token string">"外观不错，但是续航时间没有达到宣传效果。"</span><span class="token punctuation">,</span>    <span class="token punctuation">]</span><span class="token punctuation">,</span>    padding<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    truncation<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    max_length<span class="token operator">=</span><span class="token number">64</span><span class="token punctuation">,</span>    return_tensors<span class="token operator">=</span><span class="token string">"pt"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="9-1-padding-True"><a href="#9-1-padding-True" class="headerlink" title="9.1 padding=True"></a>9.1 <code>padding=True</code></h3><p>把当前 batch 中较短的样本补到最长样本的长度：</p><pre class="language-text" data-language="text"><code class="language-text">样本 A：8 token样本 B：15 token组成 batch 后都变成 15 token</code></pre><h3 id="9-2-padding-quot-max-length-quot"><a href="#9-2-padding-quot-max-length-quot" class="headerlink" title="9.2 padding=&quot;max_length&quot;"></a>9.2 <code>padding=&quot;max_length&quot;</code></h3><p>所有样本都补到指定的 <code>max_length</code>。优点是形状固定，缺点是短句很多时会浪费计算。</p><h3 id="9-3-truncation-True"><a href="#9-3-truncation-True" class="headerlink" title="9.3 truncation=True"></a>9.3 <code>truncation=True</code></h3><p>当 token 数超过允许长度时截断。必须意识到：</p><blockquote><p>截断不会“压缩理解”被删除的文本，它会直接丢弃超出部分。</p></blockquote><p>如果关键信息出现在长文末尾，需要考虑滑动窗口、分块、长上下文模型或任务专用聚合策略。</p><h2 id="10-动态-Padding-为什么更节省计算"><a href="#10-动态-Padding-为什么更节省计算" class="headerlink" title="10. 动态 Padding 为什么更节省计算"></a>10. 动态 Padding 为什么更节省计算</h2><p>假设整个数据集最长为 512，但某个 batch 中最长只有 83。</p><p>固定 Padding：</p><pre class="language-text" data-language="text"><code class="language-text">[B, 512]</code></pre><p>动态 Padding：</p><pre class="language-text" data-language="text"><code class="language-text">[B, 83]</code></pre><p>Self-Attention 的主要计算与序列长度平方相关：</p><p>$$<br>O(T^2)<br>$$</p><p>因此避免不必要的 Padding 往往能节省明显的显存和计算。</p><p>Transformers 提供：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> DataCollatorWithPaddingdata_collator <span class="token operator">=</span> DataCollatorWithPadding<span class="token punctuation">(</span>tokenizer<span class="token operator">=</span>tokenizer<span class="token punctuation">)</span></code></pre><p>它在组 batch 时才按照当前 batch 的最长序列进行补齐。</p><h2 id="11-AutoModel-与-AutoModelFor-有什么区别"><a href="#11-AutoModel-与-AutoModelFor-有什么区别" class="headerlink" title="11. AutoModel 与 AutoModelFor... 有什么区别"></a>11. <code>AutoModel</code> 与 <code>AutoModelFor...</code> 有什么区别</h2><h3 id="11-1-AutoModel"><a href="#11-1-AutoModel" class="headerlink" title="11.1 AutoModel"></a>11.1 <code>AutoModel</code></h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> AutoModelbackbone <span class="token operator">=</span> AutoModel<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span></code></pre><p>通常只加载基础 Backbone，返回隐藏状态。例如 BERT 常见输出形状：</p><pre class="language-text" data-language="text"><code class="language-text">last_hidden_state: [B, T, H]</code></pre><p>它适合：</p><ul><li>提取文本表示。</li><li>自己设计任务头。</li><li>研究中间隐藏状态。</li></ul><h3 id="11-2-AutoModelForSequenceClassification"><a href="#11-2-AutoModelForSequenceClassification" class="headerlink" title="11.2 AutoModelForSequenceClassification"></a>11.2 <code>AutoModelForSequenceClassification</code></h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> AutoModelForSequenceClassificationmodel <span class="token operator">=</span> AutoModelForSequenceClassification<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    model_id<span class="token punctuation">,</span>    num_labels<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>它在 Backbone 上增加文本分类头，输出：</p><pre class="language-text" data-language="text"><code class="language-text">logits: [B, num_labels]</code></pre><h3 id="11-3-常用-AutoModel-类"><a href="#11-3-常用-AutoModel-类" class="headerlink" title="11.3 常用 AutoModel 类"></a>11.3 常用 AutoModel 类</h3><table><thead><tr><th>类</th><th>任务</th></tr></thead><tbody><tr><td><code>AutoModel</code></td><td>基础隐藏表示</td></tr><tr><td><code>AutoModelForSequenceClassification</code></td><td>句子或文本分类</td></tr><tr><td><code>AutoModelForTokenClassification</code></td><td>每个 token 分类</td></tr><tr><td><code>AutoModelForQuestionAnswering</code></td><td>抽取答案起止位置</td></tr><tr><td><code>AutoModelForMaskedLM</code></td><td>预测被遮住的 token</td></tr><tr><td><code>AutoModelForCausalLM</code></td><td>自回归生成下一个 token</td></tr><tr><td><code>AutoModelForSeq2SeqLM</code></td><td>翻译、摘要等序列到序列生成</td></tr></tbody></table><h2 id="12-手动完成一次分类推理"><a href="#12-手动完成一次分类推理" class="headerlink" title="12. 手动完成一次分类推理"></a>12. 手动完成一次分类推理</h2><p>现在不使用 <code>pipeline</code>，把内部过程展开。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">from</span> transformers <span class="token keyword">import</span> <span class="token punctuation">(</span>    AutoModelForSequenceClassification<span class="token punctuation">,</span>    AutoTokenizer<span class="token punctuation">,</span><span class="token punctuation">)</span>model_id <span class="token operator">=</span> <span class="token string">"uer/roberta-base-finetuned-jd-binary-chinese"</span>tokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span>model <span class="token operator">=</span> AutoModelForSequenceClassification<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span>texts <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token string">"物流很快，商品质量也很好。"</span><span class="token punctuation">,</span>    <span class="token string">"用了几分钟就坏了，非常失望。"</span><span class="token punctuation">,</span><span class="token punctuation">]</span>inputs <span class="token operator">=</span> tokenizer<span class="token punctuation">(</span>    texts<span class="token punctuation">,</span>    padding<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    truncation<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    return_tensors<span class="token operator">=</span><span class="token string">"pt"</span><span class="token punctuation">,</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>inference_mode<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    outputs <span class="token operator">=</span> model<span class="token punctuation">(</span><span class="token operator">**</span>inputs<span class="token punctuation">)</span>logits <span class="token operator">=</span> outputs<span class="token punctuation">.</span>logitsprobabilities <span class="token operator">=</span> torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>predicted_ids <span class="token operator">=</span> probabilities<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token keyword">for</span> text<span class="token punctuation">,</span> class_id<span class="token punctuation">,</span> probs <span class="token keyword">in</span> <span class="token builtin">zip</span><span class="token punctuation">(</span>    texts<span class="token punctuation">,</span>    predicted_ids<span class="token punctuation">.</span>tolist<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    probabilities<span class="token punctuation">.</span>tolist<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    label <span class="token operator">=</span> model<span class="token punctuation">.</span>config<span class="token punctuation">.</span>id2label<span class="token punctuation">[</span>class_id<span class="token punctuation">]</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>text<span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>label<span class="token punctuation">,</span> probs<span class="token punctuation">)</span></code></pre><h3 id="12-1-为什么模型输出-logits-而不是概率"><a href="#12-1-为什么模型输出-logits-而不是概率" class="headerlink" title="12.1 为什么模型输出 logits 而不是概率"></a>12.1 为什么模型输出 logits 而不是概率</h3><p>分类头通常输出任意实数：</p><p>$$<br>z&#x3D;[z_1,z_2,\ldots,z_C]<br>$$</p><p>Softmax 才把它们转换为概率：</p><p>$$<br>P(y&#x3D;c\mid x)<br>&#x3D;\frac{e^{z_c}}{\sum_{j&#x3D;1}^{C}e^{z_j}}<br>$$</p><p>训练时交叉熵会在数值稳定的实现中直接接收 logits，所以不要在传给交叉熵之前手动 Softmax。</p><h3 id="12-2-outputs-为什么可以点号访问"><a href="#12-2-outputs-为什么可以点号访问" class="headerlink" title="12.2 outputs 为什么可以点号访问"></a>12.2 <code>outputs</code> 为什么可以点号访问</h3><p>Transformers 模型通常返回 <code>ModelOutput</code> 的具体子类，既具有命名字段，又能在一定程度上像元组一样使用：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span>outputs<span class="token punctuation">.</span>logits<span class="token punctuation">)</span></code></pre><p>若传入 <code>labels</code>，分类模型通常还会返回：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span>outputs<span class="token punctuation">.</span>loss<span class="token punctuation">)</span></code></pre><h2 id="13-一次带标签的前向传播"><a href="#13-一次带标签的前向传播" class="headerlink" title="13. 一次带标签的前向传播"></a>13. 一次带标签的前向传播</h2><pre class="language-python" data-language="python"><code class="language-python">labels <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span>outputs <span class="token operator">=</span> model<span class="token punctuation">(</span><span class="token operator">**</span>inputs<span class="token punctuation">,</span> labels<span class="token operator">=</span>labels<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>outputs<span class="token punctuation">.</span>loss<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>outputs<span class="token punctuation">.</span>logits<span class="token punctuation">.</span>shape<span class="token punctuation">)</span></code></pre><p>内部概念流程为：</p><pre class="language-text" data-language="text"><code class="language-text">文本  -> Tokenizer  -> input_ids / attention_mask  -> Transformer Backbone  -> 分类头  -> logits [B, C]  -> 与 labels 计算 CrossEntropyLoss</code></pre><p>只调用这一步不会更新参数。完整训练还需要：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>outputs<span class="token punctuation">.</span>loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p><code>Trainer</code> 会把这些训练循环工作统一封装起来。</p><h2 id="14-Hugging-Face-Datasets-是什么"><a href="#14-Hugging-Face-Datasets-是什么" class="headerlink" title="14. Hugging Face Datasets 是什么"></a>14. Hugging Face Datasets 是什么</h2><p><code>datasets</code> 提供统一的数据加载和处理接口：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> datasets <span class="token keyword">import</span> load_datasetdataset <span class="token operator">=</span> load_dataset<span class="token punctuation">(</span><span class="token string">"数据集仓库ID"</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>dataset<span class="token punctuation">)</span></code></pre><p>返回值经常是 <code>DatasetDict</code>：</p><pre class="language-text" data-language="text"><code class="language-text">DatasetDict(&#123;    train: Dataset(...)    validation: Dataset(...)    test: Dataset(...)&#125;)</code></pre><p>每个 split 都包含列名、特征类型和行数。</p><h3 id="14-1-从本地文件加载"><a href="#14-1-从本地文件加载" class="headerlink" title="14.1 从本地文件加载"></a>14.1 从本地文件加载</h3><p>CSV：</p><pre class="language-python" data-language="python"><code class="language-python">dataset <span class="token operator">=</span> load_dataset<span class="token punctuation">(</span>    <span class="token string">"csv"</span><span class="token punctuation">,</span>    data_files<span class="token operator">=</span><span class="token punctuation">&#123;</span>        <span class="token string">"train"</span><span class="token punctuation">:</span> <span class="token string">"data/train.csv"</span><span class="token punctuation">,</span>        <span class="token string">"validation"</span><span class="token punctuation">:</span> <span class="token string">"data/validation.csv"</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>JSON Lines：</p><pre class="language-python" data-language="python"><code class="language-python">dataset <span class="token operator">=</span> load_dataset<span class="token punctuation">(</span>    <span class="token string">"json"</span><span class="token punctuation">,</span>    data_files<span class="token operator">=</span><span class="token punctuation">&#123;</span><span class="token string">"train"</span><span class="token punctuation">:</span> <span class="token string">"data/train.jsonl"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>纯文本：</p><pre class="language-python" data-language="python"><code class="language-python">dataset <span class="token operator">=</span> load_dataset<span class="token punctuation">(</span>    <span class="token string">"text"</span><span class="token punctuation">,</span>    data_files<span class="token operator">=</span><span class="token punctuation">&#123;</span><span class="token string">"train"</span><span class="token punctuation">:</span> <span class="token string">"data/train.txt"</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="14-2-从-Python-数据创建"><a href="#14-2-从-Python-数据创建" class="headerlink" title="14.2 从 Python 数据创建"></a>14.2 从 Python 数据创建</h3><p>教学项目可以直接使用：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> datasets <span class="token keyword">import</span> Datasettrain_dataset <span class="token operator">=</span> Dataset<span class="token punctuation">.</span>from_dict<span class="token punctuation">(</span><span class="token punctuation">&#123;</span>    <span class="token string">"text"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"非常好用"</span><span class="token punctuation">,</span> <span class="token string">"质量太差"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"label"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span><span class="token punctuation">)</span></code></pre><h2 id="15-使用-map-批量-Tokenize"><a href="#15-使用-map-批量-Tokenize" class="headerlink" title="15. 使用 map() 批量 Tokenize"></a>15. 使用 <code>map()</code> 批量 Tokenize</h2><p>原始数据只有文本和标签：</p><pre class="language-text" data-language="text"><code class="language-text">text:  "这个产品很好用"label: 1</code></pre><p>定义预处理函数：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">tokenize_batch</span><span class="token punctuation">(</span>examples<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">return</span> tokenizer<span class="token punctuation">(</span>        examples<span class="token punctuation">[</span><span class="token string">"text"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>        truncation<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>        max_length<span class="token operator">=</span><span class="token number">128</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span></code></pre><p>批量应用：</p><pre class="language-python" data-language="python"><code class="language-python">tokenized_dataset <span class="token operator">=</span> dataset<span class="token punctuation">.</span><span class="token builtin">map</span><span class="token punctuation">(</span>    tokenize_batch<span class="token punctuation">,</span>    batched<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>处理后会多出模型输入列：</p><pre class="language-text" data-language="text"><code class="language-text">textlabelinput_idsattention_masktoken_type_ids  视模型而定</code></pre><h3 id="15-1-batched-True-是什么"><a href="#15-1-batched-True-是什么" class="headerlink" title="15.1 batched=True 是什么"></a>15.1 <code>batched=True</code> 是什么</h3><p>不使用批处理时，函数每次接收一条样本：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token punctuation">&#123;</span><span class="token string">"text"</span><span class="token punctuation">:</span> <span class="token string">"一条文本"</span><span class="token punctuation">,</span> <span class="token string">"label"</span><span class="token punctuation">:</span> <span class="token number">1</span><span class="token punctuation">&#125;</span></code></pre><p>使用 <code>batched=True</code> 后，每次接收一批列数据：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token punctuation">&#123;</span>    <span class="token string">"text"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"文本A"</span><span class="token punctuation">,</span> <span class="token string">"文本B"</span><span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"label"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span></code></pre><p>Fast Tokenizer 能够高效处理一批字符串，通常比 Python 逐条调用更快。</p><h3 id="15-2-map-通常返回新数据集"><a href="#15-2-map-通常返回新数据集" class="headerlink" title="15.2 map() 通常返回新数据集"></a>15.2 <code>map()</code> 通常返回新数据集</h3><p>Datasets 的处理方法通常不会原地修改原对象：</p><pre class="language-python" data-language="python"><code class="language-python">tokenized_dataset <span class="token operator">=</span> dataset<span class="token punctuation">.</span><span class="token builtin">map</span><span class="token punctuation">(</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span></code></pre><p>不要忘记接收返回值。</p><h3 id="15-3-为什么预处理阶段不固定-Padding"><a href="#15-3-为什么预处理阶段不固定-Padding" class="headerlink" title="15.3 为什么预处理阶段不固定 Padding"></a>15.3 为什么预处理阶段不固定 Padding</h3><p>这里有意不写：</p><pre class="language-python" data-language="python"><code class="language-python">padding<span class="token operator">=</span><span class="token string">"max_length"</span></code></pre><p>我们保留不同长度的 <code>input_ids</code>，等到 Data Collator 组成 batch 时再动态 Padding。</p><h2 id="16-Data-Collator-在哪里工作"><a href="#16-Data-Collator-在哪里工作" class="headerlink" title="16. Data Collator 在哪里工作"></a>16. Data Collator 在哪里工作</h2><p>数据处理可以分成两层：</p><pre class="language-text" data-language="text"><code class="language-text">Dataset.map  每条样本：文本 -> 长度不一的 token idDataCollatorWithPadding  每个 batch：多条样本 -> 补齐成规则张量</code></pre><p>例如三条序列长度分别为：</p><pre class="language-text" data-language="text"><code class="language-text">7, 11, 9</code></pre><p>组成 batch 后：</p><pre class="language-text" data-language="text"><code class="language-text">input_ids.shape = [3, 11]attention_mask.shape = [3, 11]labels.shape = [3]</code></pre><p>Data Collator 只整理 batch，不负责模型前向和参数更新。</p><h2 id="17-用迁移学习完成中文情感分类"><a href="#17-用迁移学习完成中文情感分类" class="headerlink" title="17. 用迁移学习完成中文情感分类"></a>17. 用迁移学习完成中文情感分类</h2><p>下面使用：</p><pre class="language-text" data-language="text"><code class="language-text">Backbone: google-bert/bert-base-chinese任务: 二分类标签: NEGATIVE / POSITIVE</code></pre><p>教学数据规模很小，只用于确认代码和数据流正确：</p><pre class="language-python" data-language="python"><code class="language-python">train_texts <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token string">"物流很快，包装完整，商品也很好用。"</span><span class="token punctuation">,</span>    <span class="token string">"音质清晰，佩戴几个小时也很舒服。"</span><span class="token punctuation">,</span>    <span class="token string">"屏幕显示细腻，系统运行十分流畅。"</span><span class="token punctuation">,</span>    <span class="token string">"客服回复及时，问题很快得到解决。"</span><span class="token punctuation">,</span>    <span class="token string">"续航表现不错，一整天使用没有压力。"</span><span class="token punctuation">,</span>    <span class="token string">"做工粗糙，刚拆开就发现了划痕。"</span><span class="token punctuation">,</span>    <span class="token string">"电池掉电特别快，完全无法正常使用。"</span><span class="token punctuation">,</span>    <span class="token string">"商品与描述不符，体验非常糟糕。"</span><span class="token punctuation">,</span>    <span class="token string">"包装已经破损，而且缺少配件。"</span><span class="token punctuation">,</span>    <span class="token string">"运行频繁卡顿，还出现了自动关机。"</span><span class="token punctuation">,</span><span class="token punctuation">]</span>train_labels <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span></code></pre><p>真实项目应使用足够大、标签可靠且与目标分布一致的数据，并保留独立测试集。</p><h2 id="18-标签映射为什么必须写清楚"><a href="#18-标签映射为什么必须写清楚" class="headerlink" title="18. 标签映射为什么必须写清楚"></a>18. 标签映射为什么必须写清楚</h2><pre class="language-python" data-language="python"><code class="language-python">id2label <span class="token operator">=</span> <span class="token punctuation">&#123;</span>    <span class="token number">0</span><span class="token punctuation">:</span> <span class="token string">"NEGATIVE"</span><span class="token punctuation">,</span>    <span class="token number">1</span><span class="token punctuation">:</span> <span class="token string">"POSITIVE"</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span>label2id <span class="token operator">=</span> <span class="token punctuation">&#123;</span>    <span class="token string">"NEGATIVE"</span><span class="token punctuation">:</span> <span class="token number">0</span><span class="token punctuation">,</span>    <span class="token string">"POSITIVE"</span><span class="token punctuation">:</span> <span class="token number">1</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span></code></pre><p>加载模型时传入：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> AutoModelForSequenceClassification<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    model_id<span class="token punctuation">,</span>    num_labels<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">,</span>    id2label<span class="token operator">=</span>id2label<span class="token punctuation">,</span>    label2id<span class="token operator">=</span>label2id<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这样保存模型后，推理结果能够直接返回可读标签，而不是含义不明确的 <code>LABEL_0</code>。</p><h2 id="19-为什么分类头会提示“部分权重未初始化”"><a href="#19-为什么分类头会提示“部分权重未初始化”" class="headerlink" title="19. 为什么分类头会提示“部分权重未初始化”"></a>19. 为什么分类头会提示“部分权重未初始化”</h2><p><code>google-bert/bert-base-chinese</code> 是预训练 Backbone，并不是已经完成当前二分类任务的模型。</p><p>当使用 <code>AutoModelForSequenceClassification</code> 加载时：</p><pre class="language-text" data-language="text"><code class="language-text">BERT Backbone 参数  &lt;- 从预训练 Checkpoint 加载新分类头参数  &lt;- 随机初始化</code></pre><p>因此出现类似“classifier weights were not initialized”的提示通常是正常的。它是在提醒：</p><blockquote><p>新任务头还没有训练，必须微调后才能用于当前标签预测。</p></blockquote><p>如果加载的是已经完成相同分类任务的 Checkpoint，却出现大量 Backbone 权重不匹配，则需要检查模型类和 Checkpoint 是否选错。</p><h2 id="20-TrainingArguments-管理什么"><a href="#20-TrainingArguments-管理什么" class="headerlink" title="20. TrainingArguments 管理什么"></a>20. <code>TrainingArguments</code> 管理什么</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> TrainingArgumentstraining_args <span class="token operator">=</span> TrainingArguments<span class="token punctuation">(</span>    output_dir<span class="token operator">=</span><span class="token string">"outputs/hf-chinese-sentiment"</span><span class="token punctuation">,</span>    learning_rate<span class="token operator">=</span><span class="token number">2e-5</span><span class="token punctuation">,</span>    per_device_train_batch_size<span class="token operator">=</span><span class="token number">4</span><span class="token punctuation">,</span>    per_device_eval_batch_size<span class="token operator">=</span><span class="token number">4</span><span class="token punctuation">,</span>    num_train_epochs<span class="token operator">=</span><span class="token number">3</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">0.01</span><span class="token punctuation">,</span>    eval_strategy<span class="token operator">=</span><span class="token string">"epoch"</span><span class="token punctuation">,</span>    save_strategy<span class="token operator">=</span><span class="token string">"epoch"</span><span class="token punctuation">,</span>    load_best_model_at_end<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    metric_for_best_model<span class="token operator">=</span><span class="token string">"accuracy"</span><span class="token punctuation">,</span>    save_total_limit<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">,</span>    logging_steps<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">,</span>    report_to<span class="token operator">=</span><span class="token string">"none"</span><span class="token punctuation">,</span>    seed<span class="token operator">=</span><span class="token number">42</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>常用参数含义：</p><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>output_dir</code></td><td>Checkpoint 与训练输出目录</td></tr><tr><td><code>learning_rate</code></td><td>优化器学习率</td></tr><tr><td><code>per_device_train_batch_size</code></td><td>每个设备上的训练 batch size</td></tr><tr><td><code>num_train_epochs</code></td><td>遍历训练集次数</td></tr><tr><td><code>weight_decay</code></td><td>权重衰减</td></tr><tr><td><code>eval_strategy</code></td><td>何时评估</td></tr><tr><td><code>save_strategy</code></td><td>何时保存 Checkpoint</td></tr><tr><td><code>load_best_model_at_end</code></td><td>训练后恢复最佳 Checkpoint</td></tr><tr><td><code>metric_for_best_model</code></td><td>选择最佳模型的指标</td></tr><tr><td><code>report_to</code></td><td>实验记录后端；<code>none</code> 表示关闭</td></tr></tbody></table><h3 id="20-1-有效-batch-size"><a href="#20-1-有效-batch-size" class="headerlink" title="20.1 有效 batch size"></a>20.1 有效 batch size</h3><p>如果有 $N$ 个设备，每个设备 batch size 为 $B$，梯度累积步数为 $G$，则每次参数更新对应的有效 batch size 约为：</p><p>$$<br>B_{effective}&#x3D;N\times B\times G<br>$$</p><p>显存不足时，可以减小 <code>per_device_train_batch_size</code>，再使用：</p><pre class="language-python" data-language="python"><code class="language-python">gradient_accumulation_steps<span class="token operator">=</span><span class="token number">4</span></code></pre><p>但梯度累积不能消除单条超长序列带来的显存压力。</p><h2 id="21-评估函数-compute-metrics"><a href="#21-评估函数-compute-metrics" class="headerlink" title="21. 评估函数 compute_metrics"></a>21. 评估函数 <code>compute_metrics</code></h2><p>二分类准确率可以直接实现，避免额外下载指标脚本：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> numpy <span class="token keyword">as</span> np<span class="token keyword">def</span> <span class="token function">compute_metrics</span><span class="token punctuation">(</span>eval_pred<span class="token punctuation">)</span><span class="token punctuation">:</span>    logits<span class="token punctuation">,</span> labels <span class="token operator">=</span> eval_pred    predictions <span class="token operator">=</span> np<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> axis<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>    accuracy <span class="token operator">=</span> <span class="token punctuation">(</span>predictions <span class="token operator">==</span> labels<span class="token punctuation">)</span><span class="token punctuation">.</span>mean<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">return</span> <span class="token punctuation">&#123;</span><span class="token string">"accuracy"</span><span class="token punctuation">:</span> <span class="token builtin">float</span><span class="token punctuation">(</span>accuracy<span class="token punctuation">)</span><span class="token punctuation">&#125;</span></code></pre><p>Trainer 在评估阶段会：</p><pre class="language-text" data-language="text"><code class="language-text">验证集 batch  -> 模型前向  -> 收集 logits 和 labels  -> 调用 compute_metrics  -> 得到 eval_accuracy</code></pre><p>分类任务不应只看 Accuracy。类别不平衡时还应关注：</p><ul><li>Precision。</li><li>Recall。</li><li>F1。</li><li>混淆矩阵。</li><li>每一类的样本数。</li></ul><h2 id="22-创建-Trainer"><a href="#22-创建-Trainer" class="headerlink" title="22. 创建 Trainer"></a>22. 创建 <code>Trainer</code></h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> Trainertrainer <span class="token operator">=</span> Trainer<span class="token punctuation">(</span>    model<span class="token operator">=</span>model<span class="token punctuation">,</span>    args<span class="token operator">=</span>training_args<span class="token punctuation">,</span>    train_dataset<span class="token operator">=</span>tokenized_dataset<span class="token punctuation">[</span><span class="token string">"train"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    eval_dataset<span class="token operator">=</span>tokenized_dataset<span class="token punctuation">[</span><span class="token string">"validation"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    processing_class<span class="token operator">=</span>tokenizer<span class="token punctuation">,</span>    data_collator<span class="token operator">=</span>data_collator<span class="token punctuation">,</span>    compute_metrics<span class="token operator">=</span>compute_metrics<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>再执行：</p><pre class="language-python" data-language="python"><code class="language-python">trainer<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span>metrics <span class="token operator">=</span> trainer<span class="token punctuation">.</span>evaluate<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>metrics<span class="token punctuation">)</span></code></pre><p><img src="https://lsworl.github.io/images/nlp-blog/hugging-face-trainer-flow.svg" alt="Trainer微调数据流"></p><h3 id="22-1-Trainer-帮我们做了什么"><a href="#22-1-Trainer-帮我们做了什么" class="headerlink" title="22.1 Trainer 帮我们做了什么"></a>22.1 <code>Trainer</code> 帮我们做了什么</h3><p>它封装了典型训练循环：</p><pre class="language-text" data-language="text"><code class="language-text">DataLoader 取 batch  -> 移动到正确设备  -> model(**batch)  -> 读取 loss  -> backward  -> 梯度累积或裁剪  -> optimizer.step  -> learning rate scheduler.step  -> 清梯度  -> 日志、评估和保存</code></pre><h3 id="22-2-Trainer-会隐藏原理吗"><a href="#22-2-Trainer-会隐藏原理吗" class="headerlink" title="22.2 Trainer 会隐藏原理吗"></a>22.2 <code>Trainer</code> 会隐藏原理吗</h3><p>工具会隐藏重复代码，但不应该隐藏理解。</p><p>至少应知道：</p><ul><li>batch 中有哪些键。</li><li>模型 <code>forward()</code> 接收哪些参数。</li><li><code>labels</code> 如何产生 loss。</li><li>优化器何时更新。</li><li>评估指标基于什么数据计算。</li><li>最佳模型按哪个指标选择。</li></ul><p>理解这些以后，<code>Trainer</code> 是减少样板代码；不理解时，它就容易变成难以调试的黑箱。</p><h2 id="23-完整项目代码"><a href="#23-完整项目代码" class="headerlink" title="23. 完整项目代码"></a>23. 完整项目代码</h2><p>完整脚本位于：</p><pre class="language-text" data-language="text"><code class="language-text">source/code/nlp-blog/huggingface_text_classification.py</code></pre><p>运行：</p><pre class="language-bash" data-language="bash"><code class="language-bash">python source/code/nlp-blog/huggingface_text_classification.py</code></pre><p>第一次运行需要联网下载 <code>google-bert/bert-base-chinese</code>。下载大小、训练速度和显存占用取决于模型与环境。</p><p>脚本执行流程：</p><pre class="language-text" data-language="text"><code class="language-text">设置随机种子  -> 构造本地 DatasetDict  -> 加载 AutoTokenizer  -> dataset.map 批量分词  -> 创建 DataCollatorWithPadding  -> 加载带分类头的 BERT  -> 创建 TrainingArguments  -> 创建 Trainer  -> train + evaluate  -> save_pretrained  -> pipeline 从本地目录重新加载  -> 输出新文本预测</code></pre><h2 id="24-保存模型、Tokenizer-与配置"><a href="#24-保存模型、Tokenizer-与配置" class="headerlink" title="24. 保存模型、Tokenizer 与配置"></a>24. 保存模型、Tokenizer 与配置</h2><pre class="language-python" data-language="python"><code class="language-python">final_dir <span class="token operator">=</span> <span class="token string">"outputs/hf-chinese-sentiment/final"</span>trainer<span class="token punctuation">.</span>save_model<span class="token punctuation">(</span>final_dir<span class="token punctuation">)</span>tokenizer<span class="token punctuation">.</span>save_pretrained<span class="token punctuation">(</span>final_dir<span class="token punctuation">)</span></code></pre><p>保存后的目录通常包含：</p><pre class="language-text" data-language="text"><code class="language-text">final/  config.json  model.safetensors  tokenizer.json  tokenizer_config.json  special_tokens_map.json  vocab.txt  training_args.bin</code></pre><h3 id="24-1-为什么必须一起保存-Tokenizer"><a href="#24-1-为什么必须一起保存-Tokenizer" class="headerlink" title="24.1 为什么必须一起保存 Tokenizer"></a>24.1 为什么必须一起保存 Tokenizer</h3><p>模型的 embedding 行号对应特定词表。只保存权重，不保存匹配的 Tokenizer，会让以后无法可靠复现输入 token id。</p><h3 id="24-2-从本地目录重新加载"><a href="#24-2-从本地目录重新加载" class="headerlink" title="24.2 从本地目录重新加载"></a>24.2 从本地目录重新加载</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> pipelineclassifier <span class="token operator">=</span> pipeline<span class="token punctuation">(</span>    <span class="token string">"text-classification"</span><span class="token punctuation">,</span>    model<span class="token operator">=</span>final_dir<span class="token punctuation">,</span>    tokenizer<span class="token operator">=</span>final_dir<span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>classifier<span class="token punctuation">(</span><span class="token string">"这个键盘手感很好。"</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p><code>from_pretrained()</code> 中的“pretrained”不意味着一定联网。它既可以接收 Hub ID，也可以接收本地目录。</p><p><img src="https://lsworl.github.io/images/nlp-blog/hugging-face-save-reload.svg" alt="模型保存与重新加载"></p><h2 id="25-Checkpoint-与最终模型的区别"><a href="#25-Checkpoint-与最终模型的区别" class="headerlink" title="25. Checkpoint 与最终模型的区别"></a>25. Checkpoint 与最终模型的区别</h2><p>训练中间目录可能包含：</p><pre class="language-text" data-language="text"><code class="language-text">checkpoint-10/checkpoint-20/</code></pre><p>它们常包含：</p><ul><li>模型权重。</li><li>优化器状态。</li><li>学习率调度器状态。</li><li>随机数状态。</li><li>Trainer 状态。</li></ul><p>这些状态用于恢复训练。</p><p>最终推理目录通常只需要：</p><ul><li>模型配置与权重。</li><li>Tokenizer 或 Processor。</li><li>推理需要的生成配置。</li><li>标签映射和说明文档。</li></ul><p>不要把“保存可推理模型”与“保存可无缝续训 Checkpoint”当成完全相同的需求。</p><h2 id="26-上传到-Hugging-Face-Hub"><a href="#26-上传到-Hugging-Face-Hub" class="headerlink" title="26. 上传到 Hugging Face Hub"></a>26. 上传到 Hugging Face Hub</h2><p>上传属于远程写操作，需要先创建账号和访问令牌。不要把令牌写进源代码或提交到 Git。</p><p>命令行登录：</p><pre class="language-bash" data-language="bash"><code class="language-bash">hf auth login</code></pre><p>使用 <code>Trainer</code> 上传：</p><pre class="language-python" data-language="python"><code class="language-python">trainer<span class="token punctuation">.</span>push_to_hub<span class="token punctuation">(</span>    commit_message<span class="token operator">=</span><span class="token string">"Fine-tune Chinese sentiment classifier"</span><span class="token punctuation">)</span></code></pre><p>也可以直接上传目录：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> huggingface_hub <span class="token keyword">import</span> HfApiapi <span class="token operator">=</span> HfApi<span class="token punctuation">(</span><span class="token punctuation">)</span>api<span class="token punctuation">.</span>upload_folder<span class="token punctuation">(</span>    folder_path<span class="token operator">=</span>final_dir<span class="token punctuation">,</span>    repo_id<span class="token operator">=</span><span class="token string">"your-name/chinese-sentiment-demo"</span><span class="token punctuation">,</span>    repo_type<span class="token operator">=</span><span class="token string">"model"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="26-1-上传前应准备什么"><a href="#26-1-上传前应准备什么" class="headerlink" title="26.1 上传前应准备什么"></a>26.1 上传前应准备什么</h3><p>至少应补充 Model Card：</p><pre class="language-text" data-language="text"><code class="language-text">模型用途基础模型训练数据来源与许可证标签定义训练超参数评估方法与结果局限、偏差与不适用场景推理示例</code></pre><p>本篇的小型手写数据只是教学样例，不适合直接发布为“可用于生产”的情感模型。</p><h2 id="27-下载缓存在哪里"><a href="#27-下载缓存在哪里" class="headerlink" title="27. 下载缓存在哪里"></a>27. 下载缓存在哪里</h2><p>默认情况下，Hub 文件会进入本地缓存。再次加载相同 revision 时，可以复用已有文件而不重复下载。</p><p>查看环境变量：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> os<span class="token keyword">print</span><span class="token punctuation">(</span>os<span class="token punctuation">.</span>getenv<span class="token punctuation">(</span><span class="token string">"HF_HOME"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>os<span class="token punctuation">.</span>getenv<span class="token punctuation">(</span><span class="token string">"HF_HUB_CACHE"</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>可以在运行前指定缓存根目录：</p><p>PowerShell：</p><pre class="language-powershell" data-language="powershell"><code class="language-powershell"><span class="token variable">$env</span>:HF_HOME = <span class="token string">"D:\hf-cache"</span></code></pre><p>或者为单次加载指定：</p><pre class="language-python" data-language="python"><code class="language-python">tokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    model_id<span class="token punctuation">,</span>    cache_dir<span class="token operator">=</span><span class="token string">"D:/hf-cache"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="27-1-离线模式"><a href="#27-1-离线模式" class="headerlink" title="27.1 离线模式"></a>27.1 离线模式</h3><p>文件已经完整缓存后，可以启用离线模式：</p><pre class="language-powershell" data-language="powershell"><code class="language-powershell"><span class="token variable">$env</span>:HF_HUB_OFFLINE = <span class="token string">"1"</span></code></pre><p>或在代码中要求只读取本地文件：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> AutoModelForSequenceClassification<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    model_id<span class="token punctuation">,</span>    local_files_only<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>如果缓存缺少必要文件，离线加载仍然会失败。</p><h2 id="28-CPU、GPU、dtype-与-device-map"><a href="#28-CPU、GPU、dtype-与-device-map" class="headerlink" title="28. CPU、GPU、dtype 与 device_map"></a>28. CPU、GPU、dtype 与 <code>device_map</code></h2><h3 id="28-1-普通小模型"><a href="#28-1-普通小模型" class="headerlink" title="28.1 普通小模型"></a>28.1 普通小模型</h3><p>手动推理时：</p><pre class="language-python" data-language="python"><code class="language-python">device <span class="token operator">=</span> torch<span class="token punctuation">.</span>device<span class="token punctuation">(</span>    <span class="token string">"cuda"</span> <span class="token keyword">if</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_available<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">else</span> <span class="token string">"cpu"</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span>inputs <span class="token operator">=</span> <span class="token punctuation">&#123;</span>name<span class="token punctuation">:</span> value<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span> <span class="token keyword">for</span> name<span class="token punctuation">,</span> value <span class="token keyword">in</span> inputs<span class="token punctuation">.</span>items<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">&#125;</span></code></pre><p>模型和输入必须位于同一设备。</p><h3 id="28-2-pipeline-指定-GPU"><a href="#28-2-pipeline-指定-GPU" class="headerlink" title="28.2 pipeline 指定 GPU"></a>28.2 <code>pipeline</code> 指定 GPU</h3><pre class="language-python" data-language="python"><code class="language-python">classifier <span class="token operator">=</span> pipeline<span class="token punctuation">(</span>    <span class="token string">"text-classification"</span><span class="token punctuation">,</span>    model<span class="token operator">=</span>model_id<span class="token punctuation">,</span>    device<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p><code>device=0</code> 通常表示第一张 CUDA GPU，<code>device=-1</code> 表示 CPU。</p><h3 id="28-3-大模型自动分配"><a href="#28-3-大模型自动分配" class="headerlink" title="28.3 大模型自动分配"></a>28.3 大模型自动分配</h3><p>对于较大模型，可以结合 Accelerate：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    model_id<span class="token punctuation">,</span>    dtype<span class="token operator">=</span><span class="token string">"auto"</span><span class="token punctuation">,</span>    device_map<span class="token operator">=</span><span class="token string">"auto"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>但这不表示任何模型都能塞进有限显存。仍需考虑：</p><ul><li>权重占用。</li><li>KV Cache。</li><li>激活值。</li><li>batch size 与序列长度。</li><li>量化精度。</li></ul><h2 id="29-文本生成：从-pipeline-到-generate"><a href="#29-文本生成：从-pipeline-到-generate" class="headerlink" title="29. 文本生成：从 pipeline 到 generate"></a>29. 文本生成：从 <code>pipeline</code> 到 <code>generate</code></h2><h3 id="29-1-使用-pipeline"><a href="#29-1-使用-pipeline" class="headerlink" title="29.1 使用 pipeline"></a>29.1 使用 <code>pipeline</code></h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> pipelinegenerator <span class="token operator">=</span> pipeline<span class="token punctuation">(</span>    <span class="token string">"text-generation"</span><span class="token punctuation">,</span>    model<span class="token operator">=</span><span class="token string">"openai-community/gpt2"</span><span class="token punctuation">,</span><span class="token punctuation">)</span>result <span class="token operator">=</span> generator<span class="token punctuation">(</span>    <span class="token string">"Machine learning is"</span><span class="token punctuation">,</span>    max_new_tokens<span class="token operator">=</span><span class="token number">30</span><span class="token punctuation">,</span>    do_sample<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    temperature<span class="token operator">=</span><span class="token number">0.8</span><span class="token punctuation">,</span>    top_p<span class="token operator">=</span><span class="token number">0.9</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>result<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token string">"generated_text"</span><span class="token punctuation">]</span><span class="token punctuation">)</span></code></pre><p>这里使用英文 Prompt，是因为 GPT-2 的训练语言与 Tokenizer 更适合英文。选模型时必须检查语言适配性。</p><h3 id="29-2-手动调用-generate"><a href="#29-2-手动调用-generate" class="headerlink" title="29.2 手动调用 generate"></a>29.2 手动调用 <code>generate</code></h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> transformers <span class="token keyword">import</span> AutoModelForCausalLM<span class="token punctuation">,</span> AutoTokenizermodel_id <span class="token operator">=</span> <span class="token string">"openai-community/gpt2"</span>tokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span>model <span class="token operator">=</span> AutoModelForCausalLM<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_id<span class="token punctuation">)</span>inputs <span class="token operator">=</span> tokenizer<span class="token punctuation">(</span>    <span class="token string">"Machine learning is"</span><span class="token punctuation">,</span>    return_tensors<span class="token operator">=</span><span class="token string">"pt"</span><span class="token punctuation">,</span><span class="token punctuation">)</span>output_ids <span class="token operator">=</span> model<span class="token punctuation">.</span>generate<span class="token punctuation">(</span>    <span class="token operator">**</span>inputs<span class="token punctuation">,</span>    max_new_tokens<span class="token operator">=</span><span class="token number">30</span><span class="token punctuation">,</span>    do_sample<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    temperature<span class="token operator">=</span><span class="token number">0.8</span><span class="token punctuation">,</span>    top_p<span class="token operator">=</span><span class="token number">0.9</span><span class="token punctuation">,</span><span class="token punctuation">)</span>text <span class="token operator">=</span> tokenizer<span class="token punctuation">.</span>decode<span class="token punctuation">(</span>    output_ids<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    skip_special_tokens<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>text<span class="token punctuation">)</span></code></pre><h3 id="29-3-max-new-tokens-与-max-length"><a href="#29-3-max-new-tokens-与-max-length" class="headerlink" title="29.3 max_new_tokens 与 max_length"></a>29.3 <code>max_new_tokens</code> 与 <code>max_length</code></h3><pre class="language-text" data-language="text"><code class="language-text">max_new_tokens  只限制新生成的 token 数max_length  限制输入 token + 新 token 的总长度</code></pre><p>控制生成长度时，<code>max_new_tokens</code> 通常更直观。</p><h3 id="29-4-聊天模型需要-Chat-Template"><a href="#29-4-聊天模型需要-Chat-Template" class="headerlink" title="29.4 聊天模型需要 Chat Template"></a>29.4 聊天模型需要 Chat Template</h3><p>聊天模型往往不是简单拼接：</p><pre class="language-text" data-language="text"><code class="language-text">system + user + assistant</code></pre><p>它们使用训练时约定的特殊格式。应优先调用对应 Tokenizer 的：</p><pre class="language-python" data-language="python"><code class="language-python">tokenizer<span class="token punctuation">.</span>apply_chat_template<span class="token punctuation">(</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span></code></pre><p>格式不匹配时，即使权重正确，模型表现也可能明显下降。</p><h2 id="30-Model-Card-应该怎么看"><a href="#30-Model-Card-应该怎么看" class="headerlink" title="30. Model Card 应该怎么看"></a>30. Model Card 应该怎么看</h2><p>选择模型时建议依次检查：</p><ol><li><strong>Task</strong>：模型是否真的为当前任务训练。</li><li><strong>Language</strong>：语言和领域是否匹配。</li><li><strong>License</strong>：是否允许你的研究或商业用途。</li><li><strong>Base model</strong>：模型从哪个 Checkpoint 微调而来。</li><li><strong>Dataset</strong>：训练数据是否可靠、是否接近目标分布。</li><li><strong>Metrics</strong>：在哪个测试集、使用什么指标得到结果。</li><li><strong>Limitations</strong>：已知偏差、风险和失败场景。</li><li><strong>Files</strong>：权重格式、大小与必要配置是否完整。</li><li><strong>Revision</strong>：项目是否需要锁定提交哈希。</li></ol><p>同一个排行榜分数不一定能迁移到你的数据。最可靠的方法仍然是在自己保留的测试集上评估。</p><h2 id="31-安全与可复现性"><a href="#31-安全与可复现性" class="headerlink" title="31. 安全与可复现性"></a>31. 安全与可复现性</h2><h3 id="31-1-谨慎使用-trust-remote-code-True"><a href="#31-1-谨慎使用-trust-remote-code-True" class="headerlink" title="31.1 谨慎使用 trust_remote_code=True"></a>31.1 谨慎使用 <code>trust_remote_code=True</code></h3><p>有些自定义模型需要执行仓库中的 Python 代码：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> AutoModel<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    model_id<span class="token punctuation">,</span>    trust_remote_code<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这意味着你明确允许远程仓库代码在本机运行。使用前应：</p><ul><li>检查仓库作者和代码。</li><li>锁定 <code>revision</code> 到可信提交。</li><li>在隔离环境中测试。</li><li>不在含敏感凭据的环境中随意执行未知代码。</li></ul><p>如果模型不需要该参数，就不要为了消除报错盲目开启。</p><h3 id="31-2-锁定-revision"><a href="#31-2-锁定-revision" class="headerlink" title="31.2 锁定 revision"></a>31.2 锁定 revision</h3><pre class="language-python" data-language="python"><code class="language-python">revision <span class="token operator">=</span> <span class="token string">"完整提交哈希"</span>tokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    model_id<span class="token punctuation">,</span>    revision<span class="token operator">=</span>revision<span class="token punctuation">,</span><span class="token punctuation">)</span>model <span class="token operator">=</span> AutoModelForSequenceClassification<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    model_id<span class="token punctuation">,</span>    revision<span class="token operator">=</span>revision<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>同一模型仓库的 <code>main</code> 分支可能更新。锁定 revision 有助于让未来运行读取相同文件。</p><h3 id="31-3-不要泄露访问令牌"><a href="#31-3-不要泄露访问令牌" class="headerlink" title="31.3 不要泄露访问令牌"></a>31.3 不要泄露访问令牌</h3><p>访问令牌不应：</p><ul><li>写进 Python 文件。</li><li>写进 Notebook 输出。</li><li>提交到 Git。</li><li>放在公开日志或截图中。</li></ul><p>应使用 Hugging Face CLI 登录、环境变量或受控的 Secret 管理系统。</p><h2 id="32-Spaces-是什么"><a href="#32-Spaces-是什么" class="headerlink" title="32. Spaces 是什么"></a>32. Spaces 是什么</h2><p>Spaces 用于托管机器学习演示或应用。常见选择包括：</p><ul><li>Gradio。</li><li>Streamlit。</li><li>静态 HTML。</li><li>Docker。</li></ul><p>一个简单 Gradio 应用可能是：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> gradio <span class="token keyword">as</span> gr<span class="token keyword">from</span> transformers <span class="token keyword">import</span> pipelineclassifier <span class="token operator">=</span> pipeline<span class="token punctuation">(</span>    <span class="token string">"text-classification"</span><span class="token punctuation">,</span>    model<span class="token operator">=</span><span class="token string">"your-name/chinese-sentiment-demo"</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">def</span> <span class="token function">predict</span><span class="token punctuation">(</span>text<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">return</span> classifier<span class="token punctuation">(</span>text<span class="token punctuation">)</span>demo <span class="token operator">=</span> gr<span class="token punctuation">.</span>Interface<span class="token punctuation">(</span>    fn<span class="token operator">=</span>predict<span class="token punctuation">,</span>    inputs<span class="token operator">=</span><span class="token string">"text"</span><span class="token punctuation">,</span>    outputs<span class="token operator">=</span><span class="token string">"json"</span><span class="token punctuation">,</span><span class="token punctuation">)</span>demo<span class="token punctuation">.</span>launch<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>需要区分：</p><pre class="language-text" data-language="text"><code class="language-text">Model Repo  保存模型资产Space Repo  保存并运行应用Inference Endpoint / Provider  提供托管推理能力</code></pre><p>它们解决的问题不同，价格、硬件和可用性也可能不同。</p><h2 id="33-常见错误"><a href="#33-常见错误" class="headerlink" title="33. 常见错误"></a>33. 常见错误</h2><h3 id="33-1-模型与-Tokenizer-不匹配"><a href="#33-1-模型与-Tokenizer-不匹配" class="headerlink" title="33.1 模型与 Tokenizer 不匹配"></a>33.1 模型与 Tokenizer 不匹配</h3><pre class="language-python" data-language="python"><code class="language-python">tokenizer <span class="token operator">=</span> AutoTokenizer<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_a<span class="token punctuation">)</span>model <span class="token operator">=</span> AutoModel<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>model_b<span class="token punctuation">)</span></code></pre><p>除非明确知道二者兼容，否则应从同一个仓库或同一套发布资产加载。</p><h3 id="33-2-任务模型类选错"><a href="#33-2-任务模型类选错" class="headerlink" title="33.2 任务模型类选错"></a>33.2 任务模型类选错</h3><p>文本分类应优先使用：</p><pre class="language-python" data-language="python"><code class="language-python">AutoModelForSequenceClassification</code></pre><p>只使用 <code>AutoModel</code> 不会自动产生类别 logits。</p><h3 id="33-3-忘记-truncation-True"><a href="#33-3-忘记-truncation-True" class="headerlink" title="33.3 忘记 truncation=True"></a>33.3 忘记 <code>truncation=True</code></h3><p>长文本超过模型支持长度时会报维度错误或索引错误。设置截断后还要评估信息损失。</p><h3 id="33-4-在整个数据集上固定-Padding-到最大长度"><a href="#33-4-在整个数据集上固定-Padding-到最大长度" class="headerlink" title="33.4 在整个数据集上固定 Padding 到最大长度"></a>33.4 在整个数据集上固定 Padding 到最大长度</h3><p>这会制造大量无效 token。优先考虑 <code>DataCollatorWithPadding</code> 的动态 Padding。</p><h3 id="33-5-把-logits-当概率"><a href="#33-5-把-logits-当概率" class="headerlink" title="33.5 把 logits 当概率"></a>33.5 把 logits 当概率</h3><p>logits 不要求位于 $[0,1]$，总和也不要求为 1。推理展示概率时再调用 Softmax。</p><h3 id="33-6-训练时先手动-Softmax"><a href="#33-6-训练时先手动-Softmax" class="headerlink" title="33.6 训练时先手动 Softmax"></a>33.6 训练时先手动 Softmax</h3><p>交叉熵应接收 logits。提前 Softmax 会改变数值性质，并与模型内置损失的预期不一致。</p><h3 id="33-7-忘记-model-eval"><a href="#33-7-忘记-model-eval" class="headerlink" title="33.7 忘记 model.eval()"></a>33.7 忘记 <code>model.eval()</code></h3><p>手动推理时应关闭 Dropout 等训练行为：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>inference_mode<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    outputs <span class="token operator">=</span> model<span class="token punctuation">(</span><span class="token operator">**</span>inputs<span class="token punctuation">)</span></code></pre><p><code>pipeline</code> 会处理常规推理模式，但手动调用时需要自己负责。</p><h3 id="33-8-模型与输入不在同一设备"><a href="#33-8-模型与输入不在同一设备" class="headerlink" title="33.8 模型与输入不在同一设备"></a>33.8 模型与输入不在同一设备</h3><p>如果模型在 GPU、输入在 CPU，会出现 device mismatch。二者必须一起移动。</p><h3 id="33-9-根据标签编号猜语义"><a href="#33-9-根据标签编号猜语义" class="headerlink" title="33.9 根据标签编号猜语义"></a>33.9 根据标签编号猜语义</h3><p>始终检查：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span>config<span class="token punctuation">.</span>id2label</code></pre><p>并与 Model Card 和数据集标签说明对照。</p><h3 id="33-10-使用测试集调超参数"><a href="#33-10-使用测试集调超参数" class="headerlink" title="33.10 使用测试集调超参数"></a>33.10 使用测试集调超参数</h3><p>正确职责是：</p><pre class="language-text" data-language="text"><code class="language-text">train       更新参数validation  选择超参数和最佳 Checkpointtest        最后一次无偏评估</code></pre><h3 id="33-11-小数据训练成功就宣称模型可用"><a href="#33-11-小数据训练成功就宣称模型可用" class="headerlink" title="33.11 小数据训练成功就宣称模型可用"></a>33.11 小数据训练成功就宣称模型可用</h3><p>训练 loss 下降只说明模型拟合了训练目标。还需要检查验证集、测试集、真实分布、偏差和失败样例。</p><h3 id="33-12-盲目相信-device-map-quot-auto-quot"><a href="#33-12-盲目相信-device-map-quot-auto-quot" class="headerlink" title="33.12 盲目相信 device_map=&quot;auto&quot;"></a>33.12 盲目相信 <code>device_map=&quot;auto&quot;</code></h3><p>它负责设备分配，不会自动解决所有显存、速度或量化问题，也不能替代容量估算。</p><h3 id="33-13-直接执行未知远程代码"><a href="#33-13-直接执行未知远程代码" class="headerlink" title="33.13 直接执行未知远程代码"></a>33.13 直接执行未知远程代码</h3><p>不要为了加载模型就无条件设置 <code>trust_remote_code=True</code>。先审查代码并锁定 revision。</p><h3 id="33-14-只保存模型，遗漏-Tokenizer"><a href="#33-14-只保存模型，遗漏-Tokenizer" class="headerlink" title="33.14 只保存模型，遗漏 Tokenizer"></a>33.14 只保存模型，遗漏 Tokenizer</h3><p>最终发布目录应同时包含匹配的预处理配置和标签映射。</p><h2 id="34-Hugging-Face-没有替你解决什么"><a href="#34-Hugging-Face-没有替你解决什么" class="headerlink" title="34. Hugging Face 没有替你解决什么"></a>34. Hugging Face 没有替你解决什么</h2><p>Hugging Face 能减少工程样板，但不会自动解决：</p><ul><li>任务定义是否正确。</li><li>数据标签是否可靠。</li><li>训练集是否泄露测试信息。</li><li>许可证是否适合使用场景。</li><li>模型偏差是否可接受。</li><li>线上延迟和吞吐是否达标。</li><li>长文本截断是否丢失关键信息。</li><li>评估集是否代表真实用户分布。</li></ul><p>模型调用变简单以后，数据、评估和安全反而更值得认真对待。</p><h2 id="35-推荐学习路线"><a href="#35-推荐学习路线" class="headerlink" title="35. 推荐学习路线"></a>35. 推荐学习路线</h2><p>可以按照下面顺序逐层深入：</p><pre class="language-text" data-language="text"><code class="language-text">1. 用 pipeline 跑通一个已有模型2. 阅读该模型的 Model Card 和 config3. 用 AutoTokenizer 查看 input_ids 与 attention_mask4. 用 AutoModelFor... 手动完成前向传播5. 理解 logits、Softmax 和标签映射6. 用 Dataset.from_dict 构造小数据7. 用 map 批量 Tokenize8. 用 DataCollatorWithPadding 组成 batch9. 用 Trainer 微调并评估10. save_pretrained 后从本地重载11. 加入独立测试集和完整指标12. 再学习 Accelerate、PEFT、量化和部署</code></pre><p>调试时优先打印：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span>dataset<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>dataset<span class="token punctuation">.</span>features<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>tokenized_dataset<span class="token punctuation">[</span><span class="token string">"train"</span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>data_collator<span class="token punctuation">(</span><span class="token punctuation">[</span>tokenized_dataset<span class="token punctuation">[</span><span class="token string">"train"</span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>model<span class="token punctuation">.</span>config<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>model<span class="token punctuation">.</span>config<span class="token punctuation">.</span>id2label<span class="token punctuation">)</span></code></pre><p>比起只盯着训练进度条，先确认数据字段、形状和标签含义往往更有效。</p><h2 id="36-总结"><a href="#36-总结" class="headerlink" title="36. 总结"></a>36. 总结</h2><p>Hugging Face 的主线可以浓缩为：</p><pre class="language-text" data-language="text"><code class="language-text">Hub  -> 找到模型、数据集与说明AutoTokenizer / AutoProcessor  -> 把原始输入转成模型张量AutoModelFor...  -> 按任务加载正确架构和预训练权重pipeline  -> 封装 preprocess、forward、postprocessDatasets + Data Collator  -> 加载、批处理并整理训练数据Trainer  -> 训练、评估、日志和 Checkpointsave_pretrained / push_to_hub  -> 保存、复用与分享</code></pre><p>需要真正掌握的核心包括：</p><ul><li>Hugging Face 是平台、库和约定组成的生态，不是单个模型。</li><li>Hub ID 指向具体 Checkpoint，不等于模型架构名称。</li><li>Tokenizer 必须与模型匹配。</li><li><code>pipeline</code> 是高级推理封装，不是新的神经网络结构。</li><li><code>AutoModel</code> 返回 Backbone 表示，<code>AutoModelFor...</code> 带任务头。</li><li>分类模型输出 logits，概率需要通过 Softmax 得到。</li><li><code>Dataset.map</code> 负责样本预处理，Data Collator 负责组成动态 Padding batch。</li><li><code>Trainer</code> 封装训练循环，但数据、损失、指标和标签含义仍需自己理解。</li><li>模型保存时应连同 Tokenizer、配置与标签映射一起保存。</li><li>Model Card、许可证、revision 和远程代码安全与模型效果同样重要。</li></ul><p>最后，用一句话概括：</p><blockquote><p>Hugging Face 把研究社区已有的模型资产变成统一、可复用的工程组件，而使用者仍然要为任务定义、数据质量、评估结论和安全边界负责。</p></blockquote><p>完整项目代码：<code>source/code/nlp-blog/huggingface_text_classification.py</code></p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>Hugging Face Transformers Quickstart: <a href="https://huggingface.co/docs/transformers/quicktour">https://huggingface.co/docs/transformers/quicktour</a></li><li>Hugging Face Pipeline Guide: <a href="https://huggingface.co/docs/transformers/main/en/pipeline_tutorial">https://huggingface.co/docs/transformers/main/en/pipeline_tutorial</a></li><li>Hugging Face AutoClass Guide: <a href="https://huggingface.co/docs/transformers/main/en/autoclass_tutorial">https://huggingface.co/docs/transformers/main/en/autoclass_tutorial</a></li><li>Hugging Face Text Classification Guide: <a href="https://huggingface.co/docs/transformers/main/en/tasks/sequence_classification">https://huggingface.co/docs/transformers/main/en/tasks/sequence_classification</a></li><li>Hugging Face Padding and Truncation: <a href="https://huggingface.co/docs/transformers/main/en/pad_truncation">https://huggingface.co/docs/transformers/main/en/pad_truncation</a></li><li>Hugging Face Datasets Quickstart: <a href="https://huggingface.co/docs/datasets/main/en/quickstart">https://huggingface.co/docs/datasets/main/en/quickstart</a></li><li>Hugging Face Datasets Processing Guide: <a href="https://huggingface.co/docs/datasets/main/en/process">https://huggingface.co/docs/datasets/main/en/process</a></li><li>Hugging Face Hub Model Cards: <a href="https://huggingface.co/docs/hub/en/model-cards">https://huggingface.co/docs/hub/en/model-cards</a></li><li>Hugging Face Hub Cache Guide: <a href="https://huggingface.co/docs/huggingface_hub/en/guides/manage-cache">https://huggingface.co/docs/huggingface_hub/en/guides/manage-cache</a></li><li>Hugging Face Hub Upload Guide: <a href="https://huggingface.co/docs/huggingface_hub/en/guides/upload">https://huggingface.co/docs/huggingface_hub/en/guides/upload</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/22/nlp-hugging-face-transformers-tutorial/</id>
    <link href="https://lsworl.github.io/2026/08/22/nlp-hugging-face-transformers-tutorial/"/>
    <published>2026-08-21T16:00:00.000Z</published>
    <summary>系统介绍Hugging Face Hub、Transformers、Datasets、Tokenizer、Pipeline与Trainer，并通过中文情感分类案例走通预训练模型加载、手动推理、微调、评估、保存和复用全过程。</summary>
    <title>Hugging Face详解：从模型下载到中文文本分类微调</title>
    <updated>2026-08-22T12:26:02.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Transformer" scheme="https://lsworl.github.io/tags/Transformer/"/>
    <category term="RoPE" scheme="https://lsworl.github.io/tags/RoPE/"/>
    <category term="Rotary Position Embedding" scheme="https://lsworl.github.io/tags/Rotary-Position-Embedding/"/>
    <category term="Self-Attention" scheme="https://lsworl.github.io/tags/Self-Attention/"/>
    <content>
      <![CDATA[<p>前面的 Transformer 和 Mini-GPT 文章分别使用了固定正弦位置编码与可学习绝对位置向量。它们都在解决同一个问题：</p><blockquote><p>Self-Attention 可以比较 token 内容，却不会天然知道 token 的先后顺序。</p></blockquote><p>这篇文章继续研究另一种位置编码：<strong>RoPE（Rotary Position Embedding，旋转位置编码）</strong>。</p><p>RoPE 的做法很特别。它不把位置向量直接加到 token 表示上，而是在每个注意力头内部，把 Query 和 Key 按位置旋转不同角度：</p><pre class="language-text" data-language="text"><code class="language-text">token hidden state  -> 线性投影得到 Q、K、V  -> 根据 position 旋转 Q 和 K  -> 计算 QK^T  -> Softmax  -> 对没有旋转的 V 加权求和</code></pre><p>它的核心价值可以浓缩为一句话：</p><blockquote><p>用绝对位置决定旋转角度，让 Query 与 Key 的点积自然只显式依赖相对位置。</p></blockquote><p>本文会从二维向量开始，逐步回答下面的问题：</p><ul><li>为什么旋转能够表示位置？</li><li>为什么两个绝对位置最后会变成一个相对距离？</li><li>为什么只旋转 Q 和 K，不旋转 V？</li><li>高维向量怎样分组旋转？</li><li><code>base=10000</code> 与不同频率分别代表什么？</li><li>怎样用 PyTorch 从零实现 RoPE？</li><li>怎样把 RoPE 接入 Mini-GPT、Causal Attention 与 KV Cache？</li><li>RoPE 是否意味着模型可以无限外推到任意长度？</li></ul><span id="more"></span><p><img src="https://lsworl.github.io/images/nlp-blog/rope-overview.svg" alt="RoPE在注意力中的整体位置"></p><h2 id="1-先回顾：Attention-为什么需要位置信息"><a href="#1-先回顾：Attention-为什么需要位置信息" class="headerlink" title="1. 先回顾：Attention 为什么需要位置信息"></a>1. 先回顾：Attention 为什么需要位置信息</h2><p>标准 Self-Attention 为：</p><p>$$<br>Q&#x3D;XW_Q,\qquad K&#x3D;XW_K,\qquad V&#x3D;XW_V<br>$$</p><p>$$<br>\operatorname{Attention}(Q,K,V)<br>&#x3D;\operatorname{softmax}\left(\frac{QK^T}{\sqrt{d_h}}+M\right)V<br>$$</p><p>其中：</p><ul><li>$X$ 是输入隐藏状态。</li><li>$d_h$ 是单个注意力头的维度。</li><li>$M$ 可以是 Causal Mask 或 Padding Mask。</li></ul><p>假设序列中有三个 token：</p><pre class="language-text" data-language="text"><code class="language-text">我  喜欢  苹果</code></pre><p>如果交换顺序：</p><pre class="language-text" data-language="text"><code class="language-text">苹果  喜欢  我</code></pre><p>在不提供任何位置信息时，Self-Attention 仍然只看到三组内容向量。它能够计算“谁和谁更相关”，却缺少可靠的顺序坐标。</p><p>需要注意：</p><blockquote><p>Causal Mask 不等于位置编码。</p></blockquote><p>Causal Mask 只规定位置 $m$ 能不能看到位置 $n$：</p><p>$$<br>M_{m,n}&#x3D;<br>\begin{cases}<br>0,&amp;n\le m\<br>-\infty,&amp;n&gt;m<br>\end{cases}<br>$$</p><p>它能阻止模型偷看未来，却没有直接告诉模型两个可见 token 相隔 1 个位置还是 100 个位置。</p><h2 id="2-常见位置编码怎样注入位置"><a href="#2-常见位置编码怎样注入位置" class="headerlink" title="2. 常见位置编码怎样注入位置"></a>2. 常见位置编码怎样注入位置</h2><p>先把 RoPE 与前文出现过的方法放在一起比较。</p><h3 id="2-1-固定正弦位置编码"><a href="#2-1-固定正弦位置编码" class="headerlink" title="2.1 固定正弦位置编码"></a>2.1 固定正弦位置编码</h3><p>原始 Transformer 构造一个与 token embedding 同维度的位置向量：</p><p>$$<br>z_m&#x3D;x_m+p_m<br>$$</p><p>位置向量使用不同频率的正弦与余弦函数生成，并在进入第一层 Transformer 前与输入相加。</p><h3 id="2-2-可学习绝对位置向量"><a href="#2-2-可学习绝对位置向量" class="headerlink" title="2.2 可学习绝对位置向量"></a>2.2 可学习绝对位置向量</h3><p>前一篇 Mini-GPT 使用：</p><pre class="language-python" data-language="python"><code class="language-python">tok_emb <span class="token operator">=</span> token_embedding<span class="token punctuation">(</span>idx<span class="token punctuation">)</span>pos_emb <span class="token operator">=</span> position_embedding<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span>T<span class="token punctuation">)</span><span class="token punctuation">)</span>x <span class="token operator">=</span> tok_emb <span class="token operator">+</span> pos_emb</code></pre><p>每个位置对应一个可学习向量。优点是直观，缺点是位置表有固定大小，并且相对关系需要模型从数据中自己学出来。</p><h3 id="2-3-RoPE"><a href="#2-3-RoPE" class="headerlink" title="2.3 RoPE"></a>2.3 RoPE</h3><p>RoPE 不再执行：</p><pre class="language-text" data-language="text"><code class="language-text">token embedding + position embedding</code></pre><p>而是在每一层 Attention 内执行：</p><p>$$<br>Q_m &#x3D; R_m(W_Q x_m)\<br>K_m &#x3D; R_m(W_K x_m)\<br>V_m &#x3D; W_V x_m<br>$$<br>其中 $R_m$ 是由位置 $m$ 决定的旋转矩阵。</p><table><thead><tr><th>方法</th><th>位置注入在哪里</th><th>主要操作</th><th>是否直接影响 QK 匹配</th></tr></thead><tbody><tr><td>固定正弦编码</td><td>Transformer 输入前</td><td>与输入相加</td><td>间接影响</td></tr><tr><td>可学习绝对位置</td><td>Transformer 输入前</td><td>查表后相加</td><td>间接影响</td></tr><tr><td>RoPE</td><td>每层 Attention 的 Q、K</td><td>按位置旋转</td><td>直接影响</td></tr></tbody></table><h2 id="3-RoPE-的第一块积木：二维旋转"><a href="#3-RoPE-的第一块积木：二维旋转" class="headerlink" title="3. RoPE 的第一块积木：二维旋转"></a>3. RoPE 的第一块积木：二维旋转</h2><p>先暂时忘记高维张量，只看二维向量：</p><p>$$<br>x&#x3D;<br>\begin{bmatrix}<br>x_1\<br>x_2<br>\end{bmatrix}<br>$$</p><p>将它逆时针旋转角度 $\phi$，使用旋转矩阵：</p><p>$$<br>R(\phi)&#x3D;<br>\begin{bmatrix}<br>\cos\phi&amp;-\sin\phi\<br>\sin\phi&amp;\cos\phi<br>\end{bmatrix}<br>$$</p><p>旋转结果为：</p><p>$$<br>R(\phi)x&#x3D;<br>\begin{bmatrix}<br>x_1\cos\phi-x_2\sin\phi\<br>x_1\sin\phi+x_2\cos\phi<br>\end{bmatrix}<br>$$</p><p><img src="https://lsworl.github.io/images/nlp-blog/rope-2d-rotation.svg" alt="二维向量按位置旋转"></p><h3 id="3-1-一个最简单的数值例子"><a href="#3-1-一个最简单的数值例子" class="headerlink" title="3.1 一个最简单的数值例子"></a>3.1 一个最简单的数值例子</h3><p>令：</p><p>$$<br>x&#x3D;<br>\begin{bmatrix}<br>1\0<br>\end{bmatrix}<br>$$</p><p>旋转 $90^\circ$：</p><p>$$<br>R(90^\circ)&#x3D;<br>\begin{bmatrix}<br>0&amp;-1\<br>1&amp;0<br>\end{bmatrix}<br>$$</p><p>所以：</p><p>$$<br>R(90^\circ)x&#x3D;<br>\begin{bmatrix}<br>0\1<br>\end{bmatrix}<br>$$</p><p>继续旋转到 $180^\circ$：</p><p>$$<br>R(180^\circ)x&#x3D;<br>\begin{bmatrix}<br>-1\0<br>\end{bmatrix}<br>$$</p><p>如果规定“每前进一个 token 就多旋转 $90^\circ$”，那么同一个内容向量在不同位置会变成：</p><table><thead><tr><th>位置 $m$</th><th>角度 $m\theta$</th><th>旋转后向量</th></tr></thead><tbody><tr><td>0</td><td>$0^\circ$</td><td>$[1,0]$</td></tr><tr><td>1</td><td>$90^\circ$</td><td>$[0,1]$</td></tr><tr><td>2</td><td>$180^\circ$</td><td>$[-1,0]$</td></tr><tr><td>3</td><td>$270^\circ$</td><td>$[0,-1]$</td></tr></tbody></table><p>这已经包含了 RoPE 最基础的直觉：</p><blockquote><p>位置不是被加进向量，而是被编码成向量的相位。</p></blockquote><h3 id="3-2-旋转不会改变向量长度"><a href="#3-2-旋转不会改变向量长度" class="headerlink" title="3.2 旋转不会改变向量长度"></a>3.2 旋转不会改变向量长度</h3><p>旋转矩阵是正交矩阵：</p><p>$$<br>R(\phi)^TR(\phi)&#x3D;I<br>$$</p><p>因此：</p><p>$$<br>|R(\phi)x|_2&#x3D;|x|_2<br>$$</p><p>也就是说，RoPE 改变方向，但不单独放大或缩小 Q、K 的模长。内容强度仍然保留，位置主要通过方向关系参与点积。</p><h2 id="4-从绝对位置到相对位置：最关键的推导"><a href="#4-从绝对位置到相对位置：最关键的推导" class="headerlink" title="4. 从绝对位置到相对位置：最关键的推导"></a>4. 从绝对位置到相对位置：最关键的推导</h2><p>假设位置 $m$ 的原始 Query 为 $q_m$，位置 $n$ 的原始 Key 为 $k_n$。</p><p>RoPE 根据各自绝对位置进行旋转：</p><p>$$<br>\widetilde q_m&#x3D;R(m\theta)q_m<br>$$</p><p>$$<br>\widetilde k_n&#x3D;R(n\theta)k_n<br>$$</p><p>Attention 分数中的点积为：</p><p>$$<br>\widetilde q_m^T\widetilde k_n<br>&#x3D;\left(R(m\theta)q_m\right)^T<br>\left(R(n\theta)k_n\right)<br>$$</p><p>把转置展开：</p><p>$$<br>&#x3D;q_m^TR(m\theta)^TR(n\theta)k_n<br>$$</p><p>旋转矩阵满足：</p><p>$$<br>R(\alpha)^T&#x3D;R(-\alpha)<br>$$</p><p>以及：</p><p>$$<br>R(\alpha)R(\beta)&#x3D;R(\alpha+\beta)<br>$$</p><p>所以：</p><p>$$<br>R(m\theta)^TR(n\theta)<br>&#x3D;R(-m\theta)R(n\theta)<br>&#x3D;R((n-m)\theta)<br>$$</p><p>最终得到：</p><p>$$<br>\boxed{<br>\widetilde q_m^T\widetilde k_n<br>&#x3D;q_m^TR((n-m)\theta)k_n<br>}<br>$$</p><p>右侧的位置项只包含 $n-m$，而不再分别依赖 $m$ 和 $n$。</p><p>这就是 RoPE 最重要的数学性质：</p><blockquote><p>Q 和 K 各自使用绝对位置旋转，但二者点积中的位置关系表现为相对距离。</p></blockquote><p><img src="https://lsworl.github.io/images/nlp-blog/rope-relative-position.svg" alt="绝对旋转产生相对位置"></p><h3 id="4-1-符号方向为什么有时写成-m-n"><a href="#4-1-符号方向为什么有时写成-m-n" class="headerlink" title="4.1 符号方向为什么有时写成 $m-n$"></a>4.1 符号方向为什么有时写成 $m-n$</h3><p>有些资料会写成 $R((m-n)\theta)$，有些写成 $R((n-m)\theta)$。差异通常来自：</p><ul><li>将 Query 放在左边还是右边。</li><li>点积推导采用行向量还是列向量。</li><li>旋转方向的定义不同。</li></ul><p>真正不变的是：</p><blockquote><p>分数依赖两个位置之差，而不是两个独立的绝对位置。</p></blockquote><p>实现时只要 Q、K 使用同一套约定即可。</p><h2 id="5-手算例子一：验证相对距离性质"><a href="#5-手算例子一：验证相对距离性质" class="headerlink" title="5. 手算例子一：验证相对距离性质"></a>5. 手算例子一：验证相对距离性质</h2><p>为了便于计算，令每个位置增加的角度为：</p><p>$$<br>\theta&#x3D;90^\circ<br>$$</p><p>原始向量为：</p><p>$$<br>q&#x3D;<br>\begin{bmatrix}<br>1\2<br>\end{bmatrix},\qquad<br>k&#x3D;<br>\begin{bmatrix}<br>3\4<br>\end{bmatrix}<br>$$</p><p>现在让 Query 位于 $m&#x3D;1$，Key 位于 $n&#x3D;2$。</p><h3 id="5-1-分别按绝对位置旋转"><a href="#5-1-分别按绝对位置旋转" class="headerlink" title="5.1 分别按绝对位置旋转"></a>5.1 分别按绝对位置旋转</h3><p>Query 旋转 $90^\circ$：</p><p>$$<br>\widetilde q_1&#x3D;R(90^\circ)q<br>&#x3D;<br>\begin{bmatrix}<br>0&amp;-1\1&amp;0<br>\end{bmatrix}<br>\begin{bmatrix}<br>1\2<br>\end{bmatrix}<br>&#x3D;<br>\begin{bmatrix}<br>-2\1<br>\end{bmatrix}<br>$$</p><p>Key 旋转 $180^\circ$：</p><p>$$<br>\widetilde k_2&#x3D;R(180^\circ)k<br>&#x3D;<br>\begin{bmatrix}<br>-1&amp;0\0&amp;-1<br>\end{bmatrix}<br>\begin{bmatrix}<br>3\4<br>\end{bmatrix}<br>&#x3D;<br>\begin{bmatrix}<br>-3\-4<br>\end{bmatrix}<br>$$</p><p>二者点积：</p><p>$$<br>\widetilde q_1^T\widetilde k_2<br>&#x3D;(-2)\times(-3)+1\times(-4)<br>&#x3D;2<br>$$</p><h3 id="5-2-只用相对距离计算"><a href="#5-2-只用相对距离计算" class="headerlink" title="5.2 只用相对距离计算"></a>5.2 只用相对距离计算</h3><p>两个位置的差为：</p><p>$$<br>n-m&#x3D;2-1&#x3D;1<br>$$</p><p>先把 Key 只旋转一个相对角度：</p><p>$$<br>R(90^\circ)k<br>&#x3D;<br>\begin{bmatrix}<br>-4\3<br>\end{bmatrix}<br>$$</p><p>再与原始 Query 点积：</p><p>$$<br>q^TR(90^\circ)k<br>&#x3D;1\times(-4)+2\times3<br>&#x3D;2<br>$$</p><p>两种算法结果完全一致：</p><pre class="language-text" data-language="text"><code class="language-text">分别使用绝对位置旋转后点积 = 2只使用相对距离旋转后点积 = 2</code></pre><p>如果把位置整体向后平移 10：</p><pre class="language-text" data-language="text"><code class="language-text">m = 11n = 12n - m 仍然等于 1</code></pre><p>位置项仍然相同。这种平移一致性正是相对位置表示需要的性质。</p><h2 id="6-手算例子二：从旋转一直算到-Attention-输出"><a href="#6-手算例子二：从旋转一直算到-Attention-输出" class="headerlink" title="6. 手算例子二：从旋转一直算到 Attention 输出"></a>6. 手算例子二：从旋转一直算到 Attention 输出</h2><p>现在模拟一个只有两个 token、单头维度为 2 的 Causal Attention。</p><p>设两个位置投影后的 Q、K 内容恰好相同：</p><p>$$<br>q_0&#x3D;q_1&#x3D;k_0&#x3D;k_1&#x3D;[1,0]<br>$$</p><p>Value 为：</p><p>$$<br>v_0&#x3D;[10,0],\qquad v_1&#x3D;[0,20]<br>$$</p><p>仍令每个位置旋转 $90^\circ$。</p><h3 id="6-1-对-Q-和-K-应用-RoPE"><a href="#6-1-对-Q-和-K-应用-RoPE" class="headerlink" title="6.1 对 Q 和 K 应用 RoPE"></a>6.1 对 Q 和 K 应用 RoPE</h3><p>位置 0 不旋转：</p><p>$$<br>\widetilde q_0&#x3D;\widetilde k_0&#x3D;[1,0]<br>$$</p><p>位置 1 旋转 $90^\circ$：</p><p>$$<br>\widetilde q_1&#x3D;\widetilde k_1&#x3D;[0,1]<br>$$</p><h3 id="6-2-计算位置-1-的注意力分数"><a href="#6-2-计算位置-1-的注意力分数" class="headerlink" title="6.2 计算位置 1 的注意力分数"></a>6.2 计算位置 1 的注意力分数</h3><p>位置 1 可以看到位置 0 和位置 1。</p><p>对位置 0：</p><p>$$<br>s_{1,0}<br>&#x3D;\frac{\widetilde q_1\cdot\widetilde k_0}{\sqrt2}<br>&#x3D;\frac{[0,1]\cdot[1,0]}{\sqrt2}<br>&#x3D;0<br>$$</p><p>对位置 1：</p><p>$$<br>s_{1,1}<br>&#x3D;\frac{\widetilde q_1\cdot\widetilde k_1}{\sqrt2}<br>&#x3D;\frac{[0,1]\cdot[0,1]}{\sqrt2}<br>&#x3D;\frac1{\sqrt2}<br>\approx0.7071<br>$$</p><h3 id="6-3-Softmax-得到权重"><a href="#6-3-Softmax-得到权重" class="headerlink" title="6.3 Softmax 得到权重"></a>6.3 Softmax 得到权重</h3><p>$$<br>\operatorname{softmax}([0,0.7071])<br>\approx[0.3302,0.6698]<br>$$</p><h3 id="6-4-对未旋转的-Value-加权求和"><a href="#6-4-对未旋转的-Value-加权求和" class="headerlink" title="6.4 对未旋转的 Value 加权求和"></a>6.4 对未旋转的 Value 加权求和</h3><p>$$<br>o_1&#x3D;0.3302v_0+0.6698v_1<br>$$</p><p>$$<br>&#x3D;0.3302[10,0]+0.6698[0,20]<br>$$</p><p>$$<br>\approx[3.3024,13.3952]<br>$$</p><p>完整过程为：</p><pre class="language-text" data-language="text"><code class="language-text">Q、K 内容相同  -> 因位置不同而旋转到不同方向  -> 点积包含相对位置信息  -> Softmax 得到位置相关权重  -> 权重对原始 V 聚合</code></pre><p>这个例子也提前回答了一个常见问题：V 不必旋转，因为位置已经参与了“应该读取哪个 V、读取多少”的权重计算。</p><h2 id="7-高维向量怎样旋转"><a href="#7-高维向量怎样旋转" class="headerlink" title="7. 高维向量怎样旋转"></a>7. 高维向量怎样旋转</h2><p>真实注意力头的维度不会只有 2。假设：</p><p>$$<br>d_h&#x3D;8<br>$$</p><p>RoPE 把最后一维两两分组：</p><pre class="language-text" data-language="text"><code class="language-text">(x0, x1)  第 0 组(x2, x3)  第 1 组(x4, x5)  第 2 组(x6, x7)  第 3 组</code></pre><p>每一组都看成一个二维平面，并使用自己的频率旋转：</p><p>$$<br>(x_{2j},x_{2j+1})<br>\xrightarrow{\ m\theta_j\ }<br>(x’<em>{2j},x’</em>{2j+1})<br>$$</p><p>第 $j$ 组的计算为：</p><p>$$<br>x’<em>{2j}<br>&#x3D;x</em>{2j}\cos(m\theta_j)<br>-x_{2j+1}\sin(m\theta_j)<br>$$</p><p>$$<br>x’<em>{2j+1}<br>&#x3D;x</em>{2j}\sin(m\theta_j)<br>+x_{2j+1}\cos(m\theta_j)<br>$$</p><p><img src="https://lsworl.github.io/images/nlp-blog/rope-frequency-pairs.svg" alt="RoPE把注意力头拆成多个频率对"></p><h3 id="7-1-为什么-head-dimension-必须是偶数"><a href="#7-1-为什么-head-dimension-必须是偶数" class="headerlink" title="7.1 为什么 head dimension 必须是偶数"></a>7.1 为什么 head dimension 必须是偶数</h3><p>每次旋转需要两个坐标组成一个平面，因此完整 RoPE 通常要求：</p><p>$$<br>d_h\bmod2&#x3D;0<br>$$</p><p>如果只旋转前 <code>rotary_dim</code> 个维度，那么需要 <code>rotary_dim</code> 为偶数；剩余维度可以保持不变，这称为 Partial RoPE。</p><h2 id="8-不同维度为什么使用不同频率"><a href="#8-不同维度为什么使用不同频率" class="headerlink" title="8. 不同维度为什么使用不同频率"></a>8. 不同维度为什么使用不同频率</h2><p>RoPE 常用频率定义为：</p><p>$$<br>\theta_j&#x3D;\text{base}^{-\frac{2j}{d_h}},<br>\qquad<br>j&#x3D;0,1,\ldots,\frac{d_h}{2}-1<br>$$</p><p>经典设置为：</p><p>$$<br>\text{base}&#x3D;10000<br>$$</p><p>位置 $m$ 对第 $j$ 组产生的实际角度为：</p><p>$$<br>\phi_{m,j}&#x3D;m\theta_j<br>$$</p><p>例如 $d_h&#x3D;4$、<code>base=10000</code>：</p><p>$$<br>\theta_0&#x3D;10000^0&#x3D;1<br>$$</p><p>$$<br>\theta_1&#x3D;10000^{-1&#x2F;2}&#x3D;0.01<br>$$</p><p>因此两个二维平面的角度分别是：</p><pre class="language-text" data-language="text"><code class="language-text">第 0 组：m × 1 rad第 1 组：m × 0.01 rad</code></pre><p>第 0 组变化快，更容易分辨邻近位置；第 1 组变化慢，可以在更长距离上保持平缓变化。</p><p>这很像同时使用多个刻度不同的时钟：</p><ul><li>高频维度像秒针，对局部位置变化敏感。</li><li>低频维度像时针，跨越较长距离后仍能提供缓慢变化的信号。</li></ul><p>多个频率一起工作，使模型可以表示不同尺度的相对距离。</p><h3 id="8-1-base-不是最大长度"><a href="#8-1-base-不是最大长度" class="headerlink" title="8.1 base 不是最大长度"></a>8.1 <code>base</code> 不是最大长度</h3><p><code>base=10000</code> 不表示“模型最多只能处理 10000 个 token”。它控制的是频率的分布。</p><p>一般来说，增大 <code>base</code> 会让低频部分旋转得更慢，但上下文能力还取决于：</p><ul><li>预训练时见过的长度。</li><li>频率缩放方案。</li><li>模型是否针对新长度继续训练。</li><li>Attention、数据与评估任务。</li></ul><p>不能只改一个 <code>base</code> 就断言上下文窗口已经可靠扩展。</p><h2 id="9-完整旋转矩阵"><a href="#9-完整旋转矩阵" class="headerlink" title="9. 完整旋转矩阵"></a>9. 完整旋转矩阵</h2><p>对偶数维向量，位置 $m$ 的旋转矩阵可以写成分块对角矩阵：</p><p>$$<br>R_m&#x3D;<br>\begin{bmatrix}<br>R(m\theta_0)&amp;0&amp;\cdots&amp;0\<br>0&amp;R(m\theta_1)&amp;\cdots&amp;0\<br>\vdots&amp;\vdots&amp;\ddots&amp;\vdots\<br>0&amp;0&amp;\cdots&amp;R(m\theta_{d_h&#x2F;2-1})<br>\end{bmatrix}<br>$$</p><p>每个小块都是：</p><p>$$<br>R(m\theta_j)&#x3D;<br>\begin{bmatrix}<br>\cos(m\theta_j)&amp;-\sin(m\theta_j)\<br>\sin(m\theta_j)&amp;\cos(m\theta_j)<br>\end{bmatrix}<br>$$</p><p>真实代码不会为每个 token 构造巨大的矩阵 $R_m$。那样既浪费内存，也会执行大量无意义的零元素乘法。</p><p>实现只需要缓存或即时计算：</p><pre class="language-text" data-language="text"><code class="language-text">cos(position × frequency)sin(position × frequency)</code></pre><p>然后使用逐元素乘法完成每个二维对的旋转。</p><h2 id="10-复数视角：旋转其实是乘相位"><a href="#10-复数视角：旋转其实是乘相位" class="headerlink" title="10. 复数视角：旋转其实是乘相位"></a>10. 复数视角：旋转其实是乘相位</h2><p>二维向量可以写成复数：</p><p>$$<br>z&#x3D;x_1+ix_2<br>$$</p><p>欧拉公式为：</p><p>$$<br>e^{i\phi}&#x3D;\cos\phi+i\sin\phi<br>$$</p><p>把复数乘以 $e^{i\phi}$：</p><p>$$<br>ze^{i\phi}<br>$$</p><p>它的模长不变，相位增加 $\phi$，几何意义正是二维旋转。</p><p>因此 RoPE 也可以写成：</p><p>$$<br>\widetilde q_{m,j}&#x3D;q_{m,j}e^{im\theta_j}<br>$$</p><p>$$<br>\widetilde k_{n,j}&#x3D;k_{n,j}e^{in\theta_j}<br>$$</p><p>这不是另一种算法，而是同一旋转的另一种表达方式。后面会分别给出实数版和复数版 PyTorch 实现。</p><h2 id="11-RoPE-在-Attention-中的准确位置"><a href="#11-RoPE-在-Attention-中的准确位置" class="headerlink" title="11. RoPE 在 Attention 中的准确位置"></a>11. RoPE 在 Attention 中的准确位置</h2><p>正确顺序是：</p><pre class="language-text" data-language="text"><code class="language-text">x: [B, T, C]  -> Q/K/V Linear  -> 拆成多个头 [B, H, T, D]  -> 对 Q、K 应用 RoPE  -> QK^T / sqrt(D)  -> Causal Mask  -> Softmax  -> Attention Weight @ V  -> 合并多个头</code></pre><p>这里：</p><ul><li>$B$：batch size。</li><li>$T$：序列长度。</li><li>$C$：模型维度。</li><li>$H$：Query 头数。</li><li>$D&#x3D;C&#x2F;H$：每个 Query 头的维度。</li></ul><p><img src="https://lsworl.github.io/images/nlp-blog/rope-tensor-shapes.svg" alt="RoPE的张量形状流"></p><h3 id="11-1-RoPE-不是旋转-token-embedding"><a href="#11-1-RoPE-不是旋转-token-embedding" class="headerlink" title="11.1 RoPE 不是旋转 token embedding"></a>11.1 RoPE 不是旋转 token embedding</h3><p>常见错误是直接写：</p><pre class="language-python" data-language="python"><code class="language-python">x <span class="token operator">=</span> apply_rope<span class="token punctuation">(</span>x<span class="token punctuation">)</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> v <span class="token operator">=</span> qkv<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">.</span>chunk<span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>标准 RoPE 的目标是 Attention 内的 Query 和 Key。更符合定义的顺序是：</p><pre class="language-python" data-language="python"><code class="language-python">q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> v <span class="token operator">=</span> qkv<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">.</span>chunk<span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>q<span class="token punctuation">,</span> k <span class="token operator">=</span> apply_rope<span class="token punctuation">(</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">)</span></code></pre><h3 id="11-2-每层都要应用"><a href="#11-2-每层都要应用" class="headerlink" title="11.2 每层都要应用"></a>11.2 每层都要应用</h3><p>每个 Transformer Block 都会产生自己的 Q 和 K，因此每一层 Attention 都需要对本层的 Q、K 应用 RoPE。</p><p>同一组位置频率可以在层之间共享，不必为每层学习不同的位置表。</p><h2 id="12-为什么只旋转-Q-和-K"><a href="#12-为什么只旋转-Q-和-K" class="headerlink" title="12. 为什么只旋转 Q 和 K"></a>12. 为什么只旋转 Q 和 K</h2><p>Attention 可以拆成两个阶段：</p><ol><li>$QK^T$ 决定“应该关注谁”。</li><li>Attention 权重乘 $V$ 决定“从对方取回什么内容”。</li></ol><p>RoPE 主要希望让匹配分数包含相对位置，所以旋转 Q、K 已经足够：</p><p>$$<br>\operatorname{softmax}<br>\left(<br>\frac{(R_mq_m)^T(R_nk_n)}{\sqrt{d_h}}<br>\right)V<br>$$</p><p>Value 本身不参与 Query-Key 相似度计算，通常保持不旋转。</p><p>可以把它理解为：</p><pre class="language-text" data-language="text"><code class="language-text">Q、K：结合内容和距离，决定读取地址与权重V：保存真正需要被聚合的内容</code></pre><p>这不是说 V 完全没有上下文信息。前一层输出本身已经包含上下文，当前层的位置影响也会通过注意力权重进入加权结果。</p><h2 id="13-PyTorch-实现一：预计算-cos-和-sin"><a href="#13-PyTorch-实现一：预计算-cos-和-sin" class="headerlink" title="13. PyTorch 实现一：预计算 cos 和 sin"></a>13. PyTorch 实现一：预计算 cos 和 sin</h2><p>先实现频率表：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">def</span> <span class="token function">precompute_rope</span><span class="token punctuation">(</span>head_dim<span class="token punctuation">,</span> max_seq_len<span class="token punctuation">,</span> base<span class="token operator">=</span><span class="token number">10000.0</span><span class="token punctuation">,</span> device<span class="token operator">=</span><span class="token boolean">None</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> head_dim <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">:</span>        <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span><span class="token string">"head_dim must be even for RoPE"</span><span class="token punctuation">)</span>    pair_index <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span>        <span class="token number">0</span><span class="token punctuation">,</span> head_dim<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span>float32<span class="token punctuation">,</span> device<span class="token operator">=</span>device    <span class="token punctuation">)</span>    inv_freq <span class="token operator">=</span> base <span class="token operator">**</span> <span class="token punctuation">(</span><span class="token operator">-</span>pair_index <span class="token operator">/</span> head_dim<span class="token punctuation">)</span>    positions <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span>        max_seq_len<span class="token punctuation">,</span> dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span>float32<span class="token punctuation">,</span> device<span class="token operator">=</span>device    <span class="token punctuation">)</span>    angles <span class="token operator">=</span> torch<span class="token punctuation">.</span>outer<span class="token punctuation">(</span>positions<span class="token punctuation">,</span> inv_freq<span class="token punctuation">)</span>    cos <span class="token operator">=</span> angles<span class="token punctuation">.</span>cos<span class="token punctuation">(</span><span class="token punctuation">)</span>    sin <span class="token operator">=</span> angles<span class="token punctuation">.</span>sin<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">return</span> cos<span class="token punctuation">,</span> sin</code></pre><p>形状变化为：</p><pre class="language-text" data-language="text"><code class="language-text">pair_index: [D/2]inv_freq:   [D/2]positions:  [T]angles:     [T, D/2]cos, sin:   [T, D/2]</code></pre><p>以 <code>D=8, T=128</code> 为例：</p><pre class="language-text" data-language="text"><code class="language-text">cos.shape = [128, 4]sin.shape = [128, 4]</code></pre><h3 id="13-1-为什么变量叫-inv-freq"><a href="#13-1-为什么变量叫-inv-freq" class="headerlink" title="13.1 为什么变量叫 inv_freq"></a>13.1 为什么变量叫 <code>inv_freq</code></h3><p>代码常写成：</p><pre class="language-python" data-language="python"><code class="language-python">inv_freq <span class="token operator">=</span> <span class="token number">1.0</span> <span class="token operator">/</span> <span class="token punctuation">(</span>    base <span class="token operator">**</span> <span class="token punctuation">(</span>torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> head_dim<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span> <span class="token operator">/</span> head_dim<span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>它与：</p><pre class="language-python" data-language="python"><code class="language-python">base <span class="token operator">**</span> <span class="token punctuation">(</span><span class="token operator">-</span>torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> head_dim<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span> <span class="token operator">/</span> head_dim<span class="token punctuation">)</span></code></pre><p>完全等价。</p><p>这里的“inverse frequency”不是对最终角度求倒数，而是用负指数构造从快到慢的一组角频率。</p><h2 id="14-PyTorch-实现二：旋转二维对"><a href="#14-PyTorch-实现二：旋转二维对" class="headerlink" title="14. PyTorch 实现二：旋转二维对"></a>14. PyTorch 实现二：旋转二维对</h2><p>输入张量形状设为：</p><pre class="language-text" data-language="text"><code class="language-text">q, k: [B, H, T, D]</code></pre><p>先实现 $[-x_2,x_1]$：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">rotate_pairs</span><span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">:</span>    x_pairs <span class="token operator">=</span> x<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">*</span>x<span class="token punctuation">.</span>shape<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>    x_even <span class="token operator">=</span> x_pairs<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span>    x_odd <span class="token operator">=</span> x_pairs<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">]</span>    rotated <span class="token operator">=</span> torch<span class="token punctuation">.</span>stack<span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token operator">-</span>x_odd<span class="token punctuation">,</span> x_even<span class="token punctuation">)</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>    <span class="token keyword">return</span> rotated<span class="token punctuation">.</span>flatten<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">)</span></code></pre><p>如果：</p><pre class="language-text" data-language="text"><code class="language-text">x = [x0, x1, x2, x3]</code></pre><p>那么：</p><pre class="language-text" data-language="text"><code class="language-text">rotate_pairs(x) = [-x1, x0, -x3, x2]</code></pre><p>再应用公式：</p><p>$$<br>R(\phi)x&#x3D;x\cos\phi+\operatorname{rotate_pairs}(x)\sin\phi<br>$$</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">apply_rope</span><span class="token punctuation">(</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">,</span> position_ids<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token comment"># cos/sin: [max_seq_len, D/2]</span>    <span class="token comment"># position_ids: [B, T]</span>    cos <span class="token operator">=</span> cos<span class="token punctuation">[</span>position_ids<span class="token punctuation">]</span><span class="token punctuation">.</span>repeat_interleave<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>    sin <span class="token operator">=</span> sin<span class="token punctuation">[</span>position_ids<span class="token punctuation">]</span><span class="token punctuation">.</span>repeat_interleave<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>    <span class="token comment"># [B, T, D] -> [B, 1, T, D]，广播到所有头</span>    cos <span class="token operator">=</span> cos<span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>    sin <span class="token operator">=</span> sin<span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>    q_float <span class="token operator">=</span> q<span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    k_float <span class="token operator">=</span> k<span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    q_rotated <span class="token operator">=</span> q_float <span class="token operator">*</span> cos <span class="token operator">+</span> rotate_pairs<span class="token punctuation">(</span>q_float<span class="token punctuation">)</span> <span class="token operator">*</span> sin    k_rotated <span class="token operator">=</span> k_float <span class="token operator">*</span> cos <span class="token operator">+</span> rotate_pairs<span class="token punctuation">(</span>k_float<span class="token punctuation">)</span> <span class="token operator">*</span> sin    <span class="token keyword">return</span> q_rotated<span class="token punctuation">.</span>to<span class="token punctuation">(</span>q<span class="token punctuation">.</span>dtype<span class="token punctuation">)</span><span class="token punctuation">,</span> k_rotated<span class="token punctuation">.</span>to<span class="token punctuation">(</span>k<span class="token punctuation">.</span>dtype<span class="token punctuation">)</span></code></pre><h3 id="14-1-为什么用-repeat-interleave-2"><a href="#14-1-为什么用-repeat-interleave-2" class="headerlink" title="14.1 为什么用 repeat_interleave(2)"></a>14.1 为什么用 <code>repeat_interleave(2)</code></h3><p>预计算时每个二维对只有一个角度：</p><pre class="language-text" data-language="text"><code class="language-text">[phi0, phi1, phi2, phi3]</code></pre><p>一对中的两个坐标要共享同一个角度，所以扩展成：</p><pre class="language-text" data-language="text"><code class="language-text">[phi0, phi0, phi1, phi1, phi2, phi2, phi3, phi3]</code></pre><p>对应的 cos 和 sin 才能与最后一维 <code>[D]</code> 逐元素运算。</p><h3 id="14-2-为什么先转成-FP32"><a href="#14-2-为什么先转成-FP32" class="headerlink" title="14.2 为什么先转成 FP32"></a>14.2 为什么先转成 FP32</h3><p>长位置与高频角度在低精度下更容易产生数值误差。教学实现中可以先把 Q、K 和角度计算提升到 FP32，旋转完成后再转回原始 dtype：</p><pre class="language-python" data-language="python"><code class="language-python">q_float <span class="token operator">=</span> q<span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>q_rotated<span class="token punctuation">.</span>to<span class="token punctuation">(</span>q<span class="token punctuation">.</span>dtype<span class="token punctuation">)</span></code></pre><p>这不是唯一实现方式，但比直接在低精度里计算大量三角函数更稳妥。</p><h2 id="15-PyTorch-实现三：封装成-nn-Module"><a href="#15-PyTorch-实现三：封装成-nn-Module" class="headerlink" title="15. PyTorch 实现三：封装成 nn.Module"></a>15. PyTorch 实现三：封装成 <code>nn.Module</code></h2><p>实际模型经常动态传入 <code>position_ids</code>，可以写成：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> torch <span class="token keyword">import</span> nn<span class="token keyword">class</span> <span class="token class-name">RotaryEmbedding</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> head_dim<span class="token punctuation">,</span> base<span class="token operator">=</span><span class="token number">10000.0</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        <span class="token keyword">if</span> head_dim <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">:</span>            <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span><span class="token string">"head_dim must be even for RoPE"</span><span class="token punctuation">)</span>        pair_index <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> head_dim<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span>        inv_freq <span class="token operator">=</span> base <span class="token operator">**</span> <span class="token punctuation">(</span><span class="token operator">-</span>pair_index <span class="token operator">/</span> head_dim<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>register_buffer<span class="token punctuation">(</span>            <span class="token string">"inv_freq"</span><span class="token punctuation">,</span> inv_freq<span class="token punctuation">,</span> persistent<span class="token operator">=</span><span class="token boolean">False</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">,</span> position_ids<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token comment"># position_ids: [B, T]</span>        angles <span class="token operator">=</span> <span class="token punctuation">(</span>            position_ids<span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>            <span class="token operator">*</span> self<span class="token punctuation">.</span>inv_freq<span class="token punctuation">.</span>view<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        <span class="token punctuation">)</span>        angles <span class="token operator">=</span> torch<span class="token punctuation">.</span>repeat_interleave<span class="token punctuation">(</span>angles<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        cos <span class="token operator">=</span> angles<span class="token punctuation">.</span>cos<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">.</span>to<span class="token punctuation">(</span>x<span class="token punctuation">.</span>dtype<span class="token punctuation">)</span>        sin <span class="token operator">=</span> angles<span class="token punctuation">.</span>sin<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">.</span>to<span class="token punctuation">(</span>x<span class="token punctuation">.</span>dtype<span class="token punctuation">)</span>        <span class="token keyword">return</span> cos<span class="token punctuation">,</span> sin</code></pre><p>这里不预先绑定最大序列长度，而是根据本批次的 <code>position_ids</code> 动态计算角度。</p><h3 id="15-1-为什么使用-register-buffer"><a href="#15-1-为什么使用-register-buffer" class="headerlink" title="15.1 为什么使用 register_buffer"></a>15.1 为什么使用 <code>register_buffer</code></h3><p><code>inv_freq</code>：</p><ul><li>不是通过梯度学习的参数。</li><li>需要随模型移动到 CPU 或 GPU。</li><li>不应该进入优化器。</li></ul><p>所以它适合注册为 buffer，而不是 <code>nn.Parameter</code>。</p><p><code>persistent=False</code> 表示不把它写入 <code>state_dict</code>，因为它可以由 <code>head_dim</code> 和 <code>base</code> 重新计算。如果希望完整保存，也可以省略该参数。</p><h2 id="16-复数版-PyTorch-实现"><a href="#16-复数版-PyTorch-实现" class="headerlink" title="16. 复数版 PyTorch 实现"></a>16. 复数版 PyTorch 实现</h2><p>复数实现更贴近“乘相位”的数学表达：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">precompute_freqs_cis</span><span class="token punctuation">(</span>head_dim<span class="token punctuation">,</span> max_seq_len<span class="token punctuation">,</span> base<span class="token operator">=</span><span class="token number">10000.0</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    pair_index <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> head_dim<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    inv_freq <span class="token operator">=</span> base <span class="token operator">**</span> <span class="token punctuation">(</span><span class="token operator">-</span>pair_index <span class="token operator">/</span> head_dim<span class="token punctuation">)</span>    positions <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span>max_seq_len<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    angles <span class="token operator">=</span> torch<span class="token punctuation">.</span>outer<span class="token punctuation">(</span>positions<span class="token punctuation">,</span> inv_freq<span class="token punctuation">)</span>    <span class="token keyword">return</span> torch<span class="token punctuation">.</span>polar<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>ones_like<span class="token punctuation">(</span>angles<span class="token punctuation">)</span><span class="token punctuation">,</span> angles<span class="token punctuation">)</span><span class="token keyword">def</span> <span class="token function">apply_rope_complex</span><span class="token punctuation">(</span>x<span class="token punctuation">,</span> freqs_cis<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token comment"># x: [B, T, H, D]</span>    original_dtype <span class="token operator">=</span> x<span class="token punctuation">.</span>dtype    x_complex <span class="token operator">=</span> torch<span class="token punctuation">.</span>view_as_complex<span class="token punctuation">(</span>        x<span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">*</span>x<span class="token punctuation">.</span>shape<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>    <span class="token punctuation">)</span>    <span class="token comment"># freqs_cis: [T, D/2] -> [1, T, 1, D/2]</span>    freqs_cis <span class="token operator">=</span> freqs_cis<span class="token punctuation">.</span>view<span class="token punctuation">(</span>        <span class="token number">1</span><span class="token punctuation">,</span> x<span class="token punctuation">.</span>shape<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> x<span class="token punctuation">.</span>shape<span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span> <span class="token operator">//</span> <span class="token number">2</span>    <span class="token punctuation">)</span>    x_rotated <span class="token operator">=</span> x_complex <span class="token operator">*</span> freqs_cis    x_out <span class="token operator">=</span> torch<span class="token punctuation">.</span>view_as_real<span class="token punctuation">(</span>x_rotated<span class="token punctuation">)</span><span class="token punctuation">.</span>flatten<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">)</span>    <span class="token keyword">return</span> x_out<span class="token punctuation">.</span>to<span class="token punctuation">(</span>original_dtype<span class="token punctuation">)</span></code></pre><p>对应关系是：</p><pre class="language-text" data-language="text"><code class="language-text">[x0, x1] -> x0 + i*x1乘 exp(i*phi)再拆回 [real, imag]</code></pre><p>实数版和复数版结果应相同。工程中选择哪一种，取决于框架支持、内核、可读性与目标硬件。</p><h2 id="17-把-RoPE-接入多头因果注意力"><a href="#17-把-RoPE-接入多头因果注意力" class="headerlink" title="17. 把 RoPE 接入多头因果注意力"></a>17. 把 RoPE 接入多头因果注意力</h2><p>下面给出一个完整、可运行的教学实现：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">import</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>functional <span class="token keyword">as</span> F<span class="token keyword">from</span> torch <span class="token keyword">import</span> nn<span class="token keyword">def</span> <span class="token function">rotate_pairs</span><span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">:</span>    x_pairs <span class="token operator">=</span> x<span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">*</span>x<span class="token punctuation">.</span>shape<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>    x_even <span class="token operator">=</span> x_pairs<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span>    x_odd <span class="token operator">=</span> x_pairs<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">]</span>    <span class="token keyword">return</span> torch<span class="token punctuation">.</span>stack<span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token operator">-</span>x_odd<span class="token punctuation">,</span> x_even<span class="token punctuation">)</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">.</span>flatten<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">)</span><span class="token keyword">def</span> <span class="token function">apply_rotary</span><span class="token punctuation">(</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">)</span><span class="token punctuation">:</span>    q_float <span class="token operator">=</span> q<span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    k_float <span class="token operator">=</span> k<span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    q_out <span class="token operator">=</span> q_float <span class="token operator">*</span> cos <span class="token operator">+</span> rotate_pairs<span class="token punctuation">(</span>q_float<span class="token punctuation">)</span> <span class="token operator">*</span> sin    k_out <span class="token operator">=</span> k_float <span class="token operator">*</span> cos <span class="token operator">+</span> rotate_pairs<span class="token punctuation">(</span>k_float<span class="token punctuation">)</span> <span class="token operator">*</span> sin    <span class="token keyword">return</span> q_out<span class="token punctuation">.</span>to<span class="token punctuation">(</span>q<span class="token punctuation">.</span>dtype<span class="token punctuation">)</span><span class="token punctuation">,</span> k_out<span class="token punctuation">.</span>to<span class="token punctuation">(</span>k<span class="token punctuation">.</span>dtype<span class="token punctuation">)</span><span class="token keyword">class</span> <span class="token class-name">RotaryEmbedding</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> head_dim<span class="token punctuation">,</span> base<span class="token operator">=</span><span class="token number">10000.0</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        <span class="token keyword">if</span> head_dim <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">:</span>            <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span><span class="token string">"head_dim must be even"</span><span class="token punctuation">)</span>        pair_index <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> head_dim<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span>        inv_freq <span class="token operator">=</span> base <span class="token operator">**</span> <span class="token punctuation">(</span><span class="token operator">-</span>pair_index <span class="token operator">/</span> head_dim<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>register_buffer<span class="token punctuation">(</span>            <span class="token string">"inv_freq"</span><span class="token punctuation">,</span> inv_freq<span class="token punctuation">,</span> persistent<span class="token operator">=</span><span class="token boolean">False</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">,</span> position_ids<span class="token punctuation">)</span><span class="token punctuation">:</span>        angles <span class="token operator">=</span> <span class="token punctuation">(</span>            position_ids<span class="token punctuation">.</span><span class="token builtin">float</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>            <span class="token operator">*</span> self<span class="token punctuation">.</span>inv_freq<span class="token punctuation">.</span>view<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        <span class="token punctuation">)</span>        angles <span class="token operator">=</span> torch<span class="token punctuation">.</span>repeat_interleave<span class="token punctuation">(</span>angles<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        <span class="token comment"># [B, 1, T, D]，头维自动广播</span>        cos <span class="token operator">=</span> angles<span class="token punctuation">.</span>cos<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>        sin <span class="token operator">=</span> angles<span class="token punctuation">.</span>sin<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>        <span class="token keyword">return</span> cos<span class="token punctuation">,</span> sin<span class="token keyword">class</span> <span class="token class-name">RoPECausalSelfAttention</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>        self<span class="token punctuation">,</span>        n_embd<span class="token punctuation">,</span>        n_head<span class="token punctuation">,</span>        dropout<span class="token operator">=</span><span class="token number">0.0</span><span class="token punctuation">,</span>        rope_base<span class="token operator">=</span><span class="token number">10000.0</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        <span class="token keyword">if</span> n_embd <span class="token operator">%</span> n_head <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">:</span>            <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span><span class="token string">"n_embd must be divisible by n_head"</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>n_head <span class="token operator">=</span> n_head        self<span class="token punctuation">.</span>head_dim <span class="token operator">=</span> n_embd <span class="token operator">//</span> n_head        <span class="token keyword">if</span> self<span class="token punctuation">.</span>head_dim <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">:</span>            <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span><span class="token string">"head_dim must be even for full RoPE"</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>qkv <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>n_embd<span class="token punctuation">,</span> <span class="token number">3</span> <span class="token operator">*</span> n_embd<span class="token punctuation">,</span> bias<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>out_proj <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>n_embd<span class="token punctuation">,</span> n_embd<span class="token punctuation">,</span> bias<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>resid_dropout <span class="token operator">=</span> nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>dropout<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>attn_dropout <span class="token operator">=</span> dropout        self<span class="token punctuation">.</span>rope <span class="token operator">=</span> RotaryEmbedding<span class="token punctuation">(</span>            head_dim<span class="token operator">=</span>self<span class="token punctuation">.</span>head_dim<span class="token punctuation">,</span>            base<span class="token operator">=</span>rope_base<span class="token punctuation">,</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">,</span> position_ids<span class="token operator">=</span><span class="token boolean">None</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        B<span class="token punctuation">,</span> T<span class="token punctuation">,</span> C <span class="token operator">=</span> x<span class="token punctuation">.</span>shape        <span class="token keyword">if</span> position_ids <span class="token keyword">is</span> <span class="token boolean">None</span><span class="token punctuation">:</span>            position_ids <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span>                T<span class="token punctuation">,</span> device<span class="token operator">=</span>x<span class="token punctuation">.</span>device            <span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">.</span>expand<span class="token punctuation">(</span>B<span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> v <span class="token operator">=</span> self<span class="token punctuation">.</span>qkv<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">.</span>chunk<span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        q <span class="token operator">=</span> q<span class="token punctuation">.</span>view<span class="token punctuation">(</span>B<span class="token punctuation">,</span> T<span class="token punctuation">,</span> self<span class="token punctuation">.</span>n_head<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span>        k <span class="token operator">=</span> k<span class="token punctuation">.</span>view<span class="token punctuation">(</span>B<span class="token punctuation">,</span> T<span class="token punctuation">,</span> self<span class="token punctuation">.</span>n_head<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span>        v <span class="token operator">=</span> v<span class="token punctuation">.</span>view<span class="token punctuation">(</span>B<span class="token punctuation">,</span> T<span class="token punctuation">,</span> self<span class="token punctuation">.</span>n_head<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span>        q <span class="token operator">=</span> q<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>        k <span class="token operator">=</span> k<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>        v <span class="token operator">=</span> v<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>        cos<span class="token punctuation">,</span> sin <span class="token operator">=</span> self<span class="token punctuation">.</span>rope<span class="token punctuation">(</span>q<span class="token punctuation">,</span> position_ids<span class="token punctuation">)</span>        q<span class="token punctuation">,</span> k <span class="token operator">=</span> apply_rotary<span class="token punctuation">(</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">)</span>        y <span class="token operator">=</span> F<span class="token punctuation">.</span>scaled_dot_product_attention<span class="token punctuation">(</span>            q<span class="token punctuation">,</span>            k<span class="token punctuation">,</span>            v<span class="token punctuation">,</span>            is_causal<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>            dropout_p<span class="token operator">=</span><span class="token punctuation">(</span>                self<span class="token punctuation">.</span>attn_dropout <span class="token keyword">if</span> self<span class="token punctuation">.</span>training <span class="token keyword">else</span> <span class="token number">0.0</span>            <span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>        y <span class="token operator">=</span> y<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">.</span>contiguous<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>view<span class="token punctuation">(</span>B<span class="token punctuation">,</span> T<span class="token punctuation">,</span> C<span class="token punctuation">)</span>        y <span class="token operator">=</span> self<span class="token punctuation">.</span>out_proj<span class="token punctuation">(</span>y<span class="token punctuation">)</span>        <span class="token keyword">return</span> self<span class="token punctuation">.</span>resid_dropout<span class="token punctuation">(</span>y<span class="token punctuation">)</span></code></pre><h3 id="17-1-与前一篇-Mini-GPT-相比改了什么"><a href="#17-1-与前一篇-Mini-GPT-相比改了什么" class="headerlink" title="17.1 与前一篇 Mini-GPT 相比改了什么"></a>17.1 与前一篇 Mini-GPT 相比改了什么</h3><p>原来的输入：</p><pre class="language-python" data-language="python"><code class="language-python">x <span class="token operator">=</span> token_embedding<span class="token punctuation">(</span>idx<span class="token punctuation">)</span> <span class="token operator">+</span> position_embedding<span class="token punctuation">(</span>pos<span class="token punctuation">)</span></code></pre><p>改成 RoPE 后：</p><pre class="language-python" data-language="python"><code class="language-python">x <span class="token operator">=</span> token_embedding<span class="token punctuation">(</span>idx<span class="token punctuation">)</span></code></pre><p>并在每层 Attention 中增加：</p><pre class="language-python" data-language="python"><code class="language-python">cos<span class="token punctuation">,</span> sin <span class="token operator">=</span> self<span class="token punctuation">.</span>rope<span class="token punctuation">(</span>q<span class="token punctuation">,</span> position_ids<span class="token punctuation">)</span>q<span class="token punctuation">,</span> k <span class="token operator">=</span> apply_rotary<span class="token punctuation">(</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">)</span></code></pre><p>也就是说：</p><ul><li>删除输入处的绝对 Position Embedding。</li><li>保留 Token Embedding。</li><li>每层都在线性投影后旋转 Q、K。</li><li>后面的 Mask、Softmax、Value 聚合不变。</li></ul><h3 id="17-2-scaled-dot-product-attention-不会自动加入-RoPE"><a href="#17-2-scaled-dot-product-attention-不会自动加入-RoPE" class="headerlink" title="17.2 scaled_dot_product_attention 不会自动加入 RoPE"></a>17.2 <code>scaled_dot_product_attention</code> 不会自动加入 RoPE</h3><p>PyTorch 的 <code>F.scaled_dot_product_attention</code> 负责缩放点积、Mask、Softmax、Dropout 与 Value 聚合，但不会猜测模型采用哪一种位置编码。</p><p>所以必须在调用它之前完成：</p><pre class="language-python" data-language="python"><code class="language-python">q<span class="token punctuation">,</span> k <span class="token operator">=</span> apply_rotary<span class="token punctuation">(</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">)</span></code></pre><h2 id="18-用断言验证实现是否正确"><a href="#18-用断言验证实现是否正确" class="headerlink" title="18. 用断言验证实现是否正确"></a>18. 用断言验证实现是否正确</h2><p>不要只看“代码能运行”。RoPE 至少应通过下面几类性质测试。</p><h3 id="18-1-旋转保持范数"><a href="#18-1-旋转保持范数" class="headerlink" title="18.1 旋转保持范数"></a>18.1 旋转保持范数</h3><pre class="language-python" data-language="python"><code class="language-python">q_rot<span class="token punctuation">,</span> k_rot <span class="token operator">=</span> apply_rotary<span class="token punctuation">(</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">)</span>torch<span class="token punctuation">.</span>testing<span class="token punctuation">.</span>assert_close<span class="token punctuation">(</span>    q<span class="token punctuation">.</span>norm<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    q_rot<span class="token punctuation">.</span>norm<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    rtol<span class="token operator">=</span><span class="token number">1e-5</span><span class="token punctuation">,</span>    atol<span class="token operator">=</span><span class="token number">1e-5</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="18-2-位置-0-不改变向量"><a href="#18-2-位置-0-不改变向量" class="headerlink" title="18.2 位置 0 不改变向量"></a>18.2 位置 0 不改变向量</h3><p>因为：</p><p>$$<br>\cos0&#x3D;1,\qquad\sin0&#x3D;0<br>$$</p><p>所以位置 0 的旋转应为恒等变换：</p><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>testing<span class="token punctuation">.</span>assert_close<span class="token punctuation">(</span>q_rot<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">,</span> q<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span></code></pre><h3 id="18-3-相同相对距离得到相同的位置作用"><a href="#18-3-相同相对距离得到相同的位置作用" class="headerlink" title="18.3 相同相对距离得到相同的位置作用"></a>18.3 相同相对距离得到相同的位置作用</h3><p>固定相同的基础 q、k，比较位置对：</p><pre class="language-text" data-language="text"><code class="language-text">(m, n) = (1, 3)(m, n) = (5, 7)</code></pre><p>它们都有：</p><p>$$<br>n-m&#x3D;2<br>$$</p><p>因此旋转后的点积应接近相同。</p><pre class="language-python" data-language="python"><code class="language-python">score_13 <span class="token operator">=</span> <span class="token punctuation">(</span>q_at_1 <span class="token operator">*</span> k_at_3<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token builtin">sum</span><span class="token punctuation">(</span><span class="token punctuation">)</span>score_57 <span class="token operator">=</span> <span class="token punctuation">(</span>q_at_5 <span class="token operator">*</span> k_at_7<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token builtin">sum</span><span class="token punctuation">(</span><span class="token punctuation">)</span>torch<span class="token punctuation">.</span>testing<span class="token punctuation">.</span>assert_close<span class="token punctuation">(</span>score_13<span class="token punctuation">,</span> score_57<span class="token punctuation">)</span></code></pre><h3 id="18-4-实数版与复数版一致"><a href="#18-4-实数版与复数版一致" class="headerlink" title="18.4 实数版与复数版一致"></a>18.4 实数版与复数版一致</h3><p>使用同一输入和频率，两种实现输出应在浮点误差范围内一致。这是发现“维度配对方式不一致”最有效的测试之一。</p><h2 id="19-RoPE-与-KV-Cache"><a href="#19-RoPE-与-KV-Cache" class="headerlink" title="19. RoPE 与 KV Cache"></a>19. RoPE 与 KV Cache</h2><p>自回归生成时，每轮只新增一个 token。假设已经缓存位置 $0$ 到 $t-1$ 的 Key 和 Value：</p><pre class="language-text" data-language="text"><code class="language-text">cache_k: [B, H_kv, t, D]cache_v: [B, H_kv, t, D]</code></pre><p>生成位置 $t$ 时：</p><ol><li>为新 token 计算 $q_t,k_t,v_t$。</li><li>使用绝对位置 <code>position_id=t</code> 旋转 $q_t,k_t$。</li><li>把旋转后的 $k_t$ 放入 Key Cache。</li><li>把未旋转的 $v_t$ 放入 Value Cache。</li><li>用 $q_t$ 对全部缓存 Key 计算分数。</li></ol><p><img src="https://lsworl.github.io/images/nlp-blog/rope-kv-cache.svg" alt="RoPE与KV Cache的生成流程"></p><h3 id="19-1-为什么缓存旋转后的-Key"><a href="#19-1-为什么缓存旋转后的-Key" class="headerlink" title="19.1 为什么缓存旋转后的 Key"></a>19.1 为什么缓存旋转后的 Key</h3><p>历史 Key 的位置不会改变。位置 7 的 Key 在后续每一轮仍然属于位置 7，因此可以在第一次产生时就旋转并缓存，后续无需重复旋转。</p><p>伪代码如下：</p><pre class="language-python" data-language="python"><code class="language-python">q_new<span class="token punctuation">,</span> k_new<span class="token punctuation">,</span> v_new <span class="token operator">=</span> project<span class="token punctuation">(</span>x_new<span class="token punctuation">)</span>position_ids <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token punctuation">[</span>cache_len<span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">,</span> device<span class="token operator">=</span>x_new<span class="token punctuation">.</span>device<span class="token punctuation">)</span>cos<span class="token punctuation">,</span> sin <span class="token operator">=</span> rope<span class="token punctuation">(</span>q_new<span class="token punctuation">,</span> position_ids<span class="token punctuation">)</span>q_new<span class="token punctuation">,</span> k_new <span class="token operator">=</span> apply_rotary<span class="token punctuation">(</span>q_new<span class="token punctuation">,</span> k_new<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">)</span>cache_k <span class="token operator">=</span> torch<span class="token punctuation">.</span>cat<span class="token punctuation">(</span><span class="token punctuation">[</span>cache_k<span class="token punctuation">,</span> k_new<span class="token punctuation">]</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">)</span>cache_v <span class="token operator">=</span> torch<span class="token punctuation">.</span>cat<span class="token punctuation">(</span><span class="token punctuation">[</span>cache_v<span class="token punctuation">,</span> v_new<span class="token punctuation">]</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">)</span>output <span class="token operator">=</span> attention<span class="token punctuation">(</span>q_new<span class="token punctuation">,</span> cache_k<span class="token punctuation">,</span> cache_v<span class="token punctuation">)</span></code></pre><h3 id="19-2-不能每轮都把新-token-当作位置-0"><a href="#19-2-不能每轮都把新-token-当作位置-0" class="headerlink" title="19.2 不能每轮都把新 token 当作位置 0"></a>19.2 不能每轮都把新 token 当作位置 0</h3><p>有 KV Cache 时，本轮输入张量长度可能只有 1：</p><pre class="language-text" data-language="text"><code class="language-text">x_new.shape = [B, 1, C]</code></pre><p>但这个 <code>1</code> 是本轮 token 数，不代表它的绝对位置为 0。</p><p>正确位置应该是：</p><pre class="language-python" data-language="python"><code class="language-python">position_id <span class="token operator">=</span> cache_len</code></pre><p>否则每个新 token 都会使用相同旋转角度，RoPE 的位置关系就被破坏。</p><h2 id="20-Padding、左填充与-position-ids"><a href="#20-Padding、左填充与-position-ids" class="headerlink" title="20. Padding、左填充与 position_ids"></a>20. Padding、左填充与 <code>position_ids</code></h2><p>不带 Padding 的 batch 可以直接使用：</p><pre class="language-python" data-language="python"><code class="language-python">position_ids <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span>T<span class="token punctuation">,</span> device<span class="token operator">=</span>x<span class="token punctuation">.</span>device<span class="token punctuation">)</span>position_ids <span class="token operator">=</span> position_ids<span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">.</span>expand<span class="token punctuation">(</span>B<span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>但左填充时：</p><pre class="language-text" data-language="text"><code class="language-text">[PAD, PAD, 我, 爱, NLP][PAD, 今天, 天气, 很, 好]</code></pre><p>简单的 <code>[0,1,2,3,4]</code> 会让第一条样本的真实 token 从位置 2 开始。很多实现会根据 Attention Mask 为每条样本构造连续位置：</p><pre class="language-python" data-language="python"><code class="language-python">position_ids <span class="token operator">=</span> attention_mask<span class="token punctuation">.</span><span class="token builtin">long</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>cumsum<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span> <span class="token operator">-</span> <span class="token number">1</span>position_ids<span class="token punctuation">.</span>masked_fill_<span class="token punctuation">(</span>attention_mask <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span></code></pre><p>得到类似：</p><pre class="language-text" data-language="text"><code class="language-text">[0, 0, 0, 1, 2][0, 0, 1, 2, 3]</code></pre><p>PAD 位置最终仍要被 Padding Mask 遮住。<code>position_ids</code> 与 Attention Mask 解决的是两个不同问题：</p><ul><li><code>position_ids</code> 决定旋转角度。</li><li>Attention Mask 决定哪些 token 能参与注意力。</li></ul><h2 id="21-MHA、GQA-和-MQA-中怎样使用-RoPE"><a href="#21-MHA、GQA-和-MQA-中怎样使用-RoPE" class="headerlink" title="21. MHA、GQA 和 MQA 中怎样使用 RoPE"></a>21. MHA、GQA 和 MQA 中怎样使用 RoPE</h2><h3 id="21-1-MHA"><a href="#21-1-MHA" class="headerlink" title="21.1 MHA"></a>21.1 MHA</h3><p>Multi-Head Attention 中：</p><pre class="language-text" data-language="text"><code class="language-text">Q: [B, H, T, D]K: [B, H, T, D]V: [B, H, T, D]</code></pre><p>所有头共享相同位置频率表，但每个头拥有不同的 Q、K 内容，所以旋转后结果仍然不同。</p><h3 id="21-2-GQA"><a href="#21-2-GQA" class="headerlink" title="21.2 GQA"></a>21.2 GQA</h3><p>Grouped-Query Attention 中 Query 头数多于 Key&#x2F;Value 头数：</p><pre class="language-text" data-language="text"><code class="language-text">Q: [B, H_q,  T, D]K: [B, H_kv, T, D]V: [B, H_kv, T, D]</code></pre><p>只要 Q 和 K 的 <code>head_dim</code> 相同，同一组 <code>[B,1,T,D]</code> 的 cos、sin 就可以分别广播到 $H_q$ 和 $H_{kv}$。</p><h3 id="21-3-MQA"><a href="#21-3-MQA" class="headerlink" title="21.3 MQA"></a>21.3 MQA</h3><p>Multi-Query Attention 只有一个 K&#x2F;V 头，也仍然可以对：</p><pre class="language-text" data-language="text"><code class="language-text">K: [B, 1, T, D]</code></pre><p>正常应用 RoPE。RoPE 与 MHA、GQA、MQA 描述的是不同维度：</p><ul><li>RoPE 负责位置关系。</li><li>MHA&#x2F;GQA&#x2F;MQA 负责 Query 与 KV 头的组织方式。</li></ul><h2 id="22-Full-RoPE-与-Partial-RoPE"><a href="#22-Full-RoPE-与-Partial-RoPE" class="headerlink" title="22. Full RoPE 与 Partial RoPE"></a>22. Full RoPE 与 Partial RoPE</h2><p>完整 RoPE 会旋转每个头的全部维度：</p><pre class="language-text" data-language="text"><code class="language-text">rotary_dim = head_dim</code></pre><p>Partial RoPE 只旋转前一部分：</p><pre class="language-python" data-language="python"><code class="language-python">q_rot<span class="token punctuation">,</span> q_pass <span class="token operator">=</span> q<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> <span class="token punctuation">:</span>rotary_dim<span class="token punctuation">]</span><span class="token punctuation">,</span> q<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> rotary_dim<span class="token punctuation">:</span><span class="token punctuation">]</span>k_rot<span class="token punctuation">,</span> k_pass <span class="token operator">=</span> k<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> <span class="token punctuation">:</span>rotary_dim<span class="token punctuation">]</span><span class="token punctuation">,</span> k<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> rotary_dim<span class="token punctuation">:</span><span class="token punctuation">]</span>q_rot<span class="token punctuation">,</span> k_rot <span class="token operator">=</span> apply_rotary<span class="token punctuation">(</span>q_rot<span class="token punctuation">,</span> k_rot<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">)</span>q <span class="token operator">=</span> torch<span class="token punctuation">.</span>cat<span class="token punctuation">(</span><span class="token punctuation">[</span>q_rot<span class="token punctuation">,</span> q_pass<span class="token punctuation">]</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>k <span class="token operator">=</span> torch<span class="token punctuation">.</span>cat<span class="token punctuation">(</span><span class="token punctuation">[</span>k_rot<span class="token punctuation">,</span> k_pass<span class="token punctuation">]</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>此时：</p><ul><li><code>rotary_dim</code> 必须是偶数。</li><li>cos、sin 必须按照 <code>rotary_dim</code> 生成。</li><li>剩余维度完全保留原值。</li></ul><p>Full RoPE 与 Partial RoPE 都是合理设计，但必须与模型训练配置和已有权重保持一致。</p><h2 id="23-两种常见的维度配对约定"><a href="#23-两种常见的维度配对约定" class="headerlink" title="23. 两种常见的维度配对约定"></a>23. 两种常见的维度配对约定</h2><p>RoPE 代码里经常出现两种组织方法。</p><h3 id="23-1-相邻配对"><a href="#23-1-相邻配对" class="headerlink" title="23.1 相邻配对"></a>23.1 相邻配对</h3><pre class="language-text" data-language="text"><code class="language-text">(x0, x1), (x2, x3), (x4, x5), ...</code></pre><p>本文实数版和复数版采用这种方式。</p><h3 id="23-2-前后半区配对"><a href="#23-2-前后半区配对" class="headerlink" title="23.2 前后半区配对"></a>23.2 前后半区配对</h3><p>另一种实现会把向量拆成两半：</p><pre class="language-python" data-language="python"><code class="language-python">x1 <span class="token operator">=</span> x<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> <span class="token punctuation">:</span>D <span class="token operator">//</span> <span class="token number">2</span><span class="token punctuation">]</span>x2 <span class="token operator">=</span> x<span class="token punctuation">[</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">,</span> D <span class="token operator">//</span> <span class="token number">2</span><span class="token punctuation">:</span><span class="token punctuation">]</span>rotated <span class="token operator">=</span> torch<span class="token punctuation">.</span>cat<span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token operator">-</span>x2<span class="token punctuation">,</span> x1<span class="token punctuation">)</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>只要频率排列与权重训练方式配套，两种约定都能实现旋转思想。</p><p>但是：</p><blockquote><p>不能只替换 <code>rotate_half</code>，却继续沿用不匹配的 cos、sin 排列或预训练权重。</p></blockquote><p>从零训练时可以选择一种并保持一致；加载已有模型时必须严格遵循该模型的实现约定。</p><h2 id="24-RoPE-与其他位置方法的比较"><a href="#24-RoPE-与其他位置方法的比较" class="headerlink" title="24. RoPE 与其他位置方法的比较"></a>24. RoPE 与其他位置方法的比较</h2><table><thead><tr><th>方法</th><th>注入方式</th><th align="right">可训练位置参数</th><th>相对位置如何体现</th><th>KV Cache 兼容性</th></tr></thead><tbody><tr><td>Learned Absolute</td><td>输入加位置向量</td><td align="right">有</td><td>模型间接学习</td><td>简单</td></tr><tr><td>Sinusoidal</td><td>输入加固定向量</td><td align="right">无</td><td>可由三角关系间接推导</td><td>简单</td></tr><tr><td>Relative Bias</td><td>Attention logits 加偏置</td><td align="right">可有可无</td><td>直接按距离加分</td><td>通常良好</td></tr><tr><td>ALiBi</td><td>logits 加距离线性偏置</td><td align="right">通常无</td><td>直接惩罚距离</td><td>良好</td></tr><tr><td>RoPE</td><td>旋转 Q、K</td><td align="right">无</td><td>QK 点积显式依赖相对相位</td><td>良好</td></tr></tbody></table><p>RoPE 的优势包括：</p><ul><li>不需要学习一张位置 embedding 表。</li><li>不改变 Q、K 的形状。</li><li>直接作用于 Attention 匹配分数。</li><li>与多头注意力和 KV Cache 容易结合。</li><li>可以通过频率缩放方法扩展上下文。</li></ul><p>它也不是没有限制：</p><ul><li>三角函数具有周期性。</li><li>超出训练长度后，模型未必理解陌生的相位分布。</li><li>不同实现的维度配对、缩放与位置约定可能不同。</li><li>长上下文能力不能只由“能算出 cos、sin”来判断。</li></ul><h2 id="25-RoPE-能无限外推吗"><a href="#25-RoPE-能无限外推吗" class="headerlink" title="25. RoPE 能无限外推吗"></a>25. RoPE 能无限外推吗</h2><p>不能。</p><p>数学上，只要给出更大的位置 $m$，当然仍然能计算：</p><p>$$<br>\cos(m\theta_j),\qquad\sin(m\theta_j)<br>$$</p><p>但“程序可以计算”不等于“模型在该长度上训练过，也不等于模型能正确使用”。</p><p>当位置远超训练范围时，可能出现：</p><ul><li>高频维度经历大量周期，产生训练中没见过的相位组合。</li><li>注意力分布发生异常变化。</li><li>短上下文能力与长上下文能力相互影响。</li><li>模型能够接收长输入，却无法准确检索或推理。</li></ul><p>因此需要把三个概念分开：</p><pre class="language-text" data-language="text"><code class="language-text">张量长度支持：代码和显存能否容纳位置编码支持：是否能为该位置计算信号有效上下文能力：模型能否可靠利用远处信息</code></pre><h3 id="25-1-Position-Interpolation"><a href="#25-1-Position-Interpolation" class="headerlink" title="25.1 Position Interpolation"></a>25.1 Position Interpolation</h3><p>假设原训练长度为 $L$，目标长度为 $L’$，其中 $L’&gt;L$。</p><p>位置插值的基本思想是把新位置压缩回原范围：</p><p>$$<br>m’&#x3D;m\frac{L}{L’}<br>$$</p><p>然后使用 $m’$ 计算 RoPE 角度。这样不直接把模型推到远超训练范围的相位，但位置会变得更密集，通常仍需要继续训练或微调。</p><h3 id="25-2-其他缩放方法"><a href="#25-2-其他缩放方法" class="headerlink" title="25.2 其他缩放方法"></a>25.2 其他缩放方法</h3><p>工程中还能看到 NTK-aware scaling、Dynamic NTK、YaRN 等方法。它们会以不同方式调整位置、频率或 Attention 缩放。</p><p>这些方法不是“随便改 base”的同义词。加载具体模型时应使用模型配置声明的 RoPE 类型和参数，不要凭名称猜测。</p><h2 id="26-计算量与参数量"><a href="#26-计算量与参数量" class="headerlink" title="26. 计算量与参数量"></a>26. 计算量与参数量</h2><p>RoPE 本身没有可训练参数。它增加的主要操作是：</p><ul><li>生成或查询 cos、sin。</li><li>对 Q、K 做逐元素乘法和加法。</li></ul><p>对于 Q、K 张量，额外计算量大致与：</p><p>$$<br>O(BHTD)<br>$$</p><p>同阶，而标准 Attention 分数矩阵的计算量为：</p><p>$$<br>O(BHT^2D)<br>$$</p><p>因此在长序列下，RoPE 通常不是 Attention 的主要复杂度来源。</p><p>如果预计算表，内存大致为：</p><p>$$<br>O(TD)<br>$$</p><p>如果动态计算，则可以减少固定缓存，但每次前向都要重新生成所需角度。实际系统会结合编译、缓存与上下文长度选择策略。</p><h2 id="27-常见错误"><a href="#27-常见错误" class="headerlink" title="27. 常见错误"></a>27. 常见错误</h2><h3 id="27-1-把-RoPE-加到-token-embedding-上"><a href="#27-1-把-RoPE-加到-token-embedding-上" class="headerlink" title="27.1 把 RoPE 加到 token embedding 上"></a>27.1 把 RoPE 加到 token embedding 上</h3><p>标准用法是在线性投影之后旋转 Q 和 K，不是把旋转结果当作一张位置向量与输入相加。</p><h3 id="27-2-旋转了-V，却忘记旋转-K"><a href="#27-2-旋转了-V，却忘记旋转-K" class="headerlink" title="27.2 旋转了 V，却忘记旋转 K"></a>27.2 旋转了 V，却忘记旋转 K</h3><p>RoPE 的相对位置性质来自 Q、K 点积。只旋转 V 无法得到同样的推导。</p><h3 id="27-3-在计算-QK-分数后才应用-RoPE"><a href="#27-3-在计算-QK-分数后才应用-RoPE" class="headerlink" title="27.3 在计算 QK 分数后才应用 RoPE"></a>27.3 在计算 QK 分数后才应用 RoPE</h3><p>必须先旋转 Q、K，再做点积：</p><pre class="language-python" data-language="python"><code class="language-python">q<span class="token punctuation">,</span> k <span class="token operator">=</span> apply_rotary<span class="token punctuation">(</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> cos<span class="token punctuation">,</span> sin<span class="token punctuation">)</span>scores <span class="token operator">=</span> q @ k<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><h3 id="27-4-head-dim-为奇数"><a href="#27-4-head-dim-为奇数" class="headerlink" title="27.4 head_dim 为奇数"></a>27.4 <code>head_dim</code> 为奇数</h3><p>完整两两配对会剩下一个坐标。应选择偶数 <code>head_dim</code>，或明确实现偶数大小的 Partial RoPE。</p><h3 id="27-5-Softmax-维度错误"><a href="#27-5-Softmax-维度错误" class="headerlink" title="27.5 Softmax 维度错误"></a>27.5 Softmax 维度错误</h3><p>RoPE 不改变 Softmax 规则。Attention 仍然沿 Key 维归一化：</p><pre class="language-python" data-language="python"><code class="language-python">weights <span class="token operator">=</span> torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>scores<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><h3 id="27-6-cos、sin-的形状无法正确广播"><a href="#27-6-cos、sin-的形状无法正确广播" class="headerlink" title="27.6 cos、sin 的形状无法正确广播"></a>27.6 cos、sin 的形状无法正确广播</h3><p>若 Q 为：</p><pre class="language-text" data-language="text"><code class="language-text">[B, H, T, D]</code></pre><p>则 cos、sin 常整理成：</p><pre class="language-text" data-language="text"><code class="language-text">[B, 1, T, D]</code></pre><p>不要把序列维与头维放反。</p><h3 id="27-7-位置-0-出现变化"><a href="#27-7-位置-0-出现变化" class="headerlink" title="27.7 位置 0 出现变化"></a>27.7 位置 0 出现变化</h3><p>位置 0 应满足恒等旋转。如果不满足，通常说明：</p><ul><li>position offset 错误。</li><li>sin&#x2F;cos 排列错误。</li><li>二维配对方式错误。</li></ul><h3 id="27-8-KV-Cache-每轮从位置-0-开始"><a href="#27-8-KV-Cache-每轮从位置-0-开始" class="headerlink" title="27.8 KV Cache 每轮从位置 0 开始"></a>27.8 KV Cache 每轮从位置 0 开始</h3><p>新 token 的位置应为历史缓存长度，而不是本轮输入张量中的局部索引 0。</p><h3 id="27-9-左填充后所有样本共用简单-arange"><a href="#27-9-左填充后所有样本共用简单-arange" class="headerlink" title="27.9 左填充后所有样本共用简单 arange"></a>27.9 左填充后所有样本共用简单 <code>arange</code></h3><p>这可能让真实 token 的位置受 PAD 数量影响。应根据框架、训练方式和 Attention Mask 正确构造 <code>position_ids</code>。</p><h3 id="27-10-混用相邻配对和前后半区配对"><a href="#27-10-混用相邻配对和前后半区配对" class="headerlink" title="27.10 混用相邻配对和前后半区配对"></a>27.10 混用相邻配对和前后半区配对</h3><p>两种方式不能只替换一半。<code>rotate</code> 函数、cos&#x2F;sin 排列与预训练权重必须来自同一约定。</p><h3 id="27-11-把-base-当成最大上下文长度"><a href="#27-11-把-base-当成最大上下文长度" class="headerlink" title="27.11 把 base 当成最大上下文长度"></a>27.11 把 <code>base</code> 当成最大上下文长度</h3><p><code>base</code> 控制频率分布，不直接等于最大 token 数。</p><h3 id="27-12-只改缩放参数，不做长上下文评估"><a href="#27-12-只改缩放参数，不做长上下文评估" class="headerlink" title="27.12 只改缩放参数，不做长上下文评估"></a>27.12 只改缩放参数，不做长上下文评估</h3><p>至少要检查：</p><ul><li>原始短上下文困惑度是否退化。</li><li>长距离检索是否准确。</li><li>不同位置上的性能是否一致。</li><li>生成、代码与推理任务是否真正受益。</li></ul><h2 id="28-高频问题"><a href="#28-高频问题" class="headerlink" title="28. 高频问题"></a>28. 高频问题</h2><h3 id="28-1-RoPE-是位置编码还是位置嵌入"><a href="#28-1-RoPE-是位置编码还是位置嵌入" class="headerlink" title="28.1 RoPE 是位置编码还是位置嵌入"></a>28.1 RoPE 是位置编码还是位置嵌入</h3><p>中文资料中两种叫法都很常见。它没有一张通过梯度学习的位置向量表，因此从实现上更接近固定的位置编码机制；论文名称使用 Rotary Position Embedding。</p><h3 id="28-2-RoPE-有可训练参数吗"><a href="#28-2-RoPE-有可训练参数吗" class="headerlink" title="28.2 RoPE 有可训练参数吗"></a>28.2 RoPE 有可训练参数吗</h3><p>标准 RoPE 没有。<code>base</code>、频率和位置规则属于超参数或固定计算。</p><h3 id="28-3-每个注意力头的-RoPE-频率一样吗"><a href="#28-3-每个注意力头的-RoPE-频率一样吗" class="headerlink" title="28.3 每个注意力头的 RoPE 频率一样吗"></a>28.3 每个注意力头的 RoPE 频率一样吗</h3><p>通常相同。每个头共享频率规则，但 Q、K 内容不同，所以结果不会相同。</p><h3 id="28-4-每一层的角度一样吗"><a href="#28-4-每一层的角度一样吗" class="headerlink" title="28.4 每一层的角度一样吗"></a>28.4 每一层的角度一样吗</h3><p>同一位置、同一维度对应的基础角度通常一样，但每层 Q、K 投影结果不同，因此实际旋转后的向量不同。</p><h3 id="28-5-还需要绝对-Position-Embedding-吗"><a href="#28-5-还需要绝对-Position-Embedding-吗" class="headerlink" title="28.5 还需要绝对 Position Embedding 吗"></a>28.5 还需要绝对 Position Embedding 吗</h3><p>采用标准 RoPE 的 Decoder-only 模型通常不会再额外加入可学习绝对位置向量。但具体架构可以组合不同方法，最终应以模型定义为准。</p><h3 id="28-6-RoPE-能替代-Causal-Mask-吗"><a href="#28-6-RoPE-能替代-Causal-Mask-吗" class="headerlink" title="28.6 RoPE 能替代 Causal Mask 吗"></a>28.6 RoPE 能替代 Causal Mask 吗</h3><p>不能。RoPE 表示位置关系，Causal Mask 阻止访问未来，两者职责不同。</p><h3 id="28-7-RoPE-会改变-Attention-的形状吗"><a href="#28-7-RoPE-会改变-Attention-的形状吗" class="headerlink" title="28.7 RoPE 会改变 Attention 的形状吗"></a>28.7 RoPE 会改变 Attention 的形状吗</h3><p>不会：</p><pre class="language-text" data-language="text"><code class="language-text">旋转前 Q/K: [B, H, T, D]旋转后 Q/K: [B, H, T, D]</code></pre><h3 id="28-8-为什么叫“旋转”而不是普通逐元素缩放"><a href="#28-8-为什么叫“旋转”而不是普通逐元素缩放" class="headerlink" title="28.8 为什么叫“旋转”而不是普通逐元素缩放"></a>28.8 为什么叫“旋转”而不是普通逐元素缩放</h3><p>因为一对坐标同时使用 cos 和 sin 交叉混合：</p><p>$$<br>(x_1,x_2)<br>\rightarrow<br>(x_1\cos\phi-x_2\sin\phi,<br>x_1\sin\phi+x_2\cos\phi)<br>$$</p><p>这是真正的二维正交旋转，不是两个坐标各自乘一个系数。</p><h2 id="29-从-Prompt-到下一个-token：RoPE-参与了哪一步"><a href="#29-从-Prompt-到下一个-token：RoPE-参与了哪一步" class="headerlink" title="29. 从 Prompt 到下一个 token：RoPE 参与了哪一步"></a>29. 从 Prompt 到下一个 token：RoPE 参与了哪一步</h2><p>承接前一篇 Mini-GPT，假设输入 Prompt：</p><pre class="language-text" data-language="text"><code class="language-text">春 天</code></pre><p>Tokenizer 得到：</p><pre class="language-text" data-language="text"><code class="language-text">[id_春, id_天]</code></pre><p>带 RoPE 的完整推理流程是：</p><pre class="language-text" data-language="text"><code class="language-text">1. Token Embedding   [B, T] -> [B, T, C]2. 进入第 1 个 Transformer Block   LayerNorm -> Q/K/V 投影3. 拆头   Q/K/V -> [B, H, T, D]4. 生成 position_ids   [0, 1]5. 应用 RoPE   位置 0 的 Q/K 不旋转   位置 1 的 Q/K 按多组频率旋转6. Causal Attention   “天”的 Query 可以与“春”“天”的旋转后 Key 匹配7. Attention 权重乘 V   聚合历史内容8. MLP、残差与后续 Block9. Final Norm + LM Head   得到最后位置的词表 logits10. Softmax 与采样    例如预测“来”</code></pre><p>RoPE 不会直接输出“来”。它的作用是改变每层 Attention 中的匹配分数，让模型在聚合“春”“天”等历史信息时能够利用相对位置。</p><p>当“来”被接回上下文：</p><pre class="language-text" data-language="text"><code class="language-text">春 天 来</code></pre><p>下一轮的位置为 <code>[0,1,2]</code>。如果使用 KV Cache，只需要为位置 2 的新 token 计算并旋转新的 Q、K，再与历史缓存交互。</p><h2 id="30-总结"><a href="#30-总结" class="headerlink" title="30. 总结"></a>30. 总结</h2><p>RoPE 的完整主线可以压缩成：</p><pre class="language-text" data-language="text"><code class="language-text">hidden state  -> Q、K、V 投影  -> 按位置和多组频率生成角度  -> 把 Q、K 的最后一维两两配对  -> 每对执行二维旋转  -> 旋转后的 QK 点积只显式依赖相对位置差  -> Causal Mask + Softmax  -> 对未旋转的 V 加权求和</code></pre><p>需要真正记住的核心包括：</p><ul><li>RoPE 旋转的是 Attention 中的 Q 和 K，不是把位置向量加到 token embedding。</li><li>二维旋转保持向量范数，并把位置编码成相位。</li><li>$R_m^TR_n&#x3D;R_{n-m}$ 让绝对位置旋转在点积中表现为相对距离。</li><li>高维向量被拆成多个二维对，每一对使用不同频率。</li><li>高频更敏感于局部变化，低频负责更长尺度的位置变化。</li><li>V 通常不旋转，因为位置已经进入 QK 匹配权重。</li><li>RoPE 不替代 Causal Mask，也不自动解决 Attention 的平方复杂度。</li><li>KV Cache 中应缓存已经按绝对位置旋转的 Key，新 token 必须使用正确的位置偏移。</li><li>能计算超长位置的 cos、sin，不代表模型能可靠利用任意长上下文。</li></ul><p>最后，用一条公式概括 RoPE：</p><p>$$<br>\boxed{<br>(R_mq_m)^T(R_nk_n)<br>&#x3D;q_m^TR_{n-m}k_n<br>}<br>$$</p><p>左边使用两个 token 的绝对位置进行旋转，右边只剩二者的相对距离。这就是 RoPE 从“旋转”走向“相对位置”的关键桥梁。</p><p>完整示例代码：<code>source/code/nlp-blog/rope.py</code></p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>Jianlin Su et al., <em>RoFormer: Enhanced Transformer with Rotary Position Embedding</em>: <a href="https://arxiv.org/abs/2104.09864">https://arxiv.org/abs/2104.09864</a></li><li>Ashish Vaswani et al., <em>Attention Is All You Need</em>: <a href="https://arxiv.org/abs/1706.03762">https://arxiv.org/abs/1706.03762</a></li><li>Hugo Touvron et al., <em>LLaMA: Open and Efficient Foundation Language Models</em>: <a href="https://arxiv.org/abs/2302.13971">https://arxiv.org/abs/2302.13971</a></li><li>Meta Llama, <code>apply_rotary_emb</code> reference implementation: <a href="https://github.com/meta-llama/llama/blob/main/llama/model.py">https://github.com/meta-llama/llama/blob/main/llama/model.py</a></li><li>PyTorch Documentation, <code>scaled_dot_product_attention</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.functional.scaled_dot_product_attention.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.functional.scaled_dot_product_attention.html</a></li><li>Shouyuan Chen et al., <em>Extending Context Window of Large Language Models via Positional Interpolation</em>: <a href="https://arxiv.org/abs/2306.15595">https://arxiv.org/abs/2306.15595</a></li><li>Bowen Peng et al., <em>YaRN: Efficient Context Window Extension of Large Language Models</em>: <a href="https://arxiv.org/abs/2309.00071">https://arxiv.org/abs/2309.00071</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/14/nlp-rope-rotary-position-embedding/</id>
    <link href="https://lsworl.github.io/2026/08/14/nlp-rope-rotary-position-embedding/"/>
    <published>2026-08-13T16:00:00.000Z</published>
    <summary>承接Transformer与Mini-GPT，从二维旋转、相对位置推导和手算例子出发，逐步理解RoPE的多频率设计、PyTorch实现、KV Cache、长度外推与常见错误。</summary>
    <title>RoPE详解：从二维旋转到PyTorch手写旋转位置编码</title>
    <updated>2026-08-15T05:41:48.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="GPT" scheme="https://lsworl.github.io/tags/GPT/"/>
    <category term="Mini-GPT" scheme="https://lsworl.github.io/tags/Mini-GPT/"/>
    <category term="Transformer" scheme="https://lsworl.github.io/tags/Transformer/"/>
    <category term="Decoder-only" scheme="https://lsworl.github.io/tags/Decoder-only/"/>
    <category term="Causal Self-Attention" scheme="https://lsworl.github.io/tags/Causal-Self-Attention/"/>
    <content>
      <![CDATA[<p>上一篇文章从 Encoder-Decoder 结构出发，完整介绍了 Transformer 的 Self-Attention、Multi-Head Attention、位置编码、Mask、Encoder 和 Decoder。</p><p>这一次，我们不再调用 <code>nn.Transformer</code>，而是只使用 PyTorch 的基础层，从零手写一个能够训练和生成文本的 Mini-GPT。</p><p>它会包含 GPT 最核心的组件：</p><ul><li>字符级 Tokenizer。</li><li>Token Embedding 和可学习 Position Embedding。</li><li>手写 Multi-Head Causal Self-Attention。</li><li>Pre-Norm Transformer Block。</li><li>GELU 前馈网络。</li><li>自回归语言模型损失。</li><li>AdamW、学习率 warmup、余弦衰减和梯度裁剪。</li><li>Temperature、Top-k 与多项式采样。</li><li>权重绑定和 KV Cache 原理。</li></ul><p>最终模型只有约 35 万个参数，可以在 CPU 上训练。它当然不是 ChatGPT，也不会拥有通用知识，但它足以把 GPT 内部最重要的数据流完整跑通。</p><p>一句话概括：</p><blockquote><p>GPT 接收一段 token 前缀，通过带因果遮罩的 Transformer Block 预测每个位置的下一个 token，再把新 token 接回输入继续生成。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-overview.svg" alt="Mini-GPT整体结构"></p><h2 id="1-GPT-到底是什么"><a href="#1-GPT-到底是什么" class="headerlink" title="1. GPT 到底是什么"></a>1. GPT 到底是什么</h2><p>GPT 是 Generative Pre-trained Transformer 的缩写：</p><ul><li><strong>Generative</strong>：通过预测和生成后续 token 建模文本。</li><li><strong>Pre-trained</strong>：先在大量文本上进行语言模型预训练。</li><li><strong>Transformer</strong>：核心网络使用 Transformer 架构。</li></ul><p>本篇文章主要实现其中的“Generative Transformer”。因为我们只使用一小段教学语料，所以更准确地说，它是一个从零训练的 Mini-GPT，而不是经过大规模预训练的通用模型。</p><h3 id="1-1-GPT-与上一篇完整-Transformer-的关系"><a href="#1-1-GPT-与上一篇完整-Transformer-的关系" class="headerlink" title="1.1 GPT 与上一篇完整 Transformer 的关系"></a>1.1 GPT 与上一篇完整 Transformer 的关系</h3><p>原始 Transformer：</p><pre class="language-text" data-language="text"><code class="language-text">Encoder  -> 产生 Memory  -> Decoder 通过 Cross-Attention 读取 Memory</code></pre><p>GPT：</p><pre class="language-text" data-language="text"><code class="language-text">只有 Decoder-only Block  -> Causal Self-Attention  -> 不需要 Encoder  -> 不需要 Cross-Attention</code></pre><p>这里有一个容易误解的地方：</p><blockquote><p>GPT 的“Decoder-only”并不是把原始 Transformer Decoder 原封不动地拿出来。</p></blockquote><p>原始 Decoder 每层有三个子层：</p><ol><li>Masked Self-Attention。</li><li>与 Encoder 交互的 Cross-Attention。</li><li>FFN。</li></ol><p>GPT 没有 Encoder，因此删除 Cross-Attention，只保留：</p><ol><li>Causal Self-Attention。</li><li>FFN。</li></ol><h2 id="2-GPT-的学习目标：预测下一个-token"><a href="#2-GPT-的学习目标：预测下一个-token" class="headerlink" title="2. GPT 的学习目标：预测下一个 token"></a>2. GPT 的学习目标：预测下一个 token</h2><p>给定 token 序列：</p><p>$$<br>x_1,x_2,\ldots,x_T<br>$$</p><p>自回归语言模型把整段序列的联合概率分解为：</p><p>$$<br>P(x_1,x_2,\ldots,x_T)<br>&#x3D;\prod_{t&#x3D;1}^{T}P(x_t\mid x_1,\ldots,x_{t-1})<br>$$</p><p>也就是：</p><pre class="language-text" data-language="text"><code class="language-text">看到前文                  预测&lt;开始>                    第 1 个 tokenx1                        x2x1 x2                     x3x1 x2 x3                  x4...x1 x2 ... x(T-1)          xT</code></pre><p>GPT 不需要人为提供“主题”“语法规则”或“下一词类别”。训练文本自身就能构造监督信号。</p><p>例如文本是：</p><pre class="language-text" data-language="text"><code class="language-text">语言模型</code></pre><p>可以得到：</p><pre class="language-text" data-language="text"><code class="language-text">输入 x：[语, 言, 模]目标 y：[言, 模, 型]</code></pre><p>输入和目标只错开一个位置。</p><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-training-shift.svg" alt="语言模型输入与目标错位"></p><h2 id="3-为什么一次前向传播能训练多个位置"><a href="#3-为什么一次前向传播能训练多个位置" class="headerlink" title="3. 为什么一次前向传播能训练多个位置"></a>3. 为什么一次前向传播能训练多个位置</h2><p>训练时，模型一次接收：</p><pre class="language-text" data-language="text"><code class="language-text">x = [语, 言, 模]</code></pre><p>并同时输出三个位置的词表 logits：</p><pre class="language-text" data-language="text"><code class="language-text">位置 0：根据“语”预测“言”位置 1：根据“语言”预测“模”位置 2：根据“语言模”预测“型”</code></pre><p>因果遮罩保证位置 0 看不到位置 1 和 2，位置 1 看不到位置 2。因此，虽然整个矩阵并行计算，每个位置仍然只使用合法的历史信息。</p><p>如果：</p><ul><li>batch size 为 $B$。</li><li>上下文长度为 $T$。</li><li>词表大小为 $V$。</li></ul><p>那么：</p><pre class="language-text" data-language="text"><code class="language-text">输入 idx： [B, T]目标 y：   [B, T]logits：   [B, T, V]</code></pre><p>一次前向传播实际上产生了 $B\times T$ 个 next-token 训练样本。</p><h2 id="4-Tokenizer：模型如何看见文字"><a href="#4-Tokenizer：模型如何看见文字" class="headerlink" title="4. Tokenizer：模型如何看见文字"></a>4. Tokenizer：模型如何看见文字</h2><p>神经网络不能直接接收字符串。Tokenizer 需要建立：</p><pre class="language-text" data-language="text"><code class="language-text">文字 -> token -> token id</code></pre><p>真实 GPT 通常使用子词或字节级 Tokenizer。为了把注意力放在模型结构上，这里使用字符级 Tokenizer。</p><h3 id="4-1-构造字符词表"><a href="#4-1-构造字符词表" class="headerlink" title="4.1 构造字符词表"></a>4.1 构造字符词表</h3><pre class="language-python" data-language="python"><code class="language-python">raw_text <span class="token operator">=</span> <span class="token triple-quoted-string string">"""春天来了，花儿在风中开放。夏天来了，蝉儿在树上歌唱。秋天来了，果实在枝头成熟。冬天来了，雪花在天空飞舞。清晨，太阳从东方升起。傍晚，月亮在云间出现。小河穿过安静的村庄。微风吹过绿色的田野。我们读书，也观察世界。我们思考，也记录问题。机器学习从数据中寻找规律。神经网络用参数表示知识。注意力让词语彼此交换信息。语言模型根据上文预测下一个字符。"""</span>text <span class="token operator">=</span> raw_text <span class="token operator">*</span> <span class="token number">120</span>chars <span class="token operator">=</span> <span class="token builtin">sorted</span><span class="token punctuation">(</span><span class="token builtin">set</span><span class="token punctuation">(</span>text<span class="token punctuation">)</span><span class="token punctuation">)</span>stoi <span class="token operator">=</span> <span class="token punctuation">&#123;</span>ch<span class="token punctuation">:</span> i <span class="token keyword">for</span> i<span class="token punctuation">,</span> ch <span class="token keyword">in</span> <span class="token builtin">enumerate</span><span class="token punctuation">(</span>chars<span class="token punctuation">)</span><span class="token punctuation">&#125;</span>itos <span class="token operator">=</span> <span class="token punctuation">&#123;</span>i<span class="token punctuation">:</span> ch <span class="token keyword">for</span> ch<span class="token punctuation">,</span> i <span class="token keyword">in</span> stoi<span class="token punctuation">.</span>items<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">&#125;</span></code></pre><ul><li><code>stoi</code> 表示 string to integer。</li><li><code>itos</code> 表示 integer to string。</li><li>本例实测词表大小为 123。</li></ul><h3 id="4-2-编码与解码"><a href="#4-2-编码与解码" class="headerlink" title="4.2 编码与解码"></a>4.2 编码与解码</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">encode</span><span class="token punctuation">(</span>s<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">return</span> <span class="token punctuation">[</span>stoi<span class="token punctuation">[</span>ch<span class="token punctuation">]</span> <span class="token keyword">for</span> ch <span class="token keyword">in</span> s<span class="token punctuation">]</span><span class="token keyword">def</span> <span class="token function">decode</span><span class="token punctuation">(</span>ids<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">return</span> <span class="token string">""</span><span class="token punctuation">.</span>join<span class="token punctuation">(</span>itos<span class="token punctuation">[</span>i<span class="token punctuation">]</span> <span class="token keyword">for</span> i <span class="token keyword">in</span> ids<span class="token punctuation">)</span></code></pre><p>测试：</p><pre class="language-python" data-language="python"><code class="language-python">ids <span class="token operator">=</span> encode<span class="token punctuation">(</span><span class="token string">"春天"</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>ids<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>decode<span class="token punctuation">(</span>ids<span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>字符级 Tokenizer 的优点：</p><ul><li>代码简单。</li><li>词表小。</li><li>不容易遇到未知字符拆分问题。</li></ul><p>缺点：</p><ul><li>同样一句话需要更多 token。</li><li>单个 token 的语义较弱。</li><li>序列变长，Attention 成本上升。</li></ul><p>Mini-GPT 使用字符级分词是为了教学，不代表它是实际大模型的最佳方案。</p><h2 id="5-把连续文本切成训练窗口"><a href="#5-把连续文本切成训练窗口" class="headerlink" title="5. 把连续文本切成训练窗口"></a>5. 把连续文本切成训练窗口</h2><p>先把完整文本编码成一维张量：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torchdata <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span>encode<span class="token punctuation">(</span>text<span class="token punctuation">)</span><span class="token punctuation">,</span> dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">long</span><span class="token punctuation">)</span>n <span class="token operator">=</span> <span class="token builtin">int</span><span class="token punctuation">(</span><span class="token number">0.9</span> <span class="token operator">*</span> <span class="token builtin">len</span><span class="token punctuation">(</span>data<span class="token punctuation">)</span><span class="token punctuation">)</span>train_data <span class="token operator">=</span> data<span class="token punctuation">[</span><span class="token punctuation">:</span>n<span class="token punctuation">]</span>val_data <span class="token operator">=</span> data<span class="token punctuation">[</span>n<span class="token punctuation">:</span><span class="token punctuation">]</span></code></pre><p>这里使用前 90% 训练，后 10% 验证。</p><p>定义最大上下文长度：</p><pre class="language-python" data-language="python"><code class="language-python">block_size <span class="token operator">=</span> <span class="token number">64</span></code></pre><p>从随机起点取 65 个连续 token：</p><pre class="language-text" data-language="text"><code class="language-text">t0 t1 t2 ... t63 t64</code></pre><p>前 64 个作为输入，后 64 个作为目标：</p><pre class="language-text" data-language="text"><code class="language-text">x = t0 t1 t2 ... t63y = t1 t2 t3 ... t64</code></pre><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">get_batch</span><span class="token punctuation">(</span>split<span class="token punctuation">,</span>block_size<span class="token punctuation">,</span> batch_size<span class="token operator">=</span><span class="token number">32</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    source <span class="token operator">=</span> train_data <span class="token keyword">if</span> split <span class="token operator">==</span> <span class="token string">"train"</span> <span class="token keyword">else</span> val_data    starts <span class="token operator">=</span> torch<span class="token punctuation">.</span>randint<span class="token punctuation">(</span>        <span class="token builtin">len</span><span class="token punctuation">(</span>source<span class="token punctuation">)</span> <span class="token operator">-</span> block_size <span class="token operator">-</span> <span class="token number">1</span><span class="token punctuation">,</span>        <span class="token punctuation">(</span>batch_size<span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span>    x <span class="token operator">=</span> torch<span class="token punctuation">.</span>stack<span class="token punctuation">(</span><span class="token punctuation">[</span>        source<span class="token punctuation">[</span>i<span class="token punctuation">:</span>i <span class="token operator">+</span> block_size<span class="token punctuation">]</span>        <span class="token keyword">for</span> i <span class="token keyword">in</span> starts    <span class="token punctuation">]</span><span class="token punctuation">)</span>    y <span class="token operator">=</span> torch<span class="token punctuation">.</span>stack<span class="token punctuation">(</span><span class="token punctuation">[</span>        source<span class="token punctuation">[</span>i <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">:</span>i <span class="token operator">+</span> block_size <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">]</span>        <span class="token keyword">for</span> i <span class="token keyword">in</span> starts    <span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token comment">#</span>    <span class="token keyword">return</span> x<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span><span class="token punctuation">,</span> y<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span></code></pre><h3 id="5-1-block-size-是什么"><a href="#5-1-block-size-是什么" class="headerlink" title="5.1 block_size 是什么"></a>5.1 <code>block_size</code> 是什么</h3><p><code>block_size</code> 是模型一次最多能使用的上下文 token 数，也称 context length 或 context window。</p><p>本例中：</p><pre class="language-text" data-language="text"><code class="language-text">block_size = 64</code></pre><p>表示模型预测下一个字符时，最多参考最近 64 个字符。</p><p>它不是 batch size：</p><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><code>batch_size</code></td><td>一次并行训练多少段文本</td></tr><tr><td><code>block_size</code></td><td>每段文本包含多少个 token</td></tr><tr><td><code>vocab_size</code></td><td>可以预测多少种 token</td></tr><tr><td><code>n_embd</code></td><td>每个 token 的向量维度</td></tr></tbody></table><h2 id="6-Mini-GPT-配置"><a href="#6-Mini-GPT-配置" class="headerlink" title="6. Mini-GPT 配置"></a>6. Mini-GPT 配置</h2><p>使用 <code>dataclass</code> 集中管理结构参数：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> dataclasses <span class="token keyword">import</span> dataclass<span class="token decorator annotation punctuation">@dataclass</span><span class="token keyword">class</span> <span class="token class-name">GPTConfig</span><span class="token punctuation">:</span>    vocab_size<span class="token punctuation">:</span> <span class="token builtin">int</span>    block_size<span class="token punctuation">:</span> <span class="token builtin">int</span> <span class="token operator">=</span> <span class="token number">64</span>    n_layer<span class="token punctuation">:</span> <span class="token builtin">int</span> <span class="token operator">=</span> <span class="token number">3</span>    n_head<span class="token punctuation">:</span> <span class="token builtin">int</span> <span class="token operator">=</span> <span class="token number">4</span>    n_embd<span class="token punctuation">:</span> <span class="token builtin">int</span> <span class="token operator">=</span> <span class="token number">96</span>    dropout<span class="token punctuation">:</span> <span class="token builtin">float</span> <span class="token operator">=</span> <span class="token number">0.1</span></code></pre><p>这些参数分别控制：</p><table><thead><tr><th>参数</th><th>作用</th><th>本例</th></tr></thead><tbody><tr><td><code>vocab_size</code></td><td>输出类别数</td><td>123</td></tr><tr><td><code>block_size</code></td><td>最大上下文长度</td><td>64</td></tr><tr><td><code>n_layer</code></td><td>GPT Block 数量</td><td>3</td></tr><tr><td><code>n_head</code></td><td>每层注意力头数</td><td>4</td></tr><tr><td><code>n_embd</code></td><td>隐藏维度</td><td>96</td></tr><tr><td><code>dropout</code></td><td>Dropout 概率</td><td>0.1</td></tr></tbody></table><p>每个头的维度为：</p><p>$$<br>d_{head}&#x3D;\frac{n_{embd}}{n_{head}}<br>&#x3D;\frac{96}{4}&#x3D;24<br>$$</p><p>因此 <code>n_embd</code> 必须能被 <code>n_head</code> 整除。</p><h2 id="7-Token-Embedding-与-Position-Embedding"><a href="#7-Token-Embedding-与-Position-Embedding" class="headerlink" title="7. Token Embedding 与 Position Embedding"></a>7. Token Embedding 与 Position Embedding</h2><p>输入 <code>idx</code> 只是整数编号：</p><pre class="language-text" data-language="text"><code class="language-text">[B, T]</code></pre><p>Token Embedding 把每个编号查表转换成向量：</p><p>$$<br>E_{token}&#x3D;W_{te}[idx]<br>$$</p><pre class="language-python" data-language="python"><code class="language-python">self<span class="token punctuation">.</span>token_embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>    config<span class="token punctuation">.</span>vocab_size<span class="token punctuation">,</span>    config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>输出形状：</p><pre class="language-text" data-language="text"><code class="language-text">[B, T, C]</code></pre><p>其中 $C&#x3D;n_{embd}$。</p><h3 id="7-1-为什么还需要位置向量"><a href="#7-1-为什么还需要位置向量" class="headerlink" title="7.1 为什么还需要位置向量"></a>7.1 为什么还需要位置向量</h3><p>Causal Mask 只能限制“能看哪些位置”，并不能单独提供每个位置的完整顺序表示。因此还要加入 Position Embedding：</p><pre class="language-python" data-language="python"><code class="language-python">self<span class="token punctuation">.</span>position_embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>    config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>    config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><pre class="language-python" data-language="python"><code class="language-python">pos <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span>t<span class="token punctuation">,</span> device<span class="token operator">=</span>idx<span class="token punctuation">.</span>device<span class="token punctuation">)</span>token_emb <span class="token operator">=</span> self<span class="token punctuation">.</span>token_embedding<span class="token punctuation">(</span>idx<span class="token punctuation">)</span>      <span class="token comment"># [B, T, C]</span>position_emb <span class="token operator">=</span> self<span class="token punctuation">.</span>position_embedding<span class="token punctuation">(</span>pos<span class="token punctuation">)</span> <span class="token comment"># [T, C]</span>x <span class="token operator">=</span> token_emb <span class="token operator">+</span> position_emb               <span class="token comment"># [B, T, C]</span></code></pre><p>相加时 <code>[T,C]</code> 会在 batch 维广播成 <code>[B,T,C]</code>。</p><p>数学上：</p><p>$$<br>x_{b,t}&#x3D;E_{token}(idx_{b,t})+E_{position}(t)<br>$$</p><p>上一篇文章使用了固定正弦位置编码。本篇采用 GPT 风格的可学习绝对位置向量，它们会与模型其他参数一起通过反向传播更新。</p><h2 id="8-GPT-的因果自注意力"><a href="#8-GPT-的因果自注意力" class="headerlink" title="8. GPT 的因果自注意力"></a>8. GPT 的因果自注意力</h2><p>上一篇文章已经介绍了普通 Self-Attention：</p><p>$$<br>\operatorname{Attention}(Q,K,V)<br>&#x3D;\operatorname{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V<br>$$</p><p>GPT 需要额外加入 Causal Mask：</p><p>$$<br>\operatorname{CausalAttention}(Q,K,V)<br>&#x3D;\operatorname{softmax}\left(<br>\frac{QK^T}{\sqrt{d_k}}+M<br>\right)V<br>$$</p><p>其中：</p><p>$$<br>M_{ij}&#x3D;<br>\begin{cases}<br>0,&amp;j\le i\<br>-\infty,&amp;j&gt;i<br>\end{cases}<br>$$</p><p>含义是第 $i$ 个 Query：</p><ul><li>可以查看自己和过去位置。</li><li>不能查看未来位置。</li></ul><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-causal-attention.svg" alt="GPT因果注意力计算"></p><h2 id="9-手写多头因果自注意力"><a href="#9-手写多头因果自注意力" class="headerlink" title="9. 手写多头因果自注意力"></a>9. 手写多头因果自注意力</h2><p>下面不使用 <code>nn.MultiheadAttention</code>，完整写出 QKV 投影、拆头、遮罩、Softmax、加权和合并头。</p><h3 id="9-1-初始化层和-Mask"><a href="#9-1-初始化层和-Mask" class="headerlink" title="9.1 初始化层和 Mask"></a>9.1 初始化层和 Mask</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> math<span class="token keyword">import</span> torch<span class="token keyword">import</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>functional <span class="token keyword">as</span> F<span class="token keyword">from</span> torch <span class="token keyword">import</span> nn<span class="token keyword">class</span> <span class="token class-name">CausalSelfAttention</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> config<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        <span class="token keyword">if</span> config<span class="token punctuation">.</span>n_embd <span class="token operator">%</span> config<span class="token punctuation">.</span>n_head <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">:</span>            <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span><span class="token string">"n_embd must be divisible by n_head"</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>n_head <span class="token operator">=</span> config<span class="token punctuation">.</span>n_head        self<span class="token punctuation">.</span>head_dim <span class="token operator">=</span> config<span class="token punctuation">.</span>n_embd <span class="token operator">//</span> config<span class="token punctuation">.</span>n_head        self<span class="token punctuation">.</span>qkv <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>            config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>            <span class="token number">3</span> <span class="token operator">*</span> config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>proj <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>            config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>            config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>attn_dropout <span class="token operator">=</span> nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>config<span class="token punctuation">.</span>dropout<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>resid_dropout <span class="token operator">=</span> nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>config<span class="token punctuation">.</span>dropout<span class="token punctuation">)</span>        mask <span class="token operator">=</span> torch<span class="token punctuation">.</span>tril<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>ones<span class="token punctuation">(</span>            config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>            config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>        <span class="token punctuation">)</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>register_buffer<span class="token punctuation">(</span>            <span class="token string">"causal_mask"</span><span class="token punctuation">,</span>            mask<span class="token punctuation">.</span>view<span class="token punctuation">(</span>                <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span>                config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>                config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>            <span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span></code></pre><p>下三角矩阵形如：</p><p>$$<br>\begin{bmatrix}<br>1&amp;0&amp;0&amp;0\<br>1&amp;1&amp;0&amp;0\<br>1&amp;1&amp;1&amp;0\<br>1&amp;1&amp;1&amp;1<br>\end{bmatrix}<br>$$</p><p>这里约定：</p><ul><li><code>1</code> 表示允许查看。</li><li><code>0</code> 表示需要遮住。</li></ul><p><code>causal_mask</code> 不是可训练参数，但需要跟随模型移动到 GPU 并保存到 <code>state_dict</code>，所以使用 <code>register_buffer</code>。</p><h3 id="9-2-一次性产生-Q、K、V"><a href="#9-2-一次性产生-Q、K、V" class="headerlink" title="9.2 一次性产生 Q、K、V"></a>9.2 一次性产生 Q、K、V</h3><pre class="language-python" data-language="python"><code class="language-python">q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> v <span class="token operator">=</span> self<span class="token punctuation">.</span>qkv<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">.</span>split<span class="token punctuation">(</span>c<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>这里使用一个大线性层，而不是三个独立线性层：</p><pre class="language-text" data-language="text"><code class="language-text">x:       [B, T, C]qkv(x):  [B, T, 3C]q:       [B, T, C]k:       [B, T, C]v:       [B, T, C]</code></pre><p>一个 <code>Linear(C, 3C)</code> 在数学上等价于把三组投影矩阵拼在一起：</p><p>$$<br>[Q,K,V]&#x3D;X[W_Q,W_K,W_V]+b<br>$$</p><p>这样写通常能减少小矩阵算子的调度开销。</p><h3 id="9-3-拆成多个头"><a href="#9-3-拆成多个头" class="headerlink" title="9.3 拆成多个头"></a>9.3 拆成多个头</h3><pre class="language-python" data-language="python"><code class="language-python">q <span class="token operator">=</span> q<span class="token punctuation">.</span>view<span class="token punctuation">(</span>    b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> self<span class="token punctuation">.</span>n_head<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span><span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>k <span class="token operator">=</span> k<span class="token punctuation">.</span>view<span class="token punctuation">(</span>    b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> self<span class="token punctuation">.</span>n_head<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span><span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>v <span class="token operator">=</span> v<span class="token punctuation">.</span>view<span class="token punctuation">(</span>    b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> self<span class="token punctuation">.</span>n_head<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span><span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span></code></pre><p>形状变化：</p><pre class="language-text" data-language="text"><code class="language-text">[B, T, C]-> [B, T, H, D]-> [B, H, T, D]</code></pre><p>其中：</p><p>$$<br>C&#x3D;H\times D<br>$$</p><p>本例是：</p><pre class="language-text" data-language="text"><code class="language-text">C = 96H = 4D = 24</code></pre><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-qkv-shapes.svg" alt="Mini-GPT QKV张量形状"></p><h3 id="9-4-计算分数并应用-Mask"><a href="#9-4-计算分数并应用-Mask" class="headerlink" title="9.4 计算分数并应用 Mask"></a>9.4 计算分数并应用 Mask</h3><pre class="language-python" data-language="python"><code class="language-python">scores <span class="token operator">=</span> q @ k<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>scores <span class="token operator">=</span> scores <span class="token operator">/</span> math<span class="token punctuation">.</span>sqrt<span class="token punctuation">(</span>self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span>scores <span class="token operator">=</span> scores<span class="token punctuation">.</span>masked_fill<span class="token punctuation">(</span>    self<span class="token punctuation">.</span>causal_mask<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span>t<span class="token punctuation">,</span> <span class="token punctuation">:</span>t<span class="token punctuation">]</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">,</span>    <span class="token builtin">float</span><span class="token punctuation">(</span><span class="token string">"-inf"</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>形状：</p><pre class="language-text" data-language="text"><code class="language-text">q:             [B, H, T, D]k.transpose:   [B, H, D, T]scores:        [B, H, T, T]causal_mask:   [1, 1, T, T]</code></pre><p>Mask 在 batch 和 head 维自动广播。</p><p>为什么填充 $-\infty$？因为：</p><p>$$<br>e^{-\infty}&#x3D;0<br>$$</p><p>经过 Softmax 后，未来位置的注意力权重会变成 0。</p><h3 id="9-5-加权求和并合并头"><a href="#9-5-加权求和并合并头" class="headerlink" title="9.5 加权求和并合并头"></a>9.5 加权求和并合并头</h3><pre class="language-python" data-language="python"><code class="language-python">weights <span class="token operator">=</span> F<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>scores<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>weights <span class="token operator">=</span> self<span class="token punctuation">.</span>attn_dropout<span class="token punctuation">(</span>weights<span class="token punctuation">)</span>y <span class="token operator">=</span> weights @ vy <span class="token operator">=</span> y<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">.</span>contiguous<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>view<span class="token punctuation">(</span>b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> c<span class="token punctuation">)</span>y <span class="token operator">=</span> self<span class="token punctuation">.</span>proj<span class="token punctuation">(</span>y<span class="token punctuation">)</span>y <span class="token operator">=</span> self<span class="token punctuation">.</span>resid_dropout<span class="token punctuation">(</span>y<span class="token punctuation">)</span><span class="token keyword">return</span> y</code></pre><p>形状变化：</p><pre class="language-text" data-language="text"><code class="language-text">weights:       [B, H, T, T]v:             [B, H, T, D]weights @ v:   [B, H, T, D]transpose:     [B, T, H, D]view:          [B, T, C]output proj:   [B, T, C]</code></pre><h3 id="9-6-完整注意力类"><a href="#9-6-完整注意力类" class="headerlink" title="9.6 完整注意力类"></a>9.6 完整注意力类</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">CausalSelfAttention</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> config<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        <span class="token keyword">if</span> config<span class="token punctuation">.</span>n_embd <span class="token operator">%</span> config<span class="token punctuation">.</span>n_head <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">:</span>            <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span>                <span class="token string">"n_embd must be divisible by n_head"</span>            <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>n_head <span class="token operator">=</span> config<span class="token punctuation">.</span>n_head        self<span class="token punctuation">.</span>head_dim <span class="token operator">=</span> config<span class="token punctuation">.</span>n_embd <span class="token operator">//</span> config<span class="token punctuation">.</span>n_head        self<span class="token punctuation">.</span>qkv <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>            config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>            <span class="token number">3</span> <span class="token operator">*</span> config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>proj <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>            config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>            config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>attn_dropout <span class="token operator">=</span> nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>config<span class="token punctuation">.</span>dropout<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>resid_dropout <span class="token operator">=</span> nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>config<span class="token punctuation">.</span>dropout<span class="token punctuation">)</span>        mask <span class="token operator">=</span> torch<span class="token punctuation">.</span>tril<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>ones<span class="token punctuation">(</span>            config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>            config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>        <span class="token punctuation">)</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>register_buffer<span class="token punctuation">(</span>            <span class="token string">"causal_mask"</span><span class="token punctuation">,</span>            mask<span class="token punctuation">.</span>view<span class="token punctuation">(</span>                <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span>                config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>                config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>            <span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> c <span class="token operator">=</span> x<span class="token punctuation">.</span>shape        q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> v <span class="token operator">=</span> self<span class="token punctuation">.</span>qkv<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">.</span>split<span class="token punctuation">(</span>c<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        q <span class="token operator">=</span> q<span class="token punctuation">.</span>view<span class="token punctuation">(</span>            b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> self<span class="token punctuation">.</span>n_head<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim        <span class="token punctuation">)</span><span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>        k <span class="token operator">=</span> k<span class="token punctuation">.</span>view<span class="token punctuation">(</span>            b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> self<span class="token punctuation">.</span>n_head<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim        <span class="token punctuation">)</span><span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>        v <span class="token operator">=</span> v<span class="token punctuation">.</span>view<span class="token punctuation">(</span>            b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> self<span class="token punctuation">.</span>n_head<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim        <span class="token punctuation">)</span><span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>        scores <span class="token operator">=</span> q @ k<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        scores <span class="token operator">=</span> scores <span class="token operator">/</span> math<span class="token punctuation">.</span>sqrt<span class="token punctuation">(</span>self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span>        scores <span class="token operator">=</span> scores<span class="token punctuation">.</span>masked_fill<span class="token punctuation">(</span>            self<span class="token punctuation">.</span>causal_mask<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span>t<span class="token punctuation">,</span> <span class="token punctuation">:</span>t<span class="token punctuation">]</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">,</span>            <span class="token builtin">float</span><span class="token punctuation">(</span><span class="token string">"-inf"</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>        weights <span class="token operator">=</span> F<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>scores<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        weights <span class="token operator">=</span> self<span class="token punctuation">.</span>attn_dropout<span class="token punctuation">(</span>weights<span class="token punctuation">)</span>        y <span class="token operator">=</span> weights @ v        y <span class="token operator">=</span> y<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">.</span>contiguous<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>view<span class="token punctuation">(</span>b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> c<span class="token punctuation">)</span>        y <span class="token operator">=</span> self<span class="token punctuation">.</span>proj<span class="token punctuation">(</span>y<span class="token punctuation">)</span>        <span class="token keyword">return</span> self<span class="token punctuation">.</span>resid_dropout<span class="token punctuation">(</span>y<span class="token punctuation">)</span></code></pre><h2 id="10-为什么-transpose-后要调用-contiguous"><a href="#10-为什么-transpose-后要调用-contiguous" class="headerlink" title="10. 为什么 transpose 后要调用 contiguous"></a>10. 为什么 <code>transpose</code> 后要调用 <code>contiguous</code></h2><p><code>transpose</code> 通常不会真的复制数据，而是改变张量的 stride，让同一块内存以不同顺序被解释。</p><p>例如：</p><pre class="language-python" data-language="python"><code class="language-python">y <span class="token operator">=</span> y<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span></code></pre><p>此时逻辑形状虽然是 <code>[B,T,H,D]</code>，底层内存排列却可能不是连续的。直接用 <code>view</code> 合并维度可能报错或不符合预期。</p><pre class="language-python" data-language="python"><code class="language-python">y <span class="token operator">=</span> y<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">.</span>contiguous<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>view<span class="token punctuation">(</span>b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> c<span class="token punctuation">)</span></code></pre><p><code>contiguous()</code> 会把数据整理成当前维度顺序对应的连续内存，再交给 <code>view</code>。</p><p>也可以使用 <code>reshape</code>，它在必要时会复制数据。但在这里显式写出 <code>contiguous().view(...)</code> 更容易看清拆头与合并头的过程。</p><h2 id="11-MLP：对每个位置进行非线性变换"><a href="#11-MLP：对每个位置进行非线性变换" class="headerlink" title="11. MLP：对每个位置进行非线性变换"></a>11. MLP：对每个位置进行非线性变换</h2><p>Attention 负责 token 之间的信息交换，MLP 负责每个 token 内部的特征变换。</p><p>GPT 风格 MLP 通常先把维度扩展到 4 倍，再缩回原维度：</p><p>$$<br>\operatorname{MLP}(x)<br>&#x3D;W_2\operatorname{GELU}(W_1x+b_1)+b_2<br>$$</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">MLP</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> config<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>net <span class="token operator">=</span> nn<span class="token punctuation">.</span>Sequential<span class="token punctuation">(</span>            nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>                config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>                <span class="token number">4</span> <span class="token operator">*</span> config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>            <span class="token punctuation">)</span><span class="token punctuation">,</span>            nn<span class="token punctuation">.</span>GELU<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>                <span class="token number">4</span> <span class="token operator">*</span> config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>                config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>            <span class="token punctuation">)</span><span class="token punctuation">,</span>            nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>config<span class="token punctuation">.</span>dropout<span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> self<span class="token punctuation">.</span>net<span class="token punctuation">(</span>x<span class="token punctuation">)</span></code></pre><p>本例形状变化：</p><pre class="language-text" data-language="text"><code class="language-text">[B, T, 96]-> [B, T, 384]-> GELU-> [B, T, 96]</code></pre><h3 id="11-1-GELU-的数学原理"><a href="#11-1-GELU-的数学原理" class="headerlink" title="11.1 GELU 的数学原理"></a>11.1 GELU 的数学原理</h3><p>GELU 全称 Gaussian Error Linear Unit，常见定义为：</p><p>$$<br>\operatorname{GELU}(x)&#x3D;x\Phi(x)<br>$$</p><p>$\Phi(x)$ 是标准正态分布的累积分布函数。</p><p>常用近似形式是：</p><p>$$<br>\operatorname{GELU}(x)<br>\approx\frac{x}{2}<br>\left[<br>1+\tanh\left(<br>\sqrt{\frac{2}{\pi}}<br>\left(x+0.044715x^3\right)<br>\right)<br>\right]<br>$$</p><p>与 ReLU 直接丢弃所有负数不同，GELU 会平滑地调节输入，负值附近仍可能保留少量信息。</p><h2 id="12-Pre-Norm-GPT-Block"><a href="#12-Pre-Norm-GPT-Block" class="headerlink" title="12. Pre-Norm GPT Block"></a>12. Pre-Norm GPT Block</h2><p>一个 Mini-GPT Block 包含：</p><ol><li>LayerNorm。</li><li>Causal Self-Attention。</li><li>残差连接。</li><li>LayerNorm。</li><li>MLP。</li><li>残差连接。</li></ol><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-block.svg" alt="Mini-GPT Pre-Norm Block"></p><p>代码非常短：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">Block</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> config<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>ln_1 <span class="token operator">=</span> nn<span class="token punctuation">.</span>LayerNorm<span class="token punctuation">(</span>config<span class="token punctuation">.</span>n_embd<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>attn <span class="token operator">=</span> CausalSelfAttention<span class="token punctuation">(</span>config<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>ln_2 <span class="token operator">=</span> nn<span class="token punctuation">.</span>LayerNorm<span class="token punctuation">(</span>config<span class="token punctuation">.</span>n_embd<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>mlp <span class="token operator">=</span> MLP<span class="token punctuation">(</span>config<span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        x <span class="token operator">=</span> x <span class="token operator">+</span> self<span class="token punctuation">.</span>attn<span class="token punctuation">(</span>self<span class="token punctuation">.</span>ln_1<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span>        x <span class="token operator">=</span> x <span class="token operator">+</span> self<span class="token punctuation">.</span>mlp<span class="token punctuation">(</span>self<span class="token punctuation">.</span>ln_2<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span>        <span class="token keyword">return</span> x</code></pre><p>数学形式：</p><p>$$<br>x’&#x3D;x+\operatorname{Attention}(\operatorname{LN}(x))<br>$$</p><p>$$<br>y&#x3D;x’+\operatorname{MLP}(\operatorname{LN}(x’))<br>$$</p><h3 id="12-1-为什么叫-Pre-Norm"><a href="#12-1-为什么叫-Pre-Norm" class="headerlink" title="12.1 为什么叫 Pre-Norm"></a>12.1 为什么叫 Pre-Norm</h3><p>因为 LayerNorm 位于 Attention 或 MLP 之前：</p><pre class="language-text" data-language="text"><code class="language-text">x -> LayerNorm -> 子层 -> 与 x 相加</code></pre><p>Post-Norm 则是：</p><pre class="language-text" data-language="text"><code class="language-text">x -> 子层 -> 与 x 相加 -> LayerNorm</code></pre><p>上一篇原始 Transformer 主要展示了 Post-Norm。GPT-2 风格模型常使用 Pre-Norm，并在全部 Block 后再增加一次最终 LayerNorm。</p><h3 id="12-2-残差连接为什么不能省"><a href="#12-2-残差连接为什么不能省" class="headerlink" title="12.2 残差连接为什么不能省"></a>12.2 残差连接为什么不能省</h3><p>残差连接：</p><p>$$<br>y&#x3D;x+F(x)<br>$$</p><p>让模型可以学习“在已有表示上增加修正”，也为深层网络提供更直接的梯度路径。</p><p>如果删除残差连接，每一层都必须完全重建表示，深层模型通常更难优化。</p><h2 id="13-组合完整-Mini-GPT"><a href="#13-组合完整-Mini-GPT" class="headerlink" title="13. 组合完整 Mini-GPT"></a>13. 组合完整 Mini-GPT</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">MiniGPT</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> config<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>config <span class="token operator">=</span> config        self<span class="token punctuation">.</span>token_embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>            config<span class="token punctuation">.</span>vocab_size<span class="token punctuation">,</span>            config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>position_embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>            config<span class="token punctuation">.</span>block_size<span class="token punctuation">,</span>            config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>dropout <span class="token operator">=</span> nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>config<span class="token punctuation">.</span>dropout<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>blocks <span class="token operator">=</span> nn<span class="token punctuation">.</span>ModuleList<span class="token punctuation">(</span><span class="token punctuation">[</span>            Block<span class="token punctuation">(</span>config<span class="token punctuation">)</span>            <span class="token keyword">for</span> _ <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span>config<span class="token punctuation">.</span>n_layer<span class="token punctuation">)</span>        <span class="token punctuation">]</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>ln_f <span class="token operator">=</span> nn<span class="token punctuation">.</span>LayerNorm<span class="token punctuation">(</span>config<span class="token punctuation">.</span>n_embd<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>lm_head <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>            config<span class="token punctuation">.</span>n_embd<span class="token punctuation">,</span>            config<span class="token punctuation">.</span>vocab_size<span class="token punctuation">,</span>            bias<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span><span class="token builtin">apply</span><span class="token punctuation">(</span>self<span class="token punctuation">.</span>_init_weights<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>lm_head<span class="token punctuation">.</span>weight <span class="token operator">=</span> self<span class="token punctuation">.</span>token_embedding<span class="token punctuation">.</span>weight</code></pre><p><code>nn.ModuleList</code> 能让 PyTorch 正确注册每个 Block 的参数。普通 Python list 虽然能保存对象，但其中模块不会自动出现在 <code>model.parameters()</code> 和 <code>state_dict()</code> 中。</p><h3 id="13-1-参数初始化"><a href="#13-1-参数初始化" class="headerlink" title="13.1 参数初始化"></a>13.1 参数初始化</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token decorator annotation punctuation">@staticmethod</span><span class="token keyword">def</span> <span class="token function">_init_weights</span><span class="token punctuation">(</span>module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> <span class="token builtin">isinstance</span><span class="token punctuation">(</span>module<span class="token punctuation">,</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">)</span><span class="token punctuation">:</span>        nn<span class="token punctuation">.</span>init<span class="token punctuation">.</span>normal_<span class="token punctuation">(</span>            module<span class="token punctuation">.</span>weight<span class="token punctuation">,</span>            mean<span class="token operator">=</span><span class="token number">0.0</span><span class="token punctuation">,</span>            std<span class="token operator">=</span><span class="token number">0.02</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>        <span class="token keyword">if</span> module<span class="token punctuation">.</span>bias <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token boolean">None</span><span class="token punctuation">:</span>            nn<span class="token punctuation">.</span>init<span class="token punctuation">.</span>zeros_<span class="token punctuation">(</span>module<span class="token punctuation">.</span>bias<span class="token punctuation">)</span>    <span class="token keyword">elif</span> <span class="token builtin">isinstance</span><span class="token punctuation">(</span>module<span class="token punctuation">,</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">)</span><span class="token punctuation">:</span>        nn<span class="token punctuation">.</span>init<span class="token punctuation">.</span>normal_<span class="token punctuation">(</span>            module<span class="token punctuation">.</span>weight<span class="token punctuation">,</span>            mean<span class="token operator">=</span><span class="token number">0.0</span><span class="token punctuation">,</span>            std<span class="token operator">=</span><span class="token number">0.02</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span></code></pre><p>这里使用均值为 0、标准差为 0.02 的正态分布初始化线性层和 Embedding。Mini-GPT 教学实现可以使用这个简单策略；更深模型还会针对残差分支进一步缩放初始化。</p><h2 id="14-Weight-Tying：输入词向量与输出分类器共享参数"><a href="#14-Weight-Tying：输入词向量与输出分类器共享参数" class="headerlink" title="14. Weight Tying：输入词向量与输出分类器共享参数"></a>14. Weight Tying：输入词向量与输出分类器共享参数</h2><p>输入层把 token id 映射成向量：</p><pre class="language-text" data-language="text"><code class="language-text">token_embedding.weight: [V, C]</code></pre><p>输出层把隐藏向量映射回词表 logits：</p><pre class="language-text" data-language="text"><code class="language-text">lm_head.weight: [V, C]</code></pre><p>两者形状相同，因此可以共享同一份参数：</p><pre class="language-python" data-language="python"><code class="language-python">self<span class="token punctuation">.</span>lm_head<span class="token punctuation">.</span>weight <span class="token operator">=</span> self<span class="token punctuation">.</span>token_embedding<span class="token punctuation">.</span>weight</code></pre><p>输入时：</p><p>$$<br>e_t&#x3D;W_E[x_t]<br>$$</p><p>输出时：</p><p>$$<br>z_t&#x3D;h_tW_E^T<br>$$</p><p>可以把它理解为：</p><ul><li>输入阶段从词表向量表中取出当前 token。</li><li>输出阶段计算隐藏状态与所有词表向量的匹配程度。</li></ul><p>共享参数的好处：</p><ul><li>减少参数量。</li><li>让输入表示空间与输出分类空间保持联系。</li></ul><p>本例若不共享，会额外增加：</p><p>$$<br>V\times C&#x3D;123\times96&#x3D;11808<br>$$</p><p>个参数。</p><h2 id="15-前向传播与语言模型头"><a href="#15-前向传播与语言模型头" class="headerlink" title="15. 前向传播与语言模型头"></a>15. 前向传播与语言模型头</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> idx<span class="token punctuation">,</span> targets<span class="token operator">=</span><span class="token boolean">None</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    b<span class="token punctuation">,</span> t <span class="token operator">=</span> idx<span class="token punctuation">.</span>shape    <span class="token keyword">if</span> t <span class="token operator">></span> self<span class="token punctuation">.</span>config<span class="token punctuation">.</span>block_size<span class="token punctuation">:</span>        <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span>            <span class="token string">"sequence is longer than block_size"</span>        <span class="token punctuation">)</span>    pos <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span>t<span class="token punctuation">,</span> device<span class="token operator">=</span>idx<span class="token punctuation">.</span>device<span class="token punctuation">)</span>    token_emb <span class="token operator">=</span> self<span class="token punctuation">.</span>token_embedding<span class="token punctuation">(</span>idx<span class="token punctuation">)</span>    position_emb <span class="token operator">=</span> self<span class="token punctuation">.</span>position_embedding<span class="token punctuation">(</span>pos<span class="token punctuation">)</span>    x <span class="token operator">=</span> token_emb <span class="token operator">+</span> position_emb    x <span class="token operator">=</span> self<span class="token punctuation">.</span>dropout<span class="token punctuation">(</span>x<span class="token punctuation">)</span>    <span class="token keyword">for</span> block <span class="token keyword">in</span> self<span class="token punctuation">.</span>blocks<span class="token punctuation">:</span>        x <span class="token operator">=</span> block<span class="token punctuation">(</span>x<span class="token punctuation">)</span>    x <span class="token operator">=</span> self<span class="token punctuation">.</span>ln_f<span class="token punctuation">(</span>x<span class="token punctuation">)</span>    logits <span class="token operator">=</span> self<span class="token punctuation">.</span>lm_head<span class="token punctuation">(</span>x<span class="token punctuation">)</span>    loss <span class="token operator">=</span> <span class="token boolean">None</span>    <span class="token keyword">if</span> targets <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token boolean">None</span><span class="token punctuation">:</span>        loss <span class="token operator">=</span> F<span class="token punctuation">.</span>cross_entropy<span class="token punctuation">(</span>            logits<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span>                <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span>                self<span class="token punctuation">.</span>config<span class="token punctuation">.</span>vocab_size<span class="token punctuation">,</span>            <span class="token punctuation">)</span><span class="token punctuation">,</span>            targets<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>    <span class="token keyword">return</span> logits<span class="token punctuation">,</span> loss</code></pre><p>完整形状变化：</p><pre class="language-text" data-language="text"><code class="language-text">idx                         [B, T]token embedding             [B, T, C]position embedding          [T, C]相加并广播                   [B, T, C]N 个 GPT Block              [B, T, C]final LayerNorm             [B, T, C]lm_head                     [B, T, V]</code></pre><h2 id="16-交叉熵为什么就是语言模型损失"><a href="#16-交叉熵为什么就是语言模型损失" class="headerlink" title="16. 交叉熵为什么就是语言模型损失"></a>16. 交叉熵为什么就是语言模型损失</h2><p>对第 $t$ 个位置，模型输出词表 logits：</p><p>$$<br>z_t&#x3D;(z_{t,1},z_{t,2},\ldots,z_{t,V})<br>$$</p><p>Softmax 转为概率：</p><p>$$<br>p_{t,j}&#x3D;\frac{e^{z_{t,j}}}{\sum_{k&#x3D;1}^{V}e^{z_{t,k}}}<br>$$</p><p>如果正确下一个 token 是 $y_t$，该位置的负对数似然为：</p><p>$$<br>\mathcal{L}<em>t&#x3D;-\log p</em>{t,y_t}<br>$$</p><p>整个 batch 的平均损失：</p><p>$$<br>\mathcal{L}<br>&#x3D;-\frac{1}{BT}<br>\sum_{b&#x3D;1}^{B}<br>\sum_{t&#x3D;1}^{T}<br>\log P(y_{b,t}\mid x_{b,\le t})<br>$$</p><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-loss.svg" alt="Mini-GPT语言模型损失"></p><h3 id="16-1-一个三分类手算例子"><a href="#16-1-一个三分类手算例子" class="headerlink" title="16.1 一个三分类手算例子"></a>16.1 一个三分类手算例子</h3><p>假设某位置 logits 为：</p><p>$$<br>z&#x3D;[2.0,1.0,0.0]<br>$$</p><p>Softmax 分母：</p><p>$$<br>e^2+e^1+e^0<br>\approx7.389+2.718+1&#x3D;11.107<br>$$</p><p>概率约为：</p><p>$$<br>p\approx[0.665,0.245,0.090]<br>$$</p><p>如果正确 token 是第 1 类：</p><p>$$<br>\mathcal{L}&#x3D;-\log(0.665)\approx0.408<br>$$</p><p>如果正确 token 是第 3 类：</p><p>$$<br>\mathcal{L}&#x3D;-\log(0.090)\approx2.408<br>$$</p><p>模型越确信错误答案，惩罚越大。</p><h3 id="16-2-为什么不用手动-Softmax"><a href="#16-2-为什么不用手动-Softmax" class="headerlink" title="16.2 为什么不用手动 Softmax"></a>16.2 为什么不用手动 Softmax</h3><p><code>F.cross_entropy</code> 内部会结合 LogSoftmax 与负对数似然，直接接收 logits：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> F<span class="token punctuation">.</span>cross_entropy<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> targets<span class="token punctuation">)</span></code></pre><p>错误写法：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> F<span class="token punctuation">.</span>cross_entropy<span class="token punctuation">(</span>    F<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    targets<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>提前 Softmax 不仅重复计算，还会损失数值稳定性。</p><h2 id="17-困惑度-Perplexity-是什么"><a href="#17-困惑度-Perplexity-是什么" class="headerlink" title="17. 困惑度 Perplexity 是什么"></a>17. 困惑度 Perplexity 是什么</h2><p>语言模型还经常报告困惑度：</p><p>$$<br>\operatorname{PPL}&#x3D;e^{\mathcal{L}}<br>$$</p><p>如果平均交叉熵为：</p><p>$$<br>\mathcal{L}&#x3D;2.0<br>$$</p><p>那么：</p><p>$$<br>\operatorname{PPL}&#x3D;e^2\approx7.39<br>$$</p><p>可以粗略理解为，模型在每一步像是在约 7.39 个同等可能候选中犹豫。</p><p>如果模型在 $V$ 个 token 上完全均匀随机预测：</p><p>$$<br>p&#x3D;\frac{1}{V}<br>$$</p><p>则：</p><p>$$<br>\mathcal{L}&#x3D;-\log\frac{1}{V}&#x3D;\log V<br>$$</p><p>本例 $V&#x3D;123$，随机模型的理论损失约为：</p><p>$$<br>\log(123)\approx4.812<br>$$</p><p>困惑度越低通常表示对验证文本预测得越好，但不同 Tokenizer、词表和数据集上的困惑度不宜直接横向比较。</p><h2 id="18-Mini-GPT-有多少参数"><a href="#18-Mini-GPT-有多少参数" class="headerlink" title="18. Mini-GPT 有多少参数"></a>18. Mini-GPT 有多少参数</h2><p>本例配置：</p><pre class="language-text" data-language="text"><code class="language-text">V = 123T = 64C = 96H = 4L = 3</code></pre><h3 id="18-1-Embedding"><a href="#18-1-Embedding" class="headerlink" title="18.1 Embedding"></a>18.1 Embedding</h3><p>Token Embedding：</p><p>$$<br>123\times96&#x3D;11808<br>$$</p><p>Position Embedding：</p><p>$$<br>64\times96&#x3D;6144<br>$$</p><h3 id="18-2-每个-Block"><a href="#18-2-每个-Block" class="headerlink" title="18.2 每个 Block"></a>18.2 每个 Block</h3><p>QKV 投影：</p><p>$$<br>96\times288+288&#x3D;27936<br>$$</p><p>输出投影：</p><p>$$<br>96\times96+96&#x3D;9312<br>$$</p><p>两次 LayerNorm：</p><p>$$<br>2\times(96+96)&#x3D;384<br>$$</p><p>MLP 第一层：</p><p>$$<br>96\times384+384&#x3D;37248<br>$$</p><p>MLP 第二层：</p><p>$$<br>384\times96+96&#x3D;36960<br>$$</p><p>每个 Block 合计：</p><p>$$<br>27936+9312+384+37248+36960&#x3D;111840<br>$$</p><p>三个 Block：</p><p>$$<br>3\times111840&#x3D;335520<br>$$</p><p>最终 LayerNorm：</p><p>$$<br>96+96&#x3D;192<br>$$</p><p>由于 <code>lm_head</code> 与 Token Embedding 权重绑定，总参数量是：</p><p>$$<br>11808+6144+335520+192&#x3D;353664<br>$$</p><p>代码验证：</p><pre class="language-python" data-language="python"><code class="language-python">parameter_count <span class="token operator">=</span> <span class="token builtin">sum</span><span class="token punctuation">(</span>    p<span class="token punctuation">.</span>numel<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">for</span> p <span class="token keyword">in</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>parameter_count<span class="token punctuation">)</span>  <span class="token comment"># 353664</span></code></pre><h2 id="19-使用-AdamW-配置参数组"><a href="#19-使用-AdamW-配置参数组" class="headerlink" title="19. 使用 AdamW 配置参数组"></a>19. 使用 AdamW 配置参数组</h2><p>并非所有参数都适合权重衰减。一个实用的教学规则是：</p><ul><li>二维及以上的权重矩阵使用 weight decay。</li><li>一维 bias 和 LayerNorm 参数不使用 weight decay。</li></ul><pre class="language-python" data-language="python"><code class="language-python">decay <span class="token operator">=</span> <span class="token punctuation">[</span>    p <span class="token keyword">for</span> p <span class="token keyword">in</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> p<span class="token punctuation">.</span>requires_grad <span class="token keyword">and</span> p<span class="token punctuation">.</span>dim<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">>=</span> <span class="token number">2</span><span class="token punctuation">]</span>no_decay <span class="token operator">=</span> <span class="token punctuation">[</span>    p <span class="token keyword">for</span> p <span class="token keyword">in</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> p<span class="token punctuation">.</span>requires_grad <span class="token keyword">and</span> p<span class="token punctuation">.</span>dim<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">&lt;</span> <span class="token number">2</span><span class="token punctuation">]</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    <span class="token punctuation">[</span>        <span class="token punctuation">&#123;</span>            <span class="token string">"params"</span><span class="token punctuation">:</span> decay<span class="token punctuation">,</span>            <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">0.1</span><span class="token punctuation">,</span>        <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>        <span class="token punctuation">&#123;</span>            <span class="token string">"params"</span><span class="token punctuation">:</span> no_decay<span class="token punctuation">,</span>            <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">0.0</span><span class="token punctuation">,</span>        <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">]</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">3e-3</span><span class="token punctuation">,</span>    betas<span class="token operator">=</span><span class="token punctuation">(</span><span class="token number">0.9</span><span class="token punctuation">,</span> <span class="token number">0.95</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>为什么 LayerNorm 参数通常不衰减？它们主要负责缩放与平移归一化结果，对这些一维参数施加同样衰减往往没有必要。</p><p>按维数分组适用于这个结构规整的 Mini-GPT。复杂生产模型更适合根据模块类型和参数名显式分组，避免误分类特殊参数。</p><h2 id="20-Warmup-与余弦学习率衰减"><a href="#20-Warmup-与余弦学习率衰减" class="headerlink" title="20. Warmup 与余弦学习率衰减"></a>20. Warmup 与余弦学习率衰减</h2><p>训练刚开始时，参数和 AdamW 的动量统计都还不稳定。直接使用最大学习率可能让更新过猛，因此先 warmup：</p><p>$$<br>\eta_t&#x3D;\eta_{max}\frac{t+1}{T_{warmup}}<br>$$</p><p>warmup 结束后使用余弦衰减：</p><p>$$<br>r&#x3D;\frac{t-T_{warmup}}<br>{T_{max}-T_{warmup}}<br>$$</p><p>$$<br>\eta_t&#x3D;\eta_{min}<br>+\frac{1}{2}<br>(1+\cos(\pi r))<br>(\eta_{max}-\eta_{min})<br>$$</p><pre class="language-python" data-language="python"><code class="language-python">max_steps <span class="token operator">=</span> <span class="token number">1000</span>warmup_steps <span class="token operator">=</span> <span class="token number">100</span>max_lr <span class="token operator">=</span> <span class="token number">3e-3</span>min_lr <span class="token operator">=</span> <span class="token number">3e-4</span><span class="token keyword">def</span> <span class="token function">get_lr</span><span class="token punctuation">(</span>step<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> step <span class="token operator">&lt;</span> warmup_steps<span class="token punctuation">:</span>        <span class="token keyword">return</span> max_lr <span class="token operator">*</span> <span class="token punctuation">(</span>step <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">)</span> <span class="token operator">/</span> warmup_steps    ratio <span class="token operator">=</span> <span class="token punctuation">(</span>        <span class="token punctuation">(</span>step <span class="token operator">-</span> warmup_steps<span class="token punctuation">)</span>        <span class="token operator">/</span> <span class="token punctuation">(</span>max_steps <span class="token operator">-</span> warmup_steps<span class="token punctuation">)</span>    <span class="token punctuation">)</span>    coeff <span class="token operator">=</span> <span class="token number">0.5</span> <span class="token operator">*</span> <span class="token punctuation">(</span>        <span class="token number">1.0</span> <span class="token operator">+</span> math<span class="token punctuation">.</span>cos<span class="token punctuation">(</span>math<span class="token punctuation">.</span>pi <span class="token operator">*</span> ratio<span class="token punctuation">)</span>    <span class="token punctuation">)</span>    <span class="token keyword">return</span> min_lr <span class="token operator">+</span> coeff <span class="token operator">*</span> <span class="token punctuation">(</span>max_lr <span class="token operator">-</span> min_lr<span class="token punctuation">)</span></code></pre><p>训练每一步更新参数组学习率：</p><pre class="language-python" data-language="python"><code class="language-python">lr <span class="token operator">=</span> get_lr<span class="token punctuation">(</span>step<span class="token punctuation">)</span><span class="token keyword">for</span> group <span class="token keyword">in</span> optimizer<span class="token punctuation">.</span>param_groups<span class="token punctuation">:</span>    group<span class="token punctuation">[</span><span class="token string">"lr"</span><span class="token punctuation">]</span> <span class="token operator">=</span> lr</code></pre><h2 id="21-完整训练循环"><a href="#21-完整训练循环" class="headerlink" title="21. 完整训练循环"></a>21. 完整训练循环</h2><pre class="language-python" data-language="python"><code class="language-python">device <span class="token operator">=</span> torch<span class="token punctuation">.</span>device<span class="token punctuation">(</span>    <span class="token string">"cuda"</span> <span class="token keyword">if</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_available<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">else</span> <span class="token string">"cpu"</span><span class="token punctuation">)</span>config <span class="token operator">=</span> GPTConfig<span class="token punctuation">(</span>vocab_size<span class="token operator">=</span><span class="token builtin">len</span><span class="token punctuation">(</span>chars<span class="token punctuation">)</span><span class="token punctuation">)</span>model <span class="token operator">=</span> MiniGPT<span class="token punctuation">(</span>config<span class="token punctuation">)</span><span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span>model<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">for</span> step <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span>max_steps<span class="token punctuation">)</span><span class="token punctuation">:</span>    lr <span class="token operator">=</span> get_lr<span class="token punctuation">(</span>step<span class="token punctuation">)</span>    <span class="token keyword">for</span> group <span class="token keyword">in</span> optimizer<span class="token punctuation">.</span>param_groups<span class="token punctuation">:</span>        group<span class="token punctuation">[</span><span class="token string">"lr"</span><span class="token punctuation">]</span> <span class="token operator">=</span> lr    x<span class="token punctuation">,</span> y <span class="token operator">=</span> get_batch<span class="token punctuation">(</span><span class="token string">"train"</span><span class="token punctuation">)</span>    _<span class="token punctuation">,</span> loss <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">,</span> y<span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span>set_to_none<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    grad_norm <span class="token operator">=</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>utils<span class="token punctuation">.</span>clip_grad_norm_<span class="token punctuation">(</span>        model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        max_norm<span class="token operator">=</span><span class="token number">1.0</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> <span class="token punctuation">(</span>step <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">)</span> <span class="token operator">%</span> <span class="token number">200</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">:</span>        <span class="token keyword">print</span><span class="token punctuation">(</span>            <span class="token string-interpolation"><span class="token string">f"step=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>step <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">&#125;</span></span><span class="token string">, "</span></span>            <span class="token string-interpolation"><span class="token string">f"loss=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span><span class="token format-spec">.4f</span><span class="token punctuation">&#125;</span></span><span class="token string">, "</span></span>            <span class="token string-interpolation"><span class="token string">f"lr=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>lr<span class="token punctuation">:</span><span class="token format-spec">.6f</span><span class="token punctuation">&#125;</span></span><span class="token string">, "</span></span>            <span class="token string-interpolation"><span class="token string">f"grad=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>grad_norm<span class="token punctuation">:</span><span class="token format-spec">.3f</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span>        <span class="token punctuation">)</span></code></pre><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-training-pipeline.svg" alt="Mini-GPT训练流程"></p><h3 id="21-1-为什么先清梯度再反向传播"><a href="#21-1-为什么先清梯度再反向传播" class="headerlink" title="21.1 为什么先清梯度再反向传播"></a>21.1 为什么先清梯度再反向传播</h3><p>PyTorch 默认累加梯度。若不清理：</p><pre class="language-python" data-language="python"><code class="language-python">loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>会把当前梯度加到上一步的 <code>.grad</code> 中。</p><p>标准单步更新顺序是：</p><pre class="language-text" data-language="text"><code class="language-text">forward-> loss-> zero_grad-> backward-> clip_grad_norm-> optimizer.step</code></pre><p><code>set_to_none=True</code> 让梯度变回 <code>None</code>，通常比逐元素填 0 更节省一些操作。</p><h3 id="21-2-梯度裁剪做了什么"><a href="#21-2-梯度裁剪做了什么" class="headerlink" title="21.2 梯度裁剪做了什么"></a>21.2 梯度裁剪做了什么</h3><p>如果所有参数梯度的总范数为：</p><p>$$<br>|g|_2<br>&#x3D;\sqrt{\sum_i|g_i|_2^2}<br>$$</p><p>当它超过阈值 $c$ 时，统一缩放：</p><p>$$<br>g_i\leftarrow g_i\frac{c}{|g|_2}<br>$$</p><p>这样保持梯度方向大致不变，只限制单次更新幅度。</p><h2 id="22-正确评估训练集与验证集"><a href="#22-正确评估训练集与验证集" class="headerlink" title="22. 正确评估训练集与验证集"></a>22. 正确评估训练集与验证集</h2><p>评估时要关闭 Dropout 和梯度：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token decorator annotation punctuation">@torch<span class="token punctuation">.</span>no_grad</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">def</span> <span class="token function">estimate_loss</span><span class="token punctuation">(</span>eval_iters<span class="token operator">=</span><span class="token number">20</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    result <span class="token operator">=</span> <span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span>    <span class="token keyword">for</span> split <span class="token keyword">in</span> <span class="token punctuation">(</span><span class="token string">"train"</span><span class="token punctuation">,</span> <span class="token string">"val"</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        losses <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>        <span class="token keyword">for</span> _ <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span>eval_iters<span class="token punctuation">)</span><span class="token punctuation">:</span>            x<span class="token punctuation">,</span> y <span class="token operator">=</span> get_batch<span class="token punctuation">(</span>split<span class="token punctuation">)</span>            _<span class="token punctuation">,</span> loss <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">,</span> y<span class="token punctuation">)</span>            losses<span class="token punctuation">.</span>append<span class="token punctuation">(</span>loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>        result<span class="token punctuation">[</span>split<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token builtin">sum</span><span class="token punctuation">(</span>losses<span class="token punctuation">)</span> <span class="token operator">/</span> <span class="token builtin">len</span><span class="token punctuation">(</span>losses<span class="token punctuation">)</span>    model<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">return</span> result</code></pre><p>为什么平均多个 batch？单个随机窗口的损失波动可能较大，多次采样平均更能反映趋势。</p><p>实测 1000 步结果：</p><pre class="language-text" data-language="text"><code class="language-text">step=200,  train=0.0191, val=0.0187step=400,  train=0.0156, val=0.0157step=600,  train=0.0124, val=0.0130step=800,  train=0.0118, val=0.0127step=1000, train=0.0115, val=0.0115</code></pre><p>这个损失看起来很低，主要因为教学语料只有 14 行，并被重复了 120 次。模型很容易记住其中模式。</p><p>而且这里是在重复之后再按位置切分，训练集和验证集包含相同句式。因此验证损失主要用于检查训练流程，并不能衡量对陌生文本的真正泛化。正式实验应先准备互不重复的训练语料与验证语料，再分别编码。</p><p>因此这个结果说明：</p><ul><li>数据构造和目标错位是正确的。</li><li>因果注意力能够学习局部与长距离关系。</li><li>完整训练链路已经跑通。</li></ul><p>它不说明：</p><ul><li>模型理解了开放世界知识。</li><li>模型能对陌生主题稳定泛化。</li><li>重复小语料是训练真实语言模型的合理方案。</li></ul><h2 id="23-自回归生成的基本过程"><a href="#23-自回归生成的基本过程" class="headerlink" title="23. 自回归生成的基本过程"></a>23. 自回归生成的基本过程</h2><p>训练完成后，给模型一个提示词：</p><pre class="language-text" data-language="text"><code class="language-text">春天</code></pre><p>生成流程是：</p><pre class="language-text" data-language="text"><code class="language-text">春天       -> 预测“来”春天来     -> 预测“了”春天来了   -> 预测“，”春天来了， -> 预测“花”...</code></pre><p>每次只使用最后一个位置的 logits：</p><pre class="language-python" data-language="python"><code class="language-python">logits <span class="token operator">=</span> logits<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token punctuation">]</span></code></pre><p>因为前面位置对应的是“过去各位置的下一个 token 分布”，我们现在只关心当前完整前缀之后是什么。</p><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-generation.svg" alt="Mini-GPT自回归生成"></p><h2 id="24-Temperature、贪心与随机采样"><a href="#24-Temperature、贪心与随机采样" class="headerlink" title="24. Temperature、贪心与随机采样"></a>24. Temperature、贪心与随机采样</h2><p>模型输出 logits $z$ 后，可以通过 Temperature 调整分布：</p><p>$$<br>p_i&#x3D;\frac{\exp(z_i&#x2F;\tau)}<br>{\sum_j\exp(z_j&#x2F;\tau)}<br>$$</p><p>其中 $\tau$ 是 temperature。</p><h3 id="24-1-Temperature-小于-1"><a href="#24-1-Temperature-小于-1" class="headerlink" title="24.1 Temperature 小于 1"></a>24.1 Temperature 小于 1</h3><pre class="language-text" data-language="text"><code class="language-text">temperature = 0.7</code></pre><p>logits 被放大，概率分布更尖锐，模型更偏向高概率 token，输出通常更稳定但更保守。</p><h3 id="24-2-Temperature-大于-1"><a href="#24-2-Temperature-大于-1" class="headerlink" title="24.2 Temperature 大于 1"></a>24.2 Temperature 大于 1</h3><pre class="language-text" data-language="text"><code class="language-text">temperature = 1.3</code></pre><p>logits 被缩小，概率分布更平坦，低概率 token 更容易被采到，输出更多样但更容易混乱。</p><h3 id="24-3-Temperature-不能为-0"><a href="#24-3-Temperature-不能为-0" class="headerlink" title="24.3 Temperature 不能为 0"></a>24.3 Temperature 不能为 0</h3><p>因为代码要计算：</p><pre class="language-python" data-language="python"><code class="language-python">logits <span class="token operator">=</span> logits <span class="token operator">/</span> temperature</code></pre><p>如果希望完全确定性地选最大概率 token，应直接使用：</p><pre class="language-python" data-language="python"><code class="language-python">next_token <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> keepdim<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span></code></pre><p>而不是把 temperature 设为 0。</p><h2 id="25-Top-k-采样"><a href="#25-Top-k-采样" class="headerlink" title="25. Top-k 采样"></a>25. Top-k 采样</h2><p>Top-k 只保留概率最高的 $k$ 个候选，其余 logits 设为 $-\infty$：</p><pre class="language-python" data-language="python"><code class="language-python">k <span class="token operator">=</span> <span class="token builtin">min</span><span class="token punctuation">(</span>top_k<span class="token punctuation">,</span> logits<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span>threshold <span class="token operator">=</span> torch<span class="token punctuation">.</span>topk<span class="token punctuation">(</span>    logits<span class="token punctuation">,</span>    k<span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">.</span>values<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">]</span>logits <span class="token operator">=</span> logits<span class="token punctuation">.</span>masked_fill<span class="token punctuation">(</span>    logits <span class="token operator">&lt;</span> threshold<span class="token punctuation">,</span>    <span class="token builtin">float</span><span class="token punctuation">(</span><span class="token string">"-inf"</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>再计算概率并采样：</p><pre class="language-python" data-language="python"><code class="language-python">probs <span class="token operator">=</span> F<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>next_token <span class="token operator">=</span> torch<span class="token punctuation">.</span>multinomial<span class="token punctuation">(</span>    probs<span class="token punctuation">,</span>    num_samples<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>Top-k 的作用是避免从词表长尾中采到极低概率 token。</p><table><thead><tr><th>策略</th><th>做法</th><th>特点</th></tr></thead><tbody><tr><td>Greedy</td><td>每次选最大概率</td><td>稳定、确定，但可能重复</td></tr><tr><td>Temperature</td><td>调整分布尖锐程度</td><td>控制保守与随机程度</td></tr><tr><td>Top-k</td><td>只保留前 $k$ 个</td><td>去除低概率长尾</td></tr><tr><td>Top-p</td><td>保留累计概率达到 $p$ 的集合</td><td>候选数量动态变化</td></tr></tbody></table><h2 id="26-完整生成函数"><a href="#26-完整生成函数" class="headerlink" title="26. 完整生成函数"></a>26. 完整生成函数</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token decorator annotation punctuation">@torch<span class="token punctuation">.</span>no_grad</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">def</span> <span class="token function">generate</span><span class="token punctuation">(</span>    self<span class="token punctuation">,</span>    idx<span class="token punctuation">,</span>    max_new_tokens<span class="token punctuation">,</span>    temperature<span class="token operator">=</span><span class="token number">1.0</span><span class="token punctuation">,</span>    top_k<span class="token operator">=</span><span class="token boolean">None</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    self<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> temperature <span class="token operator">&lt;=</span> <span class="token number">0</span><span class="token punctuation">:</span>        <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span><span class="token string">"temperature must be greater than 0"</span><span class="token punctuation">)</span>    <span class="token keyword">for</span> _ <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span>max_new_tokens<span class="token punctuation">)</span><span class="token punctuation">:</span>        idx_cond <span class="token operator">=</span> idx<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token operator">-</span>self<span class="token punctuation">.</span>config<span class="token punctuation">.</span>block_size<span class="token punctuation">:</span><span class="token punctuation">]</span>        logits<span class="token punctuation">,</span> _ <span class="token operator">=</span> self<span class="token punctuation">(</span>idx_cond<span class="token punctuation">)</span>        logits <span class="token operator">=</span> logits<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token punctuation">]</span> <span class="token operator">/</span> temperature        <span class="token keyword">if</span> top_k <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token boolean">None</span><span class="token punctuation">:</span>            k <span class="token operator">=</span> <span class="token builtin">min</span><span class="token punctuation">(</span>top_k<span class="token punctuation">,</span> logits<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span>            threshold <span class="token operator">=</span> torch<span class="token punctuation">.</span>topk<span class="token punctuation">(</span>                logits<span class="token punctuation">,</span>                k<span class="token punctuation">,</span>            <span class="token punctuation">)</span><span class="token punctuation">.</span>values<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">]</span>            logits <span class="token operator">=</span> logits<span class="token punctuation">.</span>masked_fill<span class="token punctuation">(</span>                logits <span class="token operator">&lt;</span> threshold<span class="token punctuation">,</span>                <span class="token builtin">float</span><span class="token punctuation">(</span><span class="token string">"-inf"</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            <span class="token punctuation">)</span>        probs <span class="token operator">=</span> F<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        next_token <span class="token operator">=</span> torch<span class="token punctuation">.</span>multinomial<span class="token punctuation">(</span>            probs<span class="token punctuation">,</span>            num_samples<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>        idx <span class="token operator">=</span> torch<span class="token punctuation">.</span>cat<span class="token punctuation">(</span><span class="token punctuation">(</span>idx<span class="token punctuation">,</span> next_token<span class="token punctuation">)</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>    <span class="token keyword">return</span> idx</code></pre><p>调用：</p><pre class="language-python" data-language="python"><code class="language-python">prompt <span class="token operator">=</span> <span class="token string">"春天"</span>context <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span>    <span class="token punctuation">[</span>encode<span class="token punctuation">(</span>prompt<span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">long</span><span class="token punctuation">,</span>    device<span class="token operator">=</span>device<span class="token punctuation">,</span><span class="token punctuation">)</span>result <span class="token operator">=</span> model<span class="token punctuation">.</span>generate<span class="token punctuation">(</span>    context<span class="token punctuation">,</span>    max_new_tokens<span class="token operator">=</span><span class="token number">100</span><span class="token punctuation">,</span>    temperature<span class="token operator">=</span><span class="token number">0.8</span><span class="token punctuation">,</span>    top_k<span class="token operator">=</span><span class="token number">8</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>decode<span class="token punctuation">(</span>result<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">.</span>tolist<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>一次实测输出为：</p><pre class="language-text" data-language="text"><code class="language-text">春天来了，花儿在风中开放。夏天来了，蝉儿在树上歌唱。秋天来了，果实在枝头成熟。冬天来了，雪花在天空飞舞。清晨，太阳从东方升起。傍晚，月亮在云间出现。小河穿过安静的村庄。微风吹过绿色的田野。</code></pre><p>它基本复现了训练语料。这再次说明模型学会并记忆了语料模式，不应把它误解为开放式创作能力。</p><p>本例词表没有单独定义 <code>&lt;EOS&gt;</code>，因此使用 <code>max_new_tokens</code> 强制结束生成。需要让模型自主停止时，应把 <code>&lt;EOS&gt;</code> 加入词表和训练文本，并在采到它时退出循环。</p><h3 id="26-1-为什么每次截取最后-block-size-个-token"><a href="#26-1-为什么每次截取最后-block-size-个-token" class="headerlink" title="26.1 为什么每次截取最后 block_size 个 token"></a>26.1 为什么每次截取最后 <code>block_size</code> 个 token</h3><p>位置 Embedding 只定义了：</p><pre class="language-text" data-language="text"><code class="language-text">0, 1, ..., block_size - 1</code></pre><p>如果输入无限增长，既会超过位置表范围，也会让注意力矩阵持续增大。</p><pre class="language-python" data-language="python"><code class="language-python">idx_cond <span class="token operator">=</span> idx<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token operator">-</span>self<span class="token punctuation">.</span>config<span class="token punctuation">.</span>block_size<span class="token punctuation">:</span><span class="token punctuation">]</span></code></pre><p>表示模型始终只看最近的上下文窗口。更早的 token 会被遗忘。</p><h2 id="27-训练与生成为什么一个并行、一个串行"><a href="#27-训练与生成为什么一个并行、一个串行" class="headerlink" title="27. 训练与生成为什么一个并行、一个串行"></a>27. 训练与生成为什么一个并行、一个串行</h2><p>训练时完整目标已知：</p><pre class="language-text" data-language="text"><code class="language-text">输入：春 天 来 了目标：天 来 了 ，</code></pre><p>利用 Causal Mask，可以一次并行计算所有位置。</p><p>生成时未来 token 不存在：</p><pre class="language-text" data-language="text"><code class="language-text">先生成 token t才能把它接回输入再生成 token t+1</code></pre><p>所以自回归生成在 token 维度上天然串行。</p><p>这不意味着生成内部没有并行：</p><ul><li>batch 中多个请求可以并行。</li><li>每层的大矩阵乘法可以并行。</li><li>多个注意力头可以并行。</li></ul><p>但同一条序列的第 $t+1$ 个 token 必须等待第 $t$ 个 token 产生。</p><h2 id="28-KV-Cache：为什么生成不必重复计算全部历史"><a href="#28-KV-Cache：为什么生成不必重复计算全部历史" class="headerlink" title="28. KV Cache：为什么生成不必重复计算全部历史"></a>28. KV Cache：为什么生成不必重复计算全部历史</h2><p>当前教学版 <code>generate</code> 每一步都把最近的完整上下文重新传入模型。</p><p>假设已经有：</p><pre class="language-text" data-language="text"><code class="language-text">x1 x2 x3 x4</code></pre><p>生成 <code>x5</code> 时计算了前四个位置的 K、V。接下来生成 <code>x6</code> 时，<code>x1</code> 到 <code>x4</code> 的 K、V 没有变化，却又被计算一遍。</p><p>KV Cache 会保存每层历史 token 的 Key 和 Value：</p><pre class="language-text" data-language="text"><code class="language-text">第 1 层：K_cache¹、V_cache¹第 2 层：K_cache²、V_cache²...第 L 层：K_cacheᴸ、V_cacheᴸ</code></pre><p>新 token 到来时，只计算它自己的：</p><p>$$<br>q_t, k_t, v_t<br>$$</p><p>然后追加缓存：</p><p>$$<br>K_{1:t}&#x3D;\operatorname{Concat}(K_{1:t-1},k_t)<br>$$</p><p>$$<br>V_{1:t}&#x3D;\operatorname{Concat}(V_{1:t-1},v_t)<br>$$</p><p>当前 Query 与全部缓存 Key 计算：</p><p>$$<br>\operatorname{Attention}<br>(q_t,K_{1:t},V_{1:t})<br>$$</p><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-kv-cache.svg" alt="KV Cache生成流程"></p><h3 id="28-1-KV-Cache-改变了什么"><a href="#28-1-KV-Cache-改变了什么" class="headerlink" title="28.1 KV Cache 改变了什么"></a>28.1 KV Cache 改变了什么</h3><p>没有缓存：</p><pre class="language-text" data-language="text"><code class="language-text">每一步重新计算所有历史 token 的 Q、K、V 和 Block 输出</code></pre><p>有缓存：</p><pre class="language-text" data-language="text"><code class="language-text">每一步主要计算新 token，并复用历史 K、V</code></pre><p>KV Cache 显著减少重复计算，但会占用额外显存。缓存规模与以下量近似成正比：</p><p>$$<br>2\times L\times B\times H\times T\times D<br>$$</p><p>其中前面的 2 代表 Key 和 Value。</p><p>如果缓存元素使用 $s$ 字节，显存近似为：</p><p>$$<br>M_{KV}<br>\approx2LBHTDs<br>$$</p><p>因为 $H\times D&#x3D;C$，也可以写成：</p><p>$$<br>M_{KV}\approx2LBTCs<br>$$</p><p>上下文越长、层数越多、batch 越大，KV Cache 越占显存。</p><h3 id="28-2-为什么通常不缓存-Query"><a href="#28-2-为什么通常不缓存-Query" class="headerlink" title="28.2 为什么通常不缓存 Query"></a>28.2 为什么通常不缓存 Query</h3><p>历史 Query 只在对应历史位置产生输出时使用。生成当前 token 时，只需要当前 Query 去匹配所有历史 Key，因此长期保存历史 Query 通常没有必要。</p><h2 id="29-用-PyTorch-高效-Attention-替换手写矩阵"><a href="#29-用-PyTorch-高效-Attention-替换手写矩阵" class="headerlink" title="29. 用 PyTorch 高效 Attention 替换手写矩阵"></a>29. 用 PyTorch 高效 Attention 替换手写矩阵</h2><p>为了教学，我们显式创建了 <code>[B,H,T,T]</code> 分数矩阵。理解原理后，可以使用：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> torch<span class="token punctuation">.</span>nn <span class="token keyword">import</span> functional <span class="token keyword">as</span> Fy <span class="token operator">=</span> F<span class="token punctuation">.</span>scaled_dot_product_attention<span class="token punctuation">(</span>    q<span class="token punctuation">,</span>    k<span class="token punctuation">,</span>    v<span class="token punctuation">,</span>    attn_mask<span class="token operator">=</span><span class="token boolean">None</span><span class="token punctuation">,</span>    dropout_p<span class="token operator">=</span><span class="token punctuation">(</span>        self<span class="token punctuation">.</span>attn_dropout<span class="token punctuation">.</span>p        <span class="token keyword">if</span> self<span class="token punctuation">.</span>training        <span class="token keyword">else</span> <span class="token number">0.0</span>    <span class="token punctuation">)</span><span class="token punctuation">,</span>    is_causal<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>它仍然计算同一个核心公式，但 PyTorch 会根据设备与输入选择更合适的实现。</p><p>需要注意两个细节：</p><ol><li>使用 <code>is_causal=True</code> 时，不要再同时传普通 <code>attn_mask</code>。</li><li>函数式接口不会自动根据 <code>model.eval()</code> 关闭 Dropout，所以推理时要显式传 <code>dropout_p=0.0</code>。</li></ol><p>Mask 的布尔语义也要看具体 API：本篇手写代码中 <code>1/True</code> 表示允许查看；上一篇 <code>nn.Transformer</code> 的布尔屏蔽 Mask 中 <code>True</code> 常表示禁止查看。不要凭记忆混用，应检查所调用函数的文档。</p><h2 id="30-如何验证因果-Mask-真的有效"><a href="#30-如何验证因果-Mask-真的有效" class="headerlink" title="30. 如何验证因果 Mask 真的有效"></a>30. 如何验证因果 Mask 真的有效</h2><p>只看训练损失下降还不够。可以构造一个针对未来信息泄漏的测试。</p><p>先取一段前缀：</p><pre class="language-python" data-language="python"><code class="language-python">prefix <span class="token operator">=</span> x<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token number">20</span><span class="token punctuation">]</span></code></pre><p>只修改第 15 个位置之后的 token：</p><pre class="language-python" data-language="python"><code class="language-python">changed <span class="token operator">=</span> prefix<span class="token punctuation">.</span>clone<span class="token punctuation">(</span><span class="token punctuation">)</span>changed<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">15</span><span class="token punctuation">:</span><span class="token punctuation">]</span> <span class="token operator">=</span> torch<span class="token punctuation">.</span>randint<span class="token punctuation">(</span>    <span class="token number">0</span><span class="token punctuation">,</span>    config<span class="token punctuation">.</span>vocab_size<span class="token punctuation">,</span>    changed<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">15</span><span class="token punctuation">:</span><span class="token punctuation">]</span><span class="token punctuation">.</span>shape<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>比较前 15 个位置的 logits：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span>base_logits<span class="token punctuation">,</span> _ <span class="token operator">=</span> model<span class="token punctuation">(</span>prefix<span class="token punctuation">)</span>changed_logits<span class="token punctuation">,</span> _ <span class="token operator">=</span> model<span class="token punctuation">(</span>changed<span class="token punctuation">)</span>max_diff <span class="token operator">=</span> <span class="token punctuation">(</span>    base_logits<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token number">15</span><span class="token punctuation">]</span>    <span class="token operator">-</span> changed_logits<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token number">15</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token builtin">abs</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token builtin">max</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>max_diff<span class="token punctuation">)</span></code></pre><p>实测结果：</p><pre class="language-text" data-language="text"><code class="language-text">tensor(0.)</code></pre><p>这说明修改未来 token 不会影响过去位置的输出。</p><p>注意测试时必须调用 <code>model.eval()</code>。如果 Dropout 仍然开启，两次前向传播即使输入相同也可能有随机差异。</p><h2 id="31-常见错误"><a href="#31-常见错误" class="headerlink" title="31. 常见错误"></a>31. 常见错误</h2><h3 id="31-1-输入和目标没有错开一位"><a href="#31-1-输入和目标没有错开一位" class="headerlink" title="31.1 输入和目标没有错开一位"></a>31.1 输入和目标没有错开一位</h3><p>错误：</p><pre class="language-text" data-language="text"><code class="language-text">x = t0 t1 t2 t3y = t0 t1 t2 t3</code></pre><p>模型只需复制当前 token，不是在预测下一个 token。</p><p>正确：</p><pre class="language-text" data-language="text"><code class="language-text">x = t0 t1 t2 t3y = t1 t2 t3 t4</code></pre><h3 id="31-2-没有-Causal-Mask"><a href="#31-2-没有-Causal-Mask" class="headerlink" title="31.2 没有 Causal Mask"></a>31.2 没有 Causal Mask</h3><p>模型训练时能直接看到未来答案，损失会异常漂亮，但生成时完全失效。</p><h3 id="31-3-Softmax-维度写错"><a href="#31-3-Softmax-维度写错" class="headerlink" title="31.3 Softmax 维度写错"></a>31.3 Softmax 维度写错</h3><pre class="language-python" data-language="python"><code class="language-python">weights <span class="token operator">=</span> F<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>scores<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>最后一维是 Key 位置。每个 Query 对所有 Key 的权重之和应为 1。</p><h3 id="31-4-缩放用了-n-embd"><a href="#31-4-缩放用了-n-embd" class="headerlink" title="31.4 缩放用了 n_embd"></a>31.4 缩放用了 <code>n_embd</code></h3><p>错误：</p><pre class="language-python" data-language="python"><code class="language-python">scores <span class="token operator">/=</span> math<span class="token punctuation">.</span>sqrt<span class="token punctuation">(</span>config<span class="token punctuation">.</span>n_embd<span class="token punctuation">)</span></code></pre><p>正确：</p><pre class="language-python" data-language="python"><code class="language-python">scores <span class="token operator">/=</span> math<span class="token punctuation">.</span>sqrt<span class="token punctuation">(</span>self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span></code></pre><p>点积发生在每个头的 $D&#x3D;head_dim$ 维空间中。</p><h3 id="31-5-合并头时忘记转置"><a href="#31-5-合并头时忘记转置" class="headerlink" title="31.5 合并头时忘记转置"></a>31.5 合并头时忘记转置</h3><p><code>[B,H,T,D]</code> 不能直接当成 <code>[B,T,C]</code>。必须先把 <code>T</code> 移回第二维：</p><pre class="language-python" data-language="python"><code class="language-python">y <span class="token operator">=</span> y<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">.</span>contiguous<span class="token punctuation">(</span><span class="token punctuation">)</span>y <span class="token operator">=</span> y<span class="token punctuation">.</span>view<span class="token punctuation">(</span>b<span class="token punctuation">,</span> t<span class="token punctuation">,</span> c<span class="token punctuation">)</span></code></pre><h3 id="31-6-使用普通-list-保存-Block"><a href="#31-6-使用普通-list-保存-Block" class="headerlink" title="31.6 使用普通 list 保存 Block"></a>31.6 使用普通 list 保存 Block</h3><p>错误：</p><pre class="language-python" data-language="python"><code class="language-python">self<span class="token punctuation">.</span>blocks <span class="token operator">=</span> <span class="token punctuation">[</span>Block<span class="token punctuation">(</span>config<span class="token punctuation">)</span> <span class="token keyword">for</span> _ <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span>n_layer<span class="token punctuation">)</span><span class="token punctuation">]</span></code></pre><p>正确：</p><pre class="language-python" data-language="python"><code class="language-python">self<span class="token punctuation">.</span>blocks <span class="token operator">=</span> nn<span class="token punctuation">.</span>ModuleList<span class="token punctuation">(</span><span class="token punctuation">[</span>    Block<span class="token punctuation">(</span>config<span class="token punctuation">)</span> <span class="token keyword">for</span> _ <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span>n_layer<span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">)</span></code></pre><h3 id="31-7-block-size-与输入长度不匹配"><a href="#31-7-block-size-与输入长度不匹配" class="headerlink" title="31.7 block_size 与输入长度不匹配"></a>31.7 <code>block_size</code> 与输入长度不匹配</h3><p>输入长度超过位置 Embedding 范围会报错。训练数据和生成函数都必须遵守最大上下文长度。</p><h3 id="31-8-训练时忘记-model-train"><a href="#31-8-训练时忘记-model-train" class="headerlink" title="31.8 训练时忘记 model.train()"></a>31.8 训练时忘记 <code>model.train()</code></h3><p>Dropout 会保持关闭，训练行为与预期不一致。</p><h3 id="31-9-生成时忘记-model-eval"><a href="#31-9-生成时忘记-model-eval" class="headerlink" title="31.9 生成时忘记 model.eval()"></a>31.9 生成时忘记 <code>model.eval()</code></h3><p>Dropout 会使同一前缀的 logits 额外波动，也浪费不必要的随机计算。</p><h3 id="31-10-Temperature-设为-0"><a href="#31-10-Temperature-设为-0" class="headerlink" title="31.10 Temperature 设为 0"></a>31.10 Temperature 设为 0</h3><p>会发生除零。需要确定性生成时使用 <code>argmax</code>。</p><h3 id="31-11-Top-k-大于词表大小"><a href="#31-11-Top-k-大于词表大小" class="headerlink" title="31.11 Top-k 大于词表大小"></a>31.11 Top-k 大于词表大小</h3><p>应先限制：</p><pre class="language-python" data-language="python"><code class="language-python">k <span class="token operator">=</span> <span class="token builtin">min</span><span class="token punctuation">(</span>top_k<span class="token punctuation">,</span> logits<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><h3 id="31-12-把小语料低损失当成智能"><a href="#31-12-把小语料低损失当成智能" class="headerlink" title="31.12 把小语料低损失当成智能"></a>31.12 把小语料低损失当成智能</h3><p>重复数据极易记忆。要观察泛化，应使用独立且不重复的验证文本，并检查模型对未见组合的预测。</p><h2 id="32-完整模型代码如何组织"><a href="#32-完整模型代码如何组织" class="headerlink" title="32. 完整模型代码如何组织"></a>32. 完整模型代码如何组织</h2><p>完整可运行脚本：<a href="https://lsworl.github.io/code/nlp-blog/mini_gpt.py">&#x2F;code&#x2F;nlp-blog&#x2F;mini_gpt.py</a></p><p>推荐按下面的依赖顺序放在一个脚本中：</p><pre class="language-text" data-language="text"><code class="language-text">1. import 与随机种子2. 准备语料3. encode / decode4. train / val 切分5. GPTConfig6. get_batch7. CausalSelfAttention8. MLP9. Block10. MiniGPT11. optimizer 与 get_lr12. estimate_loss13. 训练循环14. generate15. 保存模型</code></pre><p>保存 checkpoint：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> dataclasses <span class="token keyword">import</span> asdicttorch<span class="token punctuation">.</span>save<span class="token punctuation">(</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"model"</span><span class="token punctuation">:</span> model<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"optimizer"</span><span class="token punctuation">:</span> optimizer<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"config"</span><span class="token punctuation">:</span> asdict<span class="token punctuation">(</span>config<span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"stoi"</span><span class="token punctuation">:</span> stoi<span class="token punctuation">,</span>        <span class="token string">"itos"</span><span class="token punctuation">:</span> itos<span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token string">"mini_gpt.pt"</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>恢复：</p><pre class="language-python" data-language="python"><code class="language-python">checkpoint <span class="token operator">=</span> torch<span class="token punctuation">.</span>load<span class="token punctuation">(</span>    <span class="token string">"mini_gpt.pt"</span><span class="token punctuation">,</span>    map_location<span class="token operator">=</span>device<span class="token punctuation">,</span>    weights_only<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span>config <span class="token operator">=</span> GPTConfig<span class="token punctuation">(</span><span class="token operator">**</span>checkpoint<span class="token punctuation">[</span><span class="token string">"config"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>model <span class="token operator">=</span> MiniGPT<span class="token punctuation">(</span>config<span class="token punctuation">)</span><span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span>model<span class="token punctuation">.</span>load_state_dict<span class="token punctuation">(</span>checkpoint<span class="token punctuation">[</span><span class="token string">"model"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>decay <span class="token operator">=</span> <span class="token punctuation">[</span>    p <span class="token keyword">for</span> p <span class="token keyword">in</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> p<span class="token punctuation">.</span>requires_grad <span class="token keyword">and</span> p<span class="token punctuation">.</span>dim<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">>=</span> <span class="token number">2</span><span class="token punctuation">]</span>no_decay <span class="token operator">=</span> <span class="token punctuation">[</span>    p <span class="token keyword">for</span> p <span class="token keyword">in</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> p<span class="token punctuation">.</span>requires_grad <span class="token keyword">and</span> p<span class="token punctuation">.</span>dim<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">&lt;</span> <span class="token number">2</span><span class="token punctuation">]</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token string">"params"</span><span class="token punctuation">:</span> decay<span class="token punctuation">,</span> <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">0.1</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token string">"params"</span><span class="token punctuation">:</span> no_decay<span class="token punctuation">,</span> <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">0.0</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">3e-3</span><span class="token punctuation">,</span> betas<span class="token operator">=</span><span class="token punctuation">(</span><span class="token number">0.9</span><span class="token punctuation">,</span> <span class="token number">0.95</span><span class="token punctuation">)</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>load_state_dict<span class="token punctuation">(</span>checkpoint<span class="token punctuation">[</span><span class="token string">"optimizer"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>stoi <span class="token operator">=</span> checkpoint<span class="token punctuation">[</span><span class="token string">"stoi"</span><span class="token punctuation">]</span>itos <span class="token operator">=</span> checkpoint<span class="token punctuation">[</span><span class="token string">"itos"</span><span class="token punctuation">]</span></code></pre><p>Tokenizer 词表必须与训练时一致，否则相同 token id 会被解释成不同字符。</p><h2 id="33-Mini-GPT-与真实大模型还差什么"><a href="#33-Mini-GPT-与真实大模型还差什么" class="headerlink" title="33. Mini-GPT 与真实大模型还差什么"></a>33. Mini-GPT 与真实大模型还差什么</h2><p>Mini-GPT 已经包含 GPT 的核心计算骨架，但与真实系统仍有巨大差距。</p><table><thead><tr><th>方面</th><th>本文 Mini-GPT</th><th>真实语言模型</th></tr></thead><tbody><tr><td>Tokenizer</td><td>字符级</td><td>BPE、字节或子词 Tokenizer</td></tr><tr><td>参数量</td><td>35 万</td><td>数亿到更大规模</td></tr><tr><td>数据</td><td>少量重复句子</td><td>大规模清洗语料</td></tr><tr><td>上下文</td><td>64 token</td><td>通常远长于 64</td></tr><tr><td>位置编码</td><td>可学习绝对位置</td><td>也可能使用相对位置、RoPE 等</td></tr><tr><td>Attention</td><td>标准 MHA</td><td>还可能使用 MQA、GQA 等</td></tr><tr><td>训练</td><td>单机 CPU 教学</td><td>大规模分布式训练</td></tr><tr><td>精度</td><td>FP32</td><td>BF16、FP16 或混合精度</td></tr><tr><td>推理</td><td>重算上下文</td><td>KV Cache、批处理与高效内核</td></tr><tr><td>能力塑造</td><td>next-token 训练</td><td>还可能经过指令微调与偏好对齐</td></tr></tbody></table><p>规模扩大不会改变最核心的目标：</p><p>$$<br>P(x_t\mid x_{&lt;t})<br>$$</p><p>但数据、计算、训练稳定性和推理工程会变得复杂得多。</p><h2 id="34-总结"><a href="#34-总结" class="headerlink" title="34. 总结"></a>34. 总结</h2><p>Mini-GPT 的完整数据流是：</p><pre class="language-text" data-language="text"><code class="language-text">原始文本  -> Tokenizer  -> token id  -> 随机上下文窗口 x 与右移目标 y  -> Token Embedding + Position Embedding  -> 多层 Pre-Norm GPT Block       -> Causal Multi-Head Self-Attention       -> Residual Connection       -> MLP + GELU       -> Residual Connection  -> Final LayerNorm  -> LM Head  -> 每个位置的词表 logits  -> Cross Entropy  -> Backpropagation + AdamW</code></pre><p>生成流程是：</p><pre class="language-text" data-language="text"><code class="language-text">提示 token  -> 取最后 block_size 个 token  -> 模型预测最后位置的 logits  -> Temperature / Top-k  -> 采样下一个 token  -> 拼接回输入  -> 重复</code></pre><p>需要真正掌握的核心包括：</p><ul><li>GPT 是不含 Cross-Attention 的 Decoder-only 自回归 Transformer。</li><li>语言模型通过右移一位的输入和目标学习 next-token prediction。</li><li>Causal Mask 让并行训练不会泄漏未来信息。</li><li>QKV 投影后要正确拆头、计算注意力并合并头。</li><li>GPT Block 由 Causal Attention、MLP、LayerNorm 和残差连接组成。</li><li>LM Head 把每个隐藏向量投影成词表 logits。</li><li>交叉熵等价于最小化正确 token 的负对数似然。</li><li>Temperature 和 Top-k 只改变生成策略，不改变模型参数。</li><li>KV Cache 复用历史 Key 和 Value，以显存换取生成速度。</li><li>小数据上的低损失很可能来自记忆，不能直接等同于泛化能力。</li></ul><p>当你能解释下面三条公式之间的关系时，就已经抓住了 GPT 的数学主线：</p><p>$$<br>P(x_1,\ldots,x_T)<br>&#x3D;\prod_{t&#x3D;1}^{T}P(x_t\mid x_{&lt;t})<br>$$</p><p>$$<br>\operatorname{CausalAttention}(Q,K,V)<br>&#x3D;\operatorname{softmax}\left(<br>\frac{QK^T}{\sqrt{d_k}}+M<br>\right)V<br>$$</p><p>$$<br>\mathcal{L}<br>&#x3D;-\frac{1}{BT}<br>\sum_{b,t}<br>\log P(y_{b,t}\mid x_{b,\le t})<br>$$</p><p>它们分别回答：</p><ol><li>GPT 要建模什么概率。</li><li>GPT 怎样从历史 token 中提取上下文。</li><li>GPT 怎样通过训练让正确下一个 token 的概率变大。</li></ol><h2 id="35-手动模拟完整流程：从-Prompt“春天”推测下一个-Token"><a href="#35-手动模拟完整流程：从-Prompt“春天”推测下一个-Token" class="headerlink" title="35. 手动模拟完整流程：从 Prompt“春天”推测下一个 Token"></a>35. 手动模拟完整流程：从 Prompt“春天”推测下一个 Token</h2><p>前面已经把 Tokenizer、Embedding、Causal Attention、MLP、LM Head 和采样分别讲过。最后使用一组可以手算的参数，把一次完整推理串起来：</p><pre class="language-text" data-language="text"><code class="language-text">输入 Prompt“春天”  -> 编码成 token id  -> Token Embedding + Position Embedding  -> Pre-Norm Causal Self-Attention  -> 第一次残差连接  -> Pre-Norm MLP  -> 第二次残差连接  -> Final LayerNorm  -> LM Head 得到 logits  -> Softmax 得到下一个 token 概率  -> 选择“来”  -> 新 Prompt 变成“春天来”</code></pre><p><img src="https://lsworl.github.io/images/nlp-blog/mini-gpt-prompt-next-token.svg" alt="从Prompt手算下一个Token"></p><h3 id="35-1-为什么需要缩小模型"><a href="#35-1-为什么需要缩小模型" class="headerlink" title="35.1 为什么需要缩小模型"></a>35.1 为什么需要缩小模型</h3><p>正文中的真实 Mini-GPT 使用：</p><pre class="language-text" data-language="text"><code class="language-text">词表大小 V = 123隐藏维度 C = 96注意力头数 H = 4Block 数量 L = 3MLP 中间维度 = 384</code></pre><p>真实模型一次前向传播会涉及数十万次乘加，无法在纸面逐项展开。因此手算示例保留完全相同的数据流，只缩小矩阵：</p><table><thead><tr><th>项目</th><th>正文模型</th><th>手算模型</th></tr></thead><tbody><tr><td>词表大小</td><td>123</td><td>4</td></tr><tr><td>隐藏维度</td><td>96</td><td>2</td></tr><tr><td>注意力头数</td><td>4</td><td>1</td></tr><tr><td>Block 数量</td><td>3</td><td>1</td></tr><tr><td>MLP 中间维度</td><td>384</td><td>8，即 $4C$</td></tr><tr><td>Dropout</td><td>0.1</td><td>推理时关闭</td></tr></tbody></table><p>这是一组为了展示计算过程而设计的“已训练参数快照”，不是正文 35 万参数模型训练后真实权重的截取。</p><h3 id="35-2-第一步：Tokenizer-把-Prompt-转成-ID"><a href="#35-2-第一步：Tokenizer-把-Prompt-转成-ID" class="headerlink" title="35.2 第一步：Tokenizer 把 Prompt 转成 ID"></a>35.2 第一步：Tokenizer 把 Prompt 转成 ID</h3><p>假设手算词表为：</p><table><thead><tr><th>token</th><th align="right">token id</th></tr></thead><tbody><tr><td><code>春</code></td><td align="right">0</td></tr><tr><td><code>天</code></td><td align="right">1</td></tr><tr><td><code>来</code></td><td align="right">2</td></tr><tr><td><code>了</code></td><td align="right">3</td></tr></tbody></table><p>输入 Prompt：</p><pre class="language-text" data-language="text"><code class="language-text">春天</code></pre><p>经过 <code>encode</code>：</p><p>$$<br>idx&#x3D;[0,1]<br>$$</p><p>加入 batch 维后，模型真正接收：</p><p>$$<br>idx&#x3D;<br>\begin{bmatrix}<br>0&amp;1<br>\end{bmatrix}<br>$$</p><p>形状为：</p><pre class="language-text" data-language="text"><code class="language-text">[B, T] = [1, 2]</code></pre><p>因为 $T&#x3D;2$ 没有超过 <code>block_size</code>，无需截断 Prompt。</p><h3 id="35-3-第二步：查询-Token-Embedding"><a href="#35-3-第二步：查询-Token-Embedding" class="headerlink" title="35.3 第二步：查询 Token Embedding"></a>35.3 第二步：查询 Token Embedding</h3><p>假设训练后的 Token Embedding 表为：</p><p>$$<br>E_{token}&#x3D;<br>\begin{bmatrix}<br>1&amp;0\<br>0&amp;1\<br>-1&amp;1\<br>1&amp;-1<br>\end{bmatrix}<br>$$</p><p>每一行分别对应“春、天、来、了”。</p><p>查表得到：</p><p>$$<br>E_{token}([0,1])&#x3D;<br>\begin{bmatrix}<br>1&amp;0\<br>0&amp;1<br>\end{bmatrix}<br>$$</p><p>此时形状为 <code>[1,2,2]</code>；为了让矩阵更容易阅读，后面的公式省略 batch 维。</p><h3 id="35-4-第三步：加入-Position-Embedding"><a href="#35-4-第三步：加入-Position-Embedding" class="headerlink" title="35.4 第三步：加入 Position Embedding"></a>35.4 第三步：加入 Position Embedding</h3><p>假设前两个位置的可学习位置向量为：</p><p>$$<br>E_{position}(0)&#x3D;[0.1,0]<br>$$</p><p>$$<br>E_{position}(1)&#x3D;[0,0.1]<br>$$</p><p>Token Embedding 与 Position Embedding 逐元素相加：</p><p>$$<br>X&#x3D;<br>\begin{bmatrix}<br>1&amp;0\<br>0&amp;1<br>\end{bmatrix}<br>+<br>\begin{bmatrix}<br>0.1&amp;0\<br>0&amp;0.1<br>\end{bmatrix}<br>&#x3D;<br>\begin{bmatrix}<br>1.1&amp;0\<br>0&amp;1.1<br>\end{bmatrix}<br>$$</p><p>第一行表示位置 0 的“春”，第二行表示位置 1 的“天”。</p><h3 id="35-5-第四步：Attention-前的-LayerNorm"><a href="#35-5-第四步：Attention-前的-LayerNorm" class="headerlink" title="35.5 第四步：Attention 前的 LayerNorm"></a>35.5 第四步：Attention 前的 LayerNorm</h3><p>为了简化，假设 LayerNorm 的可学习参数为：</p><p>$$<br>\gamma&#x3D;[1,1],\qquad\beta&#x3D;[0,0]<br>$$</p><p>并在手算中忽略很小的 $\epsilon$。</p><p>对“春”的向量 $[1.1,0]$：</p><p>$$<br>\mu&#x3D;\frac{1.1+0}{2}&#x3D;0.55<br>$$</p><p>$$<br>\sigma<br>&#x3D;\sqrt{\frac{(1.1-0.55)^2+(0-0.55)^2}{2}}<br>&#x3D;0.55<br>$$</p><p>所以：</p><p>$$<br>\operatorname{LN}([1.1,0])&#x3D;[1,-1]<br>$$</p><p>同理：</p><p>$$<br>\operatorname{LN}([0,1.1])&#x3D;[-1,1]<br>$$</p><p>得到：</p><p>$$<br>X_{norm}&#x3D;<br>\begin{bmatrix}<br>1&amp;-1\<br>-1&amp;1<br>\end{bmatrix}<br>$$</p><h3 id="35-6-第五步：线性投影得到-Q、K、V"><a href="#35-6-第五步：线性投影得到-Q、K、V" class="headerlink" title="35.6 第五步：线性投影得到 Q、K、V"></a>35.6 第五步：线性投影得到 Q、K、V</h3><p>真实模型会学习不同的 $W_Q$、$W_K$、$W_V$。为了手算，令：</p><p>$$<br>W_Q&#x3D;W_K&#x3D;W_V&#x3D;I, b_Q&#x3D;b_K&#x3D;b_V&#x3D;0<br>$$</p><p>于是：</p><p>$$<br>Q&#x3D;K&#x3D;V&#x3D;X_{norm}<br>&#x3D;<br>\begin{bmatrix}<br>1&amp;-1\<br>-1&amp;1<br>\end{bmatrix}<br>$$</p><p>因为只有一个注意力头：</p><pre class="language-text" data-language="text"><code class="language-text">H = 1D = C / H = 2</code></pre><p>所以拆头只增加 head 维，不改变每个向量中的两个数。</p><h3 id="35-7-第六步：计算缩放点积分数"><a href="#35-7-第六步：计算缩放点积分数" class="headerlink" title="35.7 第六步：计算缩放点积分数"></a>35.7 第六步：计算缩放点积分数</h3><p>先计算：</p><p>$$<br>QK^T&#x3D;<br>\begin{bmatrix}<br>1&amp;-1\<br>-1&amp;1<br>\end{bmatrix}<br>\begin{bmatrix}<br>1&amp;-1\<br>-1&amp;1<br>\end{bmatrix}<br>&#x3D;<br>\begin{bmatrix}<br>2&amp;-2\<br>-2&amp;2<br>\end{bmatrix}<br>$$</p><p>除以 $\sqrt{D}&#x3D;\sqrt{2}$：</p><p>$$<br>S&#x3D;\frac{QK^T}{\sqrt{2}}<br>\approx<br>\begin{bmatrix}<br>1.414&amp;-1.414\<br>-1.414&amp;1.414<br>\end{bmatrix}<br>$$</p><p>矩阵第 2 行表示 token“天”对“春”和“天”的匹配分数。</p><h3 id="35-8-第七步：应用-Causal-Mask"><a href="#35-8-第七步：应用-Causal-Mask" class="headerlink" title="35.8 第七步：应用 Causal Mask"></a>35.8 第七步：应用 Causal Mask</h3><p>长度为 2 的加法型因果 Mask 是：</p><p>$$<br>M&#x3D;<br>\begin{bmatrix}<br>0&amp;-\infty\<br>0&amp;0<br>\end{bmatrix}<br>$$</p><p>加到分数矩阵：</p><p>$$<br>S+M&#x3D;<br>\begin{bmatrix}<br>1.414&amp;-\infty\<br>-1.414&amp;1.414<br>\end{bmatrix}<br>$$</p><p>它表达：</p><ul><li>“春”只能看自己，不能看未来的“天”。</li><li>“天”可以同时看“春”和自己。</li></ul><p>当前要预测 Prompt 后面的 token，因此只关心最后一行。</p><h3 id="35-9-第八步：Softmax-得到注意力权重"><a href="#35-9-第八步：Softmax-得到注意力权重" class="headerlink" title="35.9 第八步：Softmax 得到注意力权重"></a>35.9 第八步：Softmax 得到注意力权重</h3><p>第一行只有一个可见位置：</p><p>$$<br>A_1&#x3D;[1,0]<br>$$</p><p>第二行：</p><p>$$<br>A_2<br>&#x3D;\operatorname{softmax}([-1.414,1.414])<br>$$</p><p>$$<br>A_2\approx[0.0558,0.9442]<br>$$</p><p>这表示计算“天”的新表示时：</p><ul><li>从“春”的 Value 取约 5.58%。</li><li>从“天”的 Value 取约 94.42%。</li></ul><p>完整权重矩阵：</p><p>$$<br>A\approx<br>\begin{bmatrix}<br>1&amp;0\<br>0.0558&amp;0.9442<br>\end{bmatrix}<br>$$</p><p>每一行之和都是 1。</p><h3 id="35-10-第九步：对-Value-加权求和"><a href="#35-10-第九步：对-Value-加权求和" class="headerlink" title="35.10 第九步：对 Value 加权求和"></a>35.10 第九步：对 Value 加权求和</h3><p>最后一个位置的 Attention 输出：</p><p>$$<br>o_2<br>&#x3D;0.0558[1,-1]<br>+0.9442[-1,1]<br>$$</p><p>$$<br>o_2\approx[-0.8884,0.8884]<br>$$</p><p>假设注意力输出投影 $W_O&#x3D;I$、bias 为 0，输出保持不变。</p><h3 id="35-11-第十步：第一次残差连接"><a href="#35-11-第十步：第一次残差连接" class="headerlink" title="35.11 第十步：第一次残差连接"></a>35.11 第十步：第一次残差连接</h3><p>Pre-Norm Block 的残差连接使用进入 LayerNorm 前的原始 $X$：</p><p>$$<br>u_2&#x3D;x_2+o_2<br>$$</p><p>$$<br>u_2<br>&#x3D;[0,1.1]+[-0.8884,0.8884]<br>$$</p><p>$$<br>u_2\approx[-0.8884,1.9884]<br>$$</p><p>这一点很重要：残差不是与 $\operatorname{LN}(x_2)$ 相加，而是与原始 $x_2$ 相加。</p><h3 id="35-12-第十一步：第二次-LayerNorm-和-MLP"><a href="#35-12-第十一步：第二次-LayerNorm-和-MLP" class="headerlink" title="35.12 第十一步：第二次 LayerNorm 和 MLP"></a>35.12 第十一步：第二次 LayerNorm 和 MLP</h3><p>对 $u_2$ 做第二次 LayerNorm。二维向量归一化后近似为：</p><p>$$<br>\operatorname{LN}(u_2)\approx[-1,1]<br>$$</p><p>真实 Mini-GPT 的 MLP 会执行：</p><pre class="language-text" data-language="text"><code class="language-text">2 -> 8 -> GELU -> 2</code></pre><p>假设第一层只把两个输入复制到前两个中间神经元，其余 6 个输出为 0：</p><p>$$<br>W_1<br>\operatorname{LN}(u_2)<br>&#x3D;<br>[-1,1,0,0,0,0,0,0]<br>$$</p><p>GELU 的近似值：</p><p>$$<br>\operatorname{GELU}(-1)\approx-0.1587<br>$$</p><p>$$<br>\operatorname{GELU}(1)\approx0.8413<br>$$</p><p>再假设 $W_2$ 只把前两个中间值映射回两个输出维度，则：</p><p>$$<br>\operatorname{MLP}(\operatorname{LN}(u_2))<br>\approx[-0.1587,0.8413]<br>$$</p><p>第二次残差连接：</p><p>$$<br>v_2<br>&#x3D;u_2+\operatorname{MLP}(\operatorname{LN}(u_2))<br>$$</p><p>$$<br>v_2<br>\approx[-0.8884,1.9884]<br>+[-0.1587,0.8413]<br>$$</p><p>$$<br>v_2\approx[-1.0471,2.8297]<br>$$</p><p>这就是唯一一个 GPT Block 在最后位置的输出。</p><h3 id="35-13-第十二步：Final-LayerNorm"><a href="#35-13-第十二步：Final-LayerNorm" class="headerlink" title="35.13 第十二步：Final LayerNorm"></a>35.13 第十二步：Final LayerNorm</h3><p>所有 Block 之后还要进行最终 LayerNorm：</p><p>$$<br>h_2&#x3D;\operatorname{LN}(v_2)<br>$$</p><p>忽略 $\epsilon$ 并取三位小数：</p><p>$$<br>h_2\approx[-1,1]<br>$$</p><p>推测下一个 token 时只需要最后一个位置的 $h_2$，前面位置的输出不用送入采样器。</p><h3 id="35-14-第十三步：LM-Head-计算词表-Logits"><a href="#35-14-第十三步：LM-Head-计算词表-Logits" class="headerlink" title="35.14 第十三步：LM Head 计算词表 Logits"></a>35.14 第十三步：LM Head 计算词表 Logits</h3><p>假设使用 Weight Tying，因此 LM Head 与 Token Embedding 共享矩阵：</p><p>$$<br>W_{LM}&#x3D;E_{token}<br>$$</p><p>logits 为：</p><p>$$<br>z&#x3D;h_2E_{token}^T<br>$$</p><p>逐个 token 计算：</p><p>$$<br>z_{春}&#x3D;[-1,1]\cdot[1,0]&#x3D;-1<br>$$</p><p>$$<br>z_{天}&#x3D;[-1,1]\cdot[0,1]&#x3D;1<br>$$</p><p>$$<br>z_{来}&#x3D;[-1,1]\cdot[-1,1]&#x3D;2<br>$$</p><p>$$<br>z_{了}&#x3D;[-1,1]\cdot[1,-1]&#x3D;-2<br>$$</p><p>所以：</p><p>$$<br>z&#x3D;[-1,1,2,-2]<br>$$</p><p>logit 只是未归一化分数，还不是概率。</p><h3 id="35-15-第十四步：Temperature-和-Softmax"><a href="#35-15-第十四步：Temperature-和-Softmax" class="headerlink" title="35.15 第十四步：Temperature 和 Softmax"></a>35.15 第十四步：Temperature 和 Softmax</h3><p>先使用：</p><pre class="language-text" data-language="text"><code class="language-text">temperature = 1.0top_k = None</code></pre><p>所以 logits 不变。</p><p>Softmax 分母：</p><p>$$<br>e^{-1}+e^1+e^2+e^{-2}<br>\approx0.3679+2.7183+7.3891+0.1353<br>\approx10.6106<br>$$</p><p>各 token 概率：</p><p>$$<br>P(春\mid春天)\approx0.0347<br>$$</p><p>$$<br>P(天\mid春天)\approx0.2562<br>$$</p><p>$$<br>P(来\mid春天)\approx0.6964<br>$$</p><p>$$<br>P(了\mid春天)\approx0.0128<br>$$</p><p>整理成表格：</p><table><thead><tr><th>候选 token</th><th align="right">logit</th><th align="right">概率</th></tr></thead><tbody><tr><td>春</td><td align="right">-1</td><td align="right">3.47%</td></tr><tr><td>天</td><td align="right">1</td><td align="right">25.62%</td></tr><tr><td>来</td><td align="right">2</td><td align="right">69.64%</td></tr><tr><td>了</td><td align="right">-2</td><td align="right">1.28%</td></tr></tbody></table><h3 id="35-16-第十五步：选择下一个-Token"><a href="#35-16-第十五步：选择下一个-Token" class="headerlink" title="35.16 第十五步：选择下一个 Token"></a>35.16 第十五步：选择下一个 Token</h3><p>如果使用 Greedy Search：</p><p>$$<br>next_id&#x3D;\arg\max(z)&#x3D;2<br>$$</p><p>查 <code>itos</code>：</p><pre class="language-text" data-language="text"><code class="language-text">2 -> “来”</code></pre><p>因此模型对 Prompt 的一次续写是：</p><pre class="language-text" data-language="text"><code class="language-text">输入 Prompt：春天预测 Token：来新 Prompt：  春天来</code></pre><p>如果使用 <code>top_k=2</code> 的随机采样，只保留“天”和“来”，重新归一化后大约为：</p><pre class="language-text" data-language="text"><code class="language-text">P(天) = 26.89%P(来) = 73.11%</code></pre><p>这时“来”最可能被采到，但并非百分之百确定。</p><h3 id="35-17-怎样继续生成第二个新-Token"><a href="#35-17-怎样继续生成第二个新-Token" class="headerlink" title="35.17 怎样继续生成第二个新 Token"></a>35.17 怎样继续生成第二个新 Token</h3><p>生成函数把刚得到的 token id <code>2</code> 拼回输入：</p><p>$$<br>[0,1]+[2]&#x3D;[0,1,2]<br>$$</p><p>也就是：</p><pre class="language-text" data-language="text"><code class="language-text">春天 -> 春天来</code></pre><p>下一轮重新执行：</p><pre class="language-text" data-language="text"><code class="language-text">[春, 天, 来]  -> Embedding + Position Embedding  -> GPT Block  -> 取最后位置 logits  -> 预测“了”或其他 token</code></pre><p>循环直到：</p><ul><li>生成了 <code>max_new_tokens</code> 个 token。</li><li>或者模型生成 <code>&lt;EOS&gt;</code>，如果词表和训练数据定义了它。</li></ul><p>当前教学版没有 KV Cache，所以每一轮都会重新计算整个可见 Prompt；加入 KV Cache 后，可以复用“春”“天”等历史 token 的 Key 和 Value。</p><h3 id="35-18-如果这是训练过程，还会发生什么"><a href="#35-18-如果这是训练过程，还会发生什么" class="headerlink" title="35.18 如果这是训练过程，还会发生什么"></a>35.18 如果这是训练过程，还会发生什么</h3><p>推理阶段在选出或采样下一个 token 后就结束，不计算损失，也不执行反向传播。</p><p>如果这是一个训练样本，并且正确目标确实是“来”，交叉熵为：</p><p>$$<br>\mathcal{L}<br>&#x3D;-\log P(来\mid春天)<br>$$</p><p>$$<br>\mathcal{L}<br>&#x3D;-\log(0.6964)<br>\approx0.3618<br>$$</p><p>交叉熵对四个 logits 的梯度为：</p><p>$$<br>\frac{\partial\mathcal{L}}{\partial z_j}<br>&#x3D;p_j-\mathbb{1}[j&#x3D;y]<br>$$</p><p>因此：</p><p>$$<br>\frac{\partial\mathcal{L}}{\partial z}<br>\approx<br>[0.0347,0.2562,-0.3036,0.0128]<br>$$</p><p>含义是：</p><ul><li>“春、天、了”的 logit 梯度为正，梯度下降会倾向于降低它们。</li><li>正确答案“来”的 logit 梯度为负，梯度下降会倾向于提高它。</li></ul><p>梯度会继续经过 LM Head、Final LayerNorm、MLP、Attention 和 Embedding 反向传播，最后由 AdamW 更新所有相关参数。</p><h3 id="35-19-用-PyTorch-复核手算结果"><a href="#35-19-用-PyTorch-复核手算结果" class="headerlink" title="35.19 用 PyTorch 复核手算结果"></a>35.19 用 PyTorch 复核手算结果</h3><p>下面的代码只用于验证本节数字：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> math<span class="token keyword">import</span> torch<span class="token keyword">import</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>functional <span class="token keyword">as</span> Fq <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>scores <span class="token operator">=</span> q @ q<span class="token punctuation">.</span>T <span class="token operator">/</span> math<span class="token punctuation">.</span>sqrt<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">)</span>scores<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token builtin">float</span><span class="token punctuation">(</span><span class="token string">"-inf"</span><span class="token punctuation">)</span>weights <span class="token operator">=</span> F<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>scores<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>attention <span class="token operator">=</span> weights @ qx <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">1.1</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">1.1</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>u <span class="token operator">=</span> x<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span> <span class="token operator">+</span> attention<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span>ln_2 <span class="token operator">=</span> <span class="token punctuation">(</span>u <span class="token operator">-</span> u<span class="token punctuation">.</span>mean<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token operator">/</span> u<span class="token punctuation">.</span>std<span class="token punctuation">(</span>unbiased<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">)</span>mlp <span class="token operator">=</span> F<span class="token punctuation">.</span>gelu<span class="token punctuation">(</span>ln_2<span class="token punctuation">)</span>v <span class="token operator">=</span> u <span class="token operator">+</span> mlph <span class="token operator">=</span> <span class="token punctuation">(</span>v <span class="token operator">-</span> v<span class="token punctuation">.</span>mean<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token operator">/</span> v<span class="token punctuation">.</span>std<span class="token punctuation">(</span>unbiased<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">)</span>embedding <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>   <span class="token comment"># 春</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>   <span class="token comment"># 天</span>    <span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>  <span class="token comment"># 来</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>  <span class="token comment"># 了</span><span class="token punctuation">]</span><span class="token punctuation">)</span>logits <span class="token operator">=</span> h @ embedding<span class="token punctuation">.</span>Tprobs <span class="token operator">=</span> F<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>weights<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token comment"># tensor([0.0558, 0.9442])</span><span class="token keyword">print</span><span class="token punctuation">(</span>logits<span class="token punctuation">)</span><span class="token comment"># tensor([-1., 1., 2., -2.])</span><span class="token keyword">print</span><span class="token punctuation">(</span>probs<span class="token punctuation">)</span><span class="token comment"># tensor([0.0347, 0.2562, 0.6964, 0.0128])</span><span class="token keyword">print</span><span class="token punctuation">(</span>torch<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>probs<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token comment"># tensor(2)，即“来”</span></code></pre><h3 id="35-20-把整个推理流程压缩成一张表"><a href="#35-20-把整个推理流程压缩成一张表" class="headerlink" title="35.20 把整个推理流程压缩成一张表"></a>35.20 把整个推理流程压缩成一张表</h3><table><thead><tr><th>步骤</th><th>输入</th><th>输出</th></tr></thead><tbody><tr><td>Tokenizer</td><td><code>春天</code></td><td><code>[0,1]</code></td></tr><tr><td>Token Embedding</td><td><code>[0,1]</code></td><td><code>[[1,0],[0,1]]</code></td></tr><tr><td>加位置向量</td><td>Token + Position</td><td><code>[[1.1,0],[0,1.1]]</code></td></tr><tr><td>Pre-LayerNorm</td><td>$X$</td><td><code>[[1,-1],[-1,1]]</code></td></tr><tr><td>QKV 投影</td><td>$X_{norm}$</td><td>$Q&#x3D;K&#x3D;V&#x3D;X_{norm}$</td></tr><tr><td>Causal Attention</td><td>$Q,K,V,M$</td><td>最后位置 <code>[-0.8884,0.8884]</code></td></tr><tr><td>第一次残差</td><td>$x_2+o_2$</td><td><code>[-0.8884,1.9884]</code></td></tr><tr><td>LN + MLP</td><td>$u_2$</td><td><code>[-0.1587,0.8413]</code></td></tr><tr><td>第二次残差</td><td>$u_2+MLP$</td><td><code>[-1.0471,2.8297]</code></td></tr><tr><td>Final LayerNorm</td><td>$v_2$</td><td><code>[-1,1]</code></td></tr><tr><td>LM Head</td><td>$h_2E^T$</td><td><code>[-1,1,2,-2]</code></td></tr><tr><td>Softmax</td><td>logits</td><td><code>[3.47%,25.62%,69.64%,1.28%]</code></td></tr><tr><td>Greedy 选择</td><td>最大概率 id <code>2</code></td><td><code>来</code></td></tr><tr><td>拼回上下文</td><td><code>春天</code> + <code>来</code></td><td><code>春天来</code></td></tr></tbody></table><p>这个例子把“根据 Prompt 推测下一个 token”从一句抽象描述展开成了具体数字。真实 Mini-GPT 只是把相同过程扩展到更大词表、更高维向量、更多注意力头和更多 Block。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>Alec Radford et al., <em>Improving Language Understanding by Generative Pre-Training</em>: <a href="https://cdn.openai.com/research-covers/language-unsupervised/language_understanding_paper.pdf">https://cdn.openai.com/research-covers/language-unsupervised/language_understanding_paper.pdf</a></li><li>Alec Radford et al., <em>Language Models are Unsupervised Multitask Learners</em>: <a href="https://cdn.openai.com/better-language-models/language-models.pdf">https://cdn.openai.com/better-language-models/language-models.pdf</a></li><li>Ashish Vaswani et al., <em>Attention Is All You Need</em>: <a href="https://arxiv.org/abs/1706.03762">https://arxiv.org/abs/1706.03762</a></li><li>PyTorch Documentation, <code>scaled_dot_product_attention</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.functional.scaled_dot_product_attention.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.functional.scaled_dot_product_attention.html</a></li><li>PyTorch Documentation, <code>CrossEntropyLoss</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html</a></li><li>PyTorch Documentation, <code>torch.multinomial</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.multinomial.html">https://docs.pytorch.org/docs/stable/generated/torch.multinomial.html</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/12/nlp-mini-gpt-from-scratch-pytorch/</id>
    <link href="https://lsworl.github.io/2026/08/12/nlp-mini-gpt-from-scratch-pytorch/"/>
    <published>2026-08-11T16:00:00.000Z</published>
    <summary>承接Transformer，从零手写字符级Mini-GPT，深入理解自回归语言模型、因果注意力、Pre-Norm Block、交叉熵、采样策略、训练技巧与KV Cache。</summary>
    <title>手写Mini-GPT：从因果语言模型到PyTorch完整实现</title>
    <updated>2026-08-14T10:31:06.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Transformer" scheme="https://lsworl.github.io/tags/Transformer/"/>
    <category term="Self-Attention" scheme="https://lsworl.github.io/tags/Self-Attention/"/>
    <category term="Seq2Seq" scheme="https://lsworl.github.io/tags/Seq2Seq/"/>
    <category term="Multi-Head Attention" scheme="https://lsworl.github.io/tags/Multi-Head-Attention/"/>
    <content>
      <![CDATA[<p>上一篇 Seq2Seq 文章中，我们使用 GRU 构造了 Encoder-Decoder，并通过 Attention 缓解“把整句话压缩成一个向量”的信息瓶颈。</p><p>不过，RNN、LSTM 和 GRU 还有一个很难绕开的特点：</p><blockquote><p>必须按照时间顺序逐步计算，后一个位置需要等待前一个位置的隐藏状态。</p></blockquote><p>假设一句话有 100 个 token，RNN 需要从第 1 个 token 一直算到第 100 个 token。即使 GPU 很擅长矩阵并行，也不能完全消除这种前后依赖。</p><p>Transformer 改变了处理序列的方式：</p><blockquote><p>不再使用循环逐步传递信息，而是让每个 token 直接通过 Attention 查看序列中的其他 token。</p></blockquote><p>这样一来，训练时可以并行处理整段序列，也更容易建模相距很远的词之间的关系。</p><span id="more"></span><p><img src="https://lsworl.github.io/images/nlp-blog/transformer-overview.svg" alt="Transformer整体结构"></p><h2 id="1-Transformer-要解决什么问题"><a href="#1-Transformer-要解决什么问题" class="headerlink" title="1. Transformer 要解决什么问题"></a>1. Transformer 要解决什么问题</h2><p>先看一句话：</p><pre class="language-text" data-language="text"><code class="language-text">小明 把 苹果 放在 桌子 上，因为 他 刚刚 买了 它</code></pre><p>理解这句话时，需要建立多处联系：</p><ul><li>“他”更可能指向“小明”。</li><li>“它”更可能指向“苹果”。</li><li>“放在”与“桌子上”共同表达一个位置关系。</li></ul><p>在 RNN 中，前面的信息要经过多个时间步才能传到后面；序列很长时，远距离信息的传播路径也会很长。</p><p>Transformer 中，“它”可以直接对“小明”“苹果”“桌子”等所有位置计算相关程度，再把有用信息聚合回来。</p><h3 id="1-1-RNN-与-Transformer-的主要区别"><a href="#1-1-RNN-与-Transformer-的主要区别" class="headerlink" title="1.1 RNN 与 Transformer 的主要区别"></a>1.1 RNN 与 Transformer 的主要区别</h3><table><thead><tr><th>对比项</th><th>RNN、LSTM、GRU</th><th>Transformer</th></tr></thead><tbody><tr><td>序列处理</td><td>按时间步递归</td><td>整个序列并行计算</td></tr><tr><td>信息交互</td><td>通过隐藏状态逐步传递</td><td>token 之间直接计算 Attention</td></tr><tr><td>位置信息</td><td>顺序天然包含在递归中</td><td>需要额外加入位置编码</td></tr><tr><td>长距离依赖</td><td>信息传播路径较长</td><td>任意两个位置可直接交互</td></tr><tr><td>主要计算</td><td>循环和矩阵乘法</td><td>大规模矩阵乘法</td></tr><tr><td>长序列代价</td><td>串行步骤多</td><td>标准 Attention 的矩阵为 $n\times n$</td></tr></tbody></table><p>Transformer 并不是简单地“把 RNN 换成 Attention”。它还包含：</p><ul><li>Multi-Head Attention。</li><li>Positional Encoding。</li><li>Feed Forward Network。</li><li>Residual Connection。</li><li>Layer Normalization。</li><li>Padding Mask 与 Causal Mask。</li></ul><p>下面逐块拆开。</p><h2 id="2-Transformer-的整体结构"><a href="#2-Transformer-的整体结构" class="headerlink" title="2. Transformer 的整体结构"></a>2. Transformer 的整体结构</h2><p>原始 Transformer 用于机器翻译，因此仍然采用 Encoder-Decoder 结构。</p><pre class="language-text" data-language="text"><code class="language-text">源语言 token  -> Transformer Encoder  -> 上下文表示 Memory  -> Transformer Decoder  -> 目标语言下一个 token 的概率</code></pre><h3 id="2-1-Encoder-做什么"><a href="#2-1-Encoder-做什么" class="headerlink" title="2.1 Encoder 做什么"></a>2.1 Encoder 做什么</h3><p>Encoder 接收完整输入序列，让每个输入 token 与其他输入 token 交换信息，最终得到一组包含上下文的表示：</p><p>$$<br>H&#x3D;(h_1,h_2,\ldots,h_{T_x})<br>$$</p><p>与早期 Seq2Seq 只保留最后一个隐藏状态不同，Transformer Encoder 会保留每个位置的输出。</p><h3 id="2-2-Decoder-做什么"><a href="#2-2-Decoder-做什么" class="headerlink" title="2.2 Decoder 做什么"></a>2.2 Decoder 做什么</h3><p>Decoder 根据两部分信息预测下一个目标 token：</p><ol><li>已经生成的目标 token。</li><li>Encoder 输出的完整 Memory。</li></ol><p>训练时，目标序列可以整体送入 Decoder，但必须使用 Causal Mask 遮住未来位置，防止模型偷看答案。</p><h3 id="2-3-Encoder-和-Decoder-并非所有模型都同时使用"><a href="#2-3-Encoder-和-Decoder-并非所有模型都同时使用" class="headerlink" title="2.3 Encoder 和 Decoder 并非所有模型都同时使用"></a>2.3 Encoder 和 Decoder 并非所有模型都同时使用</h3><table><thead><tr><th>架构</th><th>使用部分</th><th>更适合的任务</th></tr></thead><tbody><tr><td>Encoder-only</td><td>只使用 Encoder</td><td>分类、序列标注、文本表示</td></tr><tr><td>Decoder-only</td><td>只使用 Decoder</td><td>自回归文本生成</td></tr><tr><td>Encoder-Decoder</td><td>两者都使用</td><td>翻译、摘要、序列转换</td></tr></tbody></table><p>因此，Transformer 是一套可组合的结构，不只是一种固定模型。</p><h2 id="3-输入进入-Transformer-前发生了什么"><a href="#3-输入进入-Transformer-前发生了什么" class="headerlink" title="3. 输入进入 Transformer 前发生了什么"></a>3. 输入进入 Transformer 前发生了什么</h2><p>Transformer 不直接处理文字，而是处理三维张量。</p><p>假设：</p><ul><li>batch size 为 $B$。</li><li>序列长度为 $L$。</li><li>模型维度为 $d_{model}$。</li></ul><p>输入 token id 的形状是：</p><pre class="language-text" data-language="text"><code class="language-text">[B, L]</code></pre><p>经过 Embedding 后变成：</p><pre class="language-text" data-language="text"><code class="language-text">[B, L, d_model]</code></pre><p>再加入位置编码：</p><p>$$<br>Z&#x3D;X_{embedding}+P_{position}<br>$$</p><p>最终的 $Z$ 才会送入第一个 Transformer 层。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">token id:       [32, 20]Embedding:      [32, 20, 128]位置编码相加后: [32, 20, 128]</code></pre><p>位置编码与 Embedding 的维度必须相同，才能逐元素相加。</p><h2 id="4-Attention-的直觉：查询、钥匙和值"><a href="#4-Attention-的直觉：查询、钥匙和值" class="headerlink" title="4. Attention 的直觉：查询、钥匙和值"></a>4. Attention 的直觉：查询、钥匙和值</h2><p>Attention 最容易让人困惑的三个字母是：</p><ul><li>Query，简称 $Q$。</li><li>Key，简称 $K$。</li><li>Value，简称 $V$。</li></ul><p>可以把一次检索理解为：</p><pre class="language-text" data-language="text"><code class="language-text">Query：我现在想找什么信息？Key：我这里保存的信息适合被怎样检索？Value：如果匹配成功，我真正提供什么内容？</code></pre><p>以“它”为例：</p><pre class="language-text" data-language="text"><code class="language-text">Query("它") 与 Key("小明") 计算相似度Query("它") 与 Key("苹果") 计算相似度Query("它") 与 Key("桌子") 计算相似度</code></pre><p>假设“它”与“苹果”的匹配分数最高，模型就会给“苹果”的 Value 更高权重。</p><p><img src="https://lsworl.github.io/images/nlp-blog/transformer-qkv.svg" alt="Self-Attention中的QKV"></p><h3 id="4-1-Q、K、V-从哪里来"><a href="#4-1-Q、K、V-从哪里来" class="headerlink" title="4.1 Q、K、V 从哪里来"></a>4.1 Q、K、V 从哪里来</h3><p>对于输入矩阵 $X$，通过三个不同的线性变换得到：</p><p>$$<br>Q&#x3D;XW_Q<br>$$</p><p>$$<br>K&#x3D;XW_K<br>$$</p><p>$$<br>V&#x3D;XW_V<br>$$</p><p>其中 $W_Q$、$W_K$、$W_V$ 都是可学习参数。</p><p>需要注意：</p><blockquote><p>Self-Attention 中 Q、K、V 都来自同一组输入，但使用不同参数投影，所以它们通常并不相等。</p></blockquote><p>训练开始时，这些矩阵通常是随机初始化的。反向传播会逐渐学习“怎样提出查询”“怎样参与匹配”和“怎样提供内容”。</p><h2 id="5-缩放点积注意力"><a href="#5-缩放点积注意力" class="headerlink" title="5. 缩放点积注意力"></a>5. 缩放点积注意力</h2><p>Transformer 使用的核心公式是：</p><p>$$<br>\operatorname{Attention}(Q,K,V)<br>&#x3D;\operatorname{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V<br>$$</p><p>不要急着记公式，可以拆成四步。</p><h3 id="第一步：计算匹配分数"><a href="#第一步：计算匹配分数" class="headerlink" title="第一步：计算匹配分数"></a>第一步：计算匹配分数</h3><p>$$<br>S&#x3D;QK^T<br>$$</p><p>$S_{ij}$ 表示第 $i$ 个 Query 与第 $j$ 个 Key 的匹配程度。</p><h3 id="第二步：进行缩放"><a href="#第二步：进行缩放" class="headerlink" title="第二步：进行缩放"></a>第二步：进行缩放</h3><p>$$<br>S’&#x3D;\frac{S}{\sqrt{d_k}}<br>$$</p><p>$d_k$ 是每个 Key 和 Query 的向量维度。</p><h3 id="第三步：转成注意力权重"><a href="#第三步：转成注意力权重" class="headerlink" title="第三步：转成注意力权重"></a>第三步：转成注意力权重</h3><p>$$<br>A&#x3D;\operatorname{softmax}(S’)<br>$$</p><p>Softmax 按最后一个维度进行，所以 $A$ 的每一行之和都是 1。</p><h3 id="第四步：对-Value-加权求和"><a href="#第四步：对-Value-加权求和" class="headerlink" title="第四步：对 Value 加权求和"></a>第四步：对 Value 加权求和</h3><p>$$<br>O&#x3D;AV<br>$$</p><p>输出的每个位置都融合了所有可见位置的信息。</p><h2 id="6-手算一次完整-Self-Attention"><a href="#6-手算一次完整-Self-Attention" class="headerlink" title="6. 手算一次完整 Self-Attention"></a>6. 手算一次完整 Self-Attention</h2><p>为了看清计算过程，假设只有 3 个 token，每个 token 是二维向量：</p><p>$$<br>X&#x3D;<br>\begin{bmatrix}<br>1&amp;0\<br>0&amp;1\<br>1&amp;1<br>\end{bmatrix}<br>$$</p><p>为了简化计算，暂时令$W_Q$，$W_K$，$W_V$为单位矩阵：</p><p>$$<br>W_Q&#x3D;W_K&#x3D;W_V&#x3D;I<br>$$</p><p>因此：</p><p>$$<br>Q&#x3D;K&#x3D;V&#x3D;X<br>$$</p><p>实际模型不会固定为单位矩阵，这里只是为了手算。</p><h3 id="6-1-计算-QK-T"><a href="#6-1-计算-QK-T" class="headerlink" title="6.1 计算 $QK^T$"></a>6.1 计算 $QK^T$</h3><p>$$<br>QK^T&#x3D;<br>\begin{bmatrix}<br>1&amp;0&amp;1\<br>0&amp;1&amp;1\<br>1&amp;1&amp;2<br>\end{bmatrix}<br>$$</p><p>第一行来自：</p><p>$$<br>[1,0]\cdot[1,0]&#x3D;1<br>$$</p><p>$$<br>[1,0]\cdot[0,1]&#x3D;0<br>$$</p><p>$$<br>[1,0]\cdot[1,1]&#x3D;1<br>$$</p><p>它表示第一个 token 对三个 token 的原始匹配分数。</p><h3 id="6-2-除以-sqrt-d-k"><a href="#6-2-除以-sqrt-d-k" class="headerlink" title="6.2 除以 $\sqrt{d_k}$"></a>6.2 除以 $\sqrt{d_k}$</h3><p>这里 $d_k&#x3D;2$：</p><p>$$<br>\frac{QK^T}{\sqrt{2}}<br>\approx<br>\begin{bmatrix}<br>0.707&amp;0&amp;0.707\<br>0&amp;0.707&amp;0.707\<br>0.707&amp;0.707&amp;1.414<br>\end{bmatrix}<br>$$</p><h3 id="6-3-对每一行做-Softmax"><a href="#6-3-对每一行做-Softmax" class="headerlink" title="6.3 对每一行做 Softmax"></a>6.3 对每一行做 Softmax</h3><p>以第一行为例：</p><p>$$<br>\operatorname{softmax}([0.707,0,0.707])<br>\approx[0.401,0.198,0.401]<br>$$</p><p>完整权重矩阵近似为：</p><p>$$<br>A\approx<br>\begin{bmatrix}<br>0.401&amp;0.198&amp;0.401\<br>0.198&amp;0.401&amp;0.401\<br>0.248&amp;0.248&amp;0.503<br>\end{bmatrix}<br>$$</p><p>每一行都满足：</p><p>$$<br>\sum_j A_{ij}&#x3D;1<br>$$</p><h3 id="6-4-用权重聚合-Value"><a href="#6-4-用权重聚合-Value" class="headerlink" title="6.4 用权重聚合 Value"></a>6.4 用权重聚合 Value</h3><p>第一个位置的输出是：</p><p>$$<br>o_1<br>&#x3D;0.401[1,0]+0.198[0,1]+0.401[1,1]<br>$$</p><p>$$<br>o_1\approx[0.802,0.599]<br>$$</p><p>三个位置的完整输出为：</p><p>$$<br>O&#x3D;AV\approx<br>\begin{bmatrix}<br>0.802&amp;0.599\<br>0.599&amp;0.802\<br>0.751&amp;0.751<br>\end{bmatrix}<br>$$</p><p>原来的第一个 token 是 $[1,0]$，经过 Attention 后变成 $[0.802,0.599]$。第二个维度从 0 变成了 0.599，说明它已经融合了另外两个位置的信息。</p><p><img src="https://lsworl.github.io/images/nlp-blog/transformer-attention-matrix.svg" alt="Self-Attention矩阵手算"></p><p>这就是 Self-Attention 最核心的事情：</p><blockquote><p>根据内容计算 token 之间的关系，再把其他位置的信息按权重汇总到当前位置。</p></blockquote><h2 id="7-为什么要除以-sqrt-d-k"><a href="#7-为什么要除以-sqrt-d-k" class="headerlink" title="7. 为什么要除以 $\sqrt{d_k}$"></a>7. 为什么要除以 $\sqrt{d_k}$</h2><p>如果 Query 和 Key 的每个分量都具有大致相同的方差，那么向量维度越大，点积结果的绝对值通常也越大。</p><p>例如 Softmax 的输入是：</p><pre class="language-text" data-language="text"><code class="language-text">[1, 2, 3]</code></pre><p>得到的概率还比较平滑。如果变成：</p><pre class="language-text" data-language="text"><code class="language-text">[10, 20, 30]</code></pre><p>Softmax 会非常接近 one-hot，最大值几乎占据全部概率。</p><p>这会让 Softmax 落入饱和区域，其他位置的梯度变得很小。除以 $\sqrt{d_k}$ 可以控制分数尺度，让训练更稳定。</p><p>缩放并不改变矩阵形状：</p><pre class="language-text" data-language="text"><code class="language-text">Q:       [B, H, Lq, dk]K^T:     [B, H, dk, Lk]score:   [B, H, Lq, Lk]weight:  [B, H, Lq, Lk]V:       [B, H, Lk, dv]output:  [B, H, Lq, dv]</code></pre><h2 id="8-Self-Attention、Cross-Attention-和-Masked-Self-Attention"><a href="#8-Self-Attention、Cross-Attention-和-Masked-Self-Attention" class="headerlink" title="8. Self-Attention、Cross-Attention 和 Masked Self-Attention"></a>8. Self-Attention、Cross-Attention 和 Masked Self-Attention</h2><p>三者使用同一个 Attention 公式，区别在于 Q、K、V 来自哪里，以及哪些位置允许被查看。</p><h3 id="8-1-Encoder-Self-Attention"><a href="#8-1-Encoder-Self-Attention" class="headerlink" title="8.1 Encoder Self-Attention"></a>8.1 Encoder Self-Attention</h3><pre class="language-text" data-language="text"><code class="language-text">Q 来自 Encoder 当前输入K 来自 Encoder 当前输入V 来自 Encoder 当前输入</code></pre><p>输入序列中的每个 token 都可以查看其他非 PAD token。</p><h3 id="8-2-Decoder-Masked-Self-Attention"><a href="#8-2-Decoder-Masked-Self-Attention" class="headerlink" title="8.2 Decoder Masked Self-Attention"></a>8.2 Decoder Masked Self-Attention</h3><pre class="language-text" data-language="text"><code class="language-text">Q、K、V 都来自 Decoder 当前输入</code></pre><p>但第 $t$ 个位置不能看到 $t+1$ 及更后面的答案。</p><h3 id="8-3-Encoder-Decoder-Cross-Attention"><a href="#8-3-Encoder-Decoder-Cross-Attention" class="headerlink" title="8.3 Encoder-Decoder Cross-Attention"></a>8.3 Encoder-Decoder Cross-Attention</h3><pre class="language-text" data-language="text"><code class="language-text">Q 来自 DecoderK 来自 Encoder 输出V 来自 Encoder 输出</code></pre><p>含义是：Decoder 在生成当前词时，拿自己的状态去检索源句中有用的信息。</p><table><thead><tr><th>类型</th><th>Query 来源</th><th>Key、Value 来源</th><th>典型作用</th></tr></thead><tbody><tr><td>Encoder Self-Attention</td><td>Encoder</td><td>Encoder</td><td>理解输入内部关系</td></tr><tr><td>Decoder Self-Attention</td><td>Decoder</td><td>Decoder</td><td>理解已生成内容</td></tr><tr><td>Cross-Attention</td><td>Decoder</td><td>Encoder</td><td>从输入序列检索信息</td></tr></tbody></table><h2 id="9-多头注意力为什么不是重复计算"><a href="#9-多头注意力为什么不是重复计算" class="headerlink" title="9. 多头注意力为什么不是重复计算"></a>9. 多头注意力为什么不是重复计算</h2><p>单个 Attention 头只在一个投影空间中计算关系。Multi-Head Attention 会把特征拆成多个头，让不同头学习不同的关系。</p><p>$$<br>\operatorname{head}_i<br>&#x3D;\operatorname{Attention}(QW_i^Q,KW_i^K,VW_i^V)<br>$$</p><p>$$<br>\operatorname{MultiHead}(Q,K,V)<br>&#x3D;\operatorname{Concat}(\operatorname{head}_1,\ldots,\operatorname{head}_h)W^O<br>$$</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">d_model = 512num_heads = 8head_dim = 512 / 8 = 64</code></pre><p>每个头处理 64 维特征，8 个头拼接后重新得到 512 维。</p><p><img src="https://lsworl.github.io/images/nlp-blog/transformer-multi-head.svg" alt="Multi-Head Attention计算过程"></p><p>不同头可能学习到不同模式，例如：</p><ul><li>某个头关注相邻词。</li><li>某个头关注主语与谓语。</li><li>某个头关注代词指代。</li><li>某个头关注句首或标点。</li></ul><p>这些模式不是人工指定的，而是训练目标驱动下可能出现的分工。也不能简单认为“每个头一定对应一种人类可命名的语法关系”。</p><h3 id="9-1-为什么-d-model-通常要能整除-num-heads"><a href="#9-1-为什么-d-model-通常要能整除-num-heads" class="headerlink" title="9.1 为什么 d_model 通常要能整除 num_heads"></a>9.1 为什么 <code>d_model</code> 通常要能整除 <code>num_heads</code></h3><p>常见实现会把 $d_{model}$ 平均拆给各个头：</p><p>$$<br>d_{head}&#x3D;\frac{d_{model}}{h}<br>$$</p><p>因此下面是合法的：</p><pre class="language-text" data-language="text"><code class="language-text">d_model=128, num_heads=4, head_dim=32</code></pre><p>而下面无法平均拆分：</p><pre class="language-text" data-language="text"><code class="language-text">d_model=130, num_heads=8</code></pre><p>PyTorch 的 <code>nn.MultiheadAttention</code> 也要求 <code>embed_dim</code> 能被 <code>num_heads</code> 整除。</p><h2 id="10-没有循环后，模型怎样知道顺序"><a href="#10-没有循环后，模型怎样知道顺序" class="headerlink" title="10. 没有循环后，模型怎样知道顺序"></a>10. 没有循环后，模型怎样知道顺序</h2><p>Self-Attention 本身只根据内容计算关系。如果把输入 token 的顺序打乱，又没有任何位置信息，模型无法可靠区分：</p><pre class="language-text" data-language="text"><code class="language-text">猫 追 老鼠老鼠 追 猫</code></pre><p>所以需要向词向量加入位置编码。</p><p>原始 Transformer 使用正弦和余弦位置编码：</p><p>$$<br>PE(pos,2i)&#x3D;\sin\left(\frac{pos}{10000^{2i&#x2F;d_{model}}}\right)<br>$$</p><p>$$<br>PE(pos,2i+1)&#x3D;\cos\left(\frac{pos}{10000^{2i&#x2F;d_{model}}}\right)<br>$$</p><p>不同维度使用不同频率，使每个位置拥有不同模式。</p><p><img src="https://lsworl.github.io/images/nlp-blog/transformer-positional-encoding.svg" alt="位置编码如何加入Embedding"></p><h3 id="10-1-手算一个位置编码"><a href="#10-1-手算一个位置编码" class="headerlink" title="10.1 手算一个位置编码"></a>10.1 手算一个位置编码</h3><p>假设：</p><pre class="language-text" data-language="text"><code class="language-text">pos = 2d_model = 4</code></pre><p>那么：</p><p>$$<br>PE(2,0)&#x3D;\sin(2)\approx0.9093<br>$$</p><p>$$<br>PE(2,1)&#x3D;\cos(2)\approx-0.4161<br>$$</p><p>$$<br>PE(2,2)&#x3D;\sin(2&#x2F;100)\approx0.0200<br>$$</p><p>$$<br>PE(2,3)&#x3D;\cos(2&#x2F;100)\approx0.9998<br>$$</p><p>如果这个 token 的 Embedding 是：</p><p>$$<br>e&#x3D;[0.3,-0.2,0.7,0.1]<br>$$</p><p>相加后：</p><p>$$<br>z&#x3D;e+PE(2)<br>$$</p><p>$$<br>z\approx[1.2093,-0.6161,0.7200,1.0998]<br>$$</p><p>现在的表示同时包含 token 语义和“它位于第 2 个位置”的信息。</p><p>位置编码也可以设计为可学习参数。现代模型还会使用相对位置或旋转位置编码，但理解 Transformer 基础时，先掌握“内容表示需要注入位置信息”即可。</p><h2 id="11-Transformer-Encoder-层"><a href="#11-Transformer-Encoder-层" class="headerlink" title="11. Transformer Encoder 层"></a>11. Transformer Encoder 层</h2><p>一个经典 Encoder 层包含两个主要子层：</p><ol><li>Multi-Head Self-Attention。</li><li>Position-wise Feed Forward Network。</li></ol><p>每个子层外还包含残差连接与 LayerNorm。</p><p><img src="https://lsworl.github.io/images/nlp-blog/transformer-encoder-block.svg" alt="Transformer Encoder层"></p><p>原论文中的 Post-Norm 写法可以表示为：</p><p>$$<br>Z_1&#x3D;\operatorname{LayerNorm}(X+\operatorname{MHA}(X))<br>$$</p><p>$$<br>Z_2&#x3D;\operatorname{LayerNorm}(Z_1+\operatorname{FFN}(Z_1))<br>$$</p><p>多个 Encoder 层会堆叠起来：</p><pre class="language-text" data-language="text"><code class="language-text">输入 -> Encoder Layer 1 -> Encoder Layer 2 -> ... -> Encoder Layer N</code></pre><p>第 1 层可能更多地组合局部信息，后续层继续在新的表示上建立更复杂关系。</p><h2 id="12-前馈网络-FFN-在做什么"><a href="#12-前馈网络-FFN-在做什么" class="headerlink" title="12. 前馈网络 FFN 在做什么"></a>12. 前馈网络 FFN 在做什么</h2><p>Attention 负责不同 token 之间的信息交换，FFN 则对每个位置的特征进行非线性变换。</p><p>$$<br>\operatorname{FFN}(x)&#x3D;\sigma(xW_1+b_1)W_2+b_2<br>$$</p><p>原始 Transformer 使用 ReLU：</p><p>$$<br>\operatorname{FFN}(x)&#x3D;\max(0,xW_1+b_1)W_2+b_2<br>$$</p><p>很多后续模型使用 GELU 或门控形式，但基本作用仍然是增加每个位置内部的特征变换能力。</p><p>如果：</p><pre class="language-text" data-language="text"><code class="language-text">d_model = 512dim_feedforward = 2048</code></pre><p>则 FFN 的形状变化为：</p><pre class="language-text" data-language="text"><code class="language-text">[B, L, 512]  -> Linear(512, 2048)  -> 激活函数  -> Linear(2048, 512)  -> [B, L, 512]</code></pre><p>因为 FFN 对每个位置独立使用同一组参数，所以称为 position-wise FFN。</p><h2 id="13-残差连接和-LayerNorm-为什么重要"><a href="#13-残差连接和-LayerNorm-为什么重要" class="headerlink" title="13. 残差连接和 LayerNorm 为什么重要"></a>13. 残差连接和 LayerNorm 为什么重要</h2><h3 id="13-1-残差连接"><a href="#13-1-残差连接" class="headerlink" title="13.1 残差连接"></a>13.1 残差连接</h3><p>残差连接把子层输入直接加到输出：</p><p>$$<br>y&#x3D;x+F(x)<br>$$</p><p>它提供了一条更直接的信息和梯度传播路径，使深层网络更容易优化。</p><p>注意相加要求形状一致，因此 Attention 和 FFN 子层最后都要回到 $d_{model}$ 维。</p><h3 id="13-2-LayerNorm"><a href="#13-2-LayerNorm" class="headerlink" title="13.2 LayerNorm"></a>13.2 LayerNorm</h3><p>LayerNorm 对单个样本、单个 token 的特征维进行归一化。</p><p>对向量 $x\in\mathbb{R}^{d}$：</p><p>$$<br>\mu&#x3D;\frac{1}{d}\sum_{i&#x3D;1}^{d}x_i<br>$$</p><p>$$<br>\sigma^2&#x3D;\frac{1}{d}\sum_{i&#x3D;1}^{d}(x_i-\mu)^2<br>$$</p><p>$$<br>\operatorname{LayerNorm}(x)<br>&#x3D;\gamma\frac{x-\mu}{\sqrt{\sigma^2+\epsilon}}+\beta<br>$$</p><p>$\gamma$ 和 $\beta$ 是可学习参数。</p><h3 id="13-3-Post-Norm-与-Pre-Norm"><a href="#13-3-Post-Norm-与-Pre-Norm" class="headerlink" title="13.3 Post-Norm 与 Pre-Norm"></a>13.3 Post-Norm 与 Pre-Norm</h3><p>Post-Norm：</p><p>$$<br>y&#x3D;\operatorname{LayerNorm}(x+F(x))<br>$$</p><p>Pre-Norm：</p><p>$$<br>y&#x3D;x+F(\operatorname{LayerNorm}(x))<br>$$</p><p>原始论文使用 Post-Norm。PyTorch <code>nn.Transformer</code> 的 <code>norm_first=False</code> 表示 Post-Norm，设为 <code>True</code> 则使用 Pre-Norm。</p><h2 id="14-Transformer-Decoder-层"><a href="#14-Transformer-Decoder-层" class="headerlink" title="14. Transformer Decoder 层"></a>14. Transformer Decoder 层</h2><p>一个经典 Decoder 层包含三个主要子层：</p><ol><li>Masked Multi-Head Self-Attention。</li><li>Encoder-Decoder Cross-Attention。</li><li>Feed Forward Network。</li></ol><p>每个子层同样配有残差连接和 LayerNorm。</p><pre class="language-text" data-language="text"><code class="language-text">目标前缀  -> Masked Self-Attention  -> Cross-Attention，查看 Encoder Memory  -> FFN  -> 预测下一个 token</code></pre><p>Cross-Attention 中：</p><p>$$<br>Q&#x3D;H_{decoder}W_Q<br>$$</p><p>$$<br>K&#x3D;H_{encoder}W_K<br>$$</p><p>$$<br>V&#x3D;H_{encoder}W_V<br>$$</p><p>因此 Decoder 的每个位置都能根据当前生成上下文，从源句不同位置提取信息。</p><h2 id="15-Causal-Mask：为什么训练时不能看未来"><a href="#15-Causal-Mask：为什么训练时不能看未来" class="headerlink" title="15. Causal Mask：为什么训练时不能看未来"></a>15. Causal Mask：为什么训练时不能看未来</h2><p>假设目标序列是：</p><pre class="language-text" data-language="text"><code class="language-text">&lt;BOS> I like NLP &lt;EOS></code></pre><p>训练时会错开一位：</p><pre class="language-text" data-language="text"><code class="language-text">Decoder 输入：&lt;BOS> I    like NLP预测目标：       I     like NLP  &lt;EOS></code></pre><p>虽然 Decoder 输入可以一次性并行计算，但位置 <code>I</code> 不能看到后面的 <code>like</code> 和 <code>NLP</code>，否则相当于提前看答案。</p><p>长度为 4 的 Causal Mask 可以表示为：</p><p>$$<br>M&#x3D;<br>\begin{bmatrix}<br>0&amp;-\infty&amp;-\infty&amp;-\infty\<br>0&amp;0&amp;-\infty&amp;-\infty\<br>0&amp;0&amp;0&amp;-\infty\<br>0&amp;0&amp;0&amp;0<br>\end{bmatrix}<br>$$</p><p>它会在 Softmax 之前加到分数矩阵：</p><p>$$<br>A&#x3D;\operatorname{softmax}\left(\frac{QK^T}{\sqrt{d_k}}+M\right)<br>$$</p><p>因为 $e^{-\infty}&#x3D;0$，被遮住的位置最终权重为 0。</p><p><img src="https://lsworl.github.io/images/nlp-blog/transformer-causal-mask.svg" alt="Decoder因果遮罩"></p><h3 id="15-1-为什么训练可以并行，推理仍要逐步生成"><a href="#15-1-为什么训练可以并行，推理仍要逐步生成" class="headerlink" title="15.1 为什么训练可以并行，推理仍要逐步生成"></a>15.1 为什么训练可以并行，推理仍要逐步生成</h3><p>训练时，完整目标答案已知，因此可以把右移后的整个目标序列一起输入，并用 Mask 防止信息泄漏。</p><p>推理时，未来 token 尚不存在：</p><pre class="language-text" data-language="text"><code class="language-text">&lt;BOS> -> I&lt;BOS> I -> like&lt;BOS> I like -> NLP&lt;BOS> I like NLP -> &lt;EOS></code></pre><p>所以自回归 Decoder 的推理仍然通常逐步进行。</p><h2 id="16-Padding-Mask-与-Causal-Mask-不要混淆"><a href="#16-Padding-Mask-与-Causal-Mask-不要混淆" class="headerlink" title="16. Padding Mask 与 Causal Mask 不要混淆"></a>16. Padding Mask 与 Causal Mask 不要混淆</h2><p>这两种 Mask 解决不同问题。</p><h3 id="16-1-Padding-Mask"><a href="#16-1-Padding-Mask" class="headerlink" title="16.1 Padding Mask"></a>16.1 Padding Mask</h3><p>Padding Mask 遮住为了对齐长度而添加的 <code>&lt;PAD&gt;</code>：</p><pre class="language-text" data-language="text"><code class="language-text">样本1：我 喜欢 NLP &lt;PAD> &lt;PAD>样本2：我 非常 喜欢 学习 NLP</code></pre><p>对应布尔 Mask：</p><pre class="language-text" data-language="text"><code class="language-text">样本1：[False, False, False, True,  True]样本2：[False, False, False, False, False]</code></pre><p>在 PyTorch 的 Transformer API 中，布尔 padding mask 里的 <code>True</code> 通常表示该位置不参与注意力。</p><h3 id="16-2-Causal-Mask"><a href="#16-2-Causal-Mask" class="headerlink" title="16.2 Causal Mask"></a>16.2 Causal Mask</h3><p>Causal Mask 遮住当前位置之后的未来 token，通常是一个 $L\times L$ 的上三角矩阵。</p><table><thead><tr><th>Mask</th><th>典型形状</th><th>遮住什么</th><th>常见位置</th></tr></thead><tbody><tr><td>Padding Mask</td><td><code>[B, L]</code></td><td><code>&lt;PAD&gt;</code></td><td>Encoder 与 Decoder</td></tr><tr><td>Causal Mask</td><td><code>[L, L]</code></td><td>未来 token</td><td>自回归 Decoder</td></tr></tbody></table><p>实际训练 Decoder 时，两种 Mask 往往同时存在。</p><h2 id="17-Transformer-的完整训练流程"><a href="#17-Transformer-的完整训练流程" class="headerlink" title="17. Transformer 的完整训练流程"></a>17. Transformer 的完整训练流程</h2><p>以机器翻译为例：</p><pre class="language-text" data-language="text"><code class="language-text">1. 源句和目标句分词并转成 token id2. 对 batch 内序列进行 padding3. 构造 Decoder 输入和预测目标4. 构造 src padding mask5. 构造 tgt padding mask 与 causal mask6. 前向传播得到每个位置的 logits7. 使用交叉熵计算损失，忽略 PAD8. 反向传播9. 梯度裁剪10. 优化器更新参数</code></pre><p>模型输出形状为：</p><pre class="language-text" data-language="text"><code class="language-text">logits: [B, T, vocab_size]target: [B, T]</code></pre><p>计算损失前通常展平：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>    logits<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> vocab_size<span class="token punctuation">)</span><span class="token punctuation">,</span>    target<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>其中：</p><pre class="language-python" data-language="python"><code class="language-python">loss_fn <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span>ignore_index<span class="token operator">=</span>PAD_ID<span class="token punctuation">)</span></code></pre><p><code>CrossEntropyLoss</code> 直接接收 logits，不要提前做 Softmax。</p><h2 id="18-从张量形状理解-PyTorch-Transformer"><a href="#18-从张量形状理解-PyTorch-Transformer" class="headerlink" title="18. 从张量形状理解 PyTorch Transformer"></a>18. 从张量形状理解 PyTorch Transformer</h2><p>当设置 <code>batch_first=True</code> 时，主要形状如下：</p><p><img src="https://lsworl.github.io/images/nlp-blog/transformer-pytorch-shapes.svg" alt="PyTorch Transformer张量形状"></p><table><thead><tr><th>张量</th><th>形状</th><th>含义</th></tr></thead><tbody><tr><td><code>src</code></td><td><code>[B, S]</code></td><td>源 token id</td></tr><tr><td><code>tgt_in</code></td><td><code>[B, T]</code></td><td>右移后的目标 token id</td></tr><tr><td><code>src_emb</code></td><td><code>[B, S, E]</code></td><td>源 Embedding 加位置编码</td></tr><tr><td><code>tgt_emb</code></td><td><code>[B, T, E]</code></td><td>目标 Embedding 加位置编码</td></tr><tr><td><code>tgt_mask</code></td><td><code>[T, T]</code></td><td>因果遮罩</td></tr><tr><td><code>src_padding_mask</code></td><td><code>[B, S]</code></td><td>源序列 PAD 遮罩</td></tr><tr><td><code>memory</code></td><td><code>[B, S, E]</code></td><td>Encoder 输出</td></tr><tr><td><code>decoder_output</code></td><td><code>[B, T, E]</code></td><td>Decoder 输出</td></tr><tr><td><code>logits</code></td><td><code>[B, T, V]</code></td><td>词表 logits</td></tr></tbody></table><p>其中：</p><ul><li>$S$ 是源序列长度。</li><li>$T$ 是目标序列长度。</li><li>$E&#x3D;d_{model}$。</li><li>$V$ 是词表大小。</li></ul><h2 id="19-PyTorch-的-nn-MultiheadAttention"><a href="#19-PyTorch-的-nn-MultiheadAttention" class="headerlink" title="19. PyTorch 的 nn.MultiheadAttention"></a>19. PyTorch 的 <code>nn.MultiheadAttention</code></h2><p>先看最小例子：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">from</span> torch <span class="token keyword">import</span> nntorch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span>mha <span class="token operator">=</span> nn<span class="token punctuation">.</span>MultiheadAttention<span class="token punctuation">(</span>    embed_dim<span class="token operator">=</span><span class="token number">8</span><span class="token punctuation">,</span>    num_heads<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">,</span>    batch_first<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span>x <span class="token operator">=</span> torch<span class="token punctuation">.</span>randn<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">)</span>  <span class="token comment"># [batch, seq_len, embed_dim]</span>output<span class="token punctuation">,</span> weights <span class="token operator">=</span> mha<span class="token punctuation">(</span>    query<span class="token operator">=</span>x<span class="token punctuation">,</span>    key<span class="token operator">=</span>x<span class="token punctuation">,</span>    value<span class="token operator">=</span>x<span class="token punctuation">,</span>    need_weights<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>output<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>   <span class="token comment"># torch.Size([2, 4, 8])</span><span class="token keyword">print</span><span class="token punctuation">(</span>weights<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>  <span class="token comment"># torch.Size([2, 4, 4])</span></code></pre><p>默认情况下，<code>weights</code> 会对多个头取平均。如果希望保留每个头：</p><pre class="language-python" data-language="python"><code class="language-python">output<span class="token punctuation">,</span> weights <span class="token operator">=</span> mha<span class="token punctuation">(</span>    x<span class="token punctuation">,</span> x<span class="token punctuation">,</span> x<span class="token punctuation">,</span>    need_weights<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>    average_attn_weights<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>weights<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>  <span class="token comment"># [2, 2, 4, 4]</span></code></pre><p>这四个维度分别是：</p><pre class="language-text" data-language="text"><code class="language-text">[batch, num_heads, query_len, key_len]</code></pre><h2 id="20-不依赖高级-API-实现缩放点积注意力"><a href="#20-不依赖高级-API-实现缩放点积注意力" class="headerlink" title="20. 不依赖高级 API 实现缩放点积注意力"></a>20. 不依赖高级 API 实现缩放点积注意力</h2><p>先按照公式直接实现：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> math<span class="token keyword">import</span> torch<span class="token keyword">from</span> torch <span class="token keyword">import</span> nn<span class="token keyword">def</span> <span class="token function">scaled_dot_product_attention</span><span class="token punctuation">(</span>q<span class="token punctuation">,</span> k<span class="token punctuation">,</span> v<span class="token punctuation">,</span> mask<span class="token operator">=</span><span class="token boolean">None</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token triple-quoted-string string">"""    q: [..., query_len, head_dim]    k: [..., key_len, head_dim]    v: [..., key_len, value_dim]    mask: 可广播到 [..., query_len, key_len]，True 表示遮住    """</span>    scores <span class="token operator">=</span> q @ k<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>    scores <span class="token operator">=</span> scores <span class="token operator">/</span> math<span class="token punctuation">.</span>sqrt<span class="token punctuation">(</span>q<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> mask <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token boolean">None</span><span class="token punctuation">:</span>        scores <span class="token operator">=</span> scores<span class="token punctuation">.</span>masked_fill<span class="token punctuation">(</span>mask<span class="token punctuation">,</span> <span class="token builtin">float</span><span class="token punctuation">(</span><span class="token string">"-inf"</span><span class="token punctuation">)</span><span class="token punctuation">)</span>    weights <span class="token operator">=</span> torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>scores<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>    output <span class="token operator">=</span> weights @ v    <span class="token keyword">return</span> output<span class="token punctuation">,</span> weights</code></pre><p>用前面的手算矩阵验证：</p><pre class="language-python" data-language="python"><code class="language-python">x <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>output<span class="token punctuation">,</span> weights <span class="token operator">=</span> scaled_dot_product_attention<span class="token punctuation">(</span>x<span class="token punctuation">,</span> x<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>weights<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>output<span class="token punctuation">)</span></code></pre><p>近似输出：</p><pre class="language-text" data-language="text"><code class="language-text">weights =tensor([[0.4011, 0.1978, 0.4011],        [0.1978, 0.4011, 0.4011],        [0.2483, 0.2483, 0.5035]])output =tensor([[0.8022, 0.5989],        [0.5989, 0.8022],        [0.7517, 0.7517]])</code></pre><p>这与手算结果一致。</p><h2 id="21-从零实现一个多头自注意力层"><a href="#21-从零实现一个多头自注意力层" class="headerlink" title="21. 从零实现一个多头自注意力层"></a>21. 从零实现一个多头自注意力层</h2><p>下面把线性投影、拆头、Attention、合并头完整连接起来：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">MultiHeadSelfAttention</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> d_model<span class="token punctuation">,</span> num_heads<span class="token punctuation">,</span> dropout<span class="token operator">=</span><span class="token number">0.0</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        <span class="token keyword">if</span> d_model <span class="token operator">%</span> num_heads <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">:</span>            <span class="token keyword">raise</span> ValueError<span class="token punctuation">(</span><span class="token string">"d_model 必须能被 num_heads 整除"</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>num_heads <span class="token operator">=</span> num_heads        self<span class="token punctuation">.</span>head_dim <span class="token operator">=</span> d_model <span class="token operator">//</span> num_heads        self<span class="token punctuation">.</span>q_proj <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>d_model<span class="token punctuation">,</span> d_model<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>k_proj <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>d_model<span class="token punctuation">,</span> d_model<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>v_proj <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>d_model<span class="token punctuation">,</span> d_model<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>out_proj <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>d_model<span class="token punctuation">,</span> d_model<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>dropout <span class="token operator">=</span> nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>dropout<span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">split_heads</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        batch<span class="token punctuation">,</span> seq_len<span class="token punctuation">,</span> _ <span class="token operator">=</span> x<span class="token punctuation">.</span>shape        x <span class="token operator">=</span> x<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span>            batch<span class="token punctuation">,</span> seq_len<span class="token punctuation">,</span> self<span class="token punctuation">.</span>num_heads<span class="token punctuation">,</span> self<span class="token punctuation">.</span>head_dim        <span class="token punctuation">)</span>        <span class="token keyword">return</span> x<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">,</span> mask<span class="token operator">=</span><span class="token boolean">None</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        q <span class="token operator">=</span> self<span class="token punctuation">.</span>split_heads<span class="token punctuation">(</span>self<span class="token punctuation">.</span>q_proj<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span>        k <span class="token operator">=</span> self<span class="token punctuation">.</span>split_heads<span class="token punctuation">(</span>self<span class="token punctuation">.</span>k_proj<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span>        v <span class="token operator">=</span> self<span class="token punctuation">.</span>split_heads<span class="token punctuation">(</span>self<span class="token punctuation">.</span>v_proj<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span>        scores <span class="token operator">=</span> q @ k<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        scores <span class="token operator">=</span> scores <span class="token operator">/</span> math<span class="token punctuation">.</span>sqrt<span class="token punctuation">(</span>self<span class="token punctuation">.</span>head_dim<span class="token punctuation">)</span>        <span class="token keyword">if</span> mask <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token boolean">None</span><span class="token punctuation">:</span>            scores <span class="token operator">=</span> scores<span class="token punctuation">.</span>masked_fill<span class="token punctuation">(</span>mask<span class="token punctuation">,</span> <span class="token builtin">float</span><span class="token punctuation">(</span><span class="token string">"-inf"</span><span class="token punctuation">)</span><span class="token punctuation">)</span>        weights <span class="token operator">=</span> torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>scores<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        weights <span class="token operator">=</span> self<span class="token punctuation">.</span>dropout<span class="token punctuation">(</span>weights<span class="token punctuation">)</span>        context <span class="token operator">=</span> weights @ v        context <span class="token operator">=</span> context<span class="token punctuation">.</span>transpose<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">.</span>contiguous<span class="token punctuation">(</span><span class="token punctuation">)</span>        batch<span class="token punctuation">,</span> seq_len<span class="token punctuation">,</span> _<span class="token punctuation">,</span> _ <span class="token operator">=</span> context<span class="token punctuation">.</span>shape        context <span class="token operator">=</span> context<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span>batch<span class="token punctuation">,</span> seq_len<span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        <span class="token keyword">return</span> self<span class="token punctuation">.</span>out_proj<span class="token punctuation">(</span>context<span class="token punctuation">)</span><span class="token punctuation">,</span> weights</code></pre><p>测试形状：</p><pre class="language-python" data-language="python"><code class="language-python">x <span class="token operator">=</span> torch<span class="token punctuation">.</span>randn<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">,</span> <span class="token number">16</span><span class="token punctuation">)</span>attention <span class="token operator">=</span> MultiHeadSelfAttention<span class="token punctuation">(</span>d_model<span class="token operator">=</span><span class="token number">16</span><span class="token punctuation">,</span> num_heads<span class="token operator">=</span><span class="token number">4</span><span class="token punctuation">)</span>output<span class="token punctuation">,</span> weights <span class="token operator">=</span> attention<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>output<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>   <span class="token comment"># [2, 5, 16]</span><span class="token keyword">print</span><span class="token punctuation">(</span>weights<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>  <span class="token comment"># [2, 4, 5, 5]</span></code></pre><p>拆头前后形状变化是：</p><pre class="language-text" data-language="text"><code class="language-text">[B, L, d_model]-> [B, L, num_heads, head_dim]-> [B, num_heads, L, head_dim]</code></pre><p>计算完成后再按相反顺序合并。</p><h2 id="22-PyTorch-实现位置编码"><a href="#22-PyTorch-实现位置编码" class="headerlink" title="22. PyTorch 实现位置编码"></a>22. PyTorch 实现位置编码</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> math<span class="token keyword">import</span> torch<span class="token keyword">from</span> torch <span class="token keyword">import</span> nn<span class="token keyword">class</span> <span class="token class-name">PositionalEncoding</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> d_model<span class="token punctuation">,</span> max_len<span class="token operator">=</span><span class="token number">5000</span><span class="token punctuation">,</span> dropout<span class="token operator">=</span><span class="token number">0.1</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>dropout <span class="token operator">=</span> nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>dropout<span class="token punctuation">)</span>        position <span class="token operator">=</span> torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span>max_len<span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>        div_term <span class="token operator">=</span> torch<span class="token punctuation">.</span>exp<span class="token punctuation">(</span>            torch<span class="token punctuation">.</span>arange<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> d_model<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>            <span class="token operator">*</span> <span class="token punctuation">(</span><span class="token operator">-</span>math<span class="token punctuation">.</span>log<span class="token punctuation">(</span><span class="token number">10000.0</span><span class="token punctuation">)</span> <span class="token operator">/</span> d_model<span class="token punctuation">)</span>        <span class="token punctuation">)</span>        pe <span class="token operator">=</span> torch<span class="token punctuation">.</span>zeros<span class="token punctuation">(</span>max_len<span class="token punctuation">,</span> d_model<span class="token punctuation">)</span>        pe<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">:</span><span class="token punctuation">:</span><span class="token number">2</span><span class="token punctuation">]</span> <span class="token operator">=</span> torch<span class="token punctuation">.</span>sin<span class="token punctuation">(</span>position <span class="token operator">*</span> div_term<span class="token punctuation">)</span>        pe<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">:</span><span class="token punctuation">:</span><span class="token number">2</span><span class="token punctuation">]</span> <span class="token operator">=</span> torch<span class="token punctuation">.</span>cos<span class="token punctuation">(</span>position <span class="token operator">*</span> div_term<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>register_buffer<span class="token punctuation">(</span><span class="token string">"pe"</span><span class="token punctuation">,</span> pe<span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        x <span class="token operator">=</span> x <span class="token operator">+</span> self<span class="token punctuation">.</span>pe<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span>x<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">]</span>        <span class="token keyword">return</span> self<span class="token punctuation">.</span>dropout<span class="token punctuation">(</span>x<span class="token punctuation">)</span></code></pre><h3 id="22-1-为什么使用-register-buffer"><a href="#22-1-为什么使用-register-buffer" class="headerlink" title="22.1 为什么使用 register_buffer"></a>22.1 为什么使用 <code>register_buffer</code></h3><p>位置编码不是通过梯度学习的参数，所以不应该使用 <code>nn.Parameter</code>。但它需要：</p><ul><li>跟随模型一起移动到 CPU 或 GPU。</li><li>跟随 <code>state_dict</code> 保存。</li><li>不出现在优化器的可训练参数中。</li></ul><p><code>register_buffer</code> 正好满足这些要求。</p><h2 id="23-完整例子：Transformer-学习反转数字序列"><a href="#23-完整例子：Transformer-学习反转数字序列" class="headerlink" title="23. 完整例子：Transformer 学习反转数字序列"></a>23. 完整例子：Transformer 学习反转数字序列</h2><p>为了不依赖外部数据集，下面让 Transformer 学习一个明确任务：</p><pre class="language-text" data-language="text"><code class="language-text">输入：[3, 7, 5, 9]目标：[9, 5, 7, 3, EOS]</code></pre><p>这个任务虽然简单，但完整包含：</p><ul><li>变长序列。</li><li>Padding Mask。</li><li>Causal Mask。</li><li>Encoder-Decoder Attention。</li><li>目标序列右移。</li><li>交叉熵训练。</li><li>自回归推理。</li></ul><h3 id="23-1-定义数据"><a href="#23-1-定义数据" class="headerlink" title="23.1 定义数据"></a>23.1 定义数据</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> math<span class="token keyword">import</span> random<span class="token keyword">import</span> torch<span class="token keyword">from</span> torch <span class="token keyword">import</span> nn<span class="token keyword">from</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>utils<span class="token punctuation">.</span>rnn <span class="token keyword">import</span> pad_sequencetorch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span>random<span class="token punctuation">.</span>seed<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span>PAD <span class="token operator">=</span> <span class="token number">0</span>BOS <span class="token operator">=</span> <span class="token number">1</span>EOS <span class="token operator">=</span> <span class="token number">2</span>VOCAB_SIZE <span class="token operator">=</span> <span class="token number">13</span>  <span class="token comment"># 0、1、2是特殊token，3到12是普通数字token</span><span class="token keyword">def</span> <span class="token function">make_batch</span><span class="token punctuation">(</span>batch_size<span class="token operator">=</span><span class="token number">64</span><span class="token punctuation">,</span> min_len<span class="token operator">=</span><span class="token number">3</span><span class="token punctuation">,</span> max_len<span class="token operator">=</span><span class="token number">7</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    src_list <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>    tgt_list <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>    <span class="token keyword">for</span> _ <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span>batch_size<span class="token punctuation">)</span><span class="token punctuation">:</span>        length <span class="token operator">=</span> random<span class="token punctuation">.</span>randint<span class="token punctuation">(</span>min_len<span class="token punctuation">,</span> max_len<span class="token punctuation">)</span>        src <span class="token operator">=</span> torch<span class="token punctuation">.</span>randint<span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">,</span> VOCAB_SIZE<span class="token punctuation">,</span> <span class="token punctuation">(</span>length<span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">)</span>        tgt <span class="token operator">=</span> torch<span class="token punctuation">.</span>cat<span class="token punctuation">(</span><span class="token punctuation">[</span>            torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>BOS<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            src<span class="token punctuation">.</span>flip<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>EOS<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">]</span><span class="token punctuation">)</span>        src_list<span class="token punctuation">.</span>append<span class="token punctuation">(</span>src<span class="token punctuation">)</span>        tgt_list<span class="token punctuation">.</span>append<span class="token punctuation">(</span>tgt<span class="token punctuation">)</span>    src <span class="token operator">=</span> pad_sequence<span class="token punctuation">(</span>        src_list<span class="token punctuation">,</span> batch_first<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span> padding_value<span class="token operator">=</span>PAD    <span class="token punctuation">)</span>    tgt <span class="token operator">=</span> pad_sequence<span class="token punctuation">(</span>        tgt_list<span class="token punctuation">,</span> batch_first<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span> padding_value<span class="token operator">=</span>PAD    <span class="token punctuation">)</span>    tgt_in <span class="token operator">=</span> tgt<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token punctuation">:</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span>    tgt_out <span class="token operator">=</span> tgt<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">:</span><span class="token punctuation">]</span>    <span class="token keyword">return</span> src<span class="token punctuation">,</span> tgt_in<span class="token punctuation">,</span> tgt_out</code></pre><p><code>tgt_in</code> 与 <code>tgt_out</code> 错开一位：</p><pre class="language-text" data-language="text"><code class="language-text">tgt_in:  BOS 9 5 7 3tgt_out: 9   5 7 3 EOS</code></pre><h3 id="23-2-定义-Transformer-模型"><a href="#23-2-定义-Transformer-模型" class="headerlink" title="23.2 定义 Transformer 模型"></a>23.2 定义 Transformer 模型</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">TinyTransformer</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>        self<span class="token punctuation">,</span>        vocab_size<span class="token punctuation">,</span>        d_model<span class="token operator">=</span><span class="token number">64</span><span class="token punctuation">,</span>        num_heads<span class="token operator">=</span><span class="token number">4</span><span class="token punctuation">,</span>        num_layers<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">,</span>        dim_feedforward<span class="token operator">=</span><span class="token number">128</span><span class="token punctuation">,</span>        dropout<span class="token operator">=</span><span class="token number">0.1</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>d_model <span class="token operator">=</span> d_model        self<span class="token punctuation">.</span>src_embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>            vocab_size<span class="token punctuation">,</span> d_model<span class="token punctuation">,</span> padding_idx<span class="token operator">=</span>PAD        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>tgt_embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>            vocab_size<span class="token punctuation">,</span> d_model<span class="token punctuation">,</span> padding_idx<span class="token operator">=</span>PAD        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>position <span class="token operator">=</span> PositionalEncoding<span class="token punctuation">(</span>            d_model<span class="token operator">=</span>d_model<span class="token punctuation">,</span>            max_len<span class="token operator">=</span><span class="token number">100</span><span class="token punctuation">,</span>            dropout<span class="token operator">=</span>dropout<span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>transformer <span class="token operator">=</span> nn<span class="token punctuation">.</span>Transformer<span class="token punctuation">(</span>            d_model<span class="token operator">=</span>d_model<span class="token punctuation">,</span>            nhead<span class="token operator">=</span>num_heads<span class="token punctuation">,</span>            num_encoder_layers<span class="token operator">=</span>num_layers<span class="token punctuation">,</span>            num_decoder_layers<span class="token operator">=</span>num_layers<span class="token punctuation">,</span>            dim_feedforward<span class="token operator">=</span>dim_feedforward<span class="token punctuation">,</span>            dropout<span class="token operator">=</span>dropout<span class="token punctuation">,</span>            batch_first<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>output <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>d_model<span class="token punctuation">,</span> vocab_size<span class="token punctuation">)</span>    <span class="token decorator annotation punctuation">@staticmethod</span>    <span class="token keyword">def</span> <span class="token function">causal_mask</span><span class="token punctuation">(</span>size<span class="token punctuation">,</span> device<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> torch<span class="token punctuation">.</span>triu<span class="token punctuation">(</span>            torch<span class="token punctuation">.</span>ones<span class="token punctuation">(</span>size<span class="token punctuation">,</span> size<span class="token punctuation">,</span> dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">bool</span><span class="token punctuation">,</span> device<span class="token operator">=</span>device<span class="token punctuation">)</span><span class="token punctuation">,</span>            diagonal<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> src<span class="token punctuation">,</span> tgt_in<span class="token punctuation">)</span><span class="token punctuation">:</span>        src_padding_mask <span class="token operator">=</span> src<span class="token punctuation">.</span>eq<span class="token punctuation">(</span>PAD<span class="token punctuation">)</span>        tgt_padding_mask <span class="token operator">=</span> tgt_in<span class="token punctuation">.</span>eq<span class="token punctuation">(</span>PAD<span class="token punctuation">)</span>        tgt_mask <span class="token operator">=</span> self<span class="token punctuation">.</span>causal_mask<span class="token punctuation">(</span>tgt_in<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span> tgt_in<span class="token punctuation">.</span>device<span class="token punctuation">)</span>        scale <span class="token operator">=</span> math<span class="token punctuation">.</span>sqrt<span class="token punctuation">(</span>self<span class="token punctuation">.</span>d_model<span class="token punctuation">)</span>        src_emb <span class="token operator">=</span> self<span class="token punctuation">.</span>position<span class="token punctuation">(</span>self<span class="token punctuation">.</span>src_embedding<span class="token punctuation">(</span>src<span class="token punctuation">)</span> <span class="token operator">*</span> scale<span class="token punctuation">)</span>        tgt_emb <span class="token operator">=</span> self<span class="token punctuation">.</span>position<span class="token punctuation">(</span>self<span class="token punctuation">.</span>tgt_embedding<span class="token punctuation">(</span>tgt_in<span class="token punctuation">)</span> <span class="token operator">*</span> scale<span class="token punctuation">)</span>        hidden <span class="token operator">=</span> self<span class="token punctuation">.</span>transformer<span class="token punctuation">(</span>            src<span class="token operator">=</span>src_emb<span class="token punctuation">,</span>            tgt<span class="token operator">=</span>tgt_emb<span class="token punctuation">,</span>            tgt_mask<span class="token operator">=</span>tgt_mask<span class="token punctuation">,</span>            src_key_padding_mask<span class="token operator">=</span>src_padding_mask<span class="token punctuation">,</span>            tgt_key_padding_mask<span class="token operator">=</span>tgt_padding_mask<span class="token punctuation">,</span>            memory_key_padding_mask<span class="token operator">=</span>src_padding_mask<span class="token punctuation">,</span>        <span class="token punctuation">)</span>        <span class="token keyword">return</span> self<span class="token punctuation">.</span>output<span class="token punctuation">(</span>hidden<span class="token punctuation">)</span></code></pre><p><code>memory_key_padding_mask</code> 与 <code>src_key_padding_mask</code> 通常使用同一个源序列 Mask：</p><ul><li>Encoder Self-Attention 不应查看源 <code>&lt;PAD&gt;</code>。</li><li>Decoder Cross-Attention 也不应从源 <code>&lt;PAD&gt;</code> 中取信息。</li></ul><h3 id="23-3-训练模型"><a href="#23-3-训练模型" class="headerlink" title="23.3 训练模型"></a>23.3 训练模型</h3><pre class="language-python" data-language="python"><code class="language-python">device <span class="token operator">=</span> torch<span class="token punctuation">.</span>device<span class="token punctuation">(</span>    <span class="token string">"cuda"</span> <span class="token keyword">if</span> torch<span class="token punctuation">.</span>cuda<span class="token punctuation">.</span>is_available<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">else</span> <span class="token string">"cpu"</span><span class="token punctuation">)</span>model <span class="token operator">=</span> TinyTransformer<span class="token punctuation">(</span>VOCAB_SIZE<span class="token punctuation">)</span><span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">,</span> weight_decay<span class="token operator">=</span><span class="token number">0.01</span><span class="token punctuation">)</span>loss_fn <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span>ignore_index<span class="token operator">=</span>PAD<span class="token punctuation">)</span>model<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">for</span> step <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    src<span class="token punctuation">,</span> tgt_in<span class="token punctuation">,</span> tgt_out <span class="token operator">=</span> make_batch<span class="token punctuation">(</span><span class="token punctuation">)</span>    src <span class="token operator">=</span> src<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span>    tgt_in <span class="token operator">=</span> tgt_in<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span>    tgt_out <span class="token operator">=</span> tgt_out<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>src<span class="token punctuation">,</span> tgt_in<span class="token punctuation">)</span>    loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>        logits<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> VOCAB_SIZE<span class="token punctuation">)</span><span class="token punctuation">,</span>        tgt_out<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>utils<span class="token punctuation">.</span>clip_grad_norm_<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> <span class="token punctuation">(</span>step <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">)</span> <span class="token operator">%</span> <span class="token number">100</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">:</span>        <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"step=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>step <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">&#125;</span></span><span class="token string">, loss=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span><span class="token format-spec">.4f</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span><span class="token punctuation">)</span></code></pre><p>这是一个小型教学任务，真实训练还需要：</p><ul><li>验证集与测试集。</li><li>学习率 warmup 和衰减。</li><li>更可靠的数据加载流程。</li><li>保存与恢复 checkpoint。</li><li>根据验证指标进行早停或选模。</li></ul><h3 id="23-4-自回归推理"><a href="#23-4-自回归推理" class="headerlink" title="23.4 自回归推理"></a>23.4 自回归推理</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token decorator annotation punctuation">@torch<span class="token punctuation">.</span>no_grad</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">def</span> <span class="token function">greedy_decode</span><span class="token punctuation">(</span>model<span class="token punctuation">,</span> src<span class="token punctuation">,</span> max_len<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    src <span class="token operator">=</span> src<span class="token punctuation">.</span>to<span class="token punctuation">(</span>device<span class="token punctuation">)</span>    generated <span class="token operator">=</span> torch<span class="token punctuation">.</span>full<span class="token punctuation">(</span>        <span class="token punctuation">(</span>src<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        BOS<span class="token punctuation">,</span>        dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">long</span><span class="token punctuation">,</span>        device<span class="token operator">=</span>device<span class="token punctuation">,</span>    <span class="token punctuation">)</span>    finished <span class="token operator">=</span> torch<span class="token punctuation">.</span>zeros<span class="token punctuation">(</span>        src<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">,</span> dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">bool</span><span class="token punctuation">,</span> device<span class="token operator">=</span>device    <span class="token punctuation">)</span>    <span class="token keyword">for</span> _ <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span>max_len<span class="token punctuation">)</span><span class="token punctuation">:</span>        logits <span class="token operator">=</span> model<span class="token punctuation">(</span>src<span class="token punctuation">,</span> generated<span class="token punctuation">)</span>        next_token <span class="token operator">=</span> logits<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        next_token <span class="token operator">=</span> torch<span class="token punctuation">.</span>where<span class="token punctuation">(</span>            finished<span class="token punctuation">,</span>            torch<span class="token punctuation">.</span>full_like<span class="token punctuation">(</span>next_token<span class="token punctuation">,</span> PAD<span class="token punctuation">)</span><span class="token punctuation">,</span>            next_token<span class="token punctuation">,</span>        <span class="token punctuation">)</span>        generated <span class="token operator">=</span> torch<span class="token punctuation">.</span>cat<span class="token punctuation">(</span>            <span class="token punctuation">[</span>generated<span class="token punctuation">,</span> next_token<span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span>        <span class="token punctuation">)</span>        finished <span class="token operator">=</span> finished <span class="token operator">|</span> next_token<span class="token punctuation">.</span>eq<span class="token punctuation">(</span>EOS<span class="token punctuation">)</span>        <span class="token keyword">if</span> finished<span class="token punctuation">.</span><span class="token builtin">all</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>            <span class="token keyword">break</span>    <span class="token keyword">return</span> generated<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">:</span><span class="token punctuation">]</span></code></pre><p>测试：</p><pre class="language-python" data-language="python"><code class="language-python">test_src <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">7</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">,</span> <span class="token number">9</span><span class="token punctuation">,</span> PAD<span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">6</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">7</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>result <span class="token operator">=</span> greedy_decode<span class="token punctuation">(</span>model<span class="token punctuation">,</span> test_src<span class="token punctuation">,</span> max_len<span class="token operator">=</span><span class="token number">7</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>result<span class="token punctuation">.</span>cpu<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>训练充分后，结果应接近：</p><pre class="language-text" data-language="text"><code class="language-text">tensor([[9, 5, 7, 3, 2, 0],        [7, 3, 6, 8, 4, 2]])</code></pre><p>其中 <code>2</code> 是 <code>&lt;EOS&gt;</code>，<code>0</code> 是已经结束样本后面补出的 <code>&lt;PAD&gt;</code>。</p><h2 id="24-nn-Transformer-中的重要参数"><a href="#24-nn-Transformer-中的重要参数" class="headerlink" title="24. nn.Transformer 中的重要参数"></a>24. <code>nn.Transformer</code> 中的重要参数</h2><pre class="language-python" data-language="python"><code class="language-python">nn<span class="token punctuation">.</span>Transformer<span class="token punctuation">(</span>    d_model<span class="token operator">=</span><span class="token number">512</span><span class="token punctuation">,</span>    nhead<span class="token operator">=</span><span class="token number">8</span><span class="token punctuation">,</span>    num_encoder_layers<span class="token operator">=</span><span class="token number">6</span><span class="token punctuation">,</span>    num_decoder_layers<span class="token operator">=</span><span class="token number">6</span><span class="token punctuation">,</span>    dim_feedforward<span class="token operator">=</span><span class="token number">2048</span><span class="token punctuation">,</span>    dropout<span class="token operator">=</span><span class="token number">0.1</span><span class="token punctuation">,</span>    activation<span class="token operator">=</span><span class="token string">"relu"</span><span class="token punctuation">,</span>    batch_first<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span>    norm_first<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><table><thead><tr><th>参数</th><th>含义</th><th>常见错误</th></tr></thead><tbody><tr><td><code>d_model</code></td><td>token 表示维度</td><td>与 Embedding 维度不一致</td></tr><tr><td><code>nhead</code></td><td>注意力头数</td><td><code>d_model</code> 不能被它整除</td></tr><tr><td><code>num_encoder_layers</code></td><td>Encoder 层数</td><td>层数增加后显存和计算量上升</td></tr><tr><td><code>num_decoder_layers</code></td><td>Decoder 层数</td><td>Encoder-only 任务不需要 Decoder</td></tr><tr><td><code>dim_feedforward</code></td><td>FFN 中间维度</td><td>误以为是词表大小</td></tr><tr><td><code>dropout</code></td><td>Dropout 概率</td><td>推理时忘记 <code>model.eval()</code></td></tr><tr><td><code>batch_first</code></td><td>是否使用 <code>[B,L,E]</code></td><td>与实际输入维度顺序不一致</td></tr><tr><td><code>norm_first</code></td><td>是否使用 Pre-Norm</td><td>误以为会改变输出形状</td></tr></tbody></table><p>PyTorch 的 <code>nn.Transformer</code> 更接近原始架构的教学参考实现。理解基础组件后，再学习不同模型库中的预训练 Transformer 会容易很多。</p><h2 id="25-Transformer-的计算复杂度"><a href="#25-Transformer-的计算复杂度" class="headerlink" title="25. Transformer 的计算复杂度"></a>25. Transformer 的计算复杂度</h2><p>标准 Self-Attention 的分数矩阵形状是：</p><p>$$<br>[L,L]<br>$$</p><p>因此与序列长度相关的主要计算和内存代价通常为：</p><p>$$<br>O(L^2)<br>$$</p><p>如果序列长度从 512 增加到 1024，注意力矩阵元素数量会从：</p><p>$$<br>512^2&#x3D;262144<br>$$</p><p>增加到：</p><p>$$<br>1024^2&#x3D;1048576<br>$$</p><p>也就是约 4 倍。</p><p>这说明 Transformer 的并行能力很强，但标准全局 Attention 在超长序列上也会付出明显代价。</p><p>需要同时理解两句话：</p><ul><li>Transformer 训练时比递归模型更容易并行。</li><li>标准 Self-Attention 对序列长度具有二次复杂度。</li></ul><p>它们并不矛盾。</p><h2 id="26-Transformer-与-BERT、GPT-的关系"><a href="#26-Transformer-与-BERT、GPT-的关系" class="headerlink" title="26. Transformer 与 BERT、GPT 的关系"></a>26. Transformer 与 BERT、GPT 的关系</h2><p>Transformer 是基础积木，BERT、GPT 等是基于这套积木形成的模型家族。</p><h3 id="26-1-Encoder-only"><a href="#26-1-Encoder-only" class="headerlink" title="26.1 Encoder-only"></a>26.1 Encoder-only</h3><p>只堆叠 Transformer Encoder，可以让每个 token 双向查看上下文，适合理解类任务。</p><pre class="language-text" data-language="text"><code class="language-text">完整输入 -> 双向 Self-Attention -> 上下文表示</code></pre><h3 id="26-2-Decoder-only"><a href="#26-2-Decoder-only" class="headerlink" title="26.2 Decoder-only"></a>26.2 Decoder-only</h3><p>只堆叠带 Causal Mask 的 Transformer Decoder 风格模块，适合预测下一个 token。</p><pre class="language-text" data-language="text"><code class="language-text">历史 token -> Causal Self-Attention -> 下一个 token</code></pre><h3 id="26-3-Encoder-Decoder"><a href="#26-3-Encoder-Decoder" class="headerlink" title="26.3 Encoder-Decoder"></a>26.3 Encoder-Decoder</h3><p>Encoder 理解输入，Decoder 通过 Cross-Attention 读取 Encoder 输出，适合条件生成。</p><pre class="language-text" data-language="text"><code class="language-text">输入序列 -> Encoder -> Memory目标前缀 -> Decoder + Memory -> 下一个 token</code></pre><p>“Transformer”描述的是网络结构，而“预训练语言模型”还会涉及：</p><ul><li>预训练目标。</li><li>训练数据。</li><li>Tokenizer。</li><li>优化策略。</li><li>微调或对齐方式。</li></ul><p>不要把这些概念混为一层。</p><h2 id="27-常见错误"><a href="#27-常见错误" class="headerlink" title="27. 常见错误"></a>27. 常见错误</h2><h3 id="27-1-忘记位置编码"><a href="#27-1-忘记位置编码" class="headerlink" title="27.1 忘记位置编码"></a>27.1 忘记位置编码</h3><p>如果没有循环、卷积或位置编码，Self-Attention 本身无法可靠表达 token 顺序。</p><h3 id="27-2-把-Q、K、V-理解成三个固定输入"><a href="#27-2-把-Q、K、V-理解成三个固定输入" class="headerlink" title="27.2 把 Q、K、V 理解成三个固定输入"></a>27.2 把 Q、K、V 理解成三个固定输入</h3><p>Self-Attention 中它们通常由同一个输入经过三组可学习线性投影得到。</p><h3 id="27-3-Softmax-维度写错"><a href="#27-3-Softmax-维度写错" class="headerlink" title="27.3 Softmax 维度写错"></a>27.3 Softmax 维度写错</h3><p>应该沿 Key 所在的最后一个维度归一化：</p><pre class="language-python" data-language="python"><code class="language-python">weights <span class="token operator">=</span> torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>scores<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>这样每个 Query 对所有 Key 的权重之和为 1。</p><h3 id="27-4-忘记除以-sqrt-d-k"><a href="#27-4-忘记除以-sqrt-d-k" class="headerlink" title="27.4 忘记除以 $\sqrt{d_k}$"></a>27.4 忘记除以 $\sqrt{d_k}$</h3><p>这可能让点积分数过大，使 Softmax 过早饱和。</p><h3 id="27-5-Decoder-训练时没有-Causal-Mask"><a href="#27-5-Decoder-训练时没有-Causal-Mask" class="headerlink" title="27.5 Decoder 训练时没有 Causal Mask"></a>27.5 Decoder 训练时没有 Causal Mask</h3><p>模型会直接看到未来答案，训练损失可能很好看，但推理时无法正常工作。</p><h3 id="27-6-只设置-src-key-padding-mask"><a href="#27-6-只设置-src-key-padding-mask" class="headerlink" title="27.6 只设置 src_key_padding_mask"></a>27.6 只设置 <code>src_key_padding_mask</code></h3><p>Encoder 不看源 PAD 还不够，Decoder 的 Cross-Attention 也要通过 <code>memory_key_padding_mask</code> 忽略源 PAD。</p><h3 id="27-7-Mask-中-True-的语义理解反了"><a href="#27-7-Mask-中-True-的语义理解反了" class="headerlink" title="27.7 Mask 中 True 的语义理解反了"></a>27.7 Mask 中 <code>True</code> 的语义理解反了</h3><p>在 PyTorch <code>nn.Transformer</code> 常用的布尔 Mask 中，<code>True</code> 表示该位置被遮住。不同底层 API 的 Mask 约定可能不同，使用前应检查对应文档。</p><h3 id="27-8-d-model-不能被头数整除"><a href="#27-8-d-model-不能被头数整除" class="headerlink" title="27.8 d_model 不能被头数整除"></a>27.8 <code>d_model</code> 不能被头数整除</h3><p>每个头需要获得相同的 <code>head_dim</code>，因此创建层时会直接报错。</p><h3 id="27-9-对-logits-先做-Softmax"><a href="#27-9-对-logits-先做-Softmax" class="headerlink" title="27.9 对 logits 先做 Softmax"></a>27.9 对 logits 先做 Softmax</h3><p>错误：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span> target<span class="token punctuation">)</span></code></pre><p>正确：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> target<span class="token punctuation">)</span></code></pre><h3 id="27-10-推理时忘记-eval-和-no-grad"><a href="#27-10-推理时忘记-eval-和-no-grad" class="headerlink" title="27.10 推理时忘记 eval() 和 no_grad()"></a>27.10 推理时忘记 <code>eval()</code> 和 <code>no_grad()</code></h3><p>推理应关闭 Dropout 并避免构建梯度图：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    output <span class="token operator">=</span> model<span class="token punctuation">(</span>src<span class="token punctuation">,</span> tgt<span class="token punctuation">)</span></code></pre><h3 id="27-11-把-Attention-权重当成完整解释"><a href="#27-11-把-Attention-权重当成完整解释" class="headerlink" title="27.11 把 Attention 权重当成完整解释"></a>27.11 把 Attention 权重当成完整解释</h3><p>Attention 权重能帮助观察模型的信息聚合方式，但不能自动等价为严格的因果解释或完整决策依据。</p><h2 id="28-推荐的学习与调试顺序"><a href="#28-推荐的学习与调试顺序" class="headerlink" title="28. 推荐的学习与调试顺序"></a>28. 推荐的学习与调试顺序</h2><p>学习 Transformer 时，可以按以下顺序逐步验证：</p><pre class="language-text" data-language="text"><code class="language-text">1. 手算单头 Attention2. 用 PyTorch 复现手算结果3. 理解 Q、K、V 的形状4. 实现拆头与合并头5. 加入位置编码6. 构造 Padding Mask7. 构造 Causal Mask8. 搭建单个 Encoder 层9. 搭建 Encoder-Decoder10. 用小任务验证完整训练和推理</code></pre><p>调试时优先打印：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"src:"</span><span class="token punctuation">,</span> src<span class="token punctuation">.</span>shape<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"tgt_in:"</span><span class="token punctuation">,</span> tgt_in<span class="token punctuation">.</span>shape<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"src_padding_mask:"</span><span class="token punctuation">,</span> src_padding_mask<span class="token punctuation">.</span>shape<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"tgt_mask:"</span><span class="token punctuation">,</span> tgt_mask<span class="token punctuation">.</span>shape<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"logits:"</span><span class="token punctuation">,</span> logits<span class="token punctuation">.</span>shape<span class="token punctuation">)</span></code></pre><p>对于 Attention 权重，检查：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">print</span><span class="token punctuation">(</span>weights<span class="token punctuation">.</span><span class="token builtin">sum</span><span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>在没有把一整行全部遮住的情况下，结果应接近 1。</p><h2 id="29-总结"><a href="#29-总结" class="headerlink" title="29. 总结"></a>29. 总结</h2><p>Transformer 的主线可以浓缩为：</p><pre class="language-text" data-language="text"><code class="language-text">token id  -> Embedding + Positional Encoding  -> Q、K、V 线性投影  -> 缩放点积 Attention  -> Multi-Head 拼接  -> 残差连接 + LayerNorm  -> FFN  -> 堆叠多个 Encoder / Decoder 层  -> 输出 token logits</code></pre><p>需要真正掌握的核心包括：</p><ul><li>Attention 根据 Query 与 Key 的匹配程度，对 Value 加权求和。</li><li>Self-Attention 的 Q、K、V 来自同一序列的不同投影。</li><li>Multi-Head Attention 在多个表示子空间中并行建模关系。</li><li>没有递归后必须显式注入位置信息。</li><li>Encoder 使用双向 Self-Attention 理解输入。</li><li>自回归 Decoder 必须用 Causal Mask 遮住未来。</li><li>Padding Mask 与 Causal Mask 解决的是不同问题。</li><li>Attention 负责 token 间交互，FFN 负责位置内特征变换。</li><li>残差连接和 LayerNorm 帮助深层网络稳定训练。</li><li>标准 Self-Attention 容易并行，但对序列长度具有二次复杂度。</li></ul><p>当你能独立写出下面这行公式，并说清楚每个矩阵的来源、形状和作用时，就已经抓住了 Transformer 最重要的核心：</p><p>$$<br>\operatorname{Attention}(Q,K,V)<br>&#x3D;\operatorname{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V<br>$$</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>Ashish Vaswani et al., <em>Attention Is All You Need</em>: <a href="https://arxiv.org/abs/1706.03762">https://arxiv.org/abs/1706.03762</a></li><li>PyTorch Documentation, <code>torch.nn.Transformer</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.Transformer.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.Transformer.html</a></li><li>PyTorch Documentation, <code>torch.nn.MultiheadAttention</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.MultiheadAttention.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.MultiheadAttention.html</a></li><li>PyTorch Documentation, <code>torch.nn.TransformerEncoderLayer</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.TransformerEncoderLayer.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.TransformerEncoderLayer.html</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/11/nlp-transformer-attention-pytorch/</id>
    <link href="https://lsworl.github.io/2026/08/11/nlp-transformer-attention-pytorch/"/>
    <published>2026-08-10T16:00:00.000Z</published>
    <summary>从RNN Seq2Seq的局限出发，逐步理解Q、K、V、缩放点积注意力、多头注意力、位置编码、Mask、Encoder与Decoder，并用PyTorch实现完整Transformer。</summary>
    <title>Transformer详解：从自注意力手算到PyTorch完整实现</title>
    <updated>2026-08-12T07:07:56.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="机器学习" scheme="https://lsworl.github.io/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="机器学习" scheme="https://lsworl.github.io/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="深度学习" scheme="https://lsworl.github.io/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <category term="优化算法" scheme="https://lsworl.github.io/tags/%E4%BC%98%E5%8C%96%E7%AE%97%E6%B3%95/"/>
    <category term="AdamW" scheme="https://lsworl.github.io/tags/AdamW/"/>
    <content>
      <![CDATA[<p>在上一篇 Adam Optimizer 文章中，我们知道 Adam 会同时维护梯度的一阶矩和二阶矩，并根据历史梯度为不同参数调整有效步长。</p><p>当模型出现过拟合时，我们还希望限制参数避免变得过大，于是经常会为优化器设置 <code>weight_decay</code>：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">1e-2</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这里为什么常用 AdamW，而不是简单地给 Adam 加一个 L2 正则项？AdamW 中的字母 W 又代表什么？</p><p>一句话概括：</p><blockquote><p>AdamW 把权重衰减从 Adam 的梯度与矩估计中分离出来，让“根据损失优化参数”和“按比例缩小参数”成为两件独立的事。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/ml-blog/adamw-decoupled-flow.svg" alt="AdamW 将梯度更新和权重衰减拆成两条路径"></p><h2 id="1-先回顾-Adam"><a href="#1-先回顾-Adam" class="headerlink" title="1. 先回顾 Adam"></a>1. 先回顾 Adam</h2><p>Adam 对当前梯度 $g_t$ 维护两种状态。</p><p>一阶矩：</p><p>$$<br>m_t&#x3D;\beta_1m_{t-1}+(1-\beta_1)g_t<br>$$</p><p>二阶矩：</p><p>$$<br>v_t&#x3D;\beta_2v_{t-1}+(1-\beta_2)g_t^2<br>$$</p><p>经过偏差修正后：</p><p>$$<br>\hat{m}_t&#x3D;\frac{m_t}{1-\beta_1^t}<br>$$</p><p>$$<br>\hat{v}_t&#x3D;\frac{v_t}{1-\beta_2^t}<br>$$</p><p>Adam 的参数更新为：</p><p>$$<br>\theta_t<br>&#x3D;\theta_{t-1}<br>-\alpha\frac{\hat{m}_t}{\sqrt{\hat{v}_t}+\epsilon}<br>$$</p><p>其中：</p><ul><li>$\theta$ 是模型参数。</li><li>$\alpha$ 是学习率。</li><li>$m_t$ 记录近期梯度方向。</li><li>$v_t$ 记录近期梯度平方的尺度。</li></ul><p>如果对 Adam 本身还不熟悉，可以先阅读上一篇文章：</p><p><a href="https://lsworl.github.io/2026/08/09/machine-learning-adam-optimizer/">Adam Optimizer：从梯度下降到自适应学习率</a></p><h2 id="2-为什么需要限制权重大小"><a href="#2-为什么需要限制权重大小" class="headerlink" title="2. 为什么需要限制权重大小"></a>2. 为什么需要限制权重大小</h2><p>神经网络参数很多，模型可能把训练数据中的细节甚至噪声也记住。</p><p>一种常见思路是：在保证拟合数据的同时，不希望权重无限增大。</p><p>较小的权重并不自动等于更好的模型，但限制权重规模通常可以：</p><ul><li>减少模型对个别输入特征的过度依赖。</li><li>缓解过拟合。</li><li>让参数更新更加平稳。</li><li>改善部分任务上的验证集表现。</li></ul><p>实现这一目标时，经常会遇到两个容易混淆的概念：</p><ul><li>L2 正则化。</li><li>权重衰减（Weight Decay）。</li></ul><h2 id="3-L2-正则化是什么"><a href="#3-L2-正则化是什么" class="headerlink" title="3. L2 正则化是什么"></a>3. L2 正则化是什么</h2><p>L2 正则化会在原始损失后面增加参数平方惩 罚项：</p><p>$$<br>L_{total}(\theta)<br>&#x3D;L_{data}(\theta)<br>+\frac{\lambda}{2}\lVert\theta\rVert_2^2<br>$$</p><p>其中：</p><ul><li>$L_{data}$ 是模型在训练数据上的原始损失。</li><li>$\lambda$ 控制正则化强度。</li><li>$\lVert\theta\rVert_2^2$ 是参数平方和。</li></ul><p>对参数求导：</p><p>$$<br>\nabla_\theta L_{total}<br>&#x3D;\nabla_\theta L_{data}+\lambda\theta<br>$$</p><p>令：<br>$$<br>g_t &#x3D;\nabla_\theta L_{data}<br>$$<br>则加入 L2 后，送给优化器的梯度就变成：<br>$$<br>g_t^{L2}&#x3D;g_t+\lambda\theta_{t-1}<br>$$</p><p>也就是说，L2 正则化通过“修改梯度”来影响参数。</p><h2 id="4-权重衰减是什么"><a href="#4-权重衰减是什么" class="headerlink" title="4. 权重衰减是什么"></a>4. 权重衰减是什么</h2><p>权重衰减的直觉更加直接：每次更新时，都让参数按比例缩小一点。</p><p>如果暂时不考虑损失梯度，可以写成：</p><p>$$<br>\theta_t&#x3D;(1-\alpha\lambda)\theta_{t-1}<br>$$</p><p>例如：</p><p>$$<br>\theta&#x3D;10,\quad \alpha&#x3D;0.1,\quad \lambda&#x3D;0.01<br>$$</p><p>那么衰减一次后：</p><p>$$<br>\theta’&#x3D;(1-0.1\times0.01)\times10&#x3D;9.99<br>$$</p><p>参数每一步都按固定比例衰减，而不是把衰减项混入损失梯度。</p><h2 id="5-为什么在普通-SGD-中两者看起来一样"><a href="#5-为什么在普通-SGD-中两者看起来一样" class="headerlink" title="5. 为什么在普通 SGD 中两者看起来一样"></a>5. 为什么在普通 SGD 中两者看起来一样</h2><p>普通 SGD 加 L2 后：</p><p>$$<br>\theta_t<br>&#x3D;\theta_{t-1}-\alpha(g_t+\lambda\theta_{t-1})<br>$$</p><p>展开：</p><p>$$<br>\theta_t<br>&#x3D;(1-\alpha\lambda)\theta_{t-1}<br>-\alpha g_t<br>$$</p><p>这个式子恰好可以拆成两部分：</p><ol><li>$(1-\alpha\lambda)\theta_{t-1}$：缩小原参数。</li><li>$-\alpha g_t$：根据损失梯度更新参数。</li></ol><p>因此，对于不带自适应缩放的普通 SGD，在常见设置下，L2 正则化和权重衰减可以得到等价的更新形式。</p><p>这也导致很多代码和资料把二者混在一起讨论。</p><h2 id="6-为什么到了-Adam-中就不一样了"><a href="#6-为什么到了-Adam-中就不一样了" class="headerlink" title="6. 为什么到了 Adam 中就不一样了"></a>6. 为什么到了 Adam 中就不一样了</h2><p>Adam 不会直接使用梯度 $g_t$，而是先用它更新 $m_t$ 和 $v_t$。</p><p>如果直接把 L2 项加入梯度：</p><p>$$<br>g_t’&#x3D;g_t+\lambda\theta_{t-1}<br>$$</p><p>那么 Adam 实际记录的是：</p><p>$$<br>m_t&#x3D;\beta_1m_{t-1}+(1-\beta_1)g_t’<br>$$</p><p>$$<br>v_t&#x3D;\beta_2v_{t-1}+(1-\beta_2)(g_t’)^2<br>$$</p><p>这意味着 $\lambda\theta$ 不只是缩小参数，它还会：</p><ul><li>进入一阶矩，影响历史更新方向。</li><li>进入二阶矩，影响自适应缩放尺度。</li><li>与真正的数据梯度混合。</li><li>对不同参数产生不同的有效衰减程度。</li></ul><p>因此，在 Adam 这类自适应优化器中，“把 L2 项加入梯度”与“直接按比例衰减参数”不再等价。</p><p><img src="https://lsworl.github.io/images/ml-blog/adamw-vs-adam-l2.svg" alt="Adam 加 L2 与 AdamW 的核心区别"></p><h2 id="7-AdamW-的解耦更新"><a href="#7-AdamW-的解耦更新" class="headerlink" title="7. AdamW 的解耦更新"></a>7. AdamW 的解耦更新</h2><p>AdamW 仍然使用原始数据梯度更新一阶矩和二阶矩：</p><p>$$<br>g_t&#x3D;\nabla_\theta L_{data}<br>$$</p><p>$$<br>m_t&#x3D;\beta_1m_{t-1}+(1-\beta_1)g_t<br>$$</p><p>$$<br>v_t&#x3D;\beta_2v_{t-1}+(1-\beta_2)g_t^2<br>$$</p><p>然后将 Adam 更新和权重衰减分开执行：</p><p>$$<br>\theta_t<br>&#x3D;\theta_{t-1}<br>-\alpha\frac{\hat{m}_t}{\sqrt{\hat{v}<em>t}+\epsilon}<br>-\alpha\lambda\theta</em>{t-1}<br>$$</p><p>也可以写成：</p><p>$$<br>\theta_t<br>&#x3D;(1-\alpha\lambda)\theta_{t-1}<br>-\alpha\frac{\hat{m}_t}{\sqrt{\hat{v}_t}+\epsilon}<br>$$</p><p>这里的关键不是多减了一项，而是：</p><blockquote><p>权重衰减项不进入 $m_t$ 和 $v_t$，因此不会污染 Adam 对梯度方向和梯度尺度的估计。</p></blockquote><p>字母 W 就来自 Weight Decay。</p><h2 id="8-用“零梯度”理解解耦"><a href="#8-用“零梯度”理解解耦" class="headerlink" title="8. 用“零梯度”理解解耦"></a>8. 用“零梯度”理解解耦</h2><p>假设某一步数据损失对参数的梯度刚好为 0：</p><p>$$<br>g_t&#x3D;0<br>$$</p><h3 id="Adam-L2"><a href="#Adam-L2" class="headerlink" title="Adam + L2"></a>Adam + L2</h3><p>加入 L2 后，优化器看到的梯度仍然不是 0：</p><p>$$<br>g_t’&#x3D;\lambda\theta<br>$$</p><p>它会进入一阶矩和二阶矩，并受到 Adam 自适应缩放的影响。</p><h3 id="AdamW"><a href="#AdamW" class="headerlink" title="AdamW"></a>AdamW</h3><p>AdamW 的梯度路径仍然看到 $g_t&#x3D;0$，而权重衰减独立执行：</p><p>$$<br>\theta_t&#x3D;(1-\alpha\lambda)\theta_{t-1}<br>$$</p><p>这种行为更容易解释：即使当前数据梯度为 0，参数仍按照设定比例衰减，但优化器的矩估计不会把衰减误认为数据梯度。</p><h2 id="9-手算一次-AdamW-更新"><a href="#9-手算一次-AdamW-更新" class="headerlink" title="9. 手算一次 AdamW 更新"></a>9. 手算一次 AdamW 更新</h2><p>假设：</p><p>$$<br>\theta_0&#x3D;1,\quad<br>g_1&#x3D;2<br>$$</p><p>设置：</p><p>$$<br>\alpha&#x3D;0.001,\quad<br>\lambda&#x3D;0.01,\quad<br>\beta_1&#x3D;0.9,\quad<br>\beta_2&#x3D;0.999<br>$$</p><p>为了方便手算，暂时忽略非常小的 $\epsilon$。</p><p>第一步 Adam 的偏差修正结果为：</p><p>$$<br>\hat{m}_1&#x3D;2<br>$$</p><p>$$<br>\hat{v}_1&#x3D;4<br>$$</p><p>Adam 梯度更新量：</p><p>$$<br>\alpha\frac{\hat{m}_1}{\sqrt{\hat{v}_1}}<br>&#x3D;0.001\times\frac{2}{2}<br>&#x3D;0.001<br>$$</p><p>独立权重衰减量：</p><p>$$<br>\alpha\lambda\theta_0<br>&#x3D;0.001\times0.01\times1<br>&#x3D;0.00001<br>$$</p><p>最终参数：</p><p>$$<br>\theta_1<br>&#x3D;1-0.001-0.00001<br>&#x3D;0.99899<br>$$</p><p>可以看到，梯度更新和权重衰减各自有清晰的来源。</p><p><img src="https://lsworl.github.io/images/ml-blog/adamw-hand-example.svg" alt="AdamW 一次参数更新的手算结果"></p><h2 id="10-Adam、Adam-L2-和-AdamW-对比"><a href="#10-Adam、Adam-L2-和-AdamW-对比" class="headerlink" title="10. Adam、Adam + L2 和 AdamW 对比"></a>10. Adam、Adam + L2 和 AdamW 对比</h2><table><thead><tr><th>方法</th><th>矩估计使用的梯度</th><th>参数是否直接衰减</th><th>特点</th></tr></thead><tbody><tr><td>Adam</td><td>$g_t$</td><td>否</td><td>没有额外正则化</td></tr><tr><td>Adam + L2</td><td>$g_t+\lambda\theta$</td><td>否</td><td>L2 项也进入一阶矩和二阶矩</td></tr><tr><td>AdamW</td><td>$g_t$</td><td>是</td><td>梯度更新与权重衰减解耦</td></tr></tbody></table><p>如果 <code>weight_decay=0</code>，AdamW 不执行参数衰减，其核心梯度更新与 Adam 对齐。</p><p>AdamW 的优势并不是“比 Adam 多一个字母”，而是让正则化强度不再被自适应梯度缩放隐式改变。</p><h2 id="11-PyTorch-中如何使用-AdamW"><a href="#11-PyTorch-中如何使用-AdamW" class="headerlink" title="11. PyTorch 中如何使用 AdamW"></a>11. PyTorch 中如何使用 AdamW</h2><p>最常见的写法：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">,</span>    betas<span class="token operator">=</span><span class="token punctuation">(</span><span class="token number">0.9</span><span class="token punctuation">,</span> <span class="token number">0.999</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    eps<span class="token operator">=</span><span class="token number">1e-8</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">1e-2</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>当前 PyTorch 官方接口中，AdamW 的常见默认值包括：</p><table><thead><tr><th>参数</th><th align="right">常见默认值</th><th>作用</th></tr></thead><tbody><tr><td><code>lr</code></td><td align="right"><code>1e-3</code></td><td>基础学习率</td></tr><tr><td><code>betas</code></td><td align="right"><code>(0.9, 0.999)</code></td><td>一阶矩和二阶矩的衰减系数</td></tr><tr><td><code>eps</code></td><td align="right"><code>1e-8</code></td><td>数值稳定项</td></tr><tr><td><code>weight_decay</code></td><td align="right"><code>1e-2</code></td><td>解耦式权重衰减强度</td></tr><tr><td><code>amsgrad</code></td><td align="right"><code>False</code></td><td>是否使用 AMSGrad 变体</td></tr></tbody></table><p>即使库提供默认值，实践中也建议显式写出 <code>lr</code> 和 <code>weight_decay</code>，让实验配置更加清楚。</p><h2 id="12-使用-AdamW-完整训练一个-MLP"><a href="#12-使用-AdamW-完整训练一个-MLP" class="headerlink" title="12. 使用 AdamW 完整训练一个 MLP"></a>12. 使用 AdamW 完整训练一个 MLP</h2><p>下面继续使用 XOR 二分类任务：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">from</span> torch <span class="token keyword">import</span> nntorch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">42</span><span class="token punctuation">)</span>X <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>y <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">class</span> <span class="token class-name">XORNet</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>net <span class="token operator">=</span> nn<span class="token punctuation">.</span>Sequential<span class="token punctuation">(</span>            nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            nn<span class="token punctuation">.</span>Tanh<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> self<span class="token punctuation">.</span>net<span class="token punctuation">(</span>x<span class="token punctuation">)</span>model <span class="token operator">=</span> XORNet<span class="token punctuation">(</span><span class="token punctuation">)</span>loss_fn <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">1e-2</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">1e-2</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    model<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>X<span class="token punctuation">)</span>    loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> y<span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span>set_to_none<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> epoch <span class="token operator">%</span> <span class="token number">200</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">:</span>        <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"epoch=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>epoch<span class="token punctuation">&#125;</span></span><span class="token string">, loss=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span><span class="token format-spec">.6f</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>X<span class="token punctuation">)</span>    predictions <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"predictions:"</span><span class="token punctuation">,</span> predictions<span class="token punctuation">.</span>tolist<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>训练正常时，预测结果应接近：</p><pre class="language-text" data-language="text"><code class="language-text">predictions: [0, 1, 1, 0]</code></pre><p>训练循环和 Adam 完全相同。AdamW 的解耦逻辑已经封装在 <code>optimizer.step()</code> 中，不需要手动修改损失函数。</p><h2 id="13-哪些参数通常不做权重衰减"><a href="#13-哪些参数通常不做权重衰减" class="headerlink" title="13. 哪些参数通常不做权重衰减"></a>13. 哪些参数通常不做权重衰减</h2><p>一个常见实践是：对全连接层、卷积层等权重矩阵进行衰减，但对下面这些参数关闭衰减：</p><ul><li>偏置 <code>bias</code>。</li><li>LayerNorm、BatchNorm 等归一化层的缩放和偏移参数。</li><li>某些任务中特殊定义的标量参数。</li></ul><p>原因主要是：</p><ul><li>偏置和归一化参数通常维度较低。</li><li>它们的作用不同于大规模权重矩阵。</li><li>对它们衰减未必带来期望的正则化效果。</li></ul><p>这不是不可违反的数学定律，而是一种常见实验配置。是否排除仍应以验证集结果为准。</p><h2 id="14-用参数组排除-bias-和归一化参数"><a href="#14-用参数组排除-bias-和归一化参数" class="headerlink" title="14. 用参数组排除 bias 和归一化参数"></a>14. 用参数组排除 bias 和归一化参数</h2><p>可以根据参数名称进行分组：</p><pre class="language-python" data-language="python"><code class="language-python">decay_params <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>no_decay_params <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token keyword">for</span> name<span class="token punctuation">,</span> param <span class="token keyword">in</span> model<span class="token punctuation">.</span>named_parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> <span class="token keyword">not</span> param<span class="token punctuation">.</span>requires_grad<span class="token punctuation">:</span>        <span class="token keyword">continue</span>    is_bias <span class="token operator">=</span> name<span class="token punctuation">.</span>endswith<span class="token punctuation">(</span><span class="token string">"bias"</span><span class="token punctuation">)</span>    is_norm <span class="token operator">=</span> <span class="token string">"norm"</span> <span class="token keyword">in</span> name<span class="token punctuation">.</span>lower<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> is_bias <span class="token keyword">or</span> is_norm<span class="token punctuation">:</span>        no_decay_params<span class="token punctuation">.</span>append<span class="token punctuation">(</span>param<span class="token punctuation">)</span>    <span class="token keyword">else</span><span class="token punctuation">:</span>        decay_params<span class="token punctuation">.</span>append<span class="token punctuation">(</span>param<span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    <span class="token punctuation">[</span>        <span class="token punctuation">&#123;</span>            <span class="token string">"params"</span><span class="token punctuation">:</span> decay_params<span class="token punctuation">,</span>            <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">1e-2</span><span class="token punctuation">,</span>        <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>        <span class="token punctuation">&#123;</span>            <span class="token string">"params"</span><span class="token punctuation">:</span> no_decay_params<span class="token punctuation">,</span>            <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">0.0</span><span class="token punctuation">,</span>        <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">]</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>对于结构复杂的模型，只根据名称中是否包含 <code>norm</code> 进行判断可能不够严谨。</p><p>还可以使用一个简单经验：一维参数通常是偏置或归一化参数，二维及以上参数通常是权重矩阵。</p><pre class="language-python" data-language="python"><code class="language-python">decay_params <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>no_decay_params <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token keyword">for</span> param <span class="token keyword">in</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> <span class="token keyword">not</span> param<span class="token punctuation">.</span>requires_grad<span class="token punctuation">:</span>        <span class="token keyword">continue</span>    <span class="token keyword">if</span> param<span class="token punctuation">.</span>ndim <span class="token operator">>=</span> <span class="token number">2</span><span class="token punctuation">:</span>        decay_params<span class="token punctuation">.</span>append<span class="token punctuation">(</span>param<span class="token punctuation">)</span>    <span class="token keyword">else</span><span class="token punctuation">:</span>        no_decay_params<span class="token punctuation">.</span>append<span class="token punctuation">(</span>param<span class="token punctuation">)</span></code></pre><p>这种方法也不是对所有模型都绝对正确，但在 Transformer 风格模型中很常见。</p><p><img src="https://lsworl.github.io/images/ml-blog/adamw-parameter-groups.svg" alt="AdamW 参数组中的衰减与不衰减参数"></p><h2 id="15-参数组中不能重复加入同一个参数"><a href="#15-参数组中不能重复加入同一个参数" class="headerlink" title="15. 参数组中不能重复加入同一个参数"></a>15. 参数组中不能重复加入同一个参数</h2><p>每个可训练参数只能出现在一个参数组里。</p><p>错误示例：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token string">"params"</span><span class="token punctuation">:</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">1e-2</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token string">"params"</span><span class="token punctuation">:</span> model<span class="token punctuation">.</span>classifier<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">0.0</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span></code></pre><p>如果 <code>classifier</code> 本来就在 <code>model.parameters()</code> 中，它就被重复加入了。</p><p>正确做法是先把参数划分成互不重叠的集合，再创建优化器。</p><h2 id="16-AdamW-与学习率调度"><a href="#16-AdamW-与学习率调度" class="headerlink" title="16. AdamW 与学习率调度"></a>16. AdamW 与学习率调度</h2><p>AdamW 仍然需要合理的学习率策略。</p><p>常见组合包括：</p><ul><li>AdamW + warmup。</li><li>AdamW + cosine decay。</li><li>AdamW + linear decay。</li></ul><p>例如使用余弦退火：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">1e-2</span><span class="token punctuation">,</span><span class="token punctuation">)</span>scheduler <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>lr_scheduler<span class="token punctuation">.</span>CosineAnnealingLR<span class="token punctuation">(</span>    optimizer<span class="token punctuation">,</span>    T_max<span class="token operator">=</span><span class="token number">100</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    train_one_epoch<span class="token punctuation">(</span>model<span class="token punctuation">,</span> optimizer<span class="token punctuation">)</span>    scheduler<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>因为 AdamW 的衰减量中包含学习率：</p><p>$$<br>\alpha\lambda\theta<br>$$</p><p>所以学习率随训练变化时，每一步实际施加的衰减量也会随之变化。</p><h2 id="17-如何选择-weight-decay"><a href="#17-如何选择-weight-decay" class="headerlink" title="17. 如何选择 weight_decay"></a>17. 如何选择 <code>weight_decay</code></h2><p>没有一个适合所有任务的固定值。</p><p>可以把下面的值作为搜索起点：</p><pre class="language-text" data-language="text"><code class="language-text">01e-41e-31e-21e-1</code></pre><p>常见现象：</p><ul><li><code>weight_decay</code> 太小：正则化效果不明显。</li><li><code>weight_decay</code> 太大：参数被过度压缩，训练集都难以拟合。</li><li>训练集很好、验证集较差：可以尝试适当增大。</li><li>训练集和验证集都较差：先检查模型容量、学习率和训练过程，不要只靠增大衰减解决。</li></ul><p><code>lr</code> 和 <code>weight_decay</code> 会相互影响，最好联合调节，而不是只搜索其中一个。</p><h2 id="18-保存与恢复-AdamW-状态"><a href="#18-保存与恢复-AdamW-状态" class="headerlink" title="18. 保存与恢复 AdamW 状态"></a>18. 保存与恢复 AdamW 状态</h2><p>AdamW 和 Adam 一样，需要保存一阶矩、二阶矩和更新步数。</p><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>save<span class="token punctuation">(</span><span class="token punctuation">&#123;</span>    <span class="token string">"model"</span><span class="token punctuation">:</span> model<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token string">"optimizer"</span><span class="token punctuation">:</span> optimizer<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token string">"scheduler"</span><span class="token punctuation">:</span> scheduler<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token string">"epoch"</span><span class="token punctuation">:</span> epoch<span class="token punctuation">,</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span> <span class="token string">"checkpoint.pth"</span><span class="token punctuation">)</span></code></pre><p>恢复时：</p><pre class="language-python" data-language="python"><code class="language-python">checkpoint <span class="token operator">=</span> torch<span class="token punctuation">.</span>load<span class="token punctuation">(</span><span class="token string">"checkpoint.pth"</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span>load_state_dict<span class="token punctuation">(</span>checkpoint<span class="token punctuation">[</span><span class="token string">"model"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>load_state_dict<span class="token punctuation">(</span>checkpoint<span class="token punctuation">[</span><span class="token string">"optimizer"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>scheduler<span class="token punctuation">.</span>load_state_dict<span class="token punctuation">(</span>checkpoint<span class="token punctuation">[</span><span class="token string">"scheduler"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>start_epoch <span class="token operator">=</span> checkpoint<span class="token punctuation">[</span><span class="token string">"epoch"</span><span class="token punctuation">]</span> <span class="token operator">+</span> <span class="token number">1</span></code></pre><p>只恢复模型而不恢复优化器，AdamW 的历史矩估计会重新开始，训练轨迹可能发生变化。</p><h2 id="19-常见错误"><a href="#19-常见错误" class="headerlink" title="19. 常见错误"></a>19. 常见错误</h2><h3 id="19-1-同时手动加-L2-又设置-AdamW-衰减"><a href="#19-1-同时手动加-L2-又设置-AdamW-衰减" class="headerlink" title="19.1 同时手动加 L2 又设置 AdamW 衰减"></a>19.1 同时手动加 L2 又设置 AdamW 衰减</h3><p>例如既在损失中加入参数平方和：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> data_loss <span class="token operator">+</span> lambda_ <span class="token operator">*</span> l2_penalty</code></pre><p>又设置：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span>lambda_<span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这会同时施加两种正则化。如果不是有意设计，通常属于重复计算。</p><h3 id="19-2-认为-AdamW-一定优于-Adam"><a href="#19-2-认为-AdamW-一定优于-Adam" class="headerlink" title="19.2 认为 AdamW 一定优于 Adam"></a>19.2 认为 AdamW 一定优于 Adam</h3><p>如果 <code>weight_decay=0</code>，两者没有解耦衰减上的差异。</p><p>如果任务本身不需要额外权重衰减，AdamW 不会凭空带来提升。</p><h3 id="19-3-直接照搬别人的-weight-decay"><a href="#19-3-直接照搬别人的-weight-decay" class="headerlink" title="19.3 直接照搬别人的 weight_decay"></a>19.3 直接照搬别人的 <code>weight_decay</code></h3><p>合适的衰减强度与下面因素有关：</p><ul><li>学习率。</li><li>训练步数。</li><li>模型规模。</li><li>数据量。</li><li>是否使用归一化层。</li><li>学习率调度方式。</li></ul><p>所以应通过验证集或交叉验证选择，而不是机械照搬。</p><h3 id="19-4-对所有参数一视同仁"><a href="#19-4-对所有参数一视同仁" class="headerlink" title="19.4 对所有参数一视同仁"></a>19.4 对所有参数一视同仁</h3><p>简单任务可以直接传入 <code>model.parameters()</code>，但大型模型通常值得认真区分权重矩阵、偏置和归一化参数。</p><h3 id="19-5-在训练循环中重新创建优化器"><a href="#19-5-在训练循环中重新创建优化器" class="headerlink" title="19.5 在训练循环中重新创建优化器"></a>19.5 在训练循环中重新创建优化器</h3><p>错误写法：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>这会不断丢失一阶矩、二阶矩和更新步数。优化器通常只在训练循环外创建一次。</p><h2 id="20-AdamW-适合哪些场景"><a href="#20-AdamW-适合哪些场景" class="headerlink" title="20. AdamW 适合哪些场景"></a>20. AdamW 适合哪些场景</h2><p>AdamW 经常作为这些任务的优化器起点：</p><ul><li>Transformer。</li><li>BERT、GPT 等语言模型。</li><li>Vision Transformer。</li><li>需要 Adam 自适应更新并希望使用权重衰减的任务。</li><li>预训练模型微调。</li></ul><p>不过它并不限定于 Transformer。普通 MLP、CNN 或其他模型只要适合 Adam，并且需要解耦权重衰减，也可以使用 AdamW。</p><h2 id="21-总结"><a href="#21-总结" class="headerlink" title="21. 总结"></a>21. 总结</h2><p>L2 正则化和权重衰减的目标看起来相似，都是限制参数规模，但它们的实现路径不同：</p><pre class="language-text" data-language="text"><code class="language-text">L2 正则化  -> 修改损失和梯度  -> 正则项进入 Adam 的一阶矩与二阶矩AdamW  -> 使用原始数据梯度计算矩估计  -> 单独按比例衰减参数</code></pre><p>最重要的结论是：</p><ul><li>在普通 SGD 中，L2 正则化和权重衰减在常见设置下可以等价。</li><li>在 Adam 中，把 L2 项加入梯度会受到自适应缩放影响。</li><li>AdamW 把权重衰减从梯度路径中解耦。</li><li>AdamW 的 <code>weight_decay</code> 仍然需要根据验证集调节。</li><li>大型模型中通常会对权重矩阵衰减，而排除偏置和归一化参数。</li></ul><p>PyTorch 中的基础写法是：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">1e-2</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>这行代码虽然简单，但理解“解耦”以后，才真正知道它与 Adam + L2 的区别。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>Ilya Loshchilov, Frank Hutter, <em>Decoupled Weight Decay Regularization</em>: <a href="https://arxiv.org/abs/1711.05101">https://arxiv.org/abs/1711.05101</a></li><li>PyTorch Documentation, <code>torch.optim.AdamW</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.optim.adamw.AdamW_class.html">https://docs.pytorch.org/docs/stable/generated/torch.optim.adamw.AdamW_class.html</a></li><li>PyTorch Documentation, <code>torch.optim</code>: <a href="https://docs.pytorch.org/docs/stable/optim.html">https://docs.pytorch.org/docs/stable/optim.html</a></li><li>Diederik P. Kingma, Jimmy Ba, <em>Adam: A Method for Stochastic Optimization</em>: <a href="https://arxiv.org/abs/1412.6980">https://arxiv.org/abs/1412.6980</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/10/machine-learning-adamw-optimizer/</id>
    <link href="https://lsworl.github.io/2026/08/10/machine-learning-adamw-optimizer/"/>
    <published>2026-08-09T16:00:00.000Z</published>
    <summary>从 L2 正则化与权重衰减的区别出发，理解 AdamW 的解耦思想、更新公式、手算过程、参数分组和 PyTorch 完整训练方法。</summary>
    <title>AdamW Optimizer：为什么要把权重衰减与梯度更新解耦</title>
    <updated>2026-08-10T09:02:28.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Seq2Seq" scheme="https://lsworl.github.io/tags/Seq2Seq/"/>
    <category term="Encoder-Decoder" scheme="https://lsworl.github.io/tags/Encoder-Decoder/"/>
    <category term="RNN" scheme="https://lsworl.github.io/tags/RNN/"/>
    <category term="GRU" scheme="https://lsworl.github.io/tags/GRU/"/>
    <category term="Attention" scheme="https://lsworl.github.io/tags/Attention/"/>
    <content>
      <![CDATA[<p>前面的文本处理和 Word2Vec 文章主要解决了两个问题：</p><ul><li>如何把文本转换成 token id。</li><li>如何把 token id 转换成词向量。</li></ul><p>但很多自然语言处理任务不是“输入一句话，输出一个类别”，而是“输入一个序列，输出另一个序列”。</p><p>例如机器翻译：</p><pre class="language-text" data-language="text"><code class="language-text">输入：我 喜欢 自然语言处理输出：I like natural language processing</code></pre><p>输入和输出的长度可能不同，词序也可能不同。普通 MLP 或单纯的文本分类器很难直接处理这种映射。</p><p>Seq2Seq，全称 Sequence to Sequence，就是专门学习“序列到序列”映射的一类模型框架。</p><p>一句话概括：</p><blockquote><p>Seq2Seq 先用 Encoder 理解整个输入序列，再让 Decoder 根据编码结果逐步生成输出序列。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/nlp-blog/seq2seq-overview.svg" alt="Seq2Seq Encoder-Decoder整体结构"></p><h2 id="1-什么是序列到序列任务"><a href="#1-什么是序列到序列任务" class="headerlink" title="1. 什么是序列到序列任务"></a>1. 什么是序列到序列任务</h2><p>Seq2Seq 的输入和输出都是有顺序的元素序列：</p><p>$$<br>X&#x3D;(x_1,x_2,\ldots,x_{T_x})<br>$$</p><p>$$<br>Y&#x3D;(y_1,y_2,\ldots,y_{T_y})<br>$$</p><p>其中输入长度 $T_x$ 与输出长度 $T_y$ 不必相同。</p><p>常见任务包括：</p><table><thead><tr><th>任务</th><th>输入序列</th><th>输出序列</th></tr></thead><tbody><tr><td>机器翻译</td><td>中文 token</td><td>英文 token</td></tr><tr><td>文本摘要</td><td>长文章 token</td><td>摘要 token</td></tr><tr><td>对话生成</td><td>用户问题 token</td><td>回复 token</td></tr><tr><td>语音识别</td><td>音频特征帧</td><td>文本 token</td></tr><tr><td>拼写纠正</td><td>含错误的字符序列</td><td>正确字符序列</td></tr><tr><td>时间序列预测</td><td>历史数值序列</td><td>未来数值序列</td></tr></tbody></table><p>Seq2Seq 不是某一种固定网络层，而是一种结构思想。</p><p>Encoder 和 Decoder 可以使用：</p><ul><li>RNN。</li><li>LSTM。</li><li>GRU。</li><li>CNN。</li><li>Transformer。</li></ul><p>这篇文章先从经典的 RNN&#x2F;GRU Encoder-Decoder 讲起，因为它最容易看清序列生成的本质。</p><h2 id="2-为什么普通神经网络不够用"><a href="#2-为什么普通神经网络不够用" class="headerlink" title="2. 为什么普通神经网络不够用"></a>2. 为什么普通神经网络不够用</h2><p>假设使用 MLP 做翻译，首先会遇到输入长度不固定的问题：</p><pre class="language-text" data-language="text"><code class="language-text">我 爱 你我 非常 喜欢 学习 自然语言处理</code></pre><p>两句话包含的 token 数不同，而普通全连接层通常需要固定长度输入。</p><p>即使通过 padding 把长度补齐，MLP 也不会天然理解：</p><ul><li>token 的先后顺序。</li><li>当前词与前面词之间的依赖。</li><li>输出应该在什么时候结束。</li><li>输入和输出之间的对齐关系。</li></ul><p>循环神经网络会按顺序读取 token，并不断更新隐藏状态：</p><p>$$<br>h_t&#x3D;f(x_t,h_{t-1})<br>$$</p><p>$h_t$ 同时包含当前输入和之前序列的信息，因此适合处理序列。</p><h2 id="3-Seq2Seq-的整体结构"><a href="#3-Seq2Seq-的整体结构" class="headerlink" title="3. Seq2Seq 的整体结构"></a>3. Seq2Seq 的整体结构</h2><p>经典 Seq2Seq 由两部分组成。</p><h3 id="3-1-Encoder"><a href="#3-1-Encoder" class="headerlink" title="3.1 Encoder"></a>3.1 Encoder</h3><p>Encoder 按顺序读取输入 token：</p><pre class="language-text" data-language="text"><code class="language-text">x1 -> x2 -> x3 -> ... -> xTx</code></pre><p>每一步都会更新隐藏状态：</p><p>$$<br>h_t^{enc}&#x3D;f_{enc}(E_x(x_t),h_{t-1}^{enc})<br>$$</p><p>其中：</p><ul><li>$E_x$ 是源语言 Embedding。</li><li>$x_t$ 是第 $t$ 个输入 token id。</li><li>$h_t^{enc}$ 是 Encoder 在第 $t$ 步的隐藏状态。</li></ul><p>基础 Seq2Seq 通常把最后隐藏状态作为整个输入序列的表示：</p><p>$$<br>c&#x3D;h_{T_x}^{enc}<br>$$</p><p>$c$ 常被称为上下文向量或语义向量。</p><h3 id="3-2-Decoder"><a href="#3-2-Decoder" class="headerlink" title="3.2 Decoder"></a>3.2 Decoder</h3><p>Decoder 使用 Encoder 给出的上下文向量作为初始状态，然后逐步生成输出：</p><p>$$<br>s_t&#x3D;f_{dec}(E_y(y_{t-1}),s_{t-1})<br>$$</p><p>$$<br>P(y_t\mid y_{&lt;t},X)&#x3D;\text{softmax}(Ws_t+b)<br>$$</p><p>其中：</p><ul><li>$s_t$ 是 Decoder 隐藏状态。</li><li>$y_{t-1}$ 是上一步 token。</li><li>$y_{&lt;t}$ 表示已经生成的所有 token。</li><li>输出分布表示下一个 token 的概率。</li></ul><p>Decoder 一次只生成一个 token，再把这个 token 作为下一步输入。</p><h2 id="4-lt-BOS-gt-、-lt-EOS-gt-和-lt-PAD-gt"><a href="#4-lt-BOS-gt-、-lt-EOS-gt-和-lt-PAD-gt" class="headerlink" title="4. &lt;BOS&gt;、&lt;EOS&gt; 和 &lt;PAD&gt;"></a>4. <code>&lt;BOS&gt;</code>、<code>&lt;EOS&gt;</code> 和 <code>&lt;PAD&gt;</code></h2><p>Seq2Seq 通常需要几个特殊 token。</p><table><thead><tr><th>token</th><th>含义</th><th>作用</th></tr></thead><tbody><tr><td><code>&lt;BOS&gt;</code></td><td>Begin of Sequence</td><td>告诉 Decoder 开始生成</td></tr><tr><td><code>&lt;EOS&gt;</code></td><td>End of Sequence</td><td>告诉 Decoder 序列已经结束</td></tr><tr><td><code>&lt;PAD&gt;</code></td><td>Padding</td><td>将不同长度序列补成相同长度</td></tr><tr><td><code>&lt;UNK&gt;</code></td><td>Unknown</td><td>表示词表中没有的 token</td></tr></tbody></table><p>假设目标句子是：</p><pre class="language-text" data-language="text"><code class="language-text">I like NLP</code></pre><p>训练时通常会处理成：</p><pre class="language-text" data-language="text"><code class="language-text">&lt;BOS> I like NLP &lt;EOS></code></pre><p>Decoder 的输入和预测目标会错开一位：</p><pre class="language-text" data-language="text"><code class="language-text">Decoder 输入：&lt;BOS> I    like NLP预测目标：      I     like NLP  &lt;EOS></code></pre><p>模型因此学会：</p><ul><li>看到 <code>&lt;BOS&gt;</code> 后应该输出第一个词。</li><li>看到前面的正确 token 后应该输出下一个词。</li><li>句子完成后应该输出 <code>&lt;EOS&gt;</code>。</li></ul><h2 id="5-一次翻译是怎样生成的"><a href="#5-一次翻译是怎样生成的" class="headerlink" title="5. 一次翻译是怎样生成的"></a>5. 一次翻译是怎样生成的</h2><p>假设输入是：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 学习</code></pre><p>Encoder 读取完整输入后得到上下文向量 $c$。</p><p>Decoder 的生成过程可能是：</p><pre class="language-text" data-language="text"><code class="language-text">输入 &lt;BOS> -> 输出 I输入 I     -> 输出 like输入 like  -> 输出 studying输入 studying -> 输出 &lt;EOS></code></pre><p>得到完整结果：</p><pre class="language-text" data-language="text"><code class="language-text">I like studying</code></pre><p>这个过程是自回归生成：每一步依赖此前已经生成的结果。</p><p>联合概率可以拆成：</p><p>$$<br>P(Y\mid X)<br>&#x3D;\prod_{t&#x3D;1}^{T_y}<br>P(y_t\mid y_1,\ldots,y_{t-1},X)<br>$$</p><p>训练目标是让正确目标序列的条件概率尽可能大，等价地，也可以最小化每一步 token 的交叉熵损失。</p><h2 id="6-手工理解一次-Encoder-计算"><a href="#6-手工理解一次-Encoder-计算" class="headerlink" title="6. 手工理解一次 Encoder 计算"></a>6. 手工理解一次 Encoder 计算</h2><p>真实 GRU 和 LSTM 内部有多个门控，直接手算较长。为了先理解信息如何逐步积累，可以使用一个简化 RNN：</p><p>$$<br>h_t&#x3D;\tanh(x_t+h_{t-1})<br>$$</p><p>假设输入三个 token 的一维 Embedding 分别为：</p><pre class="language-text" data-language="text"><code class="language-text">我   -> 0.2喜欢 -> 0.5学习 -> 0.8</code></pre><p>初始隐藏状态：</p><p>$$<br>h_0&#x3D;0<br>$$</p><p>读取“我”：</p><p>$$<br>h_1&#x3D;\tanh(0.2+0)\approx0.197<br>$$</p><p>读取“喜欢”：</p><p>$$<br>h_2&#x3D;\tanh(0.5+0.197)\approx0.602<br>$$</p><p>读取“学习”：</p><p>$$<br>h_3&#x3D;\tanh(0.8+0.602)\approx0.886<br>$$</p><p>最后的 $h_3$ 同时受到三个输入的影响，因此可以作为输入序列的压缩表示。</p><p>真实模型会使用多维向量和可学习矩阵：</p><p>$$<br>h_t&#x3D;\tanh(W_xx_t+W_hh_{t-1}+b)<br>$$</p><p>训练过程中，模型会自己学会哪些信息应该保留在隐藏状态中。</p><h2 id="7-为什么常用-LSTM-或-GRU"><a href="#7-为什么常用-LSTM-或-GRU" class="headerlink" title="7. 为什么常用 LSTM 或 GRU"></a>7. 为什么常用 LSTM 或 GRU</h2><p>普通 RNN 在长序列中容易出现梯度消失或梯度爆炸，较早位置的信息很难传到后面。</p><p>LSTM 和 GRU 通过门控机制控制信息流动。</p><p>以 GRU 为例，它包含：</p><ul><li>更新门：决定保留多少旧状态。</li><li>重置门：决定计算候选状态时参考多少旧信息。</li><li>候选隐藏状态：融合当前输入与历史信息。</li></ul><p>简化公式如下：</p><p>$$<br>z_t&#x3D;\sigma(W_zx_t+U_zh_{t-1})<br>$$</p><p>$$<br>r_t&#x3D;\sigma(W_rx_t+U_rh_{t-1})<br>$$</p><p>$$<br>\tilde{h}<em>t&#x3D;\tanh(W_hx_t+U_h(r_t\odot h</em>{t-1}))<br>$$</p><p>$$<br>h_t&#x3D;(1-z_t)\odot\tilde{h}<em>t+z_t\odot h</em>{t-1}<br>$$</p><p>GRU 比 LSTM 少一个独立的细胞状态，结构相对简洁。在 PyTorch 中，两者都可以直接作为 Encoder 和 Decoder 的核心层。</p><h2 id="8-Teacher-Forcing-是什么"><a href="#8-Teacher-Forcing-是什么" class="headerlink" title="8. Teacher Forcing 是什么"></a>8. Teacher Forcing 是什么</h2><p>Decoder 在第 $t$ 步需要一个“上一步 token”作为输入。</p><p>训练时有两种选择。</p><h3 id="使用模型自己的预测"><a href="#使用模型自己的预测" class="headerlink" title="使用模型自己的预测"></a>使用模型自己的预测</h3><pre class="language-text" data-language="text"><code class="language-text">模型预测 yt-1 -> 作为下一步输入</code></pre><p>如果前面预测错了，后面可能连续出错，训练初期尤其困难。</p><h3 id="使用真实目标-token"><a href="#使用真实目标-token" class="headerlink" title="使用真实目标 token"></a>使用真实目标 token</h3><pre class="language-text" data-language="text"><code class="language-text">真实标签 yt-1 -> 作为下一步输入</code></pre><p>这就是 Teacher Forcing。</p><p><img src="https://lsworl.github.io/images/nlp-blog/seq2seq-teacher-forcing.svg" alt="Teacher Forcing与自回归输入的区别"></p><p>代码中常通过概率决定使用哪一种：</p><pre class="language-python" data-language="python"><code class="language-python">use_teacher <span class="token operator">=</span> torch<span class="token punctuation">.</span>rand<span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token operator">&lt;</span> teacher_forcing_ratio<span class="token keyword">if</span> use_teacher<span class="token punctuation">:</span>    decoder_input <span class="token operator">=</span> target<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> t<span class="token punctuation">]</span><span class="token keyword">else</span><span class="token punctuation">:</span>    decoder_input <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">teacher_forcing_ratio = 0.5</code></pre><p>表示每一步有 50% 概率使用真实 token。</p><p>Teacher Forcing 可以加快训练，但也会带来训练和推理不一致的问题。</p><h2 id="9-训练与推理为什么不同"><a href="#9-训练与推理为什么不同" class="headerlink" title="9. 训练与推理为什么不同"></a>9. 训练与推理为什么不同</h2><p>训练时，我们拥有完整目标序列，可以使用 Teacher Forcing。</p><p>推理时没有标准答案，只能使用模型自己的预测：</p><pre class="language-text" data-language="text"><code class="language-text">&lt;BOS>  -> 预测 y1  -> 用 y1 预测 y2  -> 用 y2 预测 y3  -> ...  -> 生成 &lt;EOS></code></pre><p><img src="https://lsworl.github.io/images/nlp-blog/seq2seq-training-inference.svg" alt="Seq2Seq训练与推理流程对比"></p><p>训练时总能看到正确历史，推理时却可能看到自己生成的错误历史。这种差异通常称为 Exposure Bias。</p><p>常见缓解方式包括：</p><ul><li>逐步降低 Teacher Forcing 比例。</li><li>Scheduled Sampling。</li><li>使用更强的 Decoder 或 Attention。</li><li>序列级训练目标。</li></ul><p>入门时先理解问题即可，不必一次加入所有技巧。</p><h2 id="10-Seq2Seq-的损失函数"><a href="#10-Seq2Seq-的损失函数" class="headerlink" title="10. Seq2Seq 的损失函数"></a>10. Seq2Seq 的损失函数</h2><p>假设 Decoder 在每一步输出词表上的 logits：</p><p>$$<br>Z\in\mathbb{R}^{B\times T\times V}<br>$$</p><p>其中：</p><ul><li>$B$ 是 batch size。</li><li>$T$ 是目标序列长度。</li><li>$V$ 是目标词表大小。</li></ul><p>目标 token id 的形状是：</p><p>$$<br>Y\in\mathbb{R}^{B\times T}<br>$$</p><p>训练时可以把前两维展平：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>    logits<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> vocab_size<span class="token punctuation">)</span><span class="token punctuation">,</span>    target<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>如果目标序列包含 padding，需要忽略 <code>&lt;PAD&gt;</code>：</p><pre class="language-python" data-language="python"><code class="language-python">loss_fn <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span>ignore_index<span class="token operator">=</span>PAD_ID<span class="token punctuation">)</span></code></pre><p><code>CrossEntropyLoss</code> 直接接收 logits，不需要先手动执行 softmax。</p><h2 id="11-基础-Seq2Seq-的固定向量瓶颈"><a href="#11-基础-Seq2Seq-的固定向量瓶颈" class="headerlink" title="11. 基础 Seq2Seq 的固定向量瓶颈"></a>11. 基础 Seq2Seq 的固定向量瓶颈</h2><p>基础模型把整个输入序列压缩进最后一个隐藏状态：</p><p>$$<br>c&#x3D;h_{T_x}^{enc}<br>$$</p><p>对于短句可能足够，但输入变长后，单个固定长度向量需要记住：</p><ul><li>每个输入词的信息。</li><li>词序。</li><li>长距离依赖。</li><li>对生成不同目标词有用的细节。</li></ul><p>这会形成信息瓶颈。句子越长，前面信息越容易丢失。</p><h2 id="12-Attention-如何改进-Seq2Seq（Attention会专门用一篇文章介绍）"><a href="#12-Attention-如何改进-Seq2Seq（Attention会专门用一篇文章介绍）" class="headerlink" title="12. Attention 如何改进 Seq2Seq（Attention会专门用一篇文章介绍）"></a>12. Attention 如何改进 Seq2Seq（Attention会专门用一篇文章介绍）</h2><p>Attention 不再要求 Decoder 只依赖最后一个 Encoder 状态，而是保留所有 Encoder 输出：</p><p>$$<br>H&#x3D;(h_1^{enc},h_2^{enc},\ldots,h_{T_x}^{enc})<br>$$</p><p>Decoder 在第 $t$ 步计算自己与每个输入位置的相关性：</p><p>$$<br>e_{t,i}&#x3D;score(s_{t-1},h_i^{enc})<br>$$</p><p>经过 softmax 得到注意力权重：</p><p>$$<br>\alpha_{t,i}<br>&#x3D;\frac{\exp(e_{t,i})}<br>{\sum_j\exp(e_{t,j})}<br>$$</p><p>再对 Encoder 状态加权求和：</p><p>$$<br>c_t&#x3D;\sum_i\alpha_{t,i}h_i^{enc}<br>$$</p><p>每生成一个目标 token，Decoder 都可以重新选择当前最相关的输入位置。</p><p><img src="https://lsworl.github.io/images/nlp-blog/seq2seq-attention.svg" alt="Attention让Decoder在每一步查看不同输入位置"></p><p>例如生成英文单词 <code>like</code> 时，模型可能重点关注中文“喜欢”；生成 <code>NLP</code> 时，可能重点关注“自然语言处理”。</p><p>Attention 既缓解了固定向量瓶颈，也提供了输入输出之间的软对齐关系。</p><h2 id="13-PyTorch-中-GRU-的输入输出形状"><a href="#13-PyTorch-中-GRU-的输入输出形状" class="headerlink" title="13. PyTorch 中 GRU 的输入输出形状"></a>13. PyTorch 中 GRU 的输入输出形状</h2><p>下面的实现使用：</p><pre class="language-python" data-language="python"><code class="language-python">nn<span class="token punctuation">.</span>GRU<span class="token punctuation">(</span>    input_size<span class="token operator">=</span>embed_dim<span class="token punctuation">,</span>    hidden_size<span class="token operator">=</span>hidden_dim<span class="token punctuation">,</span>    batch_first<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>当 <code>batch_first=True</code> 时：</p><table><thead><tr><th>张量</th><th>形状</th><th>含义</th></tr></thead><tbody><tr><td>输入 Embedding</td><td><code>[batch, seq_len, embed_dim]</code></td><td>每个 token 的向量</td></tr><tr><td>GRU outputs</td><td><code>[batch, seq_len, hidden_dim]</code></td><td>每个时间步的输出</td></tr><tr><td>GRU hidden</td><td><code>[num_layers, batch, hidden_dim]</code></td><td>最后隐藏状态</td></tr></tbody></table><p>注意：即使设置了 <code>batch_first=True</code>，hidden 的维度顺序仍然是：</p><pre class="language-text" data-language="text"><code class="language-text">[num_layers, batch, hidden_dim]</code></pre><p>Decoder 每次只处理一个 token，因此它的 Embedding 可以整理成：</p><pre class="language-text" data-language="text"><code class="language-text">[batch, 1, embed_dim]</code></pre><p><img src="https://lsworl.github.io/images/nlp-blog/seq2seq-pytorch-shapes.svg" alt="PyTorch Seq2Seq中的主要张量形状"></p><h2 id="14-PyTorch-实现-Encoder"><a href="#14-PyTorch-实现-Encoder" class="headerlink" title="14. PyTorch 实现 Encoder"></a>14. PyTorch 实现 Encoder</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">from</span> torch <span class="token keyword">import</span> nn<span class="token keyword">class</span> <span class="token class-name">Encoder</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> vocab_size<span class="token punctuation">,</span> embed_dim<span class="token punctuation">,</span> hidden_dim<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>vocab_size<span class="token punctuation">,</span> embed_dim<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>gru <span class="token operator">=</span> nn<span class="token punctuation">.</span>GRU<span class="token punctuation">(</span>            input_size<span class="token operator">=</span>embed_dim<span class="token punctuation">,</span>            hidden_size<span class="token operator">=</span>hidden_dim<span class="token punctuation">,</span>            batch_first<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> src<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token comment"># src: [batch, src_len]</span>        embedded <span class="token operator">=</span> self<span class="token punctuation">.</span>embedding<span class="token punctuation">(</span>src<span class="token punctuation">)</span>        <span class="token comment"># embedded: [batch, src_len, embed_dim]</span>        outputs<span class="token punctuation">,</span> hidden <span class="token operator">=</span> self<span class="token punctuation">.</span>gru<span class="token punctuation">(</span>embedded<span class="token punctuation">)</span>        <span class="token comment"># outputs: [batch, src_len, hidden_dim]</span>        <span class="token comment"># hidden:  [1, batch, hidden_dim]</span>        <span class="token keyword">return</span> outputs<span class="token punctuation">,</span> hidden</code></pre><p>基础 Seq2Seq 只需要最后的 <code>hidden</code> 初始化 Decoder。</p><p>这里仍然返回 <code>outputs</code>，是为了以后扩展 Attention，因为 Attention 需要所有时间步的 Encoder 输出。</p><h2 id="15-PyTorch-实现-Decoder"><a href="#15-PyTorch-实现-Decoder" class="headerlink" title="15. PyTorch 实现 Decoder"></a>15. PyTorch 实现 Decoder</h2><p>Decoder 每次接收一个 token：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">Decoder</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> vocab_size<span class="token punctuation">,</span> embed_dim<span class="token punctuation">,</span> hidden_dim<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>vocab_size<span class="token punctuation">,</span> embed_dim<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>gru <span class="token operator">=</span> nn<span class="token punctuation">.</span>GRU<span class="token punctuation">(</span>            input_size<span class="token operator">=</span>embed_dim<span class="token punctuation">,</span>            hidden_size<span class="token operator">=</span>hidden_dim<span class="token punctuation">,</span>            batch_first<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>        self<span class="token punctuation">.</span>output_layer <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>hidden_dim<span class="token punctuation">,</span> vocab_size<span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> token<span class="token punctuation">,</span> hidden<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token comment"># token: [batch]</span>        embedded <span class="token operator">=</span> self<span class="token punctuation">.</span>embedding<span class="token punctuation">(</span>token<span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>        <span class="token comment"># embedded: [batch, 1, embed_dim]</span>        output<span class="token punctuation">,</span> hidden <span class="token operator">=</span> self<span class="token punctuation">.</span>gru<span class="token punctuation">(</span>embedded<span class="token punctuation">,</span> hidden<span class="token punctuation">)</span>        <span class="token comment"># output: [batch, 1, hidden_dim]</span>        logits <span class="token operator">=</span> self<span class="token punctuation">.</span>output_layer<span class="token punctuation">(</span>output<span class="token punctuation">.</span>squeeze<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span>        <span class="token comment"># logits: [batch, vocab_size]</span>        <span class="token keyword">return</span> logits<span class="token punctuation">,</span> hidden</code></pre><p><code>output_layer</code> 把 Decoder 隐藏状态映射为词表上的 logits。</p><h2 id="16-组合成-Seq2Seq-模型"><a href="#16-组合成-Seq2Seq-模型" class="headerlink" title="16. 组合成 Seq2Seq 模型"></a>16. 组合成 Seq2Seq 模型</h2><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">Seq2Seq</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> encoder<span class="token punctuation">,</span> decoder<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>encoder <span class="token operator">=</span> encoder        self<span class="token punctuation">.</span>decoder <span class="token operator">=</span> decoder    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> src<span class="token punctuation">,</span> target<span class="token punctuation">,</span> teacher_forcing_ratio<span class="token operator">=</span><span class="token number">0.5</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token comment"># target: [batch, target_len]，第一个 token 是 BOS</span>        _<span class="token punctuation">,</span> hidden <span class="token operator">=</span> self<span class="token punctuation">.</span>encoder<span class="token punctuation">(</span>src<span class="token punctuation">)</span>        decoder_input <span class="token operator">=</span> target<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span>        step_logits <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>        <span class="token keyword">for</span> t <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> target<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">:</span>            logits<span class="token punctuation">,</span> hidden <span class="token operator">=</span> self<span class="token punctuation">.</span>decoder<span class="token punctuation">(</span>decoder_input<span class="token punctuation">,</span> hidden<span class="token punctuation">)</span>            step_logits<span class="token punctuation">.</span>append<span class="token punctuation">(</span>logits<span class="token punctuation">)</span>            predicted_token <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>            use_teacher <span class="token operator">=</span> torch<span class="token punctuation">.</span>rand<span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token operator">&lt;</span> teacher_forcing_ratio            <span class="token keyword">if</span> use_teacher<span class="token punctuation">:</span>                decoder_input <span class="token operator">=</span> target<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> t<span class="token punctuation">]</span>            <span class="token keyword">else</span><span class="token punctuation">:</span>                decoder_input <span class="token operator">=</span> predicted_token        <span class="token comment"># [batch, target_len - 1, vocab_size]</span>        <span class="token keyword">return</span> torch<span class="token punctuation">.</span>stack<span class="token punctuation">(</span>step_logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>这里从 <code>t=1</code> 开始，是因为 <code>target[:, 0]</code> 已经作为初始 <code>&lt;BOS&gt;</code> 输入。</p><p>模型预测的是：</p><pre class="language-text" data-language="text"><code class="language-text">target[:, 1:]</code></pre><p>也就是 <code>&lt;BOS&gt;</code> 后面的所有 token，包括最后的 <code>&lt;EOS&gt;</code>。</p><h2 id="17-完整例子：学习反转数字序列"><a href="#17-完整例子：学习反转数字序列" class="headerlink" title="17. 完整例子：学习反转数字序列"></a>17. 完整例子：学习反转数字序列</h2><p>为了把注意力放在 Seq2Seq 本身，不引入分词器和真实翻译语料，下面训练一个序列反转任务。</p><p>输入：</p><pre class="language-text" data-language="text"><code class="language-text">3 7 5 9</code></pre><p>目标：</p><pre class="language-text" data-language="text"><code class="language-text">&lt;BOS> 9 5 7 3 &lt;EOS></code></pre><h3 id="17-1-定义词表和数据"><a href="#17-1-定义词表和数据" class="headerlink" title="17.1 定义词表和数据"></a>17.1 定义词表和数据</h3><pre class="language-python" data-language="python"><code class="language-python">PAD_ID <span class="token operator">=</span> <span class="token number">0</span>BOS_ID <span class="token operator">=</span> <span class="token number">1</span>EOS_ID <span class="token operator">=</span> <span class="token number">2</span><span class="token comment"># 3 到 12 表示十种普通 token</span>VOCAB_SIZE <span class="token operator">=</span> <span class="token number">13</span>SEQ_LEN <span class="token operator">=</span> <span class="token number">4</span><span class="token keyword">def</span> <span class="token function">make_batch</span><span class="token punctuation">(</span>batch_size<span class="token punctuation">)</span><span class="token punctuation">:</span>    src <span class="token operator">=</span> torch<span class="token punctuation">.</span>randint<span class="token punctuation">(</span>        low<span class="token operator">=</span><span class="token number">3</span><span class="token punctuation">,</span>        high<span class="token operator">=</span>VOCAB_SIZE<span class="token punctuation">,</span>        size<span class="token operator">=</span><span class="token punctuation">(</span>batch_size<span class="token punctuation">,</span> SEQ_LEN<span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span>    reversed_src <span class="token operator">=</span> torch<span class="token punctuation">.</span>flip<span class="token punctuation">(</span>src<span class="token punctuation">,</span> dims<span class="token operator">=</span><span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">)</span>    bos <span class="token operator">=</span> torch<span class="token punctuation">.</span>full<span class="token punctuation">(</span><span class="token punctuation">(</span>batch_size<span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span> BOS_ID<span class="token punctuation">)</span>    eos <span class="token operator">=</span> torch<span class="token punctuation">.</span>full<span class="token punctuation">(</span><span class="token punctuation">(</span>batch_size<span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span> EOS_ID<span class="token punctuation">)</span>    target <span class="token operator">=</span> torch<span class="token punctuation">.</span>cat<span class="token punctuation">(</span><span class="token punctuation">[</span>bos<span class="token punctuation">,</span> reversed_src<span class="token punctuation">,</span> eos<span class="token punctuation">]</span><span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>    <span class="token keyword">return</span> src<span class="token punctuation">,</span> target</code></pre><h3 id="17-2-创建模型和优化器"><a href="#17-2-创建模型和优化器" class="headerlink" title="17.2 创建模型和优化器"></a>17.2 创建模型和优化器</h3><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">42</span><span class="token punctuation">)</span>encoder <span class="token operator">=</span> Encoder<span class="token punctuation">(</span>    vocab_size<span class="token operator">=</span>VOCAB_SIZE<span class="token punctuation">,</span>    embed_dim<span class="token operator">=</span><span class="token number">32</span><span class="token punctuation">,</span>    hidden_dim<span class="token operator">=</span><span class="token number">64</span><span class="token punctuation">,</span><span class="token punctuation">)</span>decoder <span class="token operator">=</span> Decoder<span class="token punctuation">(</span>    vocab_size<span class="token operator">=</span>VOCAB_SIZE<span class="token punctuation">,</span>    embed_dim<span class="token operator">=</span><span class="token number">32</span><span class="token punctuation">,</span>    hidden_dim<span class="token operator">=</span><span class="token number">64</span><span class="token punctuation">,</span><span class="token punctuation">)</span>model <span class="token operator">=</span> Seq2Seq<span class="token punctuation">(</span>encoder<span class="token punctuation">,</span> decoder<span class="token punctuation">)</span>loss_fn <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span>ignore_index<span class="token operator">=</span>PAD_ID<span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">3e-3</span><span class="token punctuation">)</span></code></pre><p>Encoder 和 Decoder 的 <code>hidden_dim</code> 必须一致，这样 Encoder 最后的 hidden 才能直接作为 Decoder 初始 hidden。</p><h3 id="17-3-训练模型"><a href="#17-3-训练模型" class="headerlink" title="17.3 训练模型"></a>17.3 训练模型</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">for</span> step <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1500</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    src<span class="token punctuation">,</span> target <span class="token operator">=</span> make_batch<span class="token punctuation">(</span>batch_size<span class="token operator">=</span><span class="token number">128</span><span class="token punctuation">)</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>        src<span class="token punctuation">,</span>        target<span class="token punctuation">,</span>        teacher_forcing_ratio<span class="token operator">=</span><span class="token number">0.5</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span>    expected <span class="token operator">=</span> target<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">:</span><span class="token punctuation">]</span>    loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>        logits<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> VOCAB_SIZE<span class="token punctuation">)</span><span class="token punctuation">,</span>        expected<span class="token punctuation">.</span>reshape<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span>set_to_none<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token comment"># 循环网络中常用梯度裁剪缓解梯度爆炸</span>    nn<span class="token punctuation">.</span>utils<span class="token punctuation">.</span>clip_grad_norm_<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> max_norm<span class="token operator">=</span><span class="token number">1.0</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> step <span class="token operator">%</span> <span class="token number">300</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">:</span>        <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"step=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>step<span class="token punctuation">&#125;</span></span><span class="token string">, loss=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span><span class="token format-spec">.4f</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span><span class="token punctuation">)</span></code></pre><p>这里使用随机生成的数据，所以每一步都会看到新的数字序列。</p><h3 id="17-4-编写贪心解码"><a href="#17-4-编写贪心解码" class="headerlink" title="17.4 编写贪心解码"></a>17.4 编写贪心解码</h3><pre class="language-python" data-language="python"><code class="language-python"><span class="token decorator annotation punctuation">@torch<span class="token punctuation">.</span>no_grad</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">def</span> <span class="token function">greedy_decode</span><span class="token punctuation">(</span>model<span class="token punctuation">,</span> src<span class="token punctuation">,</span> max_len<span class="token punctuation">)</span><span class="token punctuation">:</span>    model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    _<span class="token punctuation">,</span> hidden <span class="token operator">=</span> model<span class="token punctuation">.</span>encoder<span class="token punctuation">(</span>src<span class="token punctuation">)</span>    batch_size <span class="token operator">=</span> src<span class="token punctuation">.</span>size<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span>    decoder_input <span class="token operator">=</span> torch<span class="token punctuation">.</span>full<span class="token punctuation">(</span>        <span class="token punctuation">(</span>batch_size<span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        BOS_ID<span class="token punctuation">,</span>        dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">long</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span>    generated <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>    finished <span class="token operator">=</span> torch<span class="token punctuation">.</span>zeros<span class="token punctuation">(</span>batch_size<span class="token punctuation">,</span> dtype<span class="token operator">=</span>torch<span class="token punctuation">.</span><span class="token builtin">bool</span><span class="token punctuation">)</span>    <span class="token keyword">for</span> _ <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span>max_len<span class="token punctuation">)</span><span class="token punctuation">:</span>        logits<span class="token punctuation">,</span> hidden <span class="token operator">=</span> model<span class="token punctuation">.</span>decoder<span class="token punctuation">(</span>decoder_input<span class="token punctuation">,</span> hidden<span class="token punctuation">)</span>        next_token <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>        generated<span class="token punctuation">.</span>append<span class="token punctuation">(</span>next_token<span class="token punctuation">)</span>        finished <span class="token operator">|</span><span class="token operator">=</span> next_token<span class="token punctuation">.</span>eq<span class="token punctuation">(</span>EOS_ID<span class="token punctuation">)</span>        <span class="token keyword">if</span> finished<span class="token punctuation">.</span><span class="token builtin">all</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>            <span class="token keyword">break</span>        decoder_input <span class="token operator">=</span> next_token    <span class="token keyword">return</span> torch<span class="token punctuation">.</span>stack<span class="token punctuation">(</span>generated<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><h3 id="17-5-测试结果"><a href="#17-5-测试结果" class="headerlink" title="17.5 测试结果"></a>17.5 测试结果</h3><pre class="language-python" data-language="python"><code class="language-python">test_src <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">7</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">,</span> <span class="token number">9</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">6</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>generated <span class="token operator">=</span> greedy_decode<span class="token punctuation">(</span>    model<span class="token punctuation">,</span>    test_src<span class="token punctuation">,</span>    max_len<span class="token operator">=</span>SEQ_LEN <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"input:\n"</span><span class="token punctuation">,</span> test_src<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"generated:\n"</span><span class="token punctuation">,</span> generated<span class="token punctuation">)</span></code></pre><p>理想输出应该接近：</p><pre class="language-text" data-language="text"><code class="language-text">input:tensor([[3, 7, 5, 9],        [4, 4, 8, 6]])generated:tensor([[9, 5, 7, 3, 2],        [6, 8, 4, 4, 2]])</code></pre><p>其中最后的 <code>2</code> 就是 <code>&lt;EOS&gt;</code>。</p><h2 id="18-为什么要进行梯度裁剪"><a href="#18-为什么要进行梯度裁剪" class="headerlink" title="18. 为什么要进行梯度裁剪"></a>18. 为什么要进行梯度裁剪</h2><p>循环网络会在多个时间步上反复应用相同参数。反向传播时，梯度也要沿时间展开的计算图不断传播。</p><p>这可能导致梯度爆炸：</p><pre class="language-text" data-language="text"><code class="language-text">梯度越来越大 -> 参数更新过猛 -> loss 变成 NaN</code></pre><p>PyTorch 中可以限制整体梯度范数：</p><pre class="language-python" data-language="python"><code class="language-python">nn<span class="token punctuation">.</span>utils<span class="token punctuation">.</span>clip_grad_norm_<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    max_norm<span class="token operator">=</span><span class="token number">1.0</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>梯度裁剪通常放在：</p><pre class="language-text" data-language="text"><code class="language-text">loss.backward() 之后optimizer.step() 之前</code></pre><p>它不能解决所有训练问题，但在 RNN、LSTM、GRU 训练中很常见。</p><h2 id="19-贪心搜索与-Beam-Search"><a href="#19-贪心搜索与-Beam-Search" class="headerlink" title="19. 贪心搜索与 Beam Search"></a>19. 贪心搜索与 Beam Search</h2><p>前面的推理代码每一步都选择概率最大的 token：</p><pre class="language-python" data-language="python"><code class="language-python">next_token <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>这叫贪心搜索。</p><p>它速度快，但当前看起来最好的 token 不一定能组成整体概率最高的序列。</p><p>Beam Search 会同时保留多个候选序列：</p><pre class="language-text" data-language="text"><code class="language-text">第 1 步：保留分数最高的 k 个 token第 2 步：扩展这 k 个候选，再保留总分最高的 k 个...直到生成 EOS 或达到最大长度</code></pre><p>$k$ 称为 beam size。</p><p>Beam Search 通常能得到更好的序列，但计算和内存开销也更大。</p><h2 id="20-变长序列、Padding-与-Mask"><a href="#20-变长序列、Padding-与-Mask" class="headerlink" title="20. 变长序列、Padding 与 Mask"></a>20. 变长序列、Padding 与 Mask</h2><p>真实数据中的句子长度不同，需要 padding：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 NLP &lt;EOS> &lt;PAD>今天天气 很 好 &lt;EOS></code></pre><p>需要处理两个地方。</p><h3 id="损失中忽略-PAD"><a href="#损失中忽略-PAD" class="headerlink" title="损失中忽略 PAD"></a>损失中忽略 PAD</h3><pre class="language-python" data-language="python"><code class="language-python">loss_fn <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span>ignore_index<span class="token operator">=</span>PAD_ID<span class="token punctuation">)</span></code></pre><h3 id="Encoder-中避免让-GRU-处理无效-PAD"><a href="#Encoder-中避免让-GRU-处理无效-PAD" class="headerlink" title="Encoder 中避免让 GRU 处理无效 PAD"></a>Encoder 中避免让 GRU 处理无效 PAD</h3><p>可以使用：</p><pre class="language-python" data-language="python"><code class="language-python">nn<span class="token punctuation">.</span>utils<span class="token punctuation">.</span>rnn<span class="token punctuation">.</span>pack_padded_sequence</code></pre><p>它根据真实长度压缩 batch，让循环网络跳过 padding 位置。</p><p>如果加入 Attention，还需要对 padding 位置进行 mask，防止 Decoder 把注意力分配给 <code>&lt;PAD&gt;</code>。</p><h2 id="21-Seq2Seq-如何评价"><a href="#21-Seq2Seq-如何评价" class="headerlink" title="21. Seq2Seq 如何评价"></a>21. Seq2Seq 如何评价</h2><p>不同任务使用不同指标。</p><table><thead><tr><th>任务</th><th>常见指标</th></tr></thead><tbody><tr><td>机器翻译</td><td>BLEU、COMET</td></tr><tr><td>文本摘要</td><td>ROUGE</td></tr><tr><td>语音识别</td><td>WER、CER</td></tr><tr><td>序列反转</td><td>Exact Match、token accuracy</td></tr><tr><td>时间序列预测</td><td>MAE、MSE、RMSE</td></tr></tbody></table><p>只看训练 loss 不够，还需要在验证集上使用与任务目标一致的指标。</p><p>对于生成任务，也应人工查看一些输出，因为单个自动指标不一定能完整反映流畅度和语义正确性。</p><h2 id="22-常见错误"><a href="#22-常见错误" class="headerlink" title="22. 常见错误"></a>22. 常见错误</h2><h3 id="22-1-Decoder-输入与目标没有错开"><a href="#22-1-Decoder-输入与目标没有错开" class="headerlink" title="22.1 Decoder 输入与目标没有错开"></a>22.1 Decoder 输入与目标没有错开</h3><p>正确关系：</p><pre class="language-text" data-language="text"><code class="language-text">输入：&lt;BOS> y1 y2 y3目标：y1    y2 y3 &lt;EOS></code></pre><p>如果输入和目标完全相同，模型可能只学会复制当前 token。</p><h3 id="22-2-训练时忘记预测-EOS"><a href="#22-2-训练时忘记预测-EOS" class="headerlink" title="22.2 训练时忘记预测 EOS"></a>22.2 训练时忘记预测 EOS</h3><p>目标序列必须包含 <code>&lt;EOS&gt;</code>，否则模型不知道何时停止。</p><h3 id="22-3-对-logits-先做-softmax-再传给交叉熵"><a href="#22-3-对-logits-先做-softmax-再传给交叉熵" class="headerlink" title="22.3 对 logits 先做 softmax 再传给交叉熵"></a>22.3 对 logits 先做 softmax 再传给交叉熵</h3><p>错误：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span> target<span class="token punctuation">)</span></code></pre><p>正确：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> target<span class="token punctuation">)</span></code></pre><h3 id="22-4-忘记忽略-PAD"><a href="#22-4-忘记忽略-PAD" class="headerlink" title="22.4 忘记忽略 PAD"></a>22.4 忘记忽略 PAD</h3><p>如果 padding 也参与损失，模型会花大量精力预测 <code>&lt;PAD&gt;</code>，指标也会失真。</p><h3 id="22-5-Teacher-Forcing-比例始终为-1"><a href="#22-5-Teacher-Forcing-比例始终为-1" class="headerlink" title="22.5 Teacher Forcing 比例始终为 1"></a>22.5 Teacher Forcing 比例始终为 1</h3><p>模型训练时永远只看到正确历史，推理时面对自己的预测可能表现骤降。</p><p>可以从较高比例开始，再根据任务逐步降低。</p><h3 id="22-6-Encoder-和-Decoder-隐藏维度不一致"><a href="#22-6-Encoder-和-Decoder-隐藏维度不一致" class="headerlink" title="22.6 Encoder 和 Decoder 隐藏维度不一致"></a>22.6 Encoder 和 Decoder 隐藏维度不一致</h3><p>如果直接把 Encoder hidden 传给 Decoder，两者的层数和隐藏维度必须兼容。</p><p>如果不一致，需要增加线性映射或其他桥接层。</p><h3 id="22-7-推理没有最大长度限制"><a href="#22-7-推理没有最大长度限制" class="headerlink" title="22.7 推理没有最大长度限制"></a>22.7 推理没有最大长度限制</h3><p>模型可能迟迟不生成 <code>&lt;EOS&gt;</code>。推理循环必须同时设置：</p><ul><li>遇到 <code>&lt;EOS&gt;</code> 停止。</li><li>达到 <code>max_len</code> 强制停止。</li></ul><h2 id="23-Seq2Seq-与-Transformer-的关系"><a href="#23-Seq2Seq-与-Transformer-的关系" class="headerlink" title="23. Seq2Seq 与 Transformer 的关系"></a>23. Seq2Seq 与 Transformer 的关系</h2><p>Transformer 也可以采用 Encoder-Decoder 结构，因此它同样属于广义 Seq2Seq 模型。</p><p>经典 RNN Seq2Seq：</p><pre class="language-text" data-language="text"><code class="language-text">Encoder RNN/GRU/LSTM  -> 隐藏状态或 Attention  -> Decoder RNN/GRU/LSTM</code></pre><p>Transformer Seq2Seq：</p><pre class="language-text" data-language="text"><code class="language-text">Transformer Encoder  -> Cross-Attention  -> Transformer Decoder</code></pre><p>两者的共同点包括：</p><ul><li>输入输出都是序列。</li><li>Decoder 自回归生成。</li><li>使用 <code>&lt;BOS&gt;</code>、<code>&lt;EOS&gt;</code> 和 padding mask。</li><li>训练时目标 token 向右错位。</li><li>推理时使用贪心或 Beam Search。</li></ul><p>主要区别是 Transformer 使用 Attention 并行处理序列，不再依赖逐时间步递归，因此更容易扩展到大规模训练。</p><h2 id="24-总结"><a href="#24-总结" class="headerlink" title="24. 总结"></a>24. 总结</h2><p>Seq2Seq 的核心主线是：</p><pre class="language-text" data-language="text"><code class="language-text">输入 token  -> Encoder 逐步提取序列信息  -> 得到上下文表示  -> Decoder 从 BOS 开始逐步生成  -> 生成 EOS 后停止</code></pre><p>需要重点掌握：</p><ul><li>Encoder 负责理解输入序列。</li><li>Decoder 根据上下文和历史输出预测下一个 token。</li><li>Teacher Forcing 使用真实历史帮助训练。</li><li>推理时只能使用模型自己的预测。</li><li>基础 Seq2Seq 的固定长度向量存在信息瓶颈。</li><li>Attention 让 Decoder 每一步查看不同输入位置。</li><li>PyTorch GRU 的 output 和 hidden 形状不同。</li><li>训练时需要处理 padding、梯度裁剪和输入目标错位。</li></ul><p>理解经典 Seq2Seq 后，再学习 Attention、Transformer、机器翻译和文本生成时，很多概念都会自然连接起来。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>Ilya Sutskever, Oriol Vinyals, Quoc V. Le, <em>Sequence to Sequence Learning with Neural Networks</em>: <a href="https://arxiv.org/abs/1409.3215">https://arxiv.org/abs/1409.3215</a></li><li>Dzmitry Bahdanau, Kyunghyun Cho, Yoshua Bengio, <em>Neural Machine Translation by Jointly Learning to Align and Translate</em>: <a href="https://arxiv.org/abs/1409.0473">https://arxiv.org/abs/1409.0473</a></li><li>PyTorch Documentation, <code>torch.nn.GRU</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.GRU.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.GRU.html</a></li><li>PyTorch Documentation, <code>pack_padded_sequence</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.utils.rnn.pack_padded_sequence.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.utils.rnn.pack_padded_sequence.html</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/10/nlp-seq2seq-encoder-decoder/</id>
    <link href="https://lsworl.github.io/2026/08/10/nlp-seq2seq-encoder-decoder/"/>
    <published>2026-08-09T16:00:00.000Z</published>
    <summary>从序列到序列任务出发，理解Encoder-Decoder、隐藏状态、Teacher Forcing、训练与推理差异、Attention，并用PyTorch GRU实现一个完整Seq2Seq模型。</summary>
    <title>Seq2Seq：从Encoder-Decoder到Attention与PyTorch实现</title>
    <updated>2026-08-10T12:38:54.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="机器学习" scheme="https://lsworl.github.io/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="机器学习" scheme="https://lsworl.github.io/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="深度学习" scheme="https://lsworl.github.io/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <category term="优化算法" scheme="https://lsworl.github.io/tags/%E4%BC%98%E5%8C%96%E7%AE%97%E6%B3%95/"/>
    <category term="Adam" scheme="https://lsworl.github.io/tags/Adam/"/>
    <content>
      <![CDATA[<p>训练神经网络时，我们经常会看到下面这行代码：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">0.001</span><span class="token punctuation">)</span></code></pre><p>它很短，却承担着一个关键任务：根据反向传播得到的梯度，不断更新模型中的权重和偏置。</p><p>Adam 的全称是 <strong>Adaptive Moment Estimation</strong>，可以翻译为“自适应矩估计”。它同时利用梯度的移动平均和梯度平方的移动平均，为不同参数自动调整更新方向与步长。</p><p>一句话概括：</p><blockquote><p>Adam 在动量方法的基础上记录梯度方向，又根据历史梯度大小为每个参数调整学习率，因此通常比普通 SGD 更容易上手。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/ml-blog/adam-overview.svg" alt="Adam 优化器更新参数的整体流程"></p><h2 id="1-优化器到底负责什么"><a href="#1-优化器到底负责什么" class="headerlink" title="1. 优化器到底负责什么"></a>1. 优化器到底负责什么</h2><p>神经网络训练通常包含三个核心步骤：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>它们的职责并不相同：</p><table><thead><tr><th>代码</th><th>作用</th></tr></thead><tbody><tr><td><code>optimizer.zero_grad()</code></td><td>清空上一轮保存在参数上的梯度</td></tr><tr><td><code>loss.backward()</code></td><td>根据计算图和链式法则计算梯度</td></tr><tr><td><code>optimizer.step()</code></td><td>根据梯度和优化算法更新参数</td></tr></tbody></table><p>需要特别注意：</p><blockquote><p><code>loss.backward()</code> 只负责计算梯度，真正修改参数的是 <code>optimizer.step()</code>。</p></blockquote><p>对于普通梯度下降，参数更新公式为：</p><p>$$<br>\theta_t&#x3D;\theta_{t-1}-\alpha g_t<br>$$</p><p>其中：</p><ul><li>$\theta_t$ 是第 $t$ 步更新后的参数。</li><li>$\alpha$ 是学习率。</li><li>$g_t&#x3D;\nabla_\theta L_t$ 是当前损失对参数的梯度。</li></ul><p>Adam 仍然沿着梯度方向更新参数，但不会直接使用当前梯度，而是先对梯度进行两类统计。</p><h2 id="2-为什么不总是直接使用-SGD"><a href="#2-为什么不总是直接使用-SGD" class="headerlink" title="2. 为什么不总是直接使用 SGD"></a>2. 为什么不总是直接使用 SGD</h2><p>SGD 简单、可靠，而且在很多任务上有很好的泛化能力。不过，普通 SGD 也会遇到一些困难。</p><h3 id="2-1-不同方向的梯度尺度差异很大"><a href="#2-1-不同方向的梯度尺度差异很大" class="headerlink" title="2.1 不同方向的梯度尺度差异很大"></a>2.1 不同方向的梯度尺度差异很大</h3><p>假设损失曲面像一条狭长山谷：</p><ul><li>水平方向比较平缓。</li><li>垂直方向非常陡峭。</li></ul><p>如果所有参数共用相同学习率，SGD 可能会在陡峭方向来回震荡，在平缓方向前进得很慢。</p><h3 id="2-2-小批量梯度存在噪声"><a href="#2-2-小批量梯度存在噪声" class="headerlink" title="2.2 小批量梯度存在噪声"></a>2.2 小批量梯度存在噪声</h3><p>深度学习通常使用 mini-batch 训练。每个 batch 包含的样本不同，因此每一步梯度都会略有变化。</p><p>如果只看当前梯度，更新方向可能左右摇摆。</p><h3 id="2-3-不同参数需要的有效步长可能不同"><a href="#2-3-不同参数需要的有效步长可能不同" class="headerlink" title="2.3 不同参数需要的有效步长可能不同"></a>2.3 不同参数需要的有效步长可能不同</h3><p>某些参数经常获得较大梯度，另一些参数的梯度可能很小或很稀疏。</p><p>统一学习率无法针对每个参数的历史梯度尺度进行调整。</p><p>Adam 的设计就是同时处理“方向不稳定”和“尺度不一致”这两个问题。</p><p><img src="https://lsworl.github.io/images/ml-blog/adam-components.svg" alt="SGD、动量、自适应缩放与 Adam 的关系"></p><h2 id="3-Adam-的核心思想"><a href="#3-Adam-的核心思想" class="headerlink" title="3. Adam 的核心思想"></a>3. Adam 的核心思想</h2><p>Adam 可以理解为把两种思想组合在一起：</p><ol><li>用梯度的指数移动平均估计更新方向。</li><li>用梯度平方的指数移动平均估计梯度尺度。</li></ol><p>第一部分类似 Momentum，第二部分与 RMSProp 的思路接近。</p><h3 id="3-1-一阶矩：记录梯度方向"><a href="#3-1-一阶矩：记录梯度方向" class="headerlink" title="3.1 一阶矩：记录梯度方向"></a>3.1 一阶矩：记录梯度方向</h3><p>Adam 使用下面的公式更新一阶矩：</p><p>$$<br>m_t&#x3D;\beta_1m_{t-1}+(1-\beta_1)g_t<br>$$</p><p>$m_t$ 可以理解为近期梯度的加权平均。</p><p>如果连续多个 batch 的梯度方向一致，$m_t$ 会积累这个方向；如果某一步梯度突然反向，历史信息会减小这次波动的影响。</p><p>这有点像一个带惯性的球：不会因为某一步坡度轻微变化就立即完全改变方向。</p><p>PyTorch 中 $\beta_1$ 默认通常取：</p><p>$$<br>\beta_1&#x3D;0.9<br>$$</p><p>这表示新的 $m_t$ 保留 90% 的历史估计，并加入 10% 的当前梯度。</p><h3 id="3-2-二阶矩：记录梯度大小"><a href="#3-2-二阶矩：记录梯度大小" class="headerlink" title="3.2 二阶矩：记录梯度大小"></a>3.2 二阶矩：记录梯度大小</h3><p>Adam 还会更新梯度平方的指数移动平均：</p><p>$$<br>v_t&#x3D;\beta_2v_{t-1}+(1-\beta_2)g_t^2<br>$$</p><p>这里的平方是逐元素计算。</p><p>$v_t$ 记录的是近期梯度的尺度：</p><ul><li>某个参数长期梯度较大，$v_t$ 就会较大，实际更新步长会被压小。</li><li>某个参数长期梯度较小，$v_t$ 就会较小，更新不会被过度缩小。</li></ul><p>PyTorch 中 $\beta_2$ 默认通常取：</p><p>$$<br>\beta_2&#x3D;0.999<br>$$</p><p>它比 $\beta_1$ 更接近 1，意味着梯度平方的统计通常会保留更长的历史。</p><h2 id="4-Adam-的完整更新公式"><a href="#4-Adam-的完整更新公式" class="headerlink" title="4. Adam 的完整更新公式"></a>4. Adam 的完整更新公式</h2><p>假设当前参数是 $\theta_{t-1}$，当前梯度为：</p><p>$$<br>g_t&#x3D;\nabla_\theta L_t<br>$$</p><p>Adam 的一次更新可以拆成下面几步。</p><h3 id="第一步：更新一阶矩"><a href="#第一步：更新一阶矩" class="headerlink" title="第一步：更新一阶矩"></a>第一步：更新一阶矩</h3><p>$$<br>m_t&#x3D;\beta_1m_{t-1}+(1-\beta_1)g_t<br>$$</p><h3 id="第二步：更新二阶矩"><a href="#第二步：更新二阶矩" class="headerlink" title="第二步：更新二阶矩"></a>第二步：更新二阶矩</h3><p>$$<br>v_t&#x3D;\beta_2v_{t-1}+(1-\beta_2)g_t^2<br>$$</p><h3 id="第三步：进行偏差修正"><a href="#第三步：进行偏差修正" class="headerlink" title="第三步：进行偏差修正"></a>第三步：进行偏差修正</h3><p>$$<br>\hat{m}_t&#x3D;\frac{m_t}{1-\beta_1^t}<br>$$</p><p>$$<br>\hat{v}_t&#x3D;\frac{v_t}{1-\beta_2^t}<br>$$</p><h3 id="第四步：更新参数"><a href="#第四步：更新参数" class="headerlink" title="第四步：更新参数"></a>第四步：更新参数</h3><p>$$<br>\theta_t&#x3D;<br>\theta_{t-1}<br>-\alpha\frac{\hat{m}_t}{\sqrt{\hat{v}_t}+\epsilon}<br>$$</p><p>其中：</p><ul><li>$\alpha$ 是基础学习率。</li><li>$\beta_1$ 控制一阶矩的平滑程度。</li><li>$\beta_2$ 控制二阶矩的平滑程度。</li><li>$\epsilon$ 是很小的正数，用来防止分母为 0 并提升数值稳定性。</li><li>$\hat{m}_t$ 和 $\hat{v}_t$ 是偏差修正后的估计。</li></ul><h2 id="5-为什么需要偏差修正"><a href="#5-为什么需要偏差修正" class="headerlink" title="5. 为什么需要偏差修正"></a>5. 为什么需要偏差修正</h2><p>Adam 通常把一阶矩和二阶矩初始化为 0：</p><p>$$<br>m_0&#x3D;0,\quad v_0&#x3D;0<br>$$</p><p>在第一步训练时：</p><p>$$<br>m_1&#x3D;(1-\beta_1)g_1<br>$$</p><p>如果 $\beta_1&#x3D;0.9$，那么：</p><p>$$<br>m_1&#x3D;0.1g_1<br>$$</p><p>它明显比真实梯度小，因为移动平均刚刚开始，还没有积累足够的历史信息。</p><p>二阶矩同样存在这个问题。如果 $\beta_2&#x3D;0.999$：</p><p>$$<br>v_1&#x3D;0.001g_1^2<br>$$</p><p>所以 Adam 使用 $1-\beta_1^t$ 和 $1-\beta_2^t$ 进行修正。</p><p>第一步的一阶矩修正后：</p><p>$$<br>\hat{m}_1&#x3D;\frac{0.1g_1}{1-0.9}&#x3D;g_1<br>$$</p><p>第一步的二阶矩修正后：</p><p>$$<br>\hat{v}_1&#x3D;\frac{0.001g_1^2}{1-0.999}&#x3D;g_1^2<br>$$</p><p>随着 $t$ 增大，$\beta_1^t$ 和 $\beta_2^t$ 会逐渐接近 0，偏差修正的影响也会逐渐减小。</p><p><img src="https://lsworl.github.io/images/ml-blog/adam-bias-correction.svg" alt="Adam 偏差修正解决初始估计偏小的问题"></p><h2 id="6-手工计算两步-Adam-更新"><a href="#6-手工计算两步-Adam-更新" class="headerlink" title="6. 手工计算两步 Adam 更新"></a>6. 手工计算两步 Adam 更新</h2><p>为了看清每个变量的作用，下面只优化一个参数。</p><p>假设：</p><p>$$<br>\theta_0&#x3D;1<br>$$</p><p>并使用：</p><p>$$<br>\alpha&#x3D;0.001,\quad<br>\beta_1&#x3D;0.9,\quad<br>\beta_2&#x3D;0.999<br>$$</p><p>为了简化手算，暂时忽略非常小的 $\epsilon$。</p><h3 id="6-1-第一步：梯度-g-1-x3D-2"><a href="#6-1-第一步：梯度-g-1-x3D-2" class="headerlink" title="6.1 第一步：梯度 $g_1&#x3D;2$"></a>6.1 第一步：梯度 $g_1&#x3D;2$</h3><p>一阶矩：</p><p>$$<br>m_1&#x3D;0.9\times0+0.1\times2&#x3D;0.2<br>$$</p><p>二阶矩：</p><p>$$<br>v_1&#x3D;0.999\times0+0.001\times2^2&#x3D;0.004<br>$$</p><p>偏差修正：</p><p>$$<br>\hat{m}_1&#x3D;\frac{0.2}{1-0.9}&#x3D;2<br>$$</p><p>$$<br>\hat{v}_1&#x3D;\frac{0.004}{1-0.999}&#x3D;4<br>$$</p><p>参数更新量：</p><p>$$<br>\Delta\theta_1<br>&#x3D;0.001\frac{2}{\sqrt{4}}<br>&#x3D;0.001<br>$$</p><p>所以：</p><p>$$<br>\theta_1&#x3D;1-0.001&#x3D;0.999<br>$$</p><h3 id="6-2-第二步：梯度-g-2-x3D-1"><a href="#6-2-第二步：梯度-g-2-x3D-1" class="headerlink" title="6.2 第二步：梯度 $g_2&#x3D;1$"></a>6.2 第二步：梯度 $g_2&#x3D;1$</h3><p>一阶矩：</p><p>$$<br>m_2&#x3D;0.9\times0.2+0.1\times1&#x3D;0.28<br>$$</p><p>二阶矩：</p><p>$$<br>v_2&#x3D;0.999\times0.004+0.001\times1^2&#x3D;0.004996<br>$$</p><p>偏差修正：</p><p>$$<br>\hat{m}_2&#x3D;\frac{0.28}{1-0.9^2}<br>\approx1.4737<br>$$</p><p>$$<br>\hat{v}_2&#x3D;\frac{0.004996}{1-0.999^2}<br>\approx2.4992<br>$$</p><p>参数更新量大约为：</p><p>$$<br>\Delta\theta_2<br>&#x3D;0.001\frac{1.4737}{\sqrt{2.4992}}<br>\approx0.000932<br>$$</p><p>因此：</p><p>$$<br>\theta_2\approx0.999-0.000932&#x3D;0.998068<br>$$</p><p>可以发现，虽然第二步梯度从 2 变成了 1，但 Adam 的更新并不是简单地把学习率乘以当前梯度。历史方向和历史尺度都会影响这一步。</p><p><img src="https://lsworl.github.io/images/ml-blog/adam-two-step-example.svg" alt="两步 Adam 参数更新的计算结果"></p><h2 id="7-Adam-与其他优化器的区别"><a href="#7-Adam-与其他优化器的区别" class="headerlink" title="7. Adam 与其他优化器的区别"></a>7. Adam 与其他优化器的区别</h2><table><thead><tr><th>优化器</th><th>是否记录方向</th><th>是否调整参数尺度</th><th>常见特点</th></tr></thead><tbody><tr><td>SGD</td><td>否</td><td>否</td><td>简单，更新完全由当前梯度决定</td></tr><tr><td>SGD + Momentum</td><td>是</td><td>否</td><td>减少震荡，加快稳定方向上的前进</td></tr><tr><td>RMSProp</td><td>否</td><td>是</td><td>根据梯度平方缩放每个参数的步长</td></tr><tr><td>Adam</td><td>是</td><td>是</td><td>同时使用一阶矩和二阶矩</td></tr><tr><td>AdamW</td><td>是</td><td>是</td><td>在 Adam 基础上解耦权重衰减</td></tr></tbody></table><p>Adam 并不是在所有任务上都一定优于 SGD。</p><ul><li>Adam 往往在训练前期下降更快，对学习率不那么敏感。</li><li>SGD 配合 Momentum 在部分视觉任务中可能获得更好的最终泛化效果。</li><li>稀疏梯度、大模型预训练、Transformer 等任务经常使用 Adam 或 AdamW。</li></ul><p>选择优化器时，不能只看训练损失下降速度，也要观察验证集效果。</p><h2 id="8-Adam-的超参数"><a href="#8-Adam-的超参数" class="headerlink" title="8. Adam 的超参数"></a>8. Adam 的超参数</h2><p>PyTorch 中的常见写法是：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">,</span>    betas<span class="token operator">=</span><span class="token punctuation">(</span><span class="token number">0.9</span><span class="token punctuation">,</span> <span class="token number">0.999</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    eps<span class="token operator">=</span><span class="token number">1e-8</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">0.0</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="8-1-lr"><a href="#8-1-lr" class="headerlink" title="8.1 lr"></a>8.1 <code>lr</code></h3><p><code>lr</code> 是基础学习率，也是最值得优先调整的参数。</p><p>常见起点：</p><pre class="language-text" data-language="text"><code class="language-text">1e-3 = 0.001</code></pre><p>如果训练不稳定、损失剧烈波动，可以尝试：</p><pre class="language-text" data-language="text"><code class="language-text">3e-4、1e-4</code></pre><p>如果损失下降非常慢，也可以在确保稳定的前提下适当增大学习率。</p><p>“自适应学习率”并不代表 Adam 不需要设置 <code>lr</code>。Adam 只是根据历史梯度对基础学习率进行逐参数缩放。</p><h3 id="8-2-betas"><a href="#8-2-betas" class="headerlink" title="8.2 betas"></a>8.2 <code>betas</code></h3><pre class="language-python" data-language="python"><code class="language-python">betas<span class="token operator">=</span><span class="token punctuation">(</span><span class="token number">0.9</span><span class="token punctuation">,</span> <span class="token number">0.999</span><span class="token punctuation">)</span></code></pre><ul><li><code>beta1</code> 控制梯度方向的移动平均。</li><li><code>beta2</code> 控制梯度平方的移动平均。</li></ul><p>大多数普通任务可以先使用默认值。</p><p>如果梯度变化很快，减小 <code>beta2</code> 会让二阶矩更快响应近期变化，但也会让估计更容易波动。</p><h3 id="8-3-eps"><a href="#8-3-eps" class="headerlink" title="8.3 eps"></a>8.3 <code>eps</code></h3><pre class="language-python" data-language="python"><code class="language-python">eps<span class="token operator">=</span><span class="token number">1e-8</span></code></pre><p><code>eps</code> 主要用于数值稳定。通常不需要调整。</p><p>在低精度训练或特定数值不稳定场景中，有时会使用更大的 <code>eps</code>，但应结合实际损失和梯度情况判断。</p><h3 id="8-4-weight-decay"><a href="#8-4-weight-decay" class="headerlink" title="8.4 weight_decay"></a>8.4 <code>weight_decay</code></h3><p>权重衰减用于限制参数过度增大，帮助缓解过拟合。</p><p>如果希望使用解耦式权重衰减，通常更推荐 AdamW：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">1e-2</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h2 id="9-Adam-和-AdamW-有什么区别"><a href="#9-Adam-和-AdamW-有什么区别" class="headerlink" title="9. Adam 和 AdamW 有什么区别"></a>9. Adam 和 AdamW 有什么区别</h2><p>传统 L2 正则化会把参数惩罚项加入损失函数：</p><p>$$<br>L’&#x3D;L+\frac{\lambda}{2}\lVert\theta\rVert_2^2<br>$$</p><p>它会让梯度中多出一项：</p><p>$$<br>\nabla_\theta L’&#x3D;\nabla_\theta L+\lambda\theta<br>$$</p><p>对于普通 SGD，L2 正则化与权重衰减在常见设置下可以得到等价形式。</p><p>但 Adam 会对梯度做自适应缩放。如果把 $\lambda\theta$ 直接混入梯度，这个正则化项也会被一阶矩和二阶矩处理，效果不再等同于直接衰减参数。</p><p>AdamW 将权重衰减从梯度更新中分离出来，可以直观理解为在 Adam 更新之外单独缩小参数：</p><p>$$<br>\theta_t<br>&#x3D;\theta_{t-1}<br>-\text{AdamUpdate}<br>-\alpha\lambda\theta_{t-1}<br>$$</p><p>实际使用时可以先记住：</p><blockquote><p>需要权重衰减时，尤其在 Transformer 等模型中，通常优先考虑 <code>torch.optim.AdamW</code>。</p></blockquote><h2 id="10-PyTorch-使用-Adam-完整训练一个-MLP"><a href="#10-PyTorch-使用-Adam-完整训练一个-MLP" class="headerlink" title="10. PyTorch 使用 Adam 完整训练一个 MLP"></a>10. PyTorch 使用 Adam 完整训练一个 MLP</h2><p>下面使用 Adam 训练一个 XOR 分类器。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">from</span> torch <span class="token keyword">import</span> nntorch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">42</span><span class="token punctuation">)</span>X <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>y <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">class</span> <span class="token class-name">XORNet</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>net <span class="token operator">=</span> nn<span class="token punctuation">.</span>Sequential<span class="token punctuation">(</span>            nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            nn<span class="token punctuation">.</span>Tanh<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> self<span class="token punctuation">.</span>net<span class="token punctuation">(</span>x<span class="token punctuation">)</span>model <span class="token operator">=</span> XORNet<span class="token punctuation">(</span><span class="token punctuation">)</span>loss_fn <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">0.01</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    model<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>X<span class="token punctuation">)</span>    loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> y<span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span>set_to_none<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> epoch <span class="token operator">%</span> <span class="token number">200</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">:</span>        <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"epoch=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>epoch<span class="token punctuation">&#125;</span></span><span class="token string">, loss=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span><span class="token format-spec">.6f</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>X<span class="token punctuation">)</span>    probabilities <span class="token operator">=</span> torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>    predictions <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"predictions:"</span><span class="token punctuation">,</span> predictions<span class="token punctuation">.</span>tolist<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"probabilities:\n"</span><span class="token punctuation">,</span> probabilities<span class="token punctuation">)</span></code></pre><p>训练成功后，预测类别应接近：</p><pre class="language-text" data-language="text"><code class="language-text">predictions: [0, 1, 1, 0]</code></pre><h3 id="10-1-一次训练迭代发生了什么"><a href="#10-1-一次训练迭代发生了什么" class="headerlink" title="10.1 一次训练迭代发生了什么"></a>10.1 一次训练迭代发生了什么</h3><pre class="language-python" data-language="python"><code class="language-python">logits <span class="token operator">=</span> model<span class="token punctuation">(</span>X<span class="token punctuation">)</span></code></pre><p>执行前向传播，得到两个类别的 logits。</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> y<span class="token punctuation">)</span></code></pre><p>根据真实标签计算交叉熵损失。</p><pre class="language-python" data-language="python"><code class="language-python">optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span>set_to_none<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span></code></pre><p>清空旧梯度。<code>set_to_none=True</code> 会把梯度设为 <code>None</code>，通常可以减少不必要的内存写入。</p><pre class="language-python" data-language="python"><code class="language-python">loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>Autograd 计算每个参数的梯度，并写入参数的 <code>.grad</code>。</p><pre class="language-python" data-language="python"><code class="language-python">optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>Adam 读取每个参数的梯度，更新该参数对应的 $m_t$、$v_t$ 和步数 $t$，然后修改参数值。</p><p><img src="https://lsworl.github.io/images/ml-blog/adam-pytorch-loop.svg" alt="PyTorch 中 Adam 与反向传播的协作流程"></p><h2 id="11-优化器内部保存了什么状态"><a href="#11-优化器内部保存了什么状态" class="headerlink" title="11. 优化器内部保存了什么状态"></a>11. 优化器内部保存了什么状态</h2><p>SGD 在不使用 Momentum 时，几乎只需要读取当前梯度。</p><p>Adam 则需要为每个可训练参数保存：</p><ul><li>一阶矩 $m_t$。</li><li>二阶矩 $v_t$。</li><li>当前更新步数 $t$。</li></ul><p>因此 Adam 会比普通 SGD 使用更多优化器状态内存。</p><p>如果模型参数本身占用 $P$ 单位内存，仅从概念上看，Adam 还需要额外保存与参数形状相同的一阶矩和二阶矩，优化器状态可能带来大约额外 $2P$ 的开销；实际训练还要计算参数、梯度、混合精度副本等其他内存。</p><p>可以检查优化器状态：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token comment"># 至少执行一次 optimizer.step() 后才会创建大部分状态</span>state <span class="token operator">=</span> optimizer<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>state<span class="token punctuation">.</span>keys<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>state<span class="token punctuation">[</span><span class="token string">"param_groups"</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>state<span class="token punctuation">[</span><span class="token string">"state"</span><span class="token punctuation">]</span><span class="token punctuation">)</span></code></pre><p>保存断点时，除了模型参数，还应该保存优化器状态：</p><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>save<span class="token punctuation">(</span><span class="token punctuation">&#123;</span>    <span class="token string">"model"</span><span class="token punctuation">:</span> model<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token string">"optimizer"</span><span class="token punctuation">:</span> optimizer<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token string">"epoch"</span><span class="token punctuation">:</span> epoch<span class="token punctuation">,</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span> <span class="token string">"checkpoint.pth"</span><span class="token punctuation">)</span></code></pre><p>恢复训练：</p><pre class="language-python" data-language="python"><code class="language-python">checkpoint <span class="token operator">=</span> torch<span class="token punctuation">.</span>load<span class="token punctuation">(</span><span class="token string">"checkpoint.pth"</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span>load_state_dict<span class="token punctuation">(</span>checkpoint<span class="token punctuation">[</span><span class="token string">"model"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>load_state_dict<span class="token punctuation">(</span>checkpoint<span class="token punctuation">[</span><span class="token string">"optimizer"</span><span class="token punctuation">]</span><span class="token punctuation">)</span>start_epoch <span class="token operator">=</span> checkpoint<span class="token punctuation">[</span><span class="token string">"epoch"</span><span class="token punctuation">]</span> <span class="token operator">+</span> <span class="token number">1</span></code></pre><p>如果只加载模型参数、不加载优化器状态，Adam 之前积累的一阶矩、二阶矩和步数都会丢失，恢复后的更新轨迹可能发生变化。</p><h2 id="12-为不同参数设置不同学习率"><a href="#12-为不同参数设置不同学习率" class="headerlink" title="12. 为不同参数设置不同学习率"></a>12. 为不同参数设置不同学习率</h2><p>PyTorch 优化器支持参数组。</p><p>假设一个模型包含特征提取器和分类头，可以让分类头使用更大的学习率：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"params"</span><span class="token punctuation">:</span> model<span class="token punctuation">.</span>backbone<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"lr"</span><span class="token punctuation">:</span> <span class="token number">1e-4</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span>        <span class="token string">"params"</span><span class="token punctuation">:</span> model<span class="token punctuation">.</span>classifier<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"lr"</span><span class="token punctuation">:</span> <span class="token number">1e-3</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">,</span> weight_decay<span class="token operator">=</span><span class="token number">1e-2</span><span class="token punctuation">)</span></code></pre><p>这种写法常用于迁移学习和微调：</p><ul><li>预训练层已经学到较好表示，使用较小学习率。</li><li>新加入的分类头需要从头学习，使用较大学习率。</li></ul><p>还可以对偏置和归一化参数关闭权重衰减：</p><pre class="language-python" data-language="python"><code class="language-python">decay_params <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>no_decay_params <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token keyword">for</span> name<span class="token punctuation">,</span> param <span class="token keyword">in</span> model<span class="token punctuation">.</span>named_parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">if</span> <span class="token keyword">not</span> param<span class="token punctuation">.</span>requires_grad<span class="token punctuation">:</span>        <span class="token keyword">continue</span>    <span class="token keyword">if</span> name<span class="token punctuation">.</span>endswith<span class="token punctuation">(</span><span class="token string">"bias"</span><span class="token punctuation">)</span> <span class="token keyword">or</span> <span class="token string">"norm"</span> <span class="token keyword">in</span> name<span class="token punctuation">.</span>lower<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        no_decay_params<span class="token punctuation">.</span>append<span class="token punctuation">(</span>param<span class="token punctuation">)</span>    <span class="token keyword">else</span><span class="token punctuation">:</span>        decay_params<span class="token punctuation">.</span>append<span class="token punctuation">(</span>param<span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>AdamW<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">&#123;</span><span class="token string">"params"</span><span class="token punctuation">:</span> decay_params<span class="token punctuation">,</span> <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">1e-2</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token punctuation">&#123;</span><span class="token string">"params"</span><span class="token punctuation">:</span> no_decay_params<span class="token punctuation">,</span> <span class="token string">"weight_decay"</span><span class="token punctuation">:</span> <span class="token number">0.0</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">)</span></code></pre><h2 id="13-Adam-仍然可以配合学习率调度器"><a href="#13-Adam-仍然可以配合学习率调度器" class="headerlink" title="13. Adam 仍然可以配合学习率调度器"></a>13. Adam 仍然可以配合学习率调度器</h2><p>Adam 会为不同参数调整有效步长，但基础学习率仍然非常重要。</p><p>因此 Adam 也常与学习率调度器一起使用。</p><p>例如每 20 个 epoch 把学习率乘以 0.5：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">)</span>scheduler <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>lr_scheduler<span class="token punctuation">.</span>StepLR<span class="token punctuation">(</span>    optimizer<span class="token punctuation">,</span>    step_size<span class="token operator">=</span><span class="token number">20</span><span class="token punctuation">,</span>    gamma<span class="token operator">=</span><span class="token number">0.5</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    train_one_epoch<span class="token punctuation">(</span>model<span class="token punctuation">,</span> optimizer<span class="token punctuation">)</span>    scheduler<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>Transformer 训练中还常见 warmup：训练开始时先逐渐增大学习率，避免模型在参数和优化器状态尚不稳定时迈出过大的步子。</p><h2 id="14-常见错误"><a href="#14-常见错误" class="headerlink" title="14. 常见错误"></a>14. 常见错误</h2><h3 id="14-1-忘记清空梯度"><a href="#14-1-忘记清空梯度" class="headerlink" title="14.1 忘记清空梯度"></a>14.1 忘记清空梯度</h3><p>错误写法：</p><pre class="language-python" data-language="python"><code class="language-python">loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>如果循环中一直不调用 <code>zero_grad()</code>，梯度会不断累积。</p><p>普通训练建议写成：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><h3 id="14-2-在训练循环中反复创建优化器"><a href="#14-2-在训练循环中反复创建优化器" class="headerlink" title="14.2 在训练循环中反复创建优化器"></a>14.2 在训练循环中反复创建优化器</h3><p>错误写法：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">1e-3</span><span class="token punctuation">)</span>    <span class="token comment"># training...</span></code></pre><p>每次重新创建 Adam，都会丢失之前的一阶矩和二阶矩。</p><p>优化器通常应该在训练循环外创建一次。</p><h3 id="14-3-认为-Adam-不需要调学习率"><a href="#14-3-认为-Adam-不需要调学习率" class="headerlink" title="14.3 认为 Adam 不需要调学习率"></a>14.3 认为 Adam 不需要调学习率</h3><p>Adam 的自适应缩放不能代替基础学习率调节。</p><p>如果出现下面情况，仍然要检查 <code>lr</code>：</p><ul><li>损失变成 <code>NaN</code>。</li><li>损失剧烈震荡。</li><li>参数或梯度突然变得非常大。</li><li>训练长期没有明显进展。</li></ul><h3 id="14-4-忘记保存优化器状态"><a href="#14-4-忘记保存优化器状态" class="headerlink" title="14.4 忘记保存优化器状态"></a>14.4 忘记保存优化器状态</h3><p>如果要继续训练，应同时保存：</p><ul><li><code>model.state_dict()</code></li><li><code>optimizer.state_dict()</code></li><li>当前 epoch 或 step</li><li>学习率调度器状态（如果使用）</li></ul><h3 id="14-5-把训练损失下降快等同于泛化更好"><a href="#14-5-把训练损失下降快等同于泛化更好" class="headerlink" title="14.5 把训练损失下降快等同于泛化更好"></a>14.5 把训练损失下降快等同于泛化更好</h3><p>Adam 可能很快降低训练损失，但最终模型是否更好仍然要看验证集指标。</p><h2 id="15-Adam-什么时候适合使用"><a href="#15-Adam-什么时候适合使用" class="headerlink" title="15. Adam 什么时候适合使用"></a>15. Adam 什么时候适合使用</h2><p>Adam 适合作为这些场景的起点：</p><ul><li>希望快速得到一个可用基线。</li><li>梯度尺度差异较大。</li><li>梯度较稀疏。</li><li>训练 Transformer、语言模型或复杂多任务网络。</li><li>不确定 SGD 的学习率和 Momentum 应如何配置。</li></ul><p>可以进一步考虑 AdamW 的情况：</p><ul><li>需要明确使用权重衰减。</li><li>训练 Transformer、ViT、BERT 等模型。</li><li>希望将自适应梯度更新与参数衰减分离。</li></ul><p>可能考虑 SGD + Momentum 的情况：</p><ul><li>在特定计算机视觉任务中追求最终泛化效果。</li><li>已经有成熟的学习率、Momentum 和调度策略。</li><li>优化器状态内存比较紧张。</li></ul><h2 id="16-总结"><a href="#16-总结" class="headerlink" title="16. 总结"></a>16. 总结</h2><p>Adam 的核心可以浓缩成四步：</p><pre class="language-text" data-language="text"><code class="language-text">计算当前梯度 g  -> 更新梯度移动平均 m  -> 更新梯度平方移动平均 v  -> 偏差修正后更新参数</code></pre><p>从直觉上看：</p><ul><li>$m_t$ 判断最近应该往哪个方向走。</li><li>$v_t$ 判断每个参数方向上的历史梯度有多大。</li><li>偏差修正解决训练初期统计量偏向 0 的问题。</li><li>学习率 $\alpha$ 仍然控制整体更新尺度。</li></ul><p>在 PyTorch 中，最核心的训练模板仍然是：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span>set_to_none<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>Adam 并不是一个不用调参、永远最优的优化器，但它通常稳定、易用，是深度学习实验非常实用的起点。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>Diederik P. Kingma, Jimmy Ba, <em>Adam: A Method for Stochastic Optimization</em>: <a href="https://arxiv.org/abs/1412.6980">https://arxiv.org/abs/1412.6980</a></li><li>Ilya Loshchilov, Frank Hutter, <em>Decoupled Weight Decay Regularization</em>: <a href="https://arxiv.org/abs/1711.05101">https://arxiv.org/abs/1711.05101</a></li><li>PyTorch Documentation, <code>torch.optim.Adam</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.optim.Adam.html">https://docs.pytorch.org/docs/stable/generated/torch.optim.Adam.html</a></li><li>PyTorch Documentation, <code>torch.optim.AdamW</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.optim.AdamW.html">https://docs.pytorch.org/docs/stable/generated/torch.optim.AdamW.html</a></li><li>PyTorch Tutorials, <em>Optimizing Model Parameters</em>: <a href="https://docs.pytorch.org/tutorials/beginner/basics/optimization_tutorial.html">https://docs.pytorch.org/tutorials/beginner/basics/optimization_tutorial.html</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/09/machine-learning-adam-optimizer/</id>
    <link href="https://lsworl.github.io/2026/08/09/machine-learning-adam-optimizer/"/>
    <published>2026-08-08T16:00:00.000Z</published>
    <summary>从 SGD 的局限出发，理解 Adam 的动量、二阶矩、偏差修正和参数更新公式，并通过手算例子与 PyTorch 完整代码掌握 Adam 和 AdamW 的使用方法。</summary>
    <title>Adam Optimizer：从梯度下降到自适应学习率</title>
    <updated>2026-08-09T15:23:30.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="神经网络" scheme="https://lsworl.github.io/tags/%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Word2Vec" scheme="https://lsworl.github.io/tags/Word2Vec/"/>
    <category term="词向量" scheme="https://lsworl.github.io/tags/%E8%AF%8D%E5%90%91%E9%87%8F/"/>
    <category term="文本分类" scheme="https://lsworl.github.io/tags/%E6%96%87%E6%9C%AC%E5%88%86%E7%B1%BB/"/>
    <content>
      <![CDATA[<p>前面学习文本处理时，我们已经知道：自然语言不能直接送进模型，必须先变成数字。</p><p>最朴素的方法是 One-hot。比如词表里有 10000 个词，每个词就用一个长度为 10000 的向量表示，属于自己的位置为 1，其他位置为 0。</p><p>但 One-hot 有两个明显问题：</p><ul><li>向量太稀疏，绝大多数位置都是 0。</li><li>词和词之间没有语义关系，“国王”和“王后”在 One-hot 空间里并不比“国王”和“香蕉”更接近。</li></ul><p>word2vec 要解决的就是这个问题：</p><blockquote><p>把词表示成低维、稠密、带有语义关系的向量。</p></blockquote><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">国王 -> [0.32, -0.18, 0.71, ...]王后 -> [0.29, -0.15, 0.69, ...]香蕉 -> [-0.44, 0.83, 0.12, ...]</code></pre><p>如果训练得好，“国王”和“王后”的向量会更接近，而“国王”和“香蕉”的向量会更远。</p><p>这篇文章就从 word2vec 出发，理解词向量是怎么训练出来的、怎么改进和评价词向量，并进一步过渡到神经网络文本分类器。</p><span id="more"></span><p><img src="https://lsworl.github.io/images/nlp-blog/word2vec-overview.svg" alt="从 word2vec 到神经网络分类器"></p><h2 id="1-词向量到底是什么"><a href="#1-词向量到底是什么" class="headerlink" title="1. 词向量到底是什么"></a>1. 词向量到底是什么</h2><p>词向量，也叫 word embedding，本质上就是给每个词分配一个可学习的向量。</p><p>如果词表大小是 $V$，词向量维度是 $d$，那么所有词向量可以组成一个矩阵：</p><p>$$<br>E \in \mathbb{R}^{V \times d}<br>$$</p><p>其中：</p><ul><li>$V$ 是词表大小。</li><li>$d$ 是词向量维度，例如 50、100、300。</li><li>矩阵第 $i$ 行就是第 $i$ 个词的向量。</li></ul><p>如果“喜欢”在词表中的编号是 12，那么：</p><pre class="language-text" data-language="text"><code class="language-text">embedding[12]</code></pre><p>就是“喜欢”这个词的向量。</p><p>在神经网络里，Embedding 层看起来像一层网络，但它本质上更像查表：</p><pre class="language-text" data-language="text"><code class="language-text">词 id -> 去词向量矩阵中取对应行 -> 得到词向量</code></pre><p>例如：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">import</span> torch<span class="token punctuation">.</span>nn <span class="token keyword">as</span> nnembedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>num_embeddings<span class="token operator">=</span><span class="token number">10000</span><span class="token punctuation">,</span> embedding_dim<span class="token operator">=</span><span class="token number">100</span><span class="token punctuation">)</span>word_ids <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">12</span><span class="token punctuation">,</span> <span class="token number">35</span><span class="token punctuation">,</span> <span class="token number">87</span><span class="token punctuation">]</span><span class="token punctuation">)</span>vectors <span class="token operator">=</span> embedding<span class="token punctuation">(</span>word_ids<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>vectors<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>  <span class="token comment"># torch.Size([3, 100])</span></code></pre><p>这里输入 3 个词 id，输出 3 个 100 维词向量。</p><h2 id="2-word2vec-的核心思想"><a href="#2-word2vec-的核心思想" class="headerlink" title="2. word2vec 的核心思想"></a>2. word2vec 的核心思想</h2><p>word2vec 的核心假设来自语言学中的分布式假说：</p><blockquote><p>一个词的含义，可以从它经常出现的上下文中推断出来。</p></blockquote><p>比如：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 自然语言 处理我 喜欢 机器 学习我 喜欢 深度 学习</code></pre><p>“自然语言”“机器”“深度”经常和“学习”“处理”“模型”等词一起出现。<br>随着语料越来越多，模型会逐渐发现：</p><ul><li>经常出现在相似上下文中的词，语义可能相近。</li><li>经常互相搭配的词，向量应该具有较高相似度。</li><li>很少一起出现、上下文差异很大的词，向量应该相对远一些。</li></ul><p>word2vec 并不是靠人工告诉模型“国王”和“王后”相似。<br>它是通过大量文本中的共现关系，把这种相似性训练出来。</p><h2 id="3-训练样本从哪里来"><a href="#3-训练样本从哪里来" class="headerlink" title="3. 训练样本从哪里来"></a>3. 训练样本从哪里来</h2><p>word2vec 不需要人工标注情感类别、主题类别。<br>它可以直接从普通文本中构造训练样本。</p><p>假设有一句话：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 自然语言 处理</code></pre><p>如果窗口大小为 1，那么每个词只看左右各 1 个词。</p><p>对于中心词“喜欢”：</p><pre class="language-text" data-language="text"><code class="language-text">上下文：我，自然语言中心词：喜欢</code></pre><p>对于中心词“自然语言”：</p><pre class="language-text" data-language="text"><code class="language-text">上下文：喜欢，处理中心词：自然语言</code></pre><p>也就是说，word2vec 会用滑动窗口从文本中自动构造很多训练样本。</p><h2 id="4-CBOW-和-Skip-gram"><a href="#4-CBOW-和-Skip-gram" class="headerlink" title="4. CBOW 和 Skip-gram"></a>4. CBOW 和 Skip-gram</h2><p>word2vec 常见有两种训练方式：</p><ul><li>CBOW：Continuous Bag of Words</li><li>Skip-gram</li></ul><p>它们的区别在于预测方向不同。</p><p><img src="https://lsworl.github.io/images/nlp-blog/word2vec-cbow-skipgram.svg" alt="CBOW 与 Skip-gram 对比"></p><h3 id="4-1-CBOW：用上下文预测中心词"><a href="#4-1-CBOW：用上下文预测中心词" class="headerlink" title="4.1 CBOW：用上下文预测中心词"></a>4.1 CBOW：用上下文预测中心词</h3><p>CBOW 的训练目标是：</p><pre class="language-text" data-language="text"><code class="language-text">给定上下文词，预测中心词</code></pre><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">我 ___ 自然语言 处理</code></pre><p>模型看到“我”“自然语言”“处理”，要预测中间缺失的词可能是“喜欢”。</p><p>如果用公式表达，可以写成：</p><p>$$<br>P(w_t \mid w_{t-k}, \cdots, w_{t-1}, w_{t+1}, \cdots, w_{t+k})<br>$$</p><p>其中：</p><ul><li>$w_t$ 是中心词。</li><li>$k$ 是窗口大小。</li><li>周围的词是上下文。</li></ul><p>CBOW 通常训练速度较快，因为它把多个上下文词的信息平均或相加后，用来预测一个中心词。</p><h3 id="4-2-Skip-gram：用中心词预测上下文"><a href="#4-2-Skip-gram：用中心词预测上下文" class="headerlink" title="4.2 Skip-gram：用中心词预测上下文"></a>4.2 Skip-gram：用中心词预测上下文</h3><p>Skip-gram 的训练目标刚好相反：</p><pre class="language-text" data-language="text"><code class="language-text">给定中心词，预测它周围可能出现哪些上下文词</code></pre><p>例如中心词是“喜欢”，模型要预测：</p><pre class="language-text" data-language="text"><code class="language-text">我自然语言处理机器学习</code></pre><p>也就是学习：</p><p>$$<br>P(w_{t+j} \mid w_t),\quad -k \le j \le k,\ j \ne 0<br>$$</p><p>Skip-gram 对低频词通常更友好，因为每个中心词会产生多个训练样本。</p><p>可以简单记：</p><table><thead><tr><th>方法</th><th>输入</th><th>输出</th><th>特点</th></tr></thead><tbody><tr><td>CBOW</td><td>上下文词</td><td>中心词</td><td>训练较快，对高频模式稳定</td></tr><tr><td>Skip-gram</td><td>中心词</td><td>上下文词</td><td>更适合学习低频词表示</td></tr></tbody></table><h2 id="5-一个小例子：CBOW-和-Skip-gram-如何构造样本"><a href="#5-一个小例子：CBOW-和-Skip-gram-如何构造样本" class="headerlink" title="5. 一个小例子：CBOW 和 Skip-gram 如何构造样本"></a>5. 一个小例子：CBOW 和 Skip-gram 如何构造样本</h2><p>句子：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 自然语言 处理</code></pre><p>窗口大小设为 1。</p><p>这表示对于一个中心词，只取它左边 1 个词和右边 1 个词作为上下文。</p><h3 id="5-1-Skip-gram-的样本"><a href="#5-1-Skip-gram-的样本" class="headerlink" title="5.1 Skip-gram 的样本"></a>5.1 Skip-gram 的样本</h3><p>Skip-gram 会构造：</p><table><thead><tr><th>中心词</th><th>上下文词</th></tr></thead><tbody><tr><td>我</td><td>喜欢</td></tr><tr><td>喜欢</td><td>我</td></tr><tr><td>喜欢</td><td>自然语言</td></tr><tr><td>自然语言</td><td>喜欢</td></tr><tr><td>自然语言</td><td>处理</td></tr><tr><td>处理</td><td>自然语言</td></tr></tbody></table><p>如果词表是：</p><pre class="language-text" data-language="text"><code class="language-text">我:0, 喜欢:1, 自然语言:2, 处理:3</code></pre><p>那么训练样本可以写成：</p><pre class="language-text" data-language="text"><code class="language-text">(0, 1)(1, 0)(1, 2)(2, 1)(2, 3)(3, 2)</code></pre><p>模型要做的事情是：</p><pre class="language-text" data-language="text"><code class="language-text">输入中心词 id  -> 查中心词向量  -> 和候选上下文词向量计算相似度  -> 让真实上下文词得分更高</code></pre><p>例如中心词是“喜欢”时，窗口大小为 1，它会产生两个独立的训练样本：</p><pre class="language-text" data-language="text"><code class="language-text">喜欢 -> 我喜欢 -> 自然语言</code></pre><p>也就是说，Skip-gram 会把一个中心词拆成多个“中心词、上下文词”词对。</p><h3 id="5-2-CBOW-的样本"><a href="#5-2-CBOW-的样本" class="headerlink" title="5.2 CBOW 的样本"></a>5.2 CBOW 的样本</h3><p>句子：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 自然语言 处理</code></pre><p>窗口大小设为 1。</p><p>CBOW 使用相同的句子和窗口，但预测方向相反：</p><pre class="language-text" data-language="text"><code class="language-text">输入上下文词 -> 预测中心词</code></pre><p>它会构造下面这些样本：</p><table><thead><tr><th>上下文词</th><th>中心词</th></tr></thead><tbody><tr><td>[喜欢]</td><td>我</td></tr><tr><td>[我, 自然语言]</td><td>喜欢</td></tr><tr><td>[喜欢, 处理]</td><td>自然语言</td></tr><tr><td>[自然语言]</td><td>处理</td></tr></tbody></table><p>使用前面的词表 id</p><pre class="language-text" data-language="text"><code class="language-text">我:0, 喜欢:1, 自然语言:2, 处理:3</code></pre><p>可以写成：</p><pre class="language-text" data-language="text"><code class="language-text">([1], 0)([0, 2], 1)([1, 3], 2)([2], 3)</code></pre><p>以预测“喜欢”为例，CBOW 的一条训练样本是：</p><pre class="language-text" data-language="text"><code class="language-text">输入：[我, 自然语言]目标：喜欢</code></pre><p>假设当前学到的两个上下文词向量为：</p><p>$$<br>v_{我}&#x3D;[1,0],\quad v_{自然语言}&#x3D;[0,2]<br>$$</p><p>CBOW 先对上下文词向量求平均：</p><p>$$<br>h&#x3D;\frac{v_{我}+v_{自然语言}}{2}<br>&#x3D;\frac{[1,0]+[0,2]}{2}<br>&#x3D;[0.5,1]<br>$$</p><p>然后用 $h$ 和输出层的词向量计算得分，再通过 softmax 或负采样训练，让中心词“喜欢”的得分升高。</p><p>整个过程可以写成：</p><pre class="language-text" data-language="text"><code class="language-text">[我, 自然语言]  -> 分别查找词向量  -> 求和或求平均得到上下文表示 h  -> 计算词表中各个候选词的得分  -> 让“喜欢”的得分最高</code></pre><p>这里的 Bag of Words 表示 CBOW 只关心窗口中有哪些词，不特别保留它们的先后顺序。</p><p>在句子边界，“我”和“处理”只有一个上下文词，因此表格中的输入只有一个词。实际实现中也可以通过跳过边界样本或 padding 保持输入长度一致。</p><h3 id="5-3-放在一起对比"><a href="#5-3-放在一起对比" class="headerlink" title="5.3 放在一起对比"></a>5.3 放在一起对比</h3><p>仍然以“喜欢”为中心词：</p><table><thead><tr><th>方法</th><th>输入</th><th>预测目标</th><th align="right">样本数量</th></tr></thead><tbody><tr><td>CBOW</td><td>[我, 自然语言]</td><td>喜欢</td><td align="right">1</td></tr><tr><td>Skip-gram</td><td>喜欢</td><td>我</td><td align="right">1</td></tr><tr><td>Skip-gram</td><td>喜欢</td><td>自然语言</td><td align="right">1</td></tr></tbody></table><p>所以两者最直观的区别是：</p><blockquote><p>CBOW 把多个上下文词合成一个表示，预测一个中心词；Skip-gram 从一个中心词出发，分别预测多个上下文词。</p></blockquote><h2 id="6-word2vec-的神经网络结构"><a href="#6-word2vec-的神经网络结构" class="headerlink" title="6. word2vec 的神经网络结构"></a>6. word2vec 的神经网络结构</h2><p>以 Skip-gram 为例，一个简化结构如下：</p><pre class="language-text" data-language="text"><code class="language-text">中心词 one-hot  -> 输入词向量矩阵 W_in  -> 得到中心词向量 v  -> 输出词向量矩阵 W_out  -> 预测上下文词概率</code></pre><p>如果用 One-hot 输入，假设中心词编号是 1：</p><pre class="language-text" data-language="text"><code class="language-text">[0, 1, 0, 0]</code></pre><p>乘上词向量矩阵：</p><p>$$<br>W_{in} \in \mathbb{R}^{V \times d}<br>$$</p><p>得到的其实就是第 1 行词向量。</p><p>所以从实现角度看：</p><blockquote><p>Embedding 查表 等价于 One-hot 向量乘词向量矩阵，但查表更高效。</p></blockquote><p>训练过程中，模型会不断调整 $W_{in}$ 和 $W_{out}$。<br>训练完成后，通常会取 $W_{in}$ 作为最终词向量。</p><h2 id="7-为什么不能直接对整个词表做-softmax"><a href="#7-为什么不能直接对整个词表做-softmax" class="headerlink" title="7. 为什么不能直接对整个词表做 softmax"></a>7. 为什么不能直接对整个词表做 softmax</h2><p>如果词表很小，模型可以直接预测整个词表的概率：</p><p>$$<br>P(w_o \mid w_i)&#x3D;<br>\frac{\exp(v_{w_o}^{\top}v_{w_i})}<br>{\sum_{w&#x3D;1}^{V}\exp(v_w^{\top}v_{w_i})}<br>$$</p><p>问题是，真实 NLP 任务中的词表可能非常大。<br>如果词表有 100000 个词，每个训练样本都要对 100000 个词计算 softmax，成本会很高。</p><p>因此 word2vec 常用一些近似训练方法来加速，比如：</p><ul><li>Hierarchical Softmax</li><li>Negative Sampling</li></ul><p>其中负采样最常见，也最容易理解。</p><h2 id="8-负采样：让真实词对更像，随机词对更不像"><a href="#8-负采样：让真实词对更像，随机词对更不像" class="headerlink" title="8. 负采样：让真实词对更像，随机词对更不像"></a>8. 负采样：让真实词对更像，随机词对更不像</h2><p>负采样的思路是：</p><blockquote><p>不再让模型从整个词表中选正确答案，而是让模型判断“中心词和上下文词是否真的一起出现过”。</p></blockquote><p>例如真实语料中出现过：</p><pre class="language-text" data-language="text"><code class="language-text">喜欢 -> 自然语言</code></pre><p>这就是正样本，标签为 1。</p><p>然后随机采几个词作为负样本：</p><pre class="language-text" data-language="text"><code class="language-text">喜欢 -> 冰箱喜欢 -> 火车喜欢 -> 香蕉</code></pre><p>这些大概率不是当前中心词的真实上下文，标签为 0。</p><p><img src="https://lsworl.github.io/images/nlp-blog/word2vec-negative-sampling.svg" alt="负采样训练 word2vec"></p><p>模型计算中心词向量和上下文词向量的点积：</p><p>$$<br>score &#x3D; v_c^\top v_o<br>$$</p><p>再通过 Sigmoid 得到概率：</p><p>$$<br>\sigma(score)&#x3D;\frac{1}{1+e^{-score}}<br>$$</p><p>训练目标是：</p><ul><li>正样本的 $\sigma(score)$ 越接近 1 越好。</li><li>负样本的 $\sigma(score)$ 越接近 0 越好。</li></ul><p>这会推动：</p><ul><li>经常共现的词向量更接近。</li><li>随机无关的词向量更远。</li></ul><h2 id="9-手工理解一次负采样更新"><a href="#9-手工理解一次负采样更新" class="headerlink" title="9. 手工理解一次负采样更新"></a>9. 手工理解一次负采样更新</h2><p>假设中心词是“喜欢”，真实上下文是“学习”。</p><p>它们当前向量点积为：</p><p>$$<br>v_{\text{喜欢}}^\top v_{\text{学习}} &#x3D; 0.2<br>$$</p><p>经过 Sigmoid：</p><p>$$<br>\sigma(0.2) \approx 0.55<br>$$</p><p>对于正样本来说，目标是 1，当前只有 0.55，说明模型还不够确信这两个词应该共现。<br>训练会把它们的向量往更相似的方向推。</p><p>再看一个负样本：</p><pre class="language-text" data-language="text"><code class="language-text">喜欢 -> 冰箱</code></pre><p>假设点积也是 0.2，Sigmoid 也是 0.55。<br>但负样本的目标是 0，模型现在却给了 0.55，说明它错误地认为“喜欢”和“冰箱”比较相关。<br>训练会把这两个词的向量往更不相似的方向推。</p><p>所以负采样不是直接告诉模型“每个词是什么意思”，而是通过大量这样的正负样本，让向量空间慢慢变得有结构。</p><h2 id="10-高频词和低频词的问题"><a href="#10-高频词和低频词的问题" class="headerlink" title="10. 高频词和低频词的问题"></a>10. 高频词和低频词的问题</h2><p>真实语料中有很多高频词，比如：</p><pre class="language-text" data-language="text"><code class="language-text">的、了、是、在、the、of、and</code></pre><p>这些词出现次数很多，但语义区分度不一定强。<br>如果不处理，它们会在训练样本中占据太大比例，影响词向量质量。</p><p>word2vec 常见改进包括：</p><h3 id="10-1-高频词下采样"><a href="#10-1-高频词下采样" class="headerlink" title="10.1 高频词下采样"></a>10.1 高频词下采样</h3><p>对于特别高频的词，可以按一定概率丢弃一部分训练样本。</p><p>直觉是：</p><blockquote><p>“的”少看几次，模型仍然知道它很常见；但给低频词更多机会，可能更有价值。</p></blockquote><p>这样可以减少高频虚词对训练的干扰，也能提升训练速度。</p><h3 id="10-2-负样本分布调整"><a href="#10-2-负样本分布调整" class="headerlink" title="10.2 负样本分布调整"></a>10.2 负样本分布调整</h3><p>负样本不是完全均匀随机采样。<br>常见做法是按照词频的 $3&#x2F;4$ 次方构造采样分布：</p><p>$$<br>P(w)&#x3D;\frac{f(w)^{3&#x2F;4}}{\sum_i f(w_i)^{3&#x2F;4}}<br>$$</p><p>这样既会更常抽到高频词，又不会让最高频词压倒一切。</p><h3 id="10-3-调整窗口大小"><a href="#10-3-调整窗口大小" class="headerlink" title="10.3 调整窗口大小"></a>10.3 调整窗口大小</h3><p>窗口大小会影响词向量捕捉的信息：</p><table><thead><tr><th>窗口大小</th><th>更容易学到</th></tr></thead><tbody><tr><td>小窗口</td><td>语法关系、局部搭配</td></tr><tr><td>大窗口</td><td>主题关系、语义相关</td></tr></tbody></table><p>例如小窗口可能让“吃”和“苹果”靠近，大窗口可能让“电影”“导演”“演员”靠近。</p><h3 id="10-4-增加语料和清洗质量"><a href="#10-4-增加语料和清洗质量" class="headerlink" title="10.4 增加语料和清洗质量"></a>10.4 增加语料和清洗质量</h3><p>词向量非常依赖语料。</p><p>如果语料太少，模型学不到稳定的共现关系。<br>如果语料领域不匹配，词向量也会偏向错误语义。</p><p>例如：</p><ul><li>医学语料训练出的“阳性”“阴性”和普通语料不同。</li><li>编程语料中的“类”“对象”“继承”有特定含义。</li><li>电商评论中的“苹果”可能更多指手机品牌，而不是水果。</li></ul><p>所以训练词向量时要注意语料领域。</p><h2 id="11-怎么评价词向量"><a href="#11-怎么评价词向量" class="headerlink" title="11. 怎么评价词向量"></a>11. 怎么评价词向量</h2><p>词向量训练好以后，不能只看训练 loss。<br>更重要的是看它是否真的学到了语义关系，以及能不能帮助下游任务。</p><p><img src="https://lsworl.github.io/images/nlp-blog/word2vec-evaluation.svg" alt="词向量评价方法"></p><h3 id="11-1-最近邻词"><a href="#11-1-最近邻词" class="headerlink" title="11.1 最近邻词"></a>11.1 最近邻词</h3><p>最简单的方法是找某个词最相似的词。</p><p>常用余弦相似度：</p><p>$$<br>\cos(a,b)&#x3D;\frac{a^\top b}{|a||b|}<br>$$</p><p>如果输入“国王”，比较好的结果可能是：</p><pre class="language-text" data-language="text"><code class="language-text">王后、王子、皇帝、君主</code></pre><p>如果出现大量无关词，说明词向量质量可能不够好。</p><h3 id="11-2-词类比"><a href="#11-2-词类比" class="headerlink" title="11.2 词类比"></a>11.2 词类比</h3><p>word2vec 最经典的现象之一是词向量可以表达某些方向关系。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">国王 - 男人 + 女人 ≈ 王后</code></pre><p>或者：</p><pre class="language-text" data-language="text"><code class="language-text">巴黎 - 法国 + 日本 ≈ 东京</code></pre><p>这说明向量空间中不只是“相似词靠近”，还可能存在某些可迁移的语义方向。</p><p>但要注意，这类例子不能神化。<br>它能说明词向量有一定结构，但不代表模型真正理解了人类意义上的知识。</p><h3 id="11-3-可视化"><a href="#11-3-可视化" class="headerlink" title="11.3 可视化"></a>11.3 可视化</h3><p>词向量通常是 100 维、300 维，无法直接画出来。<br>可以用 PCA、t-SNE 或 UMAP 降到二维，再观察相近词是否聚在一起。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">猫、狗、兔子北京、上海、广州篮球、足球、网球</code></pre><p>如果同类词能形成相对清晰的簇，说明向量空间有一定语义结构。</p><h3 id="11-4-下游任务评价"><a href="#11-4-下游任务评价" class="headerlink" title="11.4 下游任务评价"></a>11.4 下游任务评价</h3><p>最终最可靠的评价还是下游任务。</p><p>例如把词向量用于：</p><ul><li>文本情感分类。</li><li>新闻主题分类。</li><li>搜索召回。</li><li>推荐系统。</li><li>命名实体识别。</li></ul><p>如果使用训练好的词向量后，验证集准确率、F1、召回率等指标提升了，说明这些词向量对任务有帮助。</p><h2 id="12-word2vec-的局限"><a href="#12-word2vec-的局限" class="headerlink" title="12. word2vec 的局限"></a>12. word2vec 的局限</h2><p>word2vec 很重要，但它也有局限。</p><h3 id="12-1-一词多义问题"><a href="#12-1-一词多义问题" class="headerlink" title="12.1 一词多义问题"></a>12.1 一词多义问题</h3><p>word2vec 通常给每个词一个固定向量。</p><p>例如“苹果”：</p><pre class="language-text" data-language="text"><code class="language-text">我吃了一个苹果我买了一台苹果手机</code></pre><p>这两个句子里的“苹果”含义不同，但 word2vec 里通常只有一个“苹果”向量。</p><p>这会导致多义词表示不够精确。<br>后来 BERT、GPT 这类上下文相关模型会根据句子动态生成词表示，从而缓解这个问题。</p><h3 id="12-2-无法直接处理未登录词"><a href="#12-2-无法直接处理未登录词" class="headerlink" title="12.2 无法直接处理未登录词"></a>12.2 无法直接处理未登录词</h3><p>如果训练词表里没有某个词，word2vec 就很难直接给它一个好向量。</p><p>例如训练时没见过：</p><pre class="language-text" data-language="text"><code class="language-text">超导量子芯片</code></pre><p>模型就无法直接查到它的词向量。</p><p>后来 fastText 通过子词 n-gram 改进了这一点。<br>它不仅学习整个词的向量，也学习词内部片段的向量，因此对未登录词更友好。</p><h3 id="12-3-只利用局部窗口"><a href="#12-3-只利用局部窗口" class="headerlink" title="12.3 只利用局部窗口"></a>12.3 只利用局部窗口</h3><p>word2vec 主要基于局部上下文窗口训练。<br>它能学到很多共现关系，但对长距离依赖、句法结构、篇章关系的建模能力有限。</p><p>这也是后来 RNN、CNN、Transformer 在 NLP 中继续发展的原因。</p><h2 id="13-从词向量过渡到神经网络分类器"><a href="#13-从词向量过渡到神经网络分类器" class="headerlink" title="13. 从词向量过渡到神经网络分类器"></a>13. 从词向量过渡到神经网络分类器</h2><p>训练好词向量以后，下一步通常是把它用于下游任务。</p><p>以情感分类为例：</p><pre class="language-text" data-language="text"><code class="language-text">输入：这部电影很好看输出：正面</code></pre><p>模型流程可以是：</p><p><img src="https://lsworl.github.io/images/nlp-blog/word2vec-classifier.svg" alt="词向量到神经网络分类器"></p><p>具体步骤：</p><ol><li>分词。</li><li>把每个词转成词 id。</li><li>用 Embedding 层把词 id 转成词向量。</li><li>把多个词向量合成一个句子向量。</li><li>用全连接层输出类别 logits。</li><li>使用交叉熵损失训练分类器。</li></ol><p>一个最简单的句子分类器可以写成：</p><pre class="language-text" data-language="text"><code class="language-text">句子 token ids  -> Embedding  -> Mean Pooling  -> Linear  -> 分类 logits</code></pre><h2 id="14-PyTorch-实现一个简单文本分类器"><a href="#14-PyTorch-实现一个简单文本分类器" class="headerlink" title="14. PyTorch 实现一个简单文本分类器"></a>14. PyTorch 实现一个简单文本分类器</h2><p>下面是一个极简版本，重点看结构。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">import</span> torch<span class="token punctuation">.</span>nn <span class="token keyword">as</span> nn<span class="token keyword">class</span> <span class="token class-name">TextClassifier</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> vocab_size<span class="token punctuation">,</span> embed_dim<span class="token punctuation">,</span> num_classes<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>vocab_size<span class="token punctuation">,</span> embed_dim<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>classifier <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>embed_dim<span class="token punctuation">,</span> num_classes<span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> input_ids<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token comment"># input_ids: [batch_size, seq_len]</span>        x <span class="token operator">=</span> self<span class="token punctuation">.</span>embedding<span class="token punctuation">(</span>input_ids<span class="token punctuation">)</span>        <span class="token comment"># x: [batch_size, seq_len, embed_dim]</span>        sentence_vec <span class="token operator">=</span> x<span class="token punctuation">.</span>mean<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>        <span class="token comment"># sentence_vec: [batch_size, embed_dim]</span>        logits <span class="token operator">=</span> self<span class="token punctuation">.</span>classifier<span class="token punctuation">(</span>sentence_vec<span class="token punctuation">)</span>        <span class="token comment"># logits: [batch_size, num_classes]</span>        <span class="token keyword">return</span> logits</code></pre><p>假设：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> TextClassifier<span class="token punctuation">(</span>    vocab_size<span class="token operator">=</span><span class="token number">10000</span><span class="token punctuation">,</span>    embed_dim<span class="token operator">=</span><span class="token number">100</span><span class="token punctuation">,</span>    num_classes<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">)</span>input_ids <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">12</span><span class="token punctuation">,</span> <span class="token number">35</span><span class="token punctuation">,</span> <span class="token number">87</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">44</span><span class="token punctuation">,</span> <span class="token number">19</span><span class="token punctuation">,</span> <span class="token number">63</span><span class="token punctuation">,</span> <span class="token number">91</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">)</span>logits <span class="token operator">=</span> model<span class="token punctuation">(</span>input_ids<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>logits<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>  <span class="token comment"># torch.Size([2, 2])</span></code></pre><p>这里：</p><ul><li><code>batch_size = 2</code></li><li><code>seq_len = 5</code></li><li><code>embed_dim = 100</code></li><li><code>num_classes = 2</code></li></ul><p>输出 <code>[2, 2]</code> 表示每个句子对应两个类别的 logits。</p><h2 id="15-如何使用预训练-word2vec-初始化-Embedding"><a href="#15-如何使用预训练-word2vec-初始化-Embedding" class="headerlink" title="15. 如何使用预训练 word2vec 初始化 Embedding"></a>15. 如何使用预训练 word2vec 初始化 Embedding</h2><p>如果已经有训练好的 word2vec 向量，可以用它初始化 <code>nn.Embedding</code>。</p><p>假设预训练矩阵为：</p><pre class="language-python" data-language="python"><code class="language-python">pretrained_weight<span class="token punctuation">.</span>shape  <span class="token comment"># [vocab_size, embed_dim]</span></code></pre><p>可以这样写：</p><pre class="language-python" data-language="python"><code class="language-python">embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">.</span>from_pretrained<span class="token punctuation">(</span>    embeddings<span class="token operator">=</span>pretrained_weight<span class="token punctuation">,</span>    freeze<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">)</span></code></pre><p>其中：</p><ul><li><code>freeze=True</code>：训练分类器时固定词向量，不更新。</li><li><code>freeze=False</code>：训练分类器时允许词向量继续微调。</li></ul><p>如果数据量较小，可以先固定词向量，避免过拟合。<br>如果下游任务数据较多，通常可以允许词向量微调，让它更适合当前任务。</p><h2 id="16-为什么需要-padding-和-mask"><a href="#16-为什么需要-padding-和-mask" class="headerlink" title="16. 为什么需要 padding 和 mask"></a>16. 为什么需要 padding 和 mask</h2><p>真实句子的长度通常不一样：</p><pre class="language-text" data-language="text"><code class="language-text">很好看这部电影真的非常好看</code></pre><p>为了组成 batch，通常会 padding 到相同长度：</p><pre class="language-text" data-language="text"><code class="language-text">[很好看, PAD, PAD, PAD][这部, 电影, 真的, 好看]</code></pre><p>如果直接对所有位置做平均，PAD 的向量也会被算进去，影响句子表示。</p><p>所以更严谨的 mean pooling 应该排除 PAD：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">TextClassifier</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> vocab_size<span class="token punctuation">,</span> embed_dim<span class="token punctuation">,</span> num_classes<span class="token punctuation">,</span> pad_id<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>embedding <span class="token operator">=</span> nn<span class="token punctuation">.</span>Embedding<span class="token punctuation">(</span>vocab_size<span class="token punctuation">,</span> embed_dim<span class="token punctuation">,</span> padding_idx<span class="token operator">=</span>pad_id<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>classifier <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>embed_dim<span class="token punctuation">,</span> num_classes<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>pad_id <span class="token operator">=</span> pad_id    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> input_ids<span class="token punctuation">)</span><span class="token punctuation">:</span>        x <span class="token operator">=</span> self<span class="token punctuation">.</span>embedding<span class="token punctuation">(</span>input_ids<span class="token punctuation">)</span>        <span class="token comment"># x: [batch_size, seq_len, embed_dim]</span>        mask <span class="token operator">=</span> <span class="token punctuation">(</span>input_ids <span class="token operator">!=</span> self<span class="token punctuation">.</span>pad_id<span class="token punctuation">)</span><span class="token punctuation">.</span>unsqueeze<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>        <span class="token comment"># mask: [batch_size, seq_len, 1]</span>        x <span class="token operator">=</span> x <span class="token operator">*</span> mask        lengths <span class="token operator">=</span> mask<span class="token punctuation">.</span><span class="token builtin">sum</span><span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">.</span>clamp<span class="token punctuation">(</span><span class="token builtin">min</span><span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>        sentence_vec <span class="token operator">=</span> x<span class="token punctuation">.</span><span class="token builtin">sum</span><span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span> <span class="token operator">/</span> lengths        logits <span class="token operator">=</span> self<span class="token punctuation">.</span>classifier<span class="token punctuation">(</span>sentence_vec<span class="token punctuation">)</span>        <span class="token keyword">return</span> logits</code></pre><p>这里的 <code>padding_idx=pad_id</code> 会让 PAD 对应的 embedding 不参与更新。<br><code>mask</code> 则保证池化时不把 PAD 算进有效词。</p><h2 id="17-word2vec-和现代-Embedding-的关系"><a href="#17-word2vec-和现代-Embedding-的关系" class="headerlink" title="17. word2vec 和现代 Embedding 的关系"></a>17. word2vec 和现代 Embedding 的关系</h2><p>word2vec 是理解词向量非常好的入口。</p><p>它让我们明白：</p><ul><li>词向量不是人工规定的，而是训练出来的。</li><li>训练目标可以来自文本自身，不一定需要人工标签。</li><li>向量空间可以编码相似性、共现关系和某些语义方向。</li><li>Embedding 层本质上是可学习的查表矩阵。</li></ul><p>后来的很多模型仍然保留了这个核心思想：</p><pre class="language-text" data-language="text"><code class="language-text">离散 token -> 连续向量 -> 神经网络处理</code></pre><p>区别在于：</p><ul><li>word2vec 给每个词一个固定向量。</li><li>RNN&#x2F;CNN 会在词向量之上继续建模句子结构。</li><li>Transformer 会根据上下文动态生成 token 表示。</li><li>BERT&#x2F;GPT 里的 embedding 不只是词向量，还包含位置、上下文和深层语义信息。</li></ul><p>所以学 word2vec 并不是过时，而是在理解现代 NLP 模型的地基。</p><h2 id="18-总结"><a href="#18-总结" class="headerlink" title="18. 总结"></a>18. 总结</h2><p>word2vec 可以用一句话概括：</p><blockquote><p>通过预测上下文或中心词，让经常出现在相似上下文中的词拥有相似的向量表示。</p></blockquote><p>理解 word2vec 后，再看文本分类、RNN、CNN、Transformer，会更容易抓住核心：</p><pre class="language-text" data-language="text"><code class="language-text">先把离散词变成连续向量，再让神经网络在这些向量上学习任务需要的表示。</code></pre><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>Tomas Mikolov 等，Efficient Estimation of Word Representations in Vector Space。</li><li>Tomas Mikolov 等，Distributed Representations of Words and Phrases and their Compositionality。</li><li>Yoav Goldberg, Omer Levy，word2vec Explained: deriving Mikolov et al.’s negative-sampling word-embedding method。</li></ul>]]>
    </content>
    <id>https://lsworl.github.io/2026/08/03/nlp-word2vec-embedding-classifier/</id>
    <link href="https://lsworl.github.io/2026/08/03/nlp-word2vec-embedding-classifier/"/>
    <published>2026-08-02T16:00:00.000Z</published>
    <summary>从 word2vec 出发，理解词向量是怎么训练出来的、怎么改进和评价词向量，并进一步过渡到基于词向量的神经网络分类器。</summary>
    <title>Word2Vec：从词向量训练到神经网络分类器</title>
    <updated>2026-08-04T15:38:10.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="机器学习" scheme="https://lsworl.github.io/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="机器学习" scheme="https://lsworl.github.io/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="深度学习" scheme="https://lsworl.github.io/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    <category term="神经网络" scheme="https://lsworl.github.io/tags/%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <content>
      <![CDATA[<p>学习深度学习时，很多人一上来就会被一堆名词淹没：神经网络、反向传播、CNN、RNN、Transformer、Embedding、优化器、损失函数、过拟合、预训练、微调……</p><p>这些概念当然重要，但如果一开始没有一张地图，就很容易出现一种感觉：每个词好像都看过，但不知道它们之间到底是什么关系。</p><p>所以这篇文章不急着推公式，也不急着写复杂代码，而是先回答几个更基础的问题：</p><ul><li>深度学习到底在学什么？</li><li>神经网络为什么可以拟合复杂关系？</li><li>一个模型是如何被训练出来的？</li><li>入门之后应该按什么顺序继续学？</li></ul><p>一句话概括：</p><blockquote><p>深度学习就是用多层神经网络从数据中自动学习特征表示，再用这些表示完成分类、回归、生成、推荐、预测等任务。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/ml-blog/deep-learning-map.svg" alt="深度学习知识地图"></p><h2 id="1-深度学习是什么"><a href="#1-深度学习是什么" class="headerlink" title="1. 深度学习是什么"></a>1. 深度学习是什么</h2><p>在传统机器学习中，我们经常需要手动设计特征。</p><p>比如要判断一封邮件是不是垃圾邮件，可能会人工提取这些特征：</p><ul><li>是否包含“免费”“中奖”“限时”等关键词。</li><li>邮件长度是多少。</li><li>是否包含很多链接。</li><li>发件人是否陌生。</li></ul><p>这些特征再送入逻辑回归、决策树、随机森林、GBDT 等模型中进行预测。</p><p>深度学习的思路稍微不一样。它希望模型自己从原始数据中逐层学习特征。</p><p>以图片分类为例：</p><ul><li>浅层可能学到边缘、颜色、纹理。</li><li>中间层可能学到局部形状，比如眼睛、轮子、耳朵。</li><li>深层可能学到更抽象的概念，比如人脸、车辆、动物。</li></ul><p>这就是“深度”的含义：不是说模型一定神秘，而是说它有很多层，每一层都在对上一层的表示继续加工。</p><h2 id="2-深度学习和机器学习的关系"><a href="#2-深度学习和机器学习的关系" class="headerlink" title="2. 深度学习和机器学习的关系"></a>2. 深度学习和机器学习的关系</h2><p>可以把它们的关系理解成：</p><pre class="language-text" data-language="text"><code class="language-text">人工智能  └── 机器学习        └── 深度学习</code></pre><p>机器学习强调从数据中学习规律。深度学习是机器学习中的一个分支，它主要使用多层神经网络来学习复杂规律。</p><p>传统机器学习并没有过时。对于表格数据、样本量不大、可解释性要求高的任务，逻辑回归、决策树、随机森林、GBDT 依然非常常用。</p><p>深度学习更擅长处理这些数据：</p><ul><li>图像：图片分类、目标检测、图像分割。</li><li>文本：文本分类、机器翻译、问答系统。</li><li>语音：语音识别、语音合成。</li><li>图结构：社交网络、分子结构、推荐系统。</li><li>大规模复杂数据：多模态模型、大语言模型、生成模型。</li></ul><p>所以可以先记住一个判断标准：</p><blockquote><p>如果任务需要模型从复杂原始数据中自动提取高层特征，深度学习通常更有优势。</p></blockquote><h2 id="3-神经网络的最小单位：神经元"><a href="#3-神经网络的最小单位：神经元" class="headerlink" title="3. 神经网络的最小单位：神经元"></a>3. 神经网络的最小单位：神经元</h2><p>神经网络听起来很复杂，但最基本的计算单元其实很简单。</p><p>一个神经元通常做两件事：</p><ol><li>对输入做加权求和。</li><li>把结果送入激活函数。</li></ol><p>公式可以写成：</p><p>$$<br>z &#x3D; w_1x_1+w_2x_2+\cdots+w_nx_n+b<br>$$</p><p>$$<br>a &#x3D; \sigma(z)<br>$$</p><p>其中：</p><ul><li>$x_1,x_2,\cdots,x_n$ 是输入特征。</li><li>$w_1,w_2,\cdots,w_n$ 是权重。</li><li>$b$ 是偏置。</li><li>$\sigma$ 是激活函数。</li><li>$a$ 是这个神经元的输出。</li></ul><p><img src="https://lsworl.github.io/images/ml-blog/deep-learning-neuron.svg" alt="单个神经元的计算过程"></p><p>如果把很多神经元排成一层，就得到一个神经网络层。<br>如果把很多层堆起来，就得到一个多层神经网络。</p><h2 id="4-为什么要有激活函数"><a href="#4-为什么要有激活函数" class="headerlink" title="4. 为什么要有激活函数"></a>4. 为什么要有激活函数</h2><p>如果没有激活函数，神经网络再深，本质上仍然只是线性变换的叠加。</p><p>例如两层线性变换：</p><p>$$<br>h &#x3D; xW_1+b_1<br>$$</p><p>$$<br>y &#x3D; hW_2+b_2<br>$$</p><p>代入以后：</p><p>$$<br>y &#x3D; (xW_1+b_1)W_2+b_2<br>$$</p><p>整理之后仍然是：</p><p>$$<br>y &#x3D; xW+b<br>$$</p><p>也就是说，多层线性层叠在一起，仍然等价于一层线性层。</p><p>激活函数的作用就是引入非线性，让神经网络能够拟合更复杂的关系。</p><p>常见激活函数包括：</p><table><thead><tr><th>激活函数</th><th>公式或特点</th><th>常见用途</th></tr></thead><tbody><tr><td>Sigmoid</td><td>输出在 0 到 1 之间</td><td>二分类输出层、概率解释</td></tr><tr><td>Tanh</td><td>输出在 -1 到 1 之间</td><td>早期 RNN 中常见</td></tr><tr><td>ReLU</td><td>$\max(0,x)$</td><td>最常用的隐藏层激活函数之一</td></tr><tr><td>Leaky ReLU</td><td>负数区域保留一个小斜率</td><td>缓解 ReLU 神经元死亡问题</td></tr><tr><td>GELU</td><td>更平滑的非线性函数</td><td>Transformer 中常见</td></tr></tbody></table><p>入门阶段最重要的是先理解 ReLU：</p><p>$$<br>\text{ReLU}(x)&#x3D;\max(0,x)<br>$$</p><p>它做的事情非常朴素：小于 0 的值变成 0，大于 0 的值保持不变。<br>但就是这样一个简单函数，让网络获得了表达非线性边界的能力。</p><h2 id="5-神经网络如何完成预测"><a href="#5-神经网络如何完成预测" class="headerlink" title="5. 神经网络如何完成预测"></a>5. 神经网络如何完成预测</h2><p>假设我们要判断一张图片是猫还是狗。</p><p>模型的预测流程大致是：</p><pre class="language-text" data-language="text"><code class="language-text">图片像素  -> 神经网络第 1 层  -> 神经网络第 2 层  -> ...  -> 最后一层输出分类分数  -> 选择分数最高的类别</code></pre><p>如果最后输出两个数：</p><pre class="language-text" data-language="text"><code class="language-text">猫：2.8狗：0.7</code></pre><p>那么模型更倾向于认为这张图片是猫。</p><p>这里的 2.8 和 0.7 通常称为 logits，它们还不是严格意义上的概率。<br>如果想变成概率，可以使用 softmax：</p><p>$$<br>p_i &#x3D; \frac{e^{z_i}}{\sum_j e^{z_j}}<br>$$</p><p>softmax 会把多个分数转换成总和为 1 的概率分布。</p><h2 id="6-模型是怎么“学会”的"><a href="#6-模型是怎么“学会”的" class="headerlink" title="6. 模型是怎么“学会”的"></a>6. 模型是怎么“学会”的</h2><p>深度学习模型一开始并不会做任务。</p><p>它的参数通常是随机初始化的，所以一开始预测往往很差。<br>训练的目标就是不断调整这些参数，让模型预测越来越接近真实标签。</p><p>训练过程可以概括成 5 步：</p><ol><li>把一批数据输入模型。</li><li>模型前向传播，得到预测结果。</li><li>用损失函数衡量预测和真实标签之间的差距。</li><li>反向传播计算每个参数对损失的影响。</li><li>优化器根据梯度更新参数。</li></ol><p><img src="https://lsworl.github.io/images/ml-blog/deep-learning-training-loop.svg" alt="深度学习训练闭环"></p><p>如果用 PyTorch 写，核心训练循环通常长这样：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">for</span> x<span class="token punctuation">,</span> y <span class="token keyword">in</span> dataloader<span class="token punctuation">:</span>    pred <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">)</span>              <span class="token comment"># 1. 前向传播</span>    loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>pred<span class="token punctuation">,</span> y<span class="token punctuation">)</span>      <span class="token comment"># 2. 计算损失</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>        <span class="token comment"># 3. 清空上一轮梯度</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>              <span class="token comment"># 4. 反向传播</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>             <span class="token comment"># 5. 更新参数</span></code></pre><p>这几行代码几乎就是深度学习训练的骨架。</p><h2 id="7-损失函数是什么"><a href="#7-损失函数是什么" class="headerlink" title="7. 损失函数是什么"></a>7. 损失函数是什么</h2><p>损失函数用来衡量“模型错得有多离谱”。</p><p>不同任务会使用不同损失函数。</p><h3 id="回归任务"><a href="#回归任务" class="headerlink" title="回归任务"></a>回归任务</h3><p>如果要预测房价、温度、销量这类连续数值，常用均方误差：</p><p>$$<br>L &#x3D; \frac{1}{n}\sum_{i&#x3D;1}^{n}(\hat{y}_i-y_i)^2<br>$$</p><p>预测值和真实值差得越远，损失越大。</p><h3 id="分类任务"><a href="#分类任务" class="headerlink" title="分类任务"></a>分类任务</h3><p>如果要做图片分类、文本分类这类任务，常用交叉熵损失。</p><p>直观理解：</p><blockquote><p>如果真实类别是猫，但模型却给狗很高概率，那么损失就会很大。</p></blockquote><p>在 PyTorch 中，多分类通常使用：</p><pre class="language-python" data-language="python"><code class="language-python">loss_fn <span class="token operator">=</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>二分类也常见：</p><pre class="language-python" data-language="python"><code class="language-python">loss_fn <span class="token operator">=</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>BCEWithLogitsLoss<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>入门时不必急着死记所有损失函数，只要先抓住核心：</p><blockquote><p>损失函数给模型一个明确的优化目标，告诉模型现在的预测离正确答案还有多远。</p></blockquote><h2 id="8-反向传播和梯度下降"><a href="#8-反向传播和梯度下降" class="headerlink" title="8. 反向传播和梯度下降"></a>8. 反向传播和梯度下降</h2><p>损失函数告诉我们“错了多少”，但还需要知道“参数应该怎么改”。</p><p>这就需要反向传播。</p><p>反向传播会根据链式法则，从损失函数一路往前计算每个参数的梯度。</p><p>梯度可以理解为：</p><blockquote><p>如果这个参数稍微变大一点，损失会变大还是变小，变化速度有多快。</p></blockquote><p>有了梯度以后，优化器就可以更新参数。</p><p>最基础的梯度下降更新公式是：</p><p>$$<br>W &#x3D; W - \eta \frac{\partial L}{\partial W}<br>$$</p><p>其中：</p><ul><li>$W$ 是参数。</li><li>$L$ 是损失。</li><li>$\eta$ 是学习率。</li><li>$\frac{\partial L}{\partial W}$ 是参数的梯度。</li></ul><p>如果梯度为正，说明参数增大会让损失增大，所以要把参数减小。<br>如果梯度为负，说明参数增大会让损失减小，所以要把参数增大。</p><p>这就是“沿着让损失下降的方向走”。</p><h2 id="9-一个极简例子：模型如何学习一条直线"><a href="#9-一个极简例子：模型如何学习一条直线" class="headerlink" title="9. 一个极简例子：模型如何学习一条直线"></a>9. 一个极简例子：模型如何学习一条直线</h2><p>假设真实关系是：</p><p>$$<br>y &#x3D; 2x + 1<br>$$</p><p>但模型一开始不知道这个关系，只知道自己可以写成：</p><p>$$<br>\hat{y}&#x3D;wx+b<br>$$</p><p>刚开始可能随机得到：</p><p>$$<br>w&#x3D;0.3,\quad b&#x3D;-0.2<br>$$</p><p>当输入 $x&#x3D;3$ 时：</p><p>$$<br>y &#x3D; 2 \times 3 + 1 &#x3D; 7<br>$$</p><p>模型预测：</p><p>$$<br>\hat{y}&#x3D;0.3 \times 3 - 0.2 &#x3D; 0.7<br>$$</p><p>误差非常大。</p><p>训练会不断调整 $w$ 和 $b$，让预测值靠近真实值。<br>经过很多轮更新后，模型可能学到：</p><p>$$<br>w \approx 2,\quad b \approx 1<br>$$</p><p>这时模型就“学会”了这条直线。</p><p>更复杂的神经网络也是同样的思想，只不过参数更多、层数更深、函数形状更复杂。</p><h2 id="10-深度学习常见模型"><a href="#10-深度学习常见模型" class="headerlink" title="10. 深度学习常见模型"></a>10. 深度学习常见模型</h2><p>入门时可以先知道每类模型大致解决什么问题，不必一开始就钻到所有细节里。</p><table><thead><tr><th>模型</th><th>核心思想</th><th>常见任务</th></tr></thead><tbody><tr><td>MLP</td><td>全连接层堆叠</td><td>表格数据、基础分类回归</td></tr><tr><td>CNN</td><td>利用局部感受野和卷积核提取空间特征</td><td>图像分类、目标检测</td></tr><tr><td>RNN</td><td>按时间顺序处理序列</td><td>时间序列、早期文本任务</td></tr><tr><td>LSTM&#x2F;GRU</td><td>改进 RNN 的长期依赖问题</td><td>序列预测、语音、文本</td></tr><tr><td>Transformer</td><td>用注意力机制建模全局依赖</td><td>NLP、大模型、多模态</td></tr><tr><td>AutoEncoder</td><td>学习压缩表示并重建输入</td><td>降维、异常检测、生成模型基础</td></tr><tr><td>GNN</td><td>在图结构上进行消息传递</td><td>社交网络、推荐、分子性质预测</td></tr></tbody></table><p>可以把它们理解成不同数据形态上的神经网络变体：</p><ul><li>表格数据常用 MLP。</li><li>图像数据常用 CNN 或 ViT。</li><li>序列数据常用 RNN、LSTM、Transformer。</li><li>图数据常用 GNN。</li><li>生成任务常见 VAE、GAN、扩散模型、Transformer。</li></ul><h2 id="11-入门阶段最应该掌握什么"><a href="#11-入门阶段最应该掌握什么" class="headerlink" title="11. 入门阶段最应该掌握什么"></a>11. 入门阶段最应该掌握什么</h2><p>深度学习内容很多，但入门阶段最重要的不是追热点，而是把基本训练逻辑打牢。</p><p>建议先掌握下面这些概念：</p><table><thead><tr><th>知识点</th><th>要理解到什么程度</th></tr></thead><tbody><tr><td>Tensor</td><td>知道它是深度学习中的多维数组</td></tr><tr><td>线性层</td><td>知道本质是矩阵乘法加偏置</td></tr><tr><td>激活函数</td><td>知道它提供非线性表达能力</td></tr><tr><td>损失函数</td><td>知道它衡量预测与真实值的差距</td></tr><tr><td>梯度</td><td>知道它表示参数对损失的影响方向</td></tr><tr><td>反向传播</td><td>知道它用链式法则计算梯度</td></tr><tr><td>优化器</td><td>知道它根据梯度更新参数</td></tr><tr><td>训练集&#x2F;验证集&#x2F;测试集</td><td>知道它们分别用于训练、调参、最终评估</td></tr><tr><td>过拟合</td><td>知道模型在训练集好、在新数据差</td></tr><tr><td>正则化</td><td>知道它用于提升泛化能力</td></tr></tbody></table><p>这些内容看似基础，但后面的 CNN、Transformer、GNN 都离不开它们。</p><h2 id="12-后续学习流程"><a href="#12-后续学习流程" class="headerlink" title="12. 后续学习流程"></a>12. 后续学习流程</h2><p>如果刚入门深度学习，我建议按下面这个顺序学习。</p><p><img src="https://lsworl.github.io/images/ml-blog/deep-learning-roadmap.svg" alt="深度学习后续学习流程"></p><h3 id="第-1-阶段：补齐数学和机器学习基础"><a href="#第-1-阶段：补齐数学和机器学习基础" class="headerlink" title="第 1 阶段：补齐数学和机器学习基础"></a>第 1 阶段：补齐数学和机器学习基础</h3><p>先不用追求数学非常深入，但至少要能读懂模型训练里的基本符号。</p><p>重点学习：</p><ul><li>向量、矩阵、矩阵乘法。</li><li>导数、偏导数、链式法则。</li><li>概率、条件概率、期望。</li><li>训练集、验证集、测试集。</li><li>欠拟合、过拟合、泛化能力。</li><li>梯度下降、学习率、局部最优。</li></ul><p>建议配合已有机器学习文章学习：</p><ul><li>线性回归</li><li>逻辑回归</li><li>梯度下降</li><li>正则化与过拟合</li><li>交叉验证和网格搜索</li></ul><p>这一阶段的目标不是变成数学专家，而是看到公式不慌，知道每个符号大概在表达什么。</p><h3 id="第-2-阶段：学会-MLP-和训练流程"><a href="#第-2-阶段：学会-MLP-和训练流程" class="headerlink" title="第 2 阶段：学会 MLP 和训练流程"></a>第 2 阶段：学会 MLP 和训练流程</h3><p>MLP 是深度学习最基础的网络。</p><p>这一阶段要重点理解：</p><ul><li>输入层、隐藏层、输出层。</li><li>权重、偏置、激活函数。</li><li>前向传播。</li><li>损失函数。</li><li>反向传播。</li><li>优化器更新参数。</li></ul><p>建议至少手写一个小例子：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">import</span> torch<span class="token punctuation">.</span>nn <span class="token keyword">as</span> nnmodel <span class="token operator">=</span> nn<span class="token punctuation">.</span>Sequential<span class="token punctuation">(</span>    nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>ReLU<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span>x <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">)</span>out <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>out<span class="token punctuation">)</span></code></pre><p>先不追求复杂，能清楚说出每一层输入输出形状就很好。</p><h3 id="第-3-阶段：熟悉-PyTorch"><a href="#第-3-阶段：熟悉-PyTorch" class="headerlink" title="第 3 阶段：熟悉 PyTorch"></a>第 3 阶段：熟悉 PyTorch</h3><p>PyTorch 是深度学习实践中非常常用的框架。</p><p>这一阶段重点掌握：</p><ul><li><code>torch.Tensor</code></li><li><code>nn.Module</code></li><li><code>nn.Linear</code></li><li><code>nn.ReLU</code></li><li><code>nn.Sequential</code></li><li><code>loss.backward()</code></li><li><code>torch.optim.SGD</code></li><li><code>torch.optim.Adam</code></li><li><code>Dataset</code></li><li><code>DataLoader</code></li><li><code>model.train()</code></li><li><code>model.eval()</code></li><li><code>torch.no_grad()</code></li></ul><p>建议完成一个完整训练脚本，而不是只看零散代码。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">读取数据 -> 定义模型 -> 定义损失函数 -> 定义优化器       -> 训练循环 -> 验证模型 -> 保存模型</code></pre><p>只要把这个流程写顺，后面换 CNN、RNN、Transformer 时就不会迷路。</p><h3 id="第-4-阶段：学习-CNN"><a href="#第-4-阶段：学习-CNN" class="headerlink" title="第 4 阶段：学习 CNN"></a>第 4 阶段：学习 CNN</h3><p>CNN 适合处理图像数据。</p><p>重点理解：</p><ul><li>卷积核是什么。</li><li>感受野是什么。</li><li>padding 和 stride 有什么作用。</li><li>池化层为什么能降维。</li><li>通道数如何变化。</li></ul><p>推荐项目：</p><ul><li>MNIST 手写数字分类。</li><li>CIFAR-10 图片分类。</li><li>简单的猫狗分类。</li></ul><p>这一阶段要特别重视张量形状变化。</p><p>例如图片张量常见格式是：</p><pre class="language-text" data-language="text"><code class="language-text">batch_size x channels x height x width</code></pre><p>如果能说清楚每一层之后尺寸如何变化，CNN 就已经入门了一大半。</p><h3 id="第-5-阶段：学习序列模型"><a href="#第-5-阶段：学习序列模型" class="headerlink" title="第 5 阶段：学习序列模型"></a>第 5 阶段：学习序列模型</h3><p>序列模型主要处理文本、时间序列、语音等数据。</p><p>可以按这个顺序：</p><ol><li>RNN</li><li>LSTM &#x2F; GRU</li><li>Attention</li><li>Transformer</li></ol><p>RNN 的核心是“把上一步的信息传给下一步”。<br>Transformer 的核心是“用注意力机制让每个位置看到其他位置”。</p><p>推荐项目：</p><ul><li>文本情感分类。</li><li>股票或温度时间序列预测。</li><li>简单机器翻译。</li><li>使用 Transformer 做文本分类。</li></ul><h3 id="第-6-阶段：学习-Transformer-和大模型基础"><a href="#第-6-阶段：学习-Transformer-和大模型基础" class="headerlink" title="第 6 阶段：学习 Transformer 和大模型基础"></a>第 6 阶段：学习 Transformer 和大模型基础</h3><p>现在很多深度学习应用都绕不开 Transformer。</p><p>重点理解：</p><ul><li>Token 和 Embedding。</li><li>Self-Attention。</li><li>Query、Key、Value。</li><li>Multi-Head Attention。</li><li>Position Encoding。</li><li>Encoder 和 Decoder。</li><li>预训练和微调。</li></ul><p>入门阶段不建议一开始就直接读大模型源码。<br>更好的方式是先实现一个小型 Transformer，用小数据跑通，再去看 BERT、GPT、ViT 等模型。</p><h3 id="第-7-阶段：按兴趣选择方向"><a href="#第-7-阶段：按兴趣选择方向" class="headerlink" title="第 7 阶段：按兴趣选择方向"></a>第 7 阶段：按兴趣选择方向</h3><p>基础打好后，可以根据兴趣选择方向。</p><table><thead><tr><th>方向</th><th>推荐继续学习</th></tr></thead><tbody><tr><td>计算机视觉</td><td>CNN、ResNet、ViT、目标检测、图像分割</td></tr><tr><td>自然语言处理</td><td>Transformer、BERT、GPT、文本生成、RAG</td></tr><tr><td>推荐系统</td><td>Embedding、双塔模型、召回排序、序列推荐</td></tr><tr><td>图学习</td><td>GCN、GraphSAGE、GAT、图分类</td></tr><tr><td>生成模型</td><td>AutoEncoder、VAE、GAN、扩散模型</td></tr><tr><td>强化学习</td><td>MDP、Q-Learning、Policy Gradient、PPO</td></tr><tr><td>模型部署</td><td>ONNX、TensorRT、量化、推理服务</td></tr></tbody></table><p>不要同时开太多方向。<br>比较好的节奏是：先选一个方向做 2 到 3 个项目，再横向扩展。</p><h2 id="13-推荐的实践路线"><a href="#13-推荐的实践路线" class="headerlink" title="13. 推荐的实践路线"></a>13. 推荐的实践路线</h2><p>深度学习不是只靠看文章学会的，一定要动手。</p><p>可以按下面这个小项目顺序推进：</p><table><thead><tr><th align="right">顺序</th><th>项目</th><th>主要练习</th></tr></thead><tbody><tr><td align="right">1</td><td>用 MLP 拟合 XOR</td><td>前向传播、激活函数、损失下降</td></tr><tr><td align="right">2</td><td>用 MLP 做鸢尾花分类</td><td>表格分类、交叉熵</td></tr><tr><td align="right">3</td><td>用 CNN 做 MNIST 分类</td><td>卷积、池化、图像张量</td></tr><tr><td align="right">4</td><td>用 CNN 做 CIFAR-10 分类</td><td>更复杂图像分类、过拟合处理</td></tr><tr><td align="right">5</td><td>用 RNN&#x2F;LSTM 做文本分类</td><td>序列建模、Embedding</td></tr><tr><td align="right">6</td><td>用 Transformer 做文本分类</td><td>注意力机制、位置编码</td></tr><tr><td align="right">7</td><td>复现一个小论文模型</td><td>阅读论文、复现实验、调参</td></tr><tr><td align="right">8</td><td>部署一个模型接口</td><td>推理、保存加载、服务化</td></tr></tbody></table><p>每个项目都建议写一份记录：</p><ul><li>数据是什么。</li><li>输入输出形状是什么。</li><li>模型结构是什么。</li><li>损失函数是什么。</li><li>训练曲线如何变化。</li><li>遇到了什么问题。</li><li>最后效果如何。</li></ul><p>这样学到的东西会更扎实。</p><h2 id="14-初学者常见误区"><a href="#14-初学者常见误区" class="headerlink" title="14. 初学者常见误区"></a>14. 初学者常见误区</h2><h3 id="误区-1：一开始就追最新模型"><a href="#误区-1：一开始就追最新模型" class="headerlink" title="误区 1：一开始就追最新模型"></a>误区 1：一开始就追最新模型</h3><p>新模型当然很吸引人，但如果 MLP、损失函数、反向传播、优化器都没理解，直接看大模型会非常吃力。</p><p>更推荐的顺序是：</p><pre class="language-text" data-language="text"><code class="language-text">MLP -> CNN/RNN -> Attention -> Transformer -> 大模型</code></pre><h3 id="误区-2：只看代码，不看张量形状"><a href="#误区-2：只看代码，不看张量形状" class="headerlink" title="误区 2：只看代码，不看张量形状"></a>误区 2：只看代码，不看张量形状</h3><p>深度学习代码里最常见的问题之一就是 shape 不匹配。</p><p>看到每一层时都应该问：</p><pre class="language-text" data-language="text"><code class="language-text">输入形状是什么？输出形状是什么？batch 维在哪里？特征维在哪里？</code></pre><h3 id="误区-3：只关注训练集准确率"><a href="#误区-3：只关注训练集准确率" class="headerlink" title="误区 3：只关注训练集准确率"></a>误区 3：只关注训练集准确率</h3><p>训练集准确率高，不代表模型真的好。<br>如果验证集和测试集表现很差，很可能是过拟合。</p><p>所以训练时至少要关注：</p><ul><li>训练集损失。</li><li>验证集损失。</li><li>训练集准确率。</li><li>验证集准确率。</li></ul><h3 id="误区-4：把框架-API-当成黑盒"><a href="#误区-4：把框架-API-当成黑盒" class="headerlink" title="误区 4：把框架 API 当成黑盒"></a>误区 4：把框架 API 当成黑盒</h3><p>PyTorch 很方便，但不能完全把它当魔法。</p><p>至少要知道：</p><ul><li><code>model(x)</code> 是前向传播。</li><li><code>loss.backward()</code> 是计算梯度。</li><li><code>optimizer.step()</code> 是更新参数。</li><li><code>optimizer.zero_grad()</code> 是清空旧梯度。</li></ul><p>理解这几件事后，很多训练代码都会变得清楚。</p><h2 id="15-总结"><a href="#15-总结" class="headerlink" title="15. 总结"></a>15. 总结</h2><p>深度学习入门可以先抓住四句话：</p><ul><li>神经网络由很多层组成，每层都在变换数据表示。</li><li>激活函数让神经网络具备非线性表达能力。</li><li>损失函数告诉模型预测错了多少。</li><li>反向传播和优化器负责让参数朝着损失变小的方向更新。</li></ul><p>后续学习不要急着追复杂模型。<br>比较稳的路线是：</p><pre class="language-text" data-language="text"><code class="language-text">数学与机器学习基础  -> MLP 和训练流程  -> PyTorch 实战  -> CNN / RNN / Transformer / GNN  -> 项目实践  -> 论文复现和模型部署</code></pre><p>只要把“数据如何进入模型、模型如何输出预测、损失如何计算、参数如何更新”这条主线想清楚，后面再学任何深度学习模型，都会更容易找到位置。</p>]]>
    </content>
    <id>https://lsworl.github.io/2026/07/08/deep-learning-introduction-roadmap/</id>
    <link href="https://lsworl.github.io/2026/07/08/deep-learning-introduction-roadmap/"/>
    <published>2026-07-07T16:00:00.000Z</published>
    <summary>面向初学者梳理深度学习的核心概念、神经网络的基本计算方式、训练流程、常见模型方向，并给出后续学习路线。</summary>
    <title>深度学习入门：从神经网络到学习路线</title>
    <updated>2026-07-08T14:43:40.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="机器学习" scheme="https://lsworl.github.io/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="机器学习" scheme="https://lsworl.github.io/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="深度学习" scheme="https://lsworl.github.io/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    <category term="神经网络" scheme="https://lsworl.github.io/tags/%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C/"/>
    <category term="PyTorch" scheme="https://lsworl.github.io/tags/PyTorch/"/>
    <category term="MLP" scheme="https://lsworl.github.io/tags/MLP/"/>
    <content>
      <![CDATA[<p>MLP（Multilayer Perceptron，多层感知机）是最基础也最重要的神经网络之一。</p><p>如果说线性回归、逻辑回归只有一层线性变换，那么 MLP 就是在多层线性变换之间加入非线性激活函数，让模型能够拟合更复杂的关系。</p><p>它的结构并不神秘：</p><pre class="language-text" data-language="text"><code class="language-text">输入特征 -> 隐藏层 -> 激活函数 -> 隐藏层 -> 激活函数 -> 输出层</code></pre><p>一句话概括：</p><blockquote><p>MLP 用多个全连接层和非线性激活函数，把输入特征一步步变换成适合分类或回归的输出。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/ml-blog/mlp-architecture.svg" alt="MLP 由输入层、隐藏层和输出层组成"></p><h2 id="1-为什么需要-MLP"><a href="#1-为什么需要-MLP" class="headerlink" title="1. 为什么需要 MLP"></a>1. 为什么需要 MLP</h2><p>前面学习线性回归时，模型大致是：</p><p>$$<br>\hat{y}&#x3D;wx+b<br>$$</p><p>对于多维输入，可以写成：</p><p>$$<br>\hat{y}&#x3D;xW+b<br>$$</p><p>这类模型只能表达线性关系。</p><p>例如下面这种问题，线性模型就比较吃力：</p><pre class="language-text" data-language="text"><code class="language-text">两个特征单独看都不能决定类别，但它们组合起来以后才有意义。</code></pre><p>经典例子是 XOR：</p><table><thead><tr><th align="right">$x_1$</th><th align="right">$x_2$</th><th align="right">标签</th></tr></thead><tbody><tr><td align="right">0</td><td align="right">0</td><td align="right">0</td></tr><tr><td align="right">0</td><td align="right">1</td><td align="right">1</td></tr><tr><td align="right">1</td><td align="right">0</td><td align="right">1</td></tr><tr><td align="right">1</td><td align="right">1</td><td align="right">0</td></tr></tbody></table><p>这四个点无法用一条直线完美分开。</p><p>MLP 的作用就是：先通过隐藏层把原始特征映射到新的空间，再在新空间里完成分类或回归。</p><h2 id="2-MLP-的基本结构"><a href="#2-MLP-的基本结构" class="headerlink" title="2. MLP 的基本结构"></a>2. MLP 的基本结构</h2><p>一个最简单的 MLP 可以写成：</p><p>$$<br>h_1 &#x3D; \sigma(xW_1+b_1)<br>$$</p><p>$$<br>h_2 &#x3D; \sigma(h_1W_2+b_2)<br>$$</p><p>$$<br>\hat{y}&#x3D;h_2W_3+b_3<br>$$</p><p>其中：</p><ul><li>$x$ 是输入特征。</li><li>$W_1,W_2,W_3$ 是权重矩阵。</li><li>$b_1,b_2,b_3$ 是偏置。</li><li>$\sigma$ 是非线性激活函数。</li><li>$h_1,h_2$ 是隐藏层表示。</li></ul><p>如果是分类任务，最后通常会输出每个类别的 logits，再接 softmax 或交叉熵损失。</p><p>如果是回归任务，最后通常直接输出一个连续数值。</p><p><img src="https://lsworl.github.io/images/ml-blog/mlp-forward-computation.svg" alt="MLP 单层计算可以拆成线性变换和激活函数"></p><h2 id="3-为什么一定要激活函数"><a href="#3-为什么一定要激活函数" class="headerlink" title="3. 为什么一定要激活函数"></a>3. 为什么一定要激活函数</h2><p>如果没有激活函数，多层线性层叠在一起仍然只是线性变换。</p><p>例如：</p><p>$$<br>h&#x3D;xW_1+b_1<br>$$</p><p>$$<br>y&#x3D;hW_2+b_2<br>$$</p><p>代入得到：</p><p>$$<br>y&#x3D;(xW_1+b_1)W_2+b_2<br>$$</p><p>展开：</p><p>$$<br>y&#x3D;x(W_1W_2)+(b_1W_2+b_2)<br>$$</p><p>这仍然可以看成：</p><p>$$<br>y&#x3D;xW+b<br>$$</p><p>也就是说，如果没有非线性激活函数，堆再多层也没有本质提升。</p><p>激活函数的作用就是打破线性，让网络可以拟合复杂曲线、复杂边界和复杂特征组合。</p><p>常见激活函数包括：</p><table><thead><tr><th>激活函数</th><th>公式或含义</th><th>常见用途</th></tr></thead><tbody><tr><td>ReLU</td><td>$\max(0,x)$</td><td>默认常用，简单高效</td></tr><tr><td>Sigmoid</td><td>输出在 $(0,1)$</td><td>二分类概率、门控结构</td></tr><tr><td>Tanh</td><td>输出在 $(-1,1)$，以 0 为中心</td><td>RNN、需要正负输出的隐藏表示</td></tr><tr><td>GELU</td><td>按输入大小平滑地保留信息</td><td>Transformer、较深的 MLP</td></tr><tr><td>Softmax</td><td>多类别概率归一化</td><td>多分类输出解释</td></tr></tbody></table><p>在 MLP 的隐藏层中，最常见的是 ReLU 或 GELU。<br>如果你希望隐藏层输出既有正值又有负值，可以考虑 Tanh；如果你在写 Transformer 风格的前馈网络，GELU 会更常见。</p><p><img src="https://lsworl.github.io/images/ml-blog/mlp-activation-functions.svg" alt="常见激活函数的输出范围和特点"></p><h2 id="4-手工算一次-MLP-前向传播"><a href="#4-手工算一次-MLP-前向传播" class="headerlink" title="4. 手工算一次 MLP 前向传播"></a>4. 手工算一次 MLP 前向传播</h2><p>为了直观理解 MLP，我们手工算一个很小的网络。</p><p>假设输入为：</p><p>$$<br>x&#x3D;[2,3]<br>$$</p><p>第一层有两个神经元：</p><p>$$<br>W_1&#x3D;<br>\begin{bmatrix}<br>1 &amp; -1 \<br>2 &amp; 1<br>\end{bmatrix},<br>\quad<br>b_1&#x3D;[0,1]<br>$$</p><p>先算线性部分：</p><p>$$<br>z_1&#x3D;xW_1+b_1<br>$$</p><p>逐项计算：</p><p>$$<br>z_{1,1}&#x3D;2\times1+3\times2+0&#x3D;8<br>$$</p><p>$$<br>z_{1,2}&#x3D;2\times(-1)+3\times1+1&#x3D;2<br>$$</p><p>所以：</p><p>$$<br>z_1&#x3D;[8,2]<br>$$</p><p>经过 ReLU：</p><p>$$<br>h_1&#x3D;\text{ReLU}([8,2])&#x3D;[8,2]<br>$$</p><p>第二层输出一个数：</p><p>$$<br>W_2&#x3D;<br>\begin{bmatrix}<br>1 \<br>-2<br>\end{bmatrix},<br>\quad<br>b_2&#x3D;0.5<br>$$</p><p>则：</p><p>$$<br>\hat{y}&#x3D;h_1W_2+b_2&#x3D;8\times1+2\times(-2)+0.5&#x3D;4.5<br>$$</p><p>这就是一次完整前向传播：</p><pre class="language-text" data-language="text"><code class="language-text">输入 [2, 3]-> 第一层线性变换 [8, 2]-> ReLU [8, 2]-> 第二层线性变换 4.5</code></pre><p>如果某个中间值是负数，ReLU 会把它变成 0，这就是非线性发生的地方。</p><h2 id="5-MLP-如何训练"><a href="#5-MLP-如何训练" class="headerlink" title="5. MLP 如何训练"></a>5. MLP 如何训练</h2><p>MLP 的训练流程和其他神经网络类似：</p><ol><li>前向传播：输入样本，得到预测值。</li><li>计算损失：比较预测值和真实标签。</li><li>反向传播：根据损失计算每个参数的梯度。</li><li>参数更新：优化器根据梯度更新权重和偏置。</li><li>重复很多轮，直到模型效果稳定。</li></ol><p><img src="https://lsworl.github.io/images/ml-blog/mlp-training-loop.svg" alt="PyTorch 训练循环的核心步骤"></p><p>用公式概括，目标是最小化损失函数：</p><p>$$<br>\min_\theta \mathcal{L}(\hat{y},y)<br>$$</p><p>其中 $\theta$ 表示所有可学习参数。</p><p>参数更新可以写成：</p><p>$$<br>\theta :&#x3D; \theta-\alpha\nabla_\theta \mathcal{L}<br>$$</p><p>这和梯度下降的思想完全一致，只是神经网络里的参数更多，计算图更复杂。</p><h2 id="6-用-PyTorch-实现神经网络：从手动参数到-nn-Module"><a href="#6-用-PyTorch-实现神经网络：从手动参数到-nn-Module" class="headerlink" title="6. 用 PyTorch 实现神经网络：从手动参数到 nn.Module"></a>6. 用 PyTorch 实现神经网络：从手动参数到 <code>nn.Module</code></h2><p>单独看 <code>Tensor</code>、优化器、<code>nn.Linear</code> 和 <code>nn.Module</code> 很容易觉得它们是几个无关的 API。<br>实际上，它们只是对同一个训练过程进行了逐层封装：</p><pre class="language-text" data-language="text"><code class="language-text">手动创建权重 Tensor  -> nn.Linear 管理一层的权重和偏置  -> nn.Module 管理整个网络的子层和参数</code></pre><p><img src="https://lsworl.github.io/images/ml-blog/mlp-pytorch-evolution.svg" alt="PyTorch 构造神经网络的三个抽象层次"></p><p>下面使用同一个 XOR 二分类任务，分三步实现一个包含隐藏层的神经网络。</p><h3 id="6-1-准备训练数据"><a href="#6-1-准备训练数据" class="headerlink" title="6.1 准备训练数据"></a>6.1 准备训练数据</h3><p>XOR 有 4 个样本：两个输入相同时输出 0，不同时输出 1。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">import</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>functional <span class="token keyword">as</span> F<span class="token keyword">from</span> torch <span class="token keyword">import</span> nntorch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">42</span><span class="token punctuation">)</span>X <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>y <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span></code></pre><p><code>X</code> 的形状是 <code>[4, 2]</code>：</p><ul><li>4 表示 4 个样本。</li><li>2 表示每个样本有 2 个输入特征。</li></ul><p><code>y</code> 的形状是 <code>[4]</code>，其中每个整数是对应样本的类别索引。</p><h3 id="6-2-第一版：手动创建参数，使用-PyTorch-优化器训练"><a href="#6-2-第一版：手动创建参数，使用-PyTorch-优化器训练" class="headerlink" title="6.2 第一版：手动创建参数，使用 PyTorch 优化器训练"></a>6.2 第一版：手动创建参数，使用 PyTorch 优化器训练</h3><p>先不使用 <code>nn.Linear</code> 和 <code>nn.Module</code>，直接创建权重矩阵和偏置向量。</p><p>网络结构为：</p><pre class="language-text" data-language="text"><code class="language-text">2 个输入 -> 8 个隐藏单元 -> 2 个分类 logits</code></pre><p>因此各个参数的形状是：</p><pre class="language-text" data-language="text"><code class="language-text">W1: [2, 8]b1: [8]W2: [8, 2]b2: [2]</code></pre><p>完整代码如下：</p><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">42</span><span class="token punctuation">)</span>W1 <span class="token operator">=</span> torch<span class="token punctuation">.</span>randn<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">,</span> requires_grad<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>b1 <span class="token operator">=</span> torch<span class="token punctuation">.</span>zeros<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">,</span> requires_grad<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>W2 <span class="token operator">=</span> torch<span class="token punctuation">.</span>randn<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> requires_grad<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>b2 <span class="token operator">=</span> torch<span class="token punctuation">.</span>zeros<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> requires_grad<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span><span class="token comment"># 优化器可以直接接收需要更新的 Tensor 列表</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>SGD<span class="token punctuation">(</span><span class="token punctuation">[</span>W1<span class="token punctuation">,</span> b1<span class="token punctuation">,</span> W2<span class="token punctuation">,</span> b2<span class="token punctuation">]</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">0.1</span><span class="token punctuation">)</span><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token comment"># 前向传播：手动写出两层网络的矩阵运算</span>    hidden <span class="token operator">=</span> torch<span class="token punctuation">.</span>tanh<span class="token punctuation">(</span>X @ W1 <span class="token operator">+</span> b1<span class="token punctuation">)</span>    logits <span class="token operator">=</span> hidden @ W2 <span class="token operator">+</span> b2    <span class="token comment"># CrossEntropy 直接接收 logits 和类别索引</span>    loss <span class="token operator">=</span> F<span class="token punctuation">.</span>cross_entropy<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> y<span class="token punctuation">)</span>    <span class="token comment"># PyTorch 默认累积梯度，每轮更新前要清空旧梯度</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    hidden <span class="token operator">=</span> torch<span class="token punctuation">.</span>tanh<span class="token punctuation">(</span>X @ W1 <span class="token operator">+</span> b1<span class="token punctuation">)</span>    logits <span class="token operator">=</span> hidden @ W2 <span class="token operator">+</span> b2    pred <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>pred<span class="token punctuation">.</span>tolist<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>训练正常时，最终预测应该接近：</p><pre class="language-text" data-language="text"><code class="language-text">[0, 1, 1, 0]</code></pre><p>这个版本已经是一个完整的神经网络训练过程：</p><ol><li><code>W1</code>、<code>b1</code>、<code>W2</code>、<code>b2</code> 是可学习参数。</li><li><code>torch.tanh</code> 提供非线性。</li><li><code>F.cross_entropy</code> 计算分类损失。</li><li><code>loss.backward()</code> 通过自动求导填充参数的 <code>.grad</code>。</li><li><code>optimizer.step()</code> 根据梯度更新参数。</li></ol><p>优化器并不要求参数必须来自模型类。只要它们是可求导的叶子 Tensor，就可以交给优化器。</p><p>这种写法有助于理解原理，但参数很多时会带来几个问题：</p><ul><li>必须自己管理权重和偏置。</li><li>必须自己保证矩阵形状正确。</li><li>初始化策略需要自己选择。</li><li>保存、加载和设备迁移都需要单独处理。</li></ul><p>这就是 <code>nn.Linear</code> 要解决的第一层问题。</p><h3 id="6-3-第二版：用-nn-Linear-封装每一层"><a href="#6-3-第二版：用-nn-Linear-封装每一层" class="headerlink" title="6.3 第二版：用 nn.Linear 封装每一层"></a>6.3 第二版：用 <code>nn.Linear</code> 封装每一层</h3><p><code>nn.Linear(in_features, out_features)</code> 会在内部自动创建：</p><ul><li><code>weight</code>：可学习权重。</li><li><code>bias</code>：可学习偏置。</li><li>合适的默认初始化。</li></ul><p>因此可以用两个 <code>nn.Linear</code> 替代刚才手动创建的四个 Tensor：</p><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">42</span><span class="token punctuation">)</span>linear1 <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>in_features<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">,</span> out_features<span class="token operator">=</span><span class="token number">8</span><span class="token punctuation">)</span>linear2 <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span>in_features<span class="token operator">=</span><span class="token number">8</span><span class="token punctuation">,</span> out_features<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">)</span><span class="token comment"># 此时还没有整体 model，所以手动合并两层的参数</span>parameters <span class="token operator">=</span> <span class="token builtin">list</span><span class="token punctuation">(</span>linear1<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token builtin">list</span><span class="token punctuation">(</span>linear2<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>SGD<span class="token punctuation">(</span>parameters<span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">0.1</span><span class="token punctuation">)</span><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    hidden <span class="token operator">=</span> torch<span class="token punctuation">.</span>tanh<span class="token punctuation">(</span>linear1<span class="token punctuation">(</span>X<span class="token punctuation">)</span><span class="token punctuation">)</span>    logits <span class="token operator">=</span> linear2<span class="token punctuation">(</span>hidden<span class="token punctuation">)</span>    loss <span class="token operator">=</span> F<span class="token punctuation">.</span>cross_entropy<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> y<span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    logits <span class="token operator">=</span> linear2<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>tanh<span class="token punctuation">(</span>linear1<span class="token punctuation">(</span>X<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span>    pred <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>pred<span class="token punctuation">.</span>tolist<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>两种写法的对应关系是：</p><table><thead><tr><th>手动矩阵运算</th><th><code>nn.Linear</code> 写法</th></tr></thead><tbody><tr><td><code>X @ W1 + b1</code></td><td><code>linear1(X)</code></td></tr><tr><td><code>hidden @ W2 + b2</code></td><td><code>linear2(hidden)</code></td></tr><tr><td><code>[W1, b1, W2, b2]</code></td><td>两层的 <code>parameters()</code></td></tr></tbody></table><p>需要注意，<code>nn.Linear</code> 只表示一层仿射变换，它不会自动加入 Tanh、ReLU 等激活函数。<br>所以代码中仍然要显式写出：</p><pre class="language-python" data-language="python"><code class="language-python">hidden <span class="token operator">=</span> torch<span class="token punctuation">.</span>tanh<span class="token punctuation">(</span>linear1<span class="token punctuation">(</span>X<span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>这个版本已经不需要手动管理每层的权重和偏置，但仍然有一个问题：两个线性层还是分散的对象，并没有被组织成一个完整模型。</p><p>这就需要 <code>nn.Module</code>。</p><h3 id="6-4-第三版：用-nn-Module-组织完整网络"><a href="#6-4-第三版：用-nn-Module-组织完整网络" class="headerlink" title="6.4 第三版：用 nn.Module 组织完整网络"></a>6.4 第三版：用 <code>nn.Module</code> 组织完整网络</h3><p><code>nn.Module</code> 可以把多个层、激活函数和前向传播逻辑组织成一个模型。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">XORNet</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>linear1 <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>linear2 <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        hidden <span class="token operator">=</span> torch<span class="token punctuation">.</span>tanh<span class="token punctuation">(</span>self<span class="token punctuation">.</span>linear1<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span>        logits <span class="token operator">=</span> self<span class="token punctuation">.</span>linear2<span class="token punctuation">(</span>hidden<span class="token punctuation">)</span>        <span class="token keyword">return</span> logits</code></pre><p><code>__init__</code> 负责定义网络包含哪些子层，<code>forward</code> 负责定义数据如何流过这些层。</p><p>完整训练代码为：</p><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">42</span><span class="token punctuation">)</span>model <span class="token operator">=</span> XORNet<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>SGD<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">0.1</span><span class="token punctuation">)</span><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>X<span class="token punctuation">)</span>    loss <span class="token operator">=</span> F<span class="token punctuation">.</span>cross_entropy<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> y<span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>X<span class="token punctuation">)</span>    pred <span class="token operator">=</span> logits<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>pred<span class="token punctuation">.</span>tolist<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>与第二版相比，最关键的变化是：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token comment"># 第二版：自己收集每层参数</span>parameters <span class="token operator">=</span> <span class="token builtin">list</span><span class="token punctuation">(</span>linear1<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token builtin">list</span><span class="token punctuation">(</span>linear2<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token comment"># 第三版：nn.Module 递归收集所有子层参数</span>parameters <span class="token operator">=</span> model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>只要在 <code>__init__</code> 中把子层赋值给 <code>self.xxx</code>，<code>nn.Module</code> 就会注册它们。<br>于是 <code>model.parameters()</code> 能够递归找到 <code>linear1</code> 和 <code>linear2</code> 的所有权重与偏置。</p><h3 id="6-5-三个版本的本质没有变"><a href="#6-5-三个版本的本质没有变" class="headerlink" title="6.5 三个版本的本质没有变"></a>6.5 三个版本的本质没有变</h3><table><thead><tr><th>版本</th><th>参数由谁管理</th><th>前向传播如何定义</th><th>优化器接收什么</th></tr></thead><tbody><tr><td>手动 Tensor</td><td>开发者自己管理</td><td>直接写矩阵运算</td><td><code>[W1, b1, W2, b2]</code></td></tr><tr><td><code>nn.Linear</code></td><td>每个 Linear 层管理</td><td>手动连接各层</td><td>合并多层的 <code>parameters()</code></td></tr><tr><td><code>nn.Module</code></td><td>整个模型递归管理</td><td>定义在 <code>forward</code> 中</td><td><code>model.parameters()</code></td></tr></tbody></table><p>三个版本的数学计算实际上都是：</p><p>$$<br>h&#x3D;\tanh(XW_1+b_1)<br>$$</p><p>$$<br>logits&#x3D;hW_2+b_2<br>$$</p><p>不变的训练主线始终是：</p><pre class="language-text" data-language="text"><code class="language-text">前向传播 -> 计算损失 -> 清空梯度           -> 反向传播 -> 优化器更新参数</code></pre><p>PyTorch 的封装没有改变神经网络的数学本质，它只是让参数管理、模型复用、设备迁移和保存加载变得更可靠。</p><p>这里的输出 <code>logits</code> 是模型对每个类别给出的原始分数。<br>如果只在推理时需要概率，可以使用：</p><pre class="language-python" data-language="python"><code class="language-python">prob <span class="token operator">=</span> torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>但训练时使用 <code>F.cross_entropy</code> 或 <code>nn.CrossEntropyLoss</code> 时，不要先手动执行 softmax，因为损失函数内部已经包含了相应计算。</p><h2 id="7-完整训练例子：拟合-XOR"><a href="#7-完整训练例子：拟合-XOR" class="headerlink" title="7. 完整训练例子：拟合 XOR"></a>7. 完整训练例子：拟合 XOR</h2><p>XOR 是理解 MLP 的经典小例子。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">from</span> torch <span class="token keyword">import</span> nn<span class="token keyword">from</span> torch<span class="token punctuation">.</span>utils<span class="token punctuation">.</span>data <span class="token keyword">import</span> DataLoader<span class="token punctuation">,</span> TensorDatasettorch<span class="token punctuation">.</span>manual_seed<span class="token punctuation">(</span><span class="token number">42</span><span class="token punctuation">)</span>X <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>y <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span>dataset <span class="token operator">=</span> TensorDataset<span class="token punctuation">(</span>X<span class="token punctuation">,</span> y<span class="token punctuation">)</span>loader <span class="token operator">=</span> DataLoader<span class="token punctuation">(</span>dataset<span class="token punctuation">,</span> batch_size<span class="token operator">=</span><span class="token number">4</span><span class="token punctuation">,</span> shuffle<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span><span class="token keyword">class</span> <span class="token class-name">XORMLP</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>net <span class="token operator">=</span> nn<span class="token punctuation">.</span>Sequential<span class="token punctuation">(</span>            nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            nn<span class="token punctuation">.</span>Tanh<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>            nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> self<span class="token punctuation">.</span>net<span class="token punctuation">(</span>x<span class="token punctuation">)</span>model <span class="token operator">=</span> XORMLP<span class="token punctuation">(</span><span class="token punctuation">)</span>criterion <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">0.05</span><span class="token punctuation">)</span><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">for</span> batch_x<span class="token punctuation">,</span> batch_y <span class="token keyword">in</span> loader<span class="token punctuation">:</span>        logits <span class="token operator">=</span> model<span class="token punctuation">(</span>batch_x<span class="token punctuation">)</span>        loss <span class="token operator">=</span> criterion<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> batch_y<span class="token punctuation">)</span>        optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>        loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>        optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> epoch <span class="token operator">%</span> <span class="token number">200</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">:</span>        <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"epoch=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>epoch<span class="token punctuation">&#125;</span></span><span class="token string">, loss=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span><span class="token format-spec">.4f</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>X<span class="token punctuation">)</span>    pred <span class="token operator">=</span> torch<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"prediction:"</span><span class="token punctuation">,</span> pred<span class="token punctuation">.</span>tolist<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>如果训练正常，最终输出大致会接近：</p><pre class="language-text" data-language="text"><code class="language-text">prediction: [0, 1, 1, 0]</code></pre><p>这个例子说明：MLP 通过隐藏层和非线性激活函数，可以学到线性模型无法直接表达的决策边界。</p><h2 id="8-PyTorch-神经网络组件总览"><a href="#8-PyTorch-神经网络组件总览" class="headerlink" title="8. PyTorch 神经网络组件总览"></a>8. PyTorch 神经网络组件总览</h2><p>PyTorch 官方基础教程把机器学习工作流拆成数据、模型、优化和保存等环节；写神经网络时，常见组件也基本围绕这些环节展开。</p><p><img src="https://lsworl.github.io/images/ml-blog/mlp-pytorch-components.svg" alt="PyTorch 神经网络常用组件"></p><p>常用模块可以先按功能分组：</p><table><thead><tr><th>组件</th><th>作用</th></tr></thead><tbody><tr><td><code>torch.Tensor</code></td><td>存储数据和梯度相关信息</td></tr><tr><td><code>nn.Module</code></td><td>所有神经网络模块的基类</td></tr><tr><td><code>nn.Linear</code></td><td>全连接层，也就是仿射变换</td></tr><tr><td><code>nn.ReLU</code>、<code>nn.Sigmoid</code>、<code>nn.GELU</code></td><td>激活函数</td></tr><tr><td><code>nn.Sequential</code></td><td>顺序堆叠多个层</td></tr><tr><td><code>nn.CrossEntropyLoss</code>、<code>nn.MSELoss</code></td><td>损失函数</td></tr><tr><td><code>torch.optim.SGD</code>、<code>torch.optim.Adam</code></td><td>优化器</td></tr><tr><td><code>Dataset</code>、<code>DataLoader</code></td><td>数据集和小批量加载</td></tr><tr><td><code>model.train()</code>、<code>model.eval()</code></td><td>切换训练和推理模式</td></tr><tr><td><code>torch.no_grad()</code></td><td>推理时关闭梯度记录</td></tr></tbody></table><p>下面逐个解释常用函数和它们背后的原理。</p><h2 id="9-torch-Tensor：数据的基本容器"><a href="#9-torch-Tensor：数据的基本容器" class="headerlink" title="9. torch.Tensor：数据的基本容器"></a>9. <code>torch.Tensor</code>：数据的基本容器</h2><p>Tensor 可以理解为 PyTorch 里的多维数组。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torchx <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">2.0</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token punctuation">[</span><span class="token number">3.0</span><span class="token punctuation">,</span> <span class="token number">4.0</span><span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>x<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>  <span class="token comment"># torch.Size([2, 2])</span></code></pre><p>如果设置：</p><pre class="language-python" data-language="python"><code class="language-python">x<span class="token punctuation">.</span>requires_grad_<span class="token punctuation">(</span><span class="token boolean">True</span><span class="token punctuation">)</span></code></pre><p>PyTorch 就会追踪后续操作，用于自动求导。</p><p>例如：</p><pre class="language-python" data-language="python"><code class="language-python">x <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token number">2.0</span><span class="token punctuation">,</span> requires_grad<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>y <span class="token operator">=</span> x <span class="token operator">**</span> <span class="token number">2</span> <span class="token operator">+</span> <span class="token number">3</span> <span class="token operator">*</span> xy<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>x<span class="token punctuation">.</span>grad<span class="token punctuation">)</span></code></pre><p>手工计算：</p><p>$$<br>y&#x3D;x^2+3x<br>$$</p><p>$$<br>\frac{dy}{dx}&#x3D;2x+3<br>$$</p><p>当 $x&#x3D;2$ 时：</p><p>$$<br>\frac{dy}{dx}&#x3D;7<br>$$</p><p>所以 <code>x.grad</code> 会是 7。</p><h2 id="10-nn-Module：神经网络的基本单元"><a href="#10-nn-Module：神经网络的基本单元" class="headerlink" title="10. nn.Module：神经网络的基本单元"></a>10. <code>nn.Module</code>：神经网络的基本单元</h2><p>在 PyTorch 中，几乎所有模型和层都继承自 <code>nn.Module</code>。</p><p>它不只是一个“写模型的父类”，更像是 PyTorch 对神经网络的统一管理容器。<br>一个 <code>nn.Module</code> 至少负责下面几件事：</p><ul><li>保存可训练参数。</li><li>保存子模块。</li><li>定义前向传播。</li><li>在训练模式和推理模式之间切换。</li><li>递归地把模型移动到 CPU 或 GPU。</li><li>保存和加载模型状态。</li></ul><p>你通常需要做两件事：</p><ol><li>在 <code>__init__</code> 中定义层。</li><li>在 <code>forward</code> 中定义前向传播逻辑。</li></ol><p>例如：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">SimpleNet</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>fc1 <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>relu <span class="token operator">=</span> nn<span class="token punctuation">.</span>ReLU<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>fc2 <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        x <span class="token operator">=</span> self<span class="token punctuation">.</span>fc1<span class="token punctuation">(</span>x<span class="token punctuation">)</span>        x <span class="token operator">=</span> self<span class="token punctuation">.</span>relu<span class="token punctuation">(</span>x<span class="token punctuation">)</span>        x <span class="token operator">=</span> self<span class="token punctuation">.</span>fc2<span class="token punctuation">(</span>x<span class="token punctuation">)</span>        <span class="token keyword">return</span> x</code></pre><p>这里有一个很重要的细节：</p><blockquote><p>只要把子层赋值给 <code>self.xxx</code>，PyTorch 就会自动把它注册为当前模块的子模块。</p></blockquote><p>例如：</p><pre class="language-python" data-language="python"><code class="language-python">self<span class="token punctuation">.</span>fc1 <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">)</span></code></pre><p>这一句不仅仅是在对象上挂了一个属性，还会让 <code>fc1</code> 被 <code>nn.Module</code> 记录下来。<br>因此后面调用 <code>model.parameters()</code>、<code>model.to(device)</code>、<code>model.train()</code>、<code>model.eval()</code> 时，PyTorch 才能递归地找到这些子层。</p><p>可以打印模型参数：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> SimpleNet<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">for</span> name<span class="token punctuation">,</span> param <span class="token keyword">in</span> model<span class="token punctuation">.</span>named_parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>name<span class="token punctuation">,</span> param<span class="token punctuation">.</span>shape<span class="token punctuation">)</span></code></pre><p>可能输出：</p><pre class="language-text" data-language="text"><code class="language-text">fc1.weight torch.Size([4, 2])fc1.bias torch.Size([4])fc2.weight torch.Size([2, 4])fc2.bias torch.Size([2])</code></pre><p>这就是为什么优化器可以直接写：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">0.001</span><span class="token punctuation">)</span></code></pre><p>因为 <code>model.parameters()</code> 会返回模型中所有可训练参数。</p><h3 id="10-1-call-和-forward-的关系"><a href="#10-1-call-和-forward-的关系" class="headerlink" title="10.1 __call__ 和 forward 的关系"></a>10.1 <code>__call__</code> 和 <code>forward</code> 的关系</h3><p>平时我们通常这样调用模型：</p><pre class="language-python" data-language="python"><code class="language-python">out <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">)</span></code></pre><p>而不是直接写：</p><pre class="language-python" data-language="python"><code class="language-python">out <span class="token operator">=</span> model<span class="token punctuation">.</span>forward<span class="token punctuation">(</span>x<span class="token punctuation">)</span></code></pre><p>原因是 <code>model(x)</code> 会先进入 <code>nn.Module</code> 的 <code>__call__</code> 逻辑，再由 PyTorch 调用你写的 <code>forward</code>。<br>在这个过程中，PyTorch 会处理 hook、自动混合精度、模块状态等框架级逻辑。<br>所以一般建议：</p><pre class="language-python" data-language="python"><code class="language-python">out <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">)</span>        <span class="token comment"># 推荐</span></code></pre><p>不要直接调用：</p><pre class="language-python" data-language="python"><code class="language-python">out <span class="token operator">=</span> model<span class="token punctuation">.</span>forward<span class="token punctuation">(</span>x<span class="token punctuation">)</span>  <span class="token comment"># 不推荐作为常规写法</span></code></pre><p>可以把关系理解成：</p><pre class="language-text" data-language="text"><code class="language-text">model(x)  -> nn.Module.__call__()      -> 处理 PyTorch 内部逻辑      -> 调用 forward(x)      -> 返回输出</code></pre><h3 id="10-2-参数是怎么被注册的"><a href="#10-2-参数是怎么被注册的" class="headerlink" title="10.2 参数是怎么被注册的"></a>10.2 参数是怎么被注册的</h3><p>不是所有 <code>Tensor</code> 都会自动成为模型参数。<br>只有下面这类对象会被 <code>nn.Module</code> 当作可训练参数管理：</p><ul><li><code>nn.Parameter</code></li><li>子模块中的 <code>nn.Parameter</code></li></ul><p>例如：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">MyScale</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>scale <span class="token operator">=</span> nn<span class="token punctuation">.</span>Parameter<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token number">1.0</span><span class="token punctuation">)</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>cache <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token number">0.0</span><span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> x <span class="token operator">*</span> self<span class="token punctuation">.</span>scale</code></pre><p>这里：</p><ul><li><code>self.scale</code> 会出现在 <code>model.parameters()</code> 中。</li><li><code>self.cache</code> 只是普通张量，不会被优化器更新。</li></ul><p>可以验证：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> MyScale<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">for</span> name<span class="token punctuation">,</span> param <span class="token keyword">in</span> model<span class="token punctuation">.</span>named_parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>name<span class="token punctuation">,</span> param<span class="token punctuation">)</span></code></pre><p>输出中会看到 <code>scale</code>，但不会看到 <code>cache</code>。</p><p>如果你希望某个张量跟随模型一起保存、移动设备，但又不希望它被训练，可以使用 <code>register_buffer</code>。<br>例如 BatchNorm 中的均值、方差统计量就属于这类状态。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">RunningMean</span><span class="token punctuation">(</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>register_buffer<span class="token punctuation">(</span><span class="token string">"mean"</span><span class="token punctuation">,</span> torch<span class="token punctuation">.</span>zeros<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> x <span class="token operator">-</span> self<span class="token punctuation">.</span>mean</code></pre><p><code>mean</code> 不会被优化器更新，但会出现在 <code>state_dict()</code> 中，也会跟随 <code>model.to(device)</code> 移动。</p><h3 id="10-3-state-dict-保存的是什么"><a href="#10-3-state-dict-保存的是什么" class="headerlink" title="10.3 state_dict() 保存的是什么"></a>10.3 <code>state_dict()</code> 保存的是什么</h3><p>训练完成后，一般不会直接保存整个模型对象，而是保存 <code>state_dict()</code>：</p><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>save<span class="token punctuation">(</span>model<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">"mlp.pth"</span><span class="token punctuation">)</span></code></pre><p><code>state_dict()</code> 本质上是一个字典，里面保存了模型中各层参数和 buffer 的名字与数值。</p><pre class="language-python" data-language="python"><code class="language-python">state <span class="token operator">=</span> model<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">for</span> key <span class="token keyword">in</span> state<span class="token punctuation">:</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>key<span class="token punctuation">,</span> state<span class="token punctuation">[</span>key<span class="token punctuation">]</span><span class="token punctuation">.</span>shape<span class="token punctuation">)</span></code></pre><p>可能输出：</p><pre class="language-text" data-language="text"><code class="language-text">fc1.weight torch.Size([4, 2])fc1.bias torch.Size([4])fc2.weight torch.Size([2, 4])fc2.bias torch.Size([2])</code></pre><p>加载时需要先创建同样结构的模型，再把参数填进去：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> SimpleNet<span class="token punctuation">(</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span>load_state_dict<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>load<span class="token punctuation">(</span><span class="token string">"mlp.pth"</span><span class="token punctuation">)</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><h3 id="10-4-train-和-eval-为什么重要"><a href="#10-4-train-和-eval-为什么重要" class="headerlink" title="10.4 train() 和 eval() 为什么重要"></a>10.4 <code>train()</code> 和 <code>eval()</code> 为什么重要</h3><p><code>model.train()</code> 和 <code>model.eval()</code> 不会直接训练或停止训练，它们只是切换模型内部某些层的工作模式。</p><p>最典型的是：</p><ul><li><code>Dropout</code>：训练时随机丢弃一部分神经元，推理时不丢弃。</li><li><code>BatchNorm</code>：训练时使用当前 batch 的统计量，推理时使用累计统计量。</li></ul><p>所以验证和推理时通常要写：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    pred <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">)</span></code></pre><p>如果忘记 <code>eval()</code>，带有 Dropout 或 BatchNorm 的模型在验证时可能表现不稳定。</p><h2 id="11-nn-Linear：全连接层"><a href="#11-nn-Linear：全连接层" class="headerlink" title="11. nn.Linear：全连接层"></a>11. <code>nn.Linear</code>：全连接层</h2><p>PyTorch 官方文档中，<code>nn.Linear</code> 表示对输入做仿射线性变换：</p><p>$$<br>y&#x3D;xA^T+b<br>$$</p><p>其中：</p><ul><li>$x$ 是输入。</li><li>$A$ 是权重矩阵。</li><li>$b$ 是偏置。</li></ul><p><code>nn.Linear</code>内部会自动创建<code>linear.weight</code>和<code>linear.bias</code>，并且<code>nn.Linear</code>的返回结果就是上面中的<code>y</code>，<code>x</code>为需要传入的参数。</p><p>例子：</p><pre class="language-python" data-language="python"><code class="language-python">layer <span class="token operator">=</span> nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>x <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">2.0</span><span class="token punctuation">,</span> <span class="token number">3.0</span><span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">)</span>y <span class="token operator">=</span> layer<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>y<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>  <span class="token comment"># torch.Size([1, 2])</span></code></pre><p>如果输入形状是：</p><pre class="language-text" data-language="text"><code class="language-text">[batch_size, in_features]</code></pre><p>输出形状就是：</p><pre class="language-text" data-language="text"><code class="language-text">[batch_size, out_features]</code></pre><p>例如：</p><pre class="language-python" data-language="python"><code class="language-python">nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span></code></pre><p>表示每个样本有 3 个输入特征，经过这一层后变成 2 个输出特征。</p><p>它内部可学习的参数包括：</p><ul><li><code>weight</code>：形状为 <code>[out_features, in_features]</code>。</li><li><code>bias</code>：形状为 <code>[out_features]</code>。</li></ul><h2 id="12-激活函数：ReLU、Sigmoid、Tanh、GELU"><a href="#12-激活函数：ReLU、Sigmoid、Tanh、GELU" class="headerlink" title="12. 激活函数：ReLU、Sigmoid、Tanh、GELU"></a>12. 激活函数：<code>ReLU</code>、<code>Sigmoid</code>、<code>Tanh</code>、<code>GELU</code></h2><h3 id="12-1-ReLU"><a href="#12-1-ReLU" class="headerlink" title="12.1 ReLU"></a>12.1 ReLU</h3><p>ReLU 是最常见的隐藏层激活函数：</p><p>$$<br>\text{ReLU}(x)&#x3D;\max(0,x)<br>$$</p><p>PyTorch 写法：</p><pre class="language-python" data-language="python"><code class="language-python">relu <span class="token operator">=</span> nn<span class="token punctuation">.</span>ReLU<span class="token punctuation">(</span><span class="token punctuation">)</span>x <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">2.0</span><span class="token punctuation">,</span> <span class="token number">0.5</span><span class="token punctuation">,</span> <span class="token number">3.0</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>relu<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>输出：</p><pre class="language-text" data-language="text"><code class="language-text">tensor([0.0000, 0.5000, 3.0000])</code></pre><h3 id="12-2-Sigmoid"><a href="#12-2-Sigmoid" class="headerlink" title="12.2 Sigmoid"></a>12.2 Sigmoid</h3><p>Sigmoid 常用于二分类概率或门控结构：</p><p>$$<br>\sigma(x)&#x3D;\frac{1}{1+e^{-x}}<br>$$</p><pre class="language-python" data-language="python"><code class="language-python">sigmoid <span class="token operator">=</span> nn<span class="token punctuation">.</span>Sigmoid<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>sigmoid<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">0.0</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>输出为 0.5。</p><h3 id="12-3-Tanh"><a href="#12-3-Tanh" class="headerlink" title="12.3 Tanh"></a>12.3 Tanh</h3><p>Tanh 输出范围是 $(-1,1)$：</p><p>$$<br>\tanh(x)&#x3D;\frac{e^x-e^{-x}}{e^x+e^{-x}}<br>$$</p><pre class="language-python" data-language="python"><code class="language-python">tanh <span class="token operator">=</span> nn<span class="token punctuation">.</span>Tanh<span class="token punctuation">(</span><span class="token punctuation">)</span>x <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">2.0</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">2.0</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>tanh<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>输出大致为：</p><pre class="language-text" data-language="text"><code class="language-text">tensor([-0.9640,  0.0000,  0.9640])</code></pre><p>它和 Sigmoid 的一个重要区别是：Tanh 的输出以 0 为中心。<br>也就是说，输出可以是负数、0 或正数。</p><p>这对隐藏层表示有时更友好，因为下一层接收到的信号不总是偏向正数。<br>在早期 RNN、LSTM 的一些门控计算中，Tanh 很常见。</p><p>不过 Tanh 也有一个问题：当输入绝对值很大时，输出会接近 -1 或 1，梯度会变得很小。<br>这叫饱和区间，可能导致深层网络训练变慢。</p><p>可以这样理解：</p><pre class="language-text" data-language="text"><code class="language-text">x 很大  -> tanh(x) 接近 1   -> 曲线变平 -> 梯度变小x 很小  -> tanh(x) 接近 -1  -> 曲线变平 -> 梯度变小x 接近 0 -> tanh(x) 变化明显 -> 梯度较大</code></pre><p>所以现在普通 MLP 的隐藏层更常用 ReLU 或 GELU，而不是大量堆叠 Tanh。</p><h3 id="12-4-GELU"><a href="#12-4-GELU" class="headerlink" title="12.4 GELU"></a>12.4 GELU</h3><p>GELU 是 Transformer 中常见的激活函数，比 ReLU 更平滑。</p><p>它的全称是 Gaussian Error Linear Unit，可以粗略理解为：</p><blockquote><p>根据输入值的大小，用一个平滑的概率门控决定保留多少输入。</p></blockquote><p>常见公式写成：</p><p>$$<br>\text{GELU}(x)&#x3D;x\Phi(x)<br>$$</p><p>其中 $\Phi(x)$ 是标准正态分布的累积分布函数。</p><p>PyTorch 写法：</p><pre class="language-python" data-language="python"><code class="language-python">gelu <span class="token operator">=</span> nn<span class="token punctuation">.</span>GELU<span class="token punctuation">(</span><span class="token punctuation">)</span>x <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token operator">-</span><span class="token number">2.0</span><span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">0.5</span><span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">2.0</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>gelu<span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>输出大致为：</p><pre class="language-text" data-language="text"><code class="language-text">tensor([-0.0455, -0.1543,  0.0000,  0.8413,  1.9545])</code></pre><p>注意 GELU 和 ReLU 的差别：</p><ul><li>ReLU 对负数直接截断为 0。</li><li>GELU 对负数不是完全丢弃，而是平滑地压小。</li><li>ReLU 在 0 点有明显折角。</li><li>GELU 曲线更平滑，梯度变化更连续。</li></ul><p>这也是为什么 BERT、GPT、ViT 等 Transformer 风格模型中经常使用 GELU。</p><p>如果只是在普通表格数据上训练一个小 MLP，ReLU 通常已经足够；<br>如果网络较深，或者结构接近 Transformer 的前馈网络，可以优先考虑 GELU。</p><h3 id="12-5-激活函数怎么选"><a href="#12-5-激活函数怎么选" class="headerlink" title="12.5 激活函数怎么选"></a>12.5 激活函数怎么选</h3><p>可以先用下面这个经验表：</p><table><thead><tr><th>场景</th><th>推荐激活函数</th><th>原因</th></tr></thead><tbody><tr><td>普通 MLP 隐藏层</td><td>ReLU</td><td>简单、高效、训练稳定</td></tr><tr><td>输出二分类概率</td><td>Sigmoid</td><td>输出范围是 $(0,1)$</td></tr><tr><td>希望隐藏表示有正有负</td><td>Tanh</td><td>输出以 0 为中心</td></tr><tr><td>Transformer 风格前馈层</td><td>GELU</td><td>平滑，保留负值信息</td></tr><tr><td>多分类概率解释</td><td>Softmax</td><td>把 logits 归一化为概率</td></tr></tbody></table><p>需要注意：多分类训练时，PyTorch 的 <code>nn.CrossEntropyLoss()</code> 内部已经包含了 log-softmax，<br>所以模型最后一层通常直接输出 logits，不需要手动加 <code>Softmax</code>。</p><h2 id="13-nn-Sequential：顺序堆叠网络"><a href="#13-nn-Sequential：顺序堆叠网络" class="headerlink" title="13. nn.Sequential：顺序堆叠网络"></a>13. <code>nn.Sequential</code>：顺序堆叠网络</h2><p>如果模型就是一层接一层地执行，可以用 <code>nn.Sequential</code> 简化代码：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> nn<span class="token punctuation">.</span>Sequential<span class="token punctuation">(</span>    nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">16</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>ReLU<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">16</span><span class="token punctuation">,</span> <span class="token number">16</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>ReLU<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">16</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>它等价于：</p><pre class="language-text" data-language="text"><code class="language-text">x -> Linear -> ReLU -> Linear -> ReLU -> Linear</code></pre><p>适合结构简单的 MLP。</p><p>如果模型中有分支、多个输入、条件判断，就更适合自定义 <code>nn.Module</code> 并手写 <code>forward</code>。</p><h2 id="14-损失函数：模型到底错在哪里"><a href="#14-损失函数：模型到底错在哪里" class="headerlink" title="14. 损失函数：模型到底错在哪里"></a>14. 损失函数：模型到底错在哪里</h2><p>损失函数用于衡量预测值和真实标签之间的差距。</p><h3 id="14-1-nn-MSELoss"><a href="#14-1-nn-MSELoss" class="headerlink" title="14.1 nn.MSELoss"></a>14.1 <code>nn.MSELoss</code></h3><p>均方误差常用于回归：</p><p>$$<br>\text{MSE}&#x3D;\frac{1}{n}\sum_{i&#x3D;1}^{n}(\hat{y}_i-y_i)^2<br>$$</p><pre class="language-python" data-language="python"><code class="language-python">loss_fn <span class="token operator">=</span> nn<span class="token punctuation">.</span>MSELoss<span class="token punctuation">(</span><span class="token punctuation">)</span>pred <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">2.5</span><span class="token punctuation">,</span> <span class="token number">3.0</span><span class="token punctuation">]</span><span class="token punctuation">)</span>target <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">2.0</span><span class="token punctuation">,</span> <span class="token number">4.0</span><span class="token punctuation">]</span><span class="token punctuation">)</span>loss <span class="token operator">=</span> loss_fn<span class="token punctuation">(</span>pred<span class="token punctuation">,</span> target<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>loss<span class="token punctuation">)</span></code></pre><p>手工计算：</p><p>$$<br>\frac{(2.5-2.0)^2+(3.0-4.0)^2}{2}<br>&#x3D;<br>\frac{0.25+1}{2}<br>&#x3D;0.625<br>$$</p><h3 id="14-2-nn-CrossEntropyLoss"><a href="#14-2-nn-CrossEntropyLoss" class="headerlink" title="14.2 nn.CrossEntropyLoss"></a>14.2 <code>nn.CrossEntropyLoss</code></h3><p>交叉熵常用于多分类。</p><p>PyTorch 官方文档说明，<code>CrossEntropyLoss</code> 用于计算输入 logits 和目标标签之间的交叉熵；当目标是类别索引时，它等价于先做 <code>LogSoftmax</code>，再做 <code>NLLLoss</code>。</p><p>所以训练多分类模型时，通常这样写：</p><pre class="language-python" data-language="python"><code class="language-python">criterion <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span><span class="token punctuation">)</span>logits <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">)</span>loss <span class="token operator">=</span> criterion<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> y<span class="token punctuation">)</span></code></pre><p>不要提前写：</p><pre class="language-python" data-language="python"><code class="language-python">prob <span class="token operator">=</span> torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>loss <span class="token operator">=</span> criterion<span class="token punctuation">(</span>prob<span class="token punctuation">,</span> y<span class="token punctuation">)</span>  <span class="token comment"># 不推荐</span></code></pre><p>因为 <code>CrossEntropyLoss</code> 期望输入是未归一化的 logits。</p><p>一个简单例子：</p><pre class="language-python" data-language="python"><code class="language-python">logits <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token number">2.0</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token number">0.1</span><span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">)</span>target <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span>criterion <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span><span class="token punctuation">)</span>loss <span class="token operator">=</span> criterion<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> target<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>loss<span class="token punctuation">)</span></code></pre><p>这里标签 0 表示正确类别是第 0 类。第 0 类 logit 最大，所以损失会相对较小。</p><h3 id="14-3-nn-BCEWithLogitsLoss"><a href="#14-3-nn-BCEWithLogitsLoss" class="headerlink" title="14.3 nn.BCEWithLogitsLoss"></a>14.3 <code>nn.BCEWithLogitsLoss</code></h3><p>二分类任务常用 <code>BCEWithLogitsLoss</code>。</p><p>它把 sigmoid 和二元交叉熵结合到一起，数值稳定性比手动 <code>Sigmoid + BCELoss</code> 更好。</p><pre class="language-python" data-language="python"><code class="language-python">criterion <span class="token operator">=</span> nn<span class="token punctuation">.</span>BCEWithLogitsLoss<span class="token punctuation">(</span><span class="token punctuation">)</span>logit <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">0.8</span><span class="token punctuation">]</span><span class="token punctuation">)</span>target <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token number">1.0</span><span class="token punctuation">]</span><span class="token punctuation">)</span>loss <span class="token operator">=</span> criterion<span class="token punctuation">(</span>logit<span class="token punctuation">,</span> target<span class="token punctuation">)</span></code></pre><p>如果是单标签二分类，也可以用两个输出神经元加 <code>CrossEntropyLoss</code>；如果是多标签分类，通常用 <code>BCEWithLogitsLoss</code>。</p><h2 id="15-自动求导：loss-backward"><a href="#15-自动求导：loss-backward" class="headerlink" title="15. 自动求导：loss.backward()"></a>15. 自动求导：<code>loss.backward()</code></h2><p>PyTorch 的自动求导系统叫 autograd。</p><p>在前向传播时，PyTorch 会记录张量之间的计算关系；调用 <code>loss.backward()</code> 时，它会根据链式法则自动计算梯度。</p><p><img src="https://lsworl.github.io/images/ml-blog/mlp-autograd-graph.svg" alt="Autograd 会记录前向计算图并在 backward 时计算梯度"></p><p>例子：</p><pre class="language-python" data-language="python"><code class="language-python">x <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token number">2.0</span><span class="token punctuation">,</span> requires_grad<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>w <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token number">3.0</span><span class="token punctuation">,</span> requires_grad<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>b <span class="token operator">=</span> torch<span class="token punctuation">.</span>tensor<span class="token punctuation">(</span><span class="token number">1.0</span><span class="token punctuation">,</span> requires_grad<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>y <span class="token operator">=</span> w <span class="token operator">*</span> x <span class="token operator">+</span> bloss <span class="token operator">=</span> <span class="token punctuation">(</span>y <span class="token operator">-</span> <span class="token number">10</span><span class="token punctuation">)</span> <span class="token operator">**</span> <span class="token number">2</span>loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>w<span class="token punctuation">.</span>grad<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>b<span class="token punctuation">.</span>grad<span class="token punctuation">)</span></code></pre><p>手工理解：</p><p>$$<br>y&#x3D;wx+b&#x3D;3\times2+1&#x3D;7<br>$$</p><p>$$<br>loss&#x3D;(7-10)^2&#x3D;9<br>$$</p><p>对 $w$ 求导：</p><p>$$<br>\frac{\partial loss}{\partial w}<br>&#x3D;<br>2(y-10)\frac{\partial y}{\partial w}<br>&#x3D;<br>2(7-10)\times2<br>&#x3D;-12<br>$$</p><p>对 $b$ 求导：</p><p>$$<br>\frac{\partial loss}{\partial b}<br>&#x3D;<br>2(y-10)\frac{\partial y}{\partial b}<br>&#x3D;<br>2(7-10)\times1<br>&#x3D;-6<br>$$</p><p>所以 <code>w.grad</code> 是 <code>-12</code>，<code>b.grad</code> 是 <code>-6</code>。</p><h2 id="16-优化器：SGD-和-Adam"><a href="#16-优化器：SGD-和-Adam" class="headerlink" title="16. 优化器：SGD 和 Adam"></a>16. 优化器：<code>SGD</code> 和 <code>Adam</code></h2><p>优化器负责根据梯度更新参数。</p><p>最基础的是 SGD：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>SGD<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">0.01</span><span class="token punctuation">)</span></code></pre><p>它的思想是：</p><p>$$<br>\theta :&#x3D; \theta-\alpha\nabla_\theta \mathcal{L}<br>$$</p><p>Adam 更常用：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> lr<span class="token operator">=</span><span class="token number">0.001</span><span class="token punctuation">)</span></code></pre><p>Adam 会维护梯度的一阶矩估计和二阶矩估计，可以理解为同时考虑“梯度方向”和“梯度变化尺度”，通常比普通 SGD 更省心。</p><p>典型训练步骤是：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>为什么要 <code>zero_grad()</code>？</p><p>因为 PyTorch 默认会累积梯度。如果不清零，下一次 <code>backward()</code> 的梯度会加到上一次梯度上。</p><p>这在某些梯度累积场景有用，但普通训练中通常每个 batch 都要清零。</p><h2 id="17-Dataset-和-DataLoader"><a href="#17-Dataset-和-DataLoader" class="headerlink" title="17. Dataset 和 DataLoader"></a>17. <code>Dataset</code> 和 <code>DataLoader</code></h2><p>神经网络通常使用 mini-batch 训练。</p><p>PyTorch 中：</p><ul><li><code>Dataset</code> 负责定义“如何取一个样本”。</li><li><code>DataLoader</code> 负责批量加载、打乱和并行读取。</li></ul><p>最简单可以用 <code>TensorDataset</code>：</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> torch<span class="token punctuation">.</span>utils<span class="token punctuation">.</span>data <span class="token keyword">import</span> TensorDataset<span class="token punctuation">,</span> DataLoaderX <span class="token operator">=</span> torch<span class="token punctuation">.</span>randn<span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>y <span class="token operator">=</span> torch<span class="token punctuation">.</span>randint<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">)</span>dataset <span class="token operator">=</span> TensorDataset<span class="token punctuation">(</span>X<span class="token punctuation">,</span> y<span class="token punctuation">)</span>loader <span class="token operator">=</span> DataLoader<span class="token punctuation">(</span>    dataset<span class="token punctuation">,</span>    batch_size<span class="token operator">=</span><span class="token number">16</span><span class="token punctuation">,</span>    shuffle<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">for</span> batch_x<span class="token punctuation">,</span> batch_y <span class="token keyword">in</span> loader<span class="token punctuation">:</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>batch_x<span class="token punctuation">.</span>shape<span class="token punctuation">,</span> batch_y<span class="token punctuation">.</span>shape<span class="token punctuation">)</span>    <span class="token keyword">break</span></code></pre><p>输出形状大致是：</p><pre class="language-text" data-language="text"><code class="language-text">torch.Size([16, 2]) torch.Size([16])</code></pre><p>这说明每次取出 16 个样本。</p><h2 id="18-train-、eval-和-no-grad"><a href="#18-train-、eval-和-no-grad" class="headerlink" title="18. train()、eval() 和 no_grad()"></a>18. <code>train()</code>、<code>eval()</code> 和 <code>no_grad()</code></h2><p>训练和推理时，模型行为可能不同。</p><p>例如：</p><ul><li>Dropout 训练时会随机丢弃部分神经元，推理时不会。</li><li>BatchNorm 训练时使用当前 batch 统计量，推理时使用累计统计量。</li></ul><p>所以训练时要写：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>验证或推理时要写：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>推理时还应关闭梯度记录：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">with</span> torch<span class="token punctuation">.</span>no_grad<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">)</span>    pred <span class="token operator">=</span> torch<span class="token punctuation">.</span>argmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre><p>这样可以减少显存和计算开销。</p><h2 id="19-Dropout-和-BatchNorm"><a href="#19-Dropout-和-BatchNorm" class="headerlink" title="19. Dropout 和 BatchNorm"></a>19. Dropout 和 BatchNorm</h2><h3 id="19-1-Dropout"><a href="#19-1-Dropout" class="headerlink" title="19.1 Dropout"></a>19.1 Dropout</h3><p>Dropout 是一种正则化方法。训练时，它会随机把一部分神经元输出置 0，减少模型对某些特征的过度依赖。</p><pre class="language-python" data-language="python"><code class="language-python">nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span>p<span class="token operator">=</span><span class="token number">0.5</span><span class="token punctuation">)</span></code></pre><p>表示训练时每个元素有 50% 概率被置 0。</p><p>常见写法：</p><pre class="language-python" data-language="python"><code class="language-python">self<span class="token punctuation">.</span>net <span class="token operator">=</span> nn<span class="token punctuation">.</span>Sequential<span class="token punctuation">(</span>    nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">20</span><span class="token punctuation">,</span> <span class="token number">64</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>ReLU<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>Dropout<span class="token punctuation">(</span><span class="token number">0.5</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">64</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><h3 id="19-2-BatchNorm"><a href="#19-2-BatchNorm" class="headerlink" title="19.2 BatchNorm"></a>19.2 BatchNorm</h3><p>BatchNorm 会对一批样本的中间激活做标准化，再学习缩放和平移参数。</p><pre class="language-python" data-language="python"><code class="language-python">nn<span class="token punctuation">.</span>BatchNorm1d<span class="token punctuation">(</span><span class="token number">64</span><span class="token punctuation">)</span></code></pre><p>常见写法：</p><pre class="language-python" data-language="python"><code class="language-python">self<span class="token punctuation">.</span>net <span class="token operator">=</span> nn<span class="token punctuation">.</span>Sequential<span class="token punctuation">(</span>    nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">20</span><span class="token punctuation">,</span> <span class="token number">64</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>BatchNorm1d<span class="token punctuation">(</span><span class="token number">64</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>ReLU<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">64</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">)</span></code></pre><p>它可以让训练更稳定，但小 batch 或分布变化明显时要谨慎使用。</p><h2 id="20-保存和加载模型"><a href="#20-保存和加载模型" class="headerlink" title="20. 保存和加载模型"></a>20. 保存和加载模型</h2><p>训练完模型后，通常保存参数字典：</p><pre class="language-python" data-language="python"><code class="language-python">torch<span class="token punctuation">.</span>save<span class="token punctuation">(</span>model<span class="token punctuation">.</span>state_dict<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">"mlp.pth"</span><span class="token punctuation">)</span></code></pre><p>加载时：</p><pre class="language-python" data-language="python"><code class="language-python">model <span class="token operator">=</span> MLP<span class="token punctuation">(</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span>load_state_dict<span class="token punctuation">(</span>torch<span class="token punctuation">.</span>load<span class="token punctuation">(</span><span class="token string">"mlp.pth"</span><span class="token punctuation">)</span><span class="token punctuation">)</span>model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p><code>state_dict</code> 本质上是一个字典，保存了每一层的权重和偏置。</p><p>相比直接保存整个模型对象，保存 <code>state_dict</code> 更常见，也更灵活。</p><h2 id="21-常见错误和排查"><a href="#21-常见错误和排查" class="headerlink" title="21. 常见错误和排查"></a>21. 常见错误和排查</h2><h3 id="21-1-CrossEntropyLoss-前手动-softmax"><a href="#21-1-CrossEntropyLoss-前手动-softmax" class="headerlink" title="21.1 CrossEntropyLoss 前手动 softmax"></a>21.1 CrossEntropyLoss 前手动 softmax</h3><p>错误写法：</p><pre class="language-python" data-language="python"><code class="language-python">prob <span class="token operator">=</span> torch<span class="token punctuation">.</span>softmax<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> dim<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span>loss <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">(</span>prob<span class="token punctuation">,</span> y<span class="token punctuation">)</span></code></pre><p>正确写法：</p><pre class="language-python" data-language="python"><code class="language-python">loss <span class="token operator">=</span> nn<span class="token punctuation">.</span>CrossEntropyLoss<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">(</span>logits<span class="token punctuation">,</span> y<span class="token punctuation">)</span></code></pre><h3 id="21-2-标签类型不对"><a href="#21-2-标签类型不对" class="headerlink" title="21.2 标签类型不对"></a>21.2 标签类型不对</h3><p><code>CrossEntropyLoss</code> 的类别索引标签应是 <code>LongTensor</code>：</p><pre class="language-python" data-language="python"><code class="language-python">y <span class="token operator">=</span> y<span class="token punctuation">.</span><span class="token builtin">long</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>如果标签是 float，可能会报错或行为不符合预期。</p><h3 id="21-3-忘记清空梯度"><a href="#21-3-忘记清空梯度" class="headerlink" title="21.3 忘记清空梯度"></a>21.3 忘记清空梯度</h3><p>普通训练中，每个 batch 前要：</p><pre class="language-python" data-language="python"><code class="language-python">optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>否则梯度会累积。</p><h3 id="21-4-训练和推理模式没切换"><a href="#21-4-训练和推理模式没切换" class="headerlink" title="21.4 训练和推理模式没切换"></a>21.4 训练和推理模式没切换</h3><p>训练：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>验证：</p><pre class="language-python" data-language="python"><code class="language-python">model<span class="token punctuation">.</span><span class="token builtin">eval</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>尤其是模型中有 Dropout 或 BatchNorm 时，这一点很重要。</p><h3 id="21-5-维度不匹配"><a href="#21-5-维度不匹配" class="headerlink" title="21.5 维度不匹配"></a>21.5 维度不匹配</h3><p><code>nn.Linear(in_features, out_features)</code> 的 <code>in_features</code> 必须和输入最后一维一致。</p><p>例如输入是：</p><pre class="language-text" data-language="text"><code class="language-text">[batch_size, 20]</code></pre><p>第一层就应该是：</p><pre class="language-python" data-language="python"><code class="language-python">nn<span class="token punctuation">.</span>Linear<span class="token punctuation">(</span><span class="token number">20</span><span class="token punctuation">,</span> hidden_dim<span class="token punctuation">)</span></code></pre><h2 id="22-MLP-的优点和局限"><a href="#22-MLP-的优点和局限" class="headerlink" title="22. MLP 的优点和局限"></a>22. MLP 的优点和局限</h2><p>MLP 的优点：</p><ul><li>结构简单，容易理解。</li><li>适合表格数据、向量特征和小型任务。</li><li>可以作为很多复杂模型的基础模块。</li><li>PyTorch 实现非常直接。</li></ul><p>MLP 的局限：</p><ul><li>对图片、序列、图这类有结构的数据，不能天然利用局部关系或拓扑关系。</li><li>参数量可能较大。</li><li>对特征缩放比较敏感。</li><li>缺少 CNN、RNN、Transformer、GNN 那样的结构先验。</li></ul><p>所以：</p><pre class="language-text" data-language="text"><code class="language-text">表格/向量特征：MLP 常常是强 baseline。图片：CNN 或 ViT 往往更合适。序列：RNN、Transformer 更自然。图数据：GNN 更适合。</code></pre><h2 id="23-总结"><a href="#23-总结" class="headerlink" title="23. 总结"></a>23. 总结</h2><p>MLP 是理解深度学习和 PyTorch 的好入口。</p><p>它的核心可以概括为：</p><pre class="language-text" data-language="text"><code class="language-text">线性层负责特征变换，激活函数负责引入非线性，损失函数衡量预测错误，自动求导计算梯度，优化器更新参数。</code></pre><p>从公式上看，一层 MLP 就是：</p><p>$$<br>h&#x3D;\sigma(xW+b)<br>$$</p><p>多层堆叠后，就能表达比线性模型复杂得多的函数。</p><p>从 PyTorch 角度看，最重要的是掌握这条训练主线：</p><pre class="language-python" data-language="python"><code class="language-python">logits <span class="token operator">=</span> model<span class="token punctuation">(</span>x<span class="token punctuation">)</span>loss <span class="token operator">=</span> criterion<span class="token punctuation">(</span>logits<span class="token punctuation">,</span> y<span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p>本文主要参考了以下资料：</p><ol><li>PyTorch Documentation, <code>torch.nn.Linear</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.Linear.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.Linear.html</a></li><li>PyTorch Documentation, <code>torch.nn.CrossEntropyLoss</code>: <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html">https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html</a></li><li>PyTorch Tutorials, <em>Learn the Basics</em>: <a href="https://docs.pytorch.org/tutorials/beginner/basics/intro.html">https://docs.pytorch.org/tutorials/beginner/basics/intro.html</a></li><li>PyTorch Tutorials, <em>Automatic Differentiation with torch.autograd</em>: <a href="https://docs.pytorch.org/tutorials/beginner/basics/autogradqs_tutorial.html">https://docs.pytorch.org/tutorials/beginner/basics/autogradqs_tutorial.html</a></li><li>PyTorch Tutorials, <em>Optimizing Model Parameters</em>: <a href="https://docs.pytorch.org/tutorials/beginner/basics/optimization_tutorial.html">https://docs.pytorch.org/tutorials/beginner/basics/optimization_tutorial.html</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/07/05/machine-learning-mlp-pytorch/</id>
    <link href="https://lsworl.github.io/2026/07/05/machine-learning-mlp-pytorch/"/>
    <published>2026-07-04T16:00:00.000Z</published>
    <summary>从多层感知机的结构、前向传播、反向传播、PyTorch实现和常用神经网络函数几个角度理解MLP与PyTorch训练流程。</summary>
    <title>MLP多层感知机：从原理到PyTorch实现</title>
    <updated>2026-08-06T11:48:22.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="机器学习" scheme="https://lsworl.github.io/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="机器学习" scheme="https://lsworl.github.io/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="深度学习" scheme="https://lsworl.github.io/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    <category term="图神经网络" scheme="https://lsworl.github.io/tags/%E5%9B%BE%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C/"/>
    <category term="GNN" scheme="https://lsworl.github.io/tags/GNN/"/>
    <category term="GCN" scheme="https://lsworl.github.io/tags/GCN/"/>
    <content>
      <![CDATA[<p>前面学习线性回归、逻辑回归、决策树、GBDT 这类模型时，我们面对的数据通常是表格、向量或图片。每个样本大多可以单独看待。</p><p>但现实中还有很多数据天然带有“关系”：</p><ul><li>社交网络中，用户之间有好友关系。</li><li>推荐系统中，用户、商品、点击、购买构成交互图。</li><li>分子结构中，原子是节点，化学键是边。</li><li>交通网络中，路口是节点，道路是边。</li><li>知识图谱中，实体和实体之间有语义关系。</li></ul><p>这些数据的重点不只在于“单个对象有什么特征”，还在于“对象之间如何连接”。图神经网络（Graph Neural Network，GNN）就是专门用来处理这类图结构数据的神经网络。</p><p>一句话概括：</p><blockquote><p>GNN 让每个节点不断接收邻居的信息，更新自己的表示，从而把节点特征和图结构一起编码进向量里。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/ml-blog/gnn-graph-data-overview.svg" alt="图数据由节点、边、特征和结构共同组成"></p><h2 id="1-为什么普通神经网络不够用"><a href="#1-为什么普通神经网络不够用" class="headerlink" title="1. 为什么普通神经网络不够用"></a>1. 为什么普通神经网络不够用</h2><p>假设我们要判断一个社交网络用户是否可能对某个话题感兴趣。</p><p>如果只看用户自己的特征，例如年龄、地区、历史点击，当然能得到一些信息。</p><p>但在社交网络里，用户的邻居也很重要：</p><ul><li>他的朋友是否都关注这个话题？</li><li>他是否处在某个兴趣社区中？</li><li>他和哪些关键用户有连接？</li><li>他在网络中的位置是否特殊？</li></ul><p>普通全连接神经网络通常假设输入是固定长度向量，不能直接表达“这个节点连着哪些节点”。</p><p>CNN 适合图片，因为图片像素排列在规则网格上；RNN&#x2F;Transformer 适合序列，因为序列有明确顺序。</p><p>图数据不一样。它没有固定网格，也没有天然顺序。每个节点的邻居数量可能不同，图的大小也可能不同。</p><p>所以我们需要一种模型，既能利用节点本身的特征，又能利用节点之间的连接关系。</p><p>这就是 GNN 出现的原因。</p><h2 id="2-图数据如何表示"><a href="#2-图数据如何表示" class="headerlink" title="2. 图数据如何表示"></a>2. 图数据如何表示</h2><p>一个图通常写成：</p><p>$$<br>G&#x3D;(V,E)<br>$$</p><p>其中：</p><ul><li>$V$ 是节点集合。</li><li>$E$ 是边集合。</li></ul><p>如果图中有 $n$ 个节点，每个节点有 $d$ 维特征，那么节点特征矩阵可以写成：</p><p>$$<br>X \in \mathbb{R}^{n \times d}<br>$$</p><p>图的连接关系常用邻接矩阵表示：</p><p>$$<br>A \in \mathbb{R}^{n \times n}<br>$$</p><p>如果节点 $i$ 和节点 $j$ 之间有边，那么：</p><p>$$<br>A_{ij}&#x3D;1<br>$$</p><p>否则：</p><p>$$<br>A_{ij}&#x3D;0<br>$$</p><p>在工程实现中，尤其是稀疏图里，通常不会真的存一个巨大的邻接矩阵，而是用边列表：</p><pre class="language-text" data-language="text"><code class="language-text">edge_index = [  [0, 1, 1, 2],  [1, 0, 2, 1],]</code></pre><p>它表示边：</p><pre class="language-text" data-language="text"><code class="language-text">0 -> 11 -> 01 -> 22 -> 1</code></pre><p>这比存完整矩阵更节省空间。</p><h2 id="3-GNN-的核心思想：消息传递"><a href="#3-GNN-的核心思想：消息传递" class="headerlink" title="3. GNN 的核心思想：消息传递"></a>3. GNN 的核心思想：消息传递</h2><p>大多数 GNN 都可以用消息传递（Message Passing）来理解。</p><p>对于某个节点，它会做三件事：</p><ol><li>从邻居节点接收消息。</li><li>把邻居消息聚合起来。</li><li>用聚合后的信息更新自己的节点表示。</li></ol><p><img src="https://lsworl.github.io/images/ml-blog/gnn-message-passing.svg" alt="GNN 每一层都让节点接收邻居消息并更新自身表示"></p><p>可以写成一个通用形式：</p><p>$$<br>h_v^{(k)} &#x3D;<br>\text{UPDATE}^{(k)}<br>\left(<br>h_v^{(k-1)},<br>\text{AGGREGATE}^{(k)}<br>\left(<br>{h_u^{(k-1)}:u\in \mathcal{N}(v)}<br>\right)<br>\right)<br>$$</p><p>其中：</p><ul><li>$h_v^{(k)}$ 表示节点 $v$ 在第 $k$ 层的表示。</li><li>$\mathcal{N}(v)$ 表示节点 $v$ 的邻居集合。</li><li>AGGREGATE 表示邻居聚合函数。</li><li>UPDATE 表示节点更新函数。</li></ul><p>直观理解：</p><pre class="language-text" data-language="text"><code class="language-text">第 1 层：节点看到一跳邻居的信息。第 2 层：节点看到两跳邻居的信息。第 3 层：节点看到三跳邻居的信息。</code></pre><p>层数越深，节点能接收到越远的图结构信息。但层数也不能无限加深，因为可能出现过平滑问题，后面会讲。</p><h3 id="3-1-一个最简单的消息传递计算例子"><a href="#3-1-一个最简单的消息传递计算例子" class="headerlink" title="3.1 一个最简单的消息传递计算例子"></a>3.1 一个最简单的消息传递计算例子</h3><p>先不看复杂公式，直接用一个小图来算。</p><p>假设有 4 个节点：</p><pre class="language-text" data-language="text"><code class="language-text">0 -- 1 -- 2     |     3</code></pre><p>也就是说，节点 1 的邻居是节点 0、节点 2、节点 3。</p><p>假设每个节点只有一个简单特征，表示用户对某个话题的兴趣分数：</p><table><thead><tr><th>节点</th><th align="right">原始特征</th></tr></thead><tbody><tr><td>0</td><td align="right">2</td></tr><tr><td>1</td><td align="right">4</td></tr><tr><td>2</td><td align="right">6</td></tr><tr><td>3</td><td align="right">8</td></tr></tbody></table><p>现在我们想更新节点 1 的表示。最简单的做法是把邻居特征取平均：</p><p>$$<br>\text{neighbor_mean}(1)&#x3D;\frac{2+6+8}{3}&#x3D;\frac{16}{3}\approx5.33<br>$$</p><p>如果还想保留节点 1 自己的信息，可以把自己也放进去：</p><p>$$<br>h_1’&#x3D;\frac{2+4+6+8}{4}&#x3D;5<br>$$</p><p>这一步的含义是：节点 1 的新表示不再只是自己的原始特征 4，而是融合了周围邻居的信息，变成了 5。</p><p>如果这是一个社交网络，就可以理解为：</p><pre class="language-text" data-language="text"><code class="language-text">一个用户自己的兴趣会受朋友兴趣影响。GNN 的一层消息传递，就是把朋友的信息混进自己的表示中。</code></pre><p>再看节点 0。节点 0 只有一个邻居节点 1，如果也加上自环：</p><p>$$<br>h_0’&#x3D;\frac{2+4}{2}&#x3D;3<br>$$</p><p>所以经过一层最简单的平均聚合后，节点表示变成：</p><table><thead><tr><th>节点</th><th align="right">更新前</th><th align="right">更新后</th></tr></thead><tbody><tr><td>0</td><td align="right">2</td><td align="right">3</td></tr><tr><td>1</td><td align="right">4</td><td align="right">5</td></tr><tr><td>2</td><td align="right">6</td><td align="right">5</td></tr><tr><td>3</td><td align="right">8</td><td align="right">6</td></tr></tbody></table><p>这张表比公式更直观：GNN 的一层操作会让每个节点向邻居“借一点信息”。</p><h2 id="4-一个小例子：判断节点类别"><a href="#4-一个小例子：判断节点类别" class="headerlink" title="4. 一个小例子：判断节点类别"></a>4. 一个小例子：判断节点类别</h2><p>假设有一个论文引用网络：</p><ul><li>节点表示论文。</li><li>边表示引用关系。</li><li>节点特征是论文标题和摘要提取出的向量。</li><li>标签是论文所属领域，例如机器学习、数据库、计算机视觉。</li></ul><p>如果只看单篇论文的文字特征，模型可以做分类。</p><p>但引用关系也很有用：</p><pre class="language-text" data-language="text"><code class="language-text">一篇论文引用了很多图神经网络论文，它自己也很可能和图学习相关。</code></pre><p>GNN 会让每篇论文聚合相邻论文的信息。这样，一个节点的最终表示不仅包含自己的摘要信息，也包含它引用了谁、被谁引用、处在哪个研究社区中。</p><p>最后用节点表示做分类：</p><p>$$<br>\hat{y}_v &#x3D; \text{softmax}(W h_v)<br>$$</p><p>这就是节点分类任务。</p><h2 id="5-GNN-能解决哪些任务"><a href="#5-GNN-能解决哪些任务" class="headerlink" title="5. GNN 能解决哪些任务"></a>5. GNN 能解决哪些任务</h2><p>GNN 的预测目标可以放在不同层级。</p><p><img src="https://lsworl.github.io/images/ml-blog/gnn-task-types.svg" alt="GNN 可以做节点级、边级和图级任务"></p><h3 id="5-1-节点级任务"><a href="#5-1-节点级任务" class="headerlink" title="5.1 节点级任务"></a>5.1 节点级任务</h3><p>节点级任务是给每个节点预测标签。</p><p>例如：</p><ul><li>社交网络中预测用户兴趣。</li><li>引用网络中预测论文类别。</li><li>欺诈检测中预测账户是否异常。</li></ul><h3 id="5-2-边级任务"><a href="#5-2-边级任务" class="headerlink" title="5.2 边级任务"></a>5.2 边级任务</h3><p>边级任务是预测两个节点之间的关系。</p><p>例如：</p><ul><li>推荐系统中预测用户是否会点击商品。</li><li>社交网络中预测两个人是否可能成为好友。</li><li>知识图谱中预测实体之间是否存在某种关系。</li></ul><p>这类问题也叫链接预测（Link Prediction）。</p><h3 id="5-3-图级任务"><a href="#5-3-图级任务" class="headerlink" title="5.3 图级任务"></a>5.3 图级任务</h3><p>图级任务是给整个图预测标签。</p><p>例如：</p><ul><li>判断一个分子是否有毒。</li><li>判断一个代码函数是否存在漏洞。</li><li>判断一个蛋白质结构属于哪一类。</li></ul><p>图级任务通常需要先得到每个节点的表示，再通过 readout 或 pooling 操作得到整张图的表示。</p><h2 id="6-GCN：最经典的图卷积网络"><a href="#6-GCN：最经典的图卷积网络" class="headerlink" title="6. GCN：最经典的图卷积网络"></a>6. GCN：最经典的图卷积网络</h2><p>GCN（Graph Convolutional Network）是最经典、最常见的 GNN 模型之一。</p><p>GCN 的直觉非常简单：</p><blockquote><p>一个节点的新表示，来自它自己和邻居节点表示的加权平均，再经过线性变换和非线性激活。</p></blockquote><p>如果直接把邻居特征相加，会有一个问题：度数大的节点会收到更多信息，数值规模可能变大。</p><p>所以 GCN 会做归一化。常见形式是：</p><p>$$<br>H^{(l+1)}<br>&#x3D;<br>\sigma<br>\left(<br>\tilde{D}^{-\frac{1}{2}}<br>\tilde{A}<br>\tilde{D}^{-\frac{1}{2}}<br>H^{(l)}<br>W^{(l)}<br>\right)<br>$$</p><p>其中：</p><ul><li>$\tilde{A}&#x3D;A+I$，表示给图加自环，让节点也保留自己的信息。</li><li>$\tilde{D}$ 是 $\tilde{A}$ 的度矩阵。</li><li>$H^{(l)}$ 是第 $l$ 层节点表示。</li><li>$W^{(l)}$ 是可学习参数。</li><li>$\sigma$ 是激活函数，例如 ReLU。</li></ul><p><img src="https://lsworl.github.io/images/ml-blog/gnn-gcn-formula-flow.svg" alt="GCN 层可以理解为归一化邻居聚合加线性变换"></p><p>这个公式看起来很像矩阵运算版的消息传递：</p><pre class="language-text" data-language="text"><code class="language-text">邻接矩阵决定从哪些邻居接收消息度矩阵负责归一化权重矩阵负责特征变换激活函数引入非线性</code></pre><h2 id="7-手工理解一次-GCN-聚合"><a href="#7-手工理解一次-GCN-聚合" class="headerlink" title="7. 手工理解一次 GCN 聚合"></a>7. 手工理解一次 GCN 聚合</h2><p>假设节点 1 有两个邻居：节点 0 和节点 2。为了简单，先不考虑复杂归一化，只做平均聚合。</p><p>三个节点的特征为：</p><table><thead><tr><th>节点</th><th>特征</th></tr></thead><tbody><tr><td>0</td><td>$[1, 0]$</td></tr><tr><td>1</td><td>$[0, 1]$</td></tr><tr><td>2</td><td>$[1, 1]$</td></tr></tbody></table><p>如果给节点加自环，那么节点 1 会聚合：</p><pre class="language-text" data-language="text"><code class="language-text">节点 0、节点 1、节点 2</code></pre><p>平均后得到：</p><p>$$<br>h_1’&#x3D;<br>\frac{[1,0]+[0,1]+[1,1]}{3}<br>&#x3D;<br>\left[<br>\frac{2}{3},<br>\frac{2}{3}<br>\right]<br>$$</p><p>这个新表示已经不只是节点 1 自己的特征，而是混入了邻居节点的信息。</p><p>如果再经过一层线性变换：</p><p>$$<br>h_1’’ &#x3D; h_1’W<br>$$</p><p>模型就可以学习如何把聚合后的邻居信息转成更适合任务的表示。</p><p>这就是 GCN 的基本味道。</p><h3 id="7-1-用邻接矩阵看一次聚合"><a href="#7-1-用邻接矩阵看一次聚合" class="headerlink" title="7.1 用邻接矩阵看一次聚合"></a>7.1 用邻接矩阵看一次聚合</h3><p>上面的平均聚合也可以写成矩阵乘法。</p><p>继续使用 3 个节点的链式图：</p><pre class="language-text" data-language="text"><code class="language-text">0 -- 1 -- 2</code></pre><p>不加自环时，邻接矩阵为：</p><p>$$<br>A&#x3D;<br>\begin{bmatrix}<br>0 &amp; 1 &amp; 0 \<br>1 &amp; 0 &amp; 1 \<br>0 &amp; 1 &amp; 0<br>\end{bmatrix}<br>$$</p><p>给每个节点加自环，相当于加单位矩阵：</p><p>$$<br>\tilde{A}&#x3D;A+I&#x3D;<br>\begin{bmatrix}<br>1 &amp; 1 &amp; 0 \<br>1 &amp; 1 &amp; 1 \<br>0 &amp; 1 &amp; 1<br>\end{bmatrix}<br>$$</p><p>节点特征矩阵为：</p><p>$$<br>X&#x3D;<br>\begin{bmatrix}<br>1 &amp; 0 \<br>0 &amp; 1 \<br>1 &amp; 1<br>\end{bmatrix}<br>$$</p><p>如果先不做平均，只做相加聚合：</p><p>$$<br>\tilde{A}X&#x3D;<br>\begin{bmatrix}<br>1 &amp; 1 \<br>2 &amp; 2 \<br>1 &amp; 2<br>\end{bmatrix}<br>$$</p><p>逐行解释：</p><ul><li>第 0 行 $[1,1]$：节点 0 聚合了自己 $[1,0]$ 和节点 1 的 $[0,1]$。</li><li>第 1 行 $[2,2]$：节点 1 聚合了节点 0、自己、节点 2。</li><li>第 2 行 $[1,2]$：节点 2 聚合了节点 1 和自己。</li></ul><p>这就是邻接矩阵的作用：它决定每个节点应该把哪些节点的特征加起来。</p><h3 id="7-2-为什么-GCN-要做归一化"><a href="#7-2-为什么-GCN-要做归一化" class="headerlink" title="7.2 为什么 GCN 要做归一化"></a>7.2 为什么 GCN 要做归一化</h3><p>刚才直接相加有一个问题：邻居多的节点数值会更大。</p><p>例如节点 1 聚合了 3 个节点，所以得到 $[2,2]$；节点 0 只聚合 2 个节点，所以得到 $[1,1]$。</p><p>为了让不同度数的节点更可比，可以先看一种简单平均：</p><p>$$<br>D^{-1}\tilde{A}X<br>$$</p><p>其中 $D$ 是度矩阵。加自环后，各节点度数为：</p><table><thead><tr><th>节点</th><th align="right">度数</th></tr></thead><tbody><tr><td>0</td><td align="right">2</td></tr><tr><td>1</td><td align="right">3</td></tr><tr><td>2</td><td align="right">2</td></tr></tbody></table><p>所以简单平均后：</p><p>$$<br>D^{-1}\tilde{A}X&#x3D;<br>\begin{bmatrix}<br>\frac{1}{2} &amp; \frac{1}{2} \<br>\frac{2}{3} &amp; \frac{2}{3} \<br>\frac{1}{2} &amp; 1<br>\end{bmatrix}<br>$$</p><p>节点 1 的结果就是前面手工算过的：</p><p>$$<br>\frac{[1,0]+[0,1]+[1,1]}{3}<br>&#x3D;<br>\left[<br>\frac{2}{3},<br>\frac{2}{3}<br>\right]<br>$$</p><p>标准 GCN 使用的是对称归一化：</p><p>$$<br>\tilde{D}^{-\frac{1}{2}}\tilde{A}\tilde{D}^{-\frac{1}{2}}<br>$$</p><p>它比简单平均更平衡：一条边的权重不仅和当前节点度数有关，也和邻居节点度数有关。</p><p>对于节点 1 接收节点 0 的信息，权重为：</p><p>$$<br>\frac{1}{\sqrt{deg(1)}\sqrt{deg(0)}}<br>&#x3D;<br>\frac{1}{\sqrt{3}\sqrt{2}}<br>&#x3D;<br>\frac{1}{\sqrt{6}}<br>\approx0.408<br>$$</p><p>节点 1 接收自己信息的权重为：</p><p>$$<br>\frac{1}{\sqrt{3}\sqrt{3}}&#x3D;\frac{1}{3}\approx0.333<br>$$</p><p>节点 1 接收节点 2 的权重同样是：</p><p>$$<br>\frac{1}{\sqrt{3}\sqrt{2}}\approx0.408<br>$$</p><p>所以节点 1 的归一化聚合大致为：</p><p>$$<br>h_1’<br>&#x3D;<br>0.408[1,0]<br>+0.333[0,1]<br>+0.408[1,1]<br>$$</p><p>计算得到：</p><p>$$<br>h_1’<br>\approx<br>[0.816,;0.741]<br>$$</p><p>这个例子说明：</p><pre class="language-text" data-language="text"><code class="language-text">GCN 不是神秘地“理解图”，它本质上是在邻接关系指定的范围内，对邻居特征做带权聚合。</code></pre><h3 id="7-3-两层-GNN-为什么能看到两跳邻居"><a href="#7-3-两层-GNN-为什么能看到两跳邻居" class="headerlink" title="7.3 两层 GNN 为什么能看到两跳邻居"></a>7.3 两层 GNN 为什么能看到两跳邻居</h3><p>还是这个图：</p><pre class="language-text" data-language="text"><code class="language-text">0 -- 1 -- 2</code></pre><p>第 1 层时：</p><ul><li>节点 0 接收节点 1 的信息。</li><li>节点 1 接收节点 0 和节点 2 的信息。</li><li>节点 2 接收节点 1 的信息。</li></ul><p>所以第 1 层后，节点 1 的表示已经包含节点 0 和节点 2 的信息。</p><p>第 2 层时，节点 0 会接收节点 1 的新表示，而节点 1 的新表示里已经包含节点 2 的信息。</p><p>因此节点 0 在第 2 层后，就间接获得了节点 2 的信息。</p><p>可以这样理解：</p><pre class="language-text" data-language="text"><code class="language-text">一层：朋友影响我。两层：朋友的朋友也间接影响我。三层：朋友的朋友的朋友也会影响我。</code></pre><p>这就是为什么 GNN 层数代表了信息传播的距离。</p><h2 id="8-GraphSAGE：适合大图的采样聚合"><a href="#8-GraphSAGE：适合大图的采样聚合" class="headerlink" title="8. GraphSAGE：适合大图的采样聚合"></a>8. GraphSAGE：适合大图的采样聚合</h2><p>GCN 在整张图上做聚合，如果图很大，训练会比较困难。</p><p>例如一个社交网络可能有上亿节点。每次都使用所有邻居进行聚合，计算和内存成本都会很高。</p><p>GraphSAGE 的思路是：</p><blockquote><p>不必每次看所有邻居，可以采样固定数量的邻居，再做聚合。</p></blockquote><p><img src="https://lsworl.github.io/images/ml-blog/gnn-graphsage-sampling.svg" alt="GraphSAGE 通过采样邻居和聚合函数扩展到大图"></p><p>例如对每个节点：</p><pre class="language-text" data-language="text"><code class="language-text">第 1 层采样 10 个邻居第 2 层对每个邻居再采样 5 个邻居</code></pre><p>这样每个 batch 的计算规模就能控制住。</p><p>GraphSAGE 还强调归纳能力（inductive ability）。也就是说，训练好以后，模型可以对训练时没见过的新节点生成表示，只要这个新节点有特征和邻居信息。</p><p>这对真实系统很重要，因为社交网络、推荐系统里的新用户和新商品会不断出现。</p><h3 id="8-1-GraphSAGE-的采样例子"><a href="#8-1-GraphSAGE-的采样例子" class="headerlink" title="8.1 GraphSAGE 的采样例子"></a>8.1 GraphSAGE 的采样例子</h3><p>假设某个用户节点有 1000 个邻居。如果每次训练都聚合全部 1000 个邻居，成本会很高。</p><p>GraphSAGE 可以规定每次只采样 3 个邻居。</p><p>例如目标节点 <code>u</code> 的邻居很多：</p><pre class="language-text" data-language="text"><code class="language-text">N(u) = &#123;a, b, c, d, e, f, ...&#125;</code></pre><p>某一次训练采样到：</p><pre class="language-text" data-language="text"><code class="language-text">sample(N(u)) = &#123;a, c, f&#125;</code></pre><p>它们的特征为：</p><table><thead><tr><th>节点</th><th>特征</th></tr></thead><tbody><tr><td>a</td><td>$[1, 0]$</td></tr><tr><td>c</td><td>$[0, 2]$</td></tr><tr><td>f</td><td>$[2, 1]$</td></tr></tbody></table><p>如果使用 mean aggregator：</p><p>$$<br>h_{\mathcal{N}(u)}<br>&#x3D;<br>\frac{[1,0]+[0,2]+[2,1]}{3}<br>&#x3D;<br>[1,1]<br>$$</p><p>假设目标节点自己的特征为：</p><p>$$<br>h_u&#x3D;[2,0]<br>$$</p><p>GraphSAGE 可以把自己和邻居聚合结果拼接起来：</p><p>$$<br>\text{concat}(h_u,h_{\mathcal{N}(u)})<br>&#x3D;<br>[2,0,1,1]<br>$$</p><p>再经过一个线性层：</p><p>$$<br>h_u’&#x3D;\sigma(W[2,0,1,1])<br>$$</p><p>这样做的好处是：无论原来有 1000 个邻居还是 10 个邻居，每次都只采样固定数量，训练规模就可控了。</p><h2 id="9-GAT：让模型自己决定邻居重要性"><a href="#9-GAT：让模型自己决定邻居重要性" class="headerlink" title="9. GAT：让模型自己决定邻居重要性"></a>9. GAT：让模型自己决定邻居重要性</h2><p>GCN 通常按照图结构和度数归一化来聚合邻居，但它默认邻居的重要性主要由结构决定。</p><p>可是在很多图里，不同邻居的重要性并不一样。</p><p>例如推荐系统中，一个用户连接了很多商品，但某些最近购买的商品可能比很久以前点过的商品更重要。</p><p>GAT（Graph Attention Network）引入注意力机制，让模型学习每个邻居的权重：</p><p>$$<br>h_i’&#x3D;<br>\sigma<br>\left(<br>\sum_{j\in\mathcal{N}(i)}<br>\alpha_{ij}W h_j<br>\right)<br>$$</p><p>其中 $\alpha_{ij}$ 表示节点 $i$ 聚合邻居 $j$ 时的注意力权重。</p><p><img src="https://lsworl.github.io/images/ml-blog/gnn-gat-attention.svg" alt="GAT 使用注意力权重区分不同邻居的重要性"></p><p>如果某个邻居更有帮助，模型可以给它更高权重；如果某个邻居噪声较大，权重可以更低。</p><p>这让 GAT 在一些异质性更强、邻居质量差异更大的图上更灵活。</p><h3 id="9-1-GAT-的注意力计算例子"><a href="#9-1-GAT-的注意力计算例子" class="headerlink" title="9.1 GAT 的注意力计算例子"></a>9.1 GAT 的注意力计算例子</h3><p>假设节点 $i$ 有三个邻居，它们经过线性变换后的特征分别是：</p><table><thead><tr><th>邻居</th><th>变换后特征</th></tr></thead><tbody><tr><td>1</td><td>$[1, 0]$</td></tr><tr><td>2</td><td>$[0, 2]$</td></tr><tr><td>3</td><td>$[2, 1]$</td></tr></tbody></table><p>模型计算出注意力权重：</p><table><thead><tr><th>邻居</th><th align="right">注意力权重</th></tr></thead><tbody><tr><td>1</td><td align="right">0.2</td></tr><tr><td>2</td><td align="right">0.5</td></tr><tr><td>3</td><td align="right">0.3</td></tr></tbody></table><p>那么节点 $i$ 聚合后的表示就是加权求和：</p><p>$$<br>h_i’<br>&#x3D;<br>0.2[1,0]<br>+0.5[0,2]<br>+0.3[2,1]<br>$$</p><p>逐项计算：</p><p>$$<br>0.2[1,0]&#x3D;[0.2,0]<br>$$</p><p>$$<br>0.5[0,2]&#x3D;[0,1]<br>$$</p><p>$$<br>0.3[2,1]&#x3D;[0.6,0.3]<br>$$</p><p>所以：</p><p>$$<br>h_i’&#x3D;[0.2,0]+[0,1]+[0.6,0.3]&#x3D;[0.8,1.3]<br>$$</p><p>这个例子说明，GAT 和普通平均聚合最大的区别是：</p><pre class="language-text" data-language="text"><code class="language-text">普通聚合：邻居基本一视同仁。注意力聚合：模型自己决定谁更重要。</code></pre><p>如果邻居 2 对任务更关键，它就可以获得更高权重。</p><h2 id="10-GNN-的训练流程"><a href="#10-GNN-的训练流程" class="headerlink" title="10. GNN 的训练流程"></a>10. GNN 的训练流程</h2><p>GNN 的训练过程和普通神经网络很像，但前向传播中会显式使用图结构。</p><p><img src="https://lsworl.github.io/images/ml-blog/gnn-training-pipeline.svg" alt="GNN 的典型训练流程"></p><p>一般流程如下：</p><ol><li>准备节点特征矩阵 $X$。</li><li>准备边列表 <code>edge_index</code> 或邻接矩阵 $A$。</li><li>通过多层 GNN 做消息传递，得到节点表示。</li><li>根据任务类型做预测。</li><li>计算损失函数。</li><li>反向传播更新参数。</li></ol><p>对于节点分类，常用交叉熵损失：</p><p>$$<br>\mathcal{L}<br>&#x3D;<br>-\sum_{v\in \mathcal{V}_{train}}<br>y_v \log \hat{y}_v<br>$$</p><p>其中只在训练节点集合上计算损失。</p><p>对于图分类，则通常先把节点表示池化成图表示：</p><p>$$<br>h_G &#x3D; \text{READOUT}({h_v:v\in G})<br>$$</p><p>再用 $h_G$ 做分类或回归。</p><h3 id="10-1-图级任务的-pooling-计算例子"><a href="#10-1-图级任务的-pooling-计算例子" class="headerlink" title="10.1 图级任务的 pooling 计算例子"></a>10.1 图级任务的 pooling 计算例子</h3><p>前面很多例子都在讲“节点表示怎么算”。但如果任务是判断整个分子是否有毒，就需要整张图的表示。</p><p>假设一个小分子图有 4 个原子节点，经过 GNN 后得到节点表示：</p><table><thead><tr><th>节点</th><th>表示</th></tr></thead><tbody><tr><td>0</td><td>$[1, 0]$</td></tr><tr><td>1</td><td>$[2, 1]$</td></tr><tr><td>2</td><td>$[0, 3]$</td></tr><tr><td>3</td><td>$[1, 2]$</td></tr></tbody></table><p>如果使用 mean pooling，整张图表示为：</p><p>$$<br>h_G&#x3D;<br>\frac{[1,0]+[2,1]+[0,3]+[1,2]}{4}<br>$$</p><p>计算得到：</p><p>$$<br>h_G&#x3D;<br>\left[<br>\frac{4}{4},<br>\frac{6}{4}<br>\right]<br>&#x3D;<br>[1,1.5]<br>$$</p><p>然后把 $h_G$ 输入一个分类器：</p><p>$$<br>\hat{y}&#x3D;\text{softmax}(W h_G+b)<br>$$</p><p>如果使用 sum pooling，则：</p><p>$$<br>h_G&#x3D;[4,6]<br>$$</p><p>如果使用 max pooling，则对每一维取最大值：</p><p>$$<br>h_G&#x3D;[2,3]<br>$$</p><p>不同 pooling 的含义不同：</p><ul><li>mean pooling 看整体平均特征。</li><li>sum pooling 会保留图规模信息。</li><li>max pooling 关注是否出现某种强特征。</li></ul><p>所以图分类并不是直接“把图塞进模型”，而是先通过 GNN 得到节点表示，再通过 pooling 把多个节点表示压缩成整张图的表示。</p><h2 id="11-PyTorch-Geometric-风格代码"><a href="#11-PyTorch-Geometric-风格代码" class="headerlink" title="11. PyTorch Geometric 风格代码"></a>11. PyTorch Geometric 风格代码</h2><p>实际项目中，经常使用 PyTorch Geometric 或 DGL 来实现 GNN。</p><p>下面用 PyTorch Geometric 风格写一个两层 GCN 节点分类模型。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">import</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>functional <span class="token keyword">as</span> F<span class="token keyword">from</span> torch_geometric<span class="token punctuation">.</span>nn <span class="token keyword">import</span> GCNConv<span class="token keyword">class</span> <span class="token class-name">GCN</span><span class="token punctuation">(</span>torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>Module<span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token keyword">def</span> <span class="token function">__init__</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> input_dim<span class="token punctuation">,</span> hidden_dim<span class="token punctuation">,</span> num_classes<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>__init__<span class="token punctuation">(</span><span class="token punctuation">)</span>        self<span class="token punctuation">.</span>conv1 <span class="token operator">=</span> GCNConv<span class="token punctuation">(</span>input_dim<span class="token punctuation">,</span> hidden_dim<span class="token punctuation">)</span>        self<span class="token punctuation">.</span>conv2 <span class="token operator">=</span> GCNConv<span class="token punctuation">(</span>hidden_dim<span class="token punctuation">,</span> num_classes<span class="token punctuation">)</span>    <span class="token keyword">def</span> <span class="token function">forward</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> x<span class="token punctuation">,</span> edge_index<span class="token punctuation">)</span><span class="token punctuation">:</span>        <span class="token comment"># 第一层图卷积：节点接收一跳邻居信息</span>        x <span class="token operator">=</span> self<span class="token punctuation">.</span>conv1<span class="token punctuation">(</span>x<span class="token punctuation">,</span> edge_index<span class="token punctuation">)</span>        x <span class="token operator">=</span> F<span class="token punctuation">.</span>relu<span class="token punctuation">(</span>x<span class="token punctuation">)</span>        x <span class="token operator">=</span> F<span class="token punctuation">.</span>dropout<span class="token punctuation">(</span>x<span class="token punctuation">,</span> p<span class="token operator">=</span><span class="token number">0.5</span><span class="token punctuation">,</span> training<span class="token operator">=</span>self<span class="token punctuation">.</span>training<span class="token punctuation">)</span>        <span class="token comment"># 第二层图卷积：继续聚合邻居信息，并输出类别 logits</span>        x <span class="token operator">=</span> self<span class="token punctuation">.</span>conv2<span class="token punctuation">(</span>x<span class="token punctuation">,</span> edge_index<span class="token punctuation">)</span>        <span class="token keyword">return</span> xmodel <span class="token operator">=</span> GCN<span class="token punctuation">(</span>    input_dim<span class="token operator">=</span>dataset<span class="token punctuation">.</span>num_node_features<span class="token punctuation">,</span>    hidden_dim<span class="token operator">=</span><span class="token number">64</span><span class="token punctuation">,</span>    num_classes<span class="token operator">=</span>dataset<span class="token punctuation">.</span>num_classes<span class="token punctuation">,</span><span class="token punctuation">)</span>optimizer <span class="token operator">=</span> torch<span class="token punctuation">.</span>optim<span class="token punctuation">.</span>Adam<span class="token punctuation">(</span>    model<span class="token punctuation">.</span>parameters<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    lr<span class="token operator">=</span><span class="token number">0.01</span><span class="token punctuation">,</span>    weight_decay<span class="token operator">=</span><span class="token number">5e-4</span><span class="token punctuation">,</span><span class="token punctuation">)</span>data <span class="token operator">=</span> dataset<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token keyword">for</span> epoch <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">200</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    model<span class="token punctuation">.</span>train<span class="token punctuation">(</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>    logits <span class="token operator">=</span> model<span class="token punctuation">(</span>data<span class="token punctuation">.</span>x<span class="token punctuation">,</span> data<span class="token punctuation">.</span>edge_index<span class="token punctuation">)</span>    <span class="token comment"># 只在训练节点上计算分类损失</span>    loss <span class="token operator">=</span> F<span class="token punctuation">.</span>cross_entropy<span class="token punctuation">(</span>        logits<span class="token punctuation">[</span>data<span class="token punctuation">.</span>train_mask<span class="token punctuation">]</span><span class="token punctuation">,</span>        data<span class="token punctuation">.</span>y<span class="token punctuation">[</span>data<span class="token punctuation">.</span>train_mask<span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> epoch <span class="token operator">%</span> <span class="token number">20</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">:</span>        <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"epoch=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>epoch<span class="token punctuation">&#125;</span></span><span class="token string">, loss=</span><span class="token interpolation"><span class="token punctuation">&#123;</span>loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span><span class="token format-spec">.4f</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span><span class="token punctuation">)</span></code></pre><p>这段代码里最关键的是：</p><ul><li><code>data.x</code>：节点特征矩阵。</li><li><code>data.edge_index</code>：图的边列表。</li><li><code>GCNConv</code>：图卷积层。</li><li><code>train_mask</code>：哪些节点用于训练。</li></ul><p>和普通神经网络相比，GNN 的模型输入不仅有特征 <code>x</code>，还必须有图结构 <code>edge_index</code>。</p><h2 id="12-GNN-和-CNN、Transformer-的关系"><a href="#12-GNN-和-CNN、Transformer-的关系" class="headerlink" title="12. GNN 和 CNN、Transformer 的关系"></a>12. GNN 和 CNN、Transformer 的关系</h2><p>可以用一个角度理解它们：</p><table><thead><tr><th>模型</th><th>主要数据结构</th><th>信息如何流动</th></tr></thead><tbody><tr><td>CNN</td><td>规则网格</td><td>局部卷积核聚合邻域像素</td></tr><tr><td>RNN</td><td>序列</td><td>按时间步传递隐藏状态</td></tr><tr><td>Transformer</td><td>序列或集合</td><td>注意力在 token 间传递信息</td></tr><tr><td>GNN</td><td>图结构</td><td>节点沿边传递消息</td></tr></tbody></table><p>CNN 的邻居关系由图像网格天然决定。</p><p>GNN 的邻居关系由图的边决定。</p><p>Transformer 可以看成一种更自由的全连接信息交互，而 GNN 通常受到图结构约束，只在有边的节点之间传递信息。</p><p>所以 GNN 的优势在于：它把“谁和谁有关”这个先验结构直接放进模型里。</p><h2 id="13-过平滑问题"><a href="#13-过平滑问题" class="headerlink" title="13. 过平滑问题"></a>13. 过平滑问题</h2><p>GNN 并不是层数越深越好。</p><p>当 GNN 层数很多时，一个节点会不断混合越来越远的邻居信息。最后不同节点的表示可能变得越来越相似，这叫过平滑（Over-smoothing）。</p><p>直观地说：</p><pre class="language-text" data-language="text"><code class="language-text">每一层都在和邻居平均，平均太多次以后，大家的表示就越来越像。</code></pre><p>这会导致节点分类能力下降，因为模型难以区分不同节点。</p><p>常见缓解方法包括：</p><ul><li>使用残差连接。</li><li>使用跳层连接。</li><li>限制 GNN 层数。</li><li>使用归一化和正则化。</li><li>设计更强的消息传递结构。</li></ul><p>很多实际 GCN 模型只有 2 到 3 层，就是因为层数太深容易带来问题。</p><h2 id="14-图同质性和异质性"><a href="#14-图同质性和异质性" class="headerlink" title="14. 图同质性和异质性"></a>14. 图同质性和异质性</h2><p>很多经典 GNN 假设图具有同质性（homophily）：</p><blockquote><p>相连的节点往往属于相似类别。</p></blockquote><p>例如论文引用网络中，同一领域的论文更可能互相引用；社交网络中，兴趣相似的人更可能连接。</p><p>在这种情况下，邻居聚合通常有效。</p><p>但有些图是异质性的（heterophily）：</p><blockquote><p>相连的节点不一定相似，甚至可能经常不同类。</p></blockquote><p>例如交易网络中，正常账户可能和异常账户发生交易；知识图谱中，不同类型实体之间才会连接。</p><p>这时简单邻居平均可能反而引入噪声，需要更适合异质图的模型，例如关系图神经网络、异构图神经网络或更复杂的注意力机制。</p><h2 id="15-GNN-的常见应用"><a href="#15-GNN-的常见应用" class="headerlink" title="15. GNN 的常见应用"></a>15. GNN 的常见应用</h2><p>GNN 的应用非常广：</p><ul><li>推荐系统：用户-商品图、点击图、购买图。</li><li>风控反欺诈：账户交易网络、设备关联网络。</li><li>分子性质预测：原子和化学键组成分子图。</li><li>交通预测：道路网络上的车流量预测。</li><li>知识图谱：实体关系建模和链接预测。</li><li>社交网络：社区发现、用户分类、影响力预测。</li><li>代码分析：函数调用图、抽象语法树、依赖图。</li></ul><p>这些任务的共同点是：关系本身就是信息。</p><p>如果把图结构丢掉，只把节点当成独立样本，往往会损失大量上下文。</p><h2 id="16-GNN-的优点"><a href="#16-GNN-的优点" class="headerlink" title="16. GNN 的优点"></a>16. GNN 的优点</h2><p>GNN 的优点主要有：</p><ul><li>能直接利用图结构。</li><li>可以把节点特征和邻居信息融合起来。</li><li>适合节点、边、图三个层级的任务。</li><li>对不规则结构数据很自然。</li><li>在推荐、分子、知识图谱等场景中表现强。</li></ul><p>尤其是当关系结构很重要时，GNN 往往比只看单点特征的模型更有优势。</p><h2 id="17-GNN-的局限"><a href="#17-GNN-的局限" class="headerlink" title="17. GNN 的局限"></a>17. GNN 的局限</h2><p>GNN 也有不少挑战。</p><p>第一，大图训练成本高。</p><p>真实图可能有千万甚至上亿节点，完整图训练很难，需要采样、分布式训练或 mini-batch 技术。</p><p>第二，邻居噪声会影响结果。</p><p>如果图里有很多错误边、弱关系或恶意连接，消息传递可能把噪声扩散到更多节点。</p><p>第三，层数加深容易过平滑。</p><p>节点表示变得过于相似后，分类或预测能力会下降。</p><p>第四，动态图更难处理。</p><p>现实中的图经常随时间变化，例如新用户、新交易、新道路状态。静态 GNN 不一定能直接适应。</p><p>第五，可解释性仍然困难。</p><p>虽然注意力权重和子图解释能提供一些线索，但 GNN 的整体决策过程仍然不如决策树那样直观。</p><h2 id="18-实践建议"><a href="#18-实践建议" class="headerlink" title="18. 实践建议"></a>18. 实践建议</h2><p>使用 GNN 时，可以按下面顺序思考：</p><ol><li>先确认图结构是否真的有用，不要为了用 GNN 而强行构图。</li><li>明确任务是节点级、边级还是图级。</li><li>检查节点特征质量，只有边没有特征时效果可能受限。</li><li>从简单 GCN 或 GraphSAGE 做 baseline。</li><li>大图优先考虑邻居采样和 mini-batch 训练。</li><li>如果邻居重要性差异明显，可以尝试 GAT。</li><li>如果图是多关系或多类型节点，考虑 R-GCN 或异构图模型。</li><li>监控过平滑，不要盲目堆很多层。</li><li>做好训练、验证、测试划分，避免图数据泄漏。</li><li>和非 GNN baseline 对比，确认图结构确实带来收益。</li></ol><h2 id="19-总结"><a href="#19-总结" class="headerlink" title="19. 总结"></a>19. 总结</h2><p>GNN 的核心思想可以概括为：</p><pre class="language-text" data-language="text"><code class="language-text">节点从邻居接收消息，聚合邻居信息，更新自己的表示，多层堆叠后获得更大范围的结构上下文。</code></pre><p>如果用公式表示，就是：</p><p>$$<br>h_v^{(k)} &#x3D;<br>\text{UPDATE}^{(k)}<br>\left(<br>h_v^{(k-1)},<br>\text{AGGREGATE}^{(k)}<br>\left(<br>{h_u^{(k-1)}:u\in \mathcal{N}(v)}<br>\right)<br>\right)<br>$$</p><p>理解 GNN 时，可以抓住三个关键词：</p><ul><li>图结构：边决定哪些节点可以交换信息。</li><li>消息传递：节点通过邻居聚合更新表示。</li><li>表示学习：最终把节点、边或整张图编码成可用于预测的向量。</li></ul><p>当数据中的“关系”本身很重要时，GNN 就非常值得考虑。它不是简单地把图变成表格，而是让模型在图上直接学习。</p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p>本文主要参考了以下资料：</p><ol><li>Thomas N. Kipf, Max Welling, <em>Semi-Supervised Classification with Graph Convolutional Networks</em>, ICLR 2017. <a href="https://arxiv.org/abs/1609.02907">https://arxiv.org/abs/1609.02907</a></li><li>William L. Hamilton, Rex Ying, Jure Leskovec, <em>Inductive Representation Learning on Large Graphs</em>, NeurIPS 2017. <a href="https://arxiv.org/abs/1706.02216">https://arxiv.org/abs/1706.02216</a></li><li>Petar Veličković et al., <em>Graph Attention Networks</em>, ICLR 2018. <a href="https://arxiv.org/abs/1710.10903">https://arxiv.org/abs/1710.10903</a></li><li>Justin Gilmer et al., <em>Neural Message Passing for Quantum Chemistry</em>, ICML 2017. <a href="https://arxiv.org/abs/1704.01212">https://arxiv.org/abs/1704.01212</a></li><li>PyTorch Geometric Documentation: <a href="https://pytorch-geometric.readthedocs.io/">https://pytorch-geometric.readthedocs.io/</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/06/20/machine-learning-gnn/</id>
    <link href="https://lsworl.github.io/2026/06/20/machine-learning-gnn/"/>
    <published>2026-06-19T16:00:00.000Z</published>
    <summary>从图数据、消息传递、GCN、GraphSAGE、GAT、节点分类、图分类和PyTorch Geometric代码几个角度理解图神经网络。</summary>
    <title>图神经网络GNN：让模型学会理解关系数据</title>
    <updated>2026-06-22T05:29:08.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="机器学习" scheme="https://lsworl.github.io/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="机器学习" scheme="https://lsworl.github.io/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="强化学习" scheme="https://lsworl.github.io/tags/%E5%BC%BA%E5%8C%96%E5%AD%A6%E4%B9%A0/"/>
    <category term="策略梯度" scheme="https://lsworl.github.io/tags/%E7%AD%96%E7%95%A5%E6%A2%AF%E5%BA%A6/"/>
    <category term="Actor-Critic" scheme="https://lsworl.github.io/tags/Actor-Critic/"/>
    <category term="PPO" scheme="https://lsworl.github.io/tags/PPO/"/>
    <content>
      <![CDATA[<p>PPO（Proximal Policy Optimization，近端策略优化）是深度强化学习中非常常用的一类策略优化算法。</p><p>如果前面已经理解了 MDP、奖励、价值函数和策略，那么 PPO 要解决的问题可以这样理解：</p><pre class="language-text" data-language="text"><code class="language-text">智能体已经有一个策略了。现在我们根据新采样到的经验，让这个策略变得更好。但每次更新不能太激进，否则好不容易学到的行为可能被一下子破坏。</code></pre><p>这也是 PPO 名字里 “Proximal” 的含义：更新策略时，希望新策略离旧策略近一点。</p><p>一句话概括：</p><blockquote><p>PPO 是一种策略梯度算法，它通过裁剪新旧策略概率比值，限制单次策略更新幅度，从而在实现简单和训练稳定之间取得平衡。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/ml-blog/ppo-overview-loop.svg" alt="PPO 在采样和受约束更新之间反复循环"></p><h2 id="1-为什么需要-PPO"><a href="#1-为什么需要-PPO" class="headerlink" title="1. 为什么需要 PPO"></a>1. 为什么需要 PPO</h2><p>在强化学习中，我们最终想学到一个策略：</p><p>$$<br>\pi_\theta(a|s)<br>$$</p><p>它表示在状态 $s$ 下选择动作 $a$ 的概率。这里的 $\theta$ 是神经网络参数。</p><p>如果某个动作带来了更高的长期回报，我们就希望以后在类似状态下更倾向于选择它；如果某个动作表现很差，就希望降低它被选中的概率。</p><p>这就是策略梯度的基本思想：</p><pre class="language-text" data-language="text"><code class="language-text">好动作的概率提高坏动作的概率降低</code></pre><p>但普通策略梯度有一个问题：更新步子不好控制。</p><p>如果步子太小，学习会非常慢；如果步子太大，策略可能突然发生巨大变化，导致性能崩掉。尤其在深度强化学习里，神经网络参数的一次更新可能让动作分布明显变化。</p><p><img src="https://lsworl.github.io/images/ml-blog/ppo-policy-gradient-problem.svg" alt="策略更新过大可能破坏已有策略"></p><p>PPO 的核心就是给策略更新加一个“软限制”：</p><pre class="language-text" data-language="text"><code class="language-text">可以改进，但不要离旧策略太远</code></pre><h2 id="2-PPO-属于哪类算法"><a href="#2-PPO-属于哪类算法" class="headerlink" title="2. PPO 属于哪类算法"></a>2. PPO 属于哪类算法</h2><p>强化学习算法可以粗略分成两类：</p><table><thead><tr><th>类型</th><th>代表算法</th><th>学习对象</th></tr></thead><tbody><tr><td>价值方法</td><td>Q-Learning、DQN</td><td>学习动作价值 $Q(s,a)$</td></tr><tr><td>策略方法</td><td>REINFORCE、TRPO、PPO</td><td>直接学习策略 $\pi(a</td></tr></tbody></table><p>PPO 属于策略梯度方法，并且通常以 Actor-Critic 的形式实现：</p><ul><li>Actor：策略网络，输出动作分布。</li><li>Critic：价值网络，估计当前状态的价值。</li></ul><p>Actor 负责“怎么做”，Critic 负责“这个状态大概有多好”。Critic 的估计可以降低策略梯度的方差，让训练更稳定。</p><h2 id="3-从策略梯度开始"><a href="#3-从策略梯度开始" class="headerlink" title="3. 从策略梯度开始"></a>3. 从策略梯度开始</h2><p>策略梯度希望最大化期望回报：</p><p>$$<br>J(\theta)&#x3D;\mathbb{E}_{\tau \sim \pi_\theta}[R(\tau)]<br>$$</p><p>其中：</p><ul><li>$\tau$ 表示一条轨迹，例如 $(s_0,a_0,r_1,s_1,a_1,r_2,\dots)$。</li><li>$R(\tau)$ 表示这条轨迹的累计回报。</li><li>$\pi_\theta$ 表示当前策略。</li></ul><p>经典策略梯度可以写成：</p><p>$$<br>\nabla_\theta J(\theta)<br>&#x3D;<br>\mathbb{E}<br>\left[<br>\nabla_\theta \log \pi_\theta(a_t|s_t) A_t<br>\right]<br>$$</p><p>其中 $A_t$ 是优势函数（Advantage Function）。</p><p>优势函数可以理解为：</p><blockquote><p>当前这个动作比平均水平好多少。</p></blockquote><p>如果 $A_t&gt;0$，说明这个动作比预期更好，应该提高它的概率。</p><p>如果 $A_t&lt;0$，说明这个动作比预期更差，应该降低它的概率。</p><h2 id="4-优势函数：动作到底好不好"><a href="#4-优势函数：动作到底好不好" class="headerlink" title="4. 优势函数：动作到底好不好"></a>4. 优势函数：动作到底好不好</h2><p>动作价值函数 $Q^\pi(s,a)$ 评价的是：在状态 $s$ 下执行动作 $a$，之后按策略 $\pi$ 行动，长期回报期望是多少。</p><p>状态价值函数 $V^\pi(s)$ 评价的是：在状态 $s$ 下按当前策略行动，长期回报期望是多少。</p><p>优势函数定义为：</p><p>$$<br>A^\pi(s,a)&#x3D;Q^\pi(s,a)-V^\pi(s)<br>$$</p><p>它比较的是“这个动作”和“当前状态平均水平”的差距。</p><p>例如：</p><ul><li>当前状态平均能拿到 10 分。</li><li>某个动作预计能拿到 13 分。</li><li>那么优势就是 $+3$。</li></ul><p>这说明这个动作值得鼓励。</p><p>如果某个动作预计只能拿到 7 分，那么优势就是 $-3$，说明它应该被抑制。</p><p><img src="https://lsworl.github.io/images/ml-blog/ppo-actor-critic-gae.svg" alt="PPO 常用 Actor-Critic 结构估计策略和优势"></p><p>实际实现中，PPO 经常使用 GAE（Generalized Advantage Estimation，广义优势估计）来估计优势。它在偏差和方差之间做折中，比直接用完整回报更稳定。</p><h2 id="5-为什么需要新旧策略概率比值"><a href="#5-为什么需要新旧策略概率比值" class="headerlink" title="5. 为什么需要新旧策略概率比值"></a>5. 为什么需要新旧策略概率比值</h2><p>PPO 在更新策略时，会使用旧策略采样到的数据。</p><p>采样时使用的是旧策略：</p><p>$$<br>\pi_{\theta_{old}}(a_t|s_t)<br>$$</p><p>但更新时我们优化的是新策略：</p><p>$$<br>\pi_\theta(a_t|s_t)<br>$$</p><p>为了衡量新策略相对于旧策略有多大变化，PPO 定义了概率比值：</p><p>$$<br>r_t(\theta)&#x3D;<br>\frac{\pi_\theta(a_t|s_t)}<br>{\pi_{\theta_{old}}(a_t|s_t)}<br>$$</p><p>这个比值很直观：</p><ul><li>$r_t(\theta)&#x3D;1$：新旧策略对这个动作的概率一样。</li><li>$r_t(\theta)&gt;1$：新策略更倾向于选择这个动作。</li><li>$r_t(\theta)&lt;1$：新策略不那么倾向于选择这个动作。</li></ul><p>如果某个动作优势为正，我们希望 $r_t$ 变大；如果优势为负，我们希望 $r_t$ 变小。</p><p>但问题也在这里：如果 $r_t$ 变得太大或太小，说明策略改动过猛。</p><h2 id="6-PPO-的裁剪目标函数"><a href="#6-PPO-的裁剪目标函数" class="headerlink" title="6. PPO 的裁剪目标函数"></a>6. PPO 的裁剪目标函数</h2><p>PPO 最经典的是 clipped surrogate objective，也就是裁剪替代目标函数。</p><p>先看没有裁剪的形式：</p><p>$$<br>L^{PG}(\theta)&#x3D;<br>\mathbb{E}_t<br>\left[<br>r_t(\theta)A_t<br>\right]<br>$$</p><p>如果 $A_t&gt;0$，增大 $r_t$ 会让目标变大；如果 $A_t&lt;0$，减小 $r_t$ 会让目标变大。</p><p>这符合策略梯度的直觉，但它没有限制更新幅度。</p><p>PPO 加入裁剪：</p><p>$$<br>L^{CLIP}(\theta)&#x3D;<br>\mathbb{E}_t<br>\left[<br>\min<br>\left(<br>r_t(\theta)A_t,;<br>\text{clip}(r_t(\theta),1-\epsilon,1+\epsilon)A_t<br>\right)<br>\right]<br>$$</p><p>其中 $\epsilon$ 是裁剪范围，常见取值如 $0.1$ 或 $0.2$。</p><p><img src="https://lsworl.github.io/images/ml-blog/ppo-clipped-objective.svg" alt="PPO 用裁剪目标函数限制概率比值变化"></p><p>这个公式看起来有点绕，但核心思想很简单：</p><pre class="language-text" data-language="text"><code class="language-text">如果策略更新带来的收益来自“过度改变动作概率”，那就把这部分收益砍掉。</code></pre><p>也就是说，PPO 不直接禁止策略改变，而是不鼓励策略通过过大的概率变化来获得目标函数收益。</p><h2 id="7-裁剪项如何工作"><a href="#7-裁剪项如何工作" class="headerlink" title="7. 裁剪项如何工作"></a>7. 裁剪项如何工作</h2><p>可以分两种情况理解。</p><h3 id="7-1-当优势为正"><a href="#7-1-当优势为正" class="headerlink" title="7.1 当优势为正"></a>7.1 当优势为正</h3><p>如果 $A_t&gt;0$，说明这个动作比预期好。</p><p>我们希望新策略提高这个动作的概率，也就是让 $r_t$ 变大。</p><p>但是如果 $r_t$ 已经超过 $1+\epsilon$，PPO 会把它裁剪到 $1+\epsilon$，不再继续奖励更大的概率提升。</p><p>直观地说：</p><pre class="language-text" data-language="text"><code class="language-text">好动作可以更常选，但不要一下子提高太多。</code></pre><h3 id="7-2-当优势为负"><a href="#7-2-当优势为负" class="headerlink" title="7.2 当优势为负"></a>7.2 当优势为负</h3><p>如果 $A_t&lt;0$，说明这个动作比预期差。</p><p>我们希望新策略降低这个动作的概率，也就是让 $r_t$ 变小。</p><p>但是如果 $r_t$ 已经低于 $1-\epsilon$，PPO 会限制继续下降带来的收益。</p><p>直观地说：</p><pre class="language-text" data-language="text"><code class="language-text">坏动作可以少选，但不要一下子压得太狠。</code></pre><p><img src="https://lsworl.github.io/images/ml-blog/ppo-ratio-cases.svg" alt="PPO 根据优势正负决定提高或降低动作概率"></p><p>这就是 PPO 稳定性的来源之一：它不会让策略因为一批样本而被过度推向某个方向。</p><h2 id="8-一个手工计算的小例子"><a href="#8-一个手工计算的小例子" class="headerlink" title="8. 一个手工计算的小例子"></a>8. 一个手工计算的小例子</h2><p>为了更直观地理解 PPO 的裁剪目标函数，我们来看一个只包含几条样本的小例子。</p><p>假设裁剪范围为：</p><p>$$<br>\epsilon&#x3D;0.2<br>$$</p><p>那么概率比值 $r_t$ 会被限制在：</p><p>$$<br>[1-\epsilon,;1+\epsilon]&#x3D;[0.8,;1.2]<br>$$</p><p>也就是说，新策略相对于旧策略，对某个动作的概率最好不要一下子低于原来的 $80%$，也不要一下子高于原来的 $120%$。</p><h3 id="8-1-好动作：优势为正"><a href="#8-1-好动作：优势为正" class="headerlink" title="8.1 好动作：优势为正"></a>8.1 好动作：优势为正</h3><p>假设在某个状态下，旧策略选择动作 <code>向右</code> 的概率为：</p><p>$$<br>\pi_{\theta_{old}}(a|s)&#x3D;0.20<br>$$</p><p>这次采样后发现 <code>向右</code> 的效果不错，优势为：</p><p>$$<br>A_t&#x3D;5<br>$$</p><p>现在新策略想把这个动作的概率提高到：</p><p>$$<br>\pi_\theta(a|s)&#x3D;0.30<br>$$</p><p>那么概率比值为：</p><p>$$<br>r_t(\theta)&#x3D;\frac{0.30}{0.20}&#x3D;1.5<br>$$</p><p>如果不用裁剪，策略目标项为：</p><p>$$<br>r_t A_t &#x3D; 1.5 \times 5 &#x3D; 7.5<br>$$</p><p>但因为 $1.5$ 已经超过了 $1.2$，所以 PPO 会把它裁剪到 $1.2$：</p><p>$$<br>\text{clip}(r_t,0.8,1.2)A_t&#x3D;1.2\times5&#x3D;6<br>$$</p><p>最后 PPO 取两者中的较小值：</p><p>$$<br>\min(7.5,;6)&#x3D;6<br>$$</p><p>这表示：<code>向右</code> 确实是好动作，可以提高概率，但从 $0.20$ 一下子提高到 $0.30$ 有点激进，PPO 不会继续奖励这部分过大的提升。</p><h3 id="8-2-坏动作：优势为负"><a href="#8-2-坏动作：优势为负" class="headerlink" title="8.2 坏动作：优势为负"></a>8.2 坏动作：优势为负</h3><p>再看一个坏动作的例子。</p><p>假设旧策略选择动作 <code>跳跃</code> 的概率为：</p><p>$$<br>\pi_{\theta_{old}}(a|s)&#x3D;0.50<br>$$</p><p>这次发现它效果不好，优势为：</p><p>$$<br>A_t&#x3D;-4<br>$$</p><p>新策略想把这个动作的概率降到：</p><p>$$<br>\pi_\theta(a|s)&#x3D;0.25<br>$$</p><p>那么概率比值为：</p><p>$$<br>r_t(\theta)&#x3D;\frac{0.25}{0.50}&#x3D;0.5<br>$$</p><p>如果不用裁剪：</p><p>$$<br>r_t A_t &#x3D; 0.5 \times (-4) &#x3D; -2<br>$$</p><p>裁剪后，$0.5$ 会被限制到 $0.8$：</p><p>$$<br>\text{clip}(r_t,0.8,1.2)A_t&#x3D;0.8\times(-4)&#x3D;-3.2<br>$$</p><p>PPO 仍然取较小值：</p><p>$$<br>\min(-2,;-3.2)&#x3D;-3.2<br>$$</p><p>因为训练时会最大化这个目标，$-3.2$ 比 $-2$ 更差，所以这种“把坏动作概率一下子压得太低”的更新不会得到额外好处。</p><p>直观地说：</p><pre class="language-text" data-language="text"><code class="language-text">好动作：可以提高概率，但不要提高太猛。坏动作：可以降低概率，但不要降低太猛。</code></pre><h3 id="8-3-用表格总结"><a href="#8-3-用表格总结" class="headerlink" title="8.3 用表格总结"></a>8.3 用表格总结</h3><table><thead><tr><th>情况</th><th align="right">旧概率</th><th align="right">新概率</th><th align="right">$r_t$</th><th align="right">$A_t$</th><th align="right">未裁剪</th><th align="right">裁剪后</th><th align="right">PPO 采用</th></tr></thead><tbody><tr><td>好动作</td><td align="right">0.20</td><td align="right">0.30</td><td align="right">1.5</td><td align="right">5</td><td align="right">7.5</td><td align="right">6</td><td align="right">6</td></tr><tr><td>坏动作</td><td align="right">0.50</td><td align="right">0.25</td><td align="right">0.5</td><td align="right">-4</td><td align="right">-2</td><td align="right">-3.2</td><td align="right">-3.2</td></tr></tbody></table><p>这个例子说明，PPO 的裁剪不是为了阻止策略学习，而是为了避免策略因为一小批数据而变化得太剧烈。</p><h2 id="9-PPO-和-TRPO-的关系"><a href="#9-PPO-和-TRPO-的关系" class="headerlink" title="9. PPO 和 TRPO 的关系"></a>9. PPO 和 TRPO 的关系</h2><p>PPO 的前身思想可以追溯到 TRPO（Trust Region Policy Optimization）。</p><p>TRPO 的目标是：更新策略时限制新旧策略之间的 KL 散度，让新策略不要离旧策略太远。</p><p>可以粗略理解为：</p><pre class="language-text" data-language="text"><code class="language-text">TRPO：明确约束新旧策略距离，但实现更复杂。PPO：用裁剪目标函数近似限制更新幅度，实现更简单。</code></pre><p>PPO 没有严格求解带约束优化问题，而是把“不要改太远”这个想法融入损失函数。这样可以直接使用常规的梯度下降和小批量训练。</p><p>所以 PPO 的流行，很大一部分原因是：</p><ul><li>比普通策略梯度稳定。</li><li>比 TRPO 更容易实现。</li><li>可以复用同一批轨迹数据做多轮 minibatch 更新。</li><li>在连续控制和游戏任务中表现通常不错。</li></ul><h2 id="10-PPO-的完整损失函数"><a href="#10-PPO-的完整损失函数" class="headerlink" title="10. PPO 的完整损失函数"></a>10. PPO 的完整损失函数</h2><p>实际训练中，PPO 通常不只优化策略损失，还会同时训练价值函数，并加入熵奖励鼓励探索。</p><p>常见总目标可以写成：</p><p>$$<br>L(\theta,\phi)&#x3D;<br>L^{CLIP}(\theta)<br>-c_1 L^{VF}(\phi)<br>+c_2 H(\pi_\theta)<br>$$</p><p>其中：</p><ul><li>$L^{CLIP}$ 是策略裁剪目标。</li><li>$L^{VF}$ 是价值函数损失。</li><li>$H(\pi_\theta)$ 是策略熵。</li><li>$c_1$ 和 $c_2$ 是权重系数。</li></ul><p>价值函数损失通常是均方误差：</p><p>$$<br>L^{VF}(\phi)&#x3D;<br>\mathbb{E}_t<br>\left[<br>(V_\phi(s_t)-R_t)^2<br>\right]<br>$$</p><p>熵项用于鼓励策略保持一定随机性：</p><pre class="language-text" data-language="text"><code class="language-text">熵越大，动作分布越分散，探索越多。熵越小，动作分布越确定，探索越少。</code></pre><p>训练早期适当鼓励探索通常有帮助；训练后期策略会逐渐变得更确定。</p><h2 id="11-GAE：更稳定地估计优势"><a href="#11-GAE：更稳定地估计优势" class="headerlink" title="11. GAE：更稳定地估计优势"></a>11. GAE：更稳定地估计优势</h2><p>PPO 中常用 GAE 来估计优势。</p><p>先定义 TD 误差：</p><p>$$<br>\delta_t &#x3D;<br>r_t + \gamma V(s_{t+1}) - V(s_t)<br>$$</p><p>如果使用 GAE，优势估计为：</p><p>$$<br>\hat{A}<em>t &#x3D;<br>\delta_t<br>+(\gamma\lambda)\delta</em>{t+1}<br>+(\gamma\lambda)^2\delta_{t+2}<br>+\cdots<br>$$</p><p>其中：</p><ul><li>$\gamma$ 是折扣因子。</li><li>$\lambda$ 控制偏差和方差的折中。</li></ul><p>当 $\lambda$ 较小，估计更依赖短期 TD 误差，方差较小但偏差可能较大。</p><p>当 $\lambda$ 接近 1，估计更接近长回报，偏差较小但方差可能较大。</p><p>实践中常见取值是 $\lambda&#x3D;0.95$。</p><h2 id="12-PPO-的训练流程"><a href="#12-PPO-的训练流程" class="headerlink" title="12. PPO 的训练流程"></a>12. PPO 的训练流程</h2><p>PPO 的训练通常按下面流程进行：</p><ol><li>使用当前策略 $\pi_{\theta_{old}}$ 和环境交互，收集一批轨迹。</li><li>用奖励和价值函数计算回报 $R_t$。</li><li>用 GAE 或其他方法计算优势 $\hat{A}_t$。</li><li>保存旧策略下每个动作的 log probability。</li><li>把轨迹数据打乱，分成多个 mini-batch。</li><li>对同一批数据训练多个 epoch。</li><li>每次更新时计算新旧策略概率比 $r_t(\theta)$。</li><li>优化裁剪策略损失、价值损失和熵奖励。</li><li>用更新后的策略继续采样，进入下一轮。</li></ol><p><img src="https://lsworl.github.io/images/ml-blog/ppo-training-flow.svg" alt="PPO 的训练流程"></p><p>这和 DQN 这类 off-policy 算法不同。PPO 通常被归为 on-policy 算法，因为它主要使用当前策略刚采集的数据。虽然 PPO 会对同一批数据做多轮更新，但数据不能像 DQN 的经验回放那样长期反复使用。</p><h2 id="13-PPO-伪代码"><a href="#13-PPO-伪代码" class="headerlink" title="13. PPO 伪代码"></a>13. PPO 伪代码</h2><p>PPO 的伪代码可以写成：</p><pre class="language-text" data-language="text"><code class="language-text">initialize actor network pi_thetainitialize critic network V_phifor iteration in training_iterations:    collect trajectories using current policy pi_theta_old    compute returns R_t    compute advantages A_t    store old log probabilities log pi_theta_old(a_t|s_t)    for epoch in update_epochs:        shuffle rollout data        for minibatch in data:            compute new log probabilities log pi_theta(a_t|s_t)            compute ratio r_t = exp(new_log_prob - old_log_prob)            unclipped = r_t * A_t            clipped = clip(r_t, 1-eps, 1+eps) * A_t            policy_loss = -mean(min(unclipped, clipped))            value_loss = mean((V_phi(s_t) - R_t)^2)            entropy_bonus = mean(entropy(pi_theta(.|s_t)))            total_loss = policy_loss + c1 * value_loss - c2 * entropy_bonus            update actor and critic by gradient descent    set theta_old = theta</code></pre><p>注意这里的 <code>policy_loss</code> 前面有负号，是因为很多深度学习框架默认做梯度下降，而 PPO 的策略目标本身是要最大化。</p><h2 id="14-PyTorch-风格简化代码"><a href="#14-PyTorch-风格简化代码" class="headerlink" title="14. PyTorch 风格简化代码"></a>14. PyTorch 风格简化代码</h2><p>下面不是完整可运行的训练脚本，而是展示 PPO 更新部分最核心的代码结构。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> torch<span class="token keyword">import</span> torch<span class="token punctuation">.</span>nn<span class="token punctuation">.</span>functional <span class="token keyword">as</span> F<span class="token keyword">def</span> <span class="token function">ppo_update</span><span class="token punctuation">(</span>    actor<span class="token punctuation">,</span>    critic<span class="token punctuation">,</span>    optimizer<span class="token punctuation">,</span>    states<span class="token punctuation">,</span>    actions<span class="token punctuation">,</span>    old_log_probs<span class="token punctuation">,</span>    returns<span class="token punctuation">,</span>    advantages<span class="token punctuation">,</span>    clip_eps<span class="token operator">=</span><span class="token number">0.2</span><span class="token punctuation">,</span>    value_coef<span class="token operator">=</span><span class="token number">0.5</span><span class="token punctuation">,</span>    entropy_coef<span class="token operator">=</span><span class="token number">0.01</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    <span class="token comment"># 根据当前 actor 重新计算动作分布</span>    dist <span class="token operator">=</span> actor<span class="token punctuation">(</span>states<span class="token punctuation">)</span>    <span class="token comment"># 新策略下，旧动作的 log probability</span>    new_log_probs <span class="token operator">=</span> dist<span class="token punctuation">.</span>log_prob<span class="token punctuation">(</span>actions<span class="token punctuation">)</span>    <span class="token comment"># 新旧策略概率比：pi_new(a|s) / pi_old(a|s)</span>    ratio <span class="token operator">=</span> torch<span class="token punctuation">.</span>exp<span class="token punctuation">(</span>new_log_probs <span class="token operator">-</span> old_log_probs<span class="token punctuation">)</span>    <span class="token comment"># 裁剪前后的策略目标</span>    unclipped_objective <span class="token operator">=</span> ratio <span class="token operator">*</span> advantages    clipped_ratio <span class="token operator">=</span> torch<span class="token punctuation">.</span>clamp<span class="token punctuation">(</span>ratio<span class="token punctuation">,</span> <span class="token number">1</span> <span class="token operator">-</span> clip_eps<span class="token punctuation">,</span> <span class="token number">1</span> <span class="token operator">+</span> clip_eps<span class="token punctuation">)</span>    clipped_objective <span class="token operator">=</span> clipped_ratio <span class="token operator">*</span> advantages    <span class="token comment"># PPO 最大化 min，这里取负号变成最小化 loss</span>    policy_loss <span class="token operator">=</span> <span class="token operator">-</span>torch<span class="token punctuation">.</span>mean<span class="token punctuation">(</span>torch<span class="token punctuation">.</span><span class="token builtin">min</span><span class="token punctuation">(</span>unclipped_objective<span class="token punctuation">,</span> clipped_objective<span class="token punctuation">)</span><span class="token punctuation">)</span>    <span class="token comment"># critic 预测状态价值</span>    values <span class="token operator">=</span> critic<span class="token punctuation">(</span>states<span class="token punctuation">)</span><span class="token punctuation">.</span>squeeze<span class="token punctuation">(</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span>    value_loss <span class="token operator">=</span> F<span class="token punctuation">.</span>mse_loss<span class="token punctuation">(</span>values<span class="token punctuation">,</span> returns<span class="token punctuation">)</span>    <span class="token comment"># 熵奖励鼓励探索</span>    entropy <span class="token operator">=</span> dist<span class="token punctuation">.</span>entropy<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>mean<span class="token punctuation">(</span><span class="token punctuation">)</span>    loss <span class="token operator">=</span> policy_loss <span class="token operator">+</span> value_coef <span class="token operator">*</span> value_loss <span class="token operator">-</span> entropy_coef <span class="token operator">*</span> entropy    optimizer<span class="token punctuation">.</span>zero_grad<span class="token punctuation">(</span><span class="token punctuation">)</span>    loss<span class="token punctuation">.</span>backward<span class="token punctuation">(</span><span class="token punctuation">)</span>    optimizer<span class="token punctuation">.</span>step<span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">return</span> <span class="token punctuation">&#123;</span>        <span class="token string">"policy_loss"</span><span class="token punctuation">:</span> policy_loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"value_loss"</span><span class="token punctuation">:</span> value_loss<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"entropy"</span><span class="token punctuation">:</span> entropy<span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token string">"ratio_mean"</span><span class="token punctuation">:</span> ratio<span class="token punctuation">.</span>mean<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>item<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span></code></pre><p>这里有几个关键点：</p><ul><li><code>old_log_probs</code> 必须来自采样时的旧策略。</li><li><code>new_log_probs</code> 来自当前正在更新的策略。</li><li><code>ratio</code> 用 <code>exp(new_log_prob - old_log_prob)</code> 计算，数值上更稳定。</li><li><code>advantages</code> 通常要标准化，例如减均值、除以标准差。</li><li><code>policy_loss</code>、<code>value_loss</code> 和 <code>entropy</code> 一起构成总损失。</li></ul><h2 id="15-PPO-的关键超参数"><a href="#15-PPO-的关键超参数" class="headerlink" title="15. PPO 的关键超参数"></a>15. PPO 的关键超参数</h2><p>PPO 的效果对超参数比较敏感，常见超参数包括：</p><table><thead><tr><th>超参数</th><th>含义</th><th>常见取值</th></tr></thead><tbody><tr><td>$\gamma$</td><td>折扣因子</td><td>0.99</td></tr><tr><td>$\lambda$</td><td>GAE 参数</td><td>0.95</td></tr><tr><td>$\epsilon$</td><td>裁剪范围</td><td>0.1 或 0.2</td></tr><tr><td>rollout length</td><td>每轮采样步数</td><td>1024、2048、4096</td></tr><tr><td>update epochs</td><td>每批数据更新轮数</td><td>3 到 10</td></tr><tr><td>minibatch size</td><td>小批量大小</td><td>64、128、256</td></tr><tr><td>entropy coef</td><td>熵奖励权重</td><td>0.001 到 0.01</td></tr><tr><td>value coef</td><td>价值损失权重</td><td>0.5</td></tr><tr><td>learning rate</td><td>学习率</td><td>3e-4 左右</td></tr></tbody></table><p>其中最容易影响稳定性的通常是：</p><ul><li>学习率。</li><li>裁剪范围 $\epsilon$。</li><li>每批数据重复更新的 epoch 数。</li><li>优势是否标准化。</li></ul><p>如果学习率太大、裁剪范围太宽、同一批数据更新太多轮，PPO 仍然可能不稳定。</p><h2 id="16-PPO-的优点"><a href="#16-PPO-的优点" class="headerlink" title="16. PPO 的优点"></a>16. PPO 的优点</h2><p>PPO 的优点可以概括为：</p><ul><li>实现相对简单，不需要像 TRPO 那样处理复杂的二阶优化。</li><li>比普通策略梯度稳定。</li><li>能处理离散动作和连续动作。</li><li>可以和神经网络自然结合。</li><li>同一批采样数据可以做多轮 mini-batch 更新，样本利用率比最朴素的策略梯度更高。</li><li>在很多强化学习基准任务中表现可靠。</li></ul><p>这也是为什么很多强化学习入门项目、机器人控制任务和 RLHF 相关训练流程里都会提到 PPO。</p><h2 id="17-PPO-的局限"><a href="#17-PPO-的局限" class="headerlink" title="17. PPO 的局限"></a>17. PPO 的局限</h2><p>PPO 不是万能的。</p><p>第一，它仍然是 on-policy 方法，样本效率不如很多 off-policy 方法。</p><p>也就是说，PPO 不能无限期复用很久以前的数据。策略变化后，旧数据和当前策略差距太大，继续使用会带来偏差。</p><p>第二，它对奖励尺度敏感。</p><p>如果奖励非常大或非常不稳定，优势估计也会不稳定。实践中经常需要奖励归一化、优势标准化或 value clipping。</p><p>第三，裁剪不等于严格信赖域。</p><p>PPO 的裁剪目标只是限制某些样本上的概率比，并不严格保证整体 KL 散度一定很小。因此实践中也常监控 approximate KL，如果 KL 太大就提前停止更新。</p><p>第四，探索能力有限。</p><p>如果环境奖励稀疏，PPO 可能很难发现有效行为，需要额外探索策略、奖励塑形或课程学习。</p><h2 id="18-PPO-和-DQN、A2C、TRPO-的对比"><a href="#18-PPO-和-DQN、A2C、TRPO-的对比" class="headerlink" title="18. PPO 和 DQN、A2C、TRPO 的对比"></a>18. PPO 和 DQN、A2C、TRPO 的对比</h2><table><thead><tr><th>算法</th><th>类型</th><th>主要特点</th></tr></thead><tbody><tr><td>DQN</td><td>价值方法、off-policy</td><td>学习 $Q(s,a)$，适合离散动作</td></tr><tr><td>A2C</td><td>Actor-Critic、on-policy</td><td>同时训练策略和价值函数</td></tr><tr><td>TRPO</td><td>策略优化、on-policy</td><td>用 KL 约束限制策略更新，较复杂</td></tr><tr><td>PPO</td><td>策略优化、on-policy</td><td>用裁剪目标近似限制更新，简单稳定</td></tr></tbody></table><p>可以这样记：</p><pre class="language-text" data-language="text"><code class="language-text">DQN：学哪个动作价值高A2C：边学策略，边学价值TRPO：严格限制策略变化PPO：用裁剪近似限制策略变化</code></pre><h2 id="19-实践建议"><a href="#19-实践建议" class="headerlink" title="19. 实践建议"></a>19. 实践建议</h2><p>实现或调试 PPO 时，可以重点关注这些细节：</p><ol><li>保存采样时的 <code>old_log_probs</code>，不要在更新后重新计算旧概率。</li><li>优势 <code>advantages</code> 通常要标准化。</li><li>监控 entropy，太低说明策略可能过早变得确定。</li><li>监控 approximate KL，过大说明策略更新太猛。</li><li>监控 clip fraction，过高说明大量样本触发裁剪。</li><li>value loss 如果长期很大，说明 critic 学得不好，优势估计也会受影响。</li><li>奖励尺度差异大时，可以考虑 reward normalization。</li><li>连续动作任务中，要注意动作标准差是否塌缩得太快。</li><li>同一批 rollout 不要更新过多 epoch，否则 on-policy 假设会被破坏。</li><li>先在 CartPole、Pendulum 等小环境验证，再上复杂任务。</li></ol><h2 id="20-总结"><a href="#20-总结" class="headerlink" title="20. 总结"></a>20. 总结</h2><p>PPO 的核心思想并不神秘：</p><pre class="language-text" data-language="text"><code class="language-text">根据优势函数判断动作好坏，用新旧策略概率比调整动作概率，再用裁剪限制单次策略更新幅度。</code></pre><p>它最关键的公式是：</p><p>$$<br>r_t(\theta)&#x3D;<br>\frac{\pi_\theta(a_t|s_t)}<br>{\pi_{\theta_{old}}(a_t|s_t)}<br>$$</p><p>以及：</p><p>$$<br>L^{CLIP}(\theta)&#x3D;<br>\mathbb{E}_t<br>\left[<br>\min<br>\left(<br>r_t(\theta)A_t,;<br>\text{clip}(r_t(\theta),1-\epsilon,1+\epsilon)A_t<br>\right)<br>\right]<br>$$</p><p>理解 PPO 时，可以抓住三个关键词：</p><ul><li>优势函数：判断动作比预期好还是差。</li><li>概率比值：衡量新策略相对旧策略变化多少。</li><li>裁剪：限制策略更新不要太激进。</li></ul><p>学完 PPO 后，再看 RLHF、机器人控制、连续动作强化学习和大模型策略优化时，会更容易理解为什么训练过程总是在“提高奖励”和“别偏离太远”之间小心平衡。</p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p>本文主要参考了以下资料：</p><ol><li>John Schulman, Filip Wolski, Prafulla Dhariwal, Alec Radford, Oleg Klimov, <em>Proximal Policy Optimization Algorithms</em>, 2017. arXiv: <a href="https://arxiv.org/abs/1707.06347">https://arxiv.org/abs/1707.06347</a></li><li>John Schulman, Sergey Levine, Philipp Moritz, Michael I. Jordan, Pieter Abbeel, <em>Trust Region Policy Optimization</em>, ICML 2015.</li><li>OpenAI Spinning Up, <em>Proximal Policy Optimization</em>: <a href="https://spinningup.openai.com/en/latest/algorithms/ppo.html">https://spinningup.openai.com/en/latest/algorithms/ppo.html</a></li><li>OpenAI Spinning Up, <em>Key Concepts in RL</em>: <a href="https://spinningup.openai.com/en/latest/spinningup/rl_intro.html">https://spinningup.openai.com/en/latest/spinningup/rl_intro.html</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/06/15/machine-learning-ppo/</id>
    <link href="https://lsworl.github.io/2026/06/15/machine-learning-ppo/"/>
    <published>2026-06-14T16:00:00.000Z</published>
    <summary>从策略梯度、重要性采样比率、优势函数、裁剪目标函数、Actor-Critic结构和PyTorch伪代码几个角度理解PPO算法。</summary>
    <title>PPO算法：从策略梯度到裁剪目标函数</title>
    <updated>2026-06-15T15:44:12.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="算法" scheme="https://lsworl.github.io/categories/%E7%AE%97%E6%B3%95/"/>
    <category term="算法" scheme="https://lsworl.github.io/tags/%E7%AE%97%E6%B3%95/"/>
    <category term="任务调度" scheme="https://lsworl.github.io/tags/%E4%BB%BB%E5%8A%A1%E8%B0%83%E5%BA%A6/"/>
    <category term="异构计算" scheme="https://lsworl.github.io/tags/%E5%BC%82%E6%9E%84%E8%AE%A1%E7%AE%97/"/>
    <category term="DAG" scheme="https://lsworl.github.io/tags/DAG/"/>
    <category term="HEFT" scheme="https://lsworl.github.io/tags/HEFT/"/>
    <content>
      <![CDATA[<p>在并行计算、云计算、边缘计算和异构芯片中，我们经常会遇到这样的问题：一个应用不是一个单独任务，而是一组有依赖关系的子任务；这些子任务可以运行在不同处理器上，但每个处理器的速度并不一样。</p><p>例如：</p><ul><li>图像处理流水线中，解码、滤波、特征提取、分类之间有先后依赖。</li><li>科学计算工作流中，某些矩阵运算必须等前面的数据准备完成。</li><li>CPU、GPU、FPGA 同时存在时，不同任务适合的执行设备不同。</li><li>边缘计算中，任务可以放在本地设备、边缘服务器或云端执行。</li></ul><p>这类问题的核心是：</p><pre class="language-text" data-language="text"><code class="language-text">哪些任务先执行？每个任务放到哪个处理器上执行？怎样让整个工作流尽快完成？</code></pre><p>HEFT（Heterogeneous Earliest Finish Time，异构最早完成时间）就是一个经典的异构任务调度启发式算法。它不保证一定得到全局最优解，但因为思想清晰、计算复杂度相对较低、效果通常不错，所以经常作为异构计算调度问题中的基线算法。</p><p>一句话概括：</p><blockquote><p>HEFT 先根据任务到出口任务的“平均关键路径长度”给任务排序，再按照这个顺序把每个任务放到能最早完成它的处理器上。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/ml-blog/heft-dag-overview.svg" alt="HEFT 将有依赖关系的任务 DAG 映射到异构处理器上"></p><h2 id="1-为什么需要-HEFT"><a href="#1-为什么需要-HEFT" class="headerlink" title="1. 为什么需要 HEFT"></a>1. 为什么需要 HEFT</h2><p>假设我们有 4 个任务：</p><pre class="language-text" data-language="text"><code class="language-text">T1 -> T2 -> T4T1 -> T3 -> T4</code></pre><p>其中 <code>T2</code> 和 <code>T3</code> 都依赖 <code>T1</code>，<code>T4</code> 必须等 <code>T2</code> 和 <code>T3</code> 都完成后才能执行。</p><p>如果只有一个处理器，调度问题比较简单：按依赖顺序一个个执行就行。</p><p>但如果有多个处理器，而且处理器性能不同，问题就复杂了：</p><table><thead><tr><th>任务</th><th align="right">CPU 执行时间</th><th align="right">GPU 执行时间</th></tr></thead><tbody><tr><td>T1</td><td align="right">8</td><td align="right">4</td></tr><tr><td>T2</td><td align="right">6</td><td align="right">9</td></tr><tr><td>T3</td><td align="right">7</td><td align="right">3</td></tr><tr><td>T4</td><td align="right">5</td><td align="right">6</td></tr></tbody></table><p>可以看到：</p><ul><li><code>T1</code> 和 <code>T3</code> 在 GPU 上更快。</li><li><code>T2</code> 和 <code>T4</code> 在 CPU 上更快。</li><li>如果父任务和子任务被放到不同处理器上，还可能产生数据传输时间。</li></ul><p>所以调度器不能只看某个任务在哪个处理器上跑得最快，还要考虑依赖关系、通信代价、处理器空闲时间和全局完成时间。</p><p>HEFT 就是为这种“有依赖的异构任务调度”设计的。</p><h2 id="2-HEFT-解决的问题长什么样"><a href="#2-HEFT-解决的问题长什么样" class="headerlink" title="2. HEFT 解决的问题长什么样"></a>2. HEFT 解决的问题长什么样</h2><p>HEFT 的输入通常包括四类信息。</p><p>第一类是任务集合：</p><p>$$<br>V &#x3D; {v_1, v_2, \dots, v_n}<br>$$</p><p>每个 $v_i$ 表示一个任务。</p><p>第二类是任务依赖关系。通常用一个有向无环图（Directed Acyclic Graph，DAG）表示：</p><p>$$<br>G&#x3D;(V,E)<br>$$</p><p>如果存在边 $(v_i, v_j)$，表示任务 $v_j$ 必须等待任务 $v_i$ 完成，并拿到它产生的数据之后才能开始。</p><p>第三类是处理器集合：</p><p>$$<br>P &#x3D; {p_1, p_2, \dots, p_m}<br>$$</p><p>每个处理器可以是 CPU 核、GPU、FPGA、边缘节点或云服务器。</p><p>第四类是代价信息：</p><ul><li>$w_{i,k}$：任务 $v_i$ 在处理器 $p_k$ 上的计算时间。</li><li>$c_{i,j}$：任务 $v_i$ 到任务 $v_j$ 的平均通信时间。</li></ul><p>HEFT 的目标通常是最小化整个任务图的完成时间，也就是 makespan：</p><p>$$<br>\text{makespan}&#x3D;\max_i \text{finish}(v_i)<br>$$</p><p>直观地说，就是让最后一个任务尽早完成。</p><h2 id="3-HEFT-的整体思路"><a href="#3-HEFT-的整体思路" class="headerlink" title="3. HEFT 的整体思路"></a>3. HEFT 的整体思路</h2><p>HEFT 分成两个阶段：</p><ol><li>任务排序阶段：计算每个任务的向上排序值 $rank_u$，按照从大到小排序。</li><li>处理器选择阶段：按排序后的任务列表依次调度，每个任务选择最早完成它的处理器。</li></ol><p><img src="https://lsworl.github.io/images/ml-blog/heft-two-phases.svg" alt="HEFT 的两个主要阶段"></p><p>可以把 HEFT 理解成一种列表调度（List Scheduling）：</p><pre class="language-text" data-language="text"><code class="language-text">先把任务排成一个优先级列表再按列表顺序逐个安排到处理器上</code></pre><p>它的关键就在于两个问题：</p><ul><li>排序时，怎样判断一个任务更重要？</li><li>分配时，怎样判断一个处理器更合适？</li></ul><p>HEFT 分别用“向上排序值”和“最早完成时间”来回答这两个问题。</p><h2 id="4-平均计算代价"><a href="#4-平均计算代价" class="headerlink" title="4. 平均计算代价"></a>4. 平均计算代价</h2><p>因为处理器是异构的，同一个任务在不同处理器上的执行时间可能不同。</p><p>为了在排序阶段先不纠结具体放到哪个处理器上，HEFT 会先计算每个任务的平均计算代价：</p><p>$$<br>\overline{w_i}&#x3D;\frac{1}{m}\sum_{k&#x3D;1}^{m}w_{i,k}<br>$$</p><p>其中：</p><ul><li>$w_{i,k}$ 表示任务 $v_i$ 在处理器 $p_k$ 上的计算时间。</li><li>$m$ 表示处理器数量。</li><li>$\overline{w_i}$ 表示任务 $v_i$ 在所有处理器上的平均执行时间。</li></ul><p>例如：</p><table><thead><tr><th>任务</th><th align="right">CPU</th><th align="right">GPU</th><th align="right">平均计算代价</th></tr></thead><tbody><tr><td>T1</td><td align="right">8</td><td align="right">4</td><td align="right">6</td></tr><tr><td>T2</td><td align="right">6</td><td align="right">9</td><td align="right">7.5</td></tr><tr><td>T3</td><td align="right">7</td><td align="right">3</td><td align="right">5</td></tr><tr><td>T4</td><td align="right">5</td><td align="right">6</td><td align="right">5.5</td></tr></tbody></table><p>这个平均值不是最终执行时间，而是用于估计任务在 DAG 中的重要程度。</p><h2 id="5-平均通信代价"><a href="#5-平均通信代价" class="headerlink" title="5. 平均通信代价"></a>5. 平均通信代价</h2><p>如果任务 $v_i$ 和任务 $v_j$ 之间有依赖，并且它们被分配到不同处理器上，就可能需要传输数据。</p><p>HEFT 在计算任务优先级时，会使用平均通信代价 $\overline{c_{i,j}}$。</p><p>在很多简化例子中，可以直接给每条边一个平均通信时间。例如：</p><table><thead><tr><th>依赖边</th><th align="right">平均通信代价</th></tr></thead><tbody><tr><td>T1 -&gt; T2</td><td align="right">2</td></tr><tr><td>T1 -&gt; T3</td><td align="right">2</td></tr><tr><td>T2 -&gt; T4</td><td align="right">3</td></tr><tr><td>T3 -&gt; T4</td><td align="right">1</td></tr></tbody></table><p>如果父任务和子任务被放在同一个处理器上，实际调度时通信代价通常可以看成 0；如果放在不同处理器上，就需要加上对应通信代价。</p><p>这也是 HEFT 相比简单“最快处理器优先”更合理的地方：它不会只看计算速度，还会考虑跨处理器传输数据的成本。</p><h2 id="6-向上排序值：任务离终点还有多远"><a href="#6-向上排序值：任务离终点还有多远" class="headerlink" title="6. 向上排序值：任务离终点还有多远"></a>6. 向上排序值：任务离终点还有多远</h2><p>HEFT 排序阶段最重要的概念是向上排序值（upward rank），通常写作 $rank_u(v_i)$。</p><p>它可以理解为：</p><blockquote><p>从任务 $v_i$ 出发，沿着依赖边走到出口任务，平均还需要多少计算和通信代价。</p></blockquote><p>出口任务指的是没有后继任务的任务。对于一个出口任务 $v_i$，它的向上排序值就是自己的平均计算代价：</p><p>$$<br>rank_u(v_i)&#x3D;\overline{w_i}<br>$$</p><p>对于普通任务，递推公式是：</p><p>$$<br>rank_u(v_i)&#x3D;\overline{w_i}+<br>\max_{v_j \in succ(v_i)}<br>\left(\overline{c_{i,j}}+rank_u(v_j)\right)<br>$$</p><p>其中：</p><ul><li>$succ(v_i)$ 表示任务 $v_i$ 的直接后继任务集合。</li><li>$\overline{w_i}$ 是任务 $v_i$ 的平均计算代价。</li><li>$\overline{c_{i,j}}$ 是从任务 $v_i$ 到任务 $v_j$ 的平均通信代价。</li><li>$\max$ 表示只取最“长”的后继路径。</li></ul><p><img src="https://lsworl.github.io/images/ml-blog/heft-upward-rank.svg" alt="向上排序值衡量任务到出口任务的平均最长路径"></p><p>为什么要取最大值？</p><p>因为调度的总完成时间往往由关键路径决定。一个任务如果位于很长的后继链条上，那么它越早执行，越可能缩短整体完成时间。</p><h2 id="7-手工计算向上排序值"><a href="#7-手工计算向上排序值" class="headerlink" title="7. 手工计算向上排序值"></a>7. 手工计算向上排序值</h2><p>继续使用这个 DAG：</p><p><img src="https://lsworl.github.io/images/ml-blog/heft-upward-rank.svg" alt="向上排序值衡量任务到出口任务的平均最长路径"></p><p>平均计算代价（前面所计算的）为：</p><table><thead><tr><th>任务</th><th align="right">平均计算代价</th></tr></thead><tbody><tr><td>T1</td><td align="right">6</td></tr><tr><td>T2</td><td align="right">7.5</td></tr><tr><td>T3</td><td align="right">5</td></tr><tr><td>T4</td><td align="right">5.5</td></tr></tbody></table><p>平均通信代价为：</p><table><thead><tr><th>依赖边</th><th align="right">平均通信代价</th></tr></thead><tbody><tr><td>T1 -&gt; T2</td><td align="right">2</td></tr><tr><td>T1 -&gt; T3</td><td align="right">2</td></tr><tr><td>T2 -&gt; T4</td><td align="right">3</td></tr><tr><td>T3 -&gt; T4</td><td align="right">1</td></tr></tbody></table><p>先计算出口任务 <code>T4</code>：</p><p>$$<br>rank_u(T4)&#x3D;5.5<br>$$</p><p>再计算 <code>T2</code>：</p><p>$$<br>rank_u(T2)&#x3D;7.5+(3+5.5)&#x3D;16<br>$$</p><p>再计算 <code>T3</code>：</p><p>$$<br>rank_u(T3)&#x3D;5+(1+5.5)&#x3D;11.5<br>$$</p><p>最后计算 <code>T1</code>：</p><p>$$<br>rank_u(T1)&#x3D;6+\max(2+16,;2+11.5)&#x3D;24<br>$$</p><p>所以排序结果为：</p><table><thead><tr><th>任务</th><th align="right">向上排序值</th></tr></thead><tbody><tr><td>T1</td><td align="right">24</td></tr><tr><td>T2</td><td align="right">16</td></tr><tr><td>T3</td><td align="right">11.5</td></tr><tr><td>T4</td><td align="right">5.5</td></tr></tbody></table><p>HEFT 会按照从大到小的顺序调度：</p><pre class="language-text" data-language="text"><code class="language-text">T1 -> T2 -> T3 -> T4</code></pre><p>注意，这个顺序不违反依赖关系。<code>T4</code> 虽然排在最后，也正好需要等待 <code>T2</code> 和 <code>T3</code> 完成。</p><h2 id="8-最早开始时间和最早完成时间"><a href="#8-最早开始时间和最早完成时间" class="headerlink" title="8. 最早开始时间和最早完成时间"></a>8. 最早开始时间和最早完成时间</h2><p>排序之后，HEFT 会按任务优先级逐个选择处理器。</p><p>对于任务 $v_i$ 和处理器 $p_k$，需要计算两个时间：</p><ul><li>最早开始时间（Earliest Start Time，EST）。</li><li>最早完成时间（Earliest Finish Time，EFT）。</li></ul><p>最早开始时间由两个因素共同决定：</p><ol><li>处理器 $p_k$ 自己什么时候空闲。</li><li>任务 $v_i$ 的所有父任务结果什么时候能传过来。</li></ol><p>可以写成：</p><p>$$<br>EST(v_i,p_k)&#x3D;<br>\max<br>\left(<br>avail(p_k),<br>\max_{v_h \in pred(v_i)}<br>\left(finish(v_h)+comm(v_h,v_i)\right)<br>\right)<br>$$</p><p>其中：</p><ul><li>$avail(p_k)$ 表示处理器 $p_k$ 当前最早可用时间。</li><li>$pred(v_i)$ 表示任务 $v_i$ 的前驱任务集合。</li><li>$finish(v_h)$ 表示父任务 $v_h$ 的完成时间。</li><li>$comm(v_h,v_i)$ 表示父任务结果传到当前处理器的通信时间。</li></ul><p>最早完成时间就是：</p><p>$$<br>EFT(v_i,p_k)&#x3D;EST(v_i,p_k)+w_{i,k}<br>$$</p><p>HEFT 会把任务放到让 $EFT$ 最小的处理器上。</p><p><img src="https://lsworl.github.io/images/ml-blog/heft-earliest-finish-time.svg" alt="HEFT 为每个候选处理器计算最早完成时间"></p><h2 id="9-调度阶段的小例子"><a href="#9-调度阶段的小例子" class="headerlink" title="9. 调度阶段的小例子"></a>9. 调度阶段的小例子</h2><p>还是使用前面的任务执行时间：</p><table><thead><tr><th>任务</th><th align="right">CPU</th><th align="right">GPU</th></tr></thead><tbody><tr><td>T1</td><td align="right">8</td><td align="right">4</td></tr><tr><td>T2</td><td align="right">6</td><td align="right">9</td></tr><tr><td>T3</td><td align="right">7</td><td align="right">3</td></tr><tr><td>T4</td><td align="right">5</td><td align="right">6</td></tr></tbody></table><p>任务顺序为：</p><pre class="language-text" data-language="text"><code class="language-text">T1 -> T2 -> T3 -> T4</code></pre><h3 id="9-1-调度-T1"><a href="#9-1-调度-T1" class="headerlink" title="9.1 调度 T1"></a>9.1 调度 T1</h3><p><code>T1</code> 没有前驱任务，可以直接开始。</p><ul><li>放 CPU：$EFT&#x3D;0+8&#x3D;8$</li><li>放 GPU：$EFT&#x3D;0+4&#x3D;4$</li></ul><p>所以 <code>T1</code> 放到 GPU，时间为 $0 \sim 4$。</p><h3 id="9-2-调度-T2"><a href="#9-2-调度-T2" class="headerlink" title="9.2 调度 T2"></a>9.2 调度 T2</h3><p><code>T2</code> 依赖 <code>T1</code>。假设 <code>T1 -&gt; T2</code> 的通信代价为 2。</p><ul><li>放 CPU：<code>T1</code> 在 GPU，跨处理器通信，最早开始 $4+2&#x3D;6$，完成 $6+6&#x3D;12$。</li><li>放 GPU：同处理器不需要通信，但 GPU 到时间 4 才空闲，完成 $4+9&#x3D;13$。</li></ul><p>所以 <code>T2</code> 放到 CPU，时间为 $6 \sim 12$。</p><h3 id="9-3-调度-T3"><a href="#9-3-调度-T3" class="headerlink" title="9.3 调度 T3"></a>9.3 调度 T3</h3><p><code>T3</code> 也依赖 <code>T1</code>，通信代价为 2。</p><ul><li>放 CPU：CPU 已经安排了 <code>T2</code>，如果简单按尾部追加，需要等到 12，完成 $12+7&#x3D;19$。</li><li>放 GPU：GPU 在时间 4 空闲，同处理器无通信，完成 $4+3&#x3D;7$。</li></ul><p>所以 <code>T3</code> 放到 GPU，时间为 $4 \sim 7$。</p><h3 id="9-4-调度-T4"><a href="#9-4-调度-T4" class="headerlink" title="9.4 调度 T4"></a>9.4 调度 T4</h3><p><code>T4</code> 同时依赖 <code>T2</code> 和 <code>T3</code>。</p><p>假设：</p><ul><li><code>T2 -&gt; T4</code> 通信代价为 3。</li><li><code>T3 -&gt; T4</code> 通信代价为 1。</li></ul><p>如果放 CPU：</p><ul><li>来自 <code>T2</code>：同处理器，数据到达时间 $12$。</li><li>来自 <code>T3</code>：GPU 到 CPU，需要通信，数据到达时间 $7+1&#x3D;8$。</li><li>CPU 在时间 12 空闲，所以 $EST&#x3D;12$。</li><li>$EFT&#x3D;12+5&#x3D;17$。</li></ul><p>如果放 GPU：</p><ul><li>来自 <code>T2</code>：CPU 到 GPU，需要通信，数据到达时间 $12+3&#x3D;15$。</li><li>来自 <code>T3</code>：同处理器，数据到达时间 $7$。</li><li>GPU 在时间 7 空闲，但必须等 <code>T2</code> 数据到达，所以 $EST&#x3D;15$。</li><li>$EFT&#x3D;15+6&#x3D;21$。</li></ul><p>所以 <code>T4</code> 放到 CPU，时间为 $12 \sim 17$。</p><p>最终 makespan 为 17。</p><p><img src="https://lsworl.github.io/images/ml-blog/heft-gantt-example.svg" alt="HEFT 调度结果可以用甘特图表示"></p><h2 id="10-插入式调度是什么意思"><a href="#10-插入式调度是什么意思" class="headerlink" title="10. 插入式调度是什么意思"></a>10. 插入式调度是什么意思</h2><p>严格来说，HEFT 的处理器选择阶段常使用插入式调度（insertion-based scheduling）。</p><p>这意味着：给某个任务选择处理器时，不一定只能把任务追加到该处理器最后，也可以检查已有任务之间是否存在足够大的空隙。</p><p>例如某个处理器上已有任务：</p><pre class="language-text" data-language="text"><code class="language-text">0 ~ 3 执行 A8 ~ 12 执行 B</code></pre><p>如果当前任务最早可以在时间 4 开始，执行时间为 3，那么它可以插入到 $4 \sim 7$，而不是等到 12 以后再执行。</p><p>插入式调度能更充分利用处理器空闲片段，尤其是在任务依赖和通信导致空洞较多时，会比简单追加更好。</p><h2 id="11-HEFT-算法步骤"><a href="#11-HEFT-算法步骤" class="headerlink" title="11. HEFT 算法步骤"></a>11. HEFT 算法步骤</h2><p>HEFT 的完整流程可以概括为：</p><ol><li>根据任务图和处理器执行时间，计算每个任务的平均计算代价。</li><li>根据任务依赖边，计算平均通信代价。</li><li>从出口任务开始，递归计算每个任务的 $rank_u$。</li><li>按照 $rank_u$ 从大到小得到任务优先级列表。</li><li>依次取出列表中的任务。</li><li>对每个候选处理器计算该任务的 $EST$ 和 $EFT$。</li><li>把任务分配给 $EFT$ 最小的处理器。</li><li>更新处理器时间表和任务完成时间。</li><li>所有任务都调度完成后，得到最终 makespan。</li></ol><p>伪代码可以写成：</p><pre class="language-text" data-language="text"><code class="language-text">for each task:    compute average computation costfor each edge:    compute average communication costcompute upward rank for each tasktask_list = tasks sorted by decreasing upward rankfor task in task_list:    best_processor = None    best_finish_time = infinity    for processor in processors:        est = earliest_start_time(task, processor)        eft = est + computation_cost(task, processor)        if eft &lt; best_finish_time:            best_processor = processor            best_finish_time = eft    schedule task on best_processor</code></pre><h2 id="12-Python-手写实现"><a href="#12-Python-手写实现" class="headerlink" title="12. Python 手写实现"></a>12. Python 手写实现</h2><p>下面写一个简化版 HEFT。</p><p>为了让代码更容易理解，这里先使用“追加式调度”，也就是每个任务只能排在处理器已有任务之后，不做空隙插入。核心思想和 HEFT 一致，只是少了插入空洞的细节。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> functools <span class="token keyword">import</span> lru_cachetasks <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token string">"T1"</span><span class="token punctuation">,</span> <span class="token string">"T2"</span><span class="token punctuation">,</span> <span class="token string">"T3"</span><span class="token punctuation">,</span> <span class="token string">"T4"</span><span class="token punctuation">]</span>processors <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token string">"CPU"</span><span class="token punctuation">,</span> <span class="token string">"GPU"</span><span class="token punctuation">]</span>computation_cost <span class="token operator">=</span> <span class="token punctuation">&#123;</span>    <span class="token string">"T1"</span><span class="token punctuation">:</span> <span class="token punctuation">&#123;</span><span class="token string">"CPU"</span><span class="token punctuation">:</span> <span class="token number">8</span><span class="token punctuation">,</span> <span class="token string">"GPU"</span><span class="token punctuation">:</span> <span class="token number">4</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token string">"T2"</span><span class="token punctuation">:</span> <span class="token punctuation">&#123;</span><span class="token string">"CPU"</span><span class="token punctuation">:</span> <span class="token number">6</span><span class="token punctuation">,</span> <span class="token string">"GPU"</span><span class="token punctuation">:</span> <span class="token number">9</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token string">"T3"</span><span class="token punctuation">:</span> <span class="token punctuation">&#123;</span><span class="token string">"CPU"</span><span class="token punctuation">:</span> <span class="token number">7</span><span class="token punctuation">,</span> <span class="token string">"GPU"</span><span class="token punctuation">:</span> <span class="token number">3</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>    <span class="token string">"T4"</span><span class="token punctuation">:</span> <span class="token punctuation">&#123;</span><span class="token string">"CPU"</span><span class="token punctuation">:</span> <span class="token number">5</span><span class="token punctuation">,</span> <span class="token string">"GPU"</span><span class="token punctuation">:</span> <span class="token number">6</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span>successors <span class="token operator">=</span> <span class="token punctuation">&#123;</span>    <span class="token string">"T1"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"T2"</span><span class="token punctuation">,</span> <span class="token string">"T3"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"T2"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"T4"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"T3"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"T4"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"T4"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span>predecessors <span class="token operator">=</span> <span class="token punctuation">&#123;</span>    <span class="token string">"T1"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"T2"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"T1"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"T3"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"T1"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"T4"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"T2"</span><span class="token punctuation">,</span> <span class="token string">"T3"</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span>communication_cost <span class="token operator">=</span> <span class="token punctuation">&#123;</span>    <span class="token punctuation">(</span><span class="token string">"T1"</span><span class="token punctuation">,</span> <span class="token string">"T2"</span><span class="token punctuation">)</span><span class="token punctuation">:</span> <span class="token number">2</span><span class="token punctuation">,</span>    <span class="token punctuation">(</span><span class="token string">"T1"</span><span class="token punctuation">,</span> <span class="token string">"T3"</span><span class="token punctuation">)</span><span class="token punctuation">:</span> <span class="token number">2</span><span class="token punctuation">,</span>    <span class="token punctuation">(</span><span class="token string">"T2"</span><span class="token punctuation">,</span> <span class="token string">"T4"</span><span class="token punctuation">)</span><span class="token punctuation">:</span> <span class="token number">3</span><span class="token punctuation">,</span>    <span class="token punctuation">(</span><span class="token string">"T3"</span><span class="token punctuation">,</span> <span class="token string">"T4"</span><span class="token punctuation">)</span><span class="token punctuation">:</span> <span class="token number">1</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span><span class="token keyword">def</span> <span class="token function">average_computation_cost</span><span class="token punctuation">(</span>task<span class="token punctuation">)</span><span class="token punctuation">:</span>    total <span class="token operator">=</span> <span class="token builtin">sum</span><span class="token punctuation">(</span>computation_cost<span class="token punctuation">[</span>task<span class="token punctuation">]</span><span class="token punctuation">[</span>p<span class="token punctuation">]</span> <span class="token keyword">for</span> p <span class="token keyword">in</span> processors<span class="token punctuation">)</span>    <span class="token keyword">return</span> total <span class="token operator">/</span> <span class="token builtin">len</span><span class="token punctuation">(</span>processors<span class="token punctuation">)</span><span class="token decorator annotation punctuation">@lru_cache</span><span class="token punctuation">(</span><span class="token boolean">None</span><span class="token punctuation">)</span><span class="token keyword">def</span> <span class="token function">upward_rank</span><span class="token punctuation">(</span>task<span class="token punctuation">)</span><span class="token punctuation">:</span>    avg_compute <span class="token operator">=</span> average_computation_cost<span class="token punctuation">(</span>task<span class="token punctuation">)</span>    <span class="token keyword">if</span> <span class="token keyword">not</span> successors<span class="token punctuation">[</span>task<span class="token punctuation">]</span><span class="token punctuation">:</span>        <span class="token keyword">return</span> avg_compute    longest_child_path <span class="token operator">=</span> <span class="token builtin">max</span><span class="token punctuation">(</span>        communication_cost<span class="token punctuation">[</span><span class="token punctuation">(</span>task<span class="token punctuation">,</span> child<span class="token punctuation">)</span><span class="token punctuation">]</span> <span class="token operator">+</span> upward_rank<span class="token punctuation">(</span>child<span class="token punctuation">)</span>        <span class="token keyword">for</span> child <span class="token keyword">in</span> successors<span class="token punctuation">[</span>task<span class="token punctuation">]</span>    <span class="token punctuation">)</span>    <span class="token keyword">return</span> avg_compute <span class="token operator">+</span> longest_child_pathrank <span class="token operator">=</span> <span class="token punctuation">&#123;</span>task<span class="token punctuation">:</span> upward_rank<span class="token punctuation">(</span>task<span class="token punctuation">)</span> <span class="token keyword">for</span> task <span class="token keyword">in</span> tasks<span class="token punctuation">&#125;</span>task_order <span class="token operator">=</span> <span class="token builtin">sorted</span><span class="token punctuation">(</span>tasks<span class="token punctuation">,</span> key<span class="token operator">=</span><span class="token keyword">lambda</span> task<span class="token punctuation">:</span> rank<span class="token punctuation">[</span>task<span class="token punctuation">]</span><span class="token punctuation">,</span> reverse<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span>processor_available_time <span class="token operator">=</span> <span class="token punctuation">&#123;</span>p<span class="token punctuation">:</span> <span class="token number">0</span> <span class="token keyword">for</span> p <span class="token keyword">in</span> processors<span class="token punctuation">&#125;</span>task_schedule <span class="token operator">=</span> <span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span><span class="token keyword">for</span> task <span class="token keyword">in</span> task_order<span class="token punctuation">:</span>    best_processor <span class="token operator">=</span> <span class="token boolean">None</span>    best_start_time <span class="token operator">=</span> <span class="token boolean">None</span>    best_finish_time <span class="token operator">=</span> <span class="token builtin">float</span><span class="token punctuation">(</span><span class="token string">"inf"</span><span class="token punctuation">)</span>    <span class="token keyword">for</span> processor <span class="token keyword">in</span> processors<span class="token punctuation">:</span>        data_ready_time <span class="token operator">=</span> <span class="token number">0</span>        <span class="token keyword">for</span> parent <span class="token keyword">in</span> predecessors<span class="token punctuation">[</span>task<span class="token punctuation">]</span><span class="token punctuation">:</span>            parent_processor <span class="token operator">=</span> task_schedule<span class="token punctuation">[</span>parent<span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token string">"processor"</span><span class="token punctuation">]</span>            parent_finish <span class="token operator">=</span> task_schedule<span class="token punctuation">[</span>parent<span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token string">"finish"</span><span class="token punctuation">]</span>            <span class="token keyword">if</span> parent_processor <span class="token operator">==</span> processor<span class="token punctuation">:</span>                transfer_time <span class="token operator">=</span> <span class="token number">0</span>            <span class="token keyword">else</span><span class="token punctuation">:</span>                transfer_time <span class="token operator">=</span> communication_cost<span class="token punctuation">[</span><span class="token punctuation">(</span>parent<span class="token punctuation">,</span> task<span class="token punctuation">)</span><span class="token punctuation">]</span>            data_ready_time <span class="token operator">=</span> <span class="token builtin">max</span><span class="token punctuation">(</span>data_ready_time<span class="token punctuation">,</span> parent_finish <span class="token operator">+</span> transfer_time<span class="token punctuation">)</span>        start_time <span class="token operator">=</span> <span class="token builtin">max</span><span class="token punctuation">(</span>processor_available_time<span class="token punctuation">[</span>processor<span class="token punctuation">]</span><span class="token punctuation">,</span> data_ready_time<span class="token punctuation">)</span>        finish_time <span class="token operator">=</span> start_time <span class="token operator">+</span> computation_cost<span class="token punctuation">[</span>task<span class="token punctuation">]</span><span class="token punctuation">[</span>processor<span class="token punctuation">]</span>        <span class="token keyword">if</span> finish_time <span class="token operator">&lt;</span> best_finish_time<span class="token punctuation">:</span>            best_processor <span class="token operator">=</span> processor            best_start_time <span class="token operator">=</span> start_time            best_finish_time <span class="token operator">=</span> finish_time    task_schedule<span class="token punctuation">[</span>task<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">&#123;</span>        <span class="token string">"processor"</span><span class="token punctuation">:</span> best_processor<span class="token punctuation">,</span>        <span class="token string">"start"</span><span class="token punctuation">:</span> best_start_time<span class="token punctuation">,</span>        <span class="token string">"finish"</span><span class="token punctuation">:</span> best_finish_time<span class="token punctuation">,</span>    <span class="token punctuation">&#125;</span>    processor_available_time<span class="token punctuation">[</span>best_processor<span class="token punctuation">]</span> <span class="token operator">=</span> best_finish_time<span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"向上排序值："</span><span class="token punctuation">)</span><span class="token keyword">for</span> task <span class="token keyword">in</span> task_order<span class="token punctuation">:</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>task<span class="token punctuation">,</span> rank<span class="token punctuation">[</span>task<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"\n调度结果："</span><span class="token punctuation">)</span><span class="token keyword">for</span> task <span class="token keyword">in</span> task_order<span class="token punctuation">:</span>    item <span class="token operator">=</span> task_schedule<span class="token punctuation">[</span>task<span class="token punctuation">]</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>        task<span class="token punctuation">,</span>        <span class="token string">"->"</span><span class="token punctuation">,</span>        item<span class="token punctuation">[</span><span class="token string">"processor"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>        <span class="token string-interpolation"><span class="token string">f"</span><span class="token interpolation"><span class="token punctuation">&#123;</span>item<span class="token punctuation">[</span><span class="token string">'start'</span><span class="token punctuation">]</span><span class="token punctuation">&#125;</span></span><span class="token string"> ~ </span><span class="token interpolation"><span class="token punctuation">&#123;</span>item<span class="token punctuation">[</span><span class="token string">'finish'</span><span class="token punctuation">]</span><span class="token punctuation">&#125;</span></span><span class="token string">"</span></span><span class="token punctuation">,</span>    <span class="token punctuation">)</span>makespan <span class="token operator">=</span> <span class="token builtin">max</span><span class="token punctuation">(</span>item<span class="token punctuation">[</span><span class="token string">"finish"</span><span class="token punctuation">]</span> <span class="token keyword">for</span> item <span class="token keyword">in</span> task_schedule<span class="token punctuation">.</span>values<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"\nMakespan:"</span><span class="token punctuation">,</span> makespan<span class="token punctuation">)</span></code></pre><p>可能输出：</p><pre class="language-text" data-language="text"><code class="language-text">向上排序值：T1 24.0T2 16.0T3 11.5T4 5.5调度结果：T1 -> GPU 0 ~ 4T2 -> CPU 6 ~ 12T3 -> GPU 4 ~ 7T4 -> CPU 12 ~ 17Makespan: 17</code></pre><p>这和前面的手工分析一致。</p><h2 id="13-HEFT-的时间复杂度"><a href="#13-HEFT-的时间复杂度" class="headerlink" title="13. HEFT 的时间复杂度"></a>13. HEFT 的时间复杂度</h2><p>HEFT 的复杂度主要来自两个阶段。</p><p>排序阶段需要计算任务的向上排序值。对于 DAG 来说，可以通过递归记忆化或逆拓扑顺序完成，复杂度大致与任务数和边数相关：</p><p>$$<br>O(|V|+|E|)<br>$$</p><p>调度阶段中，每个任务都要尝试每个处理器。若使用简单追加式调度，复杂度大致为：</p><p>$$<br>O(|V||P|)<br>$$</p><p>如果使用插入式调度，还需要检查处理器时间表中的空隙，复杂度会更高。假设每个处理器上的已调度任务数量最多为 $|V|$，调度阶段可能达到：</p><p>$$<br>O(|V|^2|P|)<br>$$</p><p>即便如此，相比穷举所有任务与处理器组合，HEFT 仍然非常高效。</p><h2 id="14-HEFT-的优点"><a href="#14-HEFT-的优点" class="headerlink" title="14. HEFT 的优点"></a>14. HEFT 的优点</h2><p>HEFT 的优点很明显：</p><ul><li>思想简单，容易实现。</li><li>同时考虑任务依赖、处理器异构性和通信开销。</li><li>通过向上排序值近似抓住关键路径。</li><li>贪心调度速度快，适合作为基线算法。</li><li>在很多静态 DAG 调度问题上表现稳定。</li></ul><p>所谓静态调度，是指任务图、执行时间和通信代价在调度前大致已知。HEFT 正适合这类场景。</p><h2 id="15-HEFT-的局限"><a href="#15-HEFT-的局限" class="headerlink" title="15. HEFT 的局限"></a>15. HEFT 的局限</h2><p>HEFT 也有一些明显局限。</p><p>第一，它是启发式算法，不保证全局最优。</p><p>HEFT 每次只选择当前任务的最早完成处理器，但有时当前看起来最优的选择，会影响后面更重要任务的安排。</p><p>第二，它依赖代价估计。</p><p>如果任务执行时间和通信时间估计不准，向上排序值和最早完成时间都会受影响。现实系统中，GPU 负载、网络拥塞、缓存状态都可能导致代价波动。</p><p>第三，它主要面向静态 DAG。</p><p>如果任务是动态到达的，或者运行时才知道新的依赖关系，就需要 HEFT 的动态变体，或者使用运行时调度算法。</p><p>第四，它的目标通常是 makespan。</p><p>但有些场景更关心吞吐量、能耗、成本、截止时间或公平性。此时需要修改目标函数，或者使用其他调度算法。</p><h2 id="16-实践建议"><a href="#16-实践建议" class="headerlink" title="16. 实践建议"></a>16. 实践建议</h2><p>在实际使用 HEFT 或实现类似调度器时，可以重点关注以下几点：</p><ol><li>先确认任务依赖图必须是 DAG，不能有循环依赖。</li><li>计算时间和通信时间最好来自实际 profiling，而不是凭感觉估计。</li><li>如果通信代价很高，尽量关注父子任务是否被频繁分配到不同处理器。</li><li>如果处理器经常出现空洞，应该实现插入式调度，而不是简单追加。</li><li>如果任务运行时间波动很大，可以考虑运行时重新调度或动态 HEFT。</li><li>如果目标不只是 makespan，需要把能耗、成本或截止时间加入评价函数。</li><li>先用小 DAG 手工验证，再扩展到真实工作流。</li></ol><h2 id="17-总结"><a href="#17-总结" class="headerlink" title="17. 总结"></a>17. 总结</h2><p>HEFT 是异构计算中非常经典的 DAG 任务调度算法。</p><p>它的核心可以概括为两句话：</p><ul><li>用向上排序值 $rank_u$ 估计任务到出口任务的平均关键路径长度，从而决定任务优先级。</li><li>按优先级依次调度任务，每次选择能让当前任务最早完成的处理器。</li></ul><p>从公式上看，它的关键是：</p><p>$$<br>rank_u(v_i)&#x3D;\overline{w_i}+<br>\max_{v_j \in succ(v_i)}<br>\left(\overline{c_{i,j}}+rank_u(v_j)\right)<br>$$</p><p>以及：</p><p>$$<br>EFT(v_i,p_k)&#x3D;EST(v_i,p_k)+w_{i,k}<br>$$</p><p>理解 HEFT 后，再看异构计算、边缘计算、工作流调度、云任务编排、CPU-GPU 协同调度等问题，会更容易抓住本质：调度不是单纯地把任务放到最快设备上，而是在依赖、通信和资源竞争之间做平衡。</p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p>本文主要参考了以下资料：</p><ol><li>Haluk Topcuoglu, Salim Hariri, Min-You Wu, “Performance-effective and low-complexity task scheduling for heterogeneous computing”, <em>IEEE Transactions on Parallel and Distributed Systems</em>, 2002.</li><li>Heterogeneous earliest finish time, Wikipedia: <a href="https://en.wikipedia.org/wiki/Heterogeneous_earliest_finish_time">https://en.wikipedia.org/wiki/Heterogeneous_earliest_finish_time</a></li><li>Hamid Arabnejad, Jorge G. Barbosa, “List Scheduling Algorithm for Heterogeneous Systems by an Optimistic Cost Table”, <em>IEEE Transactions on Parallel and Distributed Systems</em>, 2014.</li><li>Luiz F. Bittencourt, Edmundo R. M. Madeira, “HCOC: a cost optimization algorithm for workflow scheduling in hybrid clouds”, <em>Journal of Internet Services and Applications</em>, 2011.</li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/06/14/heft-algorithm/</id>
    <link href="https://lsworl.github.io/2026/06/14/heft-algorithm/"/>
    <published>2026-06-13T16:00:00.000Z</published>
    <summary>从任务DAG、异构处理器、向上排序值、最早完成时间和Python实现几个角度理解HEFT异构最早完成时间调度算法。</summary>
    <title>HEFT算法：异构计算中的任务调度方法</title>
    <updated>2026-06-14T16:07:58.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="机器学习" scheme="https://lsworl.github.io/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="机器学习" scheme="https://lsworl.github.io/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="强化学习" scheme="https://lsworl.github.io/tags/%E5%BC%BA%E5%8C%96%E5%AD%A6%E4%B9%A0/"/>
    <category term="马尔可夫决策过程" scheme="https://lsworl.github.io/tags/%E9%A9%AC%E5%B0%94%E5%8F%AF%E5%A4%AB%E5%86%B3%E7%AD%96%E8%BF%87%E7%A8%8B/"/>
    <category term="MDP" scheme="https://lsworl.github.io/tags/MDP/"/>
    <content>
      <![CDATA[<p>强化学习（Reinforcement Learning，RL）研究的是：一个智能体如何在环境中不断尝试，通过奖励信号学会更好的决策。</p><p>和监督学习不同，强化学习通常没有现成的标准答案。智能体每一步都要自己选择动作，然后环境会返回新的状态和奖励。</p><p>例如：</p><ul><li>游戏角色向左走、向右走、攻击或防御。</li><li>机器人选择前进、转弯、抓取物体。</li><li>推荐系统选择给用户展示哪一个内容。</li><li>自动驾驶系统根据路况选择加速、刹车或转向。</li></ul><p>这些问题都有一个共同点：当前的选择不只影响当前结果，还会影响以后能走到哪里、以后能拿到多少奖励。</p><p>马尔可夫决策过程（Markov Decision Process，MDP）就是强化学习中描述这种“连续决策问题”的基础数学框架。</p><p>一句话概括：</p><blockquote><p>MDP 描述的是：智能体在某个状态下选择动作，环境按照一定概率转移到下一个状态，并给出奖励；智能体的目标是在长期上获得尽可能多的累计奖励。</p></blockquote><span id="more"></span><p><img src="https://lsworl.github.io/images/ml-blog/mdp-agent-environment-loop.svg" alt="强化学习中智能体和环境的交互闭环"></p><h2 id="1-为什么需要-MDP"><a href="#1-为什么需要-MDP" class="headerlink" title="1. 为什么需要 MDP"></a>1. 为什么需要 MDP</h2><p>前面学习线性回归、逻辑回归、决策树时，我们面对的问题通常是这样的：</p><pre class="language-text" data-language="text"><code class="language-text">输入一个样本 -> 模型给出一个预测</code></pre><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">房屋面积 -> 房价学习时长 -> 是否通过考试花瓣长度、花瓣宽度 -> 鸢尾花类别</code></pre><p>这类问题通常可以把每个样本看成相对独立的。</p><p>但强化学习的问题更像这样：</p><pre class="language-text" data-language="text"><code class="language-text">当前状态 -> 选择动作 -> 得到奖励 -> 进入新状态 -> 再选择动作 -> ...</code></pre><p>每一步不是孤立的。比如下棋时，当前一步可能没有立刻得分，但它可能让后面更容易获胜；机器人为了避开障碍物，当前可能要绕远一点，但长期看能更安全地到达目标。</p><p>所以强化学习关心的不是“这一步奖励最大”这么简单，而是：</p><pre class="language-text" data-language="text"><code class="language-text">从现在开始，一直决策下去，长期总奖励能不能最大</code></pre><p>MDP 正是用来把这件事数学化的工具。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-supervised-vs-rl.svg" alt="监督学习和强化学习关注点的区别"></p><h2 id="2-一个直观例子：走格子"><a href="#2-一个直观例子：走格子" class="headerlink" title="2. 一个直观例子：走格子"></a>2. 一个直观例子：走格子</h2><p>假设有一个很小的网格世界：</p><pre class="language-text" data-language="text"><code class="language-text">S  .  G.  X  .</code></pre><p>其中：</p><ul><li><code>S</code> 表示起点。</li><li><code>G</code> 表示目标位置。</li><li><code>X</code> 表示障碍物。</li><li>智能体每一步可以选择上、下、左、右。</li><li>到达 <code>G</code> 得到奖励 $+10$。</li><li>每走一步消耗 $-1$。</li><li>撞墙或撞障碍物会留在原地，并得到 $-1$。</li></ul><p>如果只看当前一步，智能体可能随便走，因为每一步大多都是 $-1$。但如果看长期回报，它应该尽快找到通往 <code>G</code> 的路线。</p><p>这个例子里：</p><ul><li>当前所在格子就是状态。</li><li>上下左右就是动作。</li><li>移动之后的位置就是下一个状态。</li><li>每一步得到的分数就是奖励。</li><li>“如何从每个格子选择动作”就是策略。</li></ul><p>这就是一个非常简单的 MDP。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-grid-world.svg" alt="走格子任务中的状态、动作、奖励和策略"></p><h2 id="3-MDP-的五元组"><a href="#3-MDP-的五元组" class="headerlink" title="3. MDP 的五元组"></a>3. MDP 的五元组</h2><p>一个标准的 MDP 通常写成五元组：</p><p>$$<br>(\mathcal{S}, \mathcal{A}, P, R, \gamma)<br>$$</p><p>分别表示：</p><ul><li>$\mathcal{S}$：状态集合（State Space）。</li><li>$\mathcal{A}$：动作集合（Action Space）。</li><li>$P$：状态转移概率（Transition Probability）。</li><li>$R$：奖励函数（Reward Function）。</li><li>$\gamma$：折扣因子（Discount Factor）。</li></ul><p>下面逐个理解。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-tuple-components.svg" alt="MDP 五元组的组成"></p><h2 id="4-状态：智能体看到的局面"><a href="#4-状态：智能体看到的局面" class="headerlink" title="4. 状态：智能体看到的局面"></a>4. 状态：智能体看到的局面</h2><p>状态用 $s$ 表示，所有可能状态组成集合 $\mathcal{S}$。</p><p>在不同任务中，状态的含义不同：</p><table><thead><tr><th>任务</th><th>状态例子</th></tr></thead><tbody><tr><td>走格子</td><td>智能体当前所在坐标</td></tr><tr><td>下棋</td><td>当前棋盘局面</td></tr><tr><td>机器人控制</td><td>位置、速度、传感器读数</td></tr><tr><td>推荐系统</td><td>用户画像、历史点击、当前页面</td></tr></tbody></table><p>状态要尽可能包含做决策需要的信息。比如在走格子中，只知道“当前奖励”是不够的，必须知道当前在什么位置，才能判断下一步往哪里走。</p><h2 id="5-动作：智能体可以做的选择"><a href="#5-动作：智能体可以做的选择" class="headerlink" title="5. 动作：智能体可以做的选择"></a>5. 动作：智能体可以做的选择</h2><p>动作用 $a$ 表示，所有可能动作组成集合 $\mathcal{A}$。</p><p>例如在走格子任务中：</p><p>$$<br>\mathcal{A} &#x3D; {\text{上}, \text{下}, \text{左}, \text{右}}<br>$$</p><p>在实际问题中，动作可以是离散的，也可以是连续的。</p><ul><li>离散动作：向左、向右、跳跃、攻击。</li><li>连续动作：机器人关节转动角度、车辆油门大小、机械臂施加的力。</li></ul><p>这篇文章先讨论离散状态、离散动作的情况，因为它最适合用来理解 MDP 的核心思想。</p><h2 id="6-状态转移概率：动作不一定产生确定结果"><a href="#6-状态转移概率：动作不一定产生确定结果" class="headerlink" title="6. 状态转移概率：动作不一定产生确定结果"></a>6. 状态转移概率：动作不一定产生确定结果</h2><p>状态转移概率描述的是：在状态 $s$ 下执行动作 $a$ 后，转移到下一个状态 $s’$ 的概率。</p><p>通常写成：</p><p>$$<br>P(s’|s,a)&#x3D;\Pr(S_{t+1}&#x3D;s’|S_t&#x3D;s,A_t&#x3D;a)<br>$$</p><p>例如在走格子中，如果环境是确定的，那么在某个位置选择“向右”，下一个状态就一定是右边那个格子。</p><p>但很多现实问题不是确定的。比如机器人选择向前走，可能因为地面打滑而偏了一点；推荐系统展示一个内容，用户可能点击，也可能不点击。</p><p>此时可以写成：</p><p>$$<br>P(s_1|s,a)&#x3D;0.8,\quad P(s_2|s,a)&#x3D;0.2<br>$$</p><p>意思是：在状态 $s$ 执行动作 $a$ 后，有 $80%$ 的概率到达 $s_1$，有 $20%$ 的概率到达 $s_2$。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-transition-probability.svg" alt="同一个动作可能对应多个后继状态"></p><h2 id="7-奖励函数：什么结果是好的"><a href="#7-奖励函数：什么结果是好的" class="headerlink" title="7. 奖励函数：什么结果是好的"></a>7. 奖励函数：什么结果是好的</h2><p>奖励函数描述的是智能体执行动作后能得到多少反馈。</p><p>一种常见写法是：</p><p>$$<br>R(s,a)&#x3D;\mathbb{E}[R_{t+1}|S_t&#x3D;s,A_t&#x3D;a]<br>$$</p><p>也可以把奖励写成和下一个状态有关：</p><p>$$<br>R(s,a,s’)&#x3D;\mathbb{E}[R_{t+1}|S_t&#x3D;s,A_t&#x3D;a,S_{t+1}&#x3D;s’]<br>$$</p><p>奖励是强化学习中的目标信号。</p><p>例如：</p><ul><li>到达终点：$+10$</li><li>每走一步：$-1$</li><li>撞到障碍物：$-5$</li><li>游戏获胜：$+1$</li><li>游戏失败：$-1$</li></ul><p>奖励设计非常重要。如果奖励设计不合理，智能体可能学到一些看起来“钻空子”的行为。</p><p>比如只奖励机器人“移动速度快”，却不惩罚撞墙，那么机器人可能会学会高速乱撞；只奖励推荐系统“点击率高”，却不考虑用户体验，系统可能会倾向于推荐标题夸张但质量低的内容。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-reward-design.svg" alt="奖励设计需要和真实目标对齐"></p><h2 id="8-折扣因子：未来奖励要不要打折"><a href="#8-折扣因子：未来奖励要不要打折" class="headerlink" title="8. 折扣因子：未来奖励要不要打折"></a>8. 折扣因子：未来奖励要不要打折</h2><p>强化学习关心长期回报，但未来太远的奖励通常要打一点折扣。</p><p>折扣因子用 $\gamma$ 表示：</p><p>$$<br>0 \le \gamma \le 1<br>$$</p><p>如果从时刻 $t$ 开始，之后的奖励依次为：</p><p>$$<br>R_{t+1}, R_{t+2}, R_{t+3}, \dots<br>$$</p><p>那么累计回报定义为：</p><p>$$<br>G_t &#x3D; R_{t+1} + \gamma R_{t+2} + \gamma^2 R_{t+3} + \cdots<br>$$</p><p>其中：</p><ul><li>$\gamma&#x3D;0$ 时，只关心下一步奖励。</li><li>$\gamma$ 越接近 $1$，越重视长期奖励。</li><li>$\gamma&lt;1$ 可以让无限步任务中的累计回报更容易收敛。</li></ul><p>举个例子，假设未来三步奖励分别是：</p><p>$$<br>R_{t+1}&#x3D;1,\quad R_{t+2}&#x3D;2,\quad R_{t+3}&#x3D;10<br>$$</p><p>如果 $\gamma&#x3D;0.9$，那么回报为：</p><p>$$<br>G_t &#x3D; 1 + 0.9 \times 2 + 0.9^2 \times 10 &#x3D; 10.9<br>$$</p><p>如果 $\gamma&#x3D;0.5$，那么回报为：</p><p>$$<br>G_t &#x3D; 1 + 0.5 \times 2 + 0.5^2 \times 10 &#x3D; 4.5<br>$$</p><p>可以看到，$\gamma$ 越小，越不在意远处的奖励。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-discount-factor.svg" alt="折扣因子会降低远期奖励的权重"></p><h2 id="9-马尔可夫性：未来只依赖现在"><a href="#9-马尔可夫性：未来只依赖现在" class="headerlink" title="9. 马尔可夫性：未来只依赖现在"></a>9. 马尔可夫性：未来只依赖现在</h2><p>MDP 中的“马尔可夫”来自马尔可夫性。</p><p>马尔可夫性可以理解为：</p><blockquote><p>只要当前状态已经包含足够信息，那么未来如何变化只和当前状态、当前动作有关，不需要再追溯更早的历史。</p></blockquote><p>用公式表示：</p><p>$$<br>\Pr(S_{t+1}&#x3D;s’|S_t&#x3D;s,A_t&#x3D;a,S_{t-1},A_{t-1},\dots)<br>&#x3D;<br>\Pr(S_{t+1}&#x3D;s’|S_t&#x3D;s,A_t&#x3D;a)<br>$$</p><p>也就是说，给定当前状态 $s$ 和当前动作 $a$ 后，过去发生过什么不再额外影响下一个状态。</p><p>这并不是说“历史不重要”，而是说：如果历史重要，就应该把历史中有用的信息放进当前状态里。</p><p>例如在股票交易中，如果只把“当前价格”作为状态，可能不满足马尔可夫性，因为过去的价格趋势也会影响决策。此时可以把过去一段时间的价格、成交量、技术指标都加入状态，让状态包含更多信息。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-markov-property.svg" alt="马尔可夫性要求当前状态总结有用历史"></p><h2 id="10-策略：在每个状态下如何选择动作"><a href="#10-策略：在每个状态下如何选择动作" class="headerlink" title="10. 策略：在每个状态下如何选择动作"></a>10. 策略：在每个状态下如何选择动作</h2><p>策略（Policy）描述的是智能体的行为方式，通常用 $\pi$ 表示。</p><p>确定性策略写成：</p><p>$$<br>a&#x3D;\pi(s)<br>$$</p><p>表示在状态 $s$ 下直接选择动作 $a$。</p><p>随机策略写成：</p><p>$$<br>\pi(a|s)&#x3D;\Pr(A_t&#x3D;a|S_t&#x3D;s)<br>$$</p><p>表示在状态 $s$ 下选择动作 $a$ 的概率。</p><p>例如在某个格子中，策略可能是：</p><table><thead><tr><th>动作</th><th align="right">概率</th></tr></thead><tbody><tr><td>上</td><td align="right">0.1</td></tr><tr><td>下</td><td align="right">0.1</td></tr><tr><td>左</td><td align="right">0.2</td></tr><tr><td>右</td><td align="right">0.6</td></tr></tbody></table><p>这表示智能体更倾向于向右走，但仍然保留一定探索其他动作的概率。</p><p>强化学习要学习的，通常就是一个好的策略。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-policy-types.svg" alt="确定性策略和随机策略的区别"></p><h2 id="11-状态价值函数"><a href="#11-状态价值函数" class="headerlink" title="11. 状态价值函数"></a>11. 状态价值函数</h2><p>状态价值函数用 $V^\pi(s)$ 表示，意思是：</p><blockquote><p>如果当前处于状态 $s$，之后一直按照策略 $\pi$ 行动，期望能获得多少累计回报。</p></blockquote><p>公式为：</p><p>$$<br>V^\pi(s)&#x3D;\mathbb{E}_\pi[G_t|S_t&#x3D;s]<br>$$</p><p>把回报展开：</p><p>$$<br>V^\pi(s)&#x3D;\mathbb{E}<em>\pi[R</em>{t+1}+\gamma R_{t+2}+\gamma^2R_{t+3}+\cdots|S_t&#x3D;s]<br>$$</p><p>价值函数可以理解为“这个状态有多好”。</p><p>在走格子任务中，离终点越近、越容易到达终点的格子，价值通常越高；离障碍物近、容易被困住的位置，价值通常较低。</p><h2 id="12-动作价值函数"><a href="#12-动作价值函数" class="headerlink" title="12. 动作价值函数"></a>12. 动作价值函数</h2><p>动作价值函数用 $Q^\pi(s,a)$ 表示，意思是：</p><blockquote><p>如果当前处于状态 $s$，先执行动作 $a$，之后再按照策略 $\pi$ 行动，期望能获得多少累计回报。</p></blockquote><p>公式为：</p><p>$$<br>Q^\pi(s,a)&#x3D;\mathbb{E}_\pi[G_t|S_t&#x3D;s,A_t&#x3D;a]<br>$$</p><p>状态价值函数关注“状态好不好”，动作价值函数关注“在这个状态下做某个动作好不好”。</p><p>二者的关系是：</p><p>$$<br>V^\pi(s)&#x3D;\sum_a \pi(a|s)Q^\pi(s,a)<br>$$</p><p>如果策略是确定性的，在状态 $s$ 下只选择动作 $\pi(s)$，那么：</p><p>$$<br>V^\pi(s)&#x3D;Q^\pi(s,\pi(s))<br>$$</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-value-vs-q.svg" alt="状态价值函数和动作价值函数的区别"></p><h2 id="13-贝尔曼方程：把长期价值拆成一步"><a href="#13-贝尔曼方程：把长期价值拆成一步" class="headerlink" title="13. 贝尔曼方程：把长期价值拆成一步"></a>13. 贝尔曼方程：把长期价值拆成一步</h2><p>强化学习中最核心的公式之一就是贝尔曼方程。</p><p>它的思想非常朴素：</p><pre class="language-text" data-language="text"><code class="language-text">当前状态的价值 = 下一步奖励 + 下一个状态的折扣价值</code></pre><p>对于某个策略 $\pi$，状态价值函数满足：</p><p>$$<br>V^\pi(s)&#x3D;\sum_a \pi(a|s)\sum_{s’}P(s’|s,a)<br>\left[R(s,a,s’)+\gamma V^\pi(s’)\right]<br>$$</p><p>这个公式看起来长，但每一部分都能对应到 MDP 的元素：</p><ul><li>$\pi(a|s)$：在状态 $s$ 下选择动作 $a$ 的概率。</li><li>$P(s’|s,a)$：执行动作 $a$ 后到达 $s’$ 的概率。</li><li>$R(s,a,s’)$：这一步得到的奖励。</li><li>$\gamma V^\pi(s’)$：下一个状态的未来价值。</li></ul><p>贝尔曼方程的关键在于：它把“无限长的未来”拆成了“眼前一步 + 剩下的未来”。</p><p>这和动态规划的思想很像：如果后面的子问题已经知道答案，那么当前问题就可以通过一步递推算出来。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-bellman-breakdown.svg" alt="贝尔曼方程把长期价值拆成当前奖励和未来价值"></p><h2 id="14-最优价值函数和最优策略"><a href="#14-最优价值函数和最优策略" class="headerlink" title="14. 最优价值函数和最优策略"></a>14. 最优价值函数和最优策略</h2><p>如果我们不想评价某一个固定策略，而是想找到最好的策略，就会用到最优价值函数。</p><p>最优状态价值函数定义为：</p><p>$$<br>V^*(s)&#x3D;\max_\pi V^\pi(s)<br>$$</p><p>意思是：在所有可能策略中，从状态 $s$ 出发能得到的最大期望回报。</p><p>最优动作价值函数定义为：</p><p>$$<br>Q^*(s,a)&#x3D;\max_\pi Q^\pi(s,a)<br>$$</p><p>对应的最优贝尔曼方程为：</p><p>$$<br>V^*(s)&#x3D;\max_a \sum_{s’}P(s’|s,a)<br>\left[R(s,a,s’)+\gamma V^*(s’)\right]<br>$$</p><p>最优策略可以从最优价值函数中取出来：</p><p>$$<br>\pi^*(s)&#x3D;\arg\max_a \sum_{s’}P(s’|s,a)<br>\left[R(s,a,s’)+\gamma V^*(s’)\right]<br>$$</p><p>直观地说，就是在每个状态下选择能让“当前奖励 + 未来价值”最大的动作。</p><h2 id="15-手工计算一个小例子"><a href="#15-手工计算一个小例子" class="headerlink" title="15. 手工计算一个小例子"></a>15. 手工计算一个小例子</h2><p>假设现在只有两个普通状态 $A$、$B$，以及一个终止状态 $T$。</p><p>动作规则如下：</p><table><thead><tr><th>当前状态</th><th>动作</th><th>下一个状态</th><th align="right">奖励</th></tr></thead><tbody><tr><td>A</td><td>去 B</td><td>B</td><td align="right">0</td></tr><tr><td>A</td><td>结束</td><td>T</td><td align="right">1</td></tr><tr><td>B</td><td>去 A</td><td>A</td><td align="right">0</td></tr><tr><td>B</td><td>结束</td><td>T</td><td align="right">3</td></tr></tbody></table><p>设折扣因子：</p><p>$$<br>\gamma&#x3D;0.9<br>$$</p><p>终止状态价值为：</p><p>$$<br>V^*(T)&#x3D;0<br>$$</p><p>对于状态 $A$，有两个选择：</p><ol><li>直接结束，得到奖励 $1$。</li><li>去 $B$，得到奖励 $0$，然后获得 $B$ 的未来价值。</li></ol><p>所以：</p><p>$$<br>V^*(A)&#x3D;\max(1,;0+0.9V^*(B))<br>$$</p><p>对于状态 $B$：</p><p>$$<br>V^*(B)&#x3D;\max(3,;0+0.9V^*(A))<br>$$</p><p>可以先猜一下：在 $B$ 直接结束可以得到 $3$，看起来很划算。</p><p>如果 $V^*(B)&#x3D;3$，那么：</p><p>$$<br>V^*(A)&#x3D;\max(1,;0.9\times 3)&#x3D;2.7<br>$$</p><p>再代回 $B$：</p><p>$$<br>V^*(B)&#x3D;\max(3,;0.9\times 2.7)&#x3D;\max(3,;2.43)&#x3D;3<br>$$</p><p>所以结果稳定：</p><p>$$<br>V^*(A)&#x3D;2.7,\quad V^*(B)&#x3D;3<br>$$</p><p>最优策略为：</p><ul><li>在 $A$ 选择“去 B”。</li><li>在 $B$ 选择“结束”。</li></ul><p>这个例子说明：状态 $A$ 直接结束只能得到 $1$，但如果先去 $B$，再结束，折扣后还能得到 $2.7$，所以更优。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-small-example-abt.svg" alt="A、B、T 小型 MDP 的状态转移关系"></p><h2 id="16-价值迭代：不断逼近最优价值"><a href="#16-价值迭代：不断逼近最优价值" class="headerlink" title="16. 价值迭代：不断逼近最优价值"></a>16. 价值迭代：不断逼近最优价值</h2><p>如果状态和动作数量不大，并且已知转移概率和奖励函数，就可以使用价值迭代（Value Iteration）求解 MDP。</p><p>价值迭代的更新公式来自最优贝尔曼方程：</p><p>$$<br>V_{k+1}(s)&#x3D;\max_a \sum_{s’}P(s’|s,a)<br>\left[R(s,a,s’)+\gamma V_k(s’)\right]<br>$$</p><p>它的思路是：</p><ol><li>先给每个状态的价值一个初始值，比如全是 $0$。</li><li>用贝尔曼最优方程更新每个状态。</li><li>重复很多轮，直到价值变化很小。</li><li>根据最终价值函数选出每个状态下最好的动作。</li></ol><p>可以把它理解为：</p><pre class="language-text" data-language="text"><code class="language-text">先粗略估计每个状态的价值然后不断让每个状态参考后继状态的价值来修正自己</code></pre><p><img src="https://lsworl.github.io/images/ml-blog/mdp-value-iteration-flow.svg" alt="价值迭代的基本流程"></p><h2 id="17-Python-实现价值迭代"><a href="#17-Python-实现价值迭代" class="headerlink" title="17. Python 实现价值迭代"></a>17. Python 实现价值迭代</h2><p>下面用 Python 实现上面 $A$、$B$、$T$ 的小 MDP。</p><pre class="language-python" data-language="python"><code class="language-python">states <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token string">"A"</span><span class="token punctuation">,</span> <span class="token string">"B"</span><span class="token punctuation">,</span> <span class="token string">"T"</span><span class="token punctuation">]</span>actions <span class="token operator">=</span> <span class="token punctuation">&#123;</span>    <span class="token string">"A"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"go_B"</span><span class="token punctuation">,</span> <span class="token string">"end"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"B"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"go_A"</span><span class="token punctuation">,</span> <span class="token string">"end"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token string">"T"</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span><span class="token comment"># transitions[(state, action)] = [(probability, next_state, reward)]</span>transitions <span class="token operator">=</span> <span class="token punctuation">&#123;</span>    <span class="token punctuation">(</span><span class="token string">"A"</span><span class="token punctuation">,</span> <span class="token string">"go_B"</span><span class="token punctuation">)</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">(</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token string">"B"</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">(</span><span class="token string">"A"</span><span class="token punctuation">,</span> <span class="token string">"end"</span><span class="token punctuation">)</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">(</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token string">"T"</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">(</span><span class="token string">"B"</span><span class="token punctuation">,</span> <span class="token string">"go_A"</span><span class="token punctuation">)</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">(</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token string">"A"</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token punctuation">(</span><span class="token string">"B"</span><span class="token punctuation">,</span> <span class="token string">"end"</span><span class="token punctuation">)</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">(</span><span class="token number">1.0</span><span class="token punctuation">,</span> <span class="token string">"T"</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span>gamma <span class="token operator">=</span> <span class="token number">0.9</span>theta <span class="token operator">=</span> <span class="token number">1e-6</span><span class="token comment"># 对应模块：初始化价值函数，终止状态和普通状态一开始都设为 0</span>V <span class="token operator">=</span> <span class="token punctuation">&#123;</span>s<span class="token punctuation">:</span> <span class="token number">0.0</span> <span class="token keyword">for</span> s <span class="token keyword">in</span> states<span class="token punctuation">&#125;</span><span class="token keyword">while</span> <span class="token boolean">True</span><span class="token punctuation">:</span>    delta <span class="token operator">=</span> <span class="token number">0.0</span>    <span class="token keyword">for</span> s <span class="token keyword">in</span> states<span class="token punctuation">:</span>        <span class="token comment"># 对应模块：终止状态没有动作，价值保持为 0</span>        <span class="token keyword">if</span> <span class="token keyword">not</span> actions<span class="token punctuation">[</span>s<span class="token punctuation">]</span><span class="token punctuation">:</span>            <span class="token keyword">continue</span>        old_value <span class="token operator">=</span> V<span class="token punctuation">[</span>s<span class="token punctuation">]</span>        action_values <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>        <span class="token keyword">for</span> a <span class="token keyword">in</span> actions<span class="token punctuation">[</span>s<span class="token punctuation">]</span><span class="token punctuation">:</span>            value <span class="token operator">=</span> <span class="token number">0.0</span>            <span class="token comment"># 对应模块：计算某个动作的期望价值</span>            <span class="token keyword">for</span> prob<span class="token punctuation">,</span> next_state<span class="token punctuation">,</span> reward <span class="token keyword">in</span> transitions<span class="token punctuation">[</span><span class="token punctuation">(</span>s<span class="token punctuation">,</span> a<span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">:</span>                value <span class="token operator">+=</span> prob <span class="token operator">*</span> <span class="token punctuation">(</span>reward <span class="token operator">+</span> gamma <span class="token operator">*</span> V<span class="token punctuation">[</span>next_state<span class="token punctuation">]</span><span class="token punctuation">)</span>            action_values<span class="token punctuation">.</span>append<span class="token punctuation">(</span>value<span class="token punctuation">)</span>        <span class="token comment"># 对应模块：贝尔曼最优更新，选择价值最大的动作</span>        V<span class="token punctuation">[</span>s<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token builtin">max</span><span class="token punctuation">(</span>action_values<span class="token punctuation">)</span>        delta <span class="token operator">=</span> <span class="token builtin">max</span><span class="token punctuation">(</span>delta<span class="token punctuation">,</span> <span class="token builtin">abs</span><span class="token punctuation">(</span>old_value <span class="token operator">-</span> V<span class="token punctuation">[</span>s<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> delta <span class="token operator">&lt;</span> theta<span class="token punctuation">:</span>        <span class="token keyword">break</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"最优价值函数："</span><span class="token punctuation">)</span><span class="token keyword">for</span> s <span class="token keyword">in</span> states<span class="token punctuation">:</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>s<span class="token punctuation">,</span> <span class="token builtin">round</span><span class="token punctuation">(</span>V<span class="token punctuation">[</span>s<span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"\n最优策略："</span><span class="token punctuation">)</span><span class="token keyword">for</span> s <span class="token keyword">in</span> states<span class="token punctuation">:</span>    <span class="token keyword">if</span> <span class="token keyword">not</span> actions<span class="token punctuation">[</span>s<span class="token punctuation">]</span><span class="token punctuation">:</span>        <span class="token keyword">continue</span>    best_action <span class="token operator">=</span> <span class="token boolean">None</span>    best_value <span class="token operator">=</span> <span class="token builtin">float</span><span class="token punctuation">(</span><span class="token string">"-inf"</span><span class="token punctuation">)</span>    <span class="token keyword">for</span> a <span class="token keyword">in</span> actions<span class="token punctuation">[</span>s<span class="token punctuation">]</span><span class="token punctuation">:</span>        value <span class="token operator">=</span> <span class="token number">0.0</span>        <span class="token keyword">for</span> prob<span class="token punctuation">,</span> next_state<span class="token punctuation">,</span> reward <span class="token keyword">in</span> transitions<span class="token punctuation">[</span><span class="token punctuation">(</span>s<span class="token punctuation">,</span> a<span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">:</span>            value <span class="token operator">+=</span> prob <span class="token operator">*</span> <span class="token punctuation">(</span>reward <span class="token operator">+</span> gamma <span class="token operator">*</span> V<span class="token punctuation">[</span>next_state<span class="token punctuation">]</span><span class="token punctuation">)</span>        <span class="token keyword">if</span> value <span class="token operator">></span> best_value<span class="token punctuation">:</span>            best_value <span class="token operator">=</span> value            best_action <span class="token operator">=</span> a    <span class="token keyword">print</span><span class="token punctuation">(</span>s<span class="token punctuation">,</span> <span class="token string">"->"</span><span class="token punctuation">,</span> best_action<span class="token punctuation">)</span></code></pre><p>输出结果大致为：</p><pre class="language-text" data-language="text"><code class="language-text">最优价值函数：A 2.7B 3.0T 0.0最优策略：A -> go_BB -> end</code></pre><p>这和前面的手工计算一致。</p><h2 id="18-MDP-和强化学习算法的关系"><a href="#18-MDP-和强化学习算法的关系" class="headerlink" title="18. MDP 和强化学习算法的关系"></a>18. MDP 和强化学习算法的关系</h2><p>MDP 是问题建模框架，而强化学习算法是在这个框架上学习策略或价值函数的方法。</p><p>常见算法可以这样理解：</p><table><thead><tr><th>算法</th><th>主要学习什么</th><th>依赖模型吗</th></tr></thead><tbody><tr><td>动态规划</td><td>$V(s)$ 或 $Q(s,a)$</td><td>需要已知 $P$ 和 $R$</td></tr><tr><td>蒙特卡洛方法</td><td>通过完整回合估计价值</td><td>不需要已知模型</td></tr><tr><td>TD 学习</td><td>用一步奖励和下一个状态估计价值</td><td>不需要已知模型</td></tr><tr><td>Q-Learning</td><td>学习最优动作价值 $Q^*(s,a)$</td><td>不需要已知模型</td></tr><tr><td>SARSA</td><td>学习当前策略下的动作价值</td><td>不需要已知模型</td></tr><tr><td>Policy Gradient</td><td>直接优化策略参数</td><td>不需要已知模型</td></tr></tbody></table><p>如果环境的转移概率 $P$ 和奖励函数 $R$ 已知，就可以用动态规划类方法直接求解。</p><p>但很多真实问题中，智能体并不知道 $P$ 和 $R$ 的完整形式，只能通过和环境交互得到样本：</p><pre class="language-text" data-language="text"><code class="language-text">(s, a, r, s')</code></pre><p>这时就需要 Q-Learning、SARSA、Policy Gradient 等强化学习算法。</p><p><img src="https://lsworl.github.io/images/ml-blog/mdp-rl-algorithm-map.svg" alt="常见强化学习算法都建立在 MDP 建模框架之上"></p><h2 id="19-MDP-中几个容易混淆的概念"><a href="#19-MDP-中几个容易混淆的概念" class="headerlink" title="19. MDP 中几个容易混淆的概念"></a>19. MDP 中几个容易混淆的概念</h2><h3 id="19-1-奖励和价值"><a href="#19-1-奖励和价值" class="headerlink" title="19.1 奖励和价值"></a>19.1 奖励和价值</h3><p>奖励是一步反馈，价值是长期回报的期望。</p><p>例如在走格子中，某一步向右走可能只得到 $-1$，但如果这一步能让智能体更接近终点，那么当前状态或当前动作的价值仍然可能很高。</p><p>所以：</p><pre class="language-text" data-language="text"><code class="language-text">奖励看一步，价值看长期</code></pre><h3 id="19-2-状态和观测"><a href="#19-2-状态和观测" class="headerlink" title="19.2 状态和观测"></a>19.2 状态和观测</h3><p>理论上的 MDP 假设智能体能看到完整状态。</p><p>但现实中智能体经常只能看到一部分信息，这叫观测（Observation）。例如自动驾驶汽车不一定知道世界的完整真实状态，只能通过摄像头、雷达等传感器获得观测。</p><p>如果观测不能完整表示状态，问题就更接近部分可观测马尔可夫决策过程（Partially Observable Markov Decision Process，POMDP）。</p><h3 id="19-3-策略和价值函数"><a href="#19-3-策略和价值函数" class="headerlink" title="19.3 策略和价值函数"></a>19.3 策略和价值函数</h3><p>策略告诉智能体“怎么做”，价值函数告诉智能体“这样做长期好不好”。</p><p>很多算法会先学习价值函数，再从价值函数中导出策略；也有算法直接学习策略。</p><h2 id="20-实践建议"><a href="#20-实践建议" class="headerlink" title="20. 实践建议"></a>20. 实践建议</h2><p>用 MDP 思考强化学习问题时，可以按下面顺序拆解：</p><ol><li>明确状态是什么，状态是否包含足够决策信息。</li><li>明确动作有哪些，是离散动作还是连续动作。</li><li>明确奖励怎么设计，避免奖励和真实目标不一致。</li><li>明确任务是否有终止状态，例如游戏结束、到达目标、失败退出。</li><li>选择合适的折扣因子 $\gamma$，短期任务可以小一点，长期任务通常接近 $1$。</li><li>如果已知环境模型，可以考虑动态规划；如果不知道模型，就通过交互数据学习。</li><li>先从小环境验证算法，再扩展到复杂环境。</li></ol><h2 id="21-总结"><a href="#21-总结" class="headerlink" title="21. 总结"></a>21. 总结</h2><p>MDP 是强化学习的地基。</p><p>它用状态、动作、转移概率、奖励函数和折扣因子，把连续决策问题描述成一个清晰的数学模型：</p><p>$$<br>(\mathcal{S}, \mathcal{A}, P, R, \gamma)<br>$$</p><p>其中，马尔可夫性要求当前状态已经包含足够信息；策略决定智能体如何行动；价值函数衡量长期回报；贝尔曼方程则把长期价值拆成“当前一步 + 未来价值”。</p><p>理解 MDP 之后，再看 Q-Learning、SARSA、DQN、Policy Gradient 等算法会顺很多。因为这些算法本质上都在回答同一个问题：</p><pre class="language-text" data-language="text"><code class="language-text">在这个 MDP 中，怎样选择动作，才能让长期累计奖励最大？</code></pre><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p>本文主要参考了以下资料：</p><ol><li>Richard S. Sutton, Andrew G. Barto, <em>Reinforcement Learning: An Introduction</em>, 2nd Edition, MIT Press, 2018. 在线版：<a href="https://incompleteideas.net/book/the-book-2nd.html">https://incompleteideas.net/book/the-book-2nd.html</a></li><li>Martin L. Puterman, <em>Markov Decision Processes: Discrete Stochastic Dynamic Programming</em>, Wiley, 1994.</li><li>Richard Bellman, “A Markovian Decision Process”, <em>Journal of Mathematics and Mechanics</em>, 1957.</li><li>David Silver, <em>Lecture 2: Markov Decision Processes</em>, UCL Course on Reinforcement Learning, 2015. 课程资料：<a href="http://www0.cs.ucl.ac.uk/staff/d.silver/web/Teaching.html">http://www0.cs.ucl.ac.uk/staff/d.silver/web/Teaching.html</a></li></ol>]]>
    </content>
    <id>https://lsworl.github.io/2026/06/13/machine-learning-markov-decision-process/</id>
    <link href="https://lsworl.github.io/2026/06/13/machine-learning-markov-decision-process/"/>
    <published>2026-06-12T16:00:00.000Z</published>
    <summary>从直觉、五元组、回报、价值函数、贝尔曼方程和价值迭代几个角度理解强化学习中的马尔可夫决策过程。</summary>
    <title>强化学习：从状态、动作到马尔可夫决策过程</title>
    <updated>2026-06-13T16:23:12.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="自然语言处理" scheme="https://lsworl.github.io/categories/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="NLP" scheme="https://lsworl.github.io/tags/NLP/"/>
    <category term="自然语言处理" scheme="https://lsworl.github.io/tags/%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E5%A4%84%E7%90%86/"/>
    <category term="Transformer" scheme="https://lsworl.github.io/tags/Transformer/"/>
    <category term="文本处理" scheme="https://lsworl.github.io/tags/%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86/"/>
    <category term="分词" scheme="https://lsworl.github.io/tags/%E5%88%86%E8%AF%8D/"/>
    <content>
      <![CDATA[<p>学习 Transformer 之前，有一个非常重要的前置问题：</p><blockquote><p>机器学习模型只能处理数字，那自然语言文本应该怎样变成数字？</p></blockquote><p>比如一句话：</p><pre class="language-text" data-language="text"><code class="language-text">我喜欢自然语言处理</code></pre><p>人可以直接理解它的意思，但模型不能直接处理汉字、单词和标点。我们需要先把文本拆成一个个 token，再把 token 映射成编号或向量，最后才能送进模型。</p><p>这篇文章就来系统梳理 NLP 中最基础的文本处理流程。</p><span id="more"></span><p><img src="https://lsworl.github.io/images/nlp-blog/text-processing-pipeline.svg" alt="文本处理流程"></p><h2 id="1-文本处理在-NLP-中的位置"><a href="#1-文本处理在-NLP-中的位置" class="headerlink" title="1. 文本处理在 NLP 中的位置"></a>1. 文本处理在 NLP 中的位置</h2><p>在前面的机器学习文章中，我们处理的大多是结构化数据，例如：</p><pre class="language-text" data-language="text"><code class="language-text">学习时长、睡眠时长、房屋面积、历史成绩</code></pre><p>这些特征天然就是数字，或者很容易转成数字。</p><p>但 NLP 处理的是文本，例如：</p><pre class="language-text" data-language="text"><code class="language-text">这部电影很好看这个商品质量一般今天天气不错</code></pre><p>模型不能直接理解这些句子，所以需要一个转换流程：</p><pre class="language-text" data-language="text"><code class="language-text">原始文本  -> 清洗与规范化  -> 分词 / tokenization  -> 构建词表  -> token 转 id  -> id 转向量  -> 输入模型</code></pre><p>传统机器学习中，文本常被转成词袋、TF-IDF 这样的稀疏向量；深度学习和 Transformer 中，文本通常先转成 token id，再通过 embedding 层变成稠密向量。</p><h2 id="2-文本清洗与规范化"><a href="#2-文本清洗与规范化" class="headerlink" title="2. 文本清洗与规范化"></a>2. 文本清洗与规范化</h2><p>文本数据通常比较脏。比如：</p><pre class="language-text" data-language="text"><code class="language-text">“这个手机太好用了！！！  http://xxx.com”“This Movie is GREAT!!!”“我    喜欢    NLP”</code></pre><p>常见清洗操作包括：</p><ul><li>去掉多余空格。</li><li>统一大小写。</li><li>去掉或保留标点符号。</li><li>去掉 HTML 标签。</li><li>去掉 URL、邮箱、手机号等特殊内容。</li><li>中文繁简转换。</li><li>表情、emoji、特殊符号处理。</li></ul><p>是否清洗，要看任务。</p><p>例如情感分析中，感叹号和 emoji 可能很重要：</p><pre class="language-text" data-language="text"><code class="language-text">太好吃了！！！太好吃了🙂太好吃了😡</code></pre><p>这些符号可能影响情绪判断，不能随便删除。</p><p>而在主题分类中，URL、HTML 标签通常没什么帮助，可以先清理。</p><p>所以文本清洗没有固定标准，核心原则是：</p><pre class="language-text" data-language="text"><code class="language-text">保留对任务有用的信息，去掉会干扰模型的噪声。</code></pre><h2 id="3-什么是分词"><a href="#3-什么是分词" class="headerlink" title="3. 什么是分词"></a>3. 什么是分词</h2><p>分词（Tokenization）就是把一段文本切成更小的单位。</p><p>这些单位叫 token。</p><p>英文中，最简单的分词可以按空格切：</p><pre class="language-text" data-language="text"><code class="language-text">I love natural language processing</code></pre><p>切分后：</p><pre class="language-text" data-language="text"><code class="language-text">["I", "love", "natural", "language", "processing"]</code></pre><p>中文更麻烦，因为中文词之间没有天然空格。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">我喜欢自然语言处理</code></pre><p>可以按字切：</p><pre class="language-text" data-language="text"><code class="language-text">["我", "喜", "欢", "自", "然", "语", "言", "处", "理"]</code></pre><p>也可以按词切：</p><pre class="language-text" data-language="text"><code class="language-text">["我", "喜欢", "自然语言处理"]</code></pre><p>切分方式不同，模型看到的信息粒度就不同。</p><h2 id="4-中文分词为什么困难"><a href="#4-中文分词为什么困难" class="headerlink" title="4. 中文分词为什么困难"></a>4. 中文分词为什么困难</h2><p>中文分词难在边界不明显。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">南京市长江大桥</code></pre><p>可能被切成：</p><pre class="language-text" data-language="text"><code class="language-text">["南京市", "长江大桥"]</code></pre><p>也可能被误切成：</p><pre class="language-text" data-language="text"><code class="language-text">["南京", "市长", "江大桥"]</code></pre><p>同一串字，不同切法含义完全不同。</p><p>再比如：</p><pre class="language-text" data-language="text"><code class="language-text">研究生命起源</code></pre><p>可以理解为：</p><pre class="language-text" data-language="text"><code class="language-text">研究 / 生命 / 起源</code></pre><p>也可以在某些语境下理解为：</p><pre class="language-text" data-language="text"><code class="language-text">研究生 / 命 / 起源</code></pre><p>虽然第二种很奇怪，但它说明中文分词需要结合上下文。</p><p>传统中文分词会使用词典、统计模型或机器学习模型；深度学习时代，很多 Transformer 模型使用字粒度或子词粒度，减少对传统中文分词的依赖。</p><h2 id="5-词表：给每个-token-一个编号"><a href="#5-词表：给每个-token-一个编号" class="headerlink" title="5. 词表：给每个 token 一个编号"></a>5. 词表：给每个 token 一个编号</h2><p>分词以后，我们得到的是 token 列表。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">["我", "喜欢", "NLP"]</code></pre><p>接下来需要构建词表（Vocabulary）：</p><table><thead><tr><th>token</th><th align="right">id</th></tr></thead><tbody><tr><td><code>[PAD]</code></td><td align="right">0</td></tr><tr><td><code>[UNK]</code></td><td align="right">1</td></tr><tr><td>我</td><td align="right">2</td></tr><tr><td>喜欢</td><td align="right">3</td></tr><tr><td>NLP</td><td align="right">4</td></tr><tr><td>学习</td><td align="right">5</td></tr></tbody></table><p>于是句子：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 NLP</code></pre><p>可以转换为：</p><pre class="language-text" data-language="text"><code class="language-text">[2, 3, 4]</code></pre><p>这里 <code>[UNK]</code> 表示未知词。如果遇到词表里没有的 token，就用 <code>[UNK]</code> 代替。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 Transformer</code></pre><p>如果词表里没有 <code>Transformer</code>，就会变成：</p><pre class="language-text" data-language="text"><code class="language-text">[2, 3, 1]</code></pre><p>这也是早期词级分词的一个问题：词表不可能包含所有新词、错别字、人名、专业术语。</p><h2 id="6-One-hot-编码"><a href="#6-One-hot-编码" class="headerlink" title="6. One-hot 编码"></a>6. One-hot 编码</h2><p>最简单的 token 表示方式是 One-hot。</p><p>假设词表有 5 个 token：</p><table><thead><tr><th>token</th><th align="right">id</th></tr></thead><tbody><tr><td>我</td><td align="right">0</td></tr><tr><td>喜欢</td><td align="right">1</td></tr><tr><td>NLP</td><td align="right">2</td></tr><tr><td>学习</td><td align="right">3</td></tr><tr><td>文本</td><td align="right">4</td></tr></tbody></table><p>那么 <code>NLP</code> 的 One-hot 向量为：</p><p>$$<br>[0,0,1,0,0]<br>$$</p><p><code>喜欢</code> 的 One-hot 向量为：</p><p>$$<br>[0,1,0,0,0]<br>$$</p><p>One-hot 的特点是：</p><ul><li>向量长度等于词表大小。</li><li>每个词只有一个位置是 1。</li><li>不同词之间看起来完全独立。</li></ul><p>问题是：One-hot 没有语义相似性。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">喜欢热爱讨厌</code></pre><p>从语义上看，“喜欢”和“热爱”更接近，但 One-hot 无法体现这种关系。</p><h2 id="7-词袋模型"><a href="#7-词袋模型" class="headerlink" title="7. 词袋模型"></a>7. 词袋模型</h2><p>词袋模型（Bag of Words）不关心词序，只统计词出现次数。</p><p>假设词表为：</p><pre class="language-text" data-language="text"><code class="language-text">["我", "喜欢", "学习", "NLP"]</code></pre><p>句子：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 学习 NLP</code></pre><p>可以表示为：</p><p>$$<br>[1,1,1,1]<br>$$</p><p>句子：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 喜欢 NLP</code></pre><p>可以表示为：</p><p>$$<br>[1,2,0,1]<br>$$</p><p>词袋模型的优点是简单，适合和传统机器学习模型结合，例如逻辑回归、朴素贝叶斯、SVM。</p><p>缺点也明显：它丢失了词序。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 你你 喜欢 我</code></pre><p>词袋表示完全一样，但语义并不完全相同。</p><p><img src="https://lsworl.github.io/images/nlp-blog/vectorization-methods.svg" alt="文本向量化方法对比"></p><h2 id="8-n-gram：保留局部词序"><a href="#8-n-gram：保留局部词序" class="headerlink" title="8. n-gram：保留局部词序"></a>8. n-gram：保留局部词序</h2><p>为了弥补词袋模型完全丢失词序的问题，可以使用 n-gram。</p><p>如果 $n&#x3D;1$，就是 unigram：</p><pre class="language-text" data-language="text"><code class="language-text">我 / 喜欢 / NLP</code></pre><p>如果 $n&#x3D;2$，就是 bigram：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢喜欢 NLP</code></pre><p>如果 $n&#x3D;3$，就是 trigram：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 NLP</code></pre><p>例如句子：</p><pre class="language-text" data-language="text"><code class="language-text">我 不 喜欢 这部电影</code></pre><p>如果只看单词：</p><pre class="language-text" data-language="text"><code class="language-text">我, 不, 喜欢, 这部电影</code></pre><p>模型可能难以捕捉“不喜欢”这个组合。</p><p>如果加入 bigram：</p><pre class="language-text" data-language="text"><code class="language-text">不 喜欢</code></pre><p>模型就更容易识别负面情绪。</p><p>n-gram 的问题是特征数量会迅速膨胀。词表越大，n 越大，特征空间越大。</p><h2 id="9-TF-IDF：让关键词更重要"><a href="#9-TF-IDF：让关键词更重要" class="headerlink" title="9. TF-IDF：让关键词更重要"></a>9. TF-IDF：让关键词更重要</h2><p>词袋模型只看词频，但有些词虽然出现很多，却没什么区分度。</p><p>例如中文文本中常见词：</p><pre class="language-text" data-language="text"><code class="language-text">的、是、了、我们、这个</code></pre><p>这些词出现频率高，但通常不太能代表文本主题。</p><p>TF-IDF 用来衡量一个词对某篇文档的重要程度。</p><p>它由两部分组成：</p><pre class="language-text" data-language="text"><code class="language-text">TF：词在当前文档中出现得多不多IDF：词在整个语料库中稀不稀有</code></pre><p>TF 可以写成：</p><p>$$<br>TF(t,d)&#x3D;\frac{\text{词 }t\text{ 在文档 }d\text{ 中出现次数}}{\text{文档 }d\text{ 的总词数}}<br>$$</p><p>IDF 可以写成：</p><p>$$<br>IDF(t)&#x3D;\log\frac{N}{DF(t)+1}<br>$$</p><p>其中：</p><ul><li>$N$ 表示总文档数。</li><li>$DF(t)$ 表示包含词 $t$ 的文档数量。</li><li>加 1 是为了避免分母为 0。</li></ul><p>TF-IDF 为：</p><p>$$<br>TFIDF(t,d)&#x3D;TF(t,d)\times IDF(t)<br>$$</p><h3 id="9-1-一个具体例子"><a href="#9-1-一个具体例子" class="headerlink" title="9.1 一个具体例子"></a>9.1 一个具体例子</h3><p>假设语料库有 100 篇文章。</p><p>词“Transformer”只出现在 5 篇文章中：</p><p>$$<br>IDF(\text{Transformer})&#x3D;\log\frac{100}{5+1}\approx2.81<br>$$</p><p>词“的”出现在 90 篇文章中：</p><p>$$<br>IDF(\text{的})&#x3D;\log\frac{100}{90+1}\approx0.09<br>$$</p><p>如果某篇文章中两个词的 TF 都是 0.02，那么：</p><p>$$<br>TFIDF(\text{Transformer})&#x3D;0.02\times2.81&#x3D;0.0562<br>$$</p><p>$$<br>TFIDF(\text{的})&#x3D;0.02\times0.09&#x3D;0.0018<br>$$</p><p>可以看到，“Transformer”的权重远高于“的”。</p><p>这就是 TF-IDF 的直觉：当前文档中常见、但整个语料中不太常见的词，通常更重要。</p><h2 id="10-词向量：让语义进入向量空间"><a href="#10-词向量：让语义进入向量空间" class="headerlink" title="10. 词向量：让语义进入向量空间"></a>10. 词向量：让语义进入向量空间</h2><p>One-hot、词袋和 TF-IDF 大多是稀疏向量。</p><p>深度学习更常用稠密向量，也就是词向量（Word Embedding）。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">我 -> [0.12, -0.08, 0.31, ...]喜欢 -> [0.40, 0.22, -0.17, ...]NLP -> [-0.11, 0.56, 0.09, ...]</code></pre><p>词向量的维度通常远小于词表大小，例如 100 维、300 维、768 维。</p><p>它的目标是让语义相近的词在向量空间里距离更近。</p><p>例如理想情况下：</p><pre class="language-text" data-language="text"><code class="language-text">喜欢 和 热爱 更接近喜欢 和 冰箱 距离更远</code></pre><p>这和前面 KNN 文章中的“距离”思想也能联系起来：向量空间中距离近，往往表示语义更接近。</p><blockquote><p>相关回顾：<a href="/2026/05/19/machine-learning-knn/">KNN：从距离到分类的直觉算法</a></p></blockquote><h2 id="11-Embedding-层做了什么"><a href="#11-Embedding-层做了什么" class="headerlink" title="11. Embedding 层做了什么"></a>11. Embedding 层做了什么</h2><p>在深度学习模型中，token id 本身只是编号，不包含语义。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">我 -> 2喜欢 -> 3NLP -> 4</code></pre><p>数字 2、3、4 只是索引，不能直接理解成大小关系。</p><p>Embedding 层可以看成一张参数表：</p><table><thead><tr><th align="right">token id</th><th>embedding</th></tr></thead><tbody><tr><td align="right">2</td><td>$[0.12,-0.08,0.31]$</td></tr><tr><td align="right">3</td><td>$[0.40,0.22,-0.17]$</td></tr><tr><td align="right">4</td><td>$[-0.11,0.56,0.09]$</td></tr></tbody></table><p>输入 token id：</p><pre class="language-text" data-language="text"><code class="language-text">[2, 3, 4]</code></pre><p>Embedding 层查表后得到：</p><pre class="language-text" data-language="text"><code class="language-text">[  [0.12, -0.08, 0.31],  [0.40, 0.22, -0.17],  [-0.11, 0.56, 0.09]]</code></pre><p>如果 embedding 维度是 $d$，序列长度是 $L$，那么输出形状是：</p><p>$$<br>L \times d<br>$$</p><p>Transformer 的输入就是这样一串向量。</p><h2 id="12-子词切分：为什么-Transformer-不只按词切"><a href="#12-子词切分：为什么-Transformer-不只按词切" class="headerlink" title="12. 子词切分：为什么 Transformer 不只按词切"></a>12. 子词切分：为什么 Transformer 不只按词切</h2><p>早期 NLP 常用词级分词，但词级分词有一个大问题：词表很难覆盖所有词。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">TransformerTransformersTransformer-basedunbelievableunbelievably</code></pre><p>如果每个词都单独进词表，词表会很大，而且遇到新词容易变成 <code>[UNK]</code>。</p><p>子词切分（Subword Tokenization）就是折中方案：</p><pre class="language-text" data-language="text"><code class="language-text">既不要按字符切得太碎也不要按完整词切得太死</code></pre><p>例如英文：</p><pre class="language-text" data-language="text"><code class="language-text">unbelievable -> un + believe + able</code></pre><p>中文里也可能按字、词或子词混合：</p><pre class="language-text" data-language="text"><code class="language-text">自然语言处理 -> 自然 / 语言 / 处理</code></pre><p>常见子词算法包括：</p><ul><li>BPE</li><li>WordPiece</li><li>SentencePiece</li></ul><p>很多 Transformer 模型都使用这类 tokenizer。</p><h2 id="13-BPE-的直觉"><a href="#13-BPE-的直觉" class="headerlink" title="13. BPE 的直觉"></a>13. BPE 的直觉</h2><p>BPE（Byte Pair Encoding）的思想可以简化理解为：</p><blockquote><p>从字符开始，不断合并最常见的相邻片段。</p></blockquote><p>假设语料中经常出现：</p><pre class="language-text" data-language="text"><code class="language-text">自然 语言自然 科学自然 风景</code></pre><p>如果“自然”出现频率很高，就可以把：</p><pre class="language-text" data-language="text"><code class="language-text">自 + 然</code></pre><p>合并成：</p><pre class="language-text" data-language="text"><code class="language-text">自然</code></pre><p>类似地，如果“语言”经常一起出现，也可以合并：</p><pre class="language-text" data-language="text"><code class="language-text">语 + 言 -> 语言</code></pre><p>最终 tokenizer 会形成一个子词词表，既能表示常见词，也能拆开不常见词。</p><p>这对 Transformer 很重要，因为它减少了 <code>[UNK]</code> 的出现，也控制了词表大小。</p><h2 id="14-特殊-token"><a href="#14-特殊-token" class="headerlink" title="14. 特殊 token"></a>14. 特殊 token</h2><p>Transformer 中经常会看到一些特殊 token。</p><table><thead><tr><th>token</th><th>含义</th></tr></thead><tbody><tr><td><code>[PAD]</code></td><td>补齐长度</td></tr><tr><td><code>[UNK]</code></td><td>未知 token</td></tr><tr><td><code>[CLS]</code></td><td>分类任务的整体表示</td></tr><tr><td><code>[SEP]</code></td><td>分隔句子</td></tr><tr><td><code>[MASK]</code></td><td>掩码语言模型中被遮住的位置</td></tr></tbody></table><p>例如 BERT 处理句子分类时，可能会把：</p><pre class="language-text" data-language="text"><code class="language-text">我 喜欢 NLP</code></pre><p>变成：</p><pre class="language-text" data-language="text"><code class="language-text">[CLS] 我 喜欢 NLP [SEP]</code></pre><p>其中 <code>[CLS]</code> 位置的输出常被用来做整个句子的分类。</p><h2 id="15-Padding-与-Attention-Mask"><a href="#15-Padding-与-Attention-Mask" class="headerlink" title="15. Padding 与 Attention Mask"></a>15. Padding 与 Attention Mask</h2><p>深度学习通常会批量训练，但一个 batch 里的句子长度可能不同。</p><p>例如：</p><pre class="language-text" data-language="text"><code class="language-text">句子 A：我 喜欢 NLP句子 B：今天 学习 文本 处理</code></pre><p>为了放进同一个矩阵，需要把短句补齐：</p><pre class="language-text" data-language="text"><code class="language-text">句子 A：[我, 喜欢, NLP, [PAD]]句子 B：[今天, 学习, 文本, 处理]</code></pre><p>但 <code>[PAD]</code> 只是补位，不应该参与模型理解。</p><p>因此需要 attention mask：</p><pre class="language-text" data-language="text"><code class="language-text">句子 A mask：[1, 1, 1, 0]句子 B mask：[1, 1, 1, 1]</code></pre><p>其中：</p><ul><li>1 表示真实 token。</li><li>0 表示 padding token。</li></ul><p><img src="https://lsworl.github.io/images/nlp-blog/padding-attention-mask.svg" alt="Padding 与 Attention Mask"></p><p>在 Transformer 中，attention mask 会告诉模型：</p><pre class="language-text" data-language="text"><code class="language-text">不要关注 [PAD] 位置</code></pre><p>这对理解 Transformer 的注意力机制非常重要。</p><h2 id="16-文本处理与传统机器学习"><a href="#16-文本处理与传统机器学习" class="headerlink" title="16. 文本处理与传统机器学习"></a>16. 文本处理与传统机器学习</h2><p>在进入深度学习之前，文本也可以用传统机器学习处理。</p><p>常见组合是：</p><pre class="language-text" data-language="text"><code class="language-text">文本 -> TF-IDF -> 逻辑回归 / SVM / 朴素贝叶斯</code></pre><p>例如情感分类：</p><pre class="language-text" data-language="text"><code class="language-text">输入：这个商品很好用输出：正面</code></pre><p>可以先用 TF-IDF 得到向量，再用逻辑回归分类。</p><p>这和前面逻辑回归文章可以联系起来：</p><blockquote><p>相关回顾：<a href="/2026/05/24/machine-learning-logistic-regression/">逻辑回归：从直线到概率的分类算法</a></p></blockquote><p>如果特征维度很高，还要注意正则化：</p><blockquote><p>相关回顾：<a href="/2026/06/04/machine-learning-regularization-overfitting/">正则化与过拟合：让模型不只记住训练集</a></p></blockquote><p>文本任务中，词表可能有几万甚至几十万个特征，过拟合风险很常见，L1&#x2F;L2 正则化会很有用。</p><h2 id="17-Python-实战：词袋与-TF-IDF"><a href="#17-Python-实战：词袋与-TF-IDF" class="headerlink" title="17. Python 实战：词袋与 TF-IDF"></a>17. Python 实战：词袋与 TF-IDF</h2><p>下面用 scikit-learn 演示如何把文本转成词袋和 TF-IDF。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>feature_extraction<span class="token punctuation">.</span>text <span class="token keyword">import</span> CountVectorizer<span class="token punctuation">,</span> TfidfVectorizertexts <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token string">"我 喜欢 自然语言处理"</span><span class="token punctuation">,</span>    <span class="token string">"我 喜欢 机器学习"</span><span class="token punctuation">,</span>    <span class="token string">"自然语言处理 和 机器学习 很有趣"</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token comment"># 词袋模型</span>count_vectorizer <span class="token operator">=</span> CountVectorizer<span class="token punctuation">(</span>token_pattern<span class="token operator">=</span><span class="token string">r"(?u)\b\w+\b"</span><span class="token punctuation">)</span>count_matrix <span class="token operator">=</span> count_vectorizer<span class="token punctuation">.</span>fit_transform<span class="token punctuation">(</span>texts<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"词表："</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>count_vectorizer<span class="token punctuation">.</span>vocabulary_<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"词袋矩阵："</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>count_matrix<span class="token punctuation">.</span>toarray<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token comment"># TF-IDF</span>tfidf_vectorizer <span class="token operator">=</span> TfidfVectorizer<span class="token punctuation">(</span>token_pattern<span class="token operator">=</span><span class="token string">r"(?u)\b\w+\b"</span><span class="token punctuation">)</span>tfidf_matrix <span class="token operator">=</span> tfidf_vectorizer<span class="token punctuation">.</span>fit_transform<span class="token punctuation">(</span>texts<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"TF-IDF 矩阵："</span><span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>tfidf_matrix<span class="token punctuation">.</span>toarray<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>这里为了演示，文本已经提前用空格分好了词。</p><p>实际中文任务中，可以先用分词工具把句子切开，再交给 <code>CountVectorizer</code> 或 <code>TfidfVectorizer</code>。</p><h2 id="18-Python-实战：文本分类-Pipeline"><a href="#18-Python-实战：文本分类-Pipeline" class="headerlink" title="18. Python 实战：文本分类 Pipeline"></a>18. Python 实战：文本分类 Pipeline</h2><p>下面用 TF-IDF + 逻辑回归做一个简单文本分类流程。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>feature_extraction<span class="token punctuation">.</span>text <span class="token keyword">import</span> TfidfVectorizer<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>linear_model <span class="token keyword">import</span> LogisticRegression<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>pipeline <span class="token keyword">import</span> Pipelinetexts <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token string">"这个 手机 很 好用"</span><span class="token punctuation">,</span>    <span class="token string">"物流 很 快 包装 很 好"</span><span class="token punctuation">,</span>    <span class="token string">"质量 太 差 了"</span><span class="token punctuation">,</span>    <span class="token string">"屏幕 有 划痕 很 失望"</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token comment"># 1 表示正面，0 表示负面</span>y <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">]</span>pipe <span class="token operator">=</span> Pipeline<span class="token punctuation">(</span><span class="token punctuation">[</span>    <span class="token punctuation">(</span><span class="token string">"tfidf"</span><span class="token punctuation">,</span> TfidfVectorizer<span class="token punctuation">(</span>token_pattern<span class="token operator">=</span><span class="token string">r"(?u)\b\w+\b"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token punctuation">(</span><span class="token string">"model"</span><span class="token punctuation">,</span> LogisticRegression<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">]</span><span class="token punctuation">)</span>pipe<span class="token punctuation">.</span>fit<span class="token punctuation">(</span>texts<span class="token punctuation">,</span> y<span class="token punctuation">)</span>new_texts <span class="token operator">=</span> <span class="token punctuation">[</span>    <span class="token string">"这个 手机 质量 很 好"</span><span class="token punctuation">,</span>    <span class="token string">"包装 很 差 很 失望"</span><span class="token punctuation">,</span><span class="token punctuation">]</span>pred <span class="token operator">=</span> pipe<span class="token punctuation">.</span>predict<span class="token punctuation">(</span>new_texts<span class="token punctuation">)</span>proba <span class="token operator">=</span> pipe<span class="token punctuation">.</span>predict_proba<span class="token punctuation">(</span>new_texts<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>pred<span class="token punctuation">)</span><span class="token keyword">print</span><span class="token punctuation">(</span>proba<span class="token punctuation">)</span></code></pre><p>这个流程和前面交叉验证文章中的 Pipeline 思想是一致的：</p><pre class="language-text" data-language="text"><code class="language-text">预处理和模型训练应该放在同一个 Pipeline 中</code></pre><p>这样做交叉验证时，可以避免数据泄漏。</p><blockquote><p>相关回顾：<a href="/2026/05/25/machine-learning-cross-validation-grid-search/">交叉验证与网格搜索：让模型选择更可靠</a></p></blockquote><h2 id="19-Transformer-前需要理解什么"><a href="#19-Transformer-前需要理解什么" class="headerlink" title="19. Transformer 前需要理解什么"></a>19. Transformer 前需要理解什么</h2><p>如果你正在学习 Transformer，文本处理里最重要的是下面几个概念：</p><ol><li>token：模型处理的基本单位。</li><li>tokenizer：把文本切成 token 的工具。</li><li>vocabulary：token 到 id 的映射表。</li><li>input_ids：token id 序列。</li><li>embedding：把 token id 映射成向量。</li><li>padding：把不同长度句子补齐。</li><li>attention_mask：告诉模型哪些位置是真实 token。</li><li>subword：用子词减少未知词问题。</li></ol><p>Transformer 不直接处理文字，它处理的是：</p><pre class="language-text" data-language="text"><code class="language-text">input_ids + attention_mask</code></pre><p>然后模型内部通过 embedding 层把 <code>input_ids</code> 变成向量序列，再进入注意力层。</p><h2 id="20-总结"><a href="#20-总结" class="headerlink" title="20. 总结"></a>20. 总结</h2><p>文本处理的核心问题是：</p><pre class="language-text" data-language="text"><code class="language-text">如何把自然语言变成模型能计算的数字表示？</code></pre><p>传统 NLP 中，常见方法包括：</p><ul><li>One-hot</li><li>词袋模型</li><li>n-gram</li><li>TF-IDF</li></ul><p>深度学习和 Transformer 中，更常见的是：</p><ul><li>tokenization</li><li>token id</li><li>embedding</li><li>subword</li><li>padding</li><li>attention mask</li></ul><p>理解这些内容以后，再学习 Transformer 时就会顺很多。因为 Transformer 的注意力机制虽然很重要，但它接收的输入并不是原始文本，而是经过 tokenizer 和 embedding 处理后的向量序列。</p>]]>
    </content>
    <id>https://lsworl.github.io/2026/06/07/nlp-text-processing/</id>
    <link href="https://lsworl.github.io/2026/06/07/nlp-text-processing/"/>
    <published>2026-06-06T16:00:00.000Z</published>
    <summary>从文本清洗、分词、词表、One-hot、词袋、TF-IDF、词向量、子词切分、padding 和 attention mask 几个角度理解 NLP 中文本如何变成模型输入。</summary>
    <title>文本处理：让自然语言变成模型能理解的数字</title>
    <updated>2026-06-07T15:14:26.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>LsWorld</name>
    </author>
    <category term="机器学习" scheme="https://lsworl.github.io/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="机器学习" scheme="https://lsworl.github.io/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    <category term="模型评估" scheme="https://lsworl.github.io/tags/%E6%A8%A1%E5%9E%8B%E8%AF%84%E4%BC%B0/"/>
    <category term="正则化" scheme="https://lsworl.github.io/tags/%E6%AD%A3%E5%88%99%E5%8C%96/"/>
    <category term="过拟合" scheme="https://lsworl.github.io/tags/%E8%BF%87%E6%8B%9F%E5%90%88/"/>
    <category term="泛化能力" scheme="https://lsworl.github.io/tags/%E6%B3%9B%E5%8C%96%E8%83%BD%E5%8A%9B/"/>
    <content>
      <![CDATA[<p>前面学习线性回归、逻辑回归、决策树、集成学习和 GBDT 时，我们一直在训练模型，让模型尽可能学到数据里的规律。</p><p>但机器学习里有一个很重要的问题：</p><blockquote><p>模型在训练集上表现很好，不代表它在新数据上也表现很好。</p></blockquote><p>如果模型把训练数据里的噪声、偶然样本、特殊细节都记住了，就会出现过拟合（Overfitting）。正则化（Regularization）就是用来控制模型复杂度、提升泛化能力的一类方法。</p><span id="more"></span><p><img src="https://lsworl.github.io/images/ml-blog/regularization-fitting.svg" alt="欠拟合、合适拟合与过拟合"></p><h2 id="1-什么是泛化能力"><a href="#1-什么是泛化能力" class="headerlink" title="1. 什么是泛化能力"></a>1. 什么是泛化能力</h2><p>机器学习真正关心的不是训练集分数，而是模型面对新数据时的表现。</p><p>例如我们训练一个模型预测学生是否通过考试。训练集中有 100 个学生，模型全部预测正确，训练准确率达到 $100%$。</p><p>这听起来很好，但如果换一批新学生，准确率只有 $65%$，说明模型可能只是记住了训练集，而没有学到稳定规律。</p><p>这种“面对新数据仍然表现好”的能力，叫泛化能力。</p><p>可以简单记住：</p><pre class="language-text" data-language="text"><code class="language-text">训练集表现好：说明模型会做旧题测试集表现好：说明模型学到了规律</code></pre><p>在交叉验证那篇文章里，我们已经讲过：不能只看一次训练&#x2F;测试划分，更稳妥的方式是用验证集或交叉验证评估模型。正则化则是进一步解决“模型太复杂”的问题。</p><blockquote><p>相关回顾：<a href="/2026/05/25/machine-learning-cross-validation-grid-search/">交叉验证与网格搜索：让模型选择更可靠</a></p></blockquote><h2 id="2-欠拟合与过拟合"><a href="#2-欠拟合与过拟合" class="headerlink" title="2. 欠拟合与过拟合"></a>2. 欠拟合与过拟合</h2><p>模型常见问题可以分成两类。</p><h3 id="2-1-欠拟合"><a href="#2-1-欠拟合" class="headerlink" title="2.1 欠拟合"></a>2.1 欠拟合</h3><p>欠拟合（Underfitting）表示模型太简单，连训练集里的基本规律都学不好。</p><p>例如用一条直线拟合明显弯曲的数据：</p><pre class="language-text" data-language="text"><code class="language-text">训练集误差高测试集误差也高</code></pre><p>这种情况通常需要：</p><ul><li>换更复杂的模型。</li><li>加入更有用的特征。</li><li>增加多项式特征或非线性模型。</li><li>减弱正则化强度。</li></ul><h3 id="2-2-过拟合"><a href="#2-2-过拟合" class="headerlink" title="2.2 过拟合"></a>2.2 过拟合</h3><p>过拟合（Overfitting）表示模型太复杂，把训练集里的噪声也记住了。</p><p>例如一棵很深的决策树，可能把每个训练样本都分得很细，训练集准确率很高，但测试集效果很差。</p><pre class="language-text" data-language="text"><code class="language-text">训练集误差低测试集误差高</code></pre><p>这种情况通常需要：</p><ul><li>降低模型复杂度。</li><li>增加正则化。</li><li>使用更多训练数据。</li><li>使用交叉验证调参。</li><li>对决策树限制深度或剪枝。</li></ul><h2 id="3-训练误差和验证误差怎么看"><a href="#3-训练误差和验证误差怎么看" class="headerlink" title="3. 训练误差和验证误差怎么看"></a>3. 训练误差和验证误差怎么看</h2><p>判断过拟合时，不要只看训练误差。</p><p><img src="https://lsworl.github.io/images/ml-blog/train-validation-error.svg" alt="训练误差与验证误差曲线"></p><p>随着模型复杂度增加，通常会出现这样的变化：</p><table><thead><tr><th>模型复杂度</th><th align="right">训练误差</th><th align="right">验证误差</th><th>状态</th></tr></thead><tbody><tr><td>太低</td><td align="right">高</td><td align="right">高</td><td>欠拟合</td></tr><tr><td>合适</td><td align="right">较低</td><td align="right">较低</td><td>泛化较好</td></tr><tr><td>太高</td><td align="right">很低</td><td align="right">变高</td><td>过拟合</td></tr></tbody></table><p>假设我们调决策树的 <code>max_depth</code>，得到结果：</p><table><thead><tr><th align="right"><code>max_depth</code></th><th align="right">训练准确率</th><th align="right">验证准确率</th></tr></thead><tbody><tr><td align="right">1</td><td align="right">0.72</td><td align="right">0.70</td></tr><tr><td align="right">3</td><td align="right">0.86</td><td align="right">0.84</td></tr><tr><td align="right">5</td><td align="right">0.93</td><td align="right">0.88</td></tr><tr><td align="right">10</td><td align="right">1.00</td><td align="right">0.79</td></tr></tbody></table><p>如果只看训练准确率，<code>max_depth=10</code> 最好。但它的验证准确率下降了，说明树可能记住了训练集细节。</p><p>更合理的选择可能是：</p><pre class="language-text" data-language="text"><code class="language-text">max_depth = 5</code></pre><p>这也是为什么交叉验证和正则化常常要一起出现：交叉验证帮助我们发现过拟合，正则化帮助我们缓解过拟合。</p><h2 id="4-偏差与方差"><a href="#4-偏差与方差" class="headerlink" title="4. 偏差与方差"></a>4. 偏差与方差</h2><p>过拟合和欠拟合也可以从偏差（Bias）与方差（Variance）理解。</p><p>偏差大，表示模型假设太简单，系统性地学不到真实规律。</p><p>方差大，表示模型对训练数据太敏感，训练数据稍微变一下，模型就明显变化。</p><p>可以简单理解：</p><table><thead><tr><th>情况</th><th>偏差</th><th>方差</th><th>表现</th></tr></thead><tbody><tr><td>欠拟合</td><td>高</td><td>低</td><td>训练集和测试集都差</td></tr><tr><td>合适拟合</td><td>较低</td><td>较低</td><td>训练集和测试集都不错</td></tr><tr><td>过拟合</td><td>低</td><td>高</td><td>训练集好，测试集差</td></tr></tbody></table><p>KNN 中 $K$ 太小、决策树太深、GBDT 树太多或学习率太大，都可能导致方差偏高。</p><blockquote><p>相关回顾：<a href="/2026/05/19/machine-learning-knn/">KNN：从距离到分类的直觉算法</a></p></blockquote><blockquote><p>相关回顾：<a href="/2026/05/27/machine-learning-decision-tree/">决策树：像做选择题一样理解分类与回归</a></p></blockquote><h2 id="5-正则化的核心思想"><a href="#5-正则化的核心思想" class="headerlink" title="5. 正则化的核心思想"></a>5. 正则化的核心思想</h2><p>正则化的核心是：</p><blockquote><p>在原来的损失函数后面，加一个惩罚项，限制模型不要太复杂。</p></blockquote><p>普通训练目标通常是：</p><p>$$<br>\min_{\theta} J(\theta)<br>$$</p><p>加入正则化后变成：</p><p>$$<br>\min_{\theta} \left[J(\theta)+\lambda \Omega(\theta)\right]<br>$$</p><p>其中：</p><ul><li>$J(\theta)$ 是原始损失函数。</li><li>$\Omega(\theta)$ 是正则化惩罚项。</li><li>$\lambda$ 是正则化强度。</li></ul><p>这个公式可以理解成：</p><pre class="language-text" data-language="text"><code class="language-text">既要预测误差小，也要模型别太复杂</code></pre><p>如果 $\lambda$ 很小，正则化影响弱，模型更自由，可能过拟合。</p><p>如果 $\lambda$ 很大，正则化影响强，模型被限制得很厉害，可能欠拟合。</p><h2 id="6-L2-正则化：让参数整体变小"><a href="#6-L2-正则化：让参数整体变小" class="headerlink" title="6. L2 正则化：让参数整体变小"></a>6. L2 正则化：让参数整体变小</h2><p>L2 正则化会惩罚参数平方和：</p><p>$$<br>\Omega(\mathbf{w})&#x3D;\sum_{j&#x3D;1}^{n}w_j^2<br>$$</p><p>加入 L2 后，目标函数可以写成：</p><p>$$<br>J_{L2}(\mathbf{w},b)<br>&#x3D;<br>J(\mathbf{w},b)<br>+\lambda \sum_{j&#x3D;1}^{n}w_j^2<br>$$</p><p>L2 的作用是让权重不要太大。</p><p>为什么权重太大可能不好？</p><p>假设线性模型为：</p><p>$$<br>\hat{y}&#x3D;w_1x_1+w_2x_2+b<br>$$</p><p>如果 $w_1&#x3D;100$，那么 $x_1$ 只要变化一点点，预测值就会剧烈变化。模型会对输入扰动很敏感，泛化能力可能变差。</p><p>L2 正则化会让参数更平滑、更保守。</p><h3 id="6-1-一个带数字的例子"><a href="#6-1-一个带数字的例子" class="headerlink" title="6.1 一个带数字的例子"></a>6.1 一个带数字的例子</h3><p>假设有两个模型，在训练集上的 MSE 都差不多：</p><table><thead><tr><th>模型</th><th align="right">MSE</th><th>参数</th></tr></thead><tbody><tr><td>模型 A</td><td align="right">10</td><td>$w_1&#x3D;2,w_2&#x3D;1$</td></tr><tr><td>模型 B</td><td align="right">9</td><td>$w_1&#x3D;20,w_2&#x3D;-15$</td></tr></tbody></table><p>如果只看 MSE，模型 B 更好。</p><p>但加入 L2 正则化，设 $\lambda&#x3D;0.1$：</p><p>模型 A：</p><p>$$<br>J_{L2}&#x3D;10+0.1(2^2+1^2)&#x3D;10+0.5&#x3D;10.5<br>$$</p><p>模型 B：</p><p>$$<br>J_{L2}&#x3D;9+0.1(20^2+(-15)^2)&#x3D;9+0.1(625)&#x3D;71.5<br>$$</p><p>加入正则化后，模型 B 虽然训练误差略低，但参数太大，惩罚非常高。模型会更倾向于选择模型 A 这种更平稳的参数。</p><p>这就是 L2 正则化的直觉：宁愿训练误差稍微高一点，也不要让模型用非常大的参数去死记训练集。</p><h2 id="7-Ridge-回归"><a href="#7-Ridge-回归" class="headerlink" title="7. Ridge 回归"></a>7. Ridge 回归</h2><p>在线性回归中加入 L2 正则化，就得到 Ridge 回归。</p><p>普通线性回归目标是：</p><p>$$<br>\min_{\mathbf{w},b}<br>\frac{1}{m}\sum_{i&#x3D;1}^{m}<br>(\hat{y}^{(i)}-y^{(i)})^2<br>$$</p><p>Ridge 回归目标是：</p><p>$$<br>\min_{\mathbf{w},b}<br>\left[<br>\frac{1}{m}\sum_{i&#x3D;1}^{m}<br>(\hat{y}^{(i)}-y^{(i)})^2<br>+\lambda\sum_{j&#x3D;1}^{n}w_j^2<br>\right]<br>$$</p><p>注意：通常不惩罚偏置 $b$，主要惩罚权重 $\mathbf{w}$。</p><p>Ridge 很适合特征之间存在相关性、参数不稳定的场景。</p><blockquote><p>相关回顾：<a href="/2026/05/23/machine-learning-linear-regression/">线性回归：从直觉到最小二乘法</a></p></blockquote><h2 id="8-L1-正则化：让一些参数变成-0"><a href="#8-L1-正则化：让一些参数变成-0" class="headerlink" title="8. L1 正则化：让一些参数变成 0"></a>8. L1 正则化：让一些参数变成 0</h2><p>L1 正则化惩罚参数绝对值之和：</p><p>$$<br>\Omega(\mathbf{w})&#x3D;\sum_{j&#x3D;1}^{n}|w_j|<br>$$</p><p>加入 L1 后：</p><p>$$<br>J_{L1}(\mathbf{w},b)<br>&#x3D;<br>J(\mathbf{w},b)<br>+\lambda \sum_{j&#x3D;1}^{n}|w_j|<br>$$</p><p>L1 的一个重要特点是：它更容易让某些参数变成 0。</p><p>如果某个特征对应的权重变成 0，就相当于模型不再使用这个特征。因此 L1 正则化可以产生稀疏模型，也能起到特征选择的作用。</p><p><img src="https://lsworl.github.io/images/ml-blog/regularization-l1-l2.svg" alt="L1 与 L2 正则化约束直觉"></p><h3 id="8-1-一个带数字的例子"><a href="#8-1-一个带数字的例子" class="headerlink" title="8.1 一个带数字的例子"></a>8.1 一个带数字的例子</h3><p>假设模型有 5 个特征：</p><pre class="language-text" data-language="text"><code class="language-text">学习时长、睡眠时长、是否吃早餐、鞋码、今天星期几</code></pre><p>预测考试是否通过时，“鞋码”和“今天星期几”可能没有什么意义。</p><p>普通模型可能学到：</p><p>$$<br>\mathbf{w}&#x3D;[1.8,0.7,0.3,0.05,-0.02]<br>$$</p><p>L1 正则化后，模型可能变成：</p><p>$$<br>\mathbf{w}&#x3D;[1.6,0.5,0.2,0,0]<br>$$</p><p>这表示模型自动把“鞋码”和“今天星期几”的权重压到了 0。</p><p>所以 L1 常用于：</p><ul><li>特征很多时做特征选择。</li><li>希望模型更简洁时。</li><li>高维稀疏数据，例如文本特征。</li></ul><h2 id="9-Lasso-回归"><a href="#9-Lasso-回归" class="headerlink" title="9. Lasso 回归"></a>9. Lasso 回归</h2><p>在线性回归中加入 L1 正则化，就得到 Lasso 回归。</p><p>目标函数为：</p><p>$$<br>\min_{\mathbf{w},b}<br>\left[<br>\frac{1}{m}\sum_{i&#x3D;1}^{m}<br>(\hat{y}^{(i)}-y^{(i)})^2<br>+\lambda\sum_{j&#x3D;1}^{n}|w_j|<br>\right]<br>$$</p><p>Lasso 和 Ridge 的区别可以简单记忆：</p><table><thead><tr><th>方法</th><th>正则项</th><th>效果</th></tr></thead><tbody><tr><td>Ridge</td><td>$\sum w_j^2$</td><td>让参数变小</td></tr><tr><td>Lasso</td><td>$\sum</td><td>w_j</td></tr></tbody></table><p>如果希望保留所有特征但让参数稳定，可以用 Ridge。</p><p>如果希望自动筛掉一些特征，可以用 Lasso。</p><h2 id="10-逻辑回归中的正则化"><a href="#10-逻辑回归中的正则化" class="headerlink" title="10. 逻辑回归中的正则化"></a>10. 逻辑回归中的正则化</h2><p>逻辑回归中也经常使用正则化。</p><p>普通逻辑回归使用交叉熵损失：</p><p>$$<br>J(\mathbf{w},b)&#x3D;<br>-\frac{1}{m}\sum_{i&#x3D;1}^{m}<br>\left[<br>y^{(i)}\log(\hat{y}^{(i)})<br>+(1-y^{(i)})\log(1-\hat{y}^{(i)})<br>\right]<br>$$</p><p>加入 L2 正则化后：</p><p>$$<br>J_{L2}(\mathbf{w},b)&#x3D;J(\mathbf{w},b)+\lambda\sum_{j&#x3D;1}^{n}w_j^2<br>$$</p><p>这可以防止逻辑回归为了把训练集分得特别开，而学到过大的权重。</p><p>例如某个二分类任务中，两个类别刚好在训练集上几乎线性可分。没有正则化时，逻辑回归可能会把权重推得很大，让决策边界非常自信。但新数据稍微偏一点，模型就可能预测错。</p><p>加入正则化后，模型会更克制，概率输出也更稳定。</p><blockquote><p>相关回顾：<a href="/2026/05/24/machine-learning-logistic-regression/">逻辑回归：从直线到概率的分类算法</a></p></blockquote><h2 id="11-正则化如何影响梯度下降"><a href="#11-正则化如何影响梯度下降" class="headerlink" title="11. 正则化如何影响梯度下降"></a>11. 正则化如何影响梯度下降</h2><p>以 L2 正则化为例：</p><p>$$<br>J_{L2}(\mathbf{w})&#x3D;J(\mathbf{w})+\lambda\sum_{j&#x3D;1}^{n}w_j^2<br>$$</p><p>对 $w_j$ 求导：</p><p>$$<br>\frac{\partial J_{L2}}{\partial w_j}<br>&#x3D;<br>\frac{\partial J}{\partial w_j}<br>+2\lambda w_j<br>$$</p><p>梯度下降更新为：</p><p>$$<br>w_j :&#x3D; w_j-\alpha<br>\left(<br>\frac{\partial J}{\partial w_j}<br>+2\lambda w_j<br>\right)<br>$$</p><p>整理一下：</p><p>$$<br>w_j :&#x3D; (1-2\alpha\lambda)w_j<br>-\alpha\frac{\partial J}{\partial w_j}<br>$$</p><p>其中 $(1-2\alpha\lambda)w_j$ 会让权重每次更新时被稍微缩小一点。</p><p>这也是为什么 L2 正则化在深度学习里常常叫 weight decay，也就是权重衰减。</p><blockquote><p>相关回顾：<a href="/2026/05/26/machine-learning-gradient-descent/">梯度下降法：机器学习如何一步步找到最优解</a></p></blockquote><h2 id="12-决策树中的正则化：限制树复杂度"><a href="#12-决策树中的正则化：限制树复杂度" class="headerlink" title="12. 决策树中的正则化：限制树复杂度"></a>12. 决策树中的正则化：限制树复杂度</h2><p>正则化不只存在于线性模型中。</p><p>对于决策树，控制模型复杂度的方式包括：</p><ul><li><code>max_depth</code>：限制最大深度。</li><li><code>min_samples_leaf</code>：限制叶子节点最少样本数。</li><li><code>min_samples_split</code>：限制内部节点继续划分所需的最少样本数。</li><li>剪枝：去掉贡献不大的分支。</li></ul><p>例如一棵树如果不限制深度，可能长成这样：</p><pre class="language-text" data-language="text"><code class="language-text">一直划分，直到每个叶子只剩 1 个样本</code></pre><p>这时训练集准确率可能很高，但新数据表现不一定好。</p><p>如果设置：</p><pre class="language-python" data-language="python"><code class="language-python">DecisionTreeClassifier<span class="token punctuation">(</span>max_depth<span class="token operator">=</span><span class="token number">3</span><span class="token punctuation">,</span> min_samples_leaf<span class="token operator">=</span><span class="token number">5</span><span class="token punctuation">)</span></code></pre><p>就相当于告诉模型：</p><pre class="language-text" data-language="text"><code class="language-text">不要把训练集切得太细，不要为了少数样本生成过于复杂的规则</code></pre><p>从广义上看，这也是一种正则化。</p><h2 id="13-GBDT-中的正则化思想"><a href="#13-GBDT-中的正则化思想" class="headerlink" title="13. GBDT 中的正则化思想"></a>13. GBDT 中的正则化思想</h2><p>GBDT 本身是 Boosting 模型，表达能力很强，也可能过拟合。</p><p>常见控制方法包括：</p><ul><li>降低 <code>learning_rate</code>。</li><li>限制单棵树深度 <code>max_depth</code>。</li><li>限制叶子节点最少样本数。</li><li>减少树数量，或使用 early stopping。</li><li>使用 <code>subsample</code> 做随机采样。</li></ul><p>GBDT 的更新公式是：</p><p>$$<br>F_t(x)&#x3D;F_{t-1}(x)+\eta h_t(x)<br>$$</p><p>其中 $\eta$ 就是学习率。</p><p>如果 $\eta$ 很大，每棵树修正幅度太猛，容易把训练集噪声也学进去。</p><p>如果 $\eta$ 较小，每棵树只修正一点点，模型更稳，但需要更多树。</p><blockquote><p>相关回顾：<a href="/2026/06/02/machine-learning-gbdt/">GBDT：从残差到梯度提升树的完整理解</a></p></blockquote><h2 id="14-深度学习中的正则化"><a href="#14-深度学习中的正则化" class="headerlink" title="14. 深度学习中的正则化"></a>14. 深度学习中的正则化</h2><p>这篇文章也可以作为进入深度学习前的过渡。</p><p>神经网络参数很多，表达能力很强，因此也很容易过拟合。深度学习里常见正则化方法包括：</p><ul><li>L2 正则化，也叫 weight decay。</li><li>Dropout，训练时随机丢弃一部分神经元。</li><li>Batch Normalization，有时也能带来一定正则化效果。</li><li>Early Stopping，验证集效果不再提升时停止训练。</li><li>数据增强，通过变换样本增加数据多样性。</li></ul><p>如果后面学习神经网络，可以把这些方法都理解为：</p><pre class="language-text" data-language="text"><code class="language-text">不要让模型只记住训练集，要让它学到更稳定、更通用的规律</code></pre><h2 id="15-Python-实战：Ridge-与-Lasso"><a href="#15-Python-实战：Ridge-与-Lasso" class="headerlink" title="15. Python 实战：Ridge 与 Lasso"></a>15. Python 实战：Ridge 与 Lasso</h2><p>下面用 scikit-learn 对比普通线性回归、Ridge 和 Lasso。</p><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">import</span> numpy <span class="token keyword">as</span> np<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>datasets <span class="token keyword">import</span> make_regression<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>linear_model <span class="token keyword">import</span> Lasso<span class="token punctuation">,</span> LinearRegression<span class="token punctuation">,</span> Ridge<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>metrics <span class="token keyword">import</span> mean_squared_error<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>model_selection <span class="token keyword">import</span> train_test_split<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>preprocessing <span class="token keyword">import</span> StandardScaler<span class="token comment"># 构造一个回归数据集，其中只有部分特征真正有用</span>X<span class="token punctuation">,</span> y <span class="token operator">=</span> make_regression<span class="token punctuation">(</span>    n_samples<span class="token operator">=</span><span class="token number">200</span><span class="token punctuation">,</span>    n_features<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">,</span>    n_informative<span class="token operator">=</span><span class="token number">4</span><span class="token punctuation">,</span>    noise<span class="token operator">=</span><span class="token number">20</span><span class="token punctuation">,</span>    random_state<span class="token operator">=</span><span class="token number">42</span><span class="token punctuation">,</span><span class="token punctuation">)</span>X_train<span class="token punctuation">,</span> X_test<span class="token punctuation">,</span> y_train<span class="token punctuation">,</span> y_test <span class="token operator">=</span> train_test_split<span class="token punctuation">(</span>    X<span class="token punctuation">,</span>    y<span class="token punctuation">,</span>    test_size<span class="token operator">=</span><span class="token number">0.2</span><span class="token punctuation">,</span>    random_state<span class="token operator">=</span><span class="token number">42</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token comment"># 正则化模型通常建议先标准化特征</span>scaler <span class="token operator">=</span> StandardScaler<span class="token punctuation">(</span><span class="token punctuation">)</span>X_train_scaled <span class="token operator">=</span> scaler<span class="token punctuation">.</span>fit_transform<span class="token punctuation">(</span>X_train<span class="token punctuation">)</span>X_test_scaled <span class="token operator">=</span> scaler<span class="token punctuation">.</span>transform<span class="token punctuation">(</span>X_test<span class="token punctuation">)</span>models <span class="token operator">=</span> <span class="token punctuation">&#123;</span>    <span class="token string">"LinearRegression"</span><span class="token punctuation">:</span> LinearRegression<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token string">"Ridge"</span><span class="token punctuation">:</span> Ridge<span class="token punctuation">(</span>alpha<span class="token operator">=</span><span class="token number">1.0</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token string">"Lasso"</span><span class="token punctuation">:</span> Lasso<span class="token punctuation">(</span>alpha<span class="token operator">=</span><span class="token number">0.1</span><span class="token punctuation">,</span> max_iter<span class="token operator">=</span><span class="token number">10000</span><span class="token punctuation">)</span><span class="token punctuation">,</span><span class="token punctuation">&#125;</span><span class="token keyword">for</span> name<span class="token punctuation">,</span> model <span class="token keyword">in</span> models<span class="token punctuation">.</span>items<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>    model<span class="token punctuation">.</span>fit<span class="token punctuation">(</span>X_train_scaled<span class="token punctuation">,</span> y_train<span class="token punctuation">)</span>    y_pred <span class="token operator">=</span> model<span class="token punctuation">.</span>predict<span class="token punctuation">(</span>X_test_scaled<span class="token punctuation">)</span>    mse <span class="token operator">=</span> mean_squared_error<span class="token punctuation">(</span>y_test<span class="token punctuation">,</span> y_pred<span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span>name<span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"MSE:"</span><span class="token punctuation">,</span> mse<span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"weights:"</span><span class="token punctuation">,</span> model<span class="token punctuation">.</span>coef_<span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre><p>运行后可以观察：</p><ul><li>Ridge 的权重通常会整体变小。</li><li>Lasso 可能会把一些权重压成 0。</li><li>普通线性回归没有额外限制，参数可能更不稳定。</li></ul><h2 id="16-Python-实战：逻辑回归正则化强度"><a href="#16-Python-实战：逻辑回归正则化强度" class="headerlink" title="16. Python 实战：逻辑回归正则化强度"></a>16. Python 实战：逻辑回归正则化强度</h2><p>在 scikit-learn 的 <code>LogisticRegression</code> 中，参数 <code>C</code> 控制正则化强度。</p><p>注意：<code>C</code> 是正则化强度的倒数。</p><pre class="language-text" data-language="text"><code class="language-text">C 越小，正则化越强C 越大，正则化越弱</code></pre><pre class="language-python" data-language="python"><code class="language-python"><span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>datasets <span class="token keyword">import</span> load_breast_cancer<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>linear_model <span class="token keyword">import</span> LogisticRegression<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>metrics <span class="token keyword">import</span> accuracy_score<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>model_selection <span class="token keyword">import</span> train_test_split<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>pipeline <span class="token keyword">import</span> Pipeline<span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>preprocessing <span class="token keyword">import</span> StandardScalerdata <span class="token operator">=</span> load_breast_cancer<span class="token punctuation">(</span><span class="token punctuation">)</span>X <span class="token operator">=</span> data<span class="token punctuation">.</span>datay <span class="token operator">=</span> data<span class="token punctuation">.</span>targetX_train<span class="token punctuation">,</span> X_test<span class="token punctuation">,</span> y_train<span class="token punctuation">,</span> y_test <span class="token operator">=</span> train_test_split<span class="token punctuation">(</span>    X<span class="token punctuation">,</span>    y<span class="token punctuation">,</span>    test_size<span class="token operator">=</span><span class="token number">0.2</span><span class="token punctuation">,</span>    random_state<span class="token operator">=</span><span class="token number">42</span><span class="token punctuation">,</span>    stratify<span class="token operator">=</span>y<span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token keyword">for</span> C <span class="token keyword">in</span> <span class="token punctuation">[</span><span class="token number">0.01</span><span class="token punctuation">,</span> <span class="token number">0.1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">10</span><span class="token punctuation">,</span> <span class="token number">100</span><span class="token punctuation">]</span><span class="token punctuation">:</span>    pipe <span class="token operator">=</span> Pipeline<span class="token punctuation">(</span><span class="token punctuation">[</span>        <span class="token punctuation">(</span><span class="token string">"scaler"</span><span class="token punctuation">,</span> StandardScaler<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">,</span>        <span class="token punctuation">(</span><span class="token string">"model"</span><span class="token punctuation">,</span> LogisticRegression<span class="token punctuation">(</span>C<span class="token operator">=</span>C<span class="token punctuation">,</span> max_iter<span class="token operator">=</span><span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">,</span>    <span class="token punctuation">]</span><span class="token punctuation">)</span>    pipe<span class="token punctuation">.</span>fit<span class="token punctuation">(</span>X_train<span class="token punctuation">,</span> y_train<span class="token punctuation">)</span>    y_pred <span class="token operator">=</span> pipe<span class="token punctuation">.</span>predict<span class="token punctuation">(</span>X_test<span class="token punctuation">)</span>    <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"C ="</span><span class="token punctuation">,</span> C<span class="token punctuation">,</span> <span class="token string">"accuracy ="</span><span class="token punctuation">,</span> accuracy_score<span class="token punctuation">(</span>y_test<span class="token punctuation">,</span> y_pred<span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre><p>如果后续配合 <code>GridSearchCV</code>，就可以系统选择更合适的 <code>C</code>。</p><h2 id="17-实践建议"><a href="#17-实践建议" class="headerlink" title="17. 实践建议"></a>17. 实践建议</h2><p>使用正则化时，可以记住这些经验：</p><ol><li>先划分训练集、验证集、测试集，不要只看训练集误差。</li><li>线性模型、逻辑回归、L1&#x2F;L2 正则化前通常需要标准化特征。</li><li>L2 更常用于稳定模型，L1 更常用于特征选择。</li><li>正则化强度太弱，可能仍然过拟合。</li><li>正则化强度太强，可能导致欠拟合。</li><li>用交叉验证选择正则化强度，不要凭感觉乱选。</li><li>对决策树、GBDT 这类模型，也要通过深度、叶子样本数、学习率等方式控制复杂度。</li></ol><h2 id="18-总结"><a href="#18-总结" class="headerlink" title="18. 总结"></a>18. 总结</h2><p>过拟合的本质是：模型在训练集上学得太细，把噪声和偶然规律也记住了。</p><p>正则化的本质是：在追求训练误差小的同时，限制模型复杂度，让模型更稳。</p><p>可以把这篇文章和前面的机器学习文章串起来看：</p><ul><li>线性回归里，Ridge 和 Lasso 是正则化版本。</li><li>逻辑回归里，正则化可以防止权重过大。</li><li>决策树里，限制深度和剪枝是在控制复杂度。</li><li>交叉验证可以帮助发现过拟合和选择正则化强度。</li><li>GBDT 里，学习率、树深度、早停也都带有正则化思想。</li></ul><p>理解正则化之后，再进入深度学习会顺很多。因为神经网络参数更多、表达能力更强，控制过拟合会变得更加重要。</p>]]>
    </content>
    <id>https://lsworl.github.io/2026/06/04/machine-learning-regularization-overfitting/</id>
    <link href="https://lsworl.github.io/2026/06/04/machine-learning-regularization-overfitting/"/>
    <published>2026-06-03T16:00:00.000Z</published>
    <summary>从欠拟合、过拟合、偏差方差、L1/L2 正则化、Ridge、Lasso、逻辑回归正则化、决策树剪枝和深度学习过渡几个角度理解正则化。</summary>
    <title>正则化与过拟合：让模型不只记住训练集</title>
    <updated>2026-06-04T12:14:20.000Z</updated>
  </entry>
</feed>
